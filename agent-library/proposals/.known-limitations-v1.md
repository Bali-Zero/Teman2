# Known limitations v1 (carry-over from spec + 2026-05-18 panel review)

These are intentional Phase 0/Phase 1 limitations documented for review.
Each must be addressed by an explicit task in Phase 1 before the loop
runs on production data.

## Carry-over from v4 Codex panel review (spec §"Known limitations v1")

| #   | Limitation                                                                                                  | Severity | Phase that addresses it                                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| L1  | `openai-codex-sdk` still in upstream pyproject (used by EvoSkill internals even if we don't call Codex CLI) | LOW      | Phase 1 vendor refresh — already removed in PR #721 commit `e2c834776` per UPSTREAM.md §1                       |
| L2  | Budget cap is per-iter NOT cumulative across iters                                                          | MEDIUM   | Phase 1: add cumulative `BUDGET_TOTAL_USD=$RUN_COST_SUM` tracking; smoke test multi-iter run                    |
| L3  | FTS5 BM25 threshold `< 1.5` not calibrated on real corpus                                                   | MEDIUM   | Phase 1 smoke: run dedup on 50 synthetic candidates vs current 02/03, measure false reject rate, tune threshold |
| L4  | Doppia redazione idempotency not tested                                                                     | LOW      | Phase 1: add unit test `test_redact_pii_idempotent` (apply twice → same output)                                 |
| L5  | Telemetry async timing could trigger false fail-closed                                                      | LOW      | Phase 1: add 2s sleep + retry-once before abort (Gemini v4 observation)                                         |

## New from 2026-05-18 3-LLM panel review (PR #721 round 2)

| #   | Limitation                                                                                            | Severity | Phase that addresses it                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L6  | **Budget cap wrapper-loop vs evoskill internal T mismatch** (Gemini HIGH)                             | HIGH     | Phase 1 must choose: (a) patch EvoSkill upstream to support `--max-iterations 1 --resume` so bash can iterate; (b) document as known-limit "BUDGET_USD enforced POST-run only via final telemetry.json check, not per-iter". Recommend (b) for Phase 1, since (a) requires upstream contribution + re-vendor. The wrapper script `scripts/agent-library-evolver-run.sh` Phase 0 already uses a no-op skeleton path — the per-iter check it documents is aspirational until (a) lands. |
| L7  | **DeepSeek harness Phase 0 stub** (Codex+Gemini CRITICAL)                                             | HIGH     | Phase 1: implement actual `vendor/evoskill/src/harness/deepseek/{executor,options}.py` with HTTP POST to api.deepseek.com/v1/chat/completions, BUDGET_USD enforcement via response.usage.total_cost_usd, exponential backoff retry, response_format JSON schema validation via Pydantic. PR #721 ships only the stub that raises `NotImplementedError`.                                                                                                                               |
| L8  | **Redaction patterns use 2-pass logic with placeholder regexes** (Codex CRITICAL via PR #721 round 2) | HIGH     | Phase 1: implement `scripts/_redact_pii.py` 2-pass logic where: pass 1 applies team_emails + antonello_personal_email + osint_internal_block; pass 2 applies generic_email_non_team (which would otherwise match team emails); pass 3 applies NPWP-bare context-aware substitution on captured groups. Yaml `replace: '\g<0>'` is a dry-run placeholder — actual substitution lives in the Python module.                                                                             |
| L9  | **`scorer.model` default `claude-sonnet-4-6` triggers anthropic raise** (DeepSeek MEDIUM)             | MEDIUM   | Phase 1: override `cfg.scorer.model` default to `"deepseek-v4-pro"` in evolver.toml `[scorer]` section; OR patch `src/cli/shared.py:make_scorer` to default to `"deepseek-v4-pro"` when in vendor strip mode. The current default would raise ImportError on first scorer call — load-bearing but not Phase 0 path.                                                                                                                                                                   |
| L10 | **`evidence_rules.yaml file_line_ref` regex misses extensionless files** (Gemini MEDIUM)              | MEDIUM   | Phase 1: change `\.[a-zA-Z0-9]+` to `(?:\.[a-zA-Z0-9]+)?` in `agent-library/config/evidence-rules.yaml` so `Makefile:12` and `Dockerfile:5` are valid citations.                                                                                                                                                                                                                                                                                                                      |
| L11 | **Telemetry default `/tmp/agent-library-evolver` weaker than launchd path** (Codex MEDIUM)            | LOW      | Phase 1: change default in `scripts/agent-library-evolver-run.sh` from `/tmp/agent-library-evolver` to `~/.agent/decisions/agent-library-evolver/telemetry` with `mkdir -m 0700`. The LaunchAgent plist already overrides correctly; only the manual-run path is weaker.                                                                                                                                                                                                              |

## How Phase 1 should consume this file

The Phase 1 wiring PR (separate from #721) must:

1. Open a checklist issue covering L6-L11 plus any new findings from
   Phase 1 panel re-review.
2. Mark each item completed/wontfix with rationale in this file before
   merging Phase 1.
3. Re-run the 3-LLM panel against the Phase 1 diff before merge.

Antonello signs off on each row.
