Use scoped searches and bounded reads for indexed documentation and ledgers. Start with filenames, headings, or rg matches, then read the sections required by the task and applicable rules. Do not load an entire large ledger, inventory, or manual solely because another file references it. Return relevant excerpts and concise summaries from tools; if output is truncated, narrow the query and reread the missing evidence. Keep routine answers concise while retaining failures, verification results, and detail the user requests. Reuse completed checks until changed code or state warrants rerunning them. Honor required independent reviews and explicit model or effort assignments. Delegate only when the user or applicable instructions request it and independent work justifies coordination; keep small coupled tasks local unless delegation is explicitly required. For routine delegated work without an explicit seat, model, effort, or specialist assignment, use routine-explorer for bounded read-only evidence gathering (Terra/medium), routine-worker for isolated implementation (Terra/medium), mechanical for deterministic checks and transformations (Luna/low), and code-reviewer for an advisory code review (Sol/high). These roles pin model and effort: use another appropriate role when an explicit assignment differs. They never replace required cross-family reviews or mission-colour final gates. Prefer fork_turns=none for self-contained tasks; include history only when needed. Each brief must specify objective, input paths and schema when relevant, owned files or read-only scope, assigned worktree path for repository edits, expected output, verification, and stop condition. Tell writers that other agents share the workspace and that they must not revert others' work. Ask for compact evidence and summaries, normally at most 500 words unless the task needs more. Reuse an existing child for related follow-ups; do not create duplicate explorers. In read-only shell tasks use python3 -c or node -e instead of heredocs. Process large inputs into aggregates or selected excerpts before returning tool output; do not print whole datasets. Read-only role instructions define task scope; live parent permission overrides may supersede role sandbox defaults. For enforced filesystem isolation, launch a separate review session with --sandbox read-only. Use the native spawn parameter agent_type, never role, and message, never brief; these names also apply to multi_agent_v1__spawn_agent. Discover tool names narrowly and inspect only the needed tool description, never dump a broad catalog. For routine code-mode exec output, explicitly use the first-line pragma // @exec: {"max_output_tokens": 1000}; increase it only when necessary to preserve required evidence. The generic tool_output_token_limit is not a universal cap on code-mode output. Every routine child brief must explicitly include its code-mode output pragma: mechanical 500, routine-explorer and routine-worker 1000, code-reviewer 1500 max_output_tokens. When supplying a code example, put that pragma on its first line; merely naming a role is insufficient to guarantee output bounding.