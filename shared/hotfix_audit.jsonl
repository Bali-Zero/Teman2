{"ts":"2026-09-20T07:48:57Z","session":"unknown","cmd":"python3 - <<'PY'\nimport pathlib\np = pathlib.Path(\".claude/skills/modus/PENDING-ARMS.md\")\ns = p.read_text()\nrow_secret = (\n    \"\\n- opened 2026-09-20 (GARUDA VOA / notifications, M5 — found while re-arming an exhausted outbox row, measured in-turn) \"\n    \"| **garuda-telegram-bot-token-on-fly-has-been-401-since-may — the Fly app and the rest of the fleet talk to TWO DIFFERENT BOTS, and the one production uses is revoked.** \"\n    \"`nuzantara-rag` carries `TELEGRAM_BOT_TOKEN` for bot id `8295471667`; `getMe` from inside the machine answers **HTTP 401**. This is not new: \"\n    \"`research/operations/2026-05-28-zero-point-recovery.md` already recorded the same id as 401 on 2026-05-28 (~4 months), with the username never established \"\n    \"(the document writes `NUZANTARA_HRF_BOT?`, question mark included). Pro and Mini instead hold bot `8918884141` = `@zantara0bot`, whose token is ALIVE (`getMe ok=true`). \"\n    \"Consequence: the six staff pages and both GARUDA alarms are silent, including the alarm that would shout that a job is exhausted — it reads the same variable. \"\n    \"**BotFather is not required for the cure:** `@zantara0bot` already reaches BOTH chats Fly is configured for — `TELEGRAM_APPROVAL_CHAT_ID` has the same sha256 on Fly and on Pro, and `TELEGRAM_ADMIN_CHAT_ID` answers `getChat` with `ok:true` (a private chat). \"\n    \"| missing arming step: Zero picks ONE — (a) align Fly to the live bot, `ssh pro 'set -a; . ~/.nuzantara-secrets.env; set +a; fly secrets set TELEGRAM_BOT_TOKEN=\\\"$TELEGRAM_BOT_TOKEN\\\" -a nuzantara-rag'` (the value never passes through a transcript), or (b) rotate `8295471667` on BotFather, where it is the bot whose token starts with that id; then I re-arm the exhausted row (see the row below). \"\n    \"| owner: operator[secret] (Zero) — a credential, his by rule \"\n    \"| proof-of-armed: from the machine, `curl -s -o /dev/null -w %{http_code} https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/getMe` returns **200** instead of 401, and the next `staff_page_charge_without_webhook` row leaves `garuda_order_outbox` with `dispatched_at` set\\n\"\n)\nrow_debt = (\n    \"\\n- opened 2026-09-20 (GARUDA VOA outbox, M5 — the class the revoked token made visible) \"\n    \"| **garuda-outbox-spends-a-money-page-on-a-transport-failure — an exhausted CHANNEL is charged to the ROW, so a page dies permanently for an outage that is not its fault.** \"\n    \"Live in PROD right now: `garuda_order_outbox` id=36, `staff_page_charge_without_webhook`, `attempts=5`, `dispatched_at IS NULL` — a money-anomaly page for a charge with no webhook, burnt against a Telegram token that answers 401 to every caller. \"\n    \"`outbox_consumer.py`'s claim is `attempts < DEFAULT_MAX_ATTEMPTS` (5), and nothing distinguishes a POISONED job (a payload no handler can ever route — the case a retry limit exists for) from an UNREACHABLE CHANNEL (401/5xx/DNS — the same for every row in the queue, and fixed in one gesture elsewhere). \"\n    \"The module beside it already names the shape in its own header: \\\"A brief provider outage (a Telegram 5xx, an expired token, a DNS blip) is therefore enough to exhaust a money-anomaly page permanently, and nothing anywhere said so\\\" — written 2026-08-28, and PROD now carries the row that proves it. \"\n    \"| missing arming step: one backend-rag PR where a delivery failure attributable to the CHANNEL does not consume the row's attempt budget (or restores it when the channel returns), with the guilt test that today's code fails — a 401 on the send path must leave `attempts` unchanged while a genuinely unroutable payload still exhausts; plus the one-off `UPDATE garuda_order_outbox SET attempts = 0 WHERE id = 36` once the token above is alive, which is mine and takes seconds. \"\n    \"| owner: me (sessione M5, backend-rag GARUDA lane) for the code; the re-arm waits on operator[secret] above (deadline 2026-10-04) \"\n    \"| proof-of-armed: on origin/main a test drives a send that fails with 401 and asserts the row keeps its attempt budget, and fails when the distinction is reverted; in PROD, row 36 shows `dispatched_at` set after the channel is restored\\n\"\n)\np.write_text(s.rstrip(\"\\n\") + \"\\n\" + row_secret + row_debt)\nprint(\"2 righe aperte\")\nPY\npython3 scripts/pending_arms_report.py 2>&1 | grep -c \"garuda-telegram-bot-token-on-fly\\|garuda-outbox-spends-a-money-page\"; python3 scripts/pending_arms_report.py 2>&1 | grep -i \"malformed\\|phantom\" | head -3","cwd":"/Users/balizero/nuzantara/.worktrees/docs-garuda-telegram-token-row-20260920","follow_up_pr":null}
