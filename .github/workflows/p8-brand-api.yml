name: P8 Brand-API Gate

# FASE-5 (P8 safe slice) — verifies the brand-api build-artifact is in sync
# with the .tsx sources (gate G6) and the token-lint catches hardcoded colors
# (gate G2). Pure-Python, zero network, zero PII.
#
# This workflow is the REAL consumer of scripts/brand_api_gen.py and
# scripts/brand_token_lint.py — it names them explicitly so the meta-verifier
# (scripts/verify_the_verifiers.py, kind=lint_script) sees the gate ARMED.

# NOTE (BUCO #1 skip→success sentinel, 2026-06-11): top-level pull_request
# `paths:` filter REMOVED so this can become a required-status-check without
# the pending-forever trap (W69). The relevant-paths decision lives in the
# "Did relevant paths change?" step; on a path-miss PR the job ENDS success.
on:
  pull_request: # NO top-level paths: — always trigger (sentinel gates the work)
  merge_group:
  workflow_dispatch:

# Dependabot branch: ALL Dependabot PRs share one group per workflow (not
# per-run), bounding concurrent Dependabot runs of THIS workflow to <=2
# (Agent PR Contract rule 5) instead of unbounded parallel.
# cancel-in-progress:false protects whichever run is RUNNING; GitHub keeps
# only one PENDING per group, so a 3rd+ near-simultaneous arrival cancels the
# pending run rather than queuing behind it — visible in the PR's checks tab
# (never silent), recoverable with `gh run rerun` (confirmed:
# docs.github.com/en/actions/using-jobs/using-concurrency + kimi-code/k3
# refuter round on PR #5043). Non-Dependabot branch is suffixed by
# github.run_id, which is unique per run and therefore never collides — the
# prior no-concurrency-block behavior (every run fully independent) is
# preserved exactly.
concurrency:
  group: ${{ github.actor == 'dependabot[bot]' && format('dependabot-lockfile-{0}', github.workflow) || format('p8-brand-api-{0}-{1}', github.ref, github.run_id) }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  brand-api-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # full history so the sentinel git diff sees PR changes
          # blob:none — partial clone: every commit and tree, no file contents (fetched on
          # demand). `fetch-depth: 0` is KEPT, so this gate's `git diff --name-only` resolves
          # exactly as before; only blob DELIVERY becomes lazy. Measured on this repository:
          # a full clone moves 2,289 MB, blob:none moves 23.3 MB in 21s.
          # Not a tidy-up: on 2026-07-27 this same checkout ate 297s of a 300s job budget in
          # adversarial-review-gate and 597s of 600s in P6 and was KILLED, so those gates never
          # ran. A timeout kill reports `cancelled`, and a cancelled REQUIRED check EJECTS the
          # merge-queue entry, which then sits open with auto-merge off. See #3375 for the
          # measurement and for why the flag's presence in the fetch log — not a green check —
          # is the proof (an unsupported input is silently ignored by Actions).
          filter: blob:none

      # BUCO #1 sentinel (pattern: p1s2-mutation-incremental.yml). The regex is
      # the EXACT original pull_request `paths:` list, each anchored ^...$.
      - name: Did relevant paths change?
        id: relevant
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "::notice::non-PR event (${{ github.event_name }}) — running full gate"
            exit 0
          fi
          base="${{ github.event.pull_request.base.sha }}"
          if git diff --name-only "$base"...HEAD | grep -qE \
            '^(scripts/brand_api_gen\.py|scripts/brand_token_lint\.py|scripts/test_brand_api\.py|packages/core/components/.*|packages/design-system/brand-api/.*|docs/design/components-catalog\.md|\.github/workflows/p8-brand-api\.yml)$'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "::notice::no relevant paths changed — sentinel success"
          fi

      - uses: actions/setup-python@v7
        if: steps.relevant.outputs.run == 'true'
        with:
          python-version: "3.11"

      - name: Install pytest
        if: steps.relevant.outputs.run == 'true'
        run: pip install --quiet pytest

      # G6 — the committed components.json + catalog MUST match a fresh
      # generation. If a .tsx changed but brand_api_gen.py was not re-run,
      # this fails (artifact drift caught at PR time, not at runtime).
      - name: brand_api_gen --check (G6 idempotence)
        if: steps.relevant.outputs.run == 'true'
        run: python3 scripts/brand_api_gen.py --check

      # G2 — token-lint self-test + scope scan: no hardcoded color literals
      # in the generated surface.
      - name: brand_token_lint self-test (G2)
        if: steps.relevant.outputs.run == 'true'
        run: python3 scripts/brand_token_lint.py --self-test

      - name: brand_token_lint scope scan (G2)
        if: steps.relevant.outputs.run == 'true'
        run: python3 scripts/brand_token_lint.py

      # Falsifiable gate tests — exercises brand_api_gen.py + brand_token_lint.py
      - name: pytest gate (scripts/test_brand_api.py)
        if: steps.relevant.outputs.run == 'true'
        run: PYTHONPATH=scripts python3 -m pytest scripts/test_brand_api.py -q

      # syntax smoke — fail fast if either script does not even parse.
      - name: py_compile brand_api_gen.py + brand_token_lint.py
        if: steps.relevant.outputs.run == 'true'
        run: |
          python3 -m py_compile scripts/brand_api_gen.py
          python3 -m py_compile scripts/brand_token_lint.py
          python3 -m py_compile scripts/test_brand_api.py
