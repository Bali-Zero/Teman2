// GUILT (grader round 1, HIGH): the legacy `users`/`ports` spelling of `src`/`dst`, still
// honoured by Tailscale and invisible to an auditor that reads only the modern field names.
// SHELL-ROUTE: /term -> 127.0.0.1:7681 is ttyd -W zsh.
{
  "hosts": {"pro": "100.107.22.111", "m5": "100.110.186.116", "mini": "100.93.236.6"},
  "tagOwners": {"tag:team-device": ["autogroup:admin"]},
  "acls": [
    {"action": "accept", "proto": "tcp", "users": ["tag:team-device"], "ports": ["pro:22"]}
  ],
  "ssh": [],
  "tests": [{"src": "tag:team-device", "deny": ["pro:443"]}]
}
