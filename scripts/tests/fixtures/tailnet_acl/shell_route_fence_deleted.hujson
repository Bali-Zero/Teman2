// GUILT: the SHELL-ROUTE banner's CLOSING fence is deleted. The previous check inferred the
// block's end from that decorative fence and, finding none, ran to end-of-file — so the body
// could be gutted and the required tokens parked anywhere below, including inside the JSON, and
// the guard returned green. That is a FALSE GREEN in the very check written to prevent one.
// An unterminated block is malformed, not permissive.
// ============================================================================
// SHELL-ROUTE: the reason this file exists. Do not delete this block.
// ============================================================================
// (body gutted; no closing fence follows, so the span has no structural end)
{
  "hosts": {
    "pro":              "100.107.22.111",
    "mini":             "100.93.236.6",
    "m5":               "100.110.186.116",
    "iphone-14":        "100.113.83.92",
    "iphone175":        "100.77.16.7",
    "apple-vision-pro": "100.97.28.18"
  },
  "acls": [
    {"action": "accept", "proto": "tcp", "src": ["m5"], "dst": ["pro:443"]}
  ],
  "ssh": [],
  "tests": [
    {"src": "m5", "accept": ["pro:443"]},
    {"src": "tag:team-device", "deny": ["pro:443"]}
  ],
  "//": "tokens parked outside the block, which used to be enough: 7681 /term ttyd"
}
