// GUILT (round-2 gate, finding C): an autogroup as an ordinary ACL destination — which current
// Tailscale syntax permits, as this policy's own corrected comment says. If that correction is
// true, this is a live bypass; the guard must not depend on the claim being false.
// SHELL-ROUTE: /term -> 127.0.0.1:7681 is ttyd -W zsh.
{
  "hosts": {"pro": "100.107.22.111", "mini": "100.93.236.6"},
  "tagOwners": {"tag:team-device": ["autogroup:admin"]},
  "acls": [
    {"action": "accept", "proto": "tcp", "src": ["mini"], "dst": ["autogroup:member:443"]}
  ],
  "ssh": [],
  "tests": [{"src": "tag:team-device", "deny": ["pro:443"]}]
}
