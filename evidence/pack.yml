brief_ref: evidence/brief.yml
pii_scan: clean
plan_ref: "garuda-maxstay-shared-engine — promote the B1 max-total-stay boundary guard (constants.b1_max_total_stay_exceeded, PR #4685) from the owner-local internal_preview_cli (its sole caller) into the shared services.garuda_flow.intake.build_verdict() engine, as a DECLINE + new neutral DeclineCode rather than a bare error, so a future public-funnel restore cannot silently reintroduce the 61-day-ACCEPT bug on the client-facing surface"
gear: 2
lanes:
  - lane: B1
    role: build
    seat: "sonnet-5 (session, mini-pro2)"
    note: "Re-verified the single-caller grep, read intake.py/eligibility.py/safe_clock.py/internal_preview_cli.py to confirm the promotion pattern already used for EXTENSION_ALREADY_USED and the issuance submission-window gate, wrote the GUILT test first and captured it RED against unmodified source, then added DeclineCode.EXTENSION_EXCEEDS_MAX_STAY (eligibility.py), layered the guard into build_verdict() reusing StayWindow.max_total_days (intake.py), fixed one pre-existing test fixture the new (correct) guard incidentally caught, and added a wire-safety proof mirroring the file's existing pattern."
seat_diversity: satisfied
seat_diversity_note: "Single build lane (fewer than 2), so the >=2-build-lane non-Anthropic-seat rule does not fire on this pack. No separate review lane was dispatched by this BUILD lane itself — verification is the RED/GREEN test discipline (receipts below) plus the repo's automated required CI gates (Gear floor, actionlint, RAG data-invariant tripwires, test suite) that gate the merge queue independent of this pack."
diff:
  net_lines: "+141 / -9 across 4 files (eligibility.py +11/-0, intake.py +48/-8, test_garuda_voa_no_internal_leak.py +27/-0, test_intake.py +55/-1). Measured with `git diff --numstat` against merge-base 1c647c020e5bc1bab0aba8df482a1d3fd0e66a16."
  behaviour_change: "New DeclineCode.EXTENSION_EXCEEDS_MAX_STAY member (additive to the enum). build_verdict() now DECLINEs an EXTENSION case whose printed voa_expiry_date implies a day-difference >= the B1 legal max (60), where before it silently ACCEPTed. Unreachable in production today: the only caller close enough to reach this path (internal_preview_cli) already raises PreviewInputError earlier in its own validation and never calls build_verdict() with such an input, and the public VOA funnel that would call build_verdict() directly is retired (PR #4344). compute_stay() was reordered earlier inside build_verdict() (pure function, no behavioural effect) so the new guard can reuse StayWindow.max_total_days instead of re-deriving the VISA_META arithmetic a second time."
receipts:
  - claim: "Re-verified PR #4685's guard has exactly ONE caller before building anything on that premise"
    cmd: "grep -rn 'b1_max_total_stay_exceeded|max_total_stay' apps/backend-rag/backend/services/garuda_flow/ apps/backend-rag/backend/tools/internal_preview_cli.py"
    exit: 0
    ts: "2026-08-23T15:00Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "Baseline suite is 164 passed, 0 failed on unmodified source (matches the mandate's stated baseline exactly)"
    cmd: 'PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/ backend/tests/app/routers/test_garuda_voa.py backend/tests/app/routers/test_garuda_voa_no_internal_leak.py -o addopts="" -v --no-header'
    exit: 0
    ts: "2026-08-23T15:18Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "RED — the GUILT test (build_verdict on an EXTENSION with day-difference==60) fails against UNMODIFIED source: verdict.decision is ACCEPT, expected DECLINE. Captured before any fix was written, proving the test is not vacuous."
    cmd: 'PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/test_intake.py::TestB1MaxTotalStayBoundary -o addopts="" -v --no-header'
    exit: 1
    ts: "2026-08-23T15:35Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "GREEN — same 3 tests (guilt @ ==60 DECLINE, innocence @ ==59 ACCEPT, issuance no-op) pass after adding DeclineCode.EXTENSION_EXCEEDS_MAX_STAY + the layered guard in build_verdict()"
    cmd: 'PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/test_intake.py::TestB1MaxTotalStayBoundary -o addopts="" -v --no-header'
    exit: 0
    ts: "2026-08-23T15:47Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "Full required suite: 168 passed (baseline 164 + 4 new), 0 failed, after also correcting one pre-existing test fixture (2020-01-01 entry_date) the new guard correctly-but-incidentally caught"
    cmd: 'PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/ backend/tests/app/routers/test_garuda_voa.py backend/tests/app/routers/test_garuda_voa_no_internal_leak.py -o addopts="" -v --no-header'
    exit: 0
    ts: "2026-08-23T15:58Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "Broader sweep for collateral breakage (routers/, visa_engine/) is clean"
    cmd: 'PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/ backend/tests/app/routers/ backend/tests/services/visa_engine/ -o addopts="" -q --no-header'
    exit: 0
    ts: "2026-08-23T16:05Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "Import chain smoke test (dependencies.py is imported by every router) still clean"
    cmd: 'python -c "from backend.app.dependencies import get_current_user; print(''OK'')"'
    exit: 0
    ts: "2026-08-23T16:06Z"
    seat: "sonnet-5 (session, mini-pro2)"
  - claim: "ruff clean on every changed file"
    cmd: "ruff check backend/services/garuda_flow/ backend/tests/services/garuda_flow/ backend/tests/app/routers/test_garuda_voa_no_internal_leak.py"
    exit: 0
    ts: "2026-08-23T16:07Z"
    seat: "sonnet-5 (session, mini-pro2)"
dissent: []
