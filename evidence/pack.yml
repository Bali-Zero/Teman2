brief_ref: evidence/brief.yml
plan_ref: >-
  P04-D1 canonical OutcomeEvent (CONTRACTS.md section 16), PR #4653; evidence pack revision 4 of 4 (2026-08-23)
  — refreshes revision 3's numbers to HEAD 8f98e5de3 after merging origin/main (14332eb3e, which landed
  PR #4635's risk_reclassification_receipt/ sanitization_receipt kinds concurrently) into this branch
  a second time. The evidence-file collision (cicatrix-superscar family #9) recurred for the same structural
  reason as revision 1; this revision resolves it the same way (--ours on both evidence files) and additionally
  proves the 3 shared registration files (cli.py, models/__init__.py, schemas/__init__.py) lost nothing
  of origin/main's concurrent additions across the merge.
diff:
  net_lines: >-
    +2334/-190, 21 files, net +2144 -- measured fresh THIS turn by `git diff --numstat origin/main HEAD`
    (see receipts). Now measured directly against origin/main's tip (14332eb3e) rather than the original
    merge-base c4242cd444, because after this revision's merge commit origin/main IS an ancestor of HEAD
    -- `git diff origin/main HEAD` therefore shows exactly this PR's own unique contribution and correctly
    excludes every one of origin/main's own commits (both the ones already incorporated at the original
    merge-base and the new ones this merge just absorbed), with no manual subtraction needed. Same 21-file
    set as revision 3 (this PR never touched a governance-receipt file); the line-count delta from revision
    3's +2170/-114 is origin/main's own concurrent edits to the 3 shared registration files (cli.py/models/__init__.py/schemas/__init__.py
    gained entries for the 2 new kinds between the two merge-bases), not new work by this PR.
  behaviour_change: >-
    Adds one new contract kind end-to-end (model + schema + fixtures + tests + registration), PLUS as
    of ce82bb93e: (a) the model's metric_profile_ref/metric_result_ref joint-presence invariant is now
    also expressed in the generated JSON Schema as a bidirectional if/then (previously Python-only), and
    (b) the supersedes-chain fixture now actually resolves to Current through the real graph.py resolver
    (previously would have quarantined). Zero lines changed in any of the 13 already-registered kinds;
    zero production backend-rag service code touched (packages/research-os-core is a standalone library,
    not yet imported by apps/backend-rag/backend/ service code); primitives.py untouched throughout.
lanes:
  - lane: research-os-p04-d1-outcome-event
    role: build
    seat: claude
receipts:
  - claim: >-
      Merge-base of this branch against origin/main is c4242cd444e8396bb37e31fbd0c945c7b69c940a (#4637,
      MetricProfile/MetricResult) — confirmed this is the commit both evidence/brief.yml and the diff scope
      are measured against.
    cmd: git merge-base HEAD origin/main
    exit: 0
    ts: 2026-08-23T09:57Z
    seat: claude
  - claim: >-
      REVISION 1 measurement (superseded in file-count/line-count by the R2b receipt below, kept for history):
      at HEAD 03af27444 the diff between merge-base and HEAD touched exactly 19 files, all under outcome_event
      (model/schema/fixtures/test) plus the 3 registration files; net +1665/-0 lines.
    cmd: git diff --numstat c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD
    exit: 0
    ts: "2026-08-23T10:05:18Z"
    seat: claude
  - claim: >-
      REVISION 3 (current) measurement: at HEAD ce82bb93e the diff between merge-base and HEAD touches 21
      files (revision 1's 19, PLUS evidence/brief.yml and evidence/pack.yml themselves, which revision 1's
      own commit added to this branch's history); net +2170/-114 = +2056 lines.
    cmd: >-
      git diff --numstat c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD | awk '{a+=$1;d+=$2} END{print a"
      "d" "a-d}'
    exit: 0
    ts: "2026-08-23T10:22:50Z"
    seat: claude
  - claim: >-
      workflow_run.py (and the sibling verification_receipt.py/conductor_handoff.py files this branch's
      own local history at commit 8d5f6a057 also touched) is byte-identical between the merge-base and HEAD
      — that commit's content landed on main separately, so those files are correctly excluded from this
      PR's real net diff.
    cmd: >-
      git diff c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD -- packages/research-os-core/research_os/models/workflow_run.py
    exit: 0
    ts: 2026-08-23T09:58Z
    seat: claude
  - claim: >-
      cli.py, models/__init__.py, and schemas/__init__.py each gained exactly one import line and one dict/tuple
      entry for OutcomeEvent; no existing entry (including workflow_run, verification_receipt, metric_profile,
      metric_result) was removed or altered.
    cmd: >-
      git diff c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD -- packages/research-os-core/research_os/cli.py
      packages/research-os-core/research_os/models/__init__.py packages/research-os-core/research_os/schemas/__init__.py
    exit: 0
    ts: 2026-08-23T09:56Z
    seat: claude
  - claim: >-
      `python -m research_os.cli fixtures --check` passes over the FULL fixture tree (all 13 registered
      kinds, 102 fixtures total, including outcome_event's 13): {"checked": 102, "failures": [], "valid":
      true}.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m research_os.cli
      fixtures --check
    exit: 0
    ts: "2026-08-23T09:59:03Z"
    seat: claude
  - claim: >-
      REVISION 3 (current, re-run fresh at HEAD ce82bb93e): the full research_os unit test suite passes:
      232 passed (was 230 at revision 1 -- +2 for the two adversarial-review-fix tests), 1 skipped (pre-existing,
      unrelated to this PR -- node/prettier unavailable in this environment for a schema-regen-parity test),
      0 failed.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os -q --no-header
    exit: 0
    ts: "2026-08-23T10:22:58Z"
    seat: claude
  - claim: >-
      REVISION 3 (current): test_outcome_event.py in isolation: 13/13 tests pass (was 11/11 -- +2 for test_supersedes_fixture_is_current_in_the_real_graph_resolver
      and test_metric_profile_and_result_schema_and_model_agree_on_all_nine_cases).
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py -q --no-header
    exit: 0
    ts: "2026-08-23T10:23:04Z"
    seat: claude
  - claim: >-
      Fix 1 (graph resolver), both arms driven through the REAL select_current_member -- ARM 1 (fixed fixture,
      strictly-later recorded_at) returns Current(member=GraphMember(object_kind='outcome_event', object_id='b0000000-0000-4000-8000-000000000003',
      object_hash='88076246fd48090e64d637b957dcde91c605c0d4195d9a3d2f19500a3b28b32b', tenant='bali-zero',
      family_id='com.example.outcome_event.family_001', recorded_at=2026-04-15 00:05:02+00:00)); ARM 2 (reproduced
      original defect -- tied recorded_at) returns exactly Quarantined(reason_codes=('successor_recorded_at_not_later',)).
      Both printed directly, not inferred from the test's pass/fail alone.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py::test_supersedes_fixture_is_current_in_the_real_graph_resolver
      -q --no-header
    exit: 0
    ts: "2026-08-23T10:23:16Z"
    seat: claude
  - claim: >-
      Fix 2 (schema if/then), all 9 combinations of {absent, explicit null, present-with-value} x 2 fields
      (metric_profile_ref, metric_result_ref) -- schema verdict, model verdict, and an independently-derived
      expected verdict AGREE on all 9: absent/absent=ACCEPT, absent/null=ACCEPT, absent/value=REJECT, null/absent=ACCEPT,
      null/null=ACCEPT, null/value=REJECT, value/absent=REJECT, value/null=REJECT, value/value=ACCEPT. Run
      both as a standalone script (printed table) and as the permanent test below.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py::test_metric_profile_and_result_schema_and_model_agree_on_all_nine_cases
      -q --no-header
    exit: 0
    ts: "2026-08-23T10:23:17Z"
    seat: claude
  - claim: >-
      test_schemas.py, including test_checked_in_schemas_are_byte_identical_to_fresh_regeneration (the regenerated
      schema carrying the new if/then matches the checked-in file byte-for-byte, via the real schema_bytes()
      generator -- not hand-edited): 5 passed, 1 pre-existing skip.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_schemas.py -q --no-header
    exit: 0
    ts: "2026-08-23T10:23:05Z"
    seat: claude
  - claim: >-
      test_schemas.py (the schema self-consistency module: generated-schema Draft 2020-12 validity, ECMA-262
      pattern compilation, checked-in-vs-regenerated byte parity) passes: 5 passed, 1 skipped (same pre-existing
      node/prettier-unavailable skip).
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_schemas.py -v
    exit: 0
    ts: "2026-08-23T10:01:37Z"
    seat: claude
  - claim: >-
      REVISION 3 (current, re-run at HEAD ce82bb93e, after outcome_event.py's new ConfigDict(json_schema_extra=...)):
      mypy --strict on the 4 touched non-test Python files (outcome_event.py, cli.py, models/__init__.py,
      schemas/__init__.py) is STILL clean: 'Success: no issues found in 4 source files'.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy
      --strict research_os/models/outcome_event.py research_os/cli.py research_os/models/__init__.py research_os/schemas/__init__.py
      --explicit-package-bases
    exit: 0
    ts: "2026-08-23T10:23:11Z"
    seat: claude
  - claim: >-
      REVISION 3 (current, re-run at HEAD ce82bb93e): mypy --strict on the touched TEST file (test_outcome_event.py)
      is STILL RED: 1 error -- 'Unused "type: ignore" comment [unused-ignore]' -- now at line 73 (was line
      62 at revision 1, shifted down by the fix-3 glob assertions and new imports; unchanged in substance/root-cause).
      Left unfixed deliberately, per both the evidence-rewrite's and the adversarial-review-fix's stated
      scope. TRUE rc recorded, not swept.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy
      --strict ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py --explicit-package-bases
    exit: 1
    ts: "2026-08-23T10:23:11Z"
    seat: claude
  - claim: >-
      Control check, same invocation style, on an ALREADY-ON-MAIN sibling file (test_workflow_run.py) with
      the analogous frozen-field-reassignment test at the same line number: clean, 0 issues. Confirms the
      line-62 finding above is a genuine `[misc]` vs `[assignment]` discrepancy specific to outcome_type's
      non-Literal type, not a config/ environment artifact of how I invoked mypy (the same invocation correctly
      consumes the ignore comment on the sibling file).
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy
      --strict ../../apps/backend-rag/backend/tests/unit/research_os/test_workflow_run.py --explicit-package-bases
    exit: 0
    ts: "2026-08-23T10:05:18Z"
    seat: claude
  - claim: >-
      Neither the repo-root backend-rag mypy config (apps/backend-rag/pyproject.toml [tool.mypy]) nor CI's
      actual mypy invocation load the pydantic mypy plugin, and CI's mypy job never checks packages/research-os-core
      at all — it strictly checks only backend/core and backend/app/models. The line-62 finding above is
      therefore NOT CI-blocking.
    cmd: grep -n 'mypy' .github/workflows/tests.yml
    exit: 0
    ts: 2026-08-23T10:03Z
    seat: claude
  - claim: >-
      checked_in_schemas_match() and validate_schema_artifacts() both return the empty tuple at HEAD ce82bb93e
      -- the regenerated schema (carrying the new if/then) matches the checked-in file byte-for-byte and
      is valid Draft 2020-12, called directly (not via pytest).
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -c "import
      sys; sys.path.insert(0,'.'); from research_os.schemas import checked_in_schemas_match, validate_schema_artifacts;
      print(checked_in_schemas_match()); print(validate_schema_artifacts())"
    exit: 0
    ts: "2026-08-23T10:24:31Z"
    seat: claude
  - claim: >-
      REVISION 4: origin/main moved to 14332eb3e (PR #4635's risk_reclassification_receipt/ sanitization_receipt,
      plus unrelated PRs) since revision 3's merge-base. `git fetch origin main` then `git merge origin/main
      --no-edit` conflicted on exactly 2 files -- evidence/brief.yml and evidence/pack.yml (the same fixed-path
      collision as revision 1, cicatrix-superscar family #9) -- with the 3 shared registration files (cli.py,
      models/__init__.py, schemas/__init__.py) auto-merging cleanly, no conflict markers.
    cmd: git fetch origin main && git merge origin/main --no-edit
    exit: 1
    ts: >-
      2026-08-23T10:36Z (single bracketing capture for this whole revision-4 batch, per-command date not
      separately shelled; merge attempt itself intentionally exits 1 on conflict, resolved below)
    seat: claude
  - claim: >-
      Both evidence files resolved --ours (this branch's version wins, matching revision 1's established
      resolution pattern for this exact collision class); 0 conflict markers remain anywhere in the tree
      after resolution.
    cmd: >-
      git checkout --ours evidence/brief.yml evidence/pack.yml && git add evidence/brief.yml evidence/pack.yml
      && git status --porcelain=v1 | grep -E '^UU|^AA|^DD' | wc -l
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      LOSS-CHECK, non-zero hit counts, not a visual 'looks fine': origin/main BEFORE this merge had exactly
      14 kinds registered in each of the 3 shared files (action_intent, action_item, approval_receipt, conductor_handoff,
      execution_attempt, metric_profile, metric_result, object_successor_edge, operational_receipt, revocation_receipt,
      risk_reclassification_receipt, sanitization_receipt, verification_receipt, workflow_run) -- outcome_event
      present in NONE of them (0 hits, correctly, since it is this PR's own addition). Measured directly
      via `git show origin/main:<path>` (read-only, working tree untouched) AND independently corroborated
      by a dispatched subagent doing the same measurement blind -- both agree exactly: 14/14/14 kinds, 0/0/0
      outcome_event hits.
    cmd: >-
      git show origin/main:packages/research-os-core/research_os/cli.py | sed -n '/^CONTRACT_MODELS/,/^}/p'
      | grep -c '": '
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      LOSS-CHECK continued: the MERGED tree (post-resolution, pre-commit at that point) has exactly 15 entries
      in each of the 3 files -- origin/main's 14 PLUS outcome_event, nothing dropped. Non-zero grep hit-count
      grid proving the pattern is alive (not a broken 0-vs-0 false-green): cli.py outcome_event=2/risk_reclassification_receipt=2/sanitization_receipt=2;
      models/__init__.py outcome_event=1/risk_reclassification_receipt=1/sanitization_receipt=1 (CamelCase
      __all__ entries don't match the lowercase snake_case substring, so only the import-path line hits);
      schemas/__init__.py outcome_event=2/risk_reclassification_receipt=2/ sanitization_receipt=2. Every
      cell non-zero.
    cmd: >-
      for f in cli.py models/__init__.py schemas/__init__.py; do grep -c outcome_event $f; grep -c risk_reclassification_receipt
      $f; grep -c sanitization_receipt $f; done
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      Merge finalized (`git commit --no-edit`, merge commit 8f98e5de3, parents b9e777a64 and origin/main's
      14332eb3e). origin/main is now a strict ancestor of HEAD, confirmed via `git merge-base --is-ancestor
      origin/main HEAD` (exit 0).
    cmd: git commit --no-edit && git merge-base --is-ancestor origin/main HEAD
    exit: 0
    ts: "2026-08-23T10:36:37Z"
    seat: claude
  - claim: >-
      REVISION 4, re-run fresh on the merged+committed tree: `python -m research_os.cli fixtures --check`
      now covers 119 fixtures (was 102 pre-merge): {"checked": 119, "failures": [], "valid": true}. The
      +17 delta is fully reconciled in `static:` below via the tool's real counting unit (one valid_*/invalid_*.json
      per fixture, .expect.json siblings never counted) -- not a naive raw-file-count sum, which would have
      overshot to 127.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m research_os.cli
      fixtures --check
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      REVISION 4: the full research_os unit test suite on the merged tree passes: 266 passed (was 232 pre-merge,
      +34 for origin/main's own test_governance_receipts.py and related additions), 1 skipped (same pre-existing
      node/prettier-unavailable skip), 0 failed.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os -q --no-header
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      REVISION 4: test_outcome_event.py in isolation, unaffected by the merge (this branch's own file, no
      conflict): still 13/13 passed.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest
      ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py -q --no-header
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      REVISION 4: mypy --strict on the 4 functional files, re-verified post-merge, still clean -- 'Success:
      no issues found in 4 source files'. mypy --strict on test_outcome_event.py still red at the same finding,
      same line 73, unchanged in substance (merge did not touch this file) -- reported honestly again, not
      swept.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy
      --strict research_os/models/outcome_event.py research_os/cli.py research_os/models/__init__.py research_os/schemas/__init__.py
      --explicit-package-bases
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      REVISION 4: checked_in_schemas_match() and validate_schema_artifacts() both still -> () on the merged
      tree -- outcome_event.schema.json unaffected by origin/main's concurrent schema additions for the
      2 other kinds.
    cmd: >-
      cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -c "import
      sys; sys.path.insert(0,'.'); from research_os.schemas import checked_in_schemas_match, validate_schema_artifacts;
      print(checked_in_schemas_match()); print(validate_schema_artifacts())"
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      scripts/evidence_pack_lint.py itself was modified by this merge (origin/main's own edit, unrelated
      to this PR); confirmed the working-tree copy is byte-identical to origin/main:scripts/evidence_pack_lint.py
      (diff empty, rc 0) before using it -- no stale /tmp copy reused.
    cmd: >-
      diff <(git show origin/main:scripts/evidence_pack_lint.py) scripts/evidence_pack_lint.py
    exit: 0
    ts: 2026-08-23T10:36Z
    seat: claude
  - claim: >-
      HONEST DISCLOSURE, not a defect: the `origin/main` label is a moving ref and it moved a second time
      DURING this same revision's work -- from 14332eb3e (what was actually fetched and merged into this
      commit) to 8d2ed25f3 (one further commit, #4658, landed independently mid- session). A bare `git diff
      origin/main HEAD` re-run AFTER that second move momentarily appeared to show HEAD missing 2 lines
      of .claude/skills/modus/PENDING-ARMS.md that origin/ main's NEW tip has -- investigated fully rather
      than assumed benign: `git diff 14332eb3e HEAD -- .claude/skills/modus/PENDING-ARMS.md` is EMPTY (0
      lines, byte-identical) -- this branch never touched that file and the merge correctly reproduced 14332eb3e's
      content for it exactly. The 2 'missing' lines are the PENDING-ARMS row that #4658 itself closed/removed
      on origin/main AFTER 14332eb3e -- work this PR was never asked to (and did not) merge in. Every number
      in this pack's diff/receipts is pinned to 14332eb3e, the SHA actually fetched and merged, not to the
      live `origin/main` label -- re-running the bare-label commands today will correctly show a further
      1-commit delta that is out of this PR's scope.
    cmd: >-
      git diff 14332eb3e5a2b8ce45479ee66a8f77dd4313e56d HEAD -- .claude/skills/modus/PENDING-ARMS.md | wc
      -l
    exit: 0
    ts: "2026-08-23T10:43:26Z"
    seat: claude
tests:
  - >-
    apps/backend-rag/backend/tests/unit/research_os (full suite, 266 passed as of the revision-4 merge commit
    8f98e5de3 -- was 232 pre-merge, +34 from origin/main's own concurrent test_governance_receipts.py and
    related additions, none of it this PR's work -- /1 pre-existing skip) and the outcome_event/schemas
    subsets in isolation — all green per receipts above. This IS the applicable test surface for a contracts-package
    addition; there is no separate integration surface because packages/research-os-core is not yet wired
    into any backend-rag service.
  - >-
    The 2 adversarial-review-fix tests specifically both drive REAL production code paths, not reimplementations:
    test_supersedes_fixture_is_current_in_the_real_graph_resolver calls the actual graph.py::select_current_member;
    test_metric_profile_and_result_schema_and_model_agree_on_all_nine_cases validates against the actual
    regenerated outcome_event.schema.json via jsonschema's Draft202012Validator AND the actual OutcomeEvent.model_validate,
    comparing both to an independently-derived expected verdict (not schema==model, which would be an identity
    check on two things built from the same source, not a guard).
static:
  - >-
    python -m research_os.cli fixtures --check -> {"checked": 119, "failures": [], "valid": true} on the
    merged tree (was 102 pre-merge). Reconciled exactly, not left as an unexplained gap: the CLI's counting
    unit is one valid_*.json or invalid_*.json fixture file -- an invalid fixture's paired *.expect.json
    sibling is opened only to read its reason_code and is NEVER separately counted (read directly from _fixture_paths()/_check_fixtures()
    in cli.py, not inferred). risk_reclassification_receipt contributes 3 valid + 7 invalid = 10 checked
    units (from 17 files on disk, 7 of them .expect.json and correctly excluded); sanitization_receipt contributes
    3 valid + 4 invalid = 7 checked units (from 11 files on disk, 4 excluded). 10 + 7 = 17, and 102 + 17
    = 119 exactly -- no residual gap. A naive 102+13+12=127 (raw fixture-directory file counts including
    .expect.json) does not match the tool's real counting unit and was never the right arithmetic to check
    against.
  - >-
    mypy --strict on outcome_event.py/cli.py/models/__init__.py/schemas/__init__.py -> clean (re-verified
    at ce82bb93e, after the ConfigDict(json_schema_extra=...) addition)
  - >-
    mypy --strict on test_outcome_event.py -> 1 unused-ignore finding (line 73 as of ce82bb93e, was line
    62), reported honestly in risks: above, verified not CI-blocking, left unfixed across both the evidence-rewrite
    AND the adversarial-review-fix commit
  - >-
    checked_in_schemas_match() and validate_schema_artifacts() both -> () at ce82bb93e (the regenerated
    schema, carrying the new if/then, matches the checked-in file byte-for-byte and is valid Draft 2020-12)
runtime_proof:
  - >-
    Not applicable in the classic sense — this is a schema/contract library addition with no service runtime
    to exercise. The closest runtime proof is the full green test suite (receipts above), which exercises
    every validator branch the model defines (round-trip, each of the 5 named invalid-fixture rejection
    reasons, immutability, object_hash tamper-detection, and the joint-presence invariant) against real
    Pydantic validation, not mocked.
dissent:
  - seat: >-
      independent adversarial review (dispatched by the requesting session, cross-family corroborated, re-verified
      on disk by the requesting session before dispatch AND by this session before implementing each fix
      -- not accepted on report alone)
    objection: >-
      Returned DEFECTIVE against ce82bb93e's predecessor state, with 2 real findings against this PR's substance:
      (1) valid_with_supersedes_and_extension.json would be quarantined by the real graph.py resolver (byte-identical
      recorded_at to its predecessor); (2) the checked- in schema expressed zero if/then/allOf/oneOf/dependentRequired
      for the metric_profile_ref/ metric_result_ref joint-presence invariant the model enforces in Python.
      Also flagged 1 test- hardening gap (empty-glob hole in test_outcome_event.py) and reconfirmed 1 already-ledgered
      out-of-scope finding (primitives.py extension-jail omission, PENDING-ARMS, not fixed here per the
      Agent PR Contract's depth-1 fix-of-a-fix limit).
    status: CONFIRMED
residual_risks:
  - >-
    The 7 spec-interpretation judgment calls documented in outcome_event.py's own module docstring (revocation_receipt_ref
    gap vs section 3.2/13.5, value:Any, aggregation_level unconstrained, completeness unconstrained float,
    source_system registry-row gap, window-ordering-by-analogy, 2 invariants deliberately unenforced) are
    open and flagged for Packet 04 to rule on. Not a defect of this PR; a defect of leaving them silently
    resolved would have been worse, per the model's own docstring stance.
  - >-
    mypy --strict finds 1 unused-ignore in test_outcome_event.py, now line 73 (was line 62 before ce82bb93e's
    fix-3 assertions shifted it) -- `[misc]` where no plugin-aware invocation of this package exists to
    make it needed. Verified NOT CI-blocking (CI never mypy-checks this package). Left unfixed across every
    revision of this branch so far -- out of scope for the evidence-rewrite AND for the adversarial-review
    fixes.
  - >-
    The ledgered primitives.py extension-jail finding (V1_RESERVED_EXTENSION_FIELD_NAMES omits ~16 of OutcomeEvent's
    own field names, repo-wide, subject_refs missing too on already-shipped kinds) remains open and deliberately
    unfixed here -- PENDING-ARMS carries its proof-of-armed and the reason the obvious cure (adding the
    nested names) is wrong (jails ~23 bare common words, over-match). primitives.py is untouched by every
    commit on this branch.
  - >-
    This evidence pack itself was, until revision 1, describing foreign work (PR #4592) -- inherited unchanged
    from origin/main's evidence/*.yml at this branch's merge-base. This is a fleet-wide pattern (cicatrix-superscar
    family #9: evidence/brief.yml and evidence/pack.yml are fixed root-relative paths every Gear>=2 PR rewrites)
    actively tracked and mitigated across multiple concurrent P04-D1 lanes as of this timestamp, not unique
    to this PR. This revision (3) exists because revision 1's numbers drifted stale the moment revision
    2 (ce82bb93e) landed real code changes without a corresponding evidence update -- the same class of
    risk, caught and corrected within the same branch rather than left for a future reader.
sources:
  - >-
    packages/research-os-core/research_os/models/outcome_event.py (this PR's primary file, updated ce82bb93e)
  - >-
    packages/research-os-core/research_os/schemas/outcome_event.schema.json (generated, this PR, regenerated
    ce82bb93e)
  - >-
    packages/research-os-core/fixtures/outcome_event/*.json (13 fixtures, this PR; valid_with_supersedes_and_extension.json
    corrected ce82bb93e)
  - >-
    apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py (this PR, +2 tests and 2 glob-assertions
    ce82bb93e)
  - >-
    packages/research-os-core/research_os/cli.py, models/__init__.py, schemas/__init__.py (registration
    diffs, this PR, untouched by ce82bb93e)
  - >-
    packages/research-os-core/research_os/models/metric_result.py (read-only reference — the json_schema_extra/ConfigDict
    precedent this PR's fix 2 follows)
  - >-
    packages/research-os-core/research_os/graph.py, research_os/models/successor_edge.py (read-only reference
    — the real resolver this PR's fix 1 test drives)
  - .github/workflows/tests.yml:635 (read-only reference — confirms CI's mypy scope)
  - >-
    apps/backend-rag/pyproject.toml [tool.mypy] (read-only reference — confirms no pydantic plugin)
pii_scan: clean
size_tokens: 7279
