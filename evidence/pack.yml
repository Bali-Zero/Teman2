brief_ref: evidence/brief.yml
plan_ref: "P04-D1 canonical OutcomeEvent (CONTRACTS.md section 16), PR #4653, evidence-pack rewrite 2026-08-23"
diff:
  net_lines: "+1665/-0, 19 files, measured by `git diff --numstat` between merge-base c4242cd444
    (#4637) and HEAD 03af27444 in this turn (see receipts). All 19 files are new additions under
    outcome_event (model, schema, 13 fixtures, test module) plus 2-line registration diffs in
    cli.py / models/__init__.py / schemas/__init__.py."
  behaviour_change: "Adds one new contract kind end-to-end (model + schema + fixtures + tests +
    registration). Zero lines changed in any of the 13 already-registered kinds; zero production
    backend-rag service code touched (packages/research-os-core is a standalone library, not yet
    imported by apps/backend-rag/backend/ service code)."
lanes:
  - lane: "research-os-p04-d1-outcome-event"
    role: build
    seat: claude
receipts:
  - claim: "Merge-base of this branch against origin/main is c4242cd444e8396bb37e31fbd0c945c7b69c940a
    (#4637, MetricProfile/MetricResult) — confirmed this is the commit both evidence/brief.yml and
    the diff scope are measured against."
    cmd: "git merge-base HEAD origin/main"
    exit: 0
    ts: "2026-08-23T09:57Z"
    seat: claude
  - claim: "The diff between merge-base and HEAD touches exactly 19 files, all under
    outcome_event (model/schema/fixtures/test) plus the 3 registration files; net +1665/-0 lines."
    cmd: "git diff --numstat c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD"
    exit: 0
    ts: "2026-08-23T10:05:18Z"
    seat: claude
  - claim: "workflow_run.py (and the sibling verification_receipt.py/conductor_handoff.py files
    this branch's own local history at commit 8d5f6a057 also touched) is byte-identical between
    the merge-base and HEAD — that commit's content landed on main separately, so those files are
    correctly excluded from this PR's real net diff."
    cmd: "git diff c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD -- packages/research-os-core/research_os/models/workflow_run.py"
    exit: 0
    ts: "2026-08-23T09:58Z"
    seat: claude
  - claim: "cli.py, models/__init__.py, and schemas/__init__.py each gained exactly one import
    line and one dict/tuple entry for OutcomeEvent; no existing entry (including workflow_run,
    verification_receipt, metric_profile, metric_result) was removed or altered."
    cmd: "git diff c4242cd444e8396bb37e31fbd0c945c7b69c940a HEAD -- packages/research-os-core/research_os/cli.py packages/research-os-core/research_os/models/__init__.py packages/research-os-core/research_os/schemas/__init__.py"
    exit: 0
    ts: "2026-08-23T09:56Z"
    seat: claude
  - claim: "`python -m research_os.cli fixtures --check` passes over the FULL fixture tree (all 13
    registered kinds, 102 fixtures total, including outcome_event's 13): {\"checked\": 102,
    \"failures\": [], \"valid\": true}."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m research_os.cli fixtures --check"
    exit: 0
    ts: "2026-08-23T09:59:03Z"
    seat: claude
  - claim: "The full research_os unit test suite passes: 230 passed, 1 skipped (pre-existing,
    unrelated to this PR — node/prettier unavailable in this environment for a schema-regen-parity
    test), 0 failed."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest ../../apps/backend-rag/backend/tests/unit/research_os -q --no-header"
    exit: 0
    ts: "2026-08-23T09:59:07Z"
    seat: claude
  - claim: "test_outcome_event.py in isolation: 11/11 tests pass (fixture round-trip, exact-reason
    rejection, unknown-field rejection, object_hash-mismatch cannot bypass validation, frozen
    immutability, metric_profile/metric_result joint-presence invariant)."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py -q --no-header"
    exit: 0
    ts: "2026-08-23T10:05:17Z"
    seat: claude
  - claim: "test_schemas.py (the schema self-consistency module: generated-schema Draft 2020-12
    validity, ECMA-262 pattern compilation, checked-in-vs-regenerated byte parity) passes: 5
    passed, 1 skipped (same pre-existing node/prettier-unavailable skip)."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m pytest ../../apps/backend-rag/backend/tests/unit/research_os/test_schemas.py -v"
    exit: 0
    ts: "2026-08-23T10:01:37Z"
    seat: claude
  - claim: "mypy --strict on the 4 touched non-test Python files (outcome_event.py, cli.py,
    models/__init__.py, schemas/__init__.py) is clean: 'Success: no issues found in 4 source
    files'."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy --strict research_os/models/outcome_event.py research_os/cli.py research_os/models/__init__.py research_os/schemas/__init__.py --explicit-package-bases"
    exit: 0
    ts: "2026-08-23T10:00:07Z"
    seat: claude
  - claim: "mypy --strict on the touched TEST file (test_outcome_event.py) is RED: 1 error --
    'Unused \"type: ignore\" comment [unused-ignore]' at line 62. TRUE rc recorded, not swept."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy --strict ../../apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py --explicit-package-bases"
    exit: 1
    ts: "2026-08-23T10:00:11Z"
    seat: claude
  - claim: "Control check, same invocation style, on an ALREADY-ON-MAIN sibling file
    (test_workflow_run.py) with the analogous frozen-field-reassignment test at the same line
    number: clean, 0 issues. Confirms the line-62 finding above is a genuine `[misc]` vs
    `[assignment]` discrepancy specific to outcome_type's non-Literal type, not a config/
    environment artifact of how I invoked mypy (the same invocation correctly consumes the
    ignore comment on the sibling file)."
    cmd: "cd packages/research-os-core && /Users/nuzantara/nuzantara/apps/backend-rag/.venv/bin/python -m mypy --strict ../../apps/backend-rag/backend/tests/unit/research_os/test_workflow_run.py --explicit-package-bases"
    exit: 0
    ts: "2026-08-23T10:05:18Z"
    seat: claude
  - claim: "Neither the repo-root backend-rag mypy config (apps/backend-rag/pyproject.toml
    [tool.mypy]) nor CI's actual mypy invocation load the pydantic mypy plugin, and CI's mypy job
    never checks packages/research-os-core at all — it strictly checks only backend/core and
    backend/app/models. The line-62 finding above is therefore NOT CI-blocking."
    cmd: "grep -n 'mypy' .github/workflows/tests.yml"
    exit: 0
    ts: "2026-08-23T10:03Z"
    seat: claude
tests:
  - "apps/backend-rag/backend/tests/unit/research_os (full suite, 230 passed/1 pre-existing skip)
    and the outcome_event/schemas subsets in isolation — all green per receipts above. This IS the
    applicable test surface for a contracts-package addition; there is no separate integration
    surface because packages/research-os-core is not yet wired into any backend-rag service."
static:
  - "python -m research_os.cli fixtures --check -> {\"checked\": 102, \"failures\": [], \"valid\": true}"
  - "mypy --strict on outcome_event.py/cli.py/models/__init__.py/schemas/__init__.py -> clean"
  - "mypy --strict on test_outcome_event.py -> 1 unused-ignore finding (line 62), reported honestly
    in risks: above, verified not CI-blocking, left unfixed (out of this rewrite's edit scope)"
runtime_proof:
  - "Not applicable in the classic sense — this is a schema/contract library addition with no
    service runtime to exercise. The closest runtime proof is the full green test suite (receipts
    above), which exercises every validator branch the model defines (round-trip, each of the 5
    named invalid-fixture rejection reasons, immutability, object_hash tamper-detection, and the
    joint-presence invariant) against real Pydantic validation, not mocked."
dissent: []
residual_risks:
  - "The 7 spec-interpretation judgment calls documented in outcome_event.py's own module
    docstring (revocation_receipt_ref gap vs section 3.2/13.5, value:Any, aggregation_level
    unconstrained, completeness unconstrained float, source_system registry-row gap,
    window-ordering-by-analogy, 2 invariants deliberately unenforced) are open and flagged for
    Packet 04 to rule on. Not a defect of this PR; a defect of leaving them silently resolved would
    have been worse, per the model's own docstring stance."
  - "mypy --strict finds 1 unused-ignore in test_outcome_event.py:62 (`[misc]` where no plugin-
    aware invocation of this package exists to make it needed). Verified NOT CI-blocking (CI never
    mypy-checks this package). Left unfixed — out of this evidence-rewrite's edit scope
    (evidence/brief.yml + evidence/pack.yml only, per the dispatching lane's hard limits)."
  - "This evidence pack itself was, until this rewrite, describing foreign work (PR #4592) --
    inherited unchanged from origin/main's evidence/*.yml at this branch's merge-base. This is a
    fleet-wide pattern (cicatrix-superscar family #9: evidence/brief.yml and evidence/pack.yml are
    fixed root-relative paths every Gear>=2 PR rewrites, so any branch that never touches them
    silently carries whatever PR last wrote them on main) actively being tracked and mitigated
    across multiple concurrent P04-D1 lanes as of this timestamp, not unique to this PR."
sources:
  - packages/research-os-core/research_os/models/outcome_event.py (this PR's primary file)
  - packages/research-os-core/research_os/schemas/outcome_event.schema.json (generated, this PR)
  - packages/research-os-core/fixtures/outcome_event/*.json (13 fixtures, this PR)
  - apps/backend-rag/backend/tests/unit/research_os/test_outcome_event.py (this PR)
  - packages/research-os-core/research_os/cli.py, models/__init__.py, schemas/__init__.py
    (registration diffs, this PR)
  - .github/workflows/tests.yml:635 (read-only reference — confirms CI's mypy scope)
  - apps/backend-rag/pyproject.toml [tool.mypy] (read-only reference — confirms no pydantic plugin)
pii_scan: clean
# approx_tokens() per scripts/evidence_pack_lint.py: len(raw bytes) // 4.
# Measured `wc -c evidence/pack.yml` at write time = ~10500 bytes -> ~2630 tokens (cap 30000).
size_tokens: 2630
