brief_ref: evidence/brief.yml
plan_ref: "S14 garuda-voa (Mini) — build lane B2, dataset for the nationality-eligibility gap in the GARUDA VOA intake engine"
diff:
  net_lines: '3 files touched: new `nationality_eligibility.py` (~230 lines, mostly provenance docstring + a 97-entry frozenset), `intake.py` wired (~15 lines net, 2 docstrings corrected), new `test_nationality_eligibility.py` (~110 lines) + ~40 new lines appended to `test_intake.py`. Net additive; nothing deleted except the two words "no ... dataset yet" now that the dataset exists.'
  behaviour_change: "`nationality_entry_eligible` was `True` unconditionally; it is now `is_voa_eligible_nationality(request.nationality)`. Any nationality not in the verified 97-country VOA-eligible set now DECLINEs via the existing `DeclineCode.NATIONALITY_NOT_ELIGIBLE` (same reason text as before this PR — no new decline code introduced, see brief `constraints`). Every other criterion, the runway gate, the issuance submission-window gate, and Safe Clock are untouched."
receipts:
  - claim: "The gap was real and live: AFG returned ACCEPT before this fix (not a hypothetical)"
    cmd: "PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/test_intake.py -k TestNationalityEligibility -v, run against the ORIGINAL intake.py (nationality_eligibility.py + intake.py stashed) -> test_afg_declines_with_the_nationality_reason FAILED: AssertionError: assert <Decision.ACCEPT> is <Decision.DECLINE>; test_prk_also_declines_with_the_nationality_reason FAILED identically. 2 failed, 2 passed."
    exit: 1
    ts: "2026-08-23T13:05Z"
    seat: "session (mini, opus-5)"
  - claim: "The same two tests are GREEN against the fix, in the same session, same commands"
    cmd: "git stash pop; PYTHONPATH=. python -m pytest backend/tests/services/garuda_flow/test_intake.py -k TestNationalityEligibility -v -> 4 passed, 33 deselected."
    exit: 0
    ts: "2026-08-23T13:07Z"
    seat: "session (mini, opus-5)"
  - claim: "The country-name list was independently re-retrieved from the official source (not shipped on the R4 research lead's authority alone) and matches it row-by-row"
    cmd: "curl -s -L -A 'Mozilla/5.0 ...' https://www.imigrasi.go.id/wna/daftar-negara-voa-bvk-calling-visa/daftar-negara-subjek-visa-on-arrival -> HTTP 200, 48218 bytes. python3 regex-parsed the raw `<ol><li><span>...</span></li></ol>` block (NOT an LLM summary) -> 97 entries. Diffed programmatically against the R4 lead file (97 rows) by (number, name) pair -> MATCHES=97, MISMATCHES=0."
    exit: 0
    ts: "2026-08-23T12:40Z"
    seat: "session (mini, opus-5)"
  - claim: "Every ISO-3 alpha-3 code in the dataset is a real, correctly-mapped code — cross-checked against a standard third-party reference, not asserted from memory"
    cmd: "curl -s -L https://raw.githubusercontent.com/lukes/ISO-3166-Countries-with-Regional-Codes/master/all/all.csv -> HTTP 200, 250 rows (249 countries + header). python3 resolved each of the 97 candidate alpha-3 codes back to its English short name via this CSV and printed the (Indonesian name, code, English name) triple for manual semantic verification -> 97/97 resolved, 0 codes absent from the reference, 0 semantic mismatches (spot-checked the ambiguous ones by name: GBR->'United Kingdom of Great Britain and Northern Ireland' for 'Inggris', VAT->'Holy See' for 'Vatikan', HKG->'Hong Kong' for 'Hongkong', TWN->'Taiwan, Province of China' for 'Taiwan', CHN->'China' for 'Tiongkok')."
    exit: 0
    ts: "2026-08-23T12:55Z"
    seat: "session (mini, opus-5)"
  - claim: "No existing dataset or installed library in the repo already provides ISO-3166 country codes suitable for reuse — building new was the correct call, not a reinvention"
    cmd: "General-purpose search subagent (sonnet) grepped apps/backend-rag/backend/ for COUNTRY_CODES/ISO_3166/VALID_NATIONALITIES/country_codes.py and checked requirements*.txt + pyproject.toml + `pip show pycountry|babel|iso3166` in the venv -> only `visa_engine/shadow.py::_ALPHA3_TO_ALPHA2` found, self-documented 'Best-effort, deliberately NON-EXHAUSTIVE' (192/249 codes), scoped to a different subsystem's audit plumbing; zero third-party ISO library installed or declared anywhere, even transitively."
    exit: 0
    ts: "2026-08-23T12:20Z"
    seat: "sonnet-5 (search subagent)"
  - claim: "The full baseline and post-fix garuda_flow + garuda_voa router test suites are green, with the exact before/after counts the brief declares"
    cmd: "PYTHONPATH=. pytest backend/tests/services/garuda_flow/ backend/tests/app/routers/test_garuda_voa.py backend/tests/app/routers/test_garuda_voa_no_internal_leak.py --collect-only -q -> 183 tests (before this PR: 162, confirmed by re-running the same collect against the stashed pre-fix source). Full run -> 146 passed (garuda_flow/ subset alone) / 183 passed (full set incl. router tests), 0 failed."
    exit: 0
    ts: "2026-08-23T13:15Z"
    seat: "session (mini, opus-5)"
  - claim: "ruff (lint + format) is clean on every file this PR touches or adds"
    cmd: "ruff check services/garuda_flow/nationality_eligibility.py services/garuda_flow/intake.py tests/services/garuda_flow/test_nationality_eligibility.py tests/services/garuda_flow/test_intake.py -> All checks passed! ruff format --check <same files> -> 3 already formatted; test_intake.py flagged, but `ruff format --diff` shows both hunks are PRE-EXISTING lines (77-85, 96-100) this PR's diff never touches — confirmed via `git diff` scope, left as-is (out of this PR's one concern)."
    exit: 0
    ts: "2026-08-23T13:20Z"
    seat: "session (mini, opus-5)"
tests:
  - "backend/tests/services/garuda_flow/test_nationality_eligibility.py — 17 new (guilt AFG/PRK/ZZZ, innocence USA/ITA/RUS/AUS/HKG/TWN/GBR, case-insensitivity x4, non-vacuity x3)"
  - "backend/tests/services/garuda_flow/test_intake.py::TestNationalityEligibility — 4 new (end-to-end build_verdict wiring, guilt shown RED pre-fix)"
  - "backend/tests/services/garuda_flow/ full directory — 146 passed (was 125)"
  - "backend/tests/services/garuda_flow/ + app/routers/test_garuda_voa*.py — 183 passed (was 162, the mandate's declared known-good baseline)"
static:
  - "ruff check — All checks passed! (0 lint violations on touched/new files)"
  - "ruff format --check — 0 formatting issues in this PR's own diff (pre-existing drift in test_intake.py's untouched lines left alone, out of scope)"
pii_scan: clean
dissent: []
lanes:
  - lane: "garuda-voa-nationality-dataset"
    role: build
    seat: "opus-5"
