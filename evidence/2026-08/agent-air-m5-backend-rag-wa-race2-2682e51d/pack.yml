# NOTE: brief_ref is the CANONICAL name, not this pack's own directory — the
# harness-floor.yml CI workflow copies both discovered per-PR files into
# /tmp/evidence-check/evidence/{brief,pack}.yml under these exact names
# before validating, so brief_ref must always read "evidence/brief.yml".
brief_ref: evidence/brief.yml
plan_ref: PR #4881 — agent/air-m5/backend-rag/wa-race2
# Diff stats below are the LITERAL output of the command in the matching
# receipt (git diff --numstat origin/main...HEAD, code files only, summed
# by hand ONLY as 93+100+203+18+3=417 / 24+0+19+9+0=52 -- the per-file rows
# are the transcription of the command's own stdout, not a re-estimate).
diff:
  files: 5
  insertions: 417
  deletions: 52
  hotzone_hits:
    - apps/backend-rag/backend/db/migrations_v2/283_wa_reply_claims.sql

lanes:
  - lane: build
    role: build
    seat: sonnet-5
  - lane: review
    role: review
    seat: independent-gear3-grader

receipts:
  - claim: "Deterministic floor for this diff is 3 (hot-zone migration path)."
    cmd: "python3 scripts/evidence_pack_lint.py --print-floor --changed-files-file /tmp/changed_files.txt"
    exit: 0
    ts: "2026-08-25T05:40:11Z"
    seat: sonnet-5
  - claim: "New migration 283_wa_reply_claims.sql is squawk-clean under the repo's standard exclusion set (renumbered 282 -> 283 after a SECOND adversarial-review finding: PR #4879, open, already claims 282 with 282_garuda_orders.sql)."
    cmd: "squawk --pg-version=15.0 --exclude=require-concurrent-index-creation --exclude=require-timeout-settings --exclude=require-concurrent-index-deletion --exclude=ban-drop-table --exclude=ban-drop-column --exclude=ban-char-field --exclude=prefer-bigint-over-smallint --exclude=prefer-text-field --exclude=prefer-robust-stmts --exclude=constraint-missing-not-valid --exclude=prefer-bigint-over-int --exclude=prefer-identity --exclude=disallowed-unique-constraint apps/backend-rag/backend/db/migrations_v2/283_wa_reply_claims.sql"
    exit: 0
    ts: "2026-08-25T07:32:10Z"
    seat: sonnet-5
  - claim: "The migration carries exactly one ROLLBACK marker."
    cmd: "grep -c '^-- === ROLLBACK ===$' apps/backend-rag/backend/db/migrations_v2/283_wa_reply_claims.sql"
    exit: 0
    ts: "2026-08-25T07:32:15Z"
    seat: sonnet-5
  - claim: "Queried the FULL open-PR set (not just main + one named PR) for migration-number claims: PR #4879 (open) claims 282 with 282_garuda_orders.sql, colliding with this branch's prior rename target. No open PR claims 283."
    cmd: "gh pr list --state open --limit 300 --json number,files -q '.[] | .number as $n | .files[]?.path | select(test(\"migrations_v2/[0-9]+_\")) | \"\\($n): \\(.)\"'"
    exit: 0
    ts: "2026-08-25T07:28:40Z"
    seat: sonnet-5
  - claim: "Confirmed main's real tip still tops out at migration 280 (via git ls-tree AND independently via the GitHub Contents API, which agree). Also found and recorded, without resolving: PR #4854 is reported MERGED and claims migration 281 in its commit message, but 281_garuda_voa_retention.sql is absent from origin/main and #4854's merge commit is not an ancestor of origin/main — chose 283 to keep a two-integer margin from every number confirmed live (280 on main, 282 claimed by #4879) so this anomaly cannot collide with this file whichever way it resolves."
    cmd: "git ls-tree origin/main -- apps/backend-rag/backend/db/migrations_v2/ | grep -E '28[0-9]_'; gh api repos/Bali-Zero/Teman2/contents/apps/backend-rag/backend/db/migrations_v2 --jq '.[].name' | grep -E '^28[0-9]_'; git merge-base --is-ancestor 09bb478323845a9e80a90afdaf877efff4018874 origin/main"
    exit: 1
    ts: "2026-08-25T07:29:30Z"
    seat: sonnet-5
  - claim: "Literal git diff --numstat origin/main...HEAD after the 283 rename, transcribed verbatim (not hand-summed): whatsapp_chat.py 93/24, 283_wa_reply_claims.sql 100/0, test_wa_double_reply_defenses.py 203/19, test_wa_inbox_webhook.py 18/9, test_whatsapp_chat_coverage.py 3/0. Code-file totals: 5 files, 417 insertions, 52 deletions."
    cmd: "git diff --numstat origin/main...HEAD"
    exit: 0
    ts: "2026-08-25T07:36:05Z"
    seat: sonnet-5
  - claim: "Full backend/tests/unit/routers/ suite is green (1502 passed, 0 failed) after the atomic-claim change, the claim-ordering fix, the collateral mock fixes, and the two new should_escalate-awaited assertions; the 4 deselected tests are a pre-existing local Postgres-role gap (asyncpg InvalidAuthorizationSpecificationError), verified via git stash/pop to reproduce identically on pre-change code."
    cmd: "PYTHONPATH=. python -m pytest backend/tests/unit/routers/ --deselect backend/tests/unit/routers/test_wa_meta_inbox_onconflict.py"
    exit: 0
    ts: "2026-08-25T07:05:00Z"
    seat: sonnet-5
  - claim: "The race-simulation defenses (_claim_wamid_reply unit tests + the two claim-gate integration tests) pass, including the exactly-one-winner and same-path-retry cases."
    cmd: "PYTHONPATH=. python -m pytest backend/tests/unit/routers/test_wa_double_reply_defenses.py -v"
    exit: 0
    ts: "2026-08-25T07:04:10Z"
    seat: sonnet-5
  - claim: "ruff is clean on both touched Python files."
    cmd: "ruff check backend/app/routers/whatsapp_chat.py backend/tests/unit/routers/test_wa_double_reply_defenses.py"
    exit: 0
    ts: "2026-08-25T07:04:55Z"
    seat: sonnet-5
  - claim: "'Visa Oracle fullstack smoke (advisory)' is genuinely advisory — it is absent from main's required_status_checks contexts, while 'Harness floor recompute' is present in that same list."
    cmd: "gh api repos/Bali-Zero/Teman2/branches/main/protection | jq .required_status_checks.contexts"
    exit: 0
    ts: "2026-08-25T05:30:18Z"
    seat: sonnet-5
  - claim: "Mutation M1 (gate legacy always early-return, applied by hardcoding `if True:` in place of `if not won_claim:`) turns test_wamid_claimed_by_legacy_proceeds_to_legacy_flow RED (AssertionError: should_escalate_mock expected to have been awaited once, awaited 0 times); test_dedup_db_error_is_non_blocking is unaffected by M1 as expected (its pool.acquire already raises before reaching that line). File cleanly reverted afterward (verified 0-diff against a pre-mutation backup)."
    cmd: "PYTHONPATH=. python -m pytest backend/tests/unit/routers/test_wa_double_reply_defenses.py::test_wamid_claimed_by_legacy_proceeds_to_legacy_flow backend/tests/unit/routers/test_wa_double_reply_defenses.py::test_dedup_db_error_is_non_blocking -v"
    exit: 1
    ts: "2026-08-25T06:55:20Z"
    seat: sonnet-5
  - claim: "Mutation M2 (the claim's except re-raises instead of swallowing) turns test_dedup_db_error_is_non_blocking RED (same AssertionError shape — should_escalate never awaited, the re-raised RuntimeError is instead caught by the OUTER try/except and swallowed into a sent error message); test_wamid_claimed_by_legacy_proceeds_to_legacy_flow is unaffected by M2 as expected (its fetchval never raises). File cleanly reverted afterward (verified 0-diff against a pre-mutation backup)."
    cmd: "PYTHONPATH=. python -m pytest backend/tests/unit/routers/test_wa_double_reply_defenses.py::test_wamid_claimed_by_legacy_proceeds_to_legacy_flow backend/tests/unit/routers/test_wa_double_reply_defenses.py::test_dedup_db_error_is_non_blocking -v"
    exit: 1
    ts: "2026-08-25T06:58:40Z"
    seat: sonnet-5
  - claim: "After reverting both mutants, the full backend/tests/unit/routers/ suite and ruff are green again."
    cmd: "PYTHONPATH=. python -m pytest backend/tests/unit/routers/ --deselect backend/tests/unit/routers/test_wa_meta_inbox_onconflict.py && ruff check backend/app/routers/whatsapp_chat.py backend/tests/unit/routers/test_wa_double_reply_defenses.py"
    exit: 0
    ts: "2026-08-25T07:05:30Z"
    seat: sonnet-5

tests:
  cmd: "PYTHONPATH=. pytest backend/tests/unit/routers/ --deselect backend/tests/unit/routers/test_wa_meta_inbox_onconflict.py"
  passed: 1502
  failed: 0
static:
  lint: ok
  typecheck: n/a-python-uses-mypy-elsewhere-not-in-touched-files
  build: n/a

runtime_proof: []

dissent:
  - seat: sonnet-5-self-review
    objection: >
      The cross-path atomic claim in process_whatsapp_message originally ran
      BEFORE the allowlist check (whatsapp_triage_service.is_allowed). A
      number this legacy path will silently ignore could still WIN the wamid
      claim and then discard it — if that same wamid also has a legitimate,
      later-arriving meta-inbox delivery (the exact double-subscription shape
      this PR defends against), meta-inbox's own claim attempt would then
      lose to a claimant that was never going to answer, leaving a real
      customer with silence from BOTH paths instead of one correct reply
      from meta-inbox.
    status: CONFIRMED
    repro: >
      Traced whatsapp_triage_service.is_allowed(): returns True
      unconditionally only when WHATSAPP_ALLOWED_NUMBERS is unset; otherwise
      gates on an allowlist, so an out-of-allowlist number is a real,
      reachable input. Reordered the claim to run AFTER the allowlist check
      in apps/backend-rag/backend/app/routers/whatsapp_chat.py
      (process_whatsapp_message, steps "0"/"0.5"); updated
      test_wamid_already_claimed_by_meta_inbox_skips_legacy_generation,
      test_wamid_claimed_by_legacy_proceeds_to_legacy_flow, and
      test_dedup_db_error_is_non_blocking to prove the new ordering.
      Full suite green after the fix: 1502 passed, 0 failed.
  - seat: independent-gear3-grader
    objection: >
      REWORK verdict on the first evidence pack, 4 findings: (1) migration
      281 collides with PR #4854's own open 281_garuda_voa_retention.sql
      (main tops out at 280 — W40 collision class), and the table needs a
      declared retention note (unbounded growth, one row per message
      forever). (2) test_wamid_claimed_by_legacy_proceeds_to_legacy_flow and
      test_dedup_db_error_is_non_blocking pass even under mutants M1 (the
      legacy claim gate hardcoded to always early-return) and M2 (the
      claim's except re-raising instead of swallowing) — both tests only
      asserted `result is None`, which the outer try/except in
      process_whatsapp_message also produces on an early-return or a
      swallowed re-raise, so neither mutant was actually being caught. (3)
      the pack's own dissent text claimed the DB-error test "actually
      exercises the DB-error path" before that claim was true. (4) the
      pack's diff insertions/deletions were stale (316/43) against a
      measured 371/52 at review time.
    status: CONFIRMED
    repro: >
      Production code judged CORRECT by the grader — only evidence and one
      number were at fault. Cured in this same PR, same branch: migration
      renamed 281 -> 282 with a retention comment declaring the gap as
      future work; both flagged tests now capture the should_escalate mock
      and call should_escalate_mock.assert_awaited_once() after the
      `result is None` assertion; re-ran M1 and M2 against the corrected
      tests — both mutants now turn the correct single test RED (receipts
      above), then the file was cleanly reverted (0-diff against a
      pre-mutation backup) and the full suite re-confirmed green; this
      pack's own diff numbers were re-measured with `git diff --numstat`
      against origin/main after all fixes (379/52, not the earlier 316/43
      nor the grader's interim 371/52 — both stale snapshots from before the
      mutation-test fix's own diff was added).
  - seat: independent-gear3-grader
    objection: >
      Second REWORK, same PR: (1) migration 282 collides with PR #4879
      (garuda_orders, open), which claimed 282 with 282_garuda_orders.sql
      BEFORE this branch's own rename to 282 — the prior round's rename
      only checked main + one named PR (#4854), not the full open-PR set,
      so it walked into a second, different collision of the exact same
      class (W40) it was meant to fix. The migration-number CI lint only
      compares main + the current PR, so this would have landed silently
      and discover_migrations would have skipped one migration at runtime.
      (2) the pack's own diff insertions figure ("379") was a hand
      recomposition using an earlier per-file count for the test file (184
      insertions) that did not match the actual diff at review time (the
      grader's own recount: 203).
    status: CONFIRMED
    repro: >
      Queried the FULL open-PR set this time, not just main + one named
      PR: `gh pr list --state open --limit 300 --json number,files -q
      '.[] | .number as $n | .files[]?.path | select(test(
      "migrations_v2/[0-9]+_")) | "\($n): \(.)"'` returned both #4879:282
      and (this branch, pre-fix) #4881:282 — confirmed collision. Also
      found, while investigating: PR #4854 is reported MERGED and its
      commit claims migration 281, but 281_garuda_voa_retention.sql is
      absent from origin/main's live tip and the merge commit is not an
      ancestor of origin/main (verified via `git merge-base
      --is-ancestor`) — flagged in the migration header and this pack's
      residual_risks as an unresolved anomaly, deliberately NOT used to
      justify reclaiming 281 here. Renamed 282 -> 283, keeping a
      two-integer margin from both numbers confirmed live (280 on main,
      282 claimed by #4879) so the anomaly cannot collide with this file
      whichever way it resolves. Re-ran squawk (0 issues) and the migration
      number lint (164 files, all unique) on the renamed file. This pack's
      diff stats are now the LITERAL transcription of `git diff --numstat
      origin/main...HEAD` run after the rename (receipt above), not a hand
      sum from an earlier state: 417 insertions / 52 deletions, 5 files.

residual_risks:
  - "test_wa_meta_inbox_onconflict.py (4 tests) fails locally with asyncpg
    InvalidAuthorizationSpecificationError — a pre-existing local Postgres
    role gap on this machine, unrelated to this change (reproduces
    identically on pre-change code via git stash/pop). Not fixed here
    (one-concern rule); does not affect CI, which provisions its own DB
    role."
  - "'Visa Oracle fullstack smoke (advisory)' is red on this PR but confirmed
    non-required (absent from branch protection's required_status_checks
    contexts) and unrelated to this change's scope — left untouched per the
    one-concern rule, per explicit team-lead instruction."
  - "wa_reply_claims has no TTL/retention: it grows by one row per inbound
    WhatsApp message forever. Declared in the migration's own header as
    future work (a claimed_at-based sweep or partitioning), not implemented
    in this PR (one-concern rule)."
  - "During a sustained DB outage, the double-reply race RETURNS: both paths
    fail to acquire/claim (best-effort, by design — a DB hiccup must never
    mute a genuinely legacy message), so both proceed and can both answer
    the same wamid. This is the deliberate trade-off #4878 already made
    (never-mute over never-duplicate during DB unreachability) and this PR
    preserves it rather than changing it — flagging it explicitly so the
    trade-off is visible, not an oversight."
  - "A crash between a won claim and the reply/ledger write leaves that
    wamid permanently claimed with no answer ever sent, unless Meta
    redelivers the webhook (which would retry the whole flow with the same
    wamid — the claim UPSERT's same-path-retry handling covers that case,
    but a redelivery is not guaranteed to happen)."
  - "The meta-inbox path claims a wamid (winning the race) even on
    human_handling=True threads, where the bot deliberately does not answer
    — this is intended silence (a human is handling the conversation), not a
    defect, but it means a 'won the claim' log line does not imply a reply
    was actually sent."
  - "UNRESOLVED, out of scope for this PR: PR #4854 is reported MERGED by
    the GitHub API and its commit message claims migration
    281_garuda_voa_retention.sql, but that file is absent from
    origin/main's live tip and #4854's merge commit
    (09bb478323845a9e80a90afdaf877efff4018874) is not an ancestor of
    origin/main. This PR does not use 281 and does not attempt to
    diagnose or fix the anomaly (one-concern rule) — flagging it because
    it looks exactly like the class of silent-drop this whole PR exists
    to prevent, just on a different table."

sources: []
pii_scan: clean
size_tokens: 4200
