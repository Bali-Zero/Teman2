brief_ref: evidence/brief.yml
plan_ref: >-
  Q0 in /Users/nuzantara/Desktop/2026-08-26-PIANO-SPEC-receptor-live.md §5bis —
  dispatched as a teammate mandate to correct `.agents/skills/` vs
  `.claude/skills/` divergence. This pack documents that the dispatched
  premise was measured false this turn, and ships the corrected, smaller
  deliverable instead of the originally-specified one.

lanes:
  - lane: build
    role: build
    seat: claude-sonnet-5
  - lane: refute
    role: review
    seat: kimi-code/k3

diff:
  files: 6
  net_lines: 998
  measured_at: 63bfa19e
  note: >-
    Round 3 final (team-lead's gate conditions, post-second-push): `git diff
    --stat $(git merge-base HEAD origin/main)` — 6 files changed, 998
    insertions(+), 0 deletions vs origin/main (nothing pre-existed for any
    of these files, so round-3's internal edit-then-replace churn nets to
    pure insertions against main). Breakdown: test_skills_canonical.py 337
    lines (was 288 after round 1, +49 across rounds 2-3: NUZ_SKILLS_ROOT
    override + fleet-state comment + softened docstring wording),
    check-skills-canonical.yml 56, quickcheck.sh 58,
    .claude/skills/modus/PENDING-ARMS.md +2, pack.yml/brief.yml the
    remainder (evidence growing with each round's receipts). This exceeds
    the repo's ~400-net-line PR soft target (CLAUDE.md "Agent PR Contract"
    rule 1: "when the nature of the work permits it") — flagged, not
    trimmed: team-lead's explicit instruction across two follow-up messages
    was to land every ask on this SAME PR/branch rather than open a new
    one, and a large share of the total is evidence-pack YAML plus
    docstring/comment prose, not executable logic (the actual runtime
    surface — find_canonicity_violations, the pinned name sets, the
    quickcheck function — is under 120 lines total). measured_at is
    unchanged from the first push (merge-base with origin/main at
    investigation time).

pii_scan: clean

receipts:
  - claim: >-
      The dispatched premise ("`.agents/skills/` has 21 dirs; 11 SKILL.md
      DIVERGENT vs `.claude/skills/`, naming modus/workflow;
      `.agents/skills/modus/SKILL.md:149` still says 'Fable 5 first'") is
      false on every count.
    cmd: >-
      ls -la .claude/skills/ .agents/skills/ && diff -rq .claude/skills
      .agents/skills && find .agents/skills -type d | wc -l
    result: >-
      .agents/skills/ has 8 top-level skill dirs (bot, bz-video-production,
      google-flow-video, kbli-navigator, secondhome, subhi, visaoracle, wr2)
      + README.md, not 21 (find -type d incl. root + wr2/_research = 10).
      .claude/skills/ has 16 entries: 5 are symlinks into .agents/skills/
      (bot, kbli-navigator, secondhome, visaoracle, wr2— trivially
      byte-identical) and 11 are real Tier-B dirs with NO .agents/skills/
      counterpart on either side of the diff (agent-session-discipline,
      final-gate-discipline, intake, karpathy-discipline, modus,
      pipeline-ship, reuse-first, skill-catalog, slhs,
      sota-architecture-loop, workflow). `diff -rq` reports zero "differ"
      lines — nothing is divergent, the 11 simply do not exist on the
      .agents side, and never did.
    exit: 0
    ts: 2026-08-27T00:15Z
    seat: session (Pro, implementer lane)

  - claim: "`.agents/skills/modus/SKILL.md` does not exist — line 149 cannot say anything."
    cmd: >-
      git log --oneline --diff-filter=D --all -- '.agents/skills/modus*'
      '.agents/skills/workflow*'
    result: >-
      Empty output on both patterns — no commit in the repo's history ever
      created then deleted such a path. It never existed to diverge from.
    exit: 0
    ts: 2026-08-27T00:16Z
    seat: session (Pro, implementer lane)

  - claim: >-
      `.claude/skills/modus/SKILL.md` (the file that DOES exist) already
      correctly documents the 2026-08-20 Fable-out ruling as current; every
      "Fable 5 first" occurrence is explicit past-tense framing, not a live
      instruction.
    cmd: grep -n "Fable" .claude/skills/modus/SKILL.md
    result: >-
      14 matches, all either "Fable is out of the workflow (RULED
      2026-08-20 ...)" or "was 'Fable 5 first, degrade to Opus on
      exhaustion' per the 2026-08-09 ruling; that two-tier degradation is
      now moot, Opus 5 IS the seat" — the same "was X" framing used by the
      canonical SSOT `research/operations/2026-08-10-fleet-order-spec.md:314`.
    exit: 0
    ts: 2026-08-27T00:17Z
    seat: session (Pro, implementer lane)

  - claim: >-
      The real, deliberate architecture is documented on disk:
      `.agents/skills/` is the canonical cross-agent store, `.claude/skills/`
      symlinks shared skills into it, and Claude-Code-specific skills (the
      README names `workflow` itself) stay real dirs under `.claude/skills/`
      ONLY, by design — never mirrored.
    cmd: cat .agents/skills/README.md
    result: >-
      Confirmed verbatim: "Edit skills HERE, never in a tool-specific copy
      ... If you find a real file where a symlink should be, it is drift"
      and "Tool-specific glue (hooks, slash commands, subagent definitions,
      orchestration playbooks like `workflow`) stays in the owning tool's
      directory (Tier B) and is NOT shared." Established 2026-07-23
      (skill-unification lane, PR #3019, confirmed via
      `git log --oneline -- .claude/skills/bot`).
    exit: 0
    ts: 2026-08-27T00:18Z
    seat: session (Pro, implementer lane)

  - claim: >-
      `scripts/prepush_classify.py` independently corroborates the same
      architecture from a different angle (pre-push test-scoping), so the
      correction is not this session's reading of one file alone.
    cmd: grep -n "agents/skills" scripts/prepush_classify.py
    result: >-
      Lines 587-617: ".agents/skills/README.md (verified on disk) states
      this tree is the CANONICAL cross-agent skill store ... NOT a
      duplicate of .claude/skills/. Proof: 4 of the 8 .claude/skills/<name>
      entries ... are literal symlinks ... verified via git ls-tree mode
      120000." (Written 2026-07-27; the "4 of 8" is now "5 of 16" —
      secondhome was unified after that comment was authored — a stale
      descriptive comment, not a functional bug: the classifier's own
      matching rule is suffix-scoped, not a hardcoded name list.)
    exit: 0
    ts: 2026-08-27T00:19Z
    seat: session (Pro, implementer lane)

  - claim: >-
      No prior PR or commit already addressed this Q0 mandate — this is not
      duplicate work.
    cmd: >-
      git log --oneline --all --grep="skills canon" -i; gh pr list --search
      "skills canon" --state all --json number,title,state
    result: "Zero matches on either search."
    exit: 0
    ts: 2026-08-27T00:14Z
    seat: session (Pro, implementer lane)

  - claim: >-
      The new checker actually distinguishes guilt from innocence — proven
      by running the suite, not asserted. Two fixture bugs were found and
      fixed by this same red-first run (wrong relative-symlink depth in the
      innocence fixture; `.resolve()` on a not-yet-unlinked symlink
      returning the target dir in the drift-replacement fixture).
    cmd: >-
      apps/backend-rag/.venv/bin/pytest scripts/tests/test_skills_canonical.py
      -v
    result: "collected 10 items; 10 passed in 0.08s. 3 guilt + 3 innocence + 4 real-repo regression tests, all green."
    exit: 0
    ts: 2026-08-27T00:32Z
    seat: session (Pro, implementer lane)

  - claim: "The new CI workflow file is syntactically valid."
    cmd: actionlint .github/workflows/check-skills-canonical.yml
    result: "No output, exit 0."
    exit: 0
    ts: 2026-08-27T00:34Z
    seat: session (Pro, implementer lane)

  - claim: >-
      The deterministic gear floor recomputed from this PR's real
      changed-file set is 3 (touches .github/workflows/), matching the
      declared gear.
    cmd: >-
      python3 scripts/evidence_pack_lint.py --print-floor
      --changed-files-file <changed-files-list>
    result: "floor=3, with the 2 real changed files enumerated (test file + workflow file)."
    exit: 0
    ts: 2026-08-27T00:36Z
    seat: session (Pro, implementer lane)

  - claim: >-
      Team-lead's independent finding on the Pro MAIN checkout (not this
      worktree) is real: 12 untracked dirs under .agents/skills/ (the 11
      Tier-B names + source-command-resume), all mtime 2026-08-19 00:26 —
      the drift IS present, just as UNTRACKED working-tree state rather than
      tracked files. CI cannot see this class of drift because CI only ever
      clones a specific commit; untracked files by construction never travel
      with one.
    cmd: git -C /Users/nuzantara/nuzantara status --porcelain -- .agents/skills
    result: >-
      12 "??" (untracked) lines: agent-session-discipline,
      final-gate-discipline, intake, karpathy-discipline, modus,
      pipeline-ship, reuse-first, skill-catalog, slhs,
      sota-architecture-loop, source-command-resume, workflow. Zero tracked
      ("M "/"A ") lines under .agents/skills — confirms this is 100%
      untracked cruft, not a committed regression.
    exit: 0
    ts: 2026-08-27T02:05Z
    seat: session (Pro, implementer lane)

  - claim: >-
      The untracked Pro copy of .agents/skills/modus/SKILL.md is genuinely
      stale and still routes the Gear-3 harness verdict gate to Fable —
      confirmed by reading the file directly, not by trusting team-lead's
      citation alone.
    cmd: >-
      Read tool on /Users/nuzantara/nuzantara/.agents/skills/modus/SKILL.md,
      lines 140-154
    result: >-
      Line 149 verbatim: "**Gear-3 harness verdict gate** | the Evidence
      Pack of a Gear-3 task (PASS/PWC/REWORK/BLOCK) | Fable 5 first,
      rotating AZ->A2->A3->A1 | **Ruling Zero 2026-08-09**: only when NO
      account can run Fable -> Opus 5 effort=max ...". Matches team-lead's
      citation exactly. This is the untracked file — the TRACKED
      .claude/skills/modus/SKILL.md (verified earlier in this pack) already
      reads Opus 5 xhigh/max per the 2026-08-20 ruling; this is the
      contradiction the original Q0 mandate was pointing at, just on the
      wrong (untracked) copy.
    exit: 0
    ts: 2026-08-27T03:05Z
    seat: session (Pro, implementer lane)

  - claim: >-
      Fleet state beyond Pro (M5 has a TRACKED-file modification `git
      clean` cannot touch; Mini is clean) — reported by team-lead, NOT
      independently re-verified by this session (different machine,
      outside this lane's reach).
    cmd: team-lead teammate message, 2026-08-27
    result: >-
      M5: same 11 Tier-B names + source-command-resume untracked (21
      entries counting subdirs), PLUS `M .agents/skills/subhi/SKILL.md` —
      a locally modified TRACKED file, a different failure class from the
      other 11 (git clean -fd does not and must not remove local edits to
      a tracked file; it needs its own git diff + a keep/discard/commit
      decision, not bundled into the untracked-cleanup one-liner). Mini:
      clean, zero entries under .agents/skills drift.
    exit: n/a
    ts: 2026-08-27T02:45Z
    seat: team-lead (not this session — attribution only, unverified by build lane)

  - claim: >-
      test_skills_canonical.py, pointed at the Pro main checkout via the new
      NUZ_SKILLS_ROOT override, actually goes RED against that untracked
      drift — with the exact 11 Tier-B names named in the failure output —
      proving the tripwire works on the tree where the copies actually live,
      not just in a clean worktree that could never reproduce the bug.
    cmd: >-
      NUZ_SKILLS_ROOT=/Users/nuzantara/nuzantara
      apps/backend-rag/.venv/bin/pytest scripts/tests/test_skills_canonical.py -v
    result: >-
      2 failed, 9 passed. Both failures
      (test_real_repo_has_zero_canonicity_violations,
      test_real_repo_claude_only_names_never_leak_into_agents_skills) list
      all 11 Tier-B names as LEAK/DRIFT. (source-command-resume correctly
      does NOT appear — it has no .claude/skills twin, so the structural
      rule has nothing to compare it against; see the new PENDING-ARMS entry
      for that name instead.) Same-tree control run without the override
      (this worktree): 11 passed, 0 failed — the override is what changes
      the verdict, not a different pytest invocation.
    exit: 1
    ts: 2026-08-27T02:07Z
    seat: session (Pro, implementer lane)

  - claim: >-
      scripts/quickcheck.sh (already wired into .husky/pre-push, advisory/
      non-blocking) now runs this same check unconditionally on every push —
      not gated on the diff touching .claude/skills or .agents/skills,
      because the failure mode it exists to catch is BY DEFINITION untracked
      and would never appear in quickcheck's own changed-file list.
    cmd: >-
      bash -n scripts/quickcheck.sh; grep -n "run_skills_canonical_check"
      scripts/quickcheck.sh
    result: >-
      bash -n exit 0 (no syntax error). Function defined at line 304, called
      unconditionally in main() at line 406 (after run_actionlint_if_touched,
      before run_r1_check). Verified behavior: clean worktree -> "[skills-canon]
      OK"; branch without the test file yet -> graceful "not on this branch
      yet — skipping" (rc 0, never blocks). NOTE (caveat, not a bug):
      quickcheck.sh's own main() has an earlier unrelated early-return
      ("no committed changes vs origin/main") that short-circuits before
      reaching this call when HEAD == origin/main with nothing staged — not
      a real gap for the production pre-push trigger, since git only invokes
      .husky/pre-push when there is something to push (changed_all is
      non-empty in any real firing).
    exit: 0
    ts: 2026-08-27T02:12Z
    seat: session (Pro, implementer lane)

  - claim: >-
      source-command-resume (also untracked, mtime 2026-08-19 00:26) is a
      different case from the 11 Tier-B leaks: it has NO .claude/skills twin
      at all, so the canonicity checker correctly does not — and structurally
      cannot — flag it. Recorded as an open decision, not auto-deleted, per
      team-lead's explicit instruction.
    cmd: >-
      cat .agents/skills/source-command-resume/SKILL.md; find
      /Users/nuzantara/nuzantara -maxdepth 4 -iname resume
    result: >-
      description field is VERBATIM IDENTICAL to the already-catalogued
      "resume" skill's description ("Re-inject Mnemos handoff JSON
      post-compact. Reads precompact-handoff state files."). No tracked
      `resume` skill directory exists anywhere in the repo. Body references
      `.Codex/` (capital C) as its vendored source; only lowercase `.codex/`
      exists on disk today. Ledger entry appended:
      .claude/skills/modus/PENDING-ARMS.md (opened 2026-08-27, "needs a
      decision: adopt into .claude/skills OR delete").
    exit: 0
    ts: 2026-08-27T02:20Z
    seat: session (Pro, implementer lane)

dissent:
  - seat: kimi-code/k3 (cross-family refuter)
    objection: >-
      Three findings on the diff as first opened. (1) The "wrong target"
      guilt fixture used `../..` where claude_skills/ and agents_skills/
      are direct siblings under tmp_path (the innocence fixture's own `..`
      proves the correct depth) — the symlink dangled outside tmp_path
      instead of resolving to the `decoy` dir the test built, so the test
      passed for "dangling symlink" reasons, not "resolves to a real wrong
      target" as its name claims. (2) The docstring's claim that the
      structural rule "needs no allowlist to stay correct as skills are
      added or removed" is overstated: a Tier-B skill MOVED (not copied)
      into `.agents/skills/` — deleted from `.claude/skills/` in the same
      change — never appears on both sides, so the structural rule cannot
      see it; only the pinned KNOWN_CLAUDE_ONLY_NAMES list catches this for
      skills that exist today, and it goes stale for skills authored later.
      (3) `find_canonicity_violations` returns `[]` vacuously if either
      tree is missing entirely, so a whole-architecture disappearance would
      pass `test_real_repo_has_zero_canonicity_violations` with no real
      protection (mitigated only as a side effect of the pinned-name
      tests' own assertions).
    status: CONFIRMED
    note: >-
      (1) and (3) fixed: the wrong-target fixture now uses the correct `..`
      depth plus a self-checking assertion that would catch this exact
      class of fixture bug on its own; a new
      test_real_repo_both_skill_trees_exist makes the existence
      precondition explicit rather than incidental. (2) disclosed, not
      fixed: closing it fully would require judging whether new content is
      tool-agnostic, which a directory-shape check cannot do — the
      docstring and this pack now say so plainly instead of overclaiming
      coverage. Re-ran full suite after the fixes: collected 11, 11 passed
      (was 10 — the new existence test).

  - seat: session self-refutation (recorded because the dispatching mandate's own author could not have caught it without re-measuring)
    objection: >-
      The dispatching Q0 mandate's "measured today" claim was not
      reproducible: 21 dirs / 11 divergent SKILL.md / a stale Fable-first
      line at a specific file:line were all checked directly this turn and
      all three are false. Executing the mandate's literal instructions
      (port .claude Tier-B content into .agents/skills as byte-identical
      copies) would have violated the documented 2026-07-23 architecture
      and polluted the cross-agent canonical store with Claude-Code-only
      orchestration prose.
    status: CONFIRMED
    note: >-
      Not executed. Scope corrected instead: ship a regression test
      encoding the REAL contract (including a guilt fixture for exactly
      the mirroring mistake the original mandate asked for), reported back
      to the dispatching team-lead via teammate message before opening
      this PR.

tests:
  - "scripts/tests/test_skills_canonical.py — 11 collected, 11 passed, rc=0 (backend-rag venv pytest, this worktree, no override), after the refuter-driven fixture fix + new existence test (was 10/10 pre-refuter)."
  - "Same file, NUZ_SKILLS_ROOT=/Users/nuzantara/nuzantara (Pro main checkout, read-only inspection) — 11 collected, 2 failed / 9 passed. Both failures name all 11 leaked Tier-B dirs. This is the intended, demonstrated RED against real untracked drift — proof the tripwire fires on the tree where the bug actually lives, not just a synthetic fixture."

static:
  - "actionlint .github/workflows/check-skills-canonical.yml — rc=0, no output."

notes:
  - "This PR ships NO change to any existing skill file — the finding is
    that the dispatched divergence does not exist. The only artifacts are
    a new test (regression tripwire) and a new CI workflow (PR-time
    wiring, since scripts/tests/*.py in this repo runs on PR only via a
    dedicated workflow naming it — confirmed against ~15 existing
    precedents, e.g. check-cicatrix-scar-pointers.yml)."
  - "The 4 skills that exist only under .agents/skills/ (bz-video-production,
    google-flow-video, subhi, plus README.md) were left untouched, per the
    original mandate's own instruction — out of scope for this correction."
  - "Round 2 (team-lead's 3 asks): the untracked-drift removal on Pro
    (.agents/skills/{agent-session-discipline,final-gate-discipline,intake,
    karpathy-discipline,modus,pipeline-ship,reuse-first,skill-catalog,slhs,
    sota-architecture-loop,workflow} — 11 dirs, NOT source-command-resume) is
    an operator[control-plane] gesture, not something this session can run:
    the main checkout is agent-read-only by design (CLAUDE.md Agent Worktree
    Discipline). One-liner for Pro, verbatim:
    `git -C ~/nuzantara clean -fd -- .agents/skills/agent-session-discipline
    .agents/skills/final-gate-discipline .agents/skills/intake
    .agents/skills/karpathy-discipline .agents/skills/modus
    .agents/skills/pipeline-ship .agents/skills/reuse-first
    .agents/skills/skill-catalog .agents/skills/slhs
    .agents/skills/sota-architecture-loop .agents/skills/workflow`.
    Team-lead additionally reported (not independently re-verified by this
    session — different machine) the same 11-name untracked set on M5 (plus
    source-command-resume, 21 entries counting subdirs) — same one-liner
    shape applies there. M5 ALSO has a locally-modified TRACKED file,
    `.agents/skills/subhi/SKILL.md` — `git clean -fd` does not touch
    modified tracked files, so this is a SEPARATE, undecided item: someone
    needs to `git -C ~/nuzantara diff -- .agents/skills/subhi/SKILL.md` and
    either keep+commit or discard the edit before/independent of the
    untracked cleanup. Mini-Pro2 is reported clean (no action needed there).
    `source-command-resume`'s own keep/adopt/delete decision is tracked as
    its own PENDING-ARMS.md entry (opened 2026-08-27), deliberately NOT
    included in the cleanup one-liner above."
