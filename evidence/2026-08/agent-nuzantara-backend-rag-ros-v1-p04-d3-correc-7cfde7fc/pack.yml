brief_ref: evidence/brief.yml
plan_ref: "P04-D3 correction: fix false matrix-approval citations after an independent REFUSE verdict (claims #10/#11/#12), 2026-08-24"
diff:
  net_lines: >-
    +403/-45, 5 files (code+test+ledger only; evidence-pack files not counted here):
    action_item_adapter.py (+203/-39, docstring + 3 field comments/warnings rewritten,
    extensions call site extended), synthesis.py (+67/-3, unbacked_refs_extension() gains an
    optional pending_ruling parameter merged into the same payload dict, a module-docstring
    invariant paragraph on extensions presence citing hashing.py:3-5/:28, and a new
    UNBACKED_REFS_EXTENSION_VERSION="1.1.0" constant replacing a hardcoded "1.0.0" literal --
    the latter two added in a gate round after the conductor's own review), test_action_item_adapter.py
    (+47/-3, two new tests plus one updated assertion), test_adapter_extensions_invariant.py
    (+84/-0, NEW file, a package-wide AST-based structural test), PENDING-ARMS.md (+2/-0, one
    new ledger row). Measured this session via `git diff --numstat` against this branch's own
    merge-base with origin/main (bf85e81b0acb09459bc9560d9deefc078ed09823, re-fetched fresh
    immediately before measuring, confirmed unchanged since branch creation).
  behaviour_change: >-
    Zero change to any adapted VALUE: priority="p2", sla.due_at from expires_at, and
    current_intent_ref=None are byte-identical to what PR #4749 shipped. Additive behavioural
    deltas only: (1) extensions['com.balizero.research-os-adapters'].payload gains a second
    key, pending_ruling. This DOES change object_hash for every ActionItem this adapter
    produces (measured: pre-correction 305489949ff73447eb4c5825ffd07c5b4708fb90cc609ee204a4714790a324ee
    vs post-correction dd7d5a2dcb869d8121793bf5ad01ee9d4ef5f54c3f6b8da4952646dfff510e22 on the
    identical fixture row) -- confirmed harmless today per brief.yml's risks section (no
    hardcoded hash literal anywhere, no parity probe compares object_hash, zero persistence of
    this kind yet, still zero non-test consumers of this adapter package). (2) The behavioural
    test (test_extensions_is_always_explicitly_set_never_omitted) and the new structural test
    (test_adapter_extensions_invariant.py) arm the always-pass-extensions convention as a gate
    -- purely additive, only adds failure modes for future regressions. (3) extension_version
    on this namespace's ExtensionValue is now "1.1.0" instead of "1.0.0" (gate-round Gap 2 fix)
    -- a real, intentional wire-visible change: any consumer that happened to assert the
    literal "1.0.0" would need updating; grepped this session, the only such assertion in the
    whole repo was this PR's own pre-existing test, which is updated in this same diff.
receipts:
  - claim: "Base SHA re-measured fresh in this session, not copied from any document/prompt"
    cmd: "git fetch origin main && git rev-parse origin/main"
    exit: 0
    ts: "2026-08-24T07:05:00Z"
    seat: "session (pro)"
  - claim: "This branch's merge-base with origin/main is identical to origin/main's own tip -- zero drift"
    cmd: "git merge-base HEAD origin/main"
    exit: 0
    ts: "2026-08-24T07:05:10Z"
    seat: "session (pro)"
  - claim: "Diff on the 3 code/test files is exactly +243/-43 (203/39 + 19/2 + 21/2)"
    cmd: "git diff --numstat -- apps/backend-rag/backend/services/research_os/action_item_adapter.py apps/backend-rag/backend/services/research_os/synthesis.py apps/backend-rag/backend/tests/services/research_os/test_action_item_adapter.py"
    exit: 0
    ts: "2026-08-24T07:20:00Z"
    seat: "session (pro)"
  - claim: "PENDING-ARMS.md diff is exactly +2/-0 (one blank separator + one new row, matching the file's own entry-separation convention)"
    cmd: "git diff --numstat -- .claude/skills/modus/PENDING-ARMS.md"
    exit: 0
    ts: "2026-08-24T07:32:00Z"
    seat: "session (pro)"
  - claim: "The full research_os adapter test suite passes, including the new pending_ruling test (9/9, was 8/8 before this correction)"
    cmd: "PYTHONPATH=. .venv/bin/python3 -m pytest backend/tests/services/research_os/ -q --no-header"
    exit: 0
    ts: "2026-08-24T07:15:00Z"
    seat: "session (pro)"
  - claim: "ruff check and ruff format --check both pass clean on the 3 touched files after auto-reformatting"
    cmd: "python3 -m ruff check backend/services/research_os/action_item_adapter.py backend/services/research_os/synthesis.py backend/tests/services/research_os/test_action_item_adapter.py && python3 -m ruff format --check <same 3 files>"
    exit: 0
    ts: "2026-08-24T07:18:00Z"
    seat: "session (pro)"
  - claim: >-
      Empirically measured (not assumed) that extensions participates in object_hash, and the
      exact before/after hash values on the identical fixture row, BEFORE building the
      pending_ruling channel, per the conductor's explicit prior instruction
    cmd: >-
      PYTHONPATH=. .venv/bin/python3 -c "construct two ActionItem model_construct drafts
      differing only in extensions payload, compute object_hash() on each, compare"
    exit: 0
    ts: "2026-08-24T06:40:00Z"
    seat: "session (pro)"
  - claim: "Zero hardcoded object_hash literal anywhere that this correction would silently break"
    cmd: "git grep -n '305489949ff73447\\|object_hash ==' -- apps/backend-rag/backend/tests apps/backend-rag/backend/services/research_os"
    exit: 1
    ts: "2026-08-24T07:22:00Z"
    seat: "session (pro)"
    # exit 1 = grep found zero hardcoded hash literals -- confirmed absence, not a failed command.
  - claim: "The not-yet-opened parity-probe slice (3b, this same worktree) never compares object_hash field-by-field, only individual adapted values"
    cmd: "grep -n 'object_hash' backend/services/research_os/parity.py"
    exit: 1
    ts: "2026-08-24T07:23:00Z"
    seat: "session (pro)"
    # exit 1 = zero references to object_hash in parity.py -- confirmed, the parity probe
    # compares mapped field values, not the hash.
  - claim: "pending_ruling and unbacked_refs are both absent from V1_RESERVED_EXTENSION_FIELD_NAMES, so validate_extensions() accepts this payload"
    cmd: 'PYTHONPATH=. .venv/bin/python3 -c "from research_os.primitives import V1_RESERVED_EXTENSION_FIELD_NAMES; print(''unbacked_refs'' in V1_RESERVED_EXTENSION_FIELD_NAMES, ''pending_ruling'' in V1_RESERVED_EXTENSION_FIELD_NAMES)"'
    exit: 0
    ts: "2026-08-24T06:20:00Z"
    seat: "session (pro)"
  - claim: "The frozen D3 compatibility matrix's item 0 and item 6 text was read directly this session, not transcribed from the reviewer's paraphrase"
    cmd: "grep -n 'Ruling must decide' -A4 <matrix-scratchpad-path> ; grep -n 'current_intent_ref' <matrix-scratchpad-path>"
    exit: 0
    ts: "2026-08-24T06:05:00Z"
    seat: "session (pro)"
  - claim: "operations-repository.ts's 24h authorization-window TTL check was read directly, grounding the expires_at correction in measured code, not the reviewer's own softer framing"
    cmd: "sed -n '685,715p' apps/bali-zero-magazine/lib/server/operations-repository.ts"
    exit: 0
    ts: "2026-08-24T06:10:00Z"
    seat: "session (pro)"
  - claim: "New PENDING-ARMS.md row parses cleanly, zero PHANTOM-OPERATOR/MALFORMED introduced"
    cmd: "python3 scripts/pending_arms_report.py --strict-phantom"
    exit: 0
    ts: "2026-08-24T07:35:00Z"
    seat: "session (pro)"
  - claim: "pending_arms_report.py's own test suite (100 tests) still passes after the ledger edit"
    cmd: "PYTHONPATH=. .venv/bin/python3 -m pytest ../../scripts/tests/test_pending_arms_report.py -q --no-header"
    exit: 0
    ts: "2026-08-24T07:36:00Z"
    seat: "session (pro)"
  - claim: >-
      Fresh three-way hash measurement (extensions omitted / extensions={} / extensions
      populated), holding every other ActionItem field constant, done via direct construction
      -- not recalled from an earlier turn -- to ground the second finding this session
      surfaced while measuring the first
    cmd: >-
      PYTHONPATH=. .venv/bin/python3 -c "build the same ActionItem 3 ways varying only
      extensions, print object_hash and model_fields_set membership for each"
    exit: 0
    ts: "2026-08-24T08:05:00Z"
    seat: "session (pro)"
    # result: A) omitted -> a42cea7f2d2bb8926e4815913a487ccd4fb634e71557fc0af8ec93205dcb7663
    # (not in model_fields_set); B) extensions={} -> ed9d45a70ea3ae5d8afda9a5028cf66366e89a19a688f314d9e880ca18abac3f
    # (in model_fields_set); C) populated -> c198815e24ddc7bf8005081788e61fbadf03bbcf389df0b7532b9f1868588c4b
    # (in model_fields_set). All three distinct, confirming presence (not just value) drives
    # object_hash.
  - claim: "hashing.py:3-5 and :28 quoted verbatim before citing them in the new invariant paragraph, not paraphrased from the conductor's message"
    cmd: "sed -n '1,35p' packages/research-os-core/research_os/hashing.py"
    exit: 0
    ts: "2026-08-24T07:55:00Z"
    seat: "session (pro)"
  - claim: >-
      Guilt-arm proof: removing the extensions= kwarg from action_item_adapter.py's real call
      site makes BOTH the new invariant test AND the pre-existing pending_ruling test fail red
      on the real adapter output; file restored afterward and full suite re-verified green
    cmd: >-
      sed removes the `extensions=unbacked_refs_extension(...)` block from
      action_item_adapter.py; pytest -k extensions_is_always_explicitly_set and -k
      pending_ruling_is_machine_checkable both re-run; file restored from backup; full suite
      re-run
    exit: 0
    ts: "2026-08-24T08:10:00Z"
    seat: "session (pro)"
    # both targeted tests FAILED as expected while extensions= was removed (one AssertionError,
    # one TypeError: 'NoneType' object is not subscriptable) -- exit 0 above refers to the
    # overall verification procedure completing as designed (guilt confirmed, file restored,
    # suite green again), not to the intentionally-red pytest runs themselves.
  - claim: "The cited sibling ledger row (ContentObject null-contract, same presence-preserving-semantics family, opposite direction) genuinely exists on PENDING-ARMS.md before citing it, not trusted from the conductor's paraphrase"
    cmd: "grep -n 'ContentObject' .claude/skills/modus/PENDING-ARMS.md"
    exit: 0
    ts: "2026-08-24T08:15:00Z"
    seat: "session (pro)"
  - claim: "Full adapter test suite (now 10 tests, was 9) passes after the invariant addition, and ruff check/format stay clean"
    cmd: "PYTHONPATH=. .venv/bin/python3 -m pytest backend/tests/services/research_os/ -q --no-header && python3 -m ruff check backend/services/research_os/synthesis.py backend/tests/services/research_os/test_action_item_adapter.py && python3 -m ruff format --check <same 2 files>"
    exit: 0
    ts: "2026-08-24T08:12:00Z"
    seat: "session (pro)"
  - claim: >-
      Gate-round Gap 1 (conductor's review): a NEW package-wide structural test
      (test_adapter_extensions_invariant.py) AST-parses every .py file in
      backend.services.research_os for build_with_object_hash(...) calls and asserts each
      passes extensions= explicitly -- closes the gap that the per-adapter behavioural test
      only protects THIS adapter, not a future sibling adapter that copies the same pattern
    cmd: "PYTHONPATH=. .venv/bin/python3 -m pytest backend/tests/services/research_os/test_adapter_extensions_invariant.py -v --no-header"
    exit: 0
    ts: "2026-08-24T09:05:00Z"
    seat: "session (pro)"
  - claim: >-
      Guilt-arm of the new structural test: removing extensions= from the real call site makes
      it fail, naming the EXACT offending file:line in the assertion message (not just "some
      test failed") -- confirmed on the real adapter source, then restored and full suite
      re-verified green (11/11)
    cmd: "sed removes the extensions=unbacked_refs_extension(...) block from action_item_adapter.py; pytest test_adapter_extensions_invariant.py -v; file restored from backup; full suite re-run"
    exit: 0
    ts: "2026-08-24T09:08:00Z"
    seat: "session (pro)"
    # the intentionally-red run failed with: "AssertionError: build_with_object_hash call(s)
    # omit extensions=, changing this adapter's canonical object identity by accident:
    # ['action_item_adapter.py:152']" -- exit 0 above refers to the overall guilt-arm procedure
    # completing as designed, not to that intentionally-red pytest invocation itself.
  - claim: >-
      Gate-round Gap 2 (conductor's review): unbacked_refs_extension()'s payload shape changed
      (gained the optional pending_ruling key) while extension_version stayed the hardcoded
      literal "1.0.0" on both shapes -- two shapes claiming one version number. Fixed by
      introducing UNBACKED_REFS_EXTENSION_VERSION = "1.1.0" (additive/minor bump) used in place
      of the literal, and updating the one pre-existing test that asserted "1.0.0"
    cmd: "grep -rn 'UNBACKED_REFS_EXTENSION\\|extension_version' backend/services/research_os/synthesis.py backend/tests/services/research_os/test_action_item_adapter.py"
    exit: 0
    ts: "2026-08-24T09:12:00Z"
    seat: "session (pro)"
  - claim: >-
      No other file in the repo hardcodes "1.0.0" tied to the
      com.balizero.research-os-adapters namespace or UNBACKED_REFS_EXTENSION -- the version
      bump does not silently break an assertion this session failed to find, independently
      re-verified via a dispatched read-only agent, not only this session's own earlier grep
    cmd: 'grep -rn "com.balizero.research-os-adapters" --include="*.py" --include="*.md" --include="*.yml" . ; grep -rn "UNBACKED_REFS_EXTENSION" --include="*.py" .'
    exit: 0
    ts: "2026-08-24T09:15:00Z"
    seat: "session (pro), cross-checked by a dispatched read-only sub-agent"
  - claim: "Full test suite (now 11 tests, was 10) passes after both gate-round fixes; ruff check/format clean on every touched and new file"
    cmd: "PYTHONPATH=. .venv/bin/python3 -m pytest backend/tests/services/research_os/ -q --no-header && python3 -m ruff check backend/services/research_os/synthesis.py backend/tests/services/research_os/test_action_item_adapter.py backend/tests/services/research_os/test_adapter_extensions_invariant.py && python3 -m ruff format --check <same 3 files>"
    exit: 0
    ts: "2026-08-24T09:18:00Z"
    seat: "session (pro)"
tests:
  - "backend/tests/services/research_os/test_action_item_adapter.py -- 7 pre-existing tests
    unaffected + 1 updated (test_unbacked_refs_are_machine_checkable_not_only_prose, now
    expects extension_version=='1.1.0') + 2 new tests:
    test_pending_ruling_is_machine_checkable_not_only_prose (asserts
    extensions['com.balizero.research-os-adapters'].payload['pending_ruling'] ==
    {'priority', 'sla.due_at', 'current_intent_ref'}) and
    test_extensions_is_always_explicitly_set_never_omitted (asserts 'extensions' in
    item.model_fields_set -- guilt-verified this session to fail red when the extensions= kwarg
    is removed from the real call site)."
  - "backend/tests/services/research_os/test_adapter_extensions_invariant.py -- NEW, 1 test
    (test_every_build_with_object_hash_call_passes_extensions_explicitly): AST-scans every .py
    file in the package for build_with_object_hash(...) calls, asserts each passes extensions=
    explicitly -- guilt-verified to name the exact offending file:line, not just 'a test
    failed', when the kwarg is removed from the real call site."
  - "backend/tests/services/research_os/ (full suite, 11 tests) -- exit 0."
  - "scripts/tests/test_pending_arms_report.py (100 tests) -- exit 0, confirms the ledger append
    does not break the report's own parser."
static:
  - "ruff check: 0 issues on every touched/new file (action_item_adapter.py, synthesis.py,
    test_action_item_adapter.py, test_adapter_extensions_invariant.py)."
  - "ruff format --check: 0 issues (some files needed --fix after manual edits, re-verified
    clean afterward, then re-ran pytest each time to confirm the reformat changed no
    behaviour)."
runtime_proof:
  - >-
    Same as PR #4749 (no live consumer, by design -- zero non-test consumers of
    backend.services.research_os anywhere in the repo, re-confirmed this session with a
    precise import-level grep after an earlier bare-name grep produced a false positive against
    an unrelated pre-existing ActionItem dataclass in apps/backend-rag/backend/services/naga).
    The functional proof is the adapter's own round-trip, re-run after this correction: a
    synthetic ops_intents row -> ActionItem with priority/sla.due_at/current_intent_ref
    unchanged in value, and extensions.payload carrying BOTH unbacked_refs and pending_ruling
    in the same dict, verified by direct execution and printed above in brief.yml.
dissent: []
lanes:
  - lane: "p04-d3-magazine-action-chain-adapters-correction"
    role: build
    seat: "claude-sonnet-5"
residual_risks:
  - >-
    The pending ruling itself (matrix items 0 and 6) is not resolved by this PR -- it is a
    business decision (Legge 5), recorded as a new PENDING-ARMS.md row, owned by Zero via the
    conductor. Slices 2 (ActionIntent), 3a (OperationalReceipt), 3b (shadow+parity) remain
    frozen/unopened pending this correction landing, per the conductor's explicit instruction,
    and must not replicate the same "matrix as approval" citation defect for any of their own
    fields that similarly await a ruling.
  - >-
    object_hash changes for every ActionItem this adapter has ever produced or will produce
    before vs. after this correction (measured, see diff.behaviour_change above). Harmless
    today; flagged so a future persistence/parity PR does not discover it as a surprise.
sources:
  - "Independent reviewer's REFUSE verdict (d4-VERDICT.md), read in full this session -- claims
    #10/#11/#12 specifically, plus the surrounding AGREE findings (#9, #13) that confirm the
    measurement work (priority/SLA absence, 1:1 receipt fusion) was not itself defective."
  - "The frozen D3 compatibility matrix (d3-matrix-FROZEN), items 0 and 6 and §1.2's field
    table, read directly and quoted verbatim in the corrected code comments, not transcribed
    from the reviewer's paraphrase."
  - "apps/bali-zero-magazine/lib/server/operations-repository.ts:685-715, read directly this
    session to ground the expires_at TTL correction in measured code."
  - "packages/research-os-core/research_os/hashing.py, read in full to confirm object_hash's
    exclude_unset=True mechanics before empirically verifying the extensions consequence."
  - "Fresh gate/test runs in this worktree, this session (receipts above) -- not recalled from
    memory or an earlier turn."
pii_scan: clean
size_tokens: 2100
