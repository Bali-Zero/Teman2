brief_ref: evidence/brief.yml
plan_ref: >-
  SQUAD H lane 4 of 5 — L03-PR2 of
  docs/plans/2026-08-29-beyond-sota-craft-wave/L03-architecture-decision-making.md,
  under the wave-2 rules in 01-INVENTORY-AND-WAVE-2-PLAN.md §5.1. Ledger: issue #5320.

lanes:
  - lane: craft-h-doctrine-citations
    role: build
    seat: claude-opus-5 (conducting session, M5 — the corpus search, the three cures, the arming, the gate)
  - lane: craft-h-citation-lint-build
    role: build
    seat:
      kimi-for-coding (non-Anthropic build lane — authored scripts/lint_doctrine_citations.py;
      graded by the conducting session, never by itself)
  - lane: craft-h-citation-refute-a
    role: review
    seat: codex gpt-5.6-sol at xhigh, --sandbox read-only
  - lane: craft-h-citation-refute-b
    role: review
    seat: kimi-code/k3

council_run: journal.jsonl

diff:
  files: 6
  net_lines: 620
  measured_at: staged tree, pre-PR
  note: >-
    Most of it is the lint and its 12 cases. The doctrine edits are three
    retraction notes. CI's --numstat-file is authoritative over this figure.

pii_scan: clean

receipts:
  - claim:
      The phantom the spec named is real, and it is one of three — the corpus was
      searched this session rather than remembered.
    cmd: git ls-tree -r origin/main --name-only | grep -c 2026-05-30-sota-ai-architecture-methodology
    result:
      "0. Same for the other two cited files: a 2026-05-31 global-skills study
      (skill-catalog) and a 2026-04-25 migrations strategy (AUTONOMOUS_OPS.md) —
      `git ls-tree | grep -iF <basename>` returns nothing for either, anywhere in the
      tree, so there was no renamed file to repoint to. The one keyword-plausible
      substitute for the sota axioms, 2026-06-30-claude-code-perfect-session-doctrine.md,
      matched only on ordinary uses of the word 'adversarial' and does not state them."
    exit: 0
    ts: 2026-08-31T02:41Z
    seat: session (M5, orchestrator lane)

  - claim: The lint is red on the pre-cure file and green on this branch, with no allowlist.
    cmd: python3 scripts/lint_doctrine_citations.py
    result:
      "clean tree: rc=0, zero output lines. With origin/main's copy of the skill file
      restored in place: rc=1 and exactly one line naming
      `.claude/skills/sota-architecture-loop/SKILL.md:13` and the missing path.
      Exit codes captured directly, not through a pipe."
    exit: 0
    ts: 2026-08-31T02:52Z
    seat: session (M5, orchestrator lane)

  - claim: Every guard is proven both ways and each is proven CAPABLE of failing.
    cmd: PYTHONDONTWRITEBYTECODE=1 python -B -m pytest scripts/tests/test_lint_doctrine_citations.py -p no:cacheprovider
    result:
      "12 passed. Mutation 7/7: restoring the no-op mask, dropping the extension
      requirement, un-anchoring the path start, disabling the fence exclusion, treating
      a command span as a citation, letting an empty corpus read clean, and adding a
      subject the workflow filter cannot reach each turn a test red. The un-anchoring
      mutation SURVIVED the first battery — every fixture used backticks and so took
      the span recogniser, leaving the regex that carried the defect unexercised; a
      bare-prose variant now kills it."
    exit: 0
    ts: 2026-08-31T03:02Z
    seat: session (M5, orchestrator lane)

  - claim:
      The job starts on every file the lint scans, and the relation is asserted rather
      than maintained by hand.
    cmd: actionlint .github/workflows/immune-enforcement.yml
    result:
      "0 findings after narrowing a glob that shadowed an existing pattern (actionlint
      caught `.claude/skills/*` overriding `.claude/skills/modus/PENDING-ARMS.md`).
      Relevance gate simulated verbatim: the lint, its test, any skill SKILL.md, any
      command, and the root doctrine files each start the job; README.md does not.
      `test_the_workflow_filter_covers_every_file_the_lint_scans` compares the lint's
      resolved subject set to the workflow's own case patterns."
    exit: 0
    ts: 2026-08-31T03:05Z
    seat: session (M5, orchestrator lane)

dissent:
  - seat: session (M5, gating the non-Anthropic build lane's output before it ran)
    objection: >-
      The lint as built reported 47 findings on a clean corpus. Four were on
      SYMBIOSIS.md — this repo's most load-bearing document — because it matched
      `docs/` in the MIDDLE of `apps/mata-garuda/docs/X.md`, a path that exists,
      and two more resolved paths belonging to another project entirely. It
      counted bare directories as citations, reported findings up to four times,
      and its region mask was a NO-OP: `ch if mask[i] else line[i]` over
      `enumerate(line)`, where `ch` IS `line[i]`, so the mask was computed, read
      as masking, and masked nothing.
    status: CONFIRMED
    resolution: >-
      accepted-and-fixed before the lint saw the corpus for real — paths anchored
      at a token boundary, a file extension required, the three recognisers made
      to cover disjoint regions, and a command span judged by its first word
      rather than by whatever sits next to the path. Had this shipped, the lint
      would have been red on SYMBIOSIS.md from its first run, which is precisely
      how a guard gets disabled.
  - seat: kimi-code/k3 (cross-family refuter)
    objection: >-
      The lint's subject globs reach `.claude/skills/**/*.md` while the workflow's
      path filter listed only `*/SKILL.md`, so a phantom citation added to
      `.claude/skills/modus/AMENDMENTS.md` would have been a finding on a PR that
      never started the finder.
    status: CONFIRMED
    resolution: >-
      accepted-and-fixed, and writing the assertion caught a SECOND escape the
      finding had not named: `CLAUDE.md` is itself a subject and was also absent
      from the filter. The relation is now asserted by
      `test_the_workflow_filter_covers_every_file_the_lint_scans` rather than kept
      in sync by hand, and mutation-proved by adding a subject the filter cannot
      reach. Same shape, and the same reason, as tg-gateway's trigger-symmetry test.
