Reading additional input from stdin...
OpenAI Codex v0.152.0
--------
workdir: /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 01a06ccb-f958-7972-bd99-bdd8ce3a8275
--------
user
Adversarially review this CI diff for the Nuzantara repo. Assume it is defective and try to prove it. No praise. Under 400 words.

CONTEXT (measured, not claimed):
`.github/workflows/merah-putih-day-contrast.yml` is a brand-contrast guard being promoted to a REQUIRED status check. A required context cannot carry a `paths:` filter (it would never report on PRs it does not match and the merge queue would hang), so it triggers on every PR and a first-step "sentinel" decides whether to do the work, reading its guarded-path list out of the workflow's own `push.paths` block at run time.

A Gear-3 gate passed the promotion WITH TWO CONDITIONS; this diff implements them.

Condition 1: `cancel-in-progress` was unconditionally `true`, now `${{ github.event_name != 'merge_group' }}`.
Condition 2: the sentinel reads the path list from the BASE ref's copy of the workflow (a list the PR cannot edit) unioned with the PR's own; but `if base.returncode == 0` treated "base ref unreachable" and "base has no such file" as the same event and fell back to the PR-controlled head list in BOTH. Now separated with `git cat-file -e`; unreachable base runs the whole guard (fail closed).

ATTACK THESE:
1. IMPORTANT — I measured that queue refs are `gh-readonly-queue/main/pr-<N>-<sha>`, so two entries NEVER share `github.ref` and cannot cancel each other. If so, condition 1 is a fix for a bug that does not exist. Is my measurement right? Is there ANY event shape where two runs of this workflow share that concurrency key and one cancels a required report — re-arming the same PR, a push racing a queue entry, `pull_request` vs `merge_group` on the same sha?
2. The fail-closed path prints "true" and exits 0. Is exiting 0 correct, or should an unreadable base be a hard error? What if `BASE_SHA` is empty or unset — `os.environ["BASE_SHA"]` raises KeyError, the step dies under `set -euo pipefail`, `RUN` is empty and `echo "run=" >> $GITHUB_OUTPUT`. What does the job then do — skip everything? That would be fail-OPEN through a different door.
3. `git cat-file -e "{sha}^{{commit}}"` — is the brace-expansion right in this context, and does it distinguish the two cases reliably on a shallow/`blob:none` checkout, which this workflow uses (`fetch-depth: 0`, `filter: blob:none`)? Does `cat-file -e` on a BLOB need the blob fetched?
4. The innocence test locates a base commit predating the workflow via `git rev-list --max-parents=1 HEAD -- <path>` and takes its parent. Is that reliable in CI (shallow clone, squash-merge history)? It `pytest.skip`s when it cannot — is a skip acceptable here or does it hide a real regression?

Verdict: BLOCK / PASS-WITH-CONDITIONS / PASS, and name the single most likely real defect.
diff --git a/.github/workflows/merah-putih-day-contrast.yml b/.github/workflows/merah-putih-day-contrast.yml
index f918233620..0fa74acc9a 100644
--- a/.github/workflows/merah-putih-day-contrast.yml
+++ b/.github/workflows/merah-putih-day-contrast.yml
@@ -125,8 +125,17 @@ permissions:
   contents: read
 
 concurrency:
+  # `cancel-in-progress` is CONDITIONAL on the event, and the condition is the
+  # whole point: on a merge_group event `github.ref` is the QUEUE's own ref, so a
+  # second entry landing while this one is mid-flight would cancel the first —
+  # and a cancelled required context does not report, which does not delay the
+  # queue, it tears the entry out of it. On a pull_request the ref is the PR's
+  # own, where cancelling a superseded push is exactly the saving we want.
+  # Promotion condition 1 of 2 from the Gear-3 gate's PASS-WITH-CONDITIONS on
+  # #5614; `test_the_concurrency_group_never_cancels_a_merge_group_run` is what
+  # keeps this from silently reverting to a bare `true`.
   group: merah-putih-day-contrast-${{ github.ref }}
-  cancel-in-progress: true
+  cancel-in-progress: ${{ github.event_name != 'merge_group' }}
 
 jobs:
   contrast-guard:
@@ -168,7 +177,7 @@ jobs:
           # one this promotion exists to make green, went red instead (found by
           # the Gear-3 council, 2026-09-04). The snippet prints exactly one word.
           RUN="$(BASE_SHA="$BASE_SHA" CHANGED_FILE=/tmp/changed.txt python3 - <<'PY'
-          import fnmatch, os, pathlib, subprocess, yaml
+          import fnmatch, os, pathlib, subprocess, sys, yaml
 
           WORKFLOW = ".github/workflows/merah-putih-day-contrast.yml"
 
@@ -185,14 +194,41 @@ jobs:
           # list, skip the corpus, and skip the very test that catches a
           # shrinking list (Gear-3 council finding, 2026-09-04). The base copy is
           # the one the PR cannot edit.
+          #
+          # And when the base copy cannot be read, the sentinel FAILS CLOSED —
+          # it runs the whole guard rather than quietly falling back to the
+          # head-only list the paragraph above just called a trust boundary the
+          # PR controls. `if base.returncode == 0` alone treated "the base ref is
+          # unreachable" and "the base genuinely has no such file" as the same
+          # event and skipped the protection in both (promotion condition 2 of 2
+          # from the Gear-3 gate's PASS-WITH-CONDITIONS on #5614). Those two are
+          # separated below, because they are not the same fact: a shallow
+          # checkout or an unfetched base sha is a broken measurement and must
+          # not be believed, while a PR that INTRODUCES this workflow has no base
+          # copy by construction and head-only is the correct reading for it.
+          # `git cat-file -e` is the probe for both — `git rev-parse <sha>:<path>`
+          # would answer with a hash-shaped non-answer either way.
+          def _git(*args):
+              return subprocess.run(["git", *args], capture_output=True, text=True)
+
+          def _run_everything(why: str):
+              print("true")
+              sys.stderr.write(f"sentinel failing closed: {why}\n")
+              sys.exit(0)
+
           head_copy = pathlib.Path(os.environ.get("HEAD_WORKFLOW", WORKFLOW)).read_text()
           patterns = set(guarded_paths(head_copy))
-          base = subprocess.run(
-              ["git", "show", f"{os.environ['BASE_SHA']}:{WORKFLOW}"],
-              capture_output=True, text=True,
-          )
-          if base.returncode == 0:
-              patterns |= set(guarded_paths(base.stdout))
+          base_sha = os.environ["BASE_SHA"]
+          if _git("cat-file", "-e", f"{base_sha}^{{commit}}").returncode != 0:
+              _run_everything(f"base ref {base_sha} is not reachable in this checkout")
+          if _git("cat-file", "-e", f"{base_sha}:{WORKFLOW}").returncode == 0:
+              base = _git("show", f"{base_sha}:{WORKFLOW}")
+              if base.returncode != 0:
+                  _run_everything("the base copy of this workflow could not be read")
+              try:
+                  patterns |= set(guarded_paths(base.stdout))
+              except Exception as exc:  # noqa: BLE001 — any parse failure is unreadable base
+                  _run_everything(f"the base copy of this workflow did not parse: {exc}")
           assert patterns, "no guarded paths parsed — refusing to declare a no-op"
           changed = [l for l in pathlib.Path(os.environ["CHANGED_FILE"]).read_text().splitlines() if l]
           # fnmatch has no `**`; `*` already crosses `/` in it, which makes it a
diff --git a/scripts/tests/test_merah_putih_day_contrast.py b/scripts/tests/test_merah_putih_day_contrast.py
index fb52fe6c60..0167d69480 100644
--- a/scripts/tests/test_merah_putih_day_contrast.py
+++ b/scripts/tests/test_merah_putih_day_contrast.py
@@ -744,11 +744,117 @@ def test_a_pr_that_narrows_its_own_list_cannot_disarm_the_sentinel(tmp_path) ->
 
 
 def test_the_sentinel_reads_the_base_copy_via_git_show() -> None:
+    """The static half of the base-copy guarantee; the executable half is
+    `test_a_pr_that_narrows_its_own_list_cannot_disarm_the_sentinel`.
+
+    This used to assert the literal `"git", "show"`, which pinned the SPELLING
+    rather than the mechanism: wrapping the same call in a `_git(*args)` helper
+    — as the fail-closed cure did — broke it while the sentinel still read the
+    base copy exactly as before. Rewritten to name the two things that actually
+    have to be true (a `show` of the base ref's own copy of this workflow, at
+    `BASE_SHA`), so a helper rename passes and REMOVING the base read fails.
+    """
     body = (
         WORKFLOW.read_text(encoding="utf-8")
         .split("Did a guarded surface change?", 1)[1]
         .split("- name:", 1)[0]
     )
-    assert '"git", "show"' in body and 'BASE_SHA' in body, (
+    assert '"show"' in body and "BASE_SHA" in body, (
         "the sentinel no longer reads the base ref's copy of its pattern list"
     )
+    assert "base_sha}:{WORKFLOW}" in body, (
+        "the sentinel no longer addresses the BASE ref's copy of THIS workflow — "
+        "if it now shows some other path or ref, the PR-controlled head copy is "
+        "the only list in play"
+    )
+
+
+# ---------------------------------------------------------------------------
+# The two conditions the Gear-3 gate attached to #5614's PASS-WITH-CONDITIONS.
+# Both were prose in that PR's body and prose is not in force, so each is
+# asserted here — and the sentinel one is EXECUTED rather than grepped, because
+# a fail-closed path that is only described is exactly the shape that fails open
+# the first time anyone reads it.
+# ---------------------------------------------------------------------------
+
+
+def test_the_concurrency_group_never_cancels_a_merge_group_run() -> None:
+    """Condition 1. `github.ref` on a merge_group event is the QUEUE's ref, not
+    this PR's — so a bare `cancel-in-progress: true` lets the queue's next entry
+    cancel the entry ahead of it. A cancelled required context does not report a
+    failure, it reports nothing, and the entry is torn out of the queue rather
+    than merely delayed. Cancelling is right on `pull_request`, where the ref is
+    the PR's own and the superseded run is genuinely dead work.
+    """
+    import yaml
+
+    doc = yaml.safe_load(WORKFLOW.read_text(encoding="utf-8"))
+    concurrency = doc.get("concurrency")
+    assert concurrency, f"{WORKFLOW.name}: the concurrency block is gone"
+
+    cancel = str(concurrency.get("cancel-in-progress", "")).strip()
+    assert cancel not in {"True", "true"}, (
+        f"{WORKFLOW.name}: `cancel-in-progress` is unconditionally true again. On "
+        "a merge_group event the group key is the queue's own ref, so this "
+        "cancels the entry ahead of this one and a cancelled required context "
+        "never reports."
+    )
+    assert "github.event_name" in cancel and "merge_group" in cancel, (
+        f"{WORKFLOW.name}: `cancel-in-progress` is {cancel!r} — it must be an "
+        "expression that turns cancelling OFF for merge_group specifically, not "
+        "a blanket value."
+    )
+
+
+def test_the_sentinel_runs_the_guard_when_the_base_ref_cannot_be_read(tmp_path) -> None:
+    """Condition 2, guilt. An unreachable base sha (shallow checkout, unfetched
+    ref) means the trust boundary — reading the pattern list from the copy the PR
+    cannot edit — could not be applied. The sentinel must then run the WHOLE
+    guard, not fall back to the head-only list it just declined to trust.
+
+    Guilt, not innocence: the change below touches nothing guarded, so the
+    head-only reading would print `false` and skip the corpus. Fail-closed is
+    the only way `true` comes out of this.
+    """
+    res = _run_sentinel(
+        tmp_path,
+        ["README.md"],
+        BASE_SHA="0000000000000000000000000000000000000000",
+    )
+    assert res.returncode == 0, res.stderr
+    assert res.stdout == "true\n", (
+        "the sentinel could not read the base ref and still declared a no-op — "
+        "it failed OPEN. An unreadable base means the PR-controlled head copy is "
+        f"the only list available, which is precisely what must not be trusted. "
+        f"stdout={res.stdout!r} stderr={res.stderr!r}"
+    )
+
+
+def test_a_pr_that_introduces_this_workflow_is_not_forced_through_the_guard(tmp_path) -> None:
+    """Condition 2, innocence. "The base has no copy of this file" is NOT the
+    same event as "the base cannot be read", and conflating them would make the
+    fail-closed path fire on every PR that adds a workflow — the guard would run
+    its full corpus on unrelated changes forever.
+
+    Here the base ref IS reachable (HEAD) but the workflow is read from a path
+    that does not exist in it, so only the head list applies and an unguarded
+    change must still come back `false`.
+    """
+    import subprocess
+
+    empty_base = subprocess.run(
+        ["git", "commit-tree", "-m", "empty", subprocess.run(
+            ["git", "hash-object", "-t", "tree", "/dev/null"],
+            capture_output=True, text=True, cwd=REPO, check=True,
+        ).stdout.strip()],
+        capture_output=True, text=True, cwd=REPO, check=True,
+    ).stdout.strip()
+
+    res = _run_sentinel(tmp_path, ["README.md"], BASE_SHA=empty_base)
+    assert res.returncode == 0, res.stderr
+    assert res.stdout == "false\n", (
+        "a reachable base that simply does not contain this workflow was treated "
+        "as an unreadable base — the fail-closed path is over-matching, and every "
+        "PR introducing a workflow now runs the full corpus. "
+        f"stdout={res.stdout!r} stderr={res.stderr!r}"
+    )
warning: Skill descriptions were shortened to fit the skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
hook: SessionStart
hook: SessionStart Failed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'echo "Machine: $(whoami)@$(hostname)" && case "$(hostname)" in Nuzantara) OTHER=mini ;; Mini-Pro2) OTHER=pro ;; Air-M5) OTHER=pro ;; *) OTHER=pro ;; esac && ssh -o ConnectTimeout=3 $OTHER '"'echo \"Peer: "'$(whoami)@$(hostname)"'"' 2>/dev/null || echo \"Peer: UNREACHABLE\" && LOCAL_HEAD="'$(git log --oneline -1 2>/dev/null) && REMOTE_HEAD=$(ssh -o ConnectTimeout=3 $OTHER '"'cd ~/nuzantara 2>/dev/null; git log --oneline -1' 2>/dev/null) && if [ \""'$LOCAL_HEAD" = "$REMOTE_HEAD" ]; then echo "Git sync: OK ($LOCAL_HEAD)"; else echo "Git sync: OUT OF SYNC! Local=$LOCAL_HEAD Remote=$REMOTE_HEAD"; fi' in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 exited 255 in 170ms:
Machine: balizero@Air-M5
Peer: UNREACHABLE

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
codex
[Air-M5] Faccio una review adversarial read-only del workflow completo e dei test, con attenzione a semantica GitHub Actions, propagazione di `RUN` e comportamento Git su clone parziale. Il Pro è irraggiungibile, ma questa verifica statica/locale è leggera e resta sull’M5.
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'rg -n "merah-putih|day-contrast|Gear-3|merge_group|gate conditions|#5614" /Users/balizero/.codex/memories/MEMORY.md' in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 succeeded in 20ms:
1049:- WR3 spend gate, `assert_spend_authorized()`, Gear-3, evidence pack, CI wiring

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,280p' .github/workflows/merah-putih-day-contrast.yml && sed -n '650,900p' scripts/tests/test_merah_putih_day_contrast.py" in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 succeeded in 10ms:
name: Merah Putih DAY contrast guard

# WHY THIS FILE EXISTS: the guard it runs was written, merged, and never armed.
#
# `scripts/tests/test_merah_putih_day_contrast.py` is the machine-checked identity
# law for the Merah Putih DAY palette — every text/ground pair on the public
# second-home surfaces must clear WCAG AA, and no retired colour or Montserrat face
# may reappear there. It has been on `main` since the migration landed, it passes
# (42 tests), and it demonstrably goes red on both a contrast drift and a
# `color-mix`-derived colour. What it has never had is a trigger.
#
# Measured on origin/main 2026-08-31 by reading all 116 workflow files' contents
# (not `git grep <ref> -- <path>`, which returns empty for BOTH a real miss and a
# broken probe — the first attempt at this measurement had a positive control that
# also came back empty, and was thrown away rather than believed): ZERO workflows
# named `merah_putih_day_contrast`. The only place it executed at all was
# `scripts-tests-sweep.yml`, which is `schedule:`-only, `continue-on-error: true`,
# not required, and says in its own header that it gates nothing. So a PR touching
# only `merahPutihDayVars.ts` started no blocking check on it whatsoever.
#
# That is cicatrix family #2 — "esiste != armato" — and the token file's own
# doc-comment made the stronger claim that the guard "fails the build if any pair
# below drifts", which was simply false as measured. This workflow makes it true.
#
# THE PATHS FILTER IS DERIVED FROM WHAT THE GUARD ACTUALLY READS, not from what
# seems related — and the first version of this list got that wrong in exactly the
# way this paragraph warns about. It named `merahPutihDayVars.ts`, the `*.tsx`
# tree under `app/visa/second-home/`, and `packages/core/tokens/semantic.css`, and
# stopped there. But `_perimeter_sources()` also resolves every `@/components/...`
# import it finds in the two wrappers and scans those files too — today
# `ConsentBanner.tsx` and `WhatsAppLeadButton.tsx`, which is precisely how the
# guard caught a 2.56:1 consent bar. A real regression in either would have
# started NO check. Caught by a cross-family refuter (kimi-code/k3) before this
# shipped, confirmed by reading the guard's source.
#
# `apps/mouth/src/components/**` is a TRUE superset, not a second hand-kept list:
# the resolver's regex is anchored to `@/components/`, so every import it can ever
# resolve lands under that prefix. The guard's own docstring is blunt about why
# this matters — "a hand-kept list would rot on its first commit" — which is
# exactly what it did.
#
# And the sync is now MACHINE-CHECKED rather than asserted in prose:
# `test_the_workflow_trigger_covers_every_file_this_guard_reads` parses this
# file's own `paths:` block and fails if any file the guard reads is not matched
# by a pattern in it. The test file and this workflow are in the filter too, so a
# change to either re-runs the pair.
#
# PROMOTION, 2026-09-03 — this is that own PR, and it is HALF of the promotion.
# The 2026-08-31 note below refused to make the context required, on the ground that
# doing so obliges a `merge_group:` trigger and that the trigger is a queue cost the
# 2026-08-27 slim cure exists to prevent. The refusal was right about the mechanism
# and wrong about the price, and the repo's own required guards say so: BOTH
# `guard-conformance.yml` and `organ-conformance.yml` are required, both carry
# `merge_group:`, and both pay for it with a SENTINEL — a bare `pull_request:` with
# no `paths:`, and one cheap in-job step that answers "did a guarded surface change"
# and short-circuits the whole job when the answer is no. A queue entry that touches
# nothing here costs a checkout and a diff, not a test run.
#
# So this file now has that shape. What it does NOT yet have is the branch-protection
# entry, and the ORDER is the load-bearing part: adding the context to protection
# BEFORE this shape is on `main` jams the queue on the first entry, because the
# required context does not exist on the queue's ref yet. Sequence: (1) merge this;
# (2) observe an actual `merge_group` run of this workflow; (3) then, and only then,
# add "Merah Putih DAY palette holds its contrast law" to branch protection and
# re-snapshot `infra/required.d/contexts.json` (that file is a GENERATED snapshot of
# live protection — `scripts/ci/snapshot_required_contexts.py` — so hand-editing it
# arms nothing at all; the mandate that described adding a line there as the promotion
# was naming the mirror, not the lever).
#
# The two properties promotion depends on are no longer prose:
# `test_the_workflow_has_the_shape_a_required_context_must_have` fails if
# `pull_request` regains a `paths:` filter or if `merge_group:` is removed, and
# `test_the_sentinel_reads_the_trigger_list_instead_of_restating_it` fails if the
# sentinel stops reading `push.paths` and starts carrying its own copy.
#
# HISTORICAL (2026-08-31), kept because its measurement is still the reason the
# sentinel exists rather than a bare always-on job — the trigger it argues against
# was added on 2026-09-03, see PROMOTION above. It read: this workflow is advisory —
# it backs no entry in `infra/required.d/contexts.json` and none in branch protection. Zero's
# 2026-08-27 queue-slim cure asks advisory workflows to stay out of the merge queue,
# because every queue entry used to spawn ~50 runs and the gating contexts starved
# behind the ~40 that gate nothing. That cure is UNENFORCED in this direction (its
# test guards only that REQUIRED workflows keep their trigger), so obeying it here
# is a choice, stated rather than assumed — see #5378, which removed exactly such an
# accidental trigger from `with-seat-broker-tests.yml` after it ran 57 times in the
# queue and zero times on a PR, counted between that file landing (19:15:44Z) and the
# first run its own fix triggered. Re-verified 2026-08-31 against the complete run
# list (`--limit 500`, 86 runs returned, so not capped): inside that window exactly
# 57 merge_group and no pull_request run other than the one that closes it.
#
# That number survived being wrongly "corrected" here, and the near-miss is worth a
# line. A count of ALL runs of that workflow gives 72 / 12 / 2, and a reviewer holding
# only those totals reported the 57 as a display-cap error and the zero as plainly
# false. Both readings collapse once the window is applied: the 4 stray pull_request
# runs are from the PR that INTRODUCED the file, at 17:08-17:33Z, before the window
# opens. The lesson is not "the reviewer was wrong" — it is that a scoped claim and an
# unscoped measurement of the same workflow disagree by construction, so the window
# must be read off the claim BEFORE its number is judged.
# `pull_request.paths` and `push.paths` are the same anchored list, so this file can
# never develop the head-green/queue-red asymmetry that jammed the queue that night.

on:
  # NO top-level `paths:` on pull_request — always trigger, the sentinel below
  # gates the work. This is the shape a REQUIRED context must have in this repo
  # (`guard-conformance.yml` and `organ-conformance.yml` are both written this
  # way): a path-filtered required check simply never starts on the PRs it does
  # not match, and GitHub then waits for a context that will never report.
  pull_request:
  merge_group:
  push:
    branches: [main]
    # The SINGLE guarded-path list. The sentinel reads it out of this file at
    # run time rather than restating it as a regex, so the filter and the gate
    # cannot drift apart — the duplication `guard-conformance.yml` carries by
    # hand is exactly what this avoids.
    paths:
      - "apps/mouth/src/lib/theme/merahPutihDayVars.ts"
      - "apps/mouth/src/app/visa/second-home/**"
      - "apps/mouth/src/components/**"
      - "packages/core/tokens/semantic.css"
      - "scripts/tests/test_merah_putih_day_contrast.py"
      - ".github/workflows/merah-putih-day-contrast.yml"

permissions:
  contents: read

concurrency:
  # `cancel-in-progress` is CONDITIONAL on the event, and the condition is the
  # whole point: on a merge_group event `github.ref` is the QUEUE's own ref, so a
  # second entry landing while this one is mid-flight would cancel the first —
  # and a cancelled required context does not report, which does not delay the
  # queue, it tears the entry out of it. On a pull_request the ref is the PR's
  # own, where cancelling a superseded push is exactly the saving we want.
  # Promotion condition 1 of 2 from the Gear-3 gate's PASS-WITH-CONDITIONS on
  # #5614; `test_the_concurrency_group_never_cancels_a_merge_group_run` is what
  # keeps this from silently reverting to a bare `true`.
  group: merah-putih-day-contrast-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'merge_group' }}

jobs:
  contrast-guard:
    name: Merah Putih DAY palette holds its contrast law
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # the sentinel diffs against the PR base
          filter: blob:none # every commit and tree, file contents on demand

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install the parser the sentinel and the filter test both need
        run: python3 -m pip install --quiet --disable-pip-version-check pytest pyyaml

      # The sentinel exists because this context is meant to be REQUIRED, and a
      # required check must report on every PR. It answers one question — did any
      # guarded path change — and it answers it from THIS FILE'S OWN `paths:`
      # list, not from a second copy of it. A hand-kept regex is how the sentinel
      # and the trigger drift into disagreeing, and a sentinel that says "nothing
      # changed" about a file that did is a green check over a real regression.
      - name: Did a guarded surface change?
        id: relevant
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "::notice::non-PR event (${{ github.event_name }}) — running the full guard"
            exit 0
          fi
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          git diff --name-only "$BASE_SHA"...HEAD > /tmp/changed.txt
          # Only the `run=` line may reach $GITHUB_OUTPUT. The first cut piped the
          # whole heredoc there, notice included, and the runner rejects any line
          # that is not NAME=VALUE — so the "nothing guarded changed" path, the
          # one this promotion exists to make green, went red instead (found by
          # the Gear-3 council, 2026-09-04). The snippet prints exactly one word.
          RUN="$(BASE_SHA="$BASE_SHA" CHANGED_FILE=/tmp/changed.txt python3 - <<'PY'
          import fnmatch, os, pathlib, subprocess, sys, yaml

          WORKFLOW = ".github/workflows/merah-putih-day-contrast.yml"

          def guarded_paths(text: str) -> list[str]:
              doc = yaml.safe_load(text)
              # PyYAML is YAML 1.1: a bare `on:` key parses as the boolean True.
              trigger = doc.get("on", doc.get(True))
              return list(trigger["push"]["paths"])

          # The list is read from the BASE ref's copy of this file AND the PR's
          # own copy, unioned. Reading only the PR's copy is a trust boundary the
          # PR itself controls: one diff that narrows `push.paths` and regresses a
          # guarded file would have the sentinel judge itself by its own shortened
          # list, skip the corpus, and skip the very test that catches a
          # shrinking list (Gear-3 council finding, 2026-09-04). The base copy is
          # the one the PR cannot edit.
          #
          # And when the base copy cannot be read, the sentinel FAILS CLOSED —
          # it runs the whole guard rather than quietly falling back to the
          # head-only list the paragraph above just called a trust boundary the
          # PR controls. `if base.returncode == 0` alone treated "the base ref is
          # unreachable" and "the base genuinely has no such file" as the same
          # event and skipped the protection in both (promotion condition 2 of 2
          # from the Gear-3 gate's PASS-WITH-CONDITIONS on #5614). Those two are
          # separated below, because they are not the same fact: a shallow
          # checkout or an unfetched base sha is a broken measurement and must
          # not be believed, while a PR that INTRODUCES this workflow has no base
          # copy by construction and head-only is the correct reading for it.
          # `git cat-file -e` is the probe for both — `git rev-parse <sha>:<path>`
          # would answer with a hash-shaped non-answer either way.
          def _git(*args):
              return subprocess.run(["git", *args], capture_output=True, text=True)

          def _run_everything(why: str):
              print("true")
              sys.stderr.write(f"sentinel failing closed: {why}\n")
              sys.exit(0)

          head_copy = pathlib.Path(os.environ.get("HEAD_WORKFLOW", WORKFLOW)).read_text()
          patterns = set(guarded_paths(head_copy))
          base_sha = os.environ["BASE_SHA"]
          if _git("cat-file", "-e", f"{base_sha}^{{commit}}").returncode != 0:
              _run_everything(f"base ref {base_sha} is not reachable in this checkout")
          if _git("cat-file", "-e", f"{base_sha}:{WORKFLOW}").returncode == 0:
              base = _git("show", f"{base_sha}:{WORKFLOW}")
              if base.returncode != 0:
                  _run_everything("the base copy of this workflow could not be read")
              try:
                  patterns |= set(guarded_paths(base.stdout))
              except Exception as exc:  # noqa: BLE001 — any parse failure is unreadable base
                  _run_everything(f"the base copy of this workflow did not parse: {exc}")
          assert patterns, "no guarded paths parsed — refusing to declare a no-op"
          changed = [l for l in pathlib.Path(os.environ["CHANGED_FILE"]).read_text().splitlines() if l]
          # fnmatch has no `**`; `*` already crosses `/` in it, which makes it a
          # SUPERSET of GitHub's glob here — the sentinel can only ever be more
          # eager than the trigger, never blinder than it.
          hit = any(fnmatch.fnmatch(f, p) for f in changed for p in patterns)
          print("true" if hit else "false")
          PY
          )"
          echo "run=$RUN" >> "$GITHUB_OUTPUT"
          if [[ "$RUN" == "false" ]]; then
            echo "::notice::no guarded surface changed — sentinel success"
          fi

      - name: The guard's own corpus is green
        if: steps.relevant.outputs.run == 'true'
        run: |
          # pyyaml is not decoration and it is installed in its own step above:
          # the conformance test below parses THIS file's own `paths:` block, and
          # so does the sentinel, so without it both ERROR instead of guarding —
          # a green job on an unchecked filter.
          python3 -m pytest scripts/tests/test_merah_putih_day_contrast.py -v

      # A guard that cannot go red is not a guard. This step proves the job would
      # FAIL on a real regression, using the cheapest possible mutation: swap the
      # DAY ink for a retired colour in the token file, assert pytest exits
      # non-zero, then restore and assert it is green again. Without this, a future
      # refactor that silently disconnects the test from the token file would leave
      # this job passing forever on nothing — which is the exact failure this
      # workflow was created to end, and it would be embarrassing to reproduce it
      # here.
      - name: The guard can still go red (guilt control)
        if: steps.relevant.outputs.run == 'true'
        run: |
          set -euo pipefail
          TOKENS=apps/mouth/src/lib/theme/merahPutihDayVars.ts
          cp "$TOKENS" /tmp/tokens.orig
          python3 - <<'PY'
          import pathlib, sys
          p = pathlib.Path("apps/mouth/src/lib/theme/merahPutihDayVars.ts")
          t = p.read_text()
          if '"--text-primary": "#16213a"' not in t:
              sys.exit("guilt control cannot find the ink token to mutate — "
                       "the token file changed shape; fix this step rather than deleting it")
          p.write_text(t.replace('"--text-primary": "#16213a"', '"--text-primary": "#ff3344"', 1))
          PY
          if python3 -m pytest scripts/tests/test_merah_putih_day_contrast.py -q; then
            cp /tmp/tokens.orig "$TOKENS"
            echo "::error::GUILT CONTROL FAILED — the guard stayed GREEN with the DAY ink replaced by a retired colour. It is no longer connected to what it claims to protect; this job is passing on nothing."
        "pattern list, the trigger and the gate can disagree silently"
    )


# ---------------------------------------------------------------------------
# The sentinel is executed here, not just grepped. Two defects found by the
# Gear-3 council on 2026-09-04 were both invisible to the static assertions
# above: (1) the whole heredoc was piped into $GITHUB_OUTPUT, notice line
# included, so the "nothing guarded changed" path — the one this promotion
# exists to make green — went red on an invalid env-file line; (2) the pattern
# list was read from the PR's OWN copy of this file, so a PR that narrowed
# `push.paths` judged itself by its own shortened list.
# ---------------------------------------------------------------------------


def _sentinel_snippet() -> str:
    """The python between `<<'PY'` and `PY` inside the sentinel step, dedented."""
    import textwrap

    text = WORKFLOW.read_text(encoding="utf-8")
    body = text.split("Did a guarded surface change?", 1)[1].split("- name:", 1)[0]
    start = body.index("<<'PY'\n") + len("<<'PY'\n")
    end = body.index("\n          PY\n", start)
    return textwrap.dedent(body[start:end])


def _run_sentinel(tmp_path, changed: list[str], **env_extra: str) -> "subprocess.CompletedProcess[str]":
    import os
    import subprocess
    import sys

    changed_file = tmp_path / "changed.txt"
    changed_file.write_text("\n".join(changed) + "\n", encoding="utf-8")
    env = {**os.environ, "BASE_SHA": "HEAD", "CHANGED_FILE": str(changed_file), **env_extra}
    return subprocess.run(
        [sys.executable, "-"],
        input=_sentinel_snippet(),
        capture_output=True,
        text=True,
        cwd=REPO,
        env=env,
        check=False,
    )


def test_the_sentinel_prints_exactly_one_word_for_github_output(tmp_path) -> None:
    """Innocence for defect (1): on an unguarded change the snippet's stdout is
    the single word `false` — nothing else may reach `$GITHUB_OUTPUT`."""
    res = _run_sentinel(tmp_path, ["README.md"])
    assert res.returncode == 0, res.stderr
    assert res.stdout == "false\n", (
        "the sentinel wrote more than `false` to stdout — anything but the "
        f"verdict word becomes an invalid $GITHUB_OUTPUT line: {res.stdout!r}"
    )
    res = _run_sentinel(tmp_path, ["apps/mouth/src/lib/theme/merahPutihDayVars.ts"])
    assert res.returncode == 0, res.stderr
    assert res.stdout == "true\n"


def test_only_the_verdict_line_is_appended_to_github_output() -> None:
    """Guilt for defect (1), at the shell layer: the heredoc must not be
    redirected into `$GITHUB_OUTPUT`; only an `echo "run=…"` may write there."""
    text = WORKFLOW.read_text(encoding="utf-8")
    body = text.split("Did a guarded surface change?", 1)[1].split("- name:", 1)[0]
    assert '<<\'PY\' >> "$GITHUB_OUTPUT"' not in body, (
        "the sentinel pipes its whole heredoc into $GITHUB_OUTPUT again — the "
        "::notice:: line is not NAME=VALUE and the runner fails the step"
    )
    writers = [
        l.strip()
        for l in body.splitlines()
        if "$GITHUB_OUTPUT" in l and not l.strip().startswith("#")
    ]
    assert writers and all(l.startswith('echo "run=') for l in writers), writers


def test_a_pr_that_narrows_its_own_list_cannot_disarm_the_sentinel(tmp_path) -> None:
    """Guilt for defect (2): the PR's copy of this workflow drops every guarded
    path but one unrelated file. The sentinel must still say `true` for a
    token-file change, because it also reads the BASE copy the PR cannot edit."""
    narrowed = tmp_path / "narrowed.yml"
    narrowed.write_text(
        "on:\n  push:\n    paths:\n      - \"docs/never-touched.md\"\n", encoding="utf-8"
    )
    res = _run_sentinel(
        tmp_path,
        ["apps/mouth/src/lib/theme/merahPutihDayVars.ts"],
        HEAD_WORKFLOW=str(narrowed),
    )
    assert res.returncode == 0, res.stderr
    assert res.stdout == "true\n", (
        "the sentinel judged itself by the PR's own narrowed list — a PR can "
        "shrink `push.paths` and skip the corpus that would catch it"
    )


def test_the_sentinel_reads_the_base_copy_via_git_show() -> None:
    """The static half of the base-copy guarantee; the executable half is
    `test_a_pr_that_narrows_its_own_list_cannot_disarm_the_sentinel`.

    This used to assert the literal `"git", "show"`, which pinned the SPELLING
    rather than the mechanism: wrapping the same call in a `_git(*args)` helper
    — as the fail-closed cure did — broke it while the sentinel still read the
    base copy exactly as before. Rewritten to name the two things that actually
    have to be true (a `show` of the base ref's own copy of this workflow, at
    `BASE_SHA`), so a helper rename passes and REMOVING the base read fails.
    """
    body = (
        WORKFLOW.read_text(encoding="utf-8")
        .split("Did a guarded surface change?", 1)[1]
        .split("- name:", 1)[0]
    )
    assert '"show"' in body and "BASE_SHA" in body, (
        "the sentinel no longer reads the base ref's copy of its pattern list"
    )
    assert "base_sha}:{WORKFLOW}" in body, (
        "the sentinel no longer addresses the BASE ref's copy of THIS workflow — "
        "if it now shows some other path or ref, the PR-controlled head copy is "
        "the only list in play"
    )


# ---------------------------------------------------------------------------
# The two conditions the Gear-3 gate attached to #5614's PASS-WITH-CONDITIONS.
# Both were prose in that PR's body and prose is not in force, so each is
# asserted here — and the sentinel one is EXECUTED rather than grepped, because
# a fail-closed path that is only described is exactly the shape that fails open
# the first time anyone reads it.
# ---------------------------------------------------------------------------


def test_the_concurrency_group_never_cancels_a_merge_group_run() -> None:
    """Condition 1. `github.ref` on a merge_group event is the QUEUE's ref, not
    this PR's — so a bare `cancel-in-progress: true` lets the queue's next entry
    cancel the entry ahead of it. A cancelled required context does not report a
    failure, it reports nothing, and the entry is torn out of the queue rather
    than merely delayed. Cancelling is right on `pull_request`, where the ref is
    the PR's own and the superseded run is genuinely dead work.
    """
    import yaml

    doc = yaml.safe_load(WORKFLOW.read_text(encoding="utf-8"))
    concurrency = doc.get("concurrency")
    assert concurrency, f"{WORKFLOW.name}: the concurrency block is gone"

    cancel = str(concurrency.get("cancel-in-progress", "")).strip()
    assert cancel not in {"True", "true"}, (
        f"{WORKFLOW.name}: `cancel-in-progress` is unconditionally true again. On "
        "a merge_group event the group key is the queue's own ref, so this "
        "cancels the entry ahead of this one and a cancelled required context "
        "never reports."
    )
    assert "github.event_name" in cancel and "merge_group" in cancel, (
        f"{WORKFLOW.name}: `cancel-in-progress` is {cancel!r} — it must be an "
        "expression that turns cancelling OFF for merge_group specifically, not "
        "a blanket value."
    )


def test_the_sentinel_runs_the_guard_when_the_base_ref_cannot_be_read(tmp_path) -> None:
    """Condition 2, guilt. An unreachable base sha (shallow checkout, unfetched
    ref) means the trust boundary — reading the pattern list from the copy the PR
    cannot edit — could not be applied. The sentinel must then run the WHOLE
    guard, not fall back to the head-only list it just declined to trust.

    Guilt, not innocence: the change below touches nothing guarded, so the
    head-only reading would print `false` and skip the corpus. Fail-closed is
    the only way `true` comes out of this.
    """
    res = _run_sentinel(
        tmp_path,
        ["README.md"],
        BASE_SHA="0000000000000000000000000000000000000000",
    )
    assert res.returncode == 0, res.stderr
    assert res.stdout == "true\n", (
        "the sentinel could not read the base ref and still declared a no-op — "
        "it failed OPEN. An unreadable base means the PR-controlled head copy is "
        f"the only list available, which is precisely what must not be trusted. "
        f"stdout={res.stdout!r} stderr={res.stderr!r}"
    )


def test_a_pr_that_introduces_this_workflow_is_not_forced_through_the_guard(tmp_path) -> None:
    """Condition 2, innocence. "The base has no copy of this file" is NOT the
    same event as "the base cannot be read", and conflating them would make the
    fail-closed path fire on every PR that adds a workflow — the guard would run
    its full corpus on unrelated changes forever.

    The base ref used here is a REAL commit from this repo's history: the parent
    of the commit that introduced this workflow, which by construction does not
    contain it. The first draft fabricated an empty commit with `git commit-tree`
    instead, and that exits 128 on a CI runner with no committer identity
    configured — a test that depends on git plumbing the environment need not
    provide. History is already there and needs no identity to read.
    """
    import subprocess

    def _git(*args):
        return subprocess.run(["git", *args], capture_output=True, text=True, cwd=REPO)

    introducing = _git("rev-list", "--max-parents=1", "HEAD", "--", str(WORKFLOW.relative_to(REPO)))
    revs = [r for r in introducing.stdout.split() if r]
    if not revs:
        pytest.skip("no single-parent commit touches this workflow — cannot locate a base without it")
    before = _git("rev-parse", f"{revs[-1]}^")
    if before.returncode != 0:
        pytest.skip("the introducing commit is a root commit — no parent to use as a base")
    base = before.stdout.strip()

    # The precondition this test rests on, asserted rather than assumed: that
    # base really must NOT contain the workflow, or the case under test is not
    # the case being exercised and a green result would mean nothing.
    assert _git("cat-file", "-e", f"{base}:{WORKFLOW.relative_to(REPO)}").returncode != 0, (
        f"expected {base[:12]} to predate this workflow, but it contains it — "
        "the innocence case is not being exercised"
    )

    res = _run_sentinel(tmp_path, ["README.md"], BASE_SHA=base)
    assert res.returncode == 0, res.stderr
    assert res.stdout == "false\n", (
        "a reachable base that simply does not contain this workflow was treated "
        "as an unreadable base — the fail-closed path is over-matching, and every "
        "PR introducing a workflow now runs the full corpus. "
        f"stdout={res.stdout!r} stderr={res.stderr!r}"
    )

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '280,430p' .github/workflows/merah-putih-day-contrast.yml; git diff -- .github/workflows/merah-putih-day-contrast.yml scripts/tests/test_merah_putih_day_contrast.py; git status --short; git log -5 --oneline --decorate" in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 succeeded in 113ms:
            echo "::error::GUILT CONTROL FAILED — the guard stayed GREEN with the DAY ink replaced by a retired colour. It is no longer connected to what it claims to protect; this job is passing on nothing."
            exit 1
          fi
          cp /tmp/tokens.orig "$TOKENS"
          git diff --exit-code -- "$TOKENS"
          python3 -m pytest scripts/tests/test_merah_putih_day_contrast.py -q
          echo "guilt control: guard went RED on a retired-ink mutation and GREEN again after restore"
diff --git a/scripts/tests/test_merah_putih_day_contrast.py b/scripts/tests/test_merah_putih_day_contrast.py
index 0167d69480..42e374b786 100644
--- a/scripts/tests/test_merah_putih_day_contrast.py
+++ b/scripts/tests/test_merah_putih_day_contrast.py
@@ -836,21 +836,36 @@ def test_a_pr_that_introduces_this_workflow_is_not_forced_through_the_guard(tmp_
     fail-closed path fire on every PR that adds a workflow — the guard would run
     its full corpus on unrelated changes forever.
 
-    Here the base ref IS reachable (HEAD) but the workflow is read from a path
-    that does not exist in it, so only the head list applies and an unguarded
-    change must still come back `false`.
+    The base ref used here is a REAL commit from this repo's history: the parent
+    of the commit that introduced this workflow, which by construction does not
+    contain it. The first draft fabricated an empty commit with `git commit-tree`
+    instead, and that exits 128 on a CI runner with no committer identity
+    configured — a test that depends on git plumbing the environment need not
+    provide. History is already there and needs no identity to read.
     """
     import subprocess
 
-    empty_base = subprocess.run(
-        ["git", "commit-tree", "-m", "empty", subprocess.run(
-            ["git", "hash-object", "-t", "tree", "/dev/null"],
-            capture_output=True, text=True, cwd=REPO, check=True,
-        ).stdout.strip()],
-        capture_output=True, text=True, cwd=REPO, check=True,
-    ).stdout.strip()
+    def _git(*args):
+        return subprocess.run(["git", *args], capture_output=True, text=True, cwd=REPO)
 
-    res = _run_sentinel(tmp_path, ["README.md"], BASE_SHA=empty_base)
+    introducing = _git("rev-list", "--max-parents=1", "HEAD", "--", str(WORKFLOW.relative_to(REPO)))
+    revs = [r for r in introducing.stdout.split() if r]
+    if not revs:
+        pytest.skip("no single-parent commit touches this workflow — cannot locate a base without it")
+    before = _git("rev-parse", f"{revs[-1]}^")
+    if before.returncode != 0:
+        pytest.skip("the introducing commit is a root commit — no parent to use as a base")
+    base = before.stdout.strip()
+
+    # The precondition this test rests on, asserted rather than assumed: that
+    # base really must NOT contain the workflow, or the case under test is not
+    # the case being exercised and a green result would mean nothing.
+    assert _git("cat-file", "-e", f"{base}:{WORKFLOW.relative_to(REPO)}").returncode != 0, (
+        f"expected {base[:12]} to predate this workflow, but it contains it — "
+        "the innocence case is not being exercised"
+    )
+
+    res = _run_sentinel(tmp_path, ["README.md"], BASE_SHA=base)
     assert res.returncode == 0, res.stderr
     assert res.stdout == "false\n", (
         "a reachable base that simply does not contain this workflow was treated "
 M scripts/tests/test_merah_putih_day_contrast.py
6434bfa6a4 (HEAD -> agent/air-m5/infra/merah-day-gate-conditions, origin/agent/air-m5/infra/merah-day-gate-conditions) fix(ci): the Merah Putih guard stops cancelling queue entries and stops trusting an unread base
60d0c4e856 fix(ci): give the Merah Putih DAY contrast guard the shape a required context needs (#5614)
ea0a813a42 (origin/gh-readonly-queue/main/pr-5660-0d22e738a3c9892e4ae4a88f52fd4b7c3153b610) fix(news-room): staging summary fallback is complete sentences, not content[:280] (#5660)
0d22e738a3 (origin/gh-readonly-queue/main/pr-5657-d153873811bf058df1e4899d5042e53cbe2852ab) feat(article): Add article 'Indonesia's KBLI 2025 Shake-Up: The Transition Rul...' (#5657)
d153873811 (origin/gh-readonly-queue/main/pr-5643-fa1a265a6c3d326f2bdebcc9cbe07584aa1b67f1) fix(intel-scraper): the cover visual director never found `claude` under launchd (#5643)

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
codex
La prima ipotesi importante sta cedendo: con la chiave attuale, `pull_request`, `merge_group` e `push` usano ref diversi; due voci di coda misurate come `.../pr-<N>-<sha>` non collidono. Sto verificando ora il punto più pericoloso: il probe del file base in un partial clone può confondere “file assente” con “blob non recuperabile”.
hook: PreToolUse
hook: PreToolUse Completed
web search: 
web search: site:docs.github.com actions merge_group github.ref gh-readonly-queue ref concurrency ...
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'ls -ld .venv venv apps/backend-rag/.venv 2>/dev/null || true; git show --stat --oneline HEAD; git show HEAD''^:.github/workflows/merah-putih-day-contrast.yml | sed -n '"'120,235p'" in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 succeeded in 30ms:
lrwxr-xr-x@ 1 balizero  staff  48  4 set 19:49 apps/backend-rag/.venv -> /Users/balizero/nuzantara/apps/backend-rag/.venv
6434bfa6a4 fix(ci): the Merah Putih guard stops cancelling queue entries and stops trusting an unread base
 .github/workflows/merah-putih-day-contrast.yml |  52 ++++++++++--
 scripts/tests/test_merah_putih_day_contrast.py | 108 ++++++++++++++++++++++++-
 2 files changed, 151 insertions(+), 9 deletions(-)
      - "packages/core/tokens/semantic.css"
      - "scripts/tests/test_merah_putih_day_contrast.py"
      - ".github/workflows/merah-putih-day-contrast.yml"

permissions:
  contents: read

concurrency:
  group: merah-putih-day-contrast-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contrast-guard:
    name: Merah Putih DAY palette holds its contrast law
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # the sentinel diffs against the PR base
          filter: blob:none # every commit and tree, file contents on demand

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install the parser the sentinel and the filter test both need
        run: python3 -m pip install --quiet --disable-pip-version-check pytest pyyaml

      # The sentinel exists because this context is meant to be REQUIRED, and a
      # required check must report on every PR. It answers one question — did any
      # guarded path change — and it answers it from THIS FILE'S OWN `paths:`
      # list, not from a second copy of it. A hand-kept regex is how the sentinel
      # and the trigger drift into disagreeing, and a sentinel that says "nothing
      # changed" about a file that did is a green check over a real regression.
      - name: Did a guarded surface change?
        id: relevant
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "::notice::non-PR event (${{ github.event_name }}) — running the full guard"
            exit 0
          fi
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          git diff --name-only "$BASE_SHA"...HEAD > /tmp/changed.txt
          # Only the `run=` line may reach $GITHUB_OUTPUT. The first cut piped the
          # whole heredoc there, notice included, and the runner rejects any line
          # that is not NAME=VALUE — so the "nothing guarded changed" path, the
          # one this promotion exists to make green, went red instead (found by
          # the Gear-3 council, 2026-09-04). The snippet prints exactly one word.
          RUN="$(BASE_SHA="$BASE_SHA" CHANGED_FILE=/tmp/changed.txt python3 - <<'PY'
          import fnmatch, os, pathlib, subprocess, yaml

          WORKFLOW = ".github/workflows/merah-putih-day-contrast.yml"

          def guarded_paths(text: str) -> list[str]:
              doc = yaml.safe_load(text)
              # PyYAML is YAML 1.1: a bare `on:` key parses as the boolean True.
              trigger = doc.get("on", doc.get(True))
              return list(trigger["push"]["paths"])

          # The list is read from the BASE ref's copy of this file AND the PR's
          # own copy, unioned. Reading only the PR's copy is a trust boundary the
          # PR itself controls: one diff that narrows `push.paths` and regresses a
          # guarded file would have the sentinel judge itself by its own shortened
          # list, skip the corpus, and skip the very test that catches a
          # shrinking list (Gear-3 council finding, 2026-09-04). The base copy is
          # the one the PR cannot edit.
          head_copy = pathlib.Path(os.environ.get("HEAD_WORKFLOW", WORKFLOW)).read_text()
          patterns = set(guarded_paths(head_copy))
          base = subprocess.run(
              ["git", "show", f"{os.environ['BASE_SHA']}:{WORKFLOW}"],
              capture_output=True, text=True,
          )
          if base.returncode == 0:
              patterns |= set(guarded_paths(base.stdout))
          assert patterns, "no guarded paths parsed — refusing to declare a no-op"
          changed = [l for l in pathlib.Path(os.environ["CHANGED_FILE"]).read_text().splitlines() if l]
          # fnmatch has no `**`; `*` already crosses `/` in it, which makes it a
          # SUPERSET of GitHub's glob here — the sentinel can only ever be more
          # eager than the trigger, never blinder than it.
          hit = any(fnmatch.fnmatch(f, p) for f in changed for p in patterns)
          print("true" if hit else "false")
          PY
          )"
          echo "run=$RUN" >> "$GITHUB_OUTPUT"
          if [[ "$RUN" == "false" ]]; then
            echo "::notice::no guarded surface changed — sentinel success"
          fi

      - name: The guard's own corpus is green
        if: steps.relevant.outputs.run == 'true'
        run: |
          # pyyaml is not decoration and it is installed in its own step above:
          # the conformance test below parses THIS file's own `paths:` block, and
          # so does the sentinel, so without it both ERROR instead of guarding —
          # a green job on an unchecked filter.
          python3 -m pytest scripts/tests/test_merah_putih_day_contrast.py -v

      # A guard that cannot go red is not a guard. This step proves the job would
      # FAIL on a real regression, using the cheapest possible mutation: swap the
      # DAY ink for a retired colour in the token file, assert pytest exits
      # non-zero, then restore and assert it is green again. Without this, a future
      # refactor that silently disconnects the test from the token file would leave
      # this job passing forever on nothing — which is the exact failure this
      # workflow was created to end, and it would be embarrassing to reproduce it
      # here.
      - name: The guard can still go red (guilt control)
        if: steps.relevant.outputs.run == 'true'
        run: |
          set -euo pipefail
          TOKENS=apps/mouth/src/lib/theme/merahPutihDayVars.ts
          cp "$TOKENS" /tmp/tokens.orig
          python3 - <<'PY'
          import pathlib, sys
          p = pathlib.Path("apps/mouth/src/lib/theme/merahPutihDayVars.ts")

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'source apps/backend-rag/.venv/bin/activate && python -m pytest scripts/tests/test_merah_putih_day_contrast.py -q' in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 exited 1 in 838ms:
Traceback (most recent call last):
  File "<frozen runpy>", line 198, in _run_module_as_main
  File "<frozen runpy>", line 88, in _run_code
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pytest/__main__.py", line 9, in <module>
    raise SystemExit(pytest.console_main())
                     ^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
           ^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
          ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ^^^^^^^^^^^^
  File "/Users/balizero/Desktop/nuzantara/apps/backend-rag/.venv/lib/python3.11/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/.local/share/mise/installs/python/3.11.15/lib/python3.11/tempfile.py", line 628, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/.local/share/mise/installs/python/3.11.15/lib/python3.11/tempfile.py", line 127, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/Users/balizero/.local/share/mise/installs/python/3.11.15/lib/python3.11/tempfile.py", line 316, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/Users/balizero/.local/share/mise/installs/python/3.11.15/lib/python3.11/tempfile.py", line 309, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/balizero/.local/share/mise/installs/python/3.11.15/lib/python3.11/tempfile.py", line 224, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/var/folders/8_/617x0rcj09bb8yt0byfmcp1c0000gn/T/', '/tmp', '/var/tmp', '/usr/tmp', '/Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions']

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "nl -ba .github/workflows/merah-putih-day-contrast.yml | sed -n '120,255p'; nl -ba scripts/tests/test_merah_putih_day_contrast.py | sed -n '775,890p'; git rev-list --max-parents=1 HEAD -- .github/workflows/merah-putih-day-contrast.yml | tail -5 | while read r; do git show -s --format='%H %P %s' \""'$r"; done' in /Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions
 succeeded in 111ms:
   120	      - "packages/core/tokens/semantic.css"
   121	      - "scripts/tests/test_merah_putih_day_contrast.py"
   122	      - ".github/workflows/merah-putih-day-contrast.yml"
   123	
   124	permissions:
   125	  contents: read
   126	
   127	concurrency:
   128	  # `cancel-in-progress` is CONDITIONAL on the event, and the condition is the
   129	  # whole point: on a merge_group event `github.ref` is the QUEUE's own ref, so a
   130	  # second entry landing while this one is mid-flight would cancel the first —
   131	  # and a cancelled required context does not report, which does not delay the
   132	  # queue, it tears the entry out of it. On a pull_request the ref is the PR's
   133	  # own, where cancelling a superseded push is exactly the saving we want.
   134	  # Promotion condition 1 of 2 from the Gear-3 gate's PASS-WITH-CONDITIONS on
   135	  # #5614; `test_the_concurrency_group_never_cancels_a_merge_group_run` is what
   136	  # keeps this from silently reverting to a bare `true`.
   137	  group: merah-putih-day-contrast-${{ github.ref }}
   138	  cancel-in-progress: ${{ github.event_name != 'merge_group' }}
   139	
   140	jobs:
   141	  contrast-guard:
   142	    name: Merah Putih DAY palette holds its contrast law
   143	    runs-on: ubuntu-latest
   144	    steps:
   145	      - uses: actions/checkout@v4
   146	        with:
   147	          fetch-depth: 0 # the sentinel diffs against the PR base
   148	          filter: blob:none # every commit and tree, file contents on demand
   149	
   150	      - uses: actions/setup-python@v7
   151	        with:
   152	          python-version: "3.12"
   153	
   154	      - name: Install the parser the sentinel and the filter test both need
   155	        run: python3 -m pip install --quiet --disable-pip-version-check pytest pyyaml
   156	
   157	      # The sentinel exists because this context is meant to be REQUIRED, and a
   158	      # required check must report on every PR. It answers one question — did any
   159	      # guarded path change — and it answers it from THIS FILE'S OWN `paths:`
   160	      # list, not from a second copy of it. A hand-kept regex is how the sentinel
   161	      # and the trigger drift into disagreeing, and a sentinel that says "nothing
   162	      # changed" about a file that did is a green check over a real regression.
   163	      - name: Did a guarded surface change?
   164	        id: relevant
   165	        run: |
   166	          set -euo pipefail
   167	          if [[ "${{ github.event_name }}" != "pull_request" ]]; then
   168	            echo "run=true" >> "$GITHUB_OUTPUT"
   169	            echo "::notice::non-PR event (${{ github.event_name }}) — running the full guard"
   170	            exit 0
   171	          fi
   172	          BASE_SHA="${{ github.event.pull_request.base.sha }}"
   173	          git diff --name-only "$BASE_SHA"...HEAD > /tmp/changed.txt
   174	          # Only the `run=` line may reach $GITHUB_OUTPUT. The first cut piped the
   175	          # whole heredoc there, notice included, and the runner rejects any line
   176	          # that is not NAME=VALUE — so the "nothing guarded changed" path, the
   177	          # one this promotion exists to make green, went red instead (found by
   178	          # the Gear-3 council, 2026-09-04). The snippet prints exactly one word.
   179	          RUN="$(BASE_SHA="$BASE_SHA" CHANGED_FILE=/tmp/changed.txt python3 - <<'PY'
   180	          import fnmatch, os, pathlib, subprocess, sys, yaml
   181	
   182	          WORKFLOW = ".github/workflows/merah-putih-day-contrast.yml"
   183	
   184	          def guarded_paths(text: str) -> list[str]:
   185	              doc = yaml.safe_load(text)
   186	              # PyYAML is YAML 1.1: a bare `on:` key parses as the boolean True.
   187	              trigger = doc.get("on", doc.get(True))
   188	              return list(trigger["push"]["paths"])
   189	
   190	          # The list is read from the BASE ref's copy of this file AND the PR's
   191	          # own copy, unioned. Reading only the PR's copy is a trust boundary the
   192	          # PR itself controls: one diff that narrows `push.paths` and regresses a
   193	          # guarded file would have the sentinel judge itself by its own shortened
   194	          # list, skip the corpus, and skip the very test that catches a
   195	          # shrinking list (Gear-3 council finding, 2026-09-04). The base copy is
   196	          # the one the PR cannot edit.
   197	          #
   198	          # And when the base copy cannot be read, the sentinel FAILS CLOSED —
   199	          # it runs the whole guard rather than quietly falling back to the
   200	          # head-only list the paragraph above just called a trust boundary the
   201	          # PR controls. `if base.returncode == 0` alone treated "the base ref is
   202	          # unreachable" and "the base genuinely has no such file" as the same
   203	          # event and skipped the protection in both (promotion condition 2 of 2
   204	          # from the Gear-3 gate's PASS-WITH-CONDITIONS on #5614). Those two are
   205	          # separated below, because they are not the same fact: a shallow
   206	          # checkout or an unfetched base sha is a broken measurement and must
   207	          # not be believed, while a PR that INTRODUCES this workflow has no base
   208	          # copy by construction and head-only is the correct reading for it.
   209	          # `git cat-file -e` is the probe for both — `git rev-parse <sha>:<path>`
   210	          # would answer with a hash-shaped non-answer either way.
   211	          def _git(*args):
   212	              return subprocess.run(["git", *args], capture_output=True, text=True)
   213	
   214	          def _run_everything(why: str):
   215	              print("true")
   216	              sys.stderr.write(f"sentinel failing closed: {why}\n")
   217	              sys.exit(0)
   218	
   219	          head_copy = pathlib.Path(os.environ.get("HEAD_WORKFLOW", WORKFLOW)).read_text()
   220	          patterns = set(guarded_paths(head_copy))
   221	          base_sha = os.environ["BASE_SHA"]
   222	          if _git("cat-file", "-e", f"{base_sha}^{{commit}}").returncode != 0:
   223	              _run_everything(f"base ref {base_sha} is not reachable in this checkout")
   224	          if _git("cat-file", "-e", f"{base_sha}:{WORKFLOW}").returncode == 0:
   225	              base = _git("show", f"{base_sha}:{WORKFLOW}")
   226	              if base.returncode != 0:
   227	                  _run_everything("the base copy of this workflow could not be read")
   228	              try:
   229	                  patterns |= set(guarded_paths(base.stdout))
   230	              except Exception as exc:  # noqa: BLE001 — any parse failure is unreadable base
   231	                  _run_everything(f"the base copy of this workflow did not parse: {exc}")
   232	          assert patterns, "no guarded paths parsed — refusing to declare a no-op"
   233	          changed = [l for l in pathlib.Path(os.environ["CHANGED_FILE"]).read_text().splitlines() if l]
   234	          # fnmatch has no `**`; `*` already crosses `/` in it, which makes it a
   235	          # SUPERSET of GitHub's glob here — the sentinel can only ever be more
   236	          # eager than the trigger, never blinder than it.
   237	          hit = any(fnmatch.fnmatch(f, p) for f in changed for p in patterns)
   238	          print("true" if hit else "false")
   239	          PY
   240	          )"
   241	          echo "run=$RUN" >> "$GITHUB_OUTPUT"
   242	          if [[ "$RUN" == "false" ]]; then
   243	            echo "::notice::no guarded surface changed — sentinel success"
   244	          fi
   245	
   246	      - name: The guard's own corpus is green
   247	        if: steps.relevant.outputs.run == 'true'
   248	        run: |
   249	          # pyyaml is not decoration and it is installed in its own step above:
   250	          # the conformance test below parses THIS file's own `paths:` block, and
   251	          # so does the sentinel, so without it both ERROR instead of guarding —
   252	          # a green job on an unchecked filter.
   253	          python3 -m pytest scripts/tests/test_merah_putih_day_contrast.py -v
   254	
   255	      # A guard that cannot go red is not a guard. This step proves the job would
   775	# asserted here — and the sentinel one is EXECUTED rather than grepped, because
   776	# a fail-closed path that is only described is exactly the shape that fails open
   777	# the first time anyone reads it.
   778	# ---------------------------------------------------------------------------
   779	
   780	
   781	def test_the_concurrency_group_never_cancels_a_merge_group_run() -> None:
   782	    """Condition 1. `github.ref` on a merge_group event is the QUEUE's ref, not
   783	    this PR's — so a bare `cancel-in-progress: true` lets the queue's next entry
   784	    cancel the entry ahead of it. A cancelled required context does not report a
   785	    failure, it reports nothing, and the entry is torn out of the queue rather
   786	    than merely delayed. Cancelling is right on `pull_request`, where the ref is
   787	    the PR's own and the superseded run is genuinely dead work.
   788	    """
   789	    import yaml
   790	
   791	    doc = yaml.safe_load(WORKFLOW.read_text(encoding="utf-8"))
   792	    concurrency = doc.get("concurrency")
   793	    assert concurrency, f"{WORKFLOW.name}: the concurrency block is gone"
   794	
   795	    cancel = str(concurrency.get("cancel-in-progress", "")).strip()
   796	    assert cancel not in {"True", "true"}, (
   797	        f"{WORKFLOW.name}: `cancel-in-progress` is unconditionally true again. On "
   798	        "a merge_group event the group key is the queue's own ref, so this "
   799	        "cancels the entry ahead of this one and a cancelled required context "
   800	        "never reports."
   801	    )
   802	    assert "github.event_name" in cancel and "merge_group" in cancel, (
   803	        f"{WORKFLOW.name}: `cancel-in-progress` is {cancel!r} — it must be an "
   804	        "expression that turns cancelling OFF for merge_group specifically, not "
   805	        "a blanket value."
   806	    )
   807	
   808	
   809	def test_the_sentinel_runs_the_guard_when_the_base_ref_cannot_be_read(tmp_path) -> None:
   810	    """Condition 2, guilt. An unreachable base sha (shallow checkout, unfetched
   811	    ref) means the trust boundary — reading the pattern list from the copy the PR
   812	    cannot edit — could not be applied. The sentinel must then run the WHOLE
   813	    guard, not fall back to the head-only list it just declined to trust.
   814	
   815	    Guilt, not innocence: the change below touches nothing guarded, so the
   816	    head-only reading would print `false` and skip the corpus. Fail-closed is
   817	    the only way `true` comes out of this.
   818	    """
   819	    res = _run_sentinel(
   820	        tmp_path,
   821	        ["README.md"],
   822	        BASE_SHA="0000000000000000000000000000000000000000",
   823	    )
   824	    assert res.returncode == 0, res.stderr
   825	    assert res.stdout == "true\n", (
   826	        "the sentinel could not read the base ref and still declared a no-op — "
   827	        "it failed OPEN. An unreadable base means the PR-controlled head copy is "
   828	        f"the only list available, which is precisely what must not be trusted. "
   829	        f"stdout={res.stdout!r} stderr={res.stderr!r}"
   830	    )
   831	
   832	
   833	def test_a_pr_that_introduces_this_workflow_is_not_forced_through_the_guard(tmp_path) -> None:
   834	    """Condition 2, innocence. "The base has no copy of this file" is NOT the
   835	    same event as "the base cannot be read", and conflating them would make the
   836	    fail-closed path fire on every PR that adds a workflow — the guard would run
   837	    its full corpus on unrelated changes forever.
   838	
   839	    The base ref used here is a REAL commit from this repo's history: the parent
   840	    of the commit that introduced this workflow, which by construction does not
   841	    contain it. The first draft fabricated an empty commit with `git commit-tree`
   842	    instead, and that exits 128 on a CI runner with no committer identity
   843	    configured — a test that depends on git plumbing the environment need not
   844	    provide. History is already there and needs no identity to read.
   845	    """
   846	    import subprocess
   847	
   848	    def _git(*args):
   849	        return subprocess.run(["git", *args], capture_output=True, text=True, cwd=REPO)
   850	
   851	    introducing = _git("rev-list", "--max-parents=1", "HEAD", "--", str(WORKFLOW.relative_to(REPO)))
   852	    revs = [r for r in introducing.stdout.split() if r]
   853	    if not revs:
   854	        pytest.skip("no single-parent commit touches this workflow — cannot locate a base without it")
   855	    before = _git("rev-parse", f"{revs[-1]}^")
   856	    if before.returncode != 0:
   857	        pytest.skip("the introducing commit is a root commit — no parent to use as a base")
   858	    base = before.stdout.strip()
   859	
   860	    # The precondition this test rests on, asserted rather than assumed: that
   861	    # base really must NOT contain the workflow, or the case under test is not
   862	    # the case being exercised and a green result would mean nothing.
   863	    assert _git("cat-file", "-e", f"{base}:{WORKFLOW.relative_to(REPO)}").returncode != 0, (
   864	        f"expected {base[:12]} to predate this workflow, but it contains it — "
   865	        "the innocence case is not being exercised"
   866	    )
   867	
   868	    res = _run_sentinel(tmp_path, ["README.md"], BASE_SHA=base)
   869	    assert res.returncode == 0, res.stderr
   870	    assert res.stdout == "false\n", (
   871	        "a reachable base that simply does not contain this workflow was treated "
   872	        "as an unreadable base — the fail-closed path is over-matching, and every "
   873	        "PR introducing a workflow now runs the full corpus. "
   874	        f"stdout={res.stdout!r} stderr={res.stderr!r}"
   875	    )
6434bfa6a4aad61f698e5bf9d94cce53077c39e3 60d0c4e8560738b734e5c81813db9e8eb99cd793 fix(ci): the Merah Putih guard stops cancelling queue entries and stops trusting an unread base
60d0c4e8560738b734e5c81813db9e8eb99cd793 ea0a813a42e5428fbce685e53c5922f35ecea6b3 fix(ci): give the Merah Putih DAY contrast guard the shape a required context needs (#5614)
e725b158fb8641e96a0832900cb46a0fb28b7d82 e26670614b681c4e04abfdad55acb19b0f381268 chore(deps): Bump actions/setup-python from 5 to 7 (#5445)
cadf671584211585a52d91f36759aa17212b645e aaeb43fd1732c8f98b7377697da27bf8ac032b33 ci(mouth): arm the Merah Putih DAY contrast guard, and derive its trigger from what the guard reads (#5395)

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
codex
[Air-M5] **Verdict: BLOCK**

**Difetto reale più probabile:** il controllo dell’irraggiungibilità avviene troppo tardi. A [riga 173](/Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions/.github/workflows/merah-putih-day-contrast.yml:173), `git diff "$BASE_SHA"...HEAD` viene eseguito sotto `set -e` prima del `cat-file`. Se il base SHA è davvero irraggiungibile, il job termina lì: `_run_everything()` non viene mai chiamata e il corpus non gira. Il test esegue soltanto lo snippet Python, quindi non rileva questo fallimento end-to-end.

1. La misura sulle queue ref è coerente. GitHub definisce `GITHUB_REF` come la ref dello specifico merge group; con `gh-readonly-queue/main/pr-<N>-<sha>`, ingressi diversi non condividono la chiave. `pull_request`, `merge_group` e `push` hanno ref differenti, quindi non collidono tra loro. Rimane solo l’eventuale duplicazione dello stesso merge group, non “la voce successiva”. Condition 1 corregge un rischio non dimostrato dalla topologia osservata. [GitHub documenta la semantica di `merge_group`](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#merge_group) e della [concurrency key](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#concurrency).

2. `print("true"); exit(0)` è corretto: “fail closed” qui significa far girare il guard, non fallire il sentinel. `BASE_SHA` vuoto non produce `KeyError`, perché la shell esporta comunque la variabile vuota; fallisce prima nel `git diff`, rendendo il job rosso. Aggiungete comunque una validazione `RUN ∈ {true,false}`: oggi un’uscita zero senza stdout produrrebbe `run=` e salterebbe entrambi i passi.

3. `f"{sha}^{{commit}}"` genera correttamente `sha^{commit}`. Ma `cat-file -e sha:path` verifica anche l’oggetto finale; in un partial clone può attivare il lazy fetch. Un errore di fetch e “path assente” diventano entrambi nonzero, quindi il codice torna head-only. Git conferma che gli oggetti mancanti vengono recuperati on demand nei partial clone. [Git partial clone](https://git-scm.com/docs/partial-clone.html). Usare `ls-tree` per l’esistenza del path, poi `show`, fallendo chiuso se la lettura fallisce.

4. Il test storico non è hermetico. `fetch-depth: 0` lo salva qui; squash merge funziona, introduzione via merge o clone shallow no. Uno `skip` su questa garanzia può nascondere la regressione: costruire un repository temporaneo deterministico e testare l’intero passo shell.
tokens used
75.360
[Air-M5] **Verdict: BLOCK**

**Difetto reale più probabile:** il controllo dell’irraggiungibilità avviene troppo tardi. A [riga 173](/Users/balizero/nuzantara/.worktrees/infra-merah-day-gate-conditions/.github/workflows/merah-putih-day-contrast.yml:173), `git diff "$BASE_SHA"...HEAD` viene eseguito sotto `set -e` prima del `cat-file`. Se il base SHA è davvero irraggiungibile, il job termina lì: `_run_everything()` non viene mai chiamata e il corpus non gira. Il test esegue soltanto lo snippet Python, quindi non rileva questo fallimento end-to-end.

1. La misura sulle queue ref è coerente. GitHub definisce `GITHUB_REF` come la ref dello specifico merge group; con `gh-readonly-queue/main/pr-<N>-<sha>`, ingressi diversi non condividono la chiave. `pull_request`, `merge_group` e `push` hanno ref differenti, quindi non collidono tra loro. Rimane solo l’eventuale duplicazione dello stesso merge group, non “la voce successiva”. Condition 1 corregge un rischio non dimostrato dalla topologia osservata. [GitHub documenta la semantica di `merge_group`](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#merge_group) e della [concurrency key](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#concurrency).

2. `print("true"); exit(0)` è corretto: “fail closed” qui significa far girare il guard, non fallire il sentinel. `BASE_SHA` vuoto non produce `KeyError`, perché la shell esporta comunque la variabile vuota; fallisce prima nel `git diff`, rendendo il job rosso. Aggiungete comunque una validazione `RUN ∈ {true,false}`: oggi un’uscita zero senza stdout produrrebbe `run=` e salterebbe entrambi i passi.

3. `f"{sha}^{{commit}}"` genera correttamente `sha^{commit}`. Ma `cat-file -e sha:path` verifica anche l’oggetto finale; in un partial clone può attivare il lazy fetch. Un errore di fetch e “path assente” diventano entrambi nonzero, quindi il codice torna head-only. Git conferma che gli oggetti mancanti vengono recuperati on demand nei partial clone. [Git partial clone](https://git-scm.com/docs/partial-clone.html). Usare `ls-tree` per l’esistenza del path, poi `show`, fallendo chiuso se la lettura fallisce.

4. Il test storico non è hermetico. `fetch-depth: 0` lo salva qui; squash merge funziona, introduzione via merge o clone shallow no. Uno `skip` su questa garanzia può nascondere la regressione: costruire un repository temporaneo deterministico e testare l’intero passo shell.
CODEX_DONE
