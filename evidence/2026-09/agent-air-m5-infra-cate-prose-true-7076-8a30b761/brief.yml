task_id: cate-prose-true-7076
gear: 3
l_level: L1
gate_class: opus
objective: |
  Discharge PWC #7076 (PENDING-ARMS row "PWC-CONDITIONS for PR #7076", gate
  comment pull/7076#issuecomment-5760649866). C1: the module docstring of
  scripts/tests/test_cate_trigger_parity.py makes two false sentences true
  (the synthetic repo; where PYTHONUSERBASE comes from) and declares two
  shapes the gate found green and undeclared. C2: an ERRATUM.md for #7076's
  evidence pack. Also correct the catE workflow header. Since
  catE-sovereignty-lint became a required context of main (2026-09-21
  ~13:00Z), the header's "a red here blocks nothing" is false. The floor is
  3 because of that path alone; every change is prose.
constraints:
  - "No executable change. The workflow's non-comment lines and the test
    module's AST minus its docstring stay identical to origin/main."
  - "Every ERRATUM correction carries a command that reproduces it, re-run
    by the writing session after #7076 merged."
  - "A Not-covered bullet is added only after a mutation of that shape was
    measured staying green. Declaring a gap that is really caught would be
    a false sentence too."
acceptance:
  - text: >
      WHEN the test module's AST minus its docstring and the workflow's
      non-comment lines are compared to origin/main, both SHALL be
      identical.
    status: verified
    probe: "Prose only: the test module AST minus its docstring and the workflow's non-comment lines are identical to origin/main"
  - text: >
      WHEN a step treats its tool's timeout exit as success
      (`timeout 1 <tool> || [ $? -eq 124 ]`), or skips when PYTHONUSERBASE
      is unset, the suite SHALL stay green, which is what the two new
      Not-covered bullets declare.
    status: verified
    probe: "The two new Not-covered bullets are true: both shapes stay green"
  - text: >
      WHEN each ERRATUM command is re-run, it SHALL print what the
      correction says.
    status: verified
    probe: "Every ERRATUM correction reproduces from its own command"
risks:
  - "Prose only. The one behavioural risk is a false declaration, and the
    battery above exists to rule it out."
instruction_to_the_grader: |
  Try to find a sentence this diff writes that is false, or a correction in
  the ERRATUM whose command does not print what it claims. Check that the
  closed ledger row does not overclaim. Read the diff, not this brief.
pii: none
