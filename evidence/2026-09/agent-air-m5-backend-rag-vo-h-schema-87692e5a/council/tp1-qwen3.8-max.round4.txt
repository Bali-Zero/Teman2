Judged from the diff text alone, no tools called.

**Syntax — blocking, found on this pass.** In the `flagged_outcomes` fixture hunk (`@@ -763,6 +947,11 @@`) the added decorator line is `+ @pytest.fixture(scope="module")` — one leading space inside the line, i.e. the file contains ` @pytest.fixture(...)`. Every other column-0 added line in the diff (`+EXPECTED_DISCLOSED_REVIEW_FLAGS: ...`, `+def test_the_corpus_carries_...`, `+from collections.abc import Sequence`) is flush left, and the context/blank lines show the renderer is byte-faithful, so this is content, not paste noise. At module level that is `IndentationError: unexpected indent`: the whole census module fails to collect, so all 84-walk assertions — including every new funnel claim — error rather than run. It is also incompatible with the `MEASURED 2026-09-13` numbers in the docstring, which could only have been printed by a module that imported; so it crept in after that measurement (rebase/edit) and is not covered by it. Fix is one character; confirm with `ast.parse` on the file or `pytest --collect-only`.

**A — omit-when-empty.** The claim survives attack. `_flag_table_violations` compares carried vs pinned sorted tuples for *every* walk in the corpus **and** flags pins with no walk, so lost / gained / stale all go red, and `carrying` non-empty catches the generator dropping the field entirely. The real residual is stated honestly in the comment: the backend table grades *committed fixtures*, so a mapper regression is caught in the frontend lane (`walk-corpus-determinism.test.ts`) and only reaches this table on regeneration — but a regenerate-and-commit flow then hits the pin, and editing both sides in one PR is the reviewable contract. No hole.

**B — guilt and innocence are both real.** Under-match and over-match guilt assert the *exact* violation string against a single-label scoped table, so a no-op helper fails them; `_scoped_flag_table` correctly yields `{}` for `offshore/tourism`, producing the `table pins []` message asserted. Innocence is deep dict equality (state, candidates, missing facts, reason codes, notices) over the 76 unflagged walks, and `test_guilt_a_fabricated_flag_deletes_a_proven_verdict` executes the engine half (`candidates == []`, `DISCLOSED_MULTI_PURPOSE_TRIP_REVIEW`) on a flag the corpus never raises. One unguarded shape: the field-by-field rebuild in `_evaluate` is only correct if those five fields are the model's *entire* field set, and no test pins that — `test_innocence_...` excludes flagged walks, which are exactly the ones that go through the rebuild. Bounded impact (the adapter rewrites to HUMAN_REVIEW unconditionally, so the census numbers can't move), but a `set(VisaOracleEvaluateRequest.model_fields) == {...}` assertion is the missing guard. Leftover, not blocking.

**C — existing callers.** No change: keyword-only, default `()`, and the rebuild sits behind `if disclosed_review_flags:`, so the coverage-floor and authoring-CLI paths construct the identical request. The new `api_models` import adds a module-load edge inside a package this file already imports from; a cycle would be a loud ImportError, not a silent one.

**D — constants and claims.** The 8 modified fixtures match `EXPECTED_DISCLOSED_REVIEW_FLAGS` row-for-row and flag-for-flag (6 × ACTIVITY_BOUNDARY, 2 × NOT_CERTAIN), key order last as the generator comment requires, JSON valid. The two censuses are arithmetically consistent with a monotone unconditional rewrite: 67−7=60, 2−1=1, 15 unchanged, 8 held, 84 total; `EXPECTED_FLAGGED_STATE_CENSUS` is derived, so the monotone property itself is under test rather than a third pinned number. Round-3 nit confirmed fixed — destination column now reads `HUMAN_REVIEW_REQUIRED` and the NOT_CERTAIN row is aligned. Two leftovers: the docstring's per-flag *from-state* column is printed but never asserted (it is derivable from `EXPECTED_OUTCOME`, so weak); and `wire.disclosed_review_flags.length` is read unguarded in the generator, a TypeError if the mapper ever makes the field optional — loud, so acceptable. `DISCLOSED_UNCERTAINTY_REVIEW` breaks the `DISCLOSED_<FLAG>_REVIEW` pattern of its two siblings, but it is asserted against live output, so a typo would be red.

**E — PII/credentials.** None. Fixtures carry synthetic walk labels, question ids and fact keys; no client data, no key, no `${VAR}` expansion. Non-goals respected: no touch to `_apply_disclosed_review_flags`, `mapDisclosedReviewFlags`, `tree.ts`, `flow.ts` or any pack.

VERDICT: DEFECT - the added `@pytest.fixture(scope="module")` line for `flagged_outcomes` carries a stray leading space, an IndentationError that stops the entire census module from collecting, so every funnel measurement in this PR fails to run.
