Dropping the indentation finding — accepted: the `+` column was the diff marker, both decorators sit at column 0 like the pre-existing `walks` fixture, and `ast.parse` + 24 collected tests settle it. Re-judging the rest.

**A. Omit-when-empty.** The two-sided pin holds for the cases the docstring names, and I could not break it: a mapper regression that stops raising a flag goes red in `walk-corpus-determinism.test.ts` (regenerated ≠ committed bytes); a regeneration committed without re-pinning goes red in `_flag_table_violations` (fixture `[]` vs table `['X']`); a total collapse of the layer trips `assert carrying`. The one path that ships green is *mapper regression → regenerate → re-pin the table*, which is indistinguishable from an intentional narrowing. The file discloses exactly this ("compared against the COMMITTED FIXTURES, never re-derived"), so it is a stated bound, not a hidden one — but "the layer cannot move without a red" is a stronger sentence than the disclosure six lines below it supports.

**B. Guilt/innocence.** Guilt is real and correctly scoped: `_scoped_flag_table` drops unpinned labels, so `pins.get(label, ())` yields `()` and the OVER-match arm fires on `offshore/tourism`; the UNDER-match arm fires on the stripped `sponsor_unsure`. Both expected strings match what `_flag_table_violations` actually formats. Two gaps:
- **`test_innocence_an_unflagged_walk_keeps_its_whole_engine_outcome` is tautological.** `_evaluate` guards the rebuild with `if disclosed_review_flags:`, and for all 76 unflagged walks `_walk_flags` returns `()`. Both fixtures therefore execute the *identical* code path on identical inputs; the assertion can only fail if `evaluator.evaluate` is non-deterministic. It passes against any implementation of the rebuild branch, including a broken one. Its docstring presents it as "the innocence half of the guard" — it guards nothing.
- The `pinned for flags but absent from the corpus` branch (stale row surviving a walk deletion) has no guilt test, though the code for it is correct.

**C. Existing callers.** Provably inert: keyword-only, default `()`, and the `if` skips the rebuild entirely. No positional signature change, import order is isort-clean (`import` block before `from` block; `visa_engine` < `.api_models` < `.bundle`). But the rebuild itself is the latent divergence point — see D.

**D. Constants / unsupported claims.** Arithmetic all reconciles: 6 `ACTIVITY_BOUNDARY` + 2 `NOT_CERTAIN` = 8 = `len(EXPECTED_DISCLOSED_REVIEW_FLAGS)`; 67−7=60, 2−1=1, 15 unchanged, 60+15+1+8=84. The 8 fixture hunks in the diff match the pinned table row for row, flag name for flag name — the diff corroborates the measurement rather than merely asserting it. Two items I cannot clear from the diff text:
- **The field-by-field reconstruction of `VisaOracleEvaluateRequest` names five fields and nothing binds it to the model's actual field set.** If the model has (or later gains) a sixth field that `build_persona_request` populates, the FUNNEL census silently evaluates a request the browser never sends and **no test goes red** — the innocence test can't catch it (both sides skip or both sides lose the same field), and the reason-code/state assertions still pass because the adapter overwrites everything anyway. `model_copy(update=...)` is *not* the fix — it skips validators, so an unknown flag name would stop raising `ValidationError`, which is the property `_walk_flags`'s docstring claims. The constructor is right; what's missing is a one-line binding such as `assert set(VisaOracleEvaluateRequest.model_fields) == {"schema_version","assessment_id","collected_at","facts","disclosed_review_flags"}`.
- `DISCLOSED_ACTIVITY_BOUNDARY_REVIEW` / `DISCLOSED_UNCERTAINTY_REVIEW` / `DISCLOSED_MULTI_PURPOSE_TRIP_REVIEW` restate `_DISCLOSED_REVIEW_REASON_CODES` unverifiably from here, but they are self-checking (a wrong string is red, not false-green). The `rulepack-prod-020.signed.json` named in the docstring is doc-rot on the next pack. Also: no flag-carrying walk is `NO_SUPPORTED_PATH`, so "unconditional for any decision the engine actually produces" is measured on SUPPORTED and NEEDS_INPUT only.

**E. PII/credentials.** None. Synthetic interview answers (visa category, currency, sponsor status); no identifiers, no keys, no `${VAR}` echoes.

The measurement tables, the fixture hunks and the inert-parameter change are sound and I would not block on the doc-rot or the missing branch-guilt. What I would block on is that the census's one new code path hand-rebuilds the wire model with no test tying the rebuild to the model's field set, while the test advertised as its innocence guard is structurally incapable of failing.

VERDICT: DEFECT - `_evaluate`'s field-by-field `VisaOracleEvaluateRequest` rebuild is bound to nothing (`test_innocence_...` is tautological because the `if disclosed_review_flags:` guard means unflagged walks never enter it), so any model field beyond the five named is silently dropped from the FUNNEL census with no red.
