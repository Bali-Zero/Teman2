**A — omit-when-empty.** The claim holds, and the two-direction pin is the load-bearing half: `_flag_table_violations` grades every corpus label against the table *and* every table label against the corpus, and `test_the_corpus_carries_the_disclosure_flag_layer` additionally asserts `carrying` is non-empty — so a generator that stops emitting the field reddens all 8 rows at once rather than reading as "84 unflagged walks". A lost flag in a *committed* fixture is caught backend-side regardless of the frontend lane. Residual dependency worth one spot-check (not a defect): the mapper-regression lane rests entirely on `walk-corpus-determinism.test.ts` comparing **raw written bytes**. That file is untouched here, and the pre-existing docstring's "byte-for-byte" wording supports it — but if that test snapshots a field allowlist instead of the serialized object, the new `disclosed_review_flags` key would be outside its reach and a mapper regression would stay green until someone regenerates. The diff itself does not weaken either guard.

**B — guilt and innocence are both real.** Guilt against a dead parameter: `test_the_flagged_census_is_the_funnel_the_applicant_meets` (funnel would collapse to 0 ≠ 8) and `test_guilt_a_fabricated_flag_deletes_a_proven_verdict` (asserts the exact reason-code list, so a no-op rebuild fails). Under/over-match guilt is correctly scoped: `_scoped_flag_table` filters on membership, which is why the OVER-match case works — `offshore/tourism` is absent from the pin, so the scoped table is `{}` and the fabricated flag is an over-match rather than being swallowed. Innocence is a full-dict comparison over all 76 unflagged walks, not a state comparison, so it would catch a rebuild that leaks into the unflagged path. `EXPECTED_FLAGGED_STATE_CENSUS` being derived rather than pinned is the right call and does have bite: it is derived from the *pinned engine* table and compared against the *measured* funnel, so a flagged walk that failed to rewrite reddens without a re-pin.

**C — no behaviour change for existing callers.** Keyword-only, defaults to `()`, and the rebuild sits behind `if disclosed_review_flags:` — the empty case never constructs a second model, so the coverage floor and authoring CLI take a byte-identical path. No positional-call risk.

**D — constants and doc claims check out; I recomputed them.** 67/15/2/0 and 60/15/1/8 both sum to 84; the deltas (−7 SUPPORTED, −1 NEEDS_INPUT, +8 HUMAN) match the per-flag table exactly (ACTIVITY_BOUNDARY 6 from SUPPORTED, NOT_CERTAIN 1 from SUPPORTED + 1 from NEEDS_INPUT), and funnel NEEDS_INPUT == 1 independently pins the NOT_CERTAIN-from-NEEDS_INPUT row the printed table claims. The 6 ACTIVITY_BOUNDARY walks reconcile with the override reasoning (merit ×2, family ×1, undecided ×2, medical ×1), the 8 changed fixture files are exactly the 8 table rows with the right flag each, 84 − 8 = 76 matches the innocence test, and "nine flags rewrite zero" = MULTI_PURPOSE_TRIP + AMBIGUOUS_SPONSOR + the seven compliance disclosures. The `_REBUILT_REQUEST_FIELDS` pin is fail-closed in both directions (a new defaulted field reddens even where dropping it would be harmless — over-strict, but deliberate and self-naming).

Two honest residues, neither blocking: (i) the field pin binds field *names*, not *values* — a rebuild that passed `facts=()` would still be invisible, though it is also unobservable, since `_apply_disclosed_review_flags` replaces candidates, missing facts, reasons and quotes wholesale, so no claim in the file would change; (ii) `test_every_disclosure_flag_reports_the_walks_it_rewrites`'s closing "a flag started or stopped firing" assert is nearly tautological (both sides are hand-pinned to the same two names) — in practice an unpinned new flag raises `KeyError` on `EXPECTED_REVIEW_REASON_FOR_FLAG[flag]` first, which is the loud failure the message promises. Reason-code strings and `EXPECTED_STATE_CENSUS`/`Counter`/`copy`/`gold_coverage_eval` bindings are not visible in the diff; the attested 25-green run (17 pre-existing + 8 new = 25, which cross-checks) is what carries them. The stray space before `@pytest.fixture` in the stdin rendering is a paste artifact, consistent with the attested `ast.parse`.

**E — clean.** Synthetic interview answers, question ids and enum literals only; no client PII, no OSINT, no credential, no key-shaped string in any of the 8 fixtures or the two source files.

VERDICT: OK
