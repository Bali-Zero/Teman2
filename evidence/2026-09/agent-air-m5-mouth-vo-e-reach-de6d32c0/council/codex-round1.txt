TRIMMED TRANSCRIPT — codex-round1.txt

The raw `codex exec` transcript of this round was 10297 lines / 859172 bytes: the seat's
own tool calls, every hook line and the full text of every file it read (this
repository's docs are large, and the seat read several). Committing three of
those is 3 MB of evidence nobody can review, so each is trimmed HERE to the two
parts a reader needs: the PROMPT the seat was given, and its FINAL ANSWER up to
and including the verdict line. Nothing between them is a claim — it is the
seat's own navigation — and nothing after the verdict is review (it is the
harness's checkpoint hook failing against the read-only sandbox). The trim is
mechanical and reproducible: prompt = first 70 lines, answer = from the last
`codex` speaker marker before the last VERDICT line, through that line.

========================================================================
PROMPT (first 70 lines, verbatim)
========================================================================
Reading additional input from stdin...
OpenAI Codex v0.154.0
--------
workdir: /Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 01a09b19-6197-7ba0-a2af-a2bafc2992db
--------
user
You are an adversarial correctness reviewer. Read the patch at
.probe-voe/review-diff.patch in this worktree (622 lines: the hand-written half of
the change). The patch is the diff between origin/main (eecd7f06) and HEAD of a PR
in the Nuzantara monorepo. The PR ALSO adds 10 generated JSON fixtures under
apps/backend-rag/backend/tests/services/visa_engine/gold_coverage/fixtures/walks/,
omitted from the patch because they are machine-generated output of the modified
generator and are byte-compared against a fresh generation by
walk-corpus-determinism.test.ts.

CONTEXT — what the change claims, so you can try to falsify it:

1. The Visa Oracle interview (apps/mouth, tree.ts/flow.ts/fact-mapper.ts) is a
   hand-written decision tree. A signed rule pack
   (apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-020.signed.json)
   decides which visa products a set of facts supports.
2. generate-walk-corpus.ts drives the REAL interview state machine over a list of
   scenarios and writes each walk's wire facts as a fixture; the backend census test
   replays every fixture against the signed pack and pins the outcome.
3. Measured before this PR: 29 products have at least one SUPPORT rule in the signed
   pack, and only 17 were ever named by a walk. The 12 missing ones were invisible to
   every test in the repo.
4. This PR adds 10 scenarios (reaching 11 of the 12 products), pins their outcomes,
   and adds a guard test that reads the SUPPORT-bearing product codes from the signed
   pack payload and fails when any is named by no walk. The 12th product (BRIDGING)
   is excused in a one-row UNREACHABLE_BY_RULING table because all its SUPPORT rules
   need a fact an owner ruling forbids collecting.
5. No file in tree.ts / flow.ts / fact-mapper.ts is touched: the claim is that every
   fact those 11 products' rules read was ALREADY collected by the interview, and only
   the corpus convention of answering each question with its FIRST option kept them
   unexercised.

Judge these specifically:
(a) Is the new guard test real, or can it pass vacuously? Could it be green while the
    class it exists to catch is present? Is reading the ENGINE decision (not the public
    one) the right subject, given the public layer's minor-privacy hold?
(b) Is UNREACHABLE_BY_RULING a hole? Can a future engineer silence a real gap with a row?
    Are the guilt tests genuinely guilt (do they fail if the checker is broken)?
(c) Does replacing the census assertion
    `census.get("HUMAN_REVIEW_REQUIRED", 0) == 0` with an assertion that names the one
    held walk and its reason WEAKEN the guard? Argue both ways and conclude.
(d) Any claim in the comments/docstrings that the code does not support, any number that
    cannot be true, or any test whose name lies about what it asserts.
(e) Correctness bugs of any kind in the added Python or TypeScript.

Rules: you may read any file in this worktree. Do not run git commands that write.
Be specific: file + line + why. If you find nothing that would change the code, say so.
End your answer with exactly one final line, either
VERDICT: OK
or
VERDICT: DEFECT
warning: Skill descriptions were shortened to fit the skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
hook: SessionStart
hook: SessionStart
hook: SessionStart Completed
hook: SessionStart Failed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed

========================================================================
FINAL ANSWER (transcript lines 10223-10255, verbatim)
========================================================================
codex
Ho trovato due difetti funzionali che richiedono modifiche.

1. **Il guard non rappresenta la semantica reale del pack.** In [_support_bearing_product_codes()](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:855), l’insieme viene costruito solo iterando `product_version_ids`. Le regole `GLOBAL` hanno obbligatoriamente quel campo a `None`, ma l’evaluator le applica a ogni prodotto ([compiler.py:1344](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/services/visa_engine/compiler.py:1344), [models.py:506](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/services/visa_engine/models.py:506)). Una futura regola GLOBAL SUPPORT può quindi rendere supportato un prodotto che nessuna walk nomina, lasciando il guard verde. Questo falsifica anche il nome `test_every_support_bearing_product_is_named_by_some_walk` e la promessa “the day a new pack is signed, this set moves by itself”.

   Inoltre, il lato SUPPORT include regole future/scadute e prodotti `DEPRECATED`/`OBSOLETE`, mentre il lato engine filtra per validità temporale e `ACTIVE` ([evaluator.py:1482](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/services/visa_engine/evaluator.py:1482), [compiler.py:1338](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/services/visa_engine/compiler.py:1338)). Qui l’effetto opposto è un falso rosso: il guard può pretendere una walk per un prodotto che l’evaluator non può emettere a `_AS_OF`.

   Il support set deve essere derivato dal pack verificato e compilato, iterando i prodotti attivi/effective e `rules_for(product, effective_at=_AS_OF)`. Servono guilt case per GLOBAL, regola fuori validità e prodotto non attivo.

2. **`UNREACHABLE_BY_RULING` può nascondere una regressione futura senza cambiare la tabella.** [_unreached_support_products()](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:900) sottrae incondizionatamente qualsiasi codice excused. Il pin a BRIDGING verifica soltanto la chiave e una sottostringa ([linea 1616](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:1616)); lo stale checker rileva soltanto prodotti già nominati.

   Oggi l’eccezione è fattualmente corretta: tutte e quattro le regole BRIDGING richiedono `intent.requested_product_code`, e il mapper lo forza a `UNKNOWN(NOT_ASKED)` ([fact-mapper.ts:917](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/fact-mapper.ts:917)). Ma se un nuovo pack aggiunge una route BRIDGING che non richiede quel fatto e nessuna walk la esercita, l’eccezione esistente continua a sopprimere il gap e tutti i test restano verdi. L’eccezione dovrebbe essere strutturata e il test dovrebbe verificare che ogni SUPPORT route effettiva del prodotto richieda ancora il fatto vietato.

   Il guilt E33G è autentico per la differenza tra support set e named set corrente. Il guilt C1 è autentico soltanto per lo stale checker: non prova che una giustificazione applicata a un prodotto realmente non raggiunto sia valida. Non copre quindi il buco dell’allowlist.

Sul cambio da `HUMAN_REVIEW_REQUIRED == 0`:

- È una debolezza rispetto al vecchio invariante: il nuovo test accetta esattamente un caso che il vecchio rifiutava. Perciò “strictly stronger” a [linea 1090](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:1090) è falso.
- È però molto più forte di un semplice `== 1`: blocca un secondo hold, uno scambio di walk e una ragione differente. Poiché l’hold del minore è intenzionale, la sostituzione è una ragionevole eccezione localizzata. La descrizione corretta è “narrowly relaxes the zero-hold invariant while pinning the permitted cause”.
- Usare la decisione ENGINE per la reachability è corretto: il quesito è se l’intervista possa produrre fatti che supportano il prodotto. La separazione dal layer pubblico è verificata esplicitamente a [linee 1549–1556](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:1549).

Altre affermazioni errate:

- Il docstring del census parla ancora di 67 walk e `2/10/55` ([linea 5](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:5)); ora sono 94 e `1/2/15/76`.
- Lo stesso docstring afferma ancora che ogni domanda usa la prima opzione ([linea 162](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:162)); le dieci nuove walk esistono precisamente per violare quella convenzione. Identico errore in [generate-walk-corpus.ts:8](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/scripts/visa-oracle/generate-walk-corpus.ts:8).
- Il default di IDR 1 miliardo non è “one order of magnitude under both” 10 miliardi e 2,5 miliardi ([generate-walk-corpus.ts:650](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/scripts/visa-oracle/generate-walk-corpus.ts:650)); rispetto a 2,5 miliardi è soltanto 2,5 volte inferiore.
- Il commento remote afferma che i default dicono sì a tutti i fatti bloccanti ([generate-walk-corpus.ts:687](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/scripts/visa-oracle/generate-walk-corpus.ts:687), [census:539](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:539)). `remote_clients` ha invece `foreign` come prima opzione, che mappa già a `serves_indonesian_clients=false` ([tree.ts:596](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/tree.ts:596)).
- “Ten walks … one per product” è matematicamente falso: dieci walk coprono undici prodotti ([walk-corpus-determinism.test.ts:72](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/walk-corpus-determinism.test.ts:72)).
- Il guilt docstring dice che C1 è nominato da 35 walk ([linea 1659](/Users/balizero/nuzantara/.worktrees/mouth-vo-e-reach/apps/backend-rag/backend/tests/services/visa_engine/test_interview_walk_census.py:1659)); il corpus risultante lo nomina in 39.

Il replay diretto corrente conferma invece i numeri sostanziali: 84 walk precedenti nominavano 17 prodotti; le dieci nuove portano a 28, aggiungendo esattamente gli 11 codici dichiarati; BRIDGING resta il ventinovesimo. Non ho trovato errori nelle dieci configurazioni TypeScript o nei candidati attualmente pinned. Pytest non ha potuto avviare i test nel sandbox read-only perché non esiste una directory temporanea scrivibile; l’errore precede l’esecuzione dei test.

VERDICT: DEFECT

