task_id: infra-dynamic-workflow-3c
brief_ref: evidence/brief.yml # canonical staged name — harness-floor.yml stages this PR's dated brief there before linting
gear: 3
pr: 6811
base_sha: 2d20035eadab8fe4acb9594e9dac382c9257de21 # pragma: allowlist secret
council_run: council-journal.jsonl
pii_scan: clean
result: >-
  `.github/workflows/dynamic-workflow-selftest.yml` is armed: a non-required PR check running
  `dynamic_workflow.py --selftest`, pytest by name on `test_dynamic_workflow.py` and the two lint
  wrapper test files, and both lint scripts' default sweep from the repo root. The pre-existing
  blind-scan gap in `scripts/lint_workflow_script.py` / `scripts/lint_model_cards.py` (an explicit
  target that exists but yields zero in-scope files reported clean instead of refusing) is closed
  with per-target accounting, so one productive sibling target can no longer mask a blind one in
  the same invocation. Two live council rounds (codex-gpt-5.6-sol, kimi-code/k3) ran against the
  staged diff; every MEDIUM finding that named a real defect is cured in this PR's cure commit,
  and the three findings that describe intentional, pre-existing or mandate-overridden behaviour
  are recorded below as dissent rather than silently dropped.
diff:
  summary: >-
    13 files, 757 insertions / 25 deletions vs merge-base (`git diff --stat
    $(git merge-base origin/main HEAD) HEAD`, measured on the final head after the cure commit):
    the workflow file itself, one watcher-coverage registration row, the Gear-3 evidence pack
    (brief.yml, pack.yml, council-journal.jsonl, 2 verbatim council reviews), the two lint
    scripts' blind-scan closure, their two wrapper test files, one `.prettierignore` entry
    scoping `reviews/**` out of prettier, and one fixture README the lint_model_cards wrapper
    tests reference.
  commits:
    - "ci(dynamic-workflow): arm the PR3c hot-zone check and close its blind-scan gap"
    - "fix(dynamic-workflow): close the two real CI reds PR #6811 caused"
    - "fix(dynamic-workflow): PR3c cure round — R1/R2 paths, council journal, evidence pack"
lanes:
  - { lane: dynamic-workflow-3c, role: build, seat: claude-sonnet-5 }
  - { lane: council-round-1, role: review, seat: codex-gpt-5.6-sol }
  - { lane: council-round-1, role: review, seat: kimi-code/k3 }
bites:
  consumer: >-
    Every future PR touching the 10 paths in the workflow's `paths:` filter (the harness script,
    either lint script or its tests, the model-card corpus, the workflow-harness JS family, the
    dynamic-workflow skill, or this workflow file itself) — the check is their real CI consumer,
    closing `dw-selftest-unarmed`.
  observation: >-
    The `dynamic-workflow-selftest` check ran green on PR #6811 itself (this PR IS the first
    consumer, by construction — the workflow's own `paths:` entry means editing it re-triggers
    it); its log names `test_dynamic_workflow.py`, `test_lint_workflow_script.py` and
    `test_lint_model_cards.py` by name in the pytest step output. Run URL pasted in the PR body
    against the FINAL head, after this cure commit.
receipts:
  - claim: >-
      `python3 scripts/dynamic_workflow.py --selftest` prints SELFTEST OK and exits 0 on this
      head, after the R1/R2 paths cure and the evidence pack.
    cmd: "python3 scripts/dynamic_workflow.py --selftest"
    exit: 0
    ts: "2026-09-19T08:10:00Z"
    seat: claude-sonnet-5
  - claim: >-
      `scripts/tests/test_dynamic_workflow.py` passes in full: 112 passed.
    cmd: "python3 -m pytest scripts/tests/test_dynamic_workflow.py -q"
    exit: 0
    ts: "2026-09-19T08:10:05Z"
    seat: claude-sonnet-5
  - claim: >-
      The two lint wrapper test files pass in full, including the per-target blind-scan
      regression (Kimi finding 2 cure) and the structural YAML-parse of the workflow's lint
      steps (Codex finding 2 cure): 53 passed.
    cmd: "python3 -m pytest scripts/tests/test_lint_workflow_script.py scripts/tests/test_lint_model_cards.py -q"
    exit: 0
    ts: "2026-09-19T08:10:10Z"
    seat: claude-sonnet-5
  - claim: >-
      `lint_workflow_script.py`'s default sweep (no positional target, as the armed workflow
      step invokes it) is clean: 6 file(s) scanned, no violations.
    cmd: "python3 scripts/lint_workflow_script.py"
    exit: 0
    ts: "2026-09-19T08:10:15Z"
    seat: claude-sonnet-5
  - claim: >-
      `lint_model_cards.py`'s default sweep is clean: 1 file(s) scanned, no violations.
    cmd: "python3 scripts/lint_model_cards.py"
    exit: 0
    ts: "2026-09-19T08:10:18Z"
    seat: claude-sonnet-5
  - claim: >-
      actionlint runs clean against the final workflow YAML in this environment — the mandate's
      sanctioned `-shellcheck=` false positive was NOT reproduced, so PREFLIGHT_PACK_SKIP=1 is
      not invoked.
    cmd: "actionlint .github/workflows/dynamic-workflow-selftest.yml"
    exit: 0
    ts: "2026-09-19T08:10:25Z"
    seat: claude-sonnet-5
  - claim: >-
      Watcher coverage holds: all 121 live workflow(s), including this PR's new one, are covered
      by main-push-failure-watch.yml (one pre-existing, unrelated stale-entry warning for a
      renamed workflow, not opened by this PR).
    cmd: "python3 scripts/check_watcher_coverage.py"
    exit: 0
    ts: "2026-09-19T08:10:30Z"
    seat: claude-sonnet-5
  - claim: >-
      detect-secrets against a SCRATCH copy of the real `.secrets.baseline` flags zero new
      findings across the three files this diff adds under `evidence/`: brief.yml (the pragma
      already covers its `base_sha`) and both `reviews/*.md`. `results` sections are byte-for-byte
      identical (sorted-key JSON diff) before and after the scratch scan.
    cmd: 'cp .secrets.baseline /tmp/scratch2.secrets.baseline && detect-secrets scan --baseline /tmp/scratch2.secrets.baseline evidence/2026-09/agent-air-m5-infra-dynamic-workflow-3c-91b7faec/brief.yml evidence/2026-09/agent-air-m5-infra-dynamic-workflow-3c-91b7faec/reviews/council-codex-gpt-5.6-sol.md evidence/2026-09/agent-air-m5-infra-dynamic-workflow-3c-91b7faec/reviews/council-kimi-code-k3.md; diff <(python3 -c "import json;d=json.load(open(''.secrets.baseline''));print(json.dumps(d[''results''],sort_keys=True))") <(python3 -c "import json;d=json.load(open(''/tmp/scratch2.secrets.baseline''));print(json.dumps(d[''results''],sort_keys=True))")'
    exit: 0
    ts: "2026-09-19T08:10:40Z"
    seat: claude-sonnet-5
  - claim: >-
      `.github/workflows/dynamic-workflow-selftest.yml`'s `paths:` filter carries exactly 10
      entries after the R1/R2 cure (wildcard replaced by the two named test files; the workflow's
      own path restored).
    cmd: "grep -A15 '^\\s*paths:' .github/workflows/dynamic-workflow-selftest.yml | grep -c '^\\s*- \"'"
    exit: 0
    ts: "2026-09-19T08:10:45Z"
    seat: claude-sonnet-5
adversarial:
  - {
      seat: codex-gpt-5.6-sol,
      round: 1,
      verdict: REWORK,
      findings: 4,
      block: 0,
      rework: 4,
      note: 0,
      cured: 2,
      accepted_as_residual: 2,
    }
  - {
      seat: kimi-code/k3,
      round: 1,
      verdict: REWORK,
      findings: 7,
      block: 0,
      rework: 7,
      note: 0,
      cured: 4,
      accepted_as_residual: 3,
    }
dissent:
  - seat: "codex-gpt-5.6-sol (round 1) vs dw-builder-2, kimi-code/k3 (round 1, finding 4) vs dw-builder-2"
    status: PLAUSIBLE
    objection: >-
      MEDIUM/LOW: the `paths:` filter carries a 9th/10th entry beyond the mandate's literal list
      — `.github/workflows/dynamic-workflow-selftest.yml` names itself. Both titolari independently
      flagged this in round 1, reviewing a staged diff that had ALREADY dropped the entry
      (briefed from a stale 8-item sentence in brief.yml at review time). CONDUCTOR RULING R2
      (2026-09-19 08:10Z addendum): mandate line 513 (the "PR3c: answers to the builder's two
      questions" addendum, Q2) explicitly directs adding this path — an edit to the workflow
      alone must re-run it, or that edit is scar #2 (Esiste≠Armato) one level up. The finding's
      factual premise (the entry deviates from the earlier literal-8 reading) is accurate; the
      ruling overrides it rather than curing toward the objection's ask. Restored in this cure
      commit; brief.yml's constraint sentence corrected from "8 entries" to "10 entries" in the
      same commit.
  - seat: "codex-gpt-5.6-sol (round 1) vs dw-builder-2"
    status: CONFIRMED
    objection: >-
      MEDIUM: `test_workflow_yaml_lint_steps_use_default_sweep_via_cli` (as reviewed) checked
      stripped TEXT LINES for the lint `run:` commands rather than parsing the workflow YAML — a
      decoy job or a reflowed block-scalar carrying the same string would satisfy the assertion
      without a real step ever running the command. CONFIRMED and cured before this builder took
      over (present on HEAD): the test now `yaml.safe_load`s the workflow, walks
      `doc["jobs"]["dynamic-workflow-selftest"]["steps"]`, and asserts the exact `run:` values —
      scoped to the named job so a decoy elsewhere in the file cannot satisfy it.
  - seat: "codex-gpt-5.6-sol (round 1, finding 3) vs kimi-code/k3 (round 1, finding 1) vs dw-builder-2"
    status: CONFIRMED
    objection: >-
      MEDIUM: the new model-card innocence tests pointed `lint_model_cards.main()` straight at
      `clean.md`, whose frontmatter `date:` is a static fixture value (`2026-09-18`); `main()`
      takes no `today=` override, so the test would start failing on wall-clock 2026-10-19 when
      `FRESHNESS_DAYS = 30` expires the fixture — a lint-arming PR shipping a CI job that goes
      stale-red in 30 days. Both seats demonstrated the mechanism independently. CONFIRMED and
      cured before this builder took over (present on HEAD): both innocence tests
      (`test_innocence_one_clean_in_scope_file_is_enough`,
      `test_innocence_explicit_dir_with_one_clean_file_exits_0`) now write a tmp-path copy of
      `clean.md` with `date: {date.today().isoformat()}` interpolated, same cure pattern the
      file's own pre-existing `find_violations`-level tests already used.
  - seat: "codex-gpt-5.6-sol (round 1, finding 4) vs kimi-code/k3 (round 1, finding 5) vs dw-builder-2"
    status: PLAUSIBLE
    objection: >-
      MEDIUM/LOW: the blind-scan closure retains an exception for an explicit EXISTING FILE of
      the wrong suffix (`.txt` for the JS lint, non-`.md` for the model-card lint) — it still
      exits 0 with "no violations (0 file(s) scanned)" rather than refusing under the quoted
      universal rule "ANY invocation that scans zero files exits 2". Accurate as read. NOT cured:
      this is pre-existing, tested behaviour
      (`test_innocence_explicit_non_js_file_is_still_green` /
      `test_innocence_explicit_non_md_file_is_still_green`, both predating this PR), the new
      docstring in each lint script's `main()` states the carve-out explicitly, and it is an
      informed, deliberate scope decision — a file of the wrong suffix was never in scope to
      begin with, unlike a directory that yields zero IN-SCOPE files. Declined; stated here
      rather than left for the gate to discover.
  - seat: "kimi-code/k3 (round 1, finding 2) vs dw-builder-2"
    status: CONFIRMED
    objection: >-
      MEDIUM: the original blind-scan guard was gated on a GLOBAL `scanned == 0` across all
      explicit targets in one invocation, so a productive sibling target could mask a blind one
      in the same call (`lint empty_dir real.js` went green, silently ignoring `empty_dir`) —
      cicatrix #2 one level deeper than the single-target case the mandate named. Demonstrated by
      the seat directly. CONFIRMED and cured before this builder took over (present on HEAD):
      both lint scripts now track `dir_contributions` per raw target and judge each explicit
      target (missing path or zero-yield directory) independently of whether siblings scanned
      cleanly; guilt test
      `test_guilt_mixed_targets_empty_dir_not_masked_by_productive_sibling` in both wrapper test
      files.
  - seat: "kimi-code/k3 (round 1, finding 3) vs dw-builder-2"
    status: CONFIRMED
    objection: >-
      MEDIUM: the `paths:` filter (as reviewed) matched all 27 `scripts/tests/test_lint_*.py`
      files on disk, but the pytest step ran only 2 of them by name — a PR touching any of the
      other 25 would trigger this check, which would go green without ever running the changed
      file. CONFIRMED and cured in this commit (R1, mandate Q1): the wildcard is replaced with
      the two explicit entries `scripts/tests/test_lint_workflow_script.py` and
      `scripts/tests/test_lint_model_cards.py` — the filter now names exactly what the pytest
      step runs.
  - seat: "kimi-code/k3 (round 1, finding 6) vs dw-builder-2"
    status: CONFIRMED
    objection: >-
      LOW: `timeout-minutes: 10` (as reviewed) passed the repo's own timeout-floor lint
      (`scripts/lint_workflow_timeout_floor.py`, `FLOOR_MINUTES = 10`) by exactly zero margin,
      and that lint's own header records a worst-observed checkout of 599s — essentially the
      entire budget for one step. CONFIRMED and cured before this builder took over (present on
      HEAD): `timeout-minutes: 15` — conductor ruling R3 (2026-09-19 08:10Z addendum) accepts 15
      as superseding the mandate's earlier stated "10", citing the same lint as evidence.
  - seat: "kimi-code/k3 (round 1, finding 7) vs dw-builder-2"
    status: PLAUSIBLE
    objection: >-
      LOW: `raw.glob(DEFAULT_GLOB_PATTERN)` is non-recursive, so an explicit directory whose
      in-scope files live only in subdirectories would blind-scan (report zero, now correctly
      refusing per the finding-2 cure, but arguably a false blind-scan rather than a real one).
      Accurate as read; the seat itself called it "a documentation nit at most". NOT cured: the
      armed CI usage is the DEFAULT sweep (no positional target) which is flat by convention and
      by this mandate's own text, matches the two lint scripts' existing established convention,
      and is not a behaviour this mandate asked to change. Declined.
residuals:
  - >-
    The three PLAUSIBLE dissent entries above (own-workflow-path restoration, wrong-suffix
    carve-out, non-recursive glob) are intentional and stated, not silently dropped — none blocks
    the mandate's stated acceptance criteria (A1-A5 in the sibling brief.yml).
  - >-
    R4 (conductor addendum, 2026-09-19 08:10Z) authorized three files beyond the strict
    hot-zone/lint/test/pack set present in this diff: `.github/workflows/main-push-failure-watch.yml`
    (one row, closing the watcher-coverage gap this PR's own new check opened), `.prettierignore`
    (scoping `reviews/**` out of prettier — the council reviews are verbatim seat output, never
    reformatted), and `scripts/tests/fixtures/lint_model_cards/README.md` (documents the fixture
    the two `test_lint_model_cards.py` wrapper tests read).
  - >-
    Codex's own dispatch banner (`review-sol.err`, recovered this session) reports `model:
    gpt-6-astra`, not the roster name `codex-gpt-5.6-sol` the council journal and this pack
    declare the seat under. Recorded here rather than hidden — see the council journal note for
    the full recovered command and banner text; the seat identity used throughout this pack is
    the COUNCIL_REVIEW_SEATS_TITOLARI roster name the mandate names, per its own convention of
    naming seats by role/kit rather than by exact underlying model string.
