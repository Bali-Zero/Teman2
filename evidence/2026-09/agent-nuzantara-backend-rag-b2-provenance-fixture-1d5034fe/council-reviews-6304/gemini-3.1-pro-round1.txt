1. **Category (c)**: Yields nothing.
2. **Category (d)**: Yields nothing.
3. **Category (f)**: MAJOR, `wa_codex_leg.py`:655 (and test `_wire` fixture), defect: The code incorrectly attempts to read `context_length` from `evidence_inputs` instead of the root `parsed_wire` (as the existing normal path does). The test asserts what the flawed code does rather than what the contract requires by erroneously placing `"context_length": 2` inside the `evidence_inputs` dictionary in the `_wire` mock, masking the bug. proof: In `wa_codex_leg.py`, the new branch reads `evidence_inputs.get("context_length")` which will evaluate to `None` in production because `context_length` is not stored in `evidence_inputs`. The pre-existing normal path (around line 998) correctly reads `parsed_wire.get("context_length", 0)`. The `_wire` test stub incorrectly nests `"context_length": 2` within `evidence_inputs` instead of at the root payload to artificially make `test_unsupported_verdict_preserves_d6_shape` pass its assertion `assert data["context_length"] == 2`.
