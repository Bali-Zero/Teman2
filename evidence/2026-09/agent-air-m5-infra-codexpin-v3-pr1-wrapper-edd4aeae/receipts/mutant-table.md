# Mutation table — C1 acceptance (gate 2026-09-26 REWORK-BUILD condition)

Each guard deleted from a COPY of the fixed wrapper; the SAME real test file (23 tests),
pointed at that copy via a repointed `_WRAPPER` line, run as a real `python3 -m pytest`
subprocess (no simulation). Full harness: `scripts/tests/mutants.py` equivalent, run this
session against `infra/launchagents/wrappers/wa-codex-broker-wrapper.sh`.

| Guard deleted                                                                                   | Result on the FIXED wrapper (this PR) | Discriminating test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NUL probe (`read -r -d ''`)                                                                     | **1 red**, 22 green                   | `test_guilt_present_but_invalid_pin_refuses[nul_same_line_planted]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| exactly-one-line check                                                                          | **1 red**, 22 green                   | `test_guilt_present_but_invalid_pin_refuses[two_lines_diff_versions_planted]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `_is_semver`                                                                                    | **2 red**, 21 green                   | `test_guilt_present_but_invalid_pin_refuses[bad_semver_planted]` AND `[traversal_escape_planted]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `-L` symlink check                                                                              | **1 red**, 22 green                   | `test_guilt_present_but_invalid_pin_refuses[symlink_pin]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `-f`/`-x` binary check                                                                          | **1 red**, 22 green                   | `test_guilt_present_but_invalid_pin_refuses[bin_missing]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `-f` half only (revert to `-x`-only, C2)                                                        | **1 red**, 25 green                   | `test_guilt_present_but_invalid_pin_refuses[bin_is_directory]` (delta-council round 1 catch, codex-gpt-5.6-sol: C2's required test was missing — added same round)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| N1 re-assert block removed (all 6 names: HOME_DIR/RUNTIME_DIR/VENV_PY/TAG/ORGAN_ID/SIDECAR_DIR) | **1 red**, 25 green                   | `test_guilt_env_cannot_clobber_post_source_literals` (delta-council round 1 catch: N1 had no dedicated regression — added same round; delta-council round 2 catch: the round-1 test clobbered only 3/6 names — RUNTIME_DIR/VENV_PY/TAG — and never checked the heartbeat, so removing ONLY the HOME_DIR/ORGAN_ID/SIDECAR_DIR re-asserts left it green; corrected by also clobbering those 3 in the daemon env and asserting the heartbeat JSON lands at the wrapper's own `HOME_DIR`/`ORGAN_ID` path with `status":"starting"`/`note":"exec daemon"`, never the hostile one; mutation-verified: removing just those 3 lines now turns this test red) |
| N4 `[ -r ]` readability check removed                                                           | **1 red**, 25 green                   | `test_guilt_mode_000_pin_reports_unreadable_not_line_count` (delta-council round 1 catch: N4 had no dedicated regression — added same round)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| (none — shipped wrapper)                                                                        | **0 red**, 26 green                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Delta-council round 1 (codex-gpt-5.6-sol, read-only sandbox) also caught that the FIRST C3 test broke
`RUNTIME_DIR`'s mode while the pin file lived OUTSIDE it (siblings, not container/contents) — the test
only proved the new top-level guard fires, not the real EACCES-reads-as-absent defect. Corrected: the
pin file now lives INSIDE `RUNTIME_DIR`; mutation-verified (removing the C3 guard now makes the test
fail with the ORIGINAL bug's exact diagnostic, `no pin file at ... (legacy)`, not a generic crash).

Acceptance (gate C1): deleting ANY of the 5 guards turns >=1 test red. **Met for all 5.**

Before this fix (gate's own table, head 08a5d93537, old 17-test file):

| Guard deleted  | Result                                                                                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| NUL probe      | 17/17 green (non-discriminating)                                                                                                                       |
| one-line check | 17/17 green (non-discriminating)                                                                                                                       |
| `_is_semver`   | 17/17 green (non-discriminating); isolating probe: `9.9` APPLIED, `../escape` APPLIED with the derived binary path landing OUTSIDE `PINNED_CODEX_ROOT` |
| `-L` symlink   | 1 red (already discriminating)                                                                                                                         |
| `-x` binary    | 1 red (already discriminating)                                                                                                                         |

The fix: `embedded_nul`/`bad_semver`/`two_lines` (pre-existing cases) never planted the binary
the fault would otherwise resolve to, so the binary-existence check (a LATER guard) masked
whichever EARLIER guard the mutation removed. Four new cases plant the binary/tree the fault
resolves to: `nul_same_line_planted` (NUL genuinely mid-line, single trailing newline — the
pre-existing `embedded_nul` case puts the NUL on its OWN line, which the one-line check alone
already rejects, independent of the NUL probe), `two_lines_diff_versions_planted` (two DIFFERENT
versions, both planted, so the guard's effect is observable), `bad_semver_planted`, and
`traversal_escape_planted` (`../escape`, planted one level above `PINNED_CODEX_ROOT`).
