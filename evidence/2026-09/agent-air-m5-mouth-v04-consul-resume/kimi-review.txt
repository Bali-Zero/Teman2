• **Verdict: PASS** — no material defects found in the supplied diff. Observations below are non-blocking; two items are unverifiable from the supplied files alone and are flagged as such.

  **What checks out**

  - **Context discrimination is runtime-enforced, not just typed.** The scope memo in `ConsentHandoff.tsx` rebuilds scope from `context` and drops `state`/`assessmentReference` for `CONSULTATION`; the untyped-caller test (smuggled `state`, `assessmentReference`, `answers`) proves the receipt and WhatsApp text stay clean. `validScope` also rejects enlarged persisted scopes (`Object.keys(value).length === 1` for CONSULTATION), so a tampered sessionStorage receipt can't widen scope either.
  - **Fresh consent on scope change, both directions.** `sameConsentScope` compares `context`, and the rerender test (ASSESSMENT→CONSULTATION→ASSESSMENT) plus the store-level context-change test cover the switch. The Edit-answers path works because `displayedOutcome` gates on `current.kind === "verdict" && !evaluating`, so Edit collapses scope to CONSULTATION and the result receipt no longer matches — UI and storage both revoke.
  - **Guardian gate.** Computed from `state.facts.birth_date` independent of outcome, so a known minor is gated pre-evaluation (tested with a 10-year-old birthdate computed from the current year — robust). Guardian un-check re-disables WhatsApp consent and hides the link.
  - **TTL.** Wall-clock expiry via fake timers at exactly `TTL - 1` / `TTL`, StrictMode-mounted, storage pruned, QR and link removed without remount. Legacy v2 receipts are pruned on load rather than carried forward — correct, since consent must not survive the policy bump.
  - **Data minimization.** WhatsApp text for CONSULTATION contains no state/reference/answers (asserted against `1990-01-01`, `nationalities`, `stay_days`); telemetry omits `state` for consultation and carries only the correlation hash.
  - **Accessibility/hydration.** `aria-expanded`/`aria-controls`/`role="region"`/`aria-labelledby` are coherent; the panel is always in the DOM so `aria-controls` never dangles; `[hidden] { display: none }` guards against CSS overrides; Escape returns focus to the toggle; SSR markup includes the disclosure with the panel hidden and no fetch — the `renderToString` test proves `ConsentHandoff` is SSR-safe (no `window` access during render).

  **Non-blocking observations**

  1. **Loading branch has no guardian gate** (`OracleShell.tsx`: `<ConsultantContact language="en" context="CONSULTATION" />` while `hydrated === null`). Unavoidable — the birth date isn't restored yet — and the exposure is only a generic consultation message with no data. Acceptable, but worth a conscious sign-off.
  2. **`OutcomeSheet.handoffSlot` is removed at the call site but `OutcomeSheet.tsx` is not among the 7 changed files.** If that prop was required, this is a type error (Vitest won't catch it — only `tsc` will); if optional, it's now dead API surface. I could not verify from the supplied diff — confirm the prop is optional and consider removing it.
  3. **`createLocalConsentReceipt` throwing on invalid scopes** is asserted by the new store tests, but the diff shows no change to that function — this assumes it already validated scope via `validScope`. Unverifiable here; if it didn't, those tests fail, which the rerun will surface anyway.
  4. Minor coverage gap: no test that a *checked guardian box* resets on context switch (verdict arriving after generic contact). The receipt-driven link revocation is tested; the guardian checkbox reset relies on the truncated `useEffect` — consider one assertion in the existing switch test.
