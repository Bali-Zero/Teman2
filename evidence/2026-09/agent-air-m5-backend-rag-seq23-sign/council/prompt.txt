You are an adversarial REFUTER reviewing a diff for the Visa Oracle rule engine (Indonesian immigration decision tree). Your job is to find defects, not to approve. Report each finding as: SEVERITY (HIGH/MEDIUM/LOW) | file:line | claim | why it is wrong | the command or test that would prove it. End with a one-line VERDICT: PASS (no HIGH/MEDIUM) or FAIL.

Context: this PR signs rule pack sequence 23 (a fold that retires 8 REQUIRE_REVIEW rules into named HARD_FILTER dead ends, flips on_unknown HUMAN_REVIEW->NEEDS_INPUT on 4 hard filters, and adds 3 NO_EFFECT threshold filters). The signed JSON (rulepack-prod-023.signed.json, 285 KB, payload_sha256 e5f791b5...2204, self-verified) is NOT shown. Shown below is everything else:
1. test_seq23_pack.py: new per-row ENGINE witnesses (TestRowEngineWitnesses) - a TRUE persona and an UNKNOWN persona per row through evaluator.evaluate_product + the applicant-facing evaluate path; GUILT reverts one row on a scratch copy and asserts that row's witness goes red naming it (collateral allowed only via a shared unknown fact, only on the UNKNOWN persona). Also a fix (F2) so the removed/inserted-row guilts assert the SPECIFIC id is named.
2. fold_pack_seq23.py: the mismatch messages now name the discrepancy (not retired / retired unexpectedly) instead of the whole expected set. Fold output is unchanged.
3. Census pins for sequence 23 (measured by a replay command: identical to seq-22), inventory pins 9/7 -> 1/0 (printed by review_hold_inventory --json on the signed tree), option-matrix remote_work false|true HUMAN_REVIEW_REQUIRED -> NO_SUPPORTED_PATH, and six gold PRODUCTION_REPLAY_EXPECTATIONS re-derived (personas 2,3,4,5,12,20) because the owner-ratified disposition table replaced their human-review holds.
4. scripts/ci/observe_visa_seq23_signed.py: a no-arg observer.
Look especially for: witnesses that are vacuous or pass for the wrong reason; guilt tests that would pass even if the row were reverted; expectations laundered from engine output without a stated legal/ratified basis; census pins that mask a real regression; anything that would let a HUMAN_REVIEW hold silently disappear beyond the 8+4 rows ratified.

DIFF:
