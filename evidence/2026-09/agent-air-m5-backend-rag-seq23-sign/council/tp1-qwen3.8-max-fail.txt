MEDIUM | apps/backend-rag/backend/tests/services/visa_engine/<REDACTED>:2336-2341 | The seq-23 state-census pin (HUMAN_REVIEW_REQUIRED: 3, NEEDS_INPUT: 1, NO_SUPPORTED_PATH: 14, SUPPORTED_CANDIDATES: 98) is identical to seq-22, yet the diff shows NO `_SEQ23_OUTCOME_CHANGES` dict added anywhere, and no mechanism (skip, version-gate, or branch) that would exclude the 14 new HARD_FILTER rules from the census walk. The 8 inserted dead-end rules (`<REDACTED>`, `hf.active-overstay`, `hf.e33g.local-market`, `<REDACTED>`, `<REDACTED>`, `hf.e33e/<REDACTED>`, `hf.bridging.*`) should, on an empty-fact census walk, fire on their `on_unknown` arm and produce NEEDS_INPUT or NO_EFFECT outcomes for products that previously evaluated to SUPPORTED_CANDIDATES, changing counts. | `grep -n "_SEQ23_OUTCOME_CHANGES\|_OUTCOME_CHANGES" apps/backend-rag/backend/tests/services/visa_engine/<REDACTED>` — if the dict is absent or empty, run `PYTHONPATH=. python -m pytest backend/tests/services/visa_engine/<REDACTED>::<REDACTED> -x` with the real signed pack; any census mismatch proves the pin was laundered from the wrong tree or the mechanism is missing.

MEDIUM | apps/backend-rag/backend/tests/services/visa_engine/test_seq23_pack.py:608-613 (ROW_WITNESSES, "<REDACTED>") | `unknown_fact = "intent.purposes"`, but the UNKNOWN persona sets `intent.purposes` to UNKNOWN while `<REDACTED>` is KNOWN. A `purposes`-gated eligibility rule (`if "EMPLOYMENT" not in purposes`) typically short-circuits to PASS/not-applicable when the purpose is unknown, so the product proof would be ELIGIBLE (or NOT_APPLICABLE), not BLOCKED_UNKNOWN on `intent.purposes`. The witness then demands `unknown_proof.status is <REDACTED> and "intent.purposes" in missing_facts`, which cannot hold — the test would be red on the signed pack itself, or (worse) if it passes, it proves the rule fires on an unrelated fact, making the `unknown_fact` field a lie that masks which fact is actually unresolved. | Run the single-parametrised case: `PYTHONPATH=. python -m pytest backend/tests/services/visa_engine/test_seq23_pack.py::TestRowEngineWitnesses::<REDACTED> -k "<REDACTED>" -x -v`; then confirm the UNKNOWN-persona proof status with a one-off `evaluate_product` call printing `proof.status` and `proof.missing_facts`.

MEDIUM | apps/backend-rag/backend/tests/services/visa_engine/test_seq23_pack.py:524-527 (EVALUATED_AT) | The comment asserts `valid_period.from` on all inserted rules is `2026-09-23T00:00:00Z`, but the signed payload is not in the diff and no test in this file asserts that boundary. If the actual `from` is later (e.g. `2026-09-24`), every engine witness silently evaluates a pack that does not contain the new rules, and all 15 innocence tests pass vacuously against seq-22 behaviour. The GUILT revert test would also pass (reverting to seq-22 shape yields the same absent-rule outcome). | Add `assert all(r.valid_period.from_ <= EVALUATED_AT for r in seq23_compiled.rules if r.rule_id in INSERTED_RULE_IDS)` immediately after compilation in `<REDACTED>`, or inspect the signed JSON: `jq '.payload.rules[] | select(.rule_id | startswith("hf.")) | .valid_period.from' <REDACTED>`.

LOW | apps/backend-rag/backend/tests/services/visa_engine/test_seq23_pack.py:775-780 (_row_witness_failures, NO_EFFECT branch) | For the three NO_EFFECT rows the UNKNOWN-persona check is `elif any(rule_id in r.rule_ids for r in unknown_proof.reasons)` — it only verifies the rule did NOT fire on the unknown fact. It does NOT verify that the product proof status is ELIGIBLE / PASS (i.e. the threshold filter genuinely had no effect). A rule that erroneously fires with a *different* reason code (not `row.reason_code`) on the unknown fact would pass this witness while still altering behaviour. | Extend the check: after the `elif`, add `elif unknown_proof.status is <REDACTED>: failures.append(...)` and run the three NO_EFFECT parametrisations.

LOW | apps/backend-rag/backend/tests/services/visa_engine/test_seq23_pack.py:749-752 (_row_witness_failures, TRUE persona) | The TRUE-persona state check only rejects `HUMAN_REVIEW_REQUIRED`; a regression that turns the TRUE persona into `NEEDS_INPUT` (e.g. a neighbouring rule's `on_unknown` mis-fires on a fact the persona leaves UNKNOWN) passes silently. The witness proves "not held" but not "correctly excluded as the terminal outcome". | Add `if true_state not in ("NO_SUPPORTED_PATH", "SUPPORTED_CANDIDATES"): failures.append(...)` and re-run the 15 parametrisations.

LOW | apps/backend-rag/backend/tests/scripts/visa_engine/<REDACTED>:89-103 | The 1/0 pins cite "A8.4 derived" but the assertion message for `len(escalations) == 0` says "the 4 bridging/B1 HARD_FILTER rules now ask instead of holding" — that accounts for 4 of the 7 seq-22 escalations. The other 3 (the overlapping HUMAN_REVIEW-stage rules that were retired, not flipped) are covered only by the 8-rule retirement, yet the message conflates the two mechanisms. If a future pack re-introduces a non-HARD_FILTER `on_unknown: HUMAN_REVIEW` escalation on a *new* rule, the pin would catch it, but the diagnostic message would mislead the on-call into checking only the 4 bridging/B1 rules. | Cosmetic; confirm by reading the escalation list on the signed pack: `jq '[.payload.rules[] | select(.on_unknown=="HUMAN_REVIEW")] | length' <REDACTED>` should print 0.

LOW | scripts/ci/<REDACTED>:67-72 | The venv fallback (`sys.executable`) means the observer can run pytest under an interpreter that lacks the project's pinned dependencies, producing import-error "failures" that are indistinguishable from real regressions in the exit-code path. Not a correctness defect in the rule logic, but a CI-reliability gap. | `rm -rf apps/backend-rag/.venv && python3 scripts/ci/<REDACTED>` in a bare container; confirm the failure message mentions missing deps rather than a test assertion.

VERDICT: FAIL
