## REFUTER Findings

**MEDIUM | test_seq23_pack.py:~600 (`_row_witness_failures`, `<REDACTED>` row) | The row witness asserts `<REDACTED>` is in the decided-level `missing_facts`, but persona 5's gold expectation says `missing=("person.guardian_consent",)` — the A7-B guardian-consent adapter transforms the missing fact | The witness checks `row.unknown_fact not in unknown["missing_facts"]` where `row.unknown_fact = "<REDACTED>"`. Persona 5's comment says "the A7-B guardian-consent adapter asks for the unanswered consent first," and the gold expectation lists only `person.guardian_consent`. If the adapter replaces (not appends) the missing fact, the decided-level check fails on the **candidate pack itself**, making `<REDACTED>` red. | Run `pytest backend/tests/services/visa_engine/test_seq23_pack.py::TestRowEngineWitnesses::<REDACTED> -k <REDACTED>` on the candidate pack; if it fails, inspect `unknown["missing_facts"]` to confirm whether `<REDACTED>` is present or replaced by `person.guardian_consent`.**

**MEDIUM | test_seq23_pack.py:~540 (`_BRIDGING_APPLICANT` definition) | `_BRIDGING_APPLICANT` does not set `<REDACTED>`; if it defaults to UNKNOWN, the global `hf.active-overstay` filter (on_unknown: NEEDS_INPUT) fires on every bridging UNKNOWN persona, potentially shadowing the bridging filter's own missing fact in the decided output | Persona 20's gold
