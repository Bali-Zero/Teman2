VERDICT: REWORK

FINDINGS:
1. [MAJOR] D5 still has nondeterministic freshness selection and an M3 false-green path — `apps/mouth/scripts/generate-llms-full.ts:37-54` consumes filesystem order, `:103-106` compares only dates, and `:150-156` then truncates to five. A read-only corpus census using the same filtering returned `2026-09-11 count=6`; all six compare equal, so filesystem order decides which article is omitted. The test at `apps/mouth/src/lib/llms-article-exports.test.ts:42-56` uses distinct dates and creates canonical fixtures already in the asserted order at `:104-108`. An insertion-order simulation with the sort removed returned `matchesAssertedBlock:true`; therefore `rework-round2-proofs.txt:20-24` proves M3 red only on that host, not across CI filesystems — add a deterministic secondary key before `slice`, test at least six same-date canonical articles across the cutoff, and test shuffled input independently of `readdirSync`; rerun M1–M3 and refreeze.
2. [MINOR] The committed preview evidence retains a false absolute claim — `evidence/2026-09/agent-air-m5-mouth-shweb-w1-tech-20260911-1bf4ea22/preview-verification.md:22-23` says every commit after `d5e267c735` touches no `apps/mouth` path, but `git show --stat 40646c54be` shows changes to the generator and export test. The later statement at `:55-57` is correct: `git diff --name-only 40646c54be..HEAD` contains only eight evidence files — qualify the earlier claim as historical/pre-D5 or replace it with the Round-2 preview statement.

UNVERIFIED: Vitest and `npm run build` were not rerun because this review is read-only and execution was expressly prohibited; the exact Node 24 coverage run and production promotion remain external gates. D2 remains independently unprovable because its frozen patch post-image bytes are absent; only its recorded SHA exists. Verified without further finding: `git diff --check ea50eb262f..HEAD` exits 0; D5’s generator diff from `08a8905f7a` is exactly the guard plus two comments; every post-`40646c54be` path is evidence-only; size totals are product+research `+460/-57`, evidence `+1028/-0`, baseline `+306/-0`; D3’s preserved body hashes to `e3004f3d…`; D4 contains 38 `is_secret:false` entries; the renamed npm-gate tests and appended adversarial-review account are accurate; no static `tsx`, 15-second timeout, canonical-ID, KBLI-skip, or Vercel build-wiring defect was found.