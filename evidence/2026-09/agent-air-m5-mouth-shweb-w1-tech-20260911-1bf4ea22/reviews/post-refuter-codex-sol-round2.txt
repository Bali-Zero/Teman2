# POST refuter round 2 — Codex Sol (codex exec --sandbox read-only), session 01a09098-31c6-78a3-a9d6-300e14627ba1, reviewed HEAD a3e1ee39bd3f6d271aeea333304c3d6964db6abe

VERDICT: REWORK

FINDINGS:
1. [BLOCKER] The frozen HEAD fails its declared cleanliness gate — `git diff --check ea50eb262f..HEAD` exits 2 on `local-proofs.txt:36` and `negative-old-build-flag.txt:31` (“new blank line at EOF”), contradicting `brief.yml:81-83` and `local-proofs.txt:33-35`; the check was green at `10ce4e8659`, then broken by `a3e1ee39bd` — remove both trailing blank lines, rerun the check on the new HEAD, commit the corrected proof, refreeze, and review that SHA.
2. [MAJOR] The ScenarioToggle visual delta was never exercised despite acceptance being marked verified — `brief.yml:84-86` requires computed styles on the real price elements, but `preview-verification.md:38-40` and `preview-browser-out.json:s1_extras.togglePrice` explicitly say `{previewPrice}` was not rendered; static source at `ScenarioToggle.tsx:318-350` contains the expected styles but does not prove runtime reachability — render a property-to-deposit scenario that exposes `previewPrice` and record its computed Cormorant and `tabular-nums` values, or mark this acceptance unverified.
3. [MAJOR] Conflicting generator flags can silently defeat the production change, and the regression test masks the case — `generate-llms-full.ts:28-29,118-121` lets legacy `LLMS_GENERATE_FULL_ONLY=1` return before ID/freshness even when ARTICLES_ONLY is also 1; `package.json:7` sets only ARTICLES_ONLY, while `llms-article-exports.test.ts:64-66` forcibly clears FULL_ONLY — make ARTICLES_ONLY dominate FULL_ONLY or explicitly set `LLMS_GENERATE_FULL_ONLY=0` in the build, and add a both-flags regression case.
4. [MINOR] The export test does not pin freshness ordering/limit or actual ID URL/body content — its sole publishable canonical EN fixture has the same date as every fixture (`llms-article-exports.test.ts:35-47`), and the URL assertion is against `en + id` (`:73-87`), so an ID-only URL/content regression or removal of sorting/`slice(0,5)` could pass — seed at least six canonical articles with distinct dates; assert the exact ordered five entries and assert the ID URL and body directly.
5. [MINOR] Frozen evidence metadata is stale or inaccurate — `contract-lock.json:7` records `frozenAt=11:10Z`, before remedial commits at 11:45Z, 11:46Z and HEAD at 13:10Z; `brief.yml:106-110` reports evidence `+608`, while `git diff --numstat base..HEAD -- evidence/...` totals `+868/-0`; D4 mechanically passes (`+306/-0`, 38 `is_secret:false` entries, only contract-lock, no regex allowlist), but `brief.yml:61-64` incorrectly describes every entry as a digest/commit ID although the Base64 entry for contract-lock line 13 corresponds to the authority-package path — correct the freeze timestamp, evidence count/block description, and D4 parenthetical.

UNVERIFIED: D2 cannot be independently proven whitespace-only from committed objects because the frozen patch/post-image bytes are absent; only its recorded SHA is committed. The exact Node 24 coverage-mode CI execution and Vercel Production environment variables/promotion were unavailable; the committed local run used Node 22, while the preview build covered the same product tree because `git diff --name-only d5e267c735..HEAD` contains only `evidence/**`. No further defect was found in D1, D3, D4 mechanics, F4’s rebuttal, the appended adversarial-review account, canonical filtering, or KBLI skipping.
rc=0 start=2026-09-11T13:11:26Z end=2026-09-11T13:22:24Z
