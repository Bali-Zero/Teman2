VERDICT: GO-WITH-CONDITIONS

FINDINGS:

1. [MINOR] PERIMETER is exact; no forbidden path or oversized hunk found — evidence: `git apply --check --whitespace=error-all secondhome-prepared.patch` → rc=0; `git apply --numstat` lists exactly the 10 paths frozen in `contract-lock.json:39-49`. `apps/mouth/package.json` changes only the build line (`secondhome-prepared.patch:97-102`), while `StudioApp.tsx` and `ScenarioToggle.tsx` each add only `fontVariantNumeric` (`:164-180`). No lockfile, MDX, pricing, registry, DB, WEB/R19, website, Visa Oracle, token or global-font path appears — required action: apply only patch SHA `90589bfa…e754f`; preserve this scope.

2. [MINOR] Retiring all four waivers is dependency-correct; none should reappear as a current finding — evidence: `package-lock.json:2883-2885` has `@hono/node-server@2.0.11`; `:15345-15347` has `find-my-way@9.7.0`; `:16105-16107` has nested `js-yaml@3.15.1`; `:18954-18956` has root `js-yaml@4.3.1`. Those versions meet the official fixes: Hono 2.0.5/2.0.10, find-my-way 9.7.0, and js-yaml 3.15.1/4.3.1 ([frvp](https://github.com/advisories/GHSA-frvp-7c67-39w9), [9mqv](https://github.com/advisories/GHSA-9mqv-5hh9-4cgg), [c96f](https://github.com/advisories/GHSA-c96f-x56v-gq3h), [5p4m](https://github.com/advisories/GHSA-5p4m-2wfm-xmqj)) — required action: no dependency or lockfile change; keep the three newer advisories explicitly unwaived as declared in `contract-lock.json:83-84`.

3. [MAJOR] The replacement gate test uses shipped `WAIVE`, and restoring any entry makes it fail, but its claim is overstated for the two Hono advisories — evidence: the new loop calls `run_cli()` without the local fixture (`secondhome-prepared.patch:270-285`); `main()` calls `evaluate(data)` with the shipped default at `scripts/ci/npm_audit_gate.py:134`. An in-memory post-patch simulation returned one finding for each empty-waiver case and zero after restoring its waiver. No other test breaks: fixture-dependent cases explicitly pass the local `WAIVE` from `test_npm_audit_gate.py:19-23`; the remaining shipped-default path at `:119-129` still expects rc=1. However, `vuln()` defaults every case to `high` (`test_npm_audit_gate.py:26-31`), while both Hono advisories are officially Moderate and the gate intentionally ignores Moderate (`npm_audit_gate.py:59,73-75`; `test_npm_audit_gate.py:92-96`). Thus a real reintroduction of those two would not “block” as the test name and `brief.yml:43-44` imply — required action: before BUILD, state that this is a synthetic high/critical reclassification test, or rewrite the assertions to distinguish actual Moderate behavior from absence of a standing waiver.

4. [MINOR] ARTICLES_ONLY control flow is correct, and FULL_ONLY output behavior remains unchanged — evidence: the patch adds `ARTICLES_ONLY` and canonical marking at `secondhome-prepared.patch:123-138`; EN is always written, then existing FULL_ONLY returns before ID at `generate-llms-full.ts:104-115`; ARTICLES_ONLY continues through ID, skips KBLI via patch `:146-147`, and executes freshness with canonical filtering at `:151-156`. Corpus census command found 2,559 published non-ID files and 799 canonical files, so freshness remains five items; it intentionally removes an `.it.mdx` duplicate while preserving canonical ordering. Census found zero uppercase locale tags and zero unknown lowercase two-letter suffixes. Known lowercase locales are noncanonical; dotted names, uppercase tags and suffixes outside `LOCALE_SET` remain canonical consistently with `publicSlug()` at `categories.ts:68-90`. FULL_ONLY still writes only EN; the added property is not serialized — required action: none.

5. [MINOR] The export regression test is statically hermetic and is wired into CI — evidence: it creates and removes a temporary cwd and places every generated file there (`secondhome-prepared.patch:313-328,355-386`); the generator derives input/output paths from `process.cwd()` (`generate-llms-full.ts:15-23`), so it cannot write the repository’s `public/`. Reverting the build line makes the test select `LLMS_GENERATE_FULL_ONLY`, leaving the seeded ID export stale and failing the ID assertion (`patch:343-370`); a read-only flag simulation selected ARTICLES_ONLY for the patch and FULL_ONLY for the reverted line. `createRequire()` resolved `tsx/cli` to root `node_modules/tsx/dist/cli.mjs` and `gray-matter` to root `node_modules/gray-matter/index.js`. `vitest.config.ts:11` includes `src/**/*.test.{ts,tsx}`, and CI runs `npx vitest --run` at `.github/workflows/tests.yml:1904-1914`. ESLint-on-stdin returned rc=0 and TypeScript transpilation reported zero syntax diagnostics — required action: execute its real green/red mutation pair after patch application.

6. [MINOR] The Vercel build change has no identified filesystem, data or committed-pin blocker — evidence: `vercel.json:4-5` runs `npm run build` after installing dev dependencies. The existing build already writes the 25.2 MB `public/llms-full.txt`; the patch adds writes of approximately 6.3 MB ID plus an 11.6 KB directory file, before `next build`. ARTICLES_ONLY does not parse KBLI data. All four public exports are tracked, but `git grep` for tests or hashes pinning `llms-full.txt`, `llms-id.txt` or `llms.txt` returned no matches; only `llms-kbli.txt` has the existing corpus pin. Local generated churn is already declared at `brief.yml:84-85` — required action: run the full build in the permitted Pro/CI lane and verify only uncommitted generated churn results.

7. [MINOR] Contract hashes and invariants are sound, but the recorded execution host is inconsistent — evidence: all 13 `contract-lock.json:60-73` source hashes returned MATCH; patch, FINAL spec, W1 spec, package-lock, package manifest and generator hashes also matched. No missing forbidden-path invariant or unobservable acceptance was found. However, `W1-studio-tech.md:5` names Mini while `brief.yml:8-10`, `contract-lock.json:10`, and `hostname` identify Air-M5 — required action: record the M5 host override and the Pro/CI location for heavy build verification before BUILD.

CONDITIONS FOR BUILD:

- Correct the Hono test/brief semantics: it proves “no waiver if high/critical,” not that the real Moderate advisories block.
- Record the actual Air-M5 execution host and route the full Next build to Pro/CI.
- After applying the exact frozen patch, run the 19 gate tests, export green/red mutation, targeted Vitest suites, TSC, lint and full build before CANDIDATO.

UNVERIFIED: Live `npm audit` could not be obtained: the registry request failed with `getaddrinfo ENOTFOUND registry.npmjs.org`, so the three declared newer highs and absence of the four retired IDs were not confirmed from fresh npm JSON. The read-only sandbox also rejected temporary-directory creation (`mktemp: Operation not permitted`; `tsx` could not create its IPC directory), so post-patch pytest/Vitest/full-build execution was impossible here. No PR preview, Vercel build or production/browser observation exists yet because the patch is not applied.