task_id: voa-6275b-prose
gear: 3
gate_class: opus
brief_ref: evidence/brief.yml
date: 2026-09-12
machine: air-m5 (M5)
lanes:
  - lane: voa-6275b-prose-build
    role: build
    seat: claude-opus-5
    note: >-
      Single build lane. The sentences being cured were written by this same
      chain one PR earlier, which is exactly why the refuter below is a
      different family and outside it.
  - lane: voa-6275b-prose-refute-codex
    role: review
    seat: >-
      codex gpt-5.6-sol (`codex exec -m gpt-5.6-sol --sandbox read-only`), OUT of the
      contribution chain. One round, ~12 minutes, rc 0: VERDICT BLOCK with 10 numbered
      findings — 8 PASS, 1 BLOCK, 1 narrowing. Cured at depth 1; see journal.jsonl and
      dissent below.
  - lane: voa-6275b-prose-refute-tp1
    role: review
    seat: >-
      tp1-qwen3.8-max (`scripts/tp1_call.py --model qwen3.8-max --effort high --max-tokens
      16000`, streaming), OUT of the contribution chain, second council seat. One delivered
      round 10:04:04Z-10:11:49Z: VERDICT BLOCK over 34 individually adjudicated claims. A
      first attempt died with RemoteDisconnected and produced no verdict; recorded, not
      hidden. kimi-code/k3 was attempted before it and returned 403 quota-exhausted — see
      journal.jsonl.
seat_diversity: exempt
seat_diversity_note: >-
  One build lane, so D3's seat-diversity requirement does not attach.
seat_override:
  kimi-code/k3: >-
    NOT INVOKED. The two-reader rule binds contract and data diffs; this PR
    modifies neither products/garuda-voa/contracts/openapi.yaml nor any data
    file. Recorded so the absence is a decision, not a silence.
council_run: journal.jsonl
outcome:
  summary: >-
    Two false sentences in the docstring of path_matches_template are replaced
    by what was measured. The claim "the contracts suite never imports the app"
    becomes "never imports FastAPI and never builds the live schema", with the
    one import it DOES perform named inline. The claim that the parity suite
    "runs on every PR inside the required backend shards" becomes a description
    of the measured pipeline: enumerated by shard_tests.py, gated by the
    change-map, fanning into the single required context.
  behaviour_change: none
diff:
  files: 1
  added: 36
  removed: 7
  note: >-
    Docstring text only. The docstring-stripped AST is identical to origin/main,
    so no executable line changed.
pii_scan: clean
pii_scan_note: >-
  Docstring prose and file paths only; no identifiers of any person.
receipts:
  - claim: >-
      The change is prose-only: no executable behaviour can differ.
    cmd: >-
      python: ast.parse both versions, delete every docstring node, compare
      ast.dump; plus a raw text comparison to prove the file did change
    result: >-
      docstring-stripped AST identical = True; raw text identical = False.
    ts: "2026-09-12T05:53:19Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      Condition 1 of gate-6275b: the shipped sentence is false and the
      replacement is true.
    cmd: >-
      grep -rn 'from backend\.|import backend' products/garuda-voa/contracts/tests/;
      grep -rn 'fastapi|main_api|app_factory' products/garuda-voa/contracts/tests/;
      grep -n 'pip install' .github/workflows/garuda-contract-parity.yml
    result: >-
      exactly one app import, test_contract_invariants.py:53, a function-local
      'from backend.services.garuda_flow.eligibility import DeclineCode' after a
      sys.path insert -- so "never imports the app" is FALSE. Zero hits for
      fastapi, main_api and app_factory, so "never imports FastAPI and never
      builds the live schema" is TRUE and the original conclusion survives. The
      workflow installs pytest and pyyaml only, at line 50.
    ts: "2026-09-12T05:53:19Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      Condition 2 of gate-6275b: where the parity suite runs, stated as measured.
    cmd: >-
      python3 scripts/ci/shard_tests.py enumerate (run from apps/backend-rag);
      the same tool with chunk --shards 3 --shard N; grep -n EXCLUDED_DIRS
      scripts/ci/shard_tests.py; grep -n for the backend-shard job, its
      change-map predicate and the fan-in job in .github/workflows/tests.yml;
      gh api repos/:owner/:repo/branches/main/protection for the required contexts
    result: >-
      enumerate emits 1607 paths and lists
      backend/tests/app/routers/test_garuda_voa_openapi_parity.py as entry 26;
      chunk --shards 3 --shard 2 contains it. EXCLUDED_DIRS at :82 is
      ("backend/tests/e2e",) and is the only exclusion. tests.yml:875 opens
      backend-shard, whose predicate at :907 is
      needs.changes.outputs.run_backend_tests != 'false' (fail-open);
      tests.yml:1197-1198 is the job backend-tests named "Backend Tests
      (Python)". The branch-protection API reports exactly ONE required context
      on main: "Backend Tests (Python)" -- so the shards are not themselves
      required, and the cured sentence says so.
    ts: "2026-09-12T05:53:19Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      Affected suites green and lint reproduced as CI runs it.
    cmd: >-
      PYTHONPATH=. pytest backend/tests/unit/middleware/
      backend/tests/app/routers/test_garuda_voa_openapi_parity.py; ruff check and
      ruff format --check on the changed file
    result: >-
      233 passed; ruff: All checks passed; 1 file already formatted.
    ts: "2026-09-12T05:53:19Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      The refuter's BLOCK is cured at depth 1, and the cure is re-measured rather
      than assumed.
    cmd: >-
      grep -n for the engine-enum parity test and for the workflow's pip install
      line; grep -n for the shard tool's filename filter; then the AST, ruff and
      pytest probes re-run on the cured file
    result: >-
      test_reason_codes_match_the_engine_enum_exactly confirmed at
      test_contract_invariants.py:380, so "checks the frozen document against
      itself" is false and the docstring now says it cannot compare the frozen
      OpenAPI document against the live generated one, naming the engine-enum
      parity it DOES perform. shard_tests.py:124 keeps only test_*.py /
      *_test.py and selects by target root, so "only exclusion" is narrowed.
      After the cure: docstring-stripped AST still identical to origin/main, raw
      text still different, ruff clean, 233 passed.
    ts: "2026-09-12T06:06:09Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      The second seat's BLOCK is cured at depth 1, and its unseeable claims are
      re-verified in-repo rather than treated as refuted.
    cmd: >-
      head -30 on the parity suite's header; sed on
      test_contract_invariants.py:51-53; grep -n for the shard filename filter;
      then AST, ruff and pytest re-run on the cured file
    result: >-
      The parity header does carry "Measured 2026-08-24/25" at :5;
      sys.path.insert sits at :52 immediately before the :53 import; the
      filename filter is at scripts/ci/shard_tests.py:124 — so the three MINORs
      and two MAJORs about "not shown in the evidence" were properties of the
      excerpt window given to the seat, not defects, and the sentences stand.
      The one finding that lands is cured: the docstring no longer ASSERTS that
      Backend Tests (Python) is required on main; it states that requiredness is
      branch-protection configuration no test pins, and reports the 2026-09-12
      API measurement as a dated observation. After the cure: docstring-stripped
      AST still identical to origin/main, ruff clean, 233 passed.
    ts: "2026-09-13T10:13:53Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
  - claim: >-
      Third-party gate condition on 0b5c34ab (PASS-WITH-CONDITIONS): the cured
      sentence claimed EXCLUSIVITY and was false. Cured, and the method error is
      recorded with it.
    cmd: >-
      gh api repos/Bali-Zero/Teman2/branches/main/protection/required_status_checks
      --jq '.contexts | length' and the unfiltered '.contexts[]' listing
    result: >-
      main carries 13 required contexts, not one; `Backend Tests (Python)` is
      among them (membership confirmed, exclusivity false). The false form came
      from the FIRST measurement being piped through `grep -i backend|parity`
      and the single surviving line being read as the whole set — a filtered
      view mistaken for a total, which is this PR's own defect class arriving by
      a third route. Both copies are cured: the docstring and the brief's second
      acceptance probe, which repeated the same sentence. After the cure:
      docstring-stripped AST still identical to origin/main, ruff clean, 233
      passed.
    ts: "2026-09-13T10:25:49Z"
    seat: claude-opus-5 (M5, build lane)
    exit: 0
dissent:
  - seat: codex-gpt-5.6-sol
    status: CONFIRMED
    objection: >-
      (Recorded from the fresh out-of-chain gate on head 0b5c34ab, not from this
      seat's own round, and kept here because the pack is where a condition that
      changed the diff belongs.) `public_endpoints.py:95-97` asserted that
      main's required contexts "were exactly [Backend Tests (Python)]".
    resolution: >-
      ACCEPTED and cured at depth 1: 13 contexts measured, membership asserted
      instead of exclusivity, in the docstring AND in the brief probe that
      repeated it. Three seats have now each caught one instance of the same
      failure in a PR whose deliverable IS prose accuracy — which is the honest
      summary of this lane.
  - seat: tp1-qwen3.8-max
    status: CONFIRMED
    objection: >-
      BLOCK: the docstring asserts that `Backend Tests (Python)` IS the required
      status check on `main`, and nothing in the material establishes branch
      protection — it is a repository-policy claim, not a code fact.
    resolution: >-
      ACCEPTED and cured at depth 1, and the reasoning is worth more than the
      edit: requiredness is MUTABLE configuration that no test in this repo
      pins, so a docstring asserting it would go silently false the day someone
      edits branch protection. That is the defect class this PR exists to cure,
      committed by the build lane while curing it — twice now, once caught by
      each seat. The sentence is now a dated measurement, explicitly not a
      property of the file.
  - seat: tp1-qwen3.8-max
    status: RETRACTED
    objection: >-
      BLOCK on the parity header's 2026-08-24/25 drift dates and on the #6275
      chronology, plus MINORs on three line numbers, all as "unsupported by the
      supplied material".
    resolution: >-
      RETRACTED against in-repo measurement: the parity file's header carries
      "Measured 2026-08-24/25" at :5, `sys.path.insert` is at :52 directly above
      the :53 import, and the filename filter is at shard_tests.py:124. The seat
      was given excerpts rather than the repo, so these are limits of its
      evidence window. Recorded in full rather than dropped, because the same
      window is what makes its surviving BLOCK credible.
  - seat: codex-gpt-5.6-sol
    status: CONFIRMED
    objection: >-
      Finding 6 (BLOCK): "so it checks the frozen document against itself" is
      false for the contracts suite as a whole —
      test_reason_codes_match_the_engine_enum_exactly compares reason-codes.yaml
      against the engine's DeclineCode enum.
    resolution: >-
      ACCEPTED and cured at depth 1. The sentence was INHERITED from #6275 and
      kept unexamined by the first draft of this PR — the same failure this PR
      exists to cure, committed while curing it. The docstring now states the
      narrow true claim and names the engine-enum parity the suite does perform.
  - seat: codex-gpt-5.6-sol
    status: CONFIRMED
    objection: >-
      Finding on "its only exclusion is EXCLUDED_DIRS": the shard enumerator also
      selects by target root and filters by test filename.
    resolution: >-
      ACCEPTED. Narrowed to "the only directory it excludes outright", with the
      filename filter named at scripts/ci/shard_tests.py:124.
  - seat: codex-gpt-5.6-sol
    status: RETRACTED
    objection: >-
      The refuter cites the workflow's pip install at garuda-contract-parity.yml:49.
    resolution: >-
      RETRACTED against the measurement: `grep -n "pip install"` returns :50 in this
      worktree, measured twice. The docstring keeps :50. Recorded rather than
      dropped, because this row is the only place the disagreement survives.
appetite_exceeded: ""
