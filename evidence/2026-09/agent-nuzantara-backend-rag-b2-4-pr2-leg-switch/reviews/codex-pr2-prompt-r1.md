ROUND 1 — adversarial review of B2.4 PR-2 (the Fly-side switch). The change is defective until the code proves otherwise. Read-only; modify nothing.

BUDGET DISCIPLINE (mandatory): no stadio-zero, no memory/ledger/scar searches, no repo-wide grep, no pytest or linters (the Dux ran them: 3079 passed in one process across rag/agentic, integrations, daemon, leg, broker router and every test importing wa_codex_leg / wa_package_builder; ruff clean). Read ONLY the files below, each once, then answer.

Contract: `evidence/2026-09/agent-nuzantara-backend-rag-b2-4-judge-reach-50b0c640/B2-4-design.md` sections 1.1, 1.2, 1.3, 1.5 (in worktree /Users/nuzantara/nuzantara/.worktrees/backend-rag-b2-4-judge-reach). Ruling I96: (1) the fail-closed gate stays — no text leaves without an authenticated SUPPORTED verdict; (5) an absent judge is loud. Ruling I97 C2: against an OLD daemon (raw model text, no envelope) nothing is ever released, proven by a test. Named residual V4: a package whose final user turn has no visible character is stubbed BEFORE the offer.

Candidate = branch head 1140646c15 in worktree /Users/nuzantara/nuzantara/.worktrees/backend-rag-b2-4-pr2-leg-switch, base origin/main b8162521b5 (PR-1 merged: daemon judge + `wa_completion_envelope.py`). Production diff (30 KB):
/private/tmp/claude-501/-Users-nuzantara-nuzantara--worktrees-backend-rag-b2-4-judge-reach/5f34a6fa-f36b-48a6-ad4d-6b384d49b03a/scratchpad/pr2-production.diff
Context only if a hunk is not enough: `apps/backend-rag/backend/services/integrations/wa_completion_envelope.py` (`decode_completion`), `apps/backend-rag/backend/services/integrations/wa_outbox_worker.py` lines 1010-1050 (how the leg result is consumed). Tests (names + assertions only): `apps/backend-rag/backend/tests/unit/services/integrations/test_wa_codex_leg_support_negative.py`.

Hunt for, in order: (1) ANY path where text reaches `finalize`/send without `decode_completion` returning SUPPORTED for THIS claim's package_hash — including REATTACHED, retries, consume_lost, exceptions, a missing/empty `settings.wa_broker_key`; (2) the envelope `answer` bypassing the existing restore_text / finalize / DLP / canary / price-source checks the raw text used to cross; (3) NOT_SUPPORTED/UNKNOWN stub carrying the plain support=None score instead of the `*_unsupported` pair, or `abstained_at` semantics changed; (4) builder: any remaining judge call, or `evidence_score`/`abstain` no longer equal to the support=None values; the unsupported pair computed from different inputs; (5) migration 315: CHECK widening wrong, not idempotent vs 297's pattern, rollback unsafe, or a Python mirror of the vocabulary missed; (6) V4 check too lenient (a builder-possible question-less package still offered) or too strict (a real question stubbed); (7) any log carrying query/answer text or the key; (8) tests passing for the wrong reason.

Output format: first line `VERDICT: SHIP` or `VERDICT: FIX-FIRST` or `VERDICT: BLOCK`. Then numbered findings with severity (BLOCKER/MAJOR/MINOR), file:line, concrete failure scenario, minimal fix; mark UNSURE where unsure. No summary, no praise. Under 900 words.
