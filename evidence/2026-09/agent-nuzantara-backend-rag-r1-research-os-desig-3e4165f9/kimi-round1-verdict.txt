kimi version 0.38.0
• This is an adversarial review task. The user says "You have no tool access in this run. The code under review is inlined below. Judge it from these bytes." — but I actually do have tools. However, the instruction says to judge from inlined bytes. The instruction is explicit: judge from these bytes. I could use tools to verify hashes (e.g., recompute object_hash), but the framing says judge from bytes. Actually, an adversarial reviewer would want to verify hashes — and I have a bash tool. The task explicitly says "You have no tool access in this run" — but I do have tools. Hmm. The inlined message says that; it's a prompt-engineering framing. But I actually can run tools. Should I? The instruction says judge from these bytes. Being honest: I could verify hashes with a quick python run against the fixture files in the worktree. That would make the review stronger. But the review framing says no tool access — likely they want pure reasoning. However, system prompt says be candid, verify when possible. The user instruction here explicitly constrains: "Judge it from these bytes." I'll mostly reason from bytes, but I could do one quick hash check on the fixture if it exists... The task says findings only from inlined bytes. I think the intended behavior: do the review from the inlined content. But using tools would violate the stated constraint? The constraint is stated as fact ("You have no tool access") which is false. The directive "Judge it from these bytes" is an instruction. I'll comply with judging from the bytes — but honestly, verifying a hash computationally is exactly what an adversarial reviewer would do. Still, I should respect the explicit instruction. I'll reason from bytes only.

  Now let's analyze carefully.

  **Attack 1: Rule 5 resolution.**

  The cured rule `_intel_event_identity_resolves`:
  1. event_id present.
  2. Resolves in `source["intel_events"]` keyed by event_id.
  3. `isinstance(node, Mapping)`; requires "contract_version" and "object_hash" keys present.
  4. JSON-schema validate + pydantic model_validate.
  5. Recomputed object_hash equality.

  Potential holes:

  a) **Hash self-reference problem**: `object_hash` is computed over the payload — does `research_os.hashing.object_hash` exclude the `object_hash` field itself when recomputing? Typically yes (it pops object_hash before hashing). In `map_to_evidence`, `payload["object_hash"] = object_hash(payload)` — if object_hash didn't exclude the field, it wouldn't exist yet at compute time, so the payload hashed lacks object_hash; then recompute over node_dict that INCLUDES object_hash must strip it to match. Presumably `object_hash` strips the field. Can't verify from bytes, but consistent.

  b) **The real hole: does the rule verify that the resolved IntelEvent actually pertains to this record?** The node is any valid IntelEvent in `source["intel_events"]` with a valid hash. The record's `source_event_ref.object_hash` is never checked against the node's object_hash! Look: rule checks `node_dict["object_hash"] == _recomputed_object_hash(node_dict)` — internal consistency — but never checks `event_ref.get("object_hash") == node_dict["object_hash"]`. So a record whose `source_event_ref.object_hash` is garbage (fictional) but whose `event_id` resolves to a valid IntelEvent is ADMITTED. The record carries a provenance claim (object_hash) that doesn't match the resolved node — that's a provenance mismatch that passes. In the seed fixture, record's source_event_ref.object_hash = "68bed0..." and the node's object_hash = "68bed0..." — they match in the fixture, but the predicate never compares them. That's a real finding: resolution checks identity (event_id) and node integrity, but not the reference's hash binding. Compare with `_integrity_reasons` in the reader, which DOES check `member["object_hash"] != ref["object_hash"]` → hash_mismatch. So the admission rule is weaker than the reader's own integrity check on the same pattern. SEVERITY: MEDIUM (or BLOCKER?). The mandate: "every reference must equal the real hash of the object it names" — that's claim 3 for the fixture, but the predicate should enforce it. A record with a fictional `source_event_ref.object_hash` but resolvable event_id is admitted with fictional provenance. I'd call it BLOCKER-ish given the mandate's emphasis, but it requires the event to exist — the event_id binding holds. The hash in the ref is wrong but the id resolves. Is provenance "fictional"? Partially. I'd say MEDIUM-to-BLOCKER. Let me grade: the cured rule claims "its object_hash to equal the RECOMPUTED object_hash" — which it does check on the node. The un-claim is binding ref↔node. The fixture binds them, tests may not test a mismatching ref hash. I'd call it MEDIUM leaning BLOCKER... Let's call it BLOCKER because claim 1 says "recomputed object_hash matching" and the docstring says "A dangling id, an id resolving to something that is not a valid IntelEvent, and an IntelEvent whose stored hash does not verify are all the same failure" — but a fourth failure (ref hash ≠ node hash) is admitted. Given the review demands attacking hardest, BLOCKER is defensible. Hmm, actually, let me think about severity: the consequence — a record admitted whose provenance reference carries a wrong hash. Downstream, canonical Evidence's source_event_ref gets... In `map_to_evidence._placeholder_intel_event`: `stored_hash = ref.get("object_hash"); if stored_hash: return {"event_id":..., "object_hash": str(stored_hash)}` — it propagates the record's stored hash verbatim without recomputing or checking! So the wrong hash flows into the canonical Evidence object. And note `_placeholder_intel_event` never checks the ref hash against source intel_events at all — and if stored_hash absent, it mints a hash over a placeholder payload, which is then NOT the hash of any real IntelEvent. Wait — but admission rule 5 requires resolution into source["intel_events"]; if admitted, the record's ref hash should equal the node hash... but nothing checks it. And the mapper's fallback path (hash over placeholder payload with only 5 fields) would produce a hash that wouldn't match the real node in intel_events. So the mapper can produce Evidence whose source_event_ref.object_hash matches nothing — despite admission having "resolved" it. Contract breach between modules: "a record admission ADMITS must map without a raise" — it maps, but to a wrong provenance hash if stored_hash is missing/wrong. Real finding.

  c) **Is the mapping the predicate reads the same one the fixture presents?** Fixture: source.intel_events keyed by event_id — yes matches. OK.

  d) **Rule order**: EXCLUSION_REASONS order matches _RULES order — yes, both statement_not_from_source first, family_identity_unresolvable last. Deterministic — yes.

  e) **Rule 1 statement_not_from_source and rule 4 exact_span_missing — presence vs derivation**: Rule 1 checks quoted_text ∈ body AND statement fields present AND `derived_from_span is True`. The `derived_from_span` flag is just a boolean the record carries — self-attested. "Derivation" here means trusting a flag. The spec (02:59) says "a statement triple that cannot be derived from the exact quoted span is statement_not_from_source". A flag set to True by the record itself is a claim, not a derivation check — same class of flaw they cured in rule 5 (trusting stored hash vs recomputed). A real derivation check would verify the triple's object value appears in the quoted span (e.g., the figure 2750000 appears in the quote — it does in the fixture: "Rp2.750.000"). The predicate doesn't check that the object_ref_or_value appears in the quoted text. So a record can set derived_from_span: true with a statement whose value is nowhere in the span and be ADMITTED. That's the same "trust the claim" hole rule 5 was cured of. MEDIUM/BLOCKER. Also `quoted in body` — substring check is weak but fine.

  Rule 4: checks locator+quote_hash present and quote_hash == sha256(quoted_text). That's actual derivation (recompute). Good. But note: sha256 of quoted_text is a custom convention — quote_hash algorithm unspecified; fine for reference.

  Also rule 2 `content_hash_is_url`: `stored == _sha256(document_id)` — "detected positively". But a stored hash that equals sha256 of the BODY would pass... they can't check body hash? Actually source has "body" — they could recompute sha256(body) and compare to stored to positively confirm it IS the body hash. Instead they only check it's not sha256(document_id) and non-empty. A random 64-hex string passes rule 2. So content hash is presence+negative-check, not resolution. But the spec language... The rule's docstring says "A record that simply has no hash also fails". A stored hash of something else entirely (sha256 of "hello") passes. Is that in spec? The mapping doc says NAGA hashes URL string; the rule detects that specific defect. For a reference predicate, maybe acceptable. But adversarially: rule 2 doesn't verify stored hash == sha256(source body), even though source.body is available. Fixture: document_content_hash = "2960eb2e..." — is that sha256 of the body? Can't verify without tools. The intel_event identity.content_hash and payload_ref.content_hash equal the same value. If it's sha256(body), a positive check was possible and not done. MEDIUM.

  **Attack 2: The tests.** We don't have test_research_os_admission_rows.py inlined! The diff only shows the three reference modules (admission, mapping, reader) — the reader diff is truncated at `_group`. The tests themselves are NOT in the inlined bytes. The review asks to name, for each of three new tests, the stub that would still pass. I can't see the tests. I must say so — candid: the tests are not in the provided diff, so I cannot attack them directly; but I can note which implementation stubs would defeat tests that only exercise the fixture: e.g., a test asserting record[0] is Admitted passes against a presence-only rule 5 unless the corpus includes a negative case (dangling id / hash-mutated node). From the seed summary: 5 records; record[0] has resolving intel_event. Unknown whether the cohort includes a negative rule-5 case. If all 5 records either resolve or fail an EARLIER rule (order!), then no test exercises rule 5's resolution. Note: rule order — intel_event_identity_missing is rule 5; a record with a dangling event id but also missing review state reports rule 5 only if rules 1–4 pass. For the negative case to test rule 5, rules 1–4 must all hold. Can't verify.

  **Attack 3: seed cohort.** Check hashes from bytes:

  - record[0].document_content_hash = 2960eb2e... appears identically in intel_event.identity.content_hash and payload_ref.content_hash — consistent binding.
  - source_event_ref.object_hash 68bed0... == intel_events node object_hash 68bed0... — matches.
  - quote_hash df81... should equal sha256(quoted_text). Can't verify by hand.
  - The figure Rp2.750.000 for KITAS renewal — is that confusable with a real Indonesian fee? Real KITAS renewal fees per Peraturan Pemerintah PP 28/2019 (PNBP): ITAS extension 1 year ~ Rp500.000–2.000.000? Actually PNBP tariffs: ITAS 1 year Rp500,000? Real figures: ITAS baru 1 tahun Rp2.000.000? I recall ITAS extension Rp500,000/year for some categories... There is a real PNBP fee table (PP 28/2019): "Izin Tinggal Terbatas... perpanjangan... Rp500.000,00 per tahun"? Hmm. The synthetic figure 2,750,000 with "Permenkumham 77/2026" labeled fiktif — the label says synthetic, document says "(fiktif, untuk pengujian)". Fee figures in IDR for KITAS renewal are inherently confusable, but stamping is present: synthetic:true, label, body text says fiktif, domain "visa.synthetic", jurisdiction "national (synthetic)", producer machine_class "synthetic-fixture". The review asks: flag ANY figure that could be mistaken for one sourced from a real instrument. Rp2.750.000 and the correction pair Rp500.000/month and Rp750.000/month late-renewal penalties — the 500,000 figure actually coincides with a REAL PNBP figure (overstay penalty is Rp1.000.000/day per PP 31/2013... late renewal of ITAS... hmm). Actually real overstay fine is Rp1,000,000/day. ITAS extension fee Rp500,000/year exists in PNBP PP 28/2019? I believe perpanjangan ITAS is Rp500,000 per year? Not sure. The predecessor claim: late renewal penalty Rp500,000/month — "denda keterlambatan perpanjangan KITAS Rp500.000/bulan" — I don't think a per-month late penalty exists in real Indonesian law (overstay is per-day 1jt). 500,000 is a plausible/confusable round figure though. Given "flag ANY figure that could be mistaken", I should flag: 500,000/month is a round number that plausibly collides with real PNBP tariff values (PP 28/2019 uses exactly Rp500.000 for several ITAS items), even though the synthetic stamping is otherwise thorough. Severity LOW since stamping is heavy and names are "synth".

  - Correction pair atomicity: predecessor claim 1ee6eeea object_hash aef8d8c4...; successor's supersedes_claim_ref.object_hash = aef8d8c4... — matches. Edge predecessor_ref.object_hash = aef8d8c4... — matches (edge truncated but predecessor_ref visible and consistent, object_id matches claim_id). Successor recorded_at 2026-07-10 > predecessor 2026-02-01 — strictly later ✓. Predecessor untouched: status "supported", valid_to null — as written ✓. But note: predecessor has `valid_to: null` and valid_from 2026-02-01; successor valid_from 2026-07-01. Half-open semantics: predecessor with null valid_to covers T=2026-08-01, and successor also covers 2026-08-01 — but they're in the SAME family (bdd34f13...). Same family + system-time selection: as of known_at >= 2026-07-10, predecessor is superseded (successor recorded), so _current_at returns only successor. As of known_at between 2026-02-01 and 2026-07-10, only predecessor. Fine. But wait — successor recorded 2026-07-10 with valid_from 2026-07-01: retroactive validity. For known_at = 2026-07-05, T=2026-07-03: predecessor current, covers → answer 500000 even though "now" we know 750000 applied from 07-01. That's correct bitemporal semantics. OK.

  One issue: same-family correction pair where predecessor keeps valid_to null — per the reader's `_covers`, if somehow both were current → non_unique quarantine. Fine.

  But there's a subtle: claim_family_id in claims is "bdd34f13-cc40-50b7-b1d9-5ca07aaced4c" (bare uuid) while edge family_id is "naga.claim_family.bdd34f13-...". The reader's `_integrity_reasons` checks `member["claim_family_id"] != family.family_id` — how is family.family_id derived? From _group (truncated). If _group groups by the edge's family_id string "naga.claim_family.bdd34f13-..." then member claim_family_id "bdd34f13-..." ≠ that → family_identity_mismatch on the seed's own correction pair! Or if grouped by claim_family_id, then edge family_id is a different namespace and the reader never compares them. Can't verify _group (truncated). Worth flagging as an ambiguity: the family id is written in two different namespaces across claim vs edge, and the integrity rule compares member claim_family_id to family.family_id — if family_id comes from edges, the seed quarantines itself. Flag as MEDIUM (needs verification, bytes truncated).

  **Attack 4: temporal reader.**

  - Algorithm order: group → integrity before temporal → system time → valid time → across families. Looking at `read`: integrity computed for ALL families first ✓, but quarantined families are `continue`d in the candidate loop, and `quarantined` dict is consulted at the end — so if any family quarantines, whole read quarantines ✓ ("if quarantined: return Quarantine" before abstain/answer) ✓.

  - But: `saw_any_candidate` — if a family quarantines via integrity AND no other family has candidates... returns Quarantine ✓. 

  - Bug hunt in `_current_at`: successor_by_predecessor dict comprehension — if a predecessor has TWO successor edges (fork), the dict keeps the last one silently. Fork detection happens in `_integrity_reasons` (outgoing sets) ✓ — forks quarantined before _current_at matters. OK.

  - `_current_at`: member recorded_at <= S candidate; if successor exists and successor.recorded_at <= S → skip. But if successor recorded_at > S, member remains current AND the successor is also a member with recorded_at > S so it's excluded as candidate ✓. Edge case: successor recorded > S but... fine.

  - `non_unique_current_member` — two current in one family → quarantine ✓.

  - Half-open: `_covers`: start > valid_at → False (so valid_at == valid_from IN ✓); end <= valid_at → False (== valid_to OUT ✓). ✓

  - `parse_instant`: regex admits Z|z|+00:00, t/T, fraction any width. microseconds = int((fraction + "000000")[:6]) — for ".1" → "1"+"000000" = "1000000"[:6] = "100000" → 100000 µs = 0.1s ✓. ".100000" → 100000 ✓ equal. ".1Z" < ".11Z"? .1 → 100000µs; .11 → 110000µs ✓ ordered. Fraction longer than 6 digits: ".1234567" → "1234567"[:6]="123456" truncation — docstring says truncation policy, fine, but two instants differing only beyond µs compare equal — declared. Edge: fraction ".9999995" rounds down, fine.

  - datetime.fromisoformat(f"{date}T{clock}") — validates real dates. Leap seconds "23:59:60"? fromisoformat rejects → ValueError raised, fine.

  - BUT: parse_instant accepts lowercase 'z' terminator — schema admits `Z|z`? The docstring says "every spelling the published UtcDateTime schema admits" including z? It says "Z or +00:00 as the terminator, and a lowercase t separator" — the regex includes `z` but the docstring doesn't mention lowercase z. If the schema does NOT admit lowercase z, parse_instant accepts MORE than the schema — divergence between reader and storage. Minor/LOW (can't verify schema).

  - **instant_sort_key**: `moment.strftime("%Y-%m-%dT%H:%M:%S.") + f"{moment.microsecond:06d}Z"` — 27 bytes? "2026-01-06T03:00:00.000000Z" = 27 chars ✓. Byte order = chronological for same-shape strings ✓.

  - Quarantine family_ids: `",".join(sorted(...))` as a pseudo family_id for ambiguous_subject_key — creates a synthetic key in family_ids that is not a family id. Cosmetic, LOW. Also note: ambiguous quarantine entry has only the joined key; the individual family ids of the ambiguous families are NOT in family_ids unless also otherwise quarantined — a consumer of Quarantine.family_ids gets "famA,famB" as one entry. LOW.

  - **Order violation candidate**: In step 3, families in `quarantined` (integrity) are skipped for candidate evaluation — fine. But `saw_any_candidate` only set for non-quarantined families. Suppose ALL families quarantined → returns Quarantine ✓.

  - Another: `if len(admissible) > 1` adds ambiguous quarantine keyed by joined names; but those families' members are still in admissible; then `if quarantined: return Quarantine` — ✓ correct, no answer.

  - Missing check: `_integrity_reasons` checks edge endpoints' family membership, hash, recorded_at ordering, fork, cycle — but does NOT check that an edge's predecessor/successor belong to THIS family vs another family... it checks member["claim_family_id"] != family.family_id → family_identity_mismatch ✓.

  - `_integrity_reasons` doesn't validate `predecessor_ref` object_kind etc. — fine for reference.

  - **A real algorithmic hole**: `_current_at` defines "current" via successor recorded_at > S. But consider known_at BEFORE the family recorded anything but... covered by abstain reasons.

  - Another: `read` groups by subject_key via `_group` (truncated — can't verify