• Review complete. I re-derived everything from the on-disk artifacts rather than trusting the briefing: read `fold_pack_seq23.py` end to end, diffed seq-22 vs seq-23 rule-by-rule in Python, re-ran the twin-basis antibody, re-computed the payload digest, read the census diff, checked evaluator semantics, and re-ran the full suite.

  **Verification results (all independently reproduced this round):**
  - Payload digest of committed `rulepack-prod-023.source.json`: `e5f791b5…272204` — matches the claim.
  - Removed set = exactly the 8 `review.*` ids; added = exactly the 11 `hf.*` ids; zero drift in any other rule (canonical JSON compare); zero drift in non-identity top-level keys; Studio rule byte-identical; the 4 modified rules differ in `on_unknown` and nothing else.
  - Antibody re-run on seq-23 output: 7 rules read twin-basis facts, **none** at HARD_FILTER/EXCLUDE stage, **no** `eq false` on them. Clean.
  - 345/345 green across the six named test files (60s, just now, post-rebase worktree).

  **Answers to your five questions:**

  **1. Verbatim `when` copy into HARD_FILTER/EXCLUDE — sound.** The two asymmetries a HUMAN_REVIEW rule can silently rely on are (a) over-inclusiveness a human clears, and (b) late-stage evaluation (facts that only exist after earlier stages). I checked all 8 donors: every `when` is a precise dead-end pattern (calling-visa 6-country list, overstay>0, minor+sponsor=false, violation-history, purpose conflicts, local-market/local-company flags), none is a "suspicion" matcher, and all read raw interview facts available at HARD_FILTER time (`derived.is_minor` derives from age, not eligibility). No `not`/`neq` three-valued traps in the copied set (the only `not` is `hf.b1.not-voa-nationality`, pre-existing, flip-only). Under the evaluator's `_partition_unknowns_by_policy`, NEEDS_INPUT turns an unknown into a question rather than a hold — strictly better than the dead `HUMAN_REVIEW` option (evaluator P1 item 4). Sound.

  **2. Branch-2 subsumption — sound, verified mechanically.** Branch 2's first conjunct is *literally identical* to `hf.calling-visa-nationality`'s entire `when` (set equality confirmed: both `{AF,IL,KP,LR,NG,SO}`). Branch 2's firing set is therefore a strict subset of the GLOBAL rule's — it cannot fire anywhere the GLOBAL rule doesn't already fire harder. Additionally verified: branch 1's two lists are disjoint (97 vs 152 codes, zero overlap), so the extracted "dual nationality" rule can never fire on a single-nationality applicant. The scope narrowing GLOBAL→B1-PRODUCTS for branch 1 is a spec decision (disposition table), faithfully implemented by reading B1's product-version id from `hf.b1.not-voa-nationality` rather than retyping it.

  **3. NO_EFFECT design — sound, no race.** `lt 3000` vs donor's `gte 3000` are disjoint on the same fact, so an EXCLUDE can never kill someone the SUPPORT rule would support; unknown income → NO_EFFECT → donor's NEEDS_INPUT owns the question (`el.e30-student-support`, `el.e33e/f.retirement` all carry NEEDS_INPUT). STUDY's `any(eq false, eq false)` fires on one declared-false even if the other conjunct is unknown — correct, one false is already a dead end. Bounds are read from the donors and asserted equal at fold time (guilt test: moving one bound to 4000 aborts the fold). One cosmetic note, not a change request: a RETIREMENT-purpose applicant blocked on *age* with income <3000 will see `RETIREMENT_INCOME_BELOW_THRESHOLD` as the named reason — true but possibly not their first blocker; acceptable since the product is unreachable regardless.

  **4. Bridging adverse-history — intended reading; recommend a call-out, not a guard.** Key finding: **the fold does not narrow scope** — the donor `review.bridging.adverse-history` was *already* `PRODUCTS [760025cf…]` (bridging only); the fold copies scope verbatim (asserted in tests). The walk-level hold in seq-22 came from the frozen precedence table (evaluator docstring P0-B: REVIEW outranks SUPPORTED unconditionally, even for a PRODUCTS-scoped trigger), not from the rule's scope. So the 8 walks flipping to SUPPORTED_CANDIDATES is the slice's *raison d'être* — product-scoped dead ends replacing walk-level holds — not a scope edit. The one fact worth surfacing: `immigration.violation_history` is read by **no other rule** in the pack (verified), so post-fold a DEPORTATION/BLACKLIST-history applicant with non-bridging options gets candidates with no human ever seeing the history — in seq-22 the precedence table incidentally guaranteed that human look. The pack never *claimed* to gate other products on it, so this is a spec-level policy acceptance (A8-1.v2), but I'd put it explicitly in the ship notes/release bite so the sign-off is on the record. No code change.

  **5. Census fix — correct reasoning; coverage loss is real but unexercisable by this PR.** The delta form `candidate − signed == nine` became structurally impossible the moment seq-22 (which contains seq-21's nine) was signed — your analysis is right. The rewrite loses the equality's "no tenth product" arm, but: (a) this fold inserts only EXCLUDE rules, which cannot create reachability, and `assert_only_expected_changes` pins the entire ruleset; (b) `EXPECTED_OUTCOME_BY_SEQUENCE` pins each walk's exact candidate tuple, so a future fold that adds reachability moves a pinned outcome. Dropping the signed-pack line in the guilt test is also correct — with seq-22 signed, that assertion would now fail on the *correct* mutation (same products, same qualifying facts), i.e. red for a reason unrelated to the guard. Optional tightening for a later fold: for a candidate that adds no products, `candidate_engine_named == engine_named` would restore the exact-set guard in delta form. Not required here.

  ---

  # VERDICT: OK

  The fold is faithful to the disposition table on every row, the anchor verification is real (verified signed seq-22, digest-chained), the antibody holds on the new output, the three derived rules are race-free, and all measured claims reproduced under my own hands (digest, diff-shape, 345/345). Two advisories for the ship notes, neither blocking: (1) call out the Q4 walk-level behavior change (adverse history no longer forces a human look when non-bridging candidates exist) so the acceptance is explicit; (2) applicant-facing copy for the 11 new reason codes / `explain.*` keys is downstream mouth-layer work, same as every prior fold — confirm it's tracked before activation.
