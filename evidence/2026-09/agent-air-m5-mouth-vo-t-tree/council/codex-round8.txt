OpenAI Codex v0.154.0
--------
workdir: /Users/balizero/nuzantara/.worktrees/mouth-vo-t-tree
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 01a09b62-f1b8-7e11-8518-18faefb53f30
--------
user
You are an adversarial reviewer on a Gear-3 council. Review ONE diff for
correctness. You are NOT the author and you do not fix anything: you find
defects or you state there are none.

## What the change is

Repo: Nuzantara monorepo, app `apps/mouth` (Next.js 16, React, TypeScript,
vitest + playwright). Surface: the Visa Oracle public funnel at
`/visa-oracle` — a deterministic immigration interview whose verdict comes
from ONE backend endpoint (`POST /api/visa-oracle/evaluate`), called once,
after the applicant confirms their answers.

The window's objective: a visitor must SEE where they are in the decision
process — which stage is open, what the current question decides, which of
the eleven purpose branches their own answer closed and why — and be able to
jump back to any answered question. The outcome must read as the last node
of the same tree.

## The rules the diff must obey (these are the review criteria)

1. The interface must NEVER decide eligibility. No product may be named,
   ranked or filtered client-side. A product code may appear ONLY when it
   came from an engine response already on screen.
2. No branching change. `getTreeSteps`, `computeNextNode`,
   `getCategoryQuestionIds`, `fact-mapper.ts` and `tree.ts` semantics must be
   untouched; the new `getProcessModel` may only PROJECT what already exists.
3. `oracle.css` is READ-ONLY by ruling. New styling must be inline and must
   use the existing `--oracle-*` / `--space-*` / `--text-*` custom
   properties. No new colour values.
4. Accessibility: keyboard reachable, reduced motion honoured, axe-clean at
   WCAG 2.0/2.1 A+AA, EN and ID copy both present and correct, no duplicate
   DOM ids (the rail renders TWICE — once in the mobile sheet, once in the
   desktop column).
5. No client PII anywhere. Synthetic values only.
6. React correctness: keys, hook rules, no state derived wrongly, no
   render-time side effects, no O(n^2) blowups on every keystroke.

## What to look for, concretely

- A place where the rail states something it cannot know (a product, an
  eligibility, a count that does not match what the reducer holds).
- `getProcessModel` disagreeing with `getTreeSteps` in any reachable state —
  including: category answered with the reserved value "unsure" (not a real
  category key), the onshore `permit_expiry` lane that jumps straight to
  `review_gate` skipping `category`, a follow-up question appended after the
  verdict, and the framing node.
- The duplicated rail (mobile + desktop) producing duplicated ids, duplicated
  live-region announcements, or two elements that a strict-mode Playwright
  locator would match.
- Copy that promises something the code does not do, in EN or in ID.
- A test that asserts its own fixture instead of the behaviour.

## Output format — MANDATORY

Write your findings as a short list. Each finding: FILE:LINE, what is wrong,
why it matters, and the minimal fix. Speculation must be labelled
"unverified". Then, as the LAST LINE of your answer, exactly one of:

VERDICT: OK
VERDICT: DEFECT

Use DEFECT only for a real, demonstrable defect under the rules above — not
for style, not for "could be nicer".

## Round 8 — the two findings you returned in round 7 are cured

`origin/main` is an ANCESTOR of HEAD, so `git diff origin/main -- apps/mouth`
is exactly this window's own work. Judge only that.

Round 7 (yours) returned two findings. Both are cured on disk, at depth 1:

1. `flow.ts` — the answered follow-up was appended AFTER `verdict`, so the
   breadcrumb ended on an answered question while the visitor was at the
   outcome. The extras are now spliced in BEFORE the terminal `verdict` step
   (`verdictIdx`), so the outcome is the last node of the trunk again. Pinned
   by "puts the answered follow-up BEFORE the verdict, not after it".
2. `process.decides_confirmation` — "Nothing has been sent yet" was false for
   anyone who came back through `REVIEW_ANSWERS`. EN now reads "No question is
   open. Confirming these answers is what asks the engine — and asks it again
   if you have already been here."; ID matches. Pinned by "does not deny a
   submission that already happened".

Earlier rounds settled: the off-spine question is no longer read as an engine
follow-up without a verdict in history (`visitedVerdict`); the
`NO_SUPPORTED_PATH` "three alternatives" string is not this window's and is
gone from main; the two-dot diff against a 28-commit-ahead main was a harness
artifact.

Find a defect that is still on disk, in this window's own diff, or say there
is none. Same output format, same last line.

warning: Skill descriptions were shortened to fit the skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
codex
[Air-M5] Verifico il diff rispetto a `origin/main` e i casi limite indicati, senza modificare file. Applico la procedura `modus` per il ruolo di revisore indipendente.

[… 2589 lines of read-only tool output elided by the author; full run kept out of the repo …]
codex
Mandato `W-VO-T`, BLUE, Gear 3: opero come revisore nel worktree indicato. Le due correzioni del round 7 sono presenti; sto verificando soprattutto il conteggio delle risposte e i ritorni dal verdetto.

[… 2878 lines of read-only tool output elided by the author; full run kept out of the repo …]
codex
Sto riproducendo gli stati con il reducer reale. Il controllo riguarda anche le fasi che contengono domande poste in momenti diversi: il riepilogo deve descrivere correttamente le risposte già conservate.

[… 256 lines of read-only tool output elided by the author; full run kept out of the repo …]
codex
- **`apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/flow.ts:1822`** — Nel percorso offshore, scegliendo `invest`, la fase `location` diventa `pending`: mostra **“not started” / “belum dimulai”** nonostante contenga tre risposte completate. Riprodotto con il reducer: `answered: 3, total: 4`. La domanda successiva di conversione rende la fase incompleta, non mai iniziata. **Fix minimo:** distinguere le fasi parzialmente completate oppure usare una traduzione di `pending` corretta anche per esse.

- **`apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/flow.ts:1884`** — Dopo `EDIT category`, il reducer conserva sei risposte, inclusa quella della categoria, ma il contatore mostra **cinque risposte** perché esclude quella del nodo `current`. Riprodotto con il reducer reale. Il conteggio contraddice i dati conservati. **Fix minimo:** contare le risposte presenti nei fatti delle domande proiettate, separatamente dallo stato navigazionale del nodo.

VERDICT: DEFECT
tokens used
102.164
- **`apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/flow.ts:1822`** — Nel percorso offshore, scegliendo `invest`, la fase `location` diventa `pending`: mostra **“not started” / “belum dimulai”** nonostante contenga tre risposte completate. Riprodotto con il reducer: `answered: 3, total: 4`. La domanda successiva di conversione rende la fase incompleta, non mai iniziata. **Fix minimo:** distinguere le fasi parzialmente completate oppure usare una traduzione di `pending` corretta anche per esse.

- **`apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/flow.ts:1884`** — Dopo `EDIT category`, il reducer conserva sei risposte, inclusa quella della categoria, ma il contatore mostra **cinque risposte** perché esclude quella del nodo `current`. Riprodotto con il reducer reale. Il conteggio contraddice i dati conservati. **Fix minimo:** contare le risposte presenti nei fatti delle domande proiettate, separatamente dallo stato navigazionale del nodo.

VERDICT: DEFECT
CODEX_EXIT=0
