{"seat": "codex-gpt-5.6-sol", "role": "review", "ok": true, "ts": "2026-09-21T04:40:00Z", "note": "One round on the finished diff (commit c7bd319212), `codex exec --sandbox read-only`, told to attack. Seven findings. ACCEPTED and fixed: (1) HIGH — the new test did not check `TMP=$(mktemp -d)`; with an empty TMP, run_case's cleanup is `rm -rf /run /home /logs`. It demonstrated this in its own sandbox, where mktemp was denied and the cleanup hit /home and /run (blocked only by the sandbox). Fixed: mktemp failure and an empty/non-directory TMP now abort before any path is built. (2) MEDIUM — the spec amendment left S4.1-S4.3 and acceptance rows A3/A4 mandating the hyphen->underscore mapping the change deletes, and its 'only other producer' omitted scripts/wr2-cron-wrapper.sh:84. Fixed: S4.1-S4.3 rewritten to the identity rule, A2/A3/A4 rewritten, A4b added for witness-less producers, both wr2 producers named. (3) MEDIUM — the stub gateway recorded any argv, so a wrapper passing `--dedup-keey` passed the corpus while the real parser rejects it and the wrapper's `|| true` hides the rejection; it proved this by running the real tg_notify parser on the mutant argv. Fixed: the GUILT and INNOCENCE cases now run the REAL tg_notify.py (stdlib-only; dry-run; tmp HOME without credentials; board redirected into TMP) and read the key back from the board row it writes. The same mutant now turns 7 checks red. ACCEPTED as pre-existing and recorded, not fixed: (4) LOW — `tr '-' '_'` and sanitize_key diverge for uppercase, dots, spaces, doubled/leading hyphens and `-n` (echo); no live caller produces those, and the relation now holds for any input because key and file come from one string. (5) LOW — the wrapper's fixed /tmp/cron-locks and pid-liveness check make test isolation probabilistic under SIGKILL + pid reuse; inherited from the wrapper. CONFIRMED sound: CI registration in both places; no new bash syntax or unbound-variable issue. It also verified that no executable consumer depends on the old hyphenated keys, and that the gateway's dedup state is keyed by exact key, so each job's ladder restarts once."}
{"seat": "kimi-code/k3", "role": "review", "ok": true, "ts": "2026-09-21T04:42:00Z", "note": "One round on the same commit, `kimi -p`, told to attack. It ran the corpus (13/13), ran it against origin/main's wrapper (exactly the 4 relation checks red, 9 innocence/fail-open green), and constructed four broken wrappers of its own — gateway never invoked (7 red), witness never written (5 red), witness renamed by a different transform (red, caught by the innocence section's literal anchor — it flagged that literal as load-bearing), `job` field raw while filename underscored (1 red). No broken wrapper passed. It measured the tr-vs-sanitize_key divergence on six inputs and found every documented live caller in the safe subset. It verified CI registration line by line in both places and bash 3.2 portability. Its DEFECTS were all in the spec amendment, and they overlap codex's (2): S4.1 still mandated a per-wrapper table, A2/A3/A4 still tested the deleted mapping, and the §4 table and 'live consequence' paragraph were left in present tense with stale line refs. All fixed: S4.1-S4.3 rewritten, acceptance rows rewritten, the §4 table marked as the pre-fix measurement. Its finding (4) — a new key could leave a duplicate row on the live board for a job already pending under the old key — was checked on Pro: the board holds 0 `cron-fail:` rows of any spelling, so the cost is zero today; recorded in the pack."}
