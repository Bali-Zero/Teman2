# Ledger rows held — W3A / PR #6287 (migration 313)

Four PENDING-ARMS rows that belong in `.claude/skills/modus/PENDING-ARMS.md` and are
written here instead, for the reason commit `fb39ef3f84` states on the record: that
ledger carries a `merge=union` driver which git honours and GitHub's merge machinery
does not, so every open PR touching it disagrees permanently. This lane is not adding
a writer to a deadlock over two rows of its own reporting.

The first two rows come out of Sol's round-O4 review of this PR, the third from round O5, the fourth from gate-6287d's mutation (c). Neither is a defect this PR
introduces; both are things it is the first to SEE, and the handoff is named in each.

**Handoff:** the visa_engine lane owns both — `operational_preflight.py`, its two
inventories, and `tests/services/visa_engine/conftest.py`'s unwind/restore pair.
Whoever next opens a PR that touches `.claude/skills/modus/PENDING-ARMS.md` while the
union-merge deadlock is resolved should move these rows into it verbatim.

---

- opened 2026-09-12 (M5, GARUDA VOA W3A — surfaced by Sol round O4 on PR #6287, CONFIRMED by this session by reading the pair; PRE-EXISTING defect, not introduced by #6287) | **`restore_garuda_voa_retention_fk` cannot tell "absent because unwind removed it" from "absent because it was never there", so on a database where an entry was NOT live going in, the unwind/restore pair hands it BACK — and the docstring claims the opposite property.** `apps/backend-rag/backend/tests/services/visa_engine/conftest.py`: `unwind_garuda_voa_retention_fk` returns one aggregate `bool` for the whole registry, and `restore_garuda_voa_retention_fk` then applies the forward SQL of every registered entry whose marker column is currently MISSING. The 2026-08-25 correction (PR #4902 follow-up) replaced an unconditional restore with that absence probe and its docstring states the result is "exactly what unwind actually took away, no more, no less" — which an absence probe cannot deliver, because absence has two causes and the probe sees one. Concretely, on a DB with 264 + 281/285/286/304 applied but 313 not: unwind skips 313 (marker absent), restore finds 313 absent and APPLIES it; if a parent migration is also missing the same path fails on `garuda_practices` instead. The existing regression test `test_restore_only_reapplies_the_entry_that_was_actually_unwound` proves the OTHER direction only (do not re-apply an entry still present) and passes throughout. Declined for #6287 deliberately: it is another product's fixture machinery, outside a migration PR's concern, and would be a fix-of-a-fix on a mechanism already corrected once (contract §1, depth 1). | next step: change `unwind`'s return contract to name WHICH entries it unwound (a set/list, not a bool) and have `restore` re-apply exactly that set — then add the missing-direction regression test: start with 313's marker absent, run unwind → forward_264 → restore, assert `garuda_practice_artifacts` is STILL absent. | owner: the visa_engine lane (fixture machinery owner), NOT W3A — W3A surfaced it and is not qualified to change another product's teardown contract mid-migration-PR | proof-of-armed: none yet — this row is a report, nothing is armed. The measurement that stands today: on the disposable PG15 shape used by #6287, `backend.db.migrate apply-all` applies EVERY migration, so the skewed starting state never arises and 307 tests pass — which is precisely why no test catches this.
- opened 2026-09-12 (M5, GARUDA VOA W3A — surfaced by Sol round O4 on PR #6287; the 313 half was CURED in that PR, this row is the REMAINDER) | **Three SECURITY DEFINER retention binders already on main are absent from both preflight inventories, and there is NO parity canary between those lists and the migrations that create such binders — the same shape that cost migration 285 weeks of 500s.** `apps/backend-rag/backend/scripts/visa_engine/operational_preflight.py::RETENTION_BINDING_TRIGGER_FUNCTIONS` and its independent twin `CANONICAL_SENSITIVE_FUNCTIONS` in `backend/tests/scripts/visa_engine/test_operational_preflight.py` list five signatures. Missing, all merged, all the same class: `bind_legacy_garuda_voa_checks_retention_policy(...)` (281), `bind_garuda_voa_check_result_retention_policy()` (286), `bind_garuda_document_retention_policy()` (304). #6287 registered its own — `bind_garuda_practice_artifact_retention_policy()` (313) — because the comment above those entries says "leaving it out is how this blind spot reopens" and 313's binder takes the same `FOR SHARE` lock the 285 incident turned into 500s. It did NOT register the other three: they belong to merged lanes and widening a migration PR into three other products' privilege inventories is not its concern. The deeper gap is the absence of a canary: `test_visa_engine_retention_fk_registry.py` exists for the FK registry and names the next author's omission at authoring time; nothing equivalent scans `migrations_v2/*.sql` for `SECURITY DEFINER` binder functions and compares them to these lists. The parity assertion that DOES exist only fires when the two inventories disagree — an author who updates neither is invisible to it. | next step: write that canary (scan the migration tree for CREATE [OR REPLACE] FUNCTION bodies carrying SECURITY DEFINER whose name matches the binder convention, assert each appears in both inventories), then register 281/286/304 in the same PR so it lands green | owner: the visa_engine lane (owner of operational_preflight and its inventories) | proof-of-armed: none yet. Measured today by this session: `grep` over `migrations_v2/*.sql` finds 4 binder signatures the lists do not carry (3 remaining after #6287); guilt mutation on #6287's own entry turns `test_preflight_inventory_matches_independently_frozen_schema_authority` RED, confirming the parity assertion works — and confirming it is blind to a double omission.
- opened 2026-09-12 (M5, GARUDA VOA W3A — surfaced by Sol round O5 on PR #6287; PRE-EXISTING property of the shared mechanism, third fact about the same pair) | **`unwind_garuda_voa_retention_fk` + `restore_garuda_voa_retention_fk` are not state-preserving for OTHER fixtures' committed rows in `visa_decision_retention_policies`: 281's rollback `DROP COLUMN IF EXISTS policy_scope`, and restore re-adds the column with its default, so every closed GARUDA_ORDER / GARUDA_MAGIC_LINK / GARUDA_DOCUMENT row another file deliberately left behind comes back as VISA_DECISION.** `apps/backend-rag/backend/db/migrations_v2/281_garuda_voa_retention.sql` rollback half, and `tests/services/visa_engine/conftest.py` unwind/restore. The three visa_engine consumers (test_shadow_match, test_evaluate_endpoint, test_shadow_evidence) and #6287's reproduction test all do this on every run; none introduced it. | next step: decide whether the pair should snapshot and re-stamp `policy_scope` per row across the 281 boundary, or whether the harness DB should be per-file — the Imperatore has named the second as the spec to write if a new cause appears | owner: the visa_engine lane | proof-of-armed: none — report only.
- opened 2026-09-12 (M5, GARUDA VOA W3A — measured by gate-6287d as mutation (c) on PR #6287, head 73b066812a; a COVERAGE GAP, not a defect) | **Nothing in #6287's tests would notice if 313's rollback acquired 285's shape — an unconditional re-narrowing of the `policy_scope` CHECK that raises CheckViolationError whenever a GARUDA_DOCUMENT row exists.** The gate re-introduced exactly that narrowing into `313_garuda_practice_artifacts.sql`'s rollback half and every test stayed GREEN: with the fixture's seed rolled back there is no committed row for the ALTER to refuse, so the mutation is invisible to the file that would have caught it a day earlier. The migration suite proves the rollback runs; it does not prove the rollback is shape-safe against rows other files leave behind. | next step: a parity canary — assert that no `migrations_v2/*.sql` rollback half rebuilds `visa_decision_retention_policies_policy_scope_check` without first guarding on `EXISTS (... WHERE policy_scope = <its own scope>)`, the guard 304 has and 285 has only for its own scope; or, cheaper, a structural test in the 313 suite that reads 313's rollback text and refuses any `ADD CONSTRAINT visa_decision_retention_policies_policy_scope_check`. To be done in S2 (Tigris store PR) from fresh main. | owner: W3A (this lane) — the canary belongs with the next PR that touches 313's neighbourhood | proof-of-armed: none — measured gap, gate-6287d report 12:46Z.
