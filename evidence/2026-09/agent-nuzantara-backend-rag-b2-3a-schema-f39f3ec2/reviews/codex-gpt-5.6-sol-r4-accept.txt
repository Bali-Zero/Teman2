codex-gpt-5.6-sol r4 on SHIPPED bytes 314 sha256 groups 6b5d3d8b b99d8d37 d8ccbd97 414264df 6fb888a4 731bb015 ac9458c9 3ee8db9f (prompt reviews/prompt-r4.txt; CODEX_HOME=~/.codex-acct2 codex exec -m gpt-5.6-sol --sandbox read-only -c model_reasoning_effort=high)
codex r4 start 2026-09-13T11:54:00Z
codex r4 exit=0 end 2026-09-13T11:56:50Z
tree unchanged (shasum -c of candidate + prompt after the run: OK)
----- seat final answer verbatim (Italian in the original) -----
Nessun finding BLOCKER, MAJOR o MINOR.

NOTE — Il rollback distruttivo resta meccanicamente invocabile: una chiamata diretta a `rollback_migration("314_wa_outbox_evidence")` eliminerebbe colonne e ledger. Non è un difetto della candidate: lo spec richiede down/up isolato, vieta solo l’uso come rollback ordinario, e nessun percorso deploy lo invoca.

NOTE — Tenendo un lock incompatibile su `public.wa_outbox`, l’apply fallirà dopo circa 5 secondi. È il comportamento fail-safe dichiarato: la transazione annulla entrambi gli `ALTER` e non registra 314.

Gli attacchi rimanenti non producono una failure:

- Su PG15/17, colonne nullable senza default non riscrivono né scansionano l’heap; i receipt confermano filenode e righe invariati.
- `SET LOCAL` è valido nella transazione aperta dal runner e si azzera alla sua conclusione.
- Tabella, tipi, GUC e nomi-colonna non dipendono da `search_path`; `TIMESTAMP WITH TIME ZONE` e `NUMERIC` sono tipi di sistema prodotti dalla grammatica SQL.
- Senza `IF NOT EXISTS`, un rerun canonico viene saltato tramite ledger; una failure parziale viene annullata atomicamente. Una colonna preesistente senza ledger è drift e deve fallire. Il bootstrap corrente è inoltre coperto dai receipt.
- L’ordine del rollback è inverso e una failure lascia entrambi i ledger intatti.
- La PR schema-only evita che il nuovo writer parta nella finestra precedente-image/fresh-image.
- Entrambe le colonne soddisfano tipo, nullabilità, assenza di default e assenza di backfill richiesti.

VERDICT {"ok": true, "sha256_groups": "6b5d3d8b b99d8d37 d8ccbd97 414264df 6fb888a4 731bb015 ac9458c9 3ee8db9f", "blockers": 0, "majors": 0, "minors": 0}
