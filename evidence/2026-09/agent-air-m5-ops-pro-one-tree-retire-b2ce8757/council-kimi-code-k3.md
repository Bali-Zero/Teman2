• **1. runtime-reconcile.sh — clean.** `runtime_exists` is assigned unconditionally at scripts/runtime-reconcile.sh:112 before every read (`:121`, `:126`, `:141`, `:177`, `:182`); all new vars are defaulted at `:28-37`, and `fetch_head`/`fetch_mtime`/`age`/`offenders` are each set on the line before their only read (`:142-147`, `:168-169`). No leftover references: `deploy_exists`, `DEPLOY_DIR`, `DEPLOY_BRANCH`, `PULL_STATE`, `SOURCE_REPO` appear in no executable line or string in the final state (heartbeat `:177-178` and `save_lesson` `:182` were both switched).

**2. The mktemp version had a real "wrong file" defect — which the second hunk itself removes, so the final file is clean on this surface.** In the intermediate version, running the copy from `/tmp` breaks the payload's `dirname "$0"` resolution: `SELF_DIR` (scripts/pro/pro-git-pull.sh:67) feeds both the allowlist path (`:84` → `/tmp/../../infra/...` → absent → treated as "no allowlist configured" → origin-wins on Pro-authoritative files → re-publish) and the Telegram gateway (`:104` → `/tg_notify.py` → absent → every alert silently unsent). On the specifics: `RC` is set on every continuing path in both versions (final: `pro-git-pull-main.sh:72`, else-branch exits at `:76`); the trap `${SNAPSHOT:+"$SNAPSHOT"}` is safe when empty and even unset under `set -u` (the `:+` form never trips unbound); snapshot leaks only on SIGKILL (trap can't run).

**3. Clean.** `-x A` is unreachable after the rename, and when it was false the old line already degenerated to exactly `exec B`; a _failed_ `exec A` killed the shell (127) rather than falling through to B, so no fallback semantics existed to lose. The only removed behavior is preferring the frozen copy — the point of the ruling.

**4. Evasion and false positive both exist.** Evasion: a multi-line `<string>` — grep is line-based and `[^<]*` cannot cross a newline, so `<string>/Users/nuzantara/\nnuzantara-deploy/scripts/x.sh</string>` slips past scripts/runtime-reconcile.sh:168 (this fleet already ships multi-line strings, infra/launchagents/com.nuzantara.verify-the-verifiers.plist:18-21); XML character references (`nuzantara&#45;deploy`, which plutil decodes) and symlink indirection also evade. False positive: **yes** — the pattern matches any `<string>` bearing the substring, not executable references, so a `StandardOutPath`/env-value string mentioning the old tree, or a booted-out-but-not-deleted plist file still sitting in `~/Library/LaunchAgents`, pages every 6h forever under STRICT=1 (cooldown `:37`); the "FUNCTIONAL refs only" comment at `:165-167` is wrong because `<string>` also carries log paths and env values, and the remediation hint at `:170` ("run the launchagent installers") fixes none of those shapes.

**5. Also wrong:**

- infra/launchagents/install_wr3_reflexion.sh:12 — PRECONDITION still tells the operator the script exists only "AFTER this PR merges to main **and the deploy-puller syncs it**"; this diff deletes the deploy-puller plist, so that sync will never come. Same rot at scripts/cost_breaker_run.sh:12 ("deploy-puller-refreshed checkout").
- Contradictory canon shipped: the wrapper now declares the self-mod hazard "turns out not to exist — MEASURED" (pro-git-pull-main.sh:56-61), while the payload it execs still mandates "run from ~/nuzantara-deploy … a puller must not live in the tree it rewrites (self-mod)" (scripts/pro/pro-git-pull.sh:50-51) and still describes its allowlist as read from "the deploy checkout … rather than the TARGET being pulled" (`:80-84`) — under one tree `$SELF_DIR/../..` _is_ the target, so the diff should have rewritten that header in the same changeset.
- scripts/runtime-reconcile.sh:144 — `git clone` writes no FETCH_HEAD, so any disaster-recovery re-clone of `~/nuzantara` breaches invariant 4 and pages until the puller's first successful tick; "never fetched" mislabels "cloned, not yet pulled".
- scripts/runtime-reconcile.sh:139 — "FETCH_HEAD cannot lie that way" is overclaimed: a manual `git fetch` in `~/nuzantara` rewrites it and masks a dead puller for as long as the operator keeps fetching (only worktree-lane fetches are per-worktree and safe).
