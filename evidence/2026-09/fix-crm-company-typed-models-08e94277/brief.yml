mandate: >-
  Replace the four `data: dict` request bodies in
  apps/backend-rag/backend/app/modules/crm/company_router.py with typed Pydantic
  models (CompanyCreate, CompanyUpdate, ClientCompanyLinkCreate,
  CompanyDocumentCreate) so date strings bind as datetime.date and bad input is
  a 422 before any SQL, not a 500 from the driver. Follow-up to merged #7030.
gear: 2
dux: claude-opus-5
implementer: claude-opus-5
organ: apps/backend-rag/backend/app/modules/crm/company_router.py (Fly backend, /api/crm/companies write endpoints)
authority: >-
  Antonello 2026-09-21, Communication Log: "Buka PR backend company: keempat
  handler company_router.py pakai model bertipe ... PREPARE SAJA, JANGAN ARM.
  Merge di backend-rag = deploy, dikerjakan Antonello." No production write.
acceptance:
  - PATCH with an ISO date string binds datetime.date (test_update_date_string_is_bound_as_date)
  - PATCH with null or "" on a date column writes NULL (test_update_null_or_empty_date_clears_the_column)
  - blank or null company_name, and null company_type/status, is 422 with no SQL awaited
  - POST {} is 422 with no SQL awaited
  - link start_date and document issue_date/expiry_date bind as datetime.date
appetite:
  wall_clock_hours: 2
