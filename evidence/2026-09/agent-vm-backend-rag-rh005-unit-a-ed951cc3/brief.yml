mandate: >-
  Fix RH005 (scripts/lint_test_reward_hacking.py: a test that asserts
  nothing) in the 19-file unit/services "group A" of a repo-wide backend
  test-quality remediation (243 RH005 findings on main @ 29aff505, split
  across 5 concurrent cloud sessions by file group). Each of the 60
  offending tests now checks the specific effect its name promises, every
  new assertion mutation-tested (production code broken -> test RED ->
  reverted). No production code, conftest.py or lockfile touched.
gear: 2
colour: BLUE
implementer: sonnet-5
organ:
  apps/backend-rag/backend/tests/unit/services (analytics, autonomous_agents,
  canva_renderer_v2, crm, ingestion, integrations, misc, monitoring, pii,
  rag, rag/agentic, search — 19 files)
authority: >-
  Dispatched via an automated background-task directive to this cloud
  session naming this exact file group and instructing "apri il PR e arma
  l'auto-merge subito ... istruzione esplicita del proprietario: non
  lasciarlo in draft". No separate quoted Zero authorization exists for
  this specific PR beyond that dispatch.
acceptance:
  - "RH005 count for these 19 files: 60 -> 0 (scripts/lint_test_reward_hacking.py
    <files> | grep -c RH005), shown in the PR body"
  - "PYTHONPATH=.:../crm-cell python3 -m pytest <19 files> -p no:cacheprovider:
    630 passed, 7 pre-existing unrelated skips"
  - "git diff --stat main...HEAD limited to the 19 test files; no production
    file left modified by mutation-testing"
appetite:
  wall_clock_hours: 2
dissent: []
