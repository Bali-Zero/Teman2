task_id: vo-b3-ui-parity
lane: mouth
machine: air-m5
base_sha: f47d773361efcd35560db641f5b17b9d2a373118 # pragma: allowlist secret - public Git commit ID, not a credential
date: 2026-09-22

gear: 2
gear_reason: >-
  Floor 2 is declared for this TEST-ONLY UI-parity runner. The authoritative
  final measurement is `python3 scripts/contract_verify.py --head <final-head> --json`;
  this brief does not count itself.

objective: >-
  MANDATE-vo.md "Slice B3 v3": a Playwright UI-parity runner that replays a
  deterministic sample derived from B4-2's 252 recorded walks. It intercepts
  the engine route and compares the rendered DOM with the recorded verdict,
  spending no engine call. The B4-2 `walk_id` equality and the outcome-class
  sample size are measured during `beforeAll`, never transcribed (re-measured
  by this finisher: 252 = 252 derived-label-set equality; 34 outcome classes,
  19 if `notices` is dropped from the grouping key).

  A THIRD RED WAS MET AND REPORTED, never cured: the checked-in B4-2 report
  (sha 20d429cc…) carries one outage row, `review-gate/blacklist`
  (`engine_state: "TEMPORARILY_UNAVAILABLE"`, `rule_pack: null` — a sweep-time
  engine hiccup, not a real verdict), which fails `beforeAll` by name and
  blocks all 34 samples before any render logic runs. None of the five
  required guilt-proof mutations (U2, U3, U5, U6, U7) could be captured via
  the actual Playwright command as a result; U2 and U3 were independently
  re-verified out-of-band (a scratch `tsx` script against the same checked-in
  report, run then deleted) and hold as stated above. Curing the outage row
  is a report-data question (a re-sweep of that one walk), out of this PR's
  touch-no-other-file scope.

scope:
  - >-
    apps/mouth/e2e/visa-oracle-live-parity.spec.ts: top-level import of the
    B5-2 library module; B4-2 report path and sha pin; derived sample-size
    measurement; every expensive call remains in beforeAll.
  - This brief.

constraints:
  - >-
    No source file, config, workflow, or dependency changed. The enumerator
    and flow reducer are read-only upstream reuse.
  - >-
    Nothing this spec runs sends a request to production or to a local engine:
    the evaluate route is always intercepted and fulfilled from the checked-in
    B4-2 record.
  - >-
    B5-1 is expected to replay every sampled class, including the ten
    review-gate clones; a replay throw remains reported and is never adjusted.
  - >-
    The codex build seat that prepared this diff did not arm, merge, push,
    or create a PR — this Sonnet finisher grades the diff, pushes the
    reviewed commit to the existing #7038 (force-with-lease, the conductor's
    declared rebase) and re-bodies it, and likewise never arms or merges.
    No client PII is included in this diff or brief.
