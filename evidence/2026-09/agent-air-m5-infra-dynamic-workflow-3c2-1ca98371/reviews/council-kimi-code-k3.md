> Seat `kimi-code/k3`, dispatched live via `kimi -m kimi-code/k3` (titolare, real
> adversarial round, 2026-09-19) against the pre-cure staged diff at base
> `2d20035eadab8fe4acb9594e9dac382c9257de21`. Verbatim output below, unedited (including the
> tool-narration lines the seat printed before its verdict).

---

[Air-M5]

Let me start by reading the diff and the relevant files.

Now let me examine the actual repo files for context — the lint scripts, test files, and existing workflow conventions.

Now the critical checks: existing main()-level tests, the fixture freshness, and whether existing repo-wide workflow lints will judge this new YAML.

Good context. Now let me verify the `--selftest` entry point, import surface (CI installs only pytest), the timeout floor value, and whether other repo lints will judge this new workflow.

Two things left to verify: whether `dynamic_workflow.py`'s imports work in a bare-CI environment, and actually running the new tests. Let me check imports first.

All edge cases confirmed by execution. One last demonstration — the freshness time-bomb mechanism in the new innocence test:

All verification done — I have everything I need. Compiling the review.

VERDICT: REWORK

Findings (numbered, one per issue; line refs are to the post-diff files, with diff-line refs where useful):

1. **MEDIUM — The new model-cards innocence test is a 30-day time-bomb, and it arms this CI workflow to go red on 2026-10-19.** `test_innocence_explicit_dir_with_one_clean_file_exits_0` (`scripts/tests/test_lint_model_cards.py`, new test at diff lines 234–246) copies the fixture `clean.md` verbatim into a tmp dir and runs `lint.main()`. That fixture has a hard-pinned `date: 2026-09-18` (verified on disk), `FRESHNESS_DAYS = 30` (`scripts/lint_model_cards.py:62`), and `main()` calls `find_violations(path, dw)` with **no `today=` override** — so it uses wall-clock today. On 2026-10-19 the fixture turns 31 days old, RULE 3 fires, `main()` returns 1, and the test's `assert rc == 0` fails. I demonstrated the mechanism directly: the same fixture with `date: 2026-08-01` exits 1 with `expired: ... 49d old (> 30d)`. The file's own comment (lines 34–39) documents exactly this staleness trap for the `find_violations`-level tests and pins `FIXTURE_TODAY` to avoid it — but `main()` accepts no `today`, so the new `main()`-level test cannot use that cure. The pre-existing `test_innocence_one_clean_in_scope_file_is_enough` (line 171) has the same latent defect; this PR replicates the pattern instead of fixing it, and its own new workflow inherits the red. Cure: in the tmp dir, write the card with `date: {date.today()}` interpolated (the pattern already used by `test_freshness_30_days_old_is_not_expired` at lines 120–133) rather than copying the date-pinned fixture. For a PR whose entire purpose is arming a lint in CI, shipping a test that makes that CI go stale-red in 30 days is the exact defect class the mission fights.

2. **MEDIUM — The blind-scan closure is incomplete: it fires only when the WHOLE invocation scans zero, so a stale/renamed directory pinned alongside a live target still goes green forever.** The guard at `scripts/lint_workflow_script.py:571-598` (and mirror at `scripts/lint_model_cards.py:196-222`) is gated on `explicit and scanned == 0`, where `scanned` is a global across all targets. I demonstrated: `python3 scripts/lint_workflow_script.py /tmp/empty_dir /tmp/oneclean/clean.js` → `✅ no violations (1 file(s) scanned)`, exit 0, the empty directory silently ignored. The mandate's guard rule as written ("ANY invocation that scans zero files exits 2") is literally satisfied, but cicatrix #2 ("exists != armed") survives in every mixed-target invocation — e.g. a hypothetical future CI line `lint_workflow_script.py infra/workflows infra/workflows-legacy` where one dir gets renamed. The armed usage (default sweep, no positional args, pinned by the new YAML test) is not exposed, which is why this is MEDIUM not HIGH. Cure: per-target accounting — any explicit raw target that individually yields zero in-scope files should exit 2 (keeping the wrong-suffix-file carve-out), not just the all-targets-zero case.

3. **MEDIUM — The `paths:` filter and the pytest step disagree about scope: the filter triggers on all 27 `scripts/tests/test_lint_*.py` files, but the workflow executes only 2 of them.** Filter at `.github/workflows/dynamic-workflow-selftest.yml:38` (`scripts/tests/test_lint_*.py` — I counted 27 matching files on disk, e.g. `test_lint_telegram_tokens.py`, `test_lint_web_surface.py`); pytest step at line 74 runs only `test_lint_workflow_script.py` and `test_lint_model_cards.py`. Net effect: a PR that changes any of the other 25 lint test files triggers this workflow, which goes green without ever executing the changed file — a check that fires on a change it does not test, one level up from the blind-scan shape this PR exists to close. I acknowledge this is partly mandate-inherited (the mandate fixed the filter list verbatim and said only "pytest on the named test files"), but the author resolved the ambiguity inconsistently: wide reading for the filter, narrow reading for the jobs. Either narrow the filter to the two exact files or run the glob. (Non-required check, so the damage is a misleading green, not a false block.)

4. **LOW — The filter carries a 9th path entry beyond the mandate's exact 8-item list.** `.github/workflows/dynamic-workflow-selftest.yml:42` adds the workflow file itself. The mandate says the filter must match its list (my review instruction: "no more, no fewer"). Self-triggering on workflow edits is defensible convention, but it is an undeclared deviation and should either be dropped or called out in the PR body.

5. **LOW — The wrong-suffix carve-out is a literal exception to the mandate's stated guard rule, preserved silently.** An explicit existing file of the wrong suffix still exits 0 with `no violations (0 file(s) scanned)` (demonstrated: `lint_workflow_script.py /tmp/notes.txt` → rc 0). This is pre-existing, tested behavior (`test_innocence_explicit_non_js_file_is_still_green`) and the new docstring states it explicitly, so it is an informed deviation from "ANY invocation that scans zero files exits 2" — but a scan that sees nothing still reports clean in this shape, and the PR body should say so rather than letting the gate discover it.

6. **LOW — `timeout-minutes: 10` passes the repo's timeout-floor lint by exactly zero margin.** `.github/workflows/dynamic-workflow-selftest.yml:55` declares 10; `scripts/lint_workflow_timeout_floor.py:73` sets `FLOOR_MINUTES = 10` and flags only `< 10`. That lint's own header records a worst-observed checkout of 599s — i.e. a worst-case checkout would consume essentially the entire budget of this job, the precise failure mode (cancelled-not-failed, never turns green) the lint exists to prevent. 15 would cost nothing.

7. **LOW — Directory expansion uses a non-recursive glob.** `raw.glob(DEFAULT_GLOB_PATTERN)` (`scripts/lint_workflow_script.py:532`, `scripts/lint_model_cards.py:166`) means an explicit directory whose in-scope files live in subdirectories exits 2 with "yielded zero .js/.md files" even though files exist deeper. The message is accurate for the glob and consistent with the flat default sweep, so this is a documentation nit at most — but the docstring's "an existing directory that yields zero .md files" overpromises slightly.

On the size question (review item 7): the ~142 inserted lines are justified, not bloat. Each lint script gets ~13 lines of guard code + ~9 lines of mandated docstring/comment (~22 each), and the tests are +27/+46, of which the YAML-shape test (17 lines) is explicitly mandated by closure item 2 and gate-12 item 3. The near-identical guard blocks in the two lint scripts mirror an existing deliberate parallelism in this codebase, and the heavy inline comments match these files' established style. The mandate's "~+25 lines" estimate simply undercounted the three mandated wrapper tests.

On security (review item 6): no untrusted PR input reaches any `run:` step; the only expression used is `github.event.pull_request.number || github.ref` in the concurrency group name, which is safe; `permissions: contents: read` is minimal; this is `pull_request` (not `pull_request_target`), so PR code runs with a read-only token; `actions/checkout@v7` / `setup-python@v7` match this repo's existing convention. Unpinned `pip install pytest` from PyPI is a minor supply-chain nit but consistent with sibling workflows.

Verification: I read the full diff at the stated path; both lint scripts' `main()` in full on-disk context (`scripts/lint_workflow_script.py:524-617`, `scripts/lint_model_cards.py:159-237`); both test files end-to-end; the `clean.md` fixture (confirmed `date: 2026-09-18`); the timeout-floor lint; and sibling workflows for convention. I then **executed** everything in the worktree at the staged head: `pytest scripts/tests/test_lint_workflow_script.py scripts/tests/test_lint_model_cards.py` → 51 passed; both default sweeps → exit 0 (6 and 1 files scanned); `dynamic_workflow.py --selftest` → SELFTEST OK; and the edge matrix by hand — explicit empty dir → exit 2 with the BLIND SCAN message naming the target; explicit dir with one clean file → exit 0; explicit nonexistent path → exit 2; wrong-suffix file → exit 0 (carve-out confirmed); **mixed empty-dir + clean-file → exit 0 (finding 2 demonstrated)**; and an aged copy of the fixture → exit 1 `expired` (finding 1's mechanism demonstrated). The guilt/innocence tests are real subprocess/module-level assertions against captured output, not tautologies; the YAML test asserts exact stripped `run:` lines and the absence of `find_violations` in the raw file (I confirmed the workflow's own header comment does not contain that string, so the test is not self-defeating).

Coverage limitation: I did not run the actual GitHub Actions workflow — no actionlint pass, no ubuntu-latest runner, no PyPI `pip install pytest` in a clean environment, and CI uses Python 3.12 where I ran 3.11 (nothing in the diff is version-sensitive, but I did not prove it). I did not review the Gear-3 evidence pack under `evidence/2026-09/` (explicitly out of scope). I did not check branch-protection/required-check settings (the workflow is deliberately non-required; promotion is stated as a later operator action). Finding 3's severity depends on my reading of "the named test files" in the mandate; a human should confirm which reading was intended. The 30-day time-bomb (finding 1) is proven by mechanism demonstration, not by waiting 30 days — a human could also prove it by running the new test with the system clock set to 2026-10-19.
