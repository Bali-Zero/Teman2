• [Air-M5] Refuter at work. I'll verify each claim against the actual files on disk — wrapper, heartbeat lib, registry checksum tooling, cron-runner, conformance gate, and the tests.

• Wrapper and heartbeat lib read. Now the checksum validator, cron-runner (rc propagation + G10 single-instance), and the home-fork linter.

• Gate is green with only advisory G10. Now: checksum verification (need a python with yaml), the new tests, the home-fork linter mechanism, and the old-wrapper regression experiment.

• Checksum verifies and all 7 tests pass on the new wrapper. Now the critical refutation test: would these tests actually catch the OLD wrapper?

• Old-wrapper experiment conclusive. Two last checks: the declared pair on disk, and whether the registry entry's label genuinely satisfies G1's textual check (already implied by the gate, but confirm the pair entry).

• All claims verified. One last process check — the branch diff showed 46 files; is that this PR or a stale main ref?

• All evidence gathered. Here is the refutation, claim by claim.

  ---

  ## REFUTER REPORT — s7-yield-canon-0926

  **Method:** read the touched files, ran the repo's own gates read-only (conformance checker, registry validator), ran the new tests, and replayed the tests against the pre-genes wrapper extracted from git. Did not touch `~/Library`, `launchctl`, or the environment.

  ### Claim 1 — "plist is byte-identical to the live copy"
  **UNVERIFIABLE from this seat — but not refuted, and machine-checked where it matters.**
  My rules forbid reading `~/Library/LaunchAgents`, so I cannot compare bytes. What I could verify: the repo copy parses (`plistlib` load OK), `Label: com.balizero.s7-yield.weekly` matches `recovery_params.label` in the registry entry, and argv is `cron-runner.sh → s7-yield-run.sh` as briefed. The declared pair exists on disk in `infra/home-fork/declared-pairs.json` with `machines: ["pro"]`, and the enforcement mechanism is real: `scripts/lint_home_fork.py:390-391` compares `sha256_file(live)` vs `sha256_file(repo)` for declared pairs. So if the "byte-identical" claim were false, the next `--check` run on Pro reports `DIVERGED`. The claim rests on the author's word today and on the linter from tomorrow. Severity: **MINOR** (accepted-on-control, not on evidence).

  ### Claim 2 — "every exit path writes a heartbeat"
  **CONFIRMED for all normal exits; two residual holes, both small.**
  - Kill switch: `scripts/s7-yield-run.sh:64-67` → `organism_heartbeat "$ORGAN_ID" "disabled" "kill-switch"; exit 0` ✓
  - Missing payload: `:73-76` → `"error" "script missing"; exit 1` ✓
  - Payload result: `:79-86` → `ok`/`error "rc=$rc"` then `exit "$rc"` ✓
  - The heartbeat writer genuinely cannot kill the caller (`scripts/lib/heartbeat.sh:57` — subshell + `|| :`) and writes atomically (`:496-502`, tmp+mv).

  **MINOR — G2 is silently waived when the library is absent.** `:60-62` installs a no-op `organism_heartbeat() { :; }`, and `test_missing_heartbeat_library_does_not_change_the_exit_code` codifies `hb is None` as the ACCEPTED outcome. So on a run where `heartbeat.sh` is missing, every exit path writes nothing and the organ ages into stale/dead with a clean exit code — the exact "green that lies" G2 exists to prevent, in miniature. Mitigation is real (the lib lives in the same `S7_YIELD_REPO_ROOT` checkout as the payload; "missing" means repo damage, which cron-runner's receipt would likely catch), so MINOR not MAJOR.
  **NIT — no signal trap.** SIGTERM/SIGKILL mid-payload writes no heartbeat. "Every exit path" means every *exit-code* path.

  ### Claim 3 — "the exit code propagates"
  **CONFIRMED.** `exit "$rc"` (`s7-yield-run.sh:86`); cron-runner preserves it via `PIPESTATUS[0]` (`scripts/cron-runner.sh:183`) and re-exits with it (`:211`), alerting on non-zero (`:208-210`). Test asserts `rc == 3` end-to-end through `/bin/bash`.

  ### Claim 4 — "kill switch writes disabled"
  **CONFIRMED.** Both spellings short-circuit before the payload (`:64`), the lib passes `disabled` through verbatim (`heartbeat.sh:227`), and the readers are documented in-file as understanding it (`EXEMPT_STATUSES`, healer side). Tests cover both `S7_YIELD_ENABLED=false` and legacy `S7_YIELD_OFF=1`, with a payload stub at rc=7 proving the payload never runs.

  ### Claim 5 — "the checksum is valid"
  **CONFIRMED by execution.** The validator recomputes sha256 over the canonical JSON of the parsed organs list (`validate_organs_registry.py:79-87`, compared at `:231-243`) — not over the file text, so the "only the checksum line edited in place" method is sound. Ran it: `✓ organs_registry.yaml valid`, rc=0. Also note `update_checksum` (`:263-274`) would have re-serialized the whole YAML; editing just the line avoided a 3000-line reformat diff. Good call.

  ### Claim 6 — "the tests would fail on the old wrapper"
  **CONFIRMED by replay.** I extracted the pre-genes wrapper (`git show aac3697bbc:scripts/s7-yield-run.sh` — old kill switch, bare `exec`, no heartbeat) into a scratch tree with the new test file: **6 failed, 1 passed**. The one pass is `test_missing_heartbeat_library_does_not_change_the_exit_code`, which passes on old and new *by design* (it asserts survival + no heartbeat, a property of the absence of the lib). Not a hole — but the PR text "the tests would fail on the old wrapper" is true only collectively. NIT on wording.

  ### Gate-level confirmation
  Ran `infra/organ-conformance/check_organ_conformance.py` on this worktree: **exit 0**; the new organ reports `missing: []`, `advisory: ['G10_single_instance']`, wrapper correctly resolved through the cron-runner hop to `scripts/s7-yield-run.sh`. G10 is advisory, not violated, because the gate's mandatory trigger is a `claude -p` spawn (`check_organ_conformance.py:360-372`) and this organ uses local Ollama. Still, no flock/pidfile exists anywhere on the path — noted as **NIT**, consistent with the genes contract's "advisory otherwise".

  ### Scope check
  `git diff origin/main...HEAD` = 6 files, 295 insertions — one concern, within contract. (The 46-file stat floating around is against the stale local `main` ref; ignore it.)

  ### Findings summary
  | # | Severity | Where | Finding |
  |---|----------|-------|---------|
  | 1 | MINOR | s7-yield-run.sh:60-62 + last test | Missing heartbeat lib = silent G2 waiver, codified as accepted |
  | 2 | MINOR | declared-pairs.json note | "Byte-identical to live" unverifiable pre-merge; rests on lint_home_fork catching it post-merge on Pro |
  | 3 | NIT | s7-yield-run.sh | No signal trap; "every exit path" = every exit-code path |
  | 4 | NIT | wrapper/cron-runner | No flock/pidfile (G10 advisory, correctly non-blocking) |

  No BLOCKING or MAJOR finding survives contact with the code. The two MINORs are declared trade-offs with real mitigations, not lies.

  VERDICT: PASS

