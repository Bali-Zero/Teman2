# Ledger rows held in evidence — GARUDA VOA W1W PR2a-v2

Three rows parked here by PR2a (`aed342999e`, `ledger-rows-held.md` of the suspended lane)
because `.claude/skills/modus/PENDING-ARMS.md` was deadlocked on 2026-09-12 (`merge=union`
append races read as DIRTY on GitHub; Imperator decision #28 gesture, as #6292 and #6286).
They are carried VERBATIM here, with one change: **row 1 CLOSES with this PR** (SPEC v2 §10),
its status prefix flipped and the proof appended; rows 2 and 3 stay open with their owners.

**Deferred, not lost.** Append all three to `PENDING-ARMS.md` unchanged — healer tick or a
dedicated ledger PR — once the file stops racing.

Rows, one per line:

- closed 2026-09-12 (M5, GARUDA VOA W1W PR2a `backend-rag/voa-store-adapter` — refuter round O2 (gpt-6-astra, account default — not Sol; decision #33) finding 1, SAME CAUSE as O1 finding 8, declared rather than cured a third time under Builder Contract 1) | **The two INSERT-race tests in `test_postgres_store.py` pin the OUTCOME of a same-key race (exactly one winner, one row, IDEMPOTENCY_CONFLICT for a differing payload) but not the BRANCH: nothing forces the loser through the `UniqueViolationError` handler rather than the sequential `SELECT ... FOR UPDATE` comparison.** The `asyncio.Barrier(2)` coordinates two EXTERNAL probes, not the lookups `commit()` makes internally, so A can run its whole transaction before B's internal lookup and B replays instead of colliding. Sol reproduced that schedule against the candidate with the whole handler deleted: both tests PASS, zero PK violations. On M5's scheduler the same mutation kills both tests on PG 17.10 and 15.19 (measured 2026-09-12, `2 failed, 48 passed`) — an observation about this machine, not a guarantee. | CLOSED by PR2a-v2 (`agent/air-m5/backend-rag/voa-store-adapter-v2`, SPEC v2 §4): `PostgresDocumentStore` carries a test seam `_CommitHooks(after_lookup, after_collision_before_reread)`; schedule A blocks both racers INSIDE their transactions after the lookup, so the second INSERT must collide on the primary key, and `test_p5_p6_schedule_a_exactly_one_winner_and_the_loser_traversed_the_pk_branch` asserts the collision counter == 1 -- the branch assertion this row said was missing. Guilt `P6_handler_deleted` is red on PG 15.19 and PG 17.10 with no scheduler caveat (brief `guilt_mutations`); a no-op seam (`S4_collision_hook_noop`) makes the race tests fail to SET UP. | formerly missing arming step: PR2b implements `SPEC-PR2b-interleaving.md` §1 (R1 test-only seam between lookup and INSERT, R2 barrier INSIDE the seam, R3 assertion that the loser traversed the PK branch) and §2 (R4/R5 semantics for "winner vanished after the collision", which 304 permits after `retention_until`). | owner: me (sessione M5, GARUDA VOA W1W, PR2b) | proof-of-armed: the "handler deleted" mutation is red on PG 17 AND PG 15 under the seam-forced schedule, and a no-op seam makes the race test fail to SET UP rather than pass; both named in PR2b's brief `guilt_mutations`.
- opened 2026-09-12 (M5, GARUDA VOA W1W PR2a — refuter round O2 (gpt-6-astra, account default — not Sol; decision #33) finding 4, SAME CAUSE as O1 finding 10, declared rather than cured a third time) | **A replayed `LowConfidenceOutcome` returns its fields in `PassportReviewFieldName`'s declaration order, not in the order the original `commit` received; `garuda_document_review_fields` has no ordinal column to remember a different one.** For every outcome `confidence.py` builds today the two coincide (it iterates the enum); for a tuple assembled in any other order they do not, and the sentence "a replay preserves the order received" is false. PR2a's port docstring (`DocumentStorePort.get_existing`) now promises the CANONICAL order explicitly, and `test_low_confidence_replay_returns_the_fields_in_the_original_order` declares it proves only that case. | missing arming step: either a consumer is found that needs the received order — then an ordinal column, which is a migration on a table 304 already created in production, so its own PR and gate (`SPEC-PR2b-interleaving.md` §3, R6) — or no such consumer exists and this row closes on that finding. Not PR2b's. | owner: operator[product] (whoever first needs received order; until then nobody, and the canonical promise stands) | proof-of-armed: `grep -c 'CANONICAL order' apps/backend-rag/backend/services/garuda_documents/ports.py` on `origin/main` returns ≥1; the row closes when a migration adds the ordinal and the store reads `ORDER BY ordinal`, or when a written decision says received order is not a requirement.
- opened 2026-09-12 (M5, GARUDA VOA W1W — found while preparing PR2a's O3 round; Imperator decision #33) | **PR #6281's merged brief (`evidence/2026-09/agent-air-m5-backend-rag-voa-store-wiring-23492b6e/brief.yml`) records its refuter as `model_id: gpt-5.6-sol`; the run's log header says `model: gpt-6-astra`.** The invocation carried no `-m`, so the account default in `~/.codex/config.toml` (`gpt-6-astra`, the Astra seat, prepare-only) carried the round. The model id in the brief was copied from the mandate text, not read from the log — the same class as a test count taken at commit N and never re-measured. The findings were real and the cures stand; the seat name is what is wrong. | missing arming step: a docs PR from fresh `origin/main`, AFTER PR2a lands, correcting `model_id` to `gpt-6-astra` with the log header as the measurement and a `model_id_source` line, as PR2a's brief now does. | owner: me (sessione M5, GARUDA VOA W1W) | proof-of-armed: `grep -c "model_id: gpt-6-astra" evidence/2026-09/agent-air-m5-backend-rag-voa-store-wiring-23492b6e/brief.yml` on `origin/main` returns 1 and `grep -c "model_id: gpt-5.6-sol"` on that file returns 0.
