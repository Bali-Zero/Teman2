# Ledger rows held in evidence — GARUDA VOA W1W, gate-6281 conditions 1 and 2

The two rows below BELONG in `.claude/skills/modus/PENDING-ARMS.md`. They are parked here
instead because that shared ledger is in deadlock today: three healer ticks and several
concurrent lanes appended to it on 2026-09-12, and `.gitattributes` declares the file
`merge=union` — a driver GitHub's own mergeability computation does not apply, so every
same-day append race reads as DIRTY on the PR even though it merges cleanly client-side.
This PR went DIRTY on that file twice; the second time the gesture changed rather than
repeating (decision #28), following the precedent #6286 set in another lane on the same day.

**These rows are deferred, not lost.** They must be appended to `PENDING-ARMS.md`
VERBATIM — by a healer tick or by a dedicated ledger PR — once the file stops racing.
Until then this file is where they are readable, and the `Bites:` of the change they
describe lives on the brief and on the test module's docstring, not on the ledger.

Rows, exactly as they were written, one per line:

- opened+closed 2026-09-12 (M5, GARUDA VOA W1W `backend-rag/voa-store-wiring` — condition 1 of the fresh out-of-chain Opus 5 `gate-6281` PASS-WITH-CONDITIONS verdict on head `a49bf81242d315f3fcb5108f3b03b196d6802225`, merge `b24b86ee68`; raised by the gate, not self-reported) | **The brief's gear comment cited the net-line count of the PR's FIRST commit as if it were the diff's, and the floor is computed over the diff.** `evidence/2026-09/agent-air-m5-backend-rag-voa-store-wiring-23492b6e/brief.yml:7` said "no hot-zone path, 167 net lines < the 400-line size term". 167 was measured on `b426487720` alone, before the refuter round added the four propagation tests and their prose. Measured on the range the floor actually reads, `git diff --numstat e4362692424785ff a49bf81242d315f3fcb5108f3b03b196d6802225`: **+426/-32 = 394 net**. The floor verdict is unchanged — 394 is still under the 400-line size term and no path is hot-zone, so `compute_floor` returns 1 either way — which is exactly why this was worth correcting rather than shrugging at: a stale number that happens to land on the same side of a threshold is the one nobody re-measures, and 394 sits six lines from flipping it. The class is the same one the gate named twice in this window: a figure measured at commit N and never re-measured at commit N+1. | missing arming step: none — corrected in this same PR, and the same PR fixes the sibling instance in the row below | owner: n/a (closed) | proof-of-armed: `grep -c '394 net lines' evidence/2026-09/agent-air-m5-backend-rag-voa-store-wiring-23492b6e/brief.yml` on `origin/main` returns 1 and `grep -c '167 net lines'` returns 0; re-running the numstat above still prints 426/32.
- opened+closed 2026-09-12 (M5, GARUDA VOA W1W — condition 2 of `gate-6281` on the same head; same class as the row above, which is why both close in one PR) | **A test module's own guilt table reported mutation counts measured against an earlier, shorter version of itself.** `apps/backend-rag/backend/tests/services/garuda_documents/test_ports_actor_scoping.py` documents each mutation with the result it produces. Its first entry — `InMemoryDocumentStore` reverted to a single-string key — recorded "2 failed, 3 passed", true of the five tests the module had before Codex Sol's CONDITION round added four more, and left uncorrected when they landed. Re-measured on the shipped nine-test file, on `origin/main`, in a disposable worktree with the mutation applied and the file restored byte-identical afterwards: **3 failed, 6 passed** — the third failure is `test_service_does_not_replay_one_actors_outcome_to_another`, which did not exist when the original figure was taken. A guilt table that under-reports its own mutation is worse than none: a reader who runs it and sees three failures has to decide whether the extra one is a real regression or stale bookkeeping, and nothing in the file told them which. | missing arming step: none — corrected in this same PR, with the corrected entry naming which version each figure belongs to so the next person who adds a test knows the number is theirs to re-measure | owner: n/a (closed) | proof-of-armed: apply the single-string-key mutation to `ports.py` and run `pytest backend/tests/services/garuda_documents/test_ports_actor_scoping.py` — it prints `3 failed, 6 passed`, matching the module docstring; `grep -c '2 failed, 3 passed' ` on that file returns 0.
