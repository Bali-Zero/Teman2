[Pro] 1. **MAJOR — CONCURRENCY/FIFO.** `apps/backend-rag/backend/services/integrations/wa_outbox_worker.py`: `NOT needs_generation OR NOT EXISTS (...)` and `older.needs_generation = true`. This is FIFO only among generated replies: a newer human-send row can bypass an older generated row, and an older human-send row cannot block a newer generated row. That violates strict FIFO for the thread, even though the advisory lock prevents simultaneous processing. Make every active older row block, or explicitly narrow the promised invariant to generated rows.

2. **CONCURRENCY — clean for generated rows.** For committed `needs_generation=true` rows, the `NOT EXISTS` predicate still sees an older row skipped by the outer `SKIP LOCKED`; its `pending`/`claimed`/`generating` status blocks the newer row. Combined with the stated pre-claim per-thread advisory lock, two workers cannot claim two generated rows of one thread concurrently. The fencing token protects ownership but is not what establishes FIFO.

3. **BLOCKER — COST/DUPLICATES/BINDING.** `apps/backend-rag/backend/db/migrations_v2/316_wa_outbox_inbound_binding_and_terminal_reasons.sql`: `ADD COLUMN IF NOT EXISTS inbound_message_id BIGINT NULL;`. Nothing in this diff writes that column, supplies a default, or installs a trigger. Therefore unchanged outbox inserts will create NULL bindings and fall back to the legacy heuristic, leaving the original retry-to-latest-message duplicate defect alive. Update the outbox producer to insert the inbound ID atomically and add a new-row invariant after legacy rows are handled.

4. **MAJOR — COST AMPLIFICATION.** `apps/backend-rag/backend/services/integrations/wa_outbox_worker.py`: `# 3. B2.5: no coalescing sweep` together with the existing `if attempts >= MAX_ATTEMPTS`. A burst of N rows can now consume up to `N × MAX_ATTEMPTS` full generations and emit N repetitive answers; there is no per-thread admission ceiling, burst budget, or duplicate-intent guard in the diff. Add bounded per-thread backpressure and an explicit terminal/handoff outcome for overflow while retaining one durable outcome per inbound message.

5. **COST/24H WINDOW — clean.** The shown code continues loading the thread as the `24h window source`; the WhatsApp service window is reopened by the thread’s latest customer message, so it should not be calculated independently from each older bound message.

6. **MAJOR — LIVE-MIGRATION LOCKING.** `apps/backend-rag/backend/db/migrations_v2/316_wa_outbox_inbound_binding_and_terminal_reasons.sql`: `ALTER TABLE wa_outbox ADD COLUMN`, the validated `ADD CONSTRAINT`, `VALIDATE CONSTRAINT`, CHECK replacement, and non-concurrent `CREATE INDEX` all run in one transaction. The initial ACCESS EXCLUSIVE lock remains held through validation and index construction, while no `lock_timeout` bounds acquisition. A long-running transaction can turn this small-table migration into an application outage through lock-queue buildup. Split the operations into migrations, set a short lock timeout, validate constraints separately, and create the index concurrently where runner semantics permit.

7. **MAJOR — MIGRATION INTEGRITY.** Same file: `FOREIGN KEY (inbound_message_id) REFERENCES meta_inbox_messages(id)`. This proves only that some message exists; it does not prove that it is inbound, belongs to the same thread, or differs from the outbound stub. A producer defect can bind a row to another thread or to its own outbound message while satisfying the FK. Add an enforceable composite relationship to the thread plus an inbound-direction invariant, or a transactionally tested trigger/application constraint.

8. **MAJOR — ROLLBACK IS LOSSY.** Same file: `ALTER TABLE wa_outbox DROP COLUMN IF EXISTS inbound_message_id;` and the restored CHECK `... ) NOT VALID`. Dropping the column permanently destroys the new audit linkage, while the old NOT VALID constraint leaves historical rows outside the restored invariant and can reject later updates to those rows. Retain the nullable column during code rollback, or archive its mappings and explicitly normalize/backfill new terminal reasons before restoring and validating the old constraint.

9. **BLOCKER — LEDGER.** Same migration: `CHECK (generation_fall_off_reason IS NULL OR generation_fall_off_reason IN (...))`. This explicitly permits every `status = 'failed'` row to have a NULL reason; the Python updates are convention, not an enforced invariant, and any missed or future writer can still create the forbidden terminal state. Add `status <> 'failed' OR generation_fall_off_reason IS NOT NULL`, backfill legacy failed rows with an allowed reason such as `unknown`, then validate the constraint.

10. **PII — clean.** The shown additions contain no client phone number, message body, or name in a log line or runtime error string.
