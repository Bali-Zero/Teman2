# Council read of v5 — Kimi K3 (refuter seat)

Seat: `~/.kimi-code/bin/kimi -m kimi-code/k3 -p …`, 2026-09-11 ~03:35 WITA, 718 s, packet inline; Kimi additionally verified citations on disk from the frozen docs worktree at base `074b4fde01` (pre-#6123 — its `migration_manager.py:311` pin is stale for that reason; `:323` holds on origin/main). Verdict: REWORK, 6 findings + 5 additions; disposition in `README.md` §Adversarial review (K5a rejected). Verbatim output:

• [Pro] I'll verify the packet's load-bearing citations on disk before issuing a verdict. Launching three parallel exploration lanes: the score chain, the test/finalizer layer, and the infra/governance citations.

• VERDICT: REWORK

Verification base: this worktree at `e56207342a6ce4d59fb40fe0637a73eec67aba85` (tip carries spec v5 + Astra round-4b receipt). All packet files present at `research/operations/2026-09-11-bot-staff-room/`. I verified citations on disk; I did **not** re-run Astra's 0.8/0.8 counterfactual probe, the production `wa_outbox` SQL, or the deployed-container greps — those are judged on code structure only.

**Core-claim audit (what falsifies each, and whether the packet's evidence survives):**

- **(a) Formatted RRF, not cosine — claim SURVIVES, one supporting citation is fabricated.** Falsifier would be: any path delivering a raw cosine to `calculate_evidence_score` as `sources[].score`. I traced all seven paths (hybrid RRF, dense fallback, rerankers ×2, curated 1.0, trusted-tool 0.85, cache, FAQ/KG). None delivers a raw cosine: `result_formatter.py:89` (`score = 1 / (1 + distance)`) is the final word on both retrieval paths, the rerankers overwrite `score` and demote the formatted value to `vector_score` (`core/reranker.py:176-180`), and `result_formatter.py` never reads `qdrant_db`'s raw `"scores"` list. #5618's "impossible cosine" argument is indeed moot. **But** the RRF arithmetic the packet uses to quantify this rests on a file that does not exist (finding 1).
- **(b) Scorer cannot see fact presence — claim SURVIVES.** Falsifier would be: the scorer semantically inspecting chunk text. `reasoning_utils.py:214-218` takes `(sources, context_gathered, query)` and touches text only via lexical keyword ratio (`:468`, `:506-511`, `:543-552` legacy floor). No embeddings, no semantics. The 0.8/0.8 probe is consistent with the code; a support signal must indeed be built.
- **(c) Golden set measures topical affinity — claim SURVIVES, verified exactly.** `test_evidence_cross_language.py:183,186-187` label three rows `True` whose contexts (`:47-56`) contain neither capital nor duration; `:207-212` the label selects sources AND context; `:216` forces acceptance; both xfails strict (`:239`, `:258`); 124 cases confirmed. The test's own docstring (`:200-202`) concedes the point.
- **(d) Migration must ship separately — claim SURVIVES.** `fly-deploy.yml:139,155-157` pre-deploy migrations SSH into the currently-running (previous) container; `:262-282` comment says so verbatim; post-deploy re-run on fresh image at `:359-363`. A writer shipped with its SQL meets absent columns in both windows. Correct.
- **(e) ACCEPT-bound-to-sha256 = approval under army-map.md:98-99 — claim OVERREAD.** Lines 98-99 define the **Dux-nomination record** ("both imperator approvals on the same packet hash"), not a rule that a hash-bound ACCEPT constitutes imperator approval of a spec packet. The ACCEPT and both hash sets exist in `codex-round-4.md` (verified); the cited legal basis does not say what the packet says it says (finding 2).

**Findings:**

1. **MEDIUM — fabricated citation `hybrid/fusion.py:4-27`; the RRF numeric table is ungrounded, and round 4's "every new citation holds" is false.** Evidence: packet README §0 — "Under the installed Qdrant client, RRF contributions are `1/(2 + zero_based_position)` (`hybrid/fusion.py:4–27`): first in one list → 0.5 → formatted 0.6667; first in both lists → 1.0 → formatted 1.0". On disk: no `fusion*.py` exists anywhere in the repo; no code computes `1/(2 + zero_based_position)`. The production path is Qdrant **server-side** (`qdrant_db.py:1309`, `{"fusion": "rrf"}`); every in-repo RRF helper uses **k=60, 1-based** ranks (`services/rag/hybrid_search.py:32,231-257`; `services/search/search_service.py:897-916`). If the server uses 1-based ranks, the packet's table is numerically wrong (first-in-one-list → 1/3 → formatted 0.6, not 0.5 → 0.6667). This contaminates B1.1(c) and B2.1, which demand exact numeric assertions "rank-derived through the known fusion" — the fusion is not known from the repo. Exact edit — README.md §0, Astra-BLOCKER bullet: replace the sentence "Under the installed Qdrant client, RRF contributions are … not a verification of the deployed server's fusion configuration)." with: _"No RRF implementation exists in this repo for the hot path: fusion is Qdrant server-side (`core/qdrant_db.py:1309`); every in-repo RRF helper uses k=60 with 1-based ranks (`services/rag/hybrid_search.py:32,231-257`, `services/search/search_service.py:897-916`). The rank → formatted-score mapping is therefore NOT derivable from the repo; B1.1's first artifact is a measured probe of the deployed server (rank ↔ returned-score pairs), and B1.1(c)'s exact-value assertions pin to that measurement, never to a formula."_ Same replacement in B1-design.md §1 Ground (the "RRF under the installed client" sentence); in B2-engine.md §4 B2.1, replace "rank-derived through the known fusion + transform" with "rank-derived through the measured fusion + transform".

2. **MEDIUM — claim (e) cites a rule that does not exist at the pin.** Evidence: `docs/architecture/dual-consul/army-map.md:98-99` verbatim — "1. **Nominate the Dux** between Opus and Sol; record it (`pilot/appointment` shape: mission id, Dux, both imperator approvals on the same packet hash)." That is the Dux-appointment record; nothing at :90-110 (or anywhere in the file — single grep hit) states that a sha256-bound ACCEPT constitutes imperator approval of a packet. The evidence file itself (`codex-round-4.md:45`) repeats the extrapolation. The ACCEPT, v4 hashes (`:33-37`) and v5 re-bind (`:39-51`) are real; the authority citation is not. Practical exposure is bounded — Zero's queue item 1 and opening instruction still gate everything — but an approved packet should not rest its approval on a misquoted rule. Exact edit — README.md §Adversarial review, round-2 paragraph: replace "Astra recorded that an explicit ACCEPT bound to the packet's hashes constitutes its approval under `army-map.md:98-99`, requiring Fable's approval of identical bytes and Zero's opening instruction." with: _"Astra recorded an explicit ACCEPT bound to the packet's hashes as its review verdict; `army-map.md:98-99` governs the Dux-appointment record, not packet approval — imperator approval of this packet consists of both hash-bound ACCEPTs plus Zero's approval under queue item 1, and no window opens without Zero's instruction."_ Same fix at round-4's "This reading is Astra's approval of the packet under `army-map.md:98-99`." and in `codex-round-4.md:7,45` on the follow-up PR.

3. **MEDIUM — B2.1(g) is either vacuous or self-suspending; the pass mechanism is never named.** Evidence: `wa_finalize.py:212-224` (verified) — the release predicate returns the stub only when `context_length <= 0 or evidence <= 0.0` or stripped text < `_ABSTAIN_MIN_SENDABLE_CHARS`. The scorer's bands are non-negative (`reasoning_utils.py:620-629`), so a relevant-but-unsupported package reaches the finalizer with evidence > 0 and context_length > 0; under unchanged D8 (`:822-898`) a substantive cautioned answer **is released**. B2.1(g) declares exactly that release "an acceptance FAILURE even when labelled cautioned" — so under unchanged D8, (g) passes only if unsupported packages never reach the finalizer carrying substantive text, i.e. the support signal's consumer suppresses generation upstream (generation gate / package build). The acceptance text never names that consumer; if instead the frozen expected dispositions are set to "released with caution", the criterion has no teeth. Exact edit — B2-engine.md §4 B2.1(g), after "(the predicate at `wa_finalize.py:204` checks context length, evidence and text length — not requested-fact support)": insert _"Mechanism: since the unchanged finalizer releases any cautioned answer with `context_length > 0`, `evidence_score > 0.0` and text ≥ `_ABSTAIN_MIN_SENDABLE_CHARS` (`wa_finalize.py:212-224`, release at `:880/:896-898`), the support signal's consumer for sufficiency MUST sit upstream of the leg call (generation gate), so an unsupported package never carries substantive text into finalization; the delivery report shows, per case, which gate produced the disposition. An expected disposition of 'released with caution' for an unsupported negative is itself an acceptance failure."_

4. **MEDIUM — the support signal's perimeter silently excludes three answer paths, so full B2 acceptance is compatible with the bot still answering unsupported questions.** Evidence: FAQ fast path (`orchestrator_core.py:428-441`) and KG fast path (`:1084-1103`) return before any scorer call (verified — no numeric `score`, early `return`); trusted-tool evidence short-circuits to a flat 0.85 (`_reasoning_evidence.py:40,356-371`, `EVIDENCE_SCORE_TRUSTED_TOOL`) without reading source scores, let alone fact support. B2 §4 requires counterfactual discrimination only on the manifest's provenance fixtures, none of which is required to be a trusted-tool/FAQ/KG cell; B2.1's per-kind bands list "KG entity / trusted-tool bypass: as their documented contracts" — i.e., unchanged. A Dux can satisfy every B2 §4 criterion while a question routed through any of these three paths gets an unsupported answer. B3's fixed battery does not cover them either. Exact edit — B2-engine.md §4 B2.1, after the per-kind band list: insert _"Perimeter decision (mandatory, recorded in the PR body): the trusted-tool 0.85 bypass and the FAQ/KG fast paths are either (i) brought under the support signal with counterfactual cells added to the SUPPLEMENT file, or (ii) explicitly excluded with a written reason and a tripwire test per path. Silence is not an option: an uncovered path that can release unsupported substantive advice must be named as a known residual risk in `pack.yml`."_

5. **LOW — citation-drift batch (substance intact, pins wrong).** All verified on disk:
   - `migration_manager.py:315-327` / "the `migrations_v2` assignment at `:323`": :323 is blank; the assignment is `:311` (`migrations_dir = Path(__file__).parent / "migrations_v2"`); the `-- === ROLLBACK ===` split lives in `migration_base.split_migration_sql` (`:17`, `:67-73`). Edit B2-engine.md §1: "(`db/migration_manager.py:315-327`, the `migrations_v2` assignment at `:323`…)" → "(`db/migration_manager.py:311`, rollback split via `migration_base.split_migration_sql`, `:17,67-73`)".
   - `AUTONOMOUS_OPS.md:82-90` contains no "BLUE Dux" or "fresh gate" (grep over the file: none); it only excludes migrations **without** a green dry-run from autonomous arming. Edit B2-engine.md header row and README "Not on your queue": cite the actual content — "autonomous at L2 with a green dry-run (`AUTONOMOUS_OPS.md:86-90`); applied files preserved (`:163`)".
   - `wa_package_builder.py:594` is the scorer call; the frozen evidence contract is `evidence_inputs` at `:599-611` (freeze comment `:591-592`). Edit B1 §1 and B2 §1 pins.
   - `test_curated_qa_government_fee_gate.py:142-143` is the innocence docstring; the review-marker cases are `TestTheReviewMarker` at `:156-203`. Edit README §0 "Two distinct corpora" bullet and B1 §3.
   - `test_evidence_cross_language.py:45-55` → contexts at `:47-56` (`:45` is `POOR_RETRIEVAL`). Edit README §0 and B1 §1.
   - `wa_finalize.py:822-845` ends before the release; the release is `:880` with the caution-note log at `:896-898`. Edit README D8 and B2 §1 to `:822-898`.
   - The "cosine" penalty is `0 < top < 0.5 and final_score > 0.15` (`reasoning_utils.py:666-668`); the packet omits the `> 0.15` guard. Edit README §0 "(strict)" → "(strict, and only when `final_score > 0.15`)".

6. **LOW — no determinism contract for an LLM-judge support signal.** B1.4 candidates (ii) and (iii) are stochastic judges; the selection contract requires classifying both members of ≥8 pairs, once, with no temperature/seed/repetition requirement, and B2.1(d)/(g) inherit one-shot runs. A candidate can pass by luck and flake in production. Exact edit — B1-design.md §4 B1.4, selection contract: append _"Each LLM-judge candidate is run at its production-intended decoding settings, pinned in the note (model, temperature, seed if available), three repetitions per pair; a candidate with any disagreement across repetitions on a pair is recorded as UNSTABLE for that pair, and an UNSTABLE pair counts as not distinguished."_

**OWN ADDITIONS:**

- **A measured probe of the deployed Qdrant RRF as B1.1's first artifact** — rank ↔ returned-score pairs from the running server, checked in; this replaces the phantom `fusion.py` table and is the only honest basis for B1.1(c)'s exact-value tests.
- **A stated answer to the refuter's closing question.** As written: yes, a Dux can satisfy every B1 §4 criterion while the bot answers unsupported questions (B1 is behaviour-preserving by design — innocence is the acceptance floor), and can satisfy every B2 §4 criterion while unsupported answers persist (i) via the trusted-tool/FAQ/KG bypasses (finding 4) and (ii) on any traffic outside the frozen sets — the packet itself limits "zero errors" to "zero observed errors on those sets". The packet should carry one explicit sentence naming B3 as the only live check and listing the permanently unmeasured paths, so nobody reads merged B2 as "the bot no longer answers unsupported questions".
- **The production-traffic gap pre-B3**: between B2.3b's merge and B3, the abstain record exists but no criterion requires anyone to query it; a one-line cron/ledger row ("`abstained_at` rows inspected at B3 open") would close it.
- **B1.5 environment check**: `scripts/.rag_canary/embedding_baseline.json` does not exist in this worktree (untracked, as the packet says) — the packet should note B1.5 verifies its presence on Pro's main checkout, not assume it.
- **Unverified-by-me, stated as such**: Astra's 0.8/0.8 counterfactual probe, the `wa_outbox` production counts, and the deployed-container greps were not re-run in this reading; the code structure supports all three, but the packet's own "re-measure before trusting" discipline should apply to them at B1/B2 open.
