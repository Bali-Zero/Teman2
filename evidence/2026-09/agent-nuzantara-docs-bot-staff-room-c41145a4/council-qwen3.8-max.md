# Council read of v5 — Qwen 3.8 Max (third strategy voice): DID NOT RUN

Seat: `python3 scripts/tp1_call.py --model qwen3.8-max --task-file … --effort max --timeout 1800` (TP1 gateway), 2026-09-11 ~03:35 WITA. Two attempts, identical command, both `rc=1` within 5 s: `HTTP 403 {"error":{"message":"Access to model denied. Please make sure you are eligible for using the model."}}`. A seat that did not run is not a seat that agreed: Qwen is not counted. The `qwen-cloud-code` CLI seat stays UNARMED pending Zero's ratification (modus SKILL.md, arsenal row); no other model was substituted.
