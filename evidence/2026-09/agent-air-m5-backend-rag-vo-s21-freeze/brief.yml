gear: 3
gear_reason: >-
  Floor measured with scripts/evidence_pack_lint.py --print-floor --print-floor-source on the
  merge-base diff (git diff --name-only / --numstat 52ebe96b62...HEAD): floor 3, source "size"
  (the folded pack and the regenerated walk/persona fixtures). Judgement agrees: a production
  rule pack candidate that Zero signs, plus apps/backend-rag/** where merging is the deploy.
ruling: RULED 2026-09-12 SAETTA
mission: SAETTA-VO3 FRECCIA (BLUE, host M5), window W-VO-S21 vo-s21-freeze — the ONE seq-21 pack Zero signs
objective: >-
  A frozen, green, re-executable seq-21 candidate that makes every one of the nine products with
  no SUPPORT rule in seq-20 (E23U, E23V, E28B, E28C, E28D, E28F, E33A, E33B, E33C) supportable on a
  fact the interview can ask, and replaces the generic OPERATIONAL_NO_PRODUCT_MATCHES_DECLARED_PURPOSES
  on the three dead-end walks (coverage report B2 rows 13-15) with a named cause and the open door.
  Successor of PR #6362, which is closed as superseded. Not signed and not activated by this window.
scope:
  - apps/backend-rag/backend/scripts/visa_engine/fold_pack_seq21.py
  - apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-021.source.json
  - apps/backend-rag/backend/services/visa_engine/{enums,fact_registry,models}.py (fact registration only, as PR #6396)
  - apps/backend-rag/backend/services/visa_engine/contracts/{contract,condition}.schema.json (regenerated)
  - apps/backend-rag/backend/tests/** (dedicated test_seq21_pack.py, count pins, regenerated walk/persona fixtures)
  - apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/engine-adapter.ts (+ test) — reason-code copy EN/ID
  - apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/fact-mapper.ts, apps/mouth/src/lib/api/{applicant-fact-paths.ts,schema.d.ts,schema.d.ts.openapi-sha256} — the ten keys as UNKNOWN(NOT_ASKED), forced by the contract tests
  - apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/fixtures/no-path-doors.replay.json (corpus fingerprint)
  - evidence/2026-09/agent-air-m5-backend-rag-vo-s21-freeze/**
constraints:
  - Forbidden - evaluate_path.py, evaluator.py, models.py beyond fact registration, oracle.css, tree.ts/flow.ts (W-VO-Q), pricing, any signing key, any DB.
  - Every SUPPORT rule depends on a qualifying fact, never on sponsor.type alone; on_unknown NEEDS_INPUT.
  - The nine review.* rules are never flipped to NO_EFFECT (fail-open).
  - No figure invented - the E28 rules turn on a declared boolean and cite the source record.
  - No client PII; no credential value anywhere; never sign or activate; never edit PENDING-ARMS.md.
  - Push once, complete.
acceptance:
  - A1 fold reproducibility - the fold CLI re-produces the committed pack at the JCS level signed by sign_pack.py; chain previous_payload_sha256 = seq-20 digest.
  - A2 guilt - stripping the qualifying conjunct hands products out on no evidence (census moves); sponsor.type NONE alone never gains E33B.
  - A3 every one of the nine products SUPPORTED on a gold fixture carrying its qualifying fact; the qualifying fact unknown gives NEEDS_INPUT, never SUPPORT.
  - A4 the three dead-end walks carry the named causes; no walk regresses to NEEDS_INPUT on intent.requested_product_code; census states unchanged.
  - A5 mouth - every reason code the highest signed and highest source pack can emit has EN/ID copy; vitest + tsc green.
  - A6 seq-21 is activatable over seq-20 by the activation writer (migration 253 legal-period coverage, sequence, chain).
  - A7 FACTS-FOR-THE-TREE.md lists fact -> question EN/ID -> product -> rule for W-VO-Q.
  - A8 dossier rev.4 (Italian, <= 60 lines) with the one signing command and the ceremony, no credential.
consumer_map:
  - Zero's offline signing ceremony (sign_pack.py on rulepack-prod-021.source.json) and the activation ceremony that follows it.
  - W-VO-Q, which reads FACTS-FOR-THE-TREE.md to add the ten interview questions.
  - The required checks that run test_seq21_pack.py, the contract pins and engine-adapter.test.ts on every future visa-engine PR.
appetite:
  budget: 8 hours, 1 PR
  stop_loss: three reds on the same cause suspends; a fix-of-a-fix stops at depth 1
