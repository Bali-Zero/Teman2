VERDICT: BLOCK
FINDINGS
1. BLOCKER, retrieval_harness.py:205; search_service.py:25; config.py:1392 — importing the harness constructs global settings before the plan-file checks at retrieval_harness.py:941-972, violating F3. Minimal cure: remove the SearchService import from module scope; obtain named-vector metadata from a side-effect-free module and import settings/CollectionManager only after plan verification.
2. BLOCKER, test_b2_2_retrieval_harness.py:52,481-502,571-601 — the subprocess installs only socket guards before importing the harness. Provider/send/cache/reranker guards are installed later in an already-imported pytest process, so F6 is not proved. Minimal cure: install every real-entrypoint guard inside the fresh subprocess before importing retrieval_harness, then run both safe and guilt cases there.
3. BLOCKER, retrieval_harness.py:447-451,874-880; search_service.py:201-206,256-337; query_router_integration.py:85-180 — `--collection` bypasses routing, and the no-override path invokes bare `QueryRouter`, while production SearchService uses `QueryRouterIntegration` and optionally SurfaceRouter. Pricing and multi-domain routing can therefore differ, making the sample unfit for threshold relief. Minimal cure: call the actual production routing entrypoint with production options and remove the live CLI collection override.
4. BLOCKER, retrieval_harness.py:574-580; test_b2_2_retrieval_harness.py:345-355 — `RetrievalError` interpolates `str(exc)`, and the test requires that message. Transport exceptions can contain credentials, headers, URLs, or retrieved content. Minimal cure: record only a fixed message, exception type, and strictly normalized numeric status code.
5. BLOCKER, measure_residual_cosines.py:576-629 — execution ownership is a non-atomic read/check/write sequence. Concurrent processes can both observe an unstarted record, both mark it started, and both spend the batch. Minimal cure: claim execution atomically with an exclusive lock or create-if-absent execution marker held through completion.
6. BLOCKER, measure_residual_cosines.py:593-615,638-645; test_b2_2_measure_residual_cosines.py:302-314 — execution validates only planned count and text hashes, then trusts mutable `record["hard_stop"]`. The supplied test proves a recorded five-attempt batch can execute only three attempts, contradicting “exactly the provider attempts recorded.” Minimal cure: validate the complete canonical pre-call record and require `hard_stop == recomputed planned_provider_attempts`.
7. BLOCKER, measure_residual_cosines.py:493-507,512-542,651-679 — a provider attempt can be spent without a persisted completion. A malformed returned vector can raise `TypeError` during cosine calculation, or fail JSON serialization during vector hashing, before the completion write. Minimal cure: validate each provider result inside the attempt boundary and make the outermost completion writer use only pre-sanitized, serialization-safe state.
8. MAJOR, measure_residual_cosines.py:498-508,623-627,674-679 — `ack_envelope` is arbitrary CLI text persisted verbatim, and `status_code` accepts arbitrary objects through `str(...)`; either can carry credentials or manifest text. Minimal cure: enforce a narrow identifier grammar for acknowledgements and accept only bounded integer status codes.
9. MAJOR, test_b2_2_retrieval_harness.py:590-591; semantic_cache.py:98-133; orchestrator_core.py:490-504 — `SemanticCache.cache_result` exists, but no production write call site was found; the production hop inspected only reads through `get_cached_result`. Thus this patch is not evidence that the actual production semantic-cache write path is guarded. Minimal cure: identify and guard the real writer call site, or explicitly prove production has no such write path.

NON-FINDINGS
retrieval_harness.py:343-391 — artifact bytes are pinned before parsing, and all requested vectors resolve before any retrieval client is touched.
retrieval_harness.py:556-605,624-654,805-845 — absent client, search exception, and zero IDs stop before scoring; `run_and_write` writes only after the full batch succeeds.
retrieval_harness.py:948-982 — plan-file byte hash and recomputed-plan equality are checked before constructing CollectionManager; the defect is the earlier settings construction through module imports.
retrieval_harness.py:317-340,485-505 — successful result serialization excludes chunk text and emits only collection-scoped IDs or score-derived hashes.
retrieval_harness.py:220-238 — the dense/primary-only scope and `support=None` upper-bound warning are explicitly recorded, including “a false acceptance seen is real; a pass does not prove support.”
retrieval_harness.py:450-480; query_router.py:625-649,737-797 — within the bare QueryRouter implementation, fresh instances and post-decision routing-stat updates prevent current per-instance state from drifting plan versus execution.
test_b2_2_retrieval_harness.py:624-703 — the guilt scenarios do turn embedding, Codex generation, cache-write, and send attempts red and prevent report creation.
wa_outbox_worker.py:1285-1295; search_service.py:979-1052; _support_signal.py:166-195 — the WhatsAppService, Ze-Rank/CrossEncoder reranker, and CodexExecClient dotted guards correspond to observed production calls.
measure_residual_cosines.py:493-507 — provider calls are counted before invocation, and ordinary provider exceptions retain only type plus status-code fields.
build_query_vectors.py:377-393 — the live CLI factory explicitly constructs AsyncOpenAI with `max_retries=0`.
measure_residual_cosines.py:784-813,609-620 — CLI paths for artifact/manifests and the B1.5 ledger are fixed, and write/execute paths require a non-empty B1.5 ledger.
