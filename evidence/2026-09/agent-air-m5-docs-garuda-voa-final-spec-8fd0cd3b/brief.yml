gear: 2
task_id: docs-garuda-voa-final-spec
l_level: L1
gate_class: opus
objective: >-
  Land the GARUDA VOA final spec (G0 + eight windows, reconciled with Astra) and commit
  Astra's option-D proof pack verbatim so Zero's operator steps and the reproduction driver
  live on main. Docs and evidence only; no runtime, migration, router or service change.
  The floor is 2 via the SIZE term alone (963 added lines, of which ~830 are the verbatim
  pack); the blast radius a reviewer reads is one spec, one pointer bullet and twelve
  evidence files that execute nowhere in CI or production.
scope:
  - docs/plans/2026-09-11-garuda-voa-final-spec.md
  - docs/plans/2026-09-03-relaunch-lanes/LANE-A-garuda-voa.md
  - evidence/2026-09/agent-air-m5-ops-garuda-voa-0911-c652d82b/
constraints:
  - >-
    The pack is committed VERBATIM (byte-identical to Astra's worktree copy); prettier was run
    and reported the .md/.json files unchanged.
  - >-
    No credential in the diff: the pack's DSNs are a local trust-authenticated disposable
    cluster and the placeholder '<new>'; a regex grep for postgres URLs with passwords,
    "password=", "secret_key" and private keys returned no match.
  - No code path in CI or on Fly imports anything under evidence/.
acceptance:
  - text: >-
      WHEN origin/main carries this merge, THEN `grep -c '^| \*\*W' docs/plans/2026-09-11-garuda-voa-final-spec.md`
      SHALL return 8 and `evidence/2026-09/agent-air-m5-ops-garuda-voa-0911-c652d82b/OPERATOR-D.md` SHALL exist.
    status: pending
    probe: "git show origin/main:docs/plans/2026-09-11-garuda-voa-final-spec.md | grep -c '^| \\*\\*W'"
  - text: >-
      WHEN the evidence directory is diffed against Astra's worktree copy, THEN `diff -r` SHALL be empty.
    status: verified
    probe: "diff -r /Users/balizero/nuzantara/.worktrees/ops-garuda-voa-0911/evidence/2026-09/agent-air-m5-ops-garuda-voa-0911-c652d82b evidence/2026-09/agent-air-m5-ops-garuda-voa-0911-c652d82b"
risks:
  - >-
    None at runtime. The only risk is documentary: a window launched from a stale spec.
    The spec names its measurement date and the two follow-up facts (G0 executed the same
    evening) land in a sibling docs PR.
pii: none
