I have reviewed both diffs against the six priority areas, tracing every branch of the verdict rule, both adapters, the retrieval guarantee's tie-break and budget, the marker's verification chain, the moratorium scope check's regexes, and the judge union logic.

**1. Verdict rule & precedence (KBLIVerdict.swift):** The `headline` ordering (nationalClosed → baliBlocked → nationalUndetermined → baliUndetermined → openInBali) correctly implements the frozen contract. The 0%-cap check sits above the status dispatch, so a missing/unknown `pma_status` with cap 0 still yields `.closed`. The `baliVerdict` checks `baliBlocked` before `NON_CLASSIFICABILE`, so the 17 <REDACTED> records stay `.blocked`. The 68 TERTUTUP records are all within the 518 `blocked == true` set (the five-cause breakdown sums to 518), so they hit the `baliBlocked` branch. No path reports OPEN or UNDETERMINED where the contract demands CLOSED, or vice versa.

**2. Adapter agreement:** Both adapters reject a scalar `skala_usaha` (typed decoder: `(try? decode([String].self)) ?? []`; raw adapter: `(row["skala_usaha"] as? [String]) ?? []`). Both parse `pma_max_asing` identically (Int direct, or String→Int, else nil). The dataset-wide test asserts zero mismatches across all 1,559 records.

**3. Retrieval guarantee (<REDACTED>):** The budget guard `hits.total <= maxSearchHits` and `rows.prefix(maxSearchHits)` cap the guarantee at 5 codes. Only one term qualifies. The tuple comparison `(weight, -total, term.count, otherTerm)` is a strict total order (weight desc, rarity asc, length desc, lexicographic asc), so the tie-break is deterministic. The promotion logic (`promoted + missing + rest`) cannot exceed the original ranked list's length plus genuinely missing codes, and the caller still clips to `maxSearchHits`. Q13 and Q23 produce empty guarantees (verified by test), so their packages are untouched.

**4. Marker (KBLIBrain.swift, Tools/kbli-bkpm-provision):** The app holds only the Ed25519 public key; it cannot sign. Every guard is sequential and fail-closed (no file → false, bad JSON → false, wrong scope → false, wrong machine → false, bad sig → false). The canonical payload is built with `.sortedKeys` in both signer and verifier, so byte-for-byte agreement is guaranteed. A tampered `machine_id`, `issued_at`, or `scope` invalidates the signature. No check is shadowed by an earlier return.

**5. moratorium_scope_check (score_p2b.py):** Reduced to four token-level criteria (source letter, date, both risk classes, no total quoted) and can only FAIL. The MEDIUM_LOW spans are stripped before the LOW_ONLY test, so "menengah rendah" does not satisfy the low-class criterion. The totals regex requires `\b(518|48)\b` within 40 chars of a code-word, preventing false hits on unrelated numbers. Permanence and universality are explicitly deferred to the judge via `class_rule_expectations`. A correct answer satisfying all four token criteria passes the decidable gate and reaches the judge; a wrong answer that also satisfies them is "unjudged" (not "correct") absent a judge verdict.

**6. Judge union (score_p2b.py):** The union adds served `package_codes` to the ground-truth records so the judge can verify claims the model actually saw. Each answer entry carries its own `package_codes`, and the rubric explicitly instructs the judge to check each run only against its own package. The union cannot let a fabrication pass because the per-run scoping instruction is present and the records are provided precisely so the judge has evidence to check *against*, not to credit blindly.

No file, line, and triggering input constituting a correctness defect was found.

VERDICT: OK
