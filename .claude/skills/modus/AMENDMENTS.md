# modus — AMENDMENTS (the loop's own scar file)

> Append-only. One line per **loop misfire**: wrong gear picked, council convened for a rubber
> stamp, a probe skipped that later bit, a stage that proved dead weight, a routing choice that
> wasted quota. This is how the loop learns about ITSELF — separate from cicatrix (which records
> organism traumas). Consumed by `infra/workflows/modus-bench.js` and by the operator.
> **modus never merges changes to itself** — proposals here become a PR only after Zero's GO (Legge 5).
>
> Format: `YYYY-MM-DD | what misfired | evidence | proposed change`

2026-07-02 | genesis run: refuter seat brittle | DeepSeek probed live → 402 Insufficient Balance (2nd time, same as 2026-06-30); GLM fallback invocation denied by permission layer → council ran 2-external-seats (Codex red-team + Gemini costruttivo) | codified in SKILL.md: refuter seat = probe-then-cascade DeepSeek→GLM→Codex; a 2-seat heterogeneous council is acceptable-degraded and MUST be declared in the report
2026-07-02 | VERIFY probe leaked a real outbound side-effect | heartbeat-organs TAC: fly-detector sandbox probe isolated HOME+PATH but inherited shell env → TELEGRAM_BOT_TOKEN present → 1 real false alert sent to admin chat mid-flight (corrected on-channel within 2 min) | probes that can trigger outbound effects (telegram/email/webhook) run under `env -i` or with alert credentials explicitly blanked; add to VERIFY stage notes
2026-07-02 | Agent-tool fan-out dead in headless flight session | all 6 parallel Agent dispatches failed with "Could not determine current tmux pane/window"; diagnosis executed inline (worked, but serialized) | modus Arsenal note: in headless/cron sessions probe the Agent tool with 1 cheap dispatch before planning fan-out; fallback = inline batching (anti-sperpero: 1 agent + more budget)

2026-07-02 | bench run 1: Workflow args arrived as a JSON-encoded STRING → `args.today` undefined → whole run silently stamped "UNKNOWN-DATE" | task wr1h8s4qb result `date:"UNKNOWN-DATE"` while the invocation passed `{today:"2026-07-02"}` | modus-bench.js now parses string args defensively and FAILS LOUD on missing/malformed `today` (fixed in this PR; proven by resume run replaying 27/28 agents from cache)

2026-07-02 | bench run 1: a refuter APPROVED a half-false proposal — claimed it verified "peak-valley pricing doubles rates mid-July" at api-docs.deepseek.com, but neither the updates nor the pricing page contains any time-of-day pricing (re-fetched live by the final gate) | survivor #11 of wf_b0ad36b1-80d; final-gate re-fetch 2026-07-02 of api-docs.deepseek.com/updates + /quick_start/pricing | no bench change needed — this is the designed catch: the operator-side final gate re-verifies every survivor (W65: even the refuter hallucinates); the alias-retirement half of #11 survived, the pricing half was trimmed

2026-07-02 | bench run 1, ALIGN-FLEET stage collides with the L1 worktree-isolation hook: stage 7 prescribes `git pull --ff-only` on MAIN checkouts, but an agent session cannot execute it even with the documented `AGENT_WORKTREE_ENFORCEMENT=false` escape (`pull` hard-blocked, `status` passes) | 2 blocked attempts on Mini main this session — alive-AI=0, tree clean, PR #1919 already on origin/main; debt routed to PENDING-ALIGN:Mini | proposed change (Zero decides): either the hook allows ff-only pull in main when alive-AI=0 ∧ tree clean, or SKILL.md ALIGN-FLEET declares agent-session main-pulls impossible-by-design and routes them to PENDING-ALIGN as the DEFAULT, reserving actual pulls to interactive/operator sessions

## bench 2026-07-02

> First live run (wf_b0ad36b1-80d, 28 agents, 23 raw → 12 survived refutation → 11.5 survived the
> final gate). Every evidence below re-verified on disk/URL by the orchestrator (Fable) this day;
> #11 trimmed to its verified half. **NOTHING below merges without Zero's explicit GO** — the bench
> proposes, the operator disposes. Apply only the checked lines, one atomic commit per amendment.

- [x] W86 not prevented — SHIP+ARM arms `--auto` with no derived-contract probe; the stage as written reproduces the exact W86 trap — SKILL.md:68 mandates immediate `gh pr merge --auto --squash` but zero docs_sync/docs-sync mentions exist in SKILL.md; W86 (cicatrix-scars.md:297-307, PR #1670→#1672) proved a separate docs-sync commit is orphaned at first-green merge, leaving `AI_ONBOARDING.md` stale and `check-docs-sync` blocking the next innocent PR — CHANGE: in SKILL.md line 68 (SHIP+ARM row), after "(exceptions → operator merges: guardrails, config-critical, migrations, force-push)." insert: "**Derived contracts travel IN the commit you arm on (W86):** if the diff moves a DOCSYNC-counted surface (backend tests/routers/services), run `python scripts/docs_sync.py` and fold the regen into the SAME commit BEFORE arming `--auto` — with `--auto` there is no 'later': merge fires at first green and any post-arm commit is orphaned. Bump already missing on a pushed PR → open a 1-line repair PR (like #1672), never re-push racing the merge." Scars-killed column: `W81, #7` → `W81, W86, #7`.
- [x] W89 (and W70) not prevented — no stage has a "sibling sweep" probe: a pattern-fix applied to 1-of-N call-sites is invisible to the loop — W89 (cicatrix-scars.md:335-345): #1825 cured `base_worker.redis_cmd` but left `stream_tools` unauthed → NOAUTH swallowed, OSINT blind 69.5h printing "56 harvested"; W70 same malattia (no sweep ever grepped `Projects/nuzantara`); SKILL.md BUILD "Diff scoped to the mandate" pushes AGAINST widening and no stage says "grep the siblings" — CHANGE: in SKILL.md line 67 (VERIFY row) driver cell, after "Risk-proportional depth." append: "**Pattern-fix = class-audit (W89/W70):** when the change cures one instance of a repeated pattern (auth call, hardcoded path, secret handling, env sourcing), grep ALL sibling call-sites THIS turn — fix the in-perimeter ones, write one PENDING-ARMS line per sibling left out; a cure applied to 1-of-N paths is a time bomb (#1825 cured `base_worker`, left `stream_tools` unauthed → 3 days blind)." Add `W89` to VERIFY's Scars-killed column.
- [x] W89 second face not prevented — PROVE-LIVE's "READ THE OUTPUT/log, not the exit code" is too weak: the producer's own log lied while zero was written; the probe must be the downstream state-delta — W89: sentinel printed "[HARVEST] Total: 56 items" every cycle while `garuda:raw` stayed frozen (len 4603, 69.5h) because the loop ignored `stream_publish`'s return; proof-of-fix was the consumer-side delta 4603→4659 (+56); W81's state-delta/Futility-Run (cicatrix-scars.md:161-173) already named this class and modus stops one proxy short — CHANGE: in SKILL.md line 69 (PROVE-LIVE row) probe column, after "`✔ Connected` = TCP handshake, not auth — run `SELECT 1`." append: "A producer's own log/counter is STILL a proxy (W89: '56 harvested' printed every cycle while the stream stayed frozen 69.5h) — prove by the DOWNSTREAM state-delta: stream length / row count / artifact content, before vs after." Scars-killed column → "#2, 503-RAG, W87, W89".
- [x] The codified refuter cascade (DeepSeek→GLM→Codex) has a known-dead middle tier with no PENDING-ARMS routing — arsenal-seat failures discovered mid-run produce a narrative, not an arm — AMENDMENTS.md:11 records GLM permission-denial but PENDING-ARMS has no GLM line (DeepSeek's 402 got one); `claude-glm` is a zsh function pulling Keychain token with no glm allowlist entry in either settings.json → next council hits the identical denial; SHIP+ARM reconciliation only enumerates artifacts the task BUILT — CHANGE: in THE ARSENAL, Refuter row (SKILL.md line 111), after "A 2-seat heterogeneous council is acceptable-degraded — DECLARED, never silent." append: "A seat that fails its live probe is itself an un-armed artifact → write a PENDING-ARMS line for the SEAT (artifact = the seat; missing arming step = the root cause — balance, permission allowlist, Keychain access; proof-of-armed = the seat's own 1-token probe passing in a live session). An AMENDMENTS narrative alone leaves the tier dead and untracked." (Ledger hygiene applied this run: GLM line added to PENDING-ARMS.)
- [x] The "durable receptor" spec permits receptors whose healthy state is indistinguishable from their dead state — the escalations-receptor arm is unclosable by its own proof on a clean board (W84 class, which SKILL.md claims killed) — verified twice (refuter + final gate): manual run of `scripts/hooks/escalations_alert_sessionstart.sh` on an empty board → exit 0, ZERO output (fail-open by design), so PENDING-ARMS line 11's proof-of-armed is unsatisfiable AND a dead receptor is byte-identical to a healthy one; SKILL.md:80-83 imposes no liveness-distinguishability requirement — CHANGE: in STATE & RE-ENTRY, extend the durable-receptor bullet (after "...NAME which receptor carries it.", line 83): "A receptor whose HEALTHY output is silence must expose a self-probe that distinguishes healthy-silent from dead (a --selftest mode or synthetic-item injection), and a proof-of-armed may NEVER be 'silence observed' — a fail-open snapshot receptor is W84 green-but-dead by construction otherwise."
- [x] Idle background shells are now auto-reaped under memory pressure (v2.1.193) — durable-receptor mechanism (3) "harness background task" is no longer guaranteed-durable — GitHub release v2.1.193 (2026-06-25) verbatim: "automatic memory-pressure reaping for idle background shell commands (disable with CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1)"; mechanism live in installed CLI 2.1.198 on this 24GB Mini where memory pressure is the norm — CHANGE: in §STATE & RE-ENTRY, durable-receptor bullet, after "(3) a harness background task — completion re-invokes the loop." insert: "CAVEAT (v2.1.193): the harness auto-reaps IDLE background shells under memory pressure — receptor (3) is no longer guaranteed-durable on RAM-tight nodes (Mini 24GB). For a load-bearing monitor set `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1`, or carry the proof on receptor (1) PENDING-ARMS instead." (Verifier note: Monitor-kind tasks are exempt from reaping — optional third mitigation.)
- [x] Nested subagents (v2.1.172, chains capped at 5 levels) — Gear 3 anatomy decomposition can delegate per-organ coordinator subagents; budget shape must count descendants — Week 24 digest verbatim: "Subagents can now spawn their own subagents... capped at five levels deep"; SKILL.md anti-sperpero rules (lines 51, 55) assume flat fan-out — the declared "~N agents" is now a tree and width is uncapped — CHANGE: in §STAGE 0 anti-sperpero rules, after "Fan-out only when items ≥3 and independent. Fan-out for READS, funnel-in for WRITES." append: "Subagents can spawn their own subagents (v2.1.172, chains capped at 5 levels): at Gear 3 one coordinator subagent per anatomical organ may fan out its own readers, keeping the orchestrator context clean — but the declared budget shape counts DESCENDANTS, not just direct children (`/agents` shows the tree)."
- [x] /cd relocates the session without rebuilding the prompt cache (v2.1.169) — preemption no longer costs a fresh session — Week 24 digest verbatim: "/cd moves the current session to a different working directory without rebuilding the prompt cache: the new directory's CLAUDE.md is appended as a message"; SKILL.md Preemption bullet (lines 87-88) previously implied a separate session/window — CHANGE: in §STATE & RE-ENTRY, Preemption bullet, after "return via the recap block." append: "`/cd` (v2.1.169) can relocate THIS session into the hotfix worktree and back without rebuilding the prompt cache (the new dir's CLAUDE.md is appended, not substituted) — preemption no longer forces a fresh session or a cache rebuild."
- [x] NO-CHANGE (verified non-event): the June 15 Agent SDK/headless billing split was PAUSED — the Arsenal's quota-economics premises stand; do not amend based on the announcement — Anthropic pulled the split the day it was to take effect ("Nothing changes for now", devops.com + thenewstack.io + digitalapplied.com corroborated); had it shipped it would have gutted cron `claude -p` on MAX quota and the CLI-subprocess sanctioned path — CHANGE: no SKILL.md edit; RE-VERIFY at next bench run (Anthropic framing was "nothing changes for now", not a cancellation).
- [x] Refuter seat: invert cascade to GLM 5.2 → DeepSeek V4 Pro → Codex (GLM-5.2 released in-window, benchmarks above V4 Pro, already installed; DeepSeek seat 402-dead at both councils) — PENDING-ARMS line 10: DeepSeek 402 Insufficient Balance, re-probed live 3/3 dead; GLM-5.2 (released 2026-06-13, MIT, 1M ctx) scores 51 vs V4 Pro 44 on Artificial Analysis Intelligence Index v4.1 and runs on the flat Coding Plan via `claude-glm` — CHANGE: SKILL.md line 111 Who cell → "**GLM 5.2 → DeepSeek V4 Pro → Codex** (probe-then-cascade)"; Invocation cell → "`claude-glm` (Keychain; probe first — locked-keychain contexts fail retrieval); DeepSeek key in `~/.openclaw/workspace/.env.master`"; append to When cell the 2026-07-02 re-route rationale (GLM 51 vs 44, flat-subscription, DeepSeek 402-dead at both councils; GLM-as-refuter still requires GLM ≠ implementer — generator≠grader); line 118 "refuter = DeepSeek/GLM" → "refuter = GLM/DeepSeek". NOTE: the GLM seat itself is un-armed today (permission denial, see PENDING-ARMS) — arming it is a precondition of this re-route.
- [x] Refuter seat annotation: DeepSeek legacy aliases retire 2026-07-24 (the silent-downgrade trap becomes a hard-fail) — official api-docs.deepseek.com/updates verbatim: "The two legacy API model names, deepseek-chat and deepseek-reasoner, will be discontinued in three months (2026-07-24)"; one live legacy usage remains at `scripts/deepseek_vs_gemini_blite.py:35` (`DEEPSEEK_MODEL = "deepseek-chat"`, verified on disk) — CHANGE: append to Refuter row When cell (SKILL.md line 111): "DeepSeek: explicit `deepseek-v4-pro`/`deepseek-v4-flash` ONLY — legacy aliases `deepseek-chat`/`deepseek-reasoner` RETIRE 2026-07-24 (the silent-downgrade trap becomes a hard-fail)." Apply-time: ship with the 1-line blite.py alias fix. [FINAL-GATE TRIM: the refuter also approved a "peak-valley pricing doubles rates during WITA hours from mid-July" leg — NOT re-verifiable on api-docs.deepseek.com updates or pricing pages (fetched live 2026-07-02) → refuted-by-default, dropped.]
- [x] Final-gate row: gate-death can be regulatory (weeks), not just quota (hours) — Fable 5 was export-control-disabled for 19 days in June; add operator-escalation threshold to the SUSPEND rule — corroborated by CNBC/NBC/CoinDesk/Forbes: Fable 5 + Mythos 5 suspended for all users 2026-06-12 under US export controls, lifted 06-30, restored 07-01; SKILL.md:106 and :89-91 only cover the hours-scale quota window — an 18-day outage would silently freeze every Gear-2/3 task on a wakeup receptor that can never fire — CHANGE: append to SKILL.md line 106 (Architect/final-gate row) When cell: "Gate-death can be regulatory, not just quota: Fable 5 was export-control-disabled 2026-06-12→30 (back 07-01). Quota-death (hours) → SUSPEND per the standing rule; availability-death >24h → still no substitution, but escalate to the operator as §Solo-operatore (the organism must not freeze silently for weeks)." Mirror the one-line qualifier at the Quota-exhaustion bullet (lines 89-91).

> **APPLIED 2026-07-03 with Zero's explicit GO** ("consiglia e esegui") — 11/12 amendments landed, one atomic commit each (A1-A8, A10-A13). #9 is NO-CHANGE by design (re-verify at next bench). A10 deviation from proposal text: the GLM-seat precondition was armed the same day (allowlist + PONG) and DeepSeek was topped-up (HTTP 200), so the inversion note records both seats live.

2026-07-02 | GROUND slip nel BUILD proprioception: 2 path repo del registry (agent_worktree_cleanup/verify_connectome wrappers) scritti a memoria invece che verificati su disco — il PRIMO run dell'organo li ha smascherati ("NO REPO COUNTERPART") | costo: 1 iterazione fix+rerun; beffa: l'organo anti-confini ha debuttato correggendo il suo costruttore | proposed change: nessuna nuova regola — GROUND già lo impone ("every LOAD-BEARING file:line re-executed THIS turn"); il registry di un probe È load-bearing. Reminder, non lacuna.
2026-07-05 | Gear-3 background read-subagents (A6/A13) delivered AFTER the orchestrator had re-done both lanes first-hand — a13 even marked its task completed with the report still in the mailbox; spalla-review went idle twice with no report at all. Double work + a task-board that lied briefly. | TAC-2 session: a6/a13 spawned ~17:15, reports landed ~19:20 via teammate-message; both lanes already shipped in PR #1969 from first-hand evidence; the late reports still added value (pinned call-site, second registry) but only as post-hoc leads | proposed: for load-bearing Gear-3 read-lanes either (a) run the subagent synchronously (run_in_background:false) when its output gates a cure, or (b) poll/SendMessage at the stage boundary BEFORE starting the lane yourself; treat a subagent-completed task status as UNVERIFIED until its report is read (it can close the task before the mailbox flushes)

- 2026-07-05 | VERIFY(red-team) landed AFTER SHIP+ARM — auto-merge was armed at PR-open while the Codex verdict was still in flight; #1963 merged carrying a fixable regression (/\_next/ newly robots-blocked for Googlebot) live ~1h; the continuation fix on the pre-squash branch then went permanently CONFLICTING (#1968 closed unmerged, W88) and had to be rebuilt as #1974 | evidence: #1963 mergedAt ~09:52Z vs red-team task completion ~10:15Z; #1968 mergeable=CONFLICTING post-squash | proposed change: when a red-team seat is dispatched on a PR's diff, do NOT arm `--auto` until the verdict is verified (or dispatch red-team BEFORE opening the PR); post-merge continuation fixes go on a FRESH branch from main, never on the pre-squash branch

- 2026-07-05 | VERIFY pattern-fix class-audit swept prod CODE but not TESTS asserting on source — abstain-SSOT migration removed the bare EvidenceScoreConstants.ABSTAIN_THRESHOLD read from reasoning.py, and CI round-3 died on test_fluidity_and_strength_simple.py:274 asserting the OLD wiring string-in-source (a test file outside the suites run locally) | evidence: PR #1964 CI rounds — Backend Tests failed at 4% with -x on exactly that assert; local subtree runs (services/rag + unit/services/rag) never touched integration/zantara | proposed change: the W89 class-audit grep MUST include backend/tests (and any source-level assert) for every symbol/pattern the diff removes or migrates — 'grep tests for the old symbol' costs one command and buys a CI round

- 2026-07-06 | provider-clock blindness: the loop treats flat-sub seats as always-equal, but z.ai deducts Coding-Plan quota 3x during China peak (14:00-18:00 CST ≈ WITA afternoon — today's "529 wall" was this, not overload) and DeepSeek starts 2x surge pricing mid-July in windows that ARE our overnight cron slots (22:00-01:00 + 03:00-07:00 WITA) | evidence: research/operations/2026-07-06-glm52-zai-deep-research.md + 2026-07-06-deepseek-spark-deep-research.md | proposed change: Arsenal table gains a "provider clock" column; big GLM jobs scheduled off China-peak; DeepSeek-consuming cron shifted out of surge windows before 2026-07-15; operator approves via SKILL.md PR
- 2026-07-11 | The final gate is not exempt — two orchestrator self-misses in one night, same class it bounces lanes for: (1) first hook-fix commit set OLLAMA_BASE_URL, the module-global's NAME, not the pydantic env (settings.ollama_url ⇒ OLLAMA_URL, config.py:127) — a silent no-op corrected only after empirical probe (784f876784 → b9b2c3f808); (2) `git push … | tail` masked the push's real exit code — the task reported completed/0 while the output said "push blocked" (scar #2 shape, relived by the gatekeeper) | evidence: session f901c64e ledger-staging §Orchestrator self-gate + §TRAIN replan; probe transcripts (OLLAMA_BASE_URL=dead-port → URL unchanged; OLLAMA_URL=dead-port → overrides) | proposed change: no new rule — VERIFY already commands probe-over-reasoning and read-output-not-exit-code; reminder that BOTH bind the orchestrator/final-gate too (like the 2026-07-02 GROUND slip: reminder, not gap). Mechanical aid adopted: never pipe a gated `git push`; redirect to a file so the task exit code stays authoritative
- 2026-07-12 | GROUND gap in the orchestrator: dispatched a full cure-lane (tg-cohort6) for a red CI check already fixed+merged 6h earlier (#2280) — the session's own memory index even cited it ("gateway #2280") | evidence: lane returned no-op after full worktree setup (this session's fleet-cleanup arc) | proposed change: before dispatching any cure-lane for a failing check, grep the last 24h of merged PRs (`gh pr list --state merged --search "<check-name>"`) — 10 seconds that saves a lane
