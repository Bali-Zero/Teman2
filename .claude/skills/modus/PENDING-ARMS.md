# modus — PENDING-ARMS (the W81 ledger: built ≠ armed)

> Anything created-but-not-{merged, installed, propagated, armed} lands here at SHIP+ARM and is
> re-read at TRIAGE of every subsequent modus run. Remove a line ONLY when the arming is PROVEN
> live (a probe of the work, never an exit code). Distinguish legitimate firebreaks (operator
> gate, Legge 5, business decision) from tech debt — say which it is.
>
> Format: `opened YYYY-MM-DD | artifact | missing arming step | owner (me|operator) | proof-of-armed`

- opened 2026-07-02 | DeepSeek V4 Pro refuter tier | balance top-up — probed live this day: HTTP 402 Insufficient Balance (2nd consecutive council with the seat dead) | operator | a 1-token chat/completions call returns 200
- opened 2026-07-02 | escalations receptor (`scripts/hooks/escalations_alert_sessionstart.sh`, PR #1852) | hooks entry ships with the modus PR in repo `.claude/settings.json`; liveness provable only at next session boot per machine | me | a nuzantara session boot shows the "🚨 ESCALATIONS BOARD" block (or a manual run emits valid hook JSON with current board state)
- opened 2026-07-03 | organism.supervisor heartbeat — fresh NOWHERE in the fleet (M5 relic 2026-06-05, Pro/Mini have no file at all); flagged by proprioception organs_heartbeat on M5 | organ-registry decision: retired (→ remove M5 relic + registry entry) or re-arm on Pro/Mini (→ plist + heartbeat lib call) | me (diagnose) + operator (decision) | either the relic is gone and the probe stops flagging, or a fresh `~/.organism/last_seen/organism.supervisor.json` beats on its assigned node
- opened 2026-07-03 | post-#1945 HOME wrapper refresh — `~/scripts/regulatory-watcher-run.sh` on Pro AND M5 must be re-copied from the MERGED canon (heartbeat+sonnet-5 fused); Pro was refreshed pre-merge (sonnet-5 only, no heartbeat), M5 still has the heartbeat-only fork | me (this session, post-merge) | `cmp -s ~/scripts/regulatory-watcher-run.sh <repo>/infra/launchagents/wrappers/regulatory-watcher-run.sh` passes on Pro and M5
- opened 2026-07-02 | GLM 5.2 refuter tier (`claude-glm`) | permission/allowlist arming — genesis council invocation denied by permission layer; no `glm` entry in repo `.claude/settings.json` nor `~/.claude/settings.json` (verified 2026-07-02 bench run 1); function + `~/.claude-glm` dir exist but the seat is un-armed | operator | a live `claude-glm` 1-token probe returns a completion in-session
- opened 2026-07-02 | 9 dead/unhealthy heartbeat organs (doctrine §8) — DIAGNOSED, 5 cure-PRs armed (#1916/#1918/#1921/#1923/#1924); TAC research/operations/2026-07-03-heartbeat-organs-tac.md | remaining arming: (a) cp bridge+fly-detector repo→~/scripts + rm 2 retired sidecars — DONE+proven 2026-07-02 (bridge run live, sidecars not recreated; fly-detector cmp=origin/main), (b) Pro pull + kickstart federation & pg-bridge daemons, (c) REDIS*PASSWORD verified in secrets env, (d) cell.observatory env+plist bootstrap, (e) canva watchdog retire-vs-fix decision, (f) Keychain nuzantara_readonly on Pro (pg.sh W87 recurrence), (g) P1: 4 eventbus daemons (~/scripts/eventbus/) crash-looping AuthenticationError since 06-30 — CURED this PR (reverse-promotion HOME→infra/eventbus + `password=REDIS_PASSWORD` on pool+health-check); arming same session: REDIS_PASSWORD into Pro secrets env (0600) + deploy infra/eventbus→~/scripts/eventbus (cmp) + kickstart 3 daemons; proof gate = logs free of AuthenticationError + pids stable >10min | operator (b-f) | organism_alert boot receptor shows 0 findings for these organs; federation sidecar ts <3min; consumer log free of AuthenticationError; eventbus pids stable >10min — *(riga rientrata: era misfiled sotto "## closed" dal commit cc40a125a; spostata al bench run 1; aggiornata a stato-diagnosed dalla TAC, 2026-07-02)\_

- opened 2026-07-02 | WR2 C1 outcome-probes (#1920 merged) + C2 runtime stamps (#1929) + C2b RUNTIME_STALE probe (#1931) | il deploy clone si auto-aggiorna via deploy-puller, ma supervisor+watchdog sono daemon long-running: servono `launchctl kickstart -k gui/$(id -u)/com.balizero.wr2.supervisor` e `…supervisor-watchdog` per caricare il codice nuovo | me (post-flight) o operator | `~/logs/wr2_supervisor_watchdog.log` mostra le righe outcome-probe (`leases ok` / `ALERT P1 …`) e `~/.organism/last_seen/wr2.supervisor.runtime.json` esiste con head == HEAD del deploy clone
- opened 2026-07-02 | C2 strict gate (`WR2_RUNTIME_SHA_GATE=strict` nel plist html-apply) | resta warn-only finché i log warn-mode non dimostrano zero falsi positivi (~1 settimana) — firebreak legittimo, non debito | operator | 7g di log html-apply senza `runtime-sha gate:` spurie, poi flip env + un render verde con gate strict
- opened 2026-07-02 | C2 kickstart post-pull (`WR2_DEPLOY_PULL_KICKSTART=1` nel plist deploy-puller) | flag OFF by default (mandato flight: live-pipeline changes OFF); arming = env nel plist + bootout/bootstrap del puller | operator (o me con GO) | `~/logs/wr2-deploy-pull.log` mostra `kickstart -k com.balizero.wr2.supervisor OK` dopo un pull advanced
- opened 2026-07-02 | C3 launchagent_reconcile (#1926) — wiring cron settimanale | NESSUN plist installato by design (176 daemon + W84): firebreak dichiarato, on-demand `python3 scripts/launchagent_reconcile.py` | operator | n/a (firebreak, non debito)
- opened 2026-07-02 | C3 --apply dei 21 junk delete-eligible (report live Pro 2026-07-02: 219 vivi, 94 junk, 21 eligible/73 protetti) | REPORT-ONLY finché l'operatore non rivede il report; poi `--apply --min-age-days 30` | operator | re-run report mostra junk delete-eligible = 0
- opened 2026-07-02 | C1 probe SQL provato su prod (Keychain ACL nega in headless, exit 36) | prova live rinviata al primo sweep del watchdog riavviato | me (post-flight) | prima riga `state ages ok` / `ALERT` nel log watchdog post-restart
- opened 2026-07-02 | superseded branch `agent/nuzantara/infra/dependabot-vitest-bump` (2026-06-02, vitest ^4.1.0 intent now on main as ^4.1.9) | delete is operator-only: branch is NOT blob-identical to main (older pins), W88 letter says report-only even though rescue value is zero | operator | branch absent from `git branch -r`

- opened 2026-07-03 | sonnet-5 cron migration (PR #1917) — repo pins bumped but LIVE HOME wrappers still pin 4-6; UPDATE 2026-07-03: `regulatory-watcher-run.sh` on Pro refreshed from repo canon (sonnet-5 live, cmp-verified) and `eventbus/meta_dispatcher.py` closed by #1944 — remaining: `nb-curator-daily.sh`, `wr2-ig-metrics-analyst-run.sh`, `competitor-monitor-run.sh`, `yield-optimizer-run.sh` | operator runs the one-liners in `research/operations/2026-07-03-sonnet5-cron-migration.md` §Solo-operatore | operator | respective cron logs show `used: claude-sonnet-5`
- opened 2026-07-03 | `~/scripts/eventbus/meta_dispatcher.py` is AHEAD of `infra/eventbus/meta_dispatcher.py` (BZ_REDIS_HOST/PORT env param never promoted back — #1 HOME-fork reverse debt) | CLOSED this PR — reverse-promotion done for meta_dispatcher + publisher + cron_log_sentinel (HOME content, sonnet-5 pins kept); post-deploy `cmp -s` repo-vs-HOME passes exactly | done 2026-07-02 | `cmp -s` repo-vs-HOME on the 3 files
- opened 2026-07-03 | WR3 reflexion weekly cron executes 816-byte S7.3 STUB (`~/.claude/skills/bali-zero-brand/wr3/_reflexion-synthesis.py`, exit 0) — full `scripts/wr3_reflexion_synthesis.py` never runs (pre-existing Esiste≠Armato found during sonnet-5 inventory) | operator decision: point plist at the real script or accept firebreak until WR3 S7.5 | operator | `~/.claude/skills/bali-zero-brand/wr3/*/lessons.md` gains entries after a Sunday run

- opened 2026-07-02 | proprioception daily cron | DELIBERATE FIREBREAK, not tech debt: v1 is receptor-only (no session → no alarm, accepted in runbook §Known limits; 176 daemons + W84 argue against a hasty 177th); arming = operator GO after v1 proves signal quality | operator | operator GO + installer PR, then `guardian_freshness` self-probe stays green without manual runs

## closed (proof recorded)

- closed 2026-07-03 | fleet MAIN pulls (M5 + Pro + Mini, incl. post-#1919 and doc-organs #1936/#1939/#1940 lines) | M5 pulled by operator; Mini pulled 2026-07-02 via ssh (ff-only, clean tree); Pro pulled 2026-07-03 after stash-park (sibling in-flight work preserved as branch `wip/pro-parked-matagaruda-20260703`, commit fe1f55530, ZERO discarded; 5 wa-mirror/wa-dashboard files restored only after blob-proof identical to origin/main) | PROVEN: all three MAIN checkouts at ada453325 this session (M5 receptor line, Pro `rev-parse` post-pull, Mini receptor line) — every content-grep proof of the old lines is subsumed by ≥#1944
- closed 2026-07-03 | Pro staged vitest ^1.6.1 bump | absorbed into the stash-park branch | PROVEN: `git diff --cached` on Pro main = empty AND package.json shows ^4.1.9 (both re-checked live)
- closed 2026-07-03 | proprioception receptor 3/3 + full local runs Pro+Mini | receptor emitted 🧭 on M5 (session boot), Pro (manual hook run, fresh report) and Mini (manual hook run); first full runs done on Mini (2026-07-02, machine=mini, head 85d074ae6) and Pro (2026-07-03, machine=pro, 8 probes, caught 5 real DIVERGED incl. 4 stranded regulatory deltas → PR #1945) | PROVEN: `last.json` exists on all three with correct machine label; the organ's findings already produced two cures

- closed 2026-07-02 | PR #1915 vitest ^4.1.9 critical fix (GHSA-5xrq-8626-4rwp) | auto-merge landed 08:21Z | PROVEN: origin/main blobs show `"vitest": "^4.1.9"` in admin-dashboard-local, wa-mirror, team-agent/bridge (re-grepped post-merge); root npm audit critical 1→0 on the tested tree
- closed 2026-07-02 | PR #1925 pip 35-advisory close | auto-merge landed 08:25Z; fly-deploy run 28576180393 success | PROVEN: `post-deploy-verify.sh 1925` exit 0 — `/health` on nuzantara-rag "healthy, version=v100-qdrant"; main's both lock blobs byte-identical to the tested branch (pip-audit prod 0-vulnerable carries by blob identity)
- closed 2026-07-02 | opus-mythos deprecation banner M5+Pro+Mini | operator ran the idempotent one-liner on all 3 | PROVEN: `grep -c SUPERSEDED` = 2 on M5, Pro, Mini (description prefix + body banner); live skill registry on M5 reloaded with the superseded description
- closed 2026-07-02 | main-checkout pull on M5 (PENDING-ALIGN:M5) | operator pulled same-day | PROVEN: HEAD `3509f3e2ab`, `.claude/skills/modus/SKILL.md` present, receptor hook wired in `.claude/settings.json`
