# Python files (not supported by Prettier)
*.py

# Build outputs
dist/
build/
.next/
out/

# Dependencies
node_modules/
.venv/
venv/

# MCP server installations (third-party code)
.mcp-servers/

# Vendored upstream code (byte-identical to upstream — see vendor/*/UPSTREAM.md)
# Our additions (UPSTREAM.md per vendor dir) ARE prettier-formatted.
vendor/evoskill/.claude/
vendor/evoskill/docs/
vendor/evoskill/examples/
vendor/evoskill/notebooks/
vendor/evoskill/scripts/
vendor/evoskill/README.md
vendor/evoskill/Dockerfile
vendor/evoskill/uv.lock

# Logs
*.log

# Environment files
.env
.env.local
.env.*.local
.cursorrules

# Extracted data (PDFs, raw text)
lampiran/

# Large data files (auto-generated JSON)
source_documents/
data/
archives/kbli-feb2026/KBLI_2025_*.json

# Shell scripts (not supported by Prettier)
*.sh

# macOS plist files (not supported by Prettier)
*.plist

# Mypy cache
**/.mypy_cache/

# Coverage reports (auto-generated)
**/coverage/

# Auto-generated LLMs files
apps/mouth/scripts/generate-llms-full.ts

# Workspace API files (auto-generated or third-party)
apps/mouth/src/lib/api/portal/portal.api.ts
apps/mouth/src/lib/api/workspace/lkpm.api.ts

# Session summaries (docs)
SESSION_SUMMARY_MARCH_2026.md
apps/cell/.venv/

# DOCSYNC-managed files (auto-regenerated by scripts/docs_sync.py)
# Prettier table alignment conflicts with DOCSYNC markers.
CLAUDE.md
README.md
docs/AI_ONBOARDING.md
docs/DOCS_INVENTORY.md

# Research captures (CLAUDE.md §15) — ad-hoc auditable prose, NOT code.
# CI runs no prettier on research/; the pre-commit hook should match it,
# otherwise a merge that carries pre-existing non-conformant research md
# (authored by another session) is blocked locally though CI is green.
research/

# Quarantined attempts kept for audit only — should NOT be reformatted
# (the byte-identical content is the point: it shows what we tried).
.disabled/

# P7 LEARN shadow proposals — GENERATED by agent-library/learn/lesson_harvester.py.
# Prettier must NOT reformat them: the generator is the sole authority on layout
# and the CI gate (lesson_harvester.py --check) asserts byte-identity.
agent-library/learn/proposals/

# P8 brand-api build-artifacts — GENERATED by scripts/brand_api_gen.py.
# Prettier must NOT reformat them: the generator is the sole authority on their
# byte layout, and the CI gate (brand_api_gen.py --check) asserts the on-disk
# file is byte-identical to a fresh generation. Letting prettier touch them
# would make --check fail forever (generator output != prettier output).
packages/design-system/brand-api/
docs/design/components-catalog.md
