{
"value": {
"answer": "Come Senior Platform Engineer di Nuzantara v5.2.0, procedo con l'analisi architetturale per il TICKET A.2 basandomi rigorosamente sui bundle sorgente (snapshot 2026-03-23). Il codice sorgente è la verità assoluta e guiderà le mie risposte.\n\n### Q3.1. Il pattern EventBus + handlers era già canonico al 2026-03-23?\n**SÌ, assolutamente.**\nIl pattern basato sull'EventBus è l'infrastruttura canonica e validata per il disaccoppiamento dei domini e la gestione di eventi asincroni nel backend. Come verificato nel file `apps/backend-rag/backend/services/events/event_bus.py`, il sistema utilizza PostgreSQL LISTEN/NOTIFY accoppiato al pattern Outbox (`apps/backend-rag/backend/services/events/outbox.py`) per garantire la durabilità.\nAnche se il file specifico `compliance_handlers.py` potrebbe essere una convenzione interna di naming non esplicitamente tracciata nei log di root, l'infrastruttura di routing degli eventi verso handler dedicati è il design canonico. Il dizionario `PG_CHANNEL_MAP` mappa nativamente canali come `practice_changed` (→ `practice.status_changed`), `compliance_alert` (→ `compliance.alert`) e `lkpm_ingest_completed` (→ `lkpm.ingest_completed`), concepiti esattamente per essere consumati da worker/handler isolati senza bloccare i router FastAPI in `apps/backend-rag/backend/app/routers/`.\n\n### Q3.2. Quali segnali CRM suggeriscono una location naturale per l'HGT?\nL'obiettivo dell'HGT (Horizontal Gene Transfer) è estrarre e diffondere _scoperte strutturali_ (es. \"L'ufficio Imigrasi ora richiede il documento X per completare il KITAS\"). Pertanto, i segnali più ricchi di intelligenza procedurale sono le **transizioni di stato delle pratiche**.\nIl segnale ottimale è `practice_changed` emesso dai router in `apps/backend-rag/backend/app/routers/crm_practices.py` (specialmente quando una pratica transita su `completed` o subisce variazioni nei `missing_documents`). In quel preciso momento, il sistema ha a disposizione la timeline completa della pratica e può dedurre pattern strutturali sui requisiti o sulle latenze (es. scostamenti tra `expected_duration` e `actual_duration`). Anche `lkpm_ingest_completed` è un eccellente trigger secondario per dedurre pattern sulle compliance aziendali.\n\n### Q3.3. L'idea di \"co-locazione con crm-cell\" è ancora valida?\n**ASSOLUTAMENTE NO. È un vicolo cieco architetturale.**\nCome ho stabilito nelle mie precedenti revisioni forensi, la directory `apps/crm-cell/` **è vaporware**. Non esiste nello snapshot del 2026-03-23. Tutta la logica CRM vive attualmente nel monolite `apps/backend-rag/backend/services/crm/`.\nInseguire il purismo architetturale cercando di iniettare codice in una cellula fantasma (Opzione δ) non genererà alcun valore e bloccherà l'esecuzione in un limbo. Poiché la logica di dominio esiste nel backend RAG, il publisher HGT deve agganciarsi lì, accettando temporaneamente la violazione architetturale (il layer RAG che parla con il layer `cell_core.hgt.publisher`). L'Opzione β è l'unica che rispetta la topologia reale del codice.\n\n---\n\n### RECOMMENDATION: OPZIONE β (New `crm_hgt_handlers.py`)\n\n**Rationale:**\nL'Opzione β (`apps/backend-rag/backend/services/events/handlers/crm_hgt_handlers.py`) è l'approccio più pulito e difendibile:\n1. **Scarta α (`practice_status_listener.py`)**: Sarebbe troppo limitante se in futuro volessimo estrarre pattern HGT da eventi non legati alle pratiche (es. `client_changed` o `lkpm_ingest_completed`).\n2. **Scarta γ (Estendere `compliance_handlers.py`)**: Violazione palese del Single Responsibility Principle. La compliance e l'evoluzione genomica (HGT) sono domini logicamente separati.\n3. **Scarta δ (`crm_cell/poller.py`)**: Come spiegato, `crm-cell` non esiste.\nCreare un `crm_hgt_handlers.py` dedicato permette di ascoltare molteplici canali dall'EventBus, astrarre i payload CRM in oggetti `StructuralPattern` (preservando il contratto a 9 campi di `HGTPublisher`), e isolare la dipendenza da `cell_core` in un singolo file del backend.\n\n**Effort Estimate:**\n~4-6 ore. L'infrastruttura sottostante (`CrmHGTBridge` async) è già fusa. Il lavoro si riduce a:\n1. Creare la classe handler in `crm_hgt_handlers.py`.\n2. Mappare i payload di `practice_changed` in `StructuralPattern` (assicurandosi di convertire i nested dict in JSON string per evitare il crash dello stream Redis individuato nella QA 3.2).\n3. Registrare l'handler sul bus principale al boot dell'applicazione.\n\n**Hidden Coupling con TICKET B e C:**\n* **Zero coupling con TICKET B (`intel-scraper-cell`)**: Sono due publisher indipendenti. L'intel scraper analizza l'esterno (siti gov), il CRM handler analizza l'interno (latenze pratiche). Nessuna collisione.\n* **Coupling asincrono con TICKET C (`sentinel_cell` consumer)**: Scegliendo l'Opzione β, inizieremo a scrivere sul Redis stream `cell:skills` con `domain=\"crm\"` (o `\"generic\"` in base alla whitelist di `cell_core.hgt.domains`). Devi assicurarti che il consumer in TICKET C (nella `sentinel_cell`) sia in grado di parsare payload provenienti da un `cell_origin=\"backend-rag\"`, ignorando o gestendo correttamente domini non previsti per l'OSINT puro. Non ci saranno crash a runtime, ma il Consumer del Ticket C dovrà essere resiliente alle nuove iniezioni di dati strutturati CRM.",
"conversation_id": "20cd6007-21e6-42d3-8271-ec8edabf2e5c",
"sources_used": [],
"citations": {},
"references": []
}
}
