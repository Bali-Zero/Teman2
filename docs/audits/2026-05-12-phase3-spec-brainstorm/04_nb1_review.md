{
"value": {
"answer": "Come Senior Platform Engineer di Nuzantara v5.2.0, basandomi rigorosamente sui bundle sorgente reali (snapshot 2026-03-23) e sullo storico della nostra architettura, ecco l'analisi forense per i 4 quesiti del TICKET A/B/C. \n\nRicorda la nostra regola fondamentale: il codice è l'unica verità.\n\n**Q3.1. Dove in `apps/backend-rag/backend/services/crm/` emerge più naturalmente un `StructuralPattern`? (TICKET A.2 caller)**\nCome stabilito nei referti precedenti, la directory `apps/crm-cell/` **NON ESISTE**. L'intera logica CRM vive nel backend RAG in `apps/backend-rag/backend/services/crm/`. \nSe si deve forzare l'emissione di un `StructuralPattern` (che rappresenta una scoperta di regola procedurale, come definito in `apps/bali-intel-scraper/backend/cell/hgt_publisher.py`), il location canonico più sensato è **`apps/backend-rag/backend/services/crm/ai_crm_extractor.py`** o **`auto_crm_service.py`**. In questi file l'AI estrae entità e pattern dalle conversazioni client. \n*Attenzione però al disallineamento architetturale:* `StructuralPattern` e HGT appartengono a `packages/cell-core/`. Inserire un HGT publisher dentro un servizio FastAPI/RAG classico mischia il layer API con il layer Cellule (organism). \n\n**Q3.2. `validate_domain` in `packages/cell-core/cell_core/hgt/domains.py` è il posto giusto per registrare \"crm\"?**\n**SÌ, assolutamente.** \nL'evidenza empirica risiede in `apps/bali-intel-scraper/tests/unit/cell/test_hgt_publisher.py`. Il test `test_publish_unknown_domain_normalized_to_generic` dimostra esplicitamente che qualsiasi dominio non riconosciuto cade nel fallback: _\"Unknown domain falls through validate_domain -> 'generic'\"_. \nQuesto conferma che `validate_domain` applica una whitelist hardcodata centralizzata nel package `cell-core`. Non c'è alcun meccanismo \"cell-side\" di estensione dinamica: devi modificare fisicamente `packages/cell-core/cell_core/hgt/domains.py` per accettare `\"crm\"`.\n\n**Q3.3. Esiste `apps/cell-core/hgt_coordinator/` con un meccanismo di graduation (FASE 4)?**\n**È VAPORWARE ASSOLUTO.** \n1. Il path corretto per il core delle cellule è `packages/cell-core/` (incluso tramite symlink/editable install nel `Dockerfile`), non `apps/cell-core/`.\n2. Dichiaro esplicitamente che **non esiste alcuna directory `hgt_coordinator/`** nel codebase a mia disposizione. Il meccanismo HGT attuale è puramente point-to-point via stream Redis (`cell:skills`) invocato da `cell_core.hgt.publisher.HGTPublisher`. Qualsiasi mention a un \"graduation mechanism (propose-only → applied)\" di coordinamento centrale HGT è fantascienza non ancora implementata.\n\n**Q3.4. Ci sono altre celle/runners NON menzionate che subirebbero impatti?**\nSì. Lo spec allucina i path `apps/cell/`, `apps/openclaw-hgt-coordinator/` e `apps/organism/`, che **non esistono**. \nLa cellula reale e attiva che state omettendo e che subirebbe impatti fatali in caso di modifiche a `cell_core` è la **SEO Cell**. Questa vive in **`apps/evaluator/seo_cell/`** (con `create_seo_cell()` in `cell.py` e il runner in `run_seo_cell.py`). Se TICKET A/B/C altera i contratti base di `PulseLoop` o le validazioni dei domini in `cell_core` per forzare l'inclusione di HGT, la SEO Cell (che al momento in pre_natal e Sprint 1 usa solo action=\"none\" e non ha HGT) potrebbe schiantarsi.\n\n---\n\n### VERDICT: BLOCK\n\n**Rationale:** \nL'esecuzione della Phase 3 HGT deve essere fermata. Più della metà dello spec tenta di operare su path e architetture che nel codice sorgente non esistono:\n1. Il TICKET A è costruito sul nulla: cerca di iniettare codice in `apps/crm-cell/` (che è vaporware) invece di interfacciarsi con il vero backend `apps/backend-rag/backend/services/crm/`.\n2. I criteri di promozione (FASE 4) dipendono da un `hgt_coordinator` totalmente assente dalla codebase (il sistema HGT odierno non ha un orchestratore di approvazione per skills geniche, scrive direttamente su stream Redis).\n3. Modificare il `cell_core` per assecondare questi tickt orfani rischia di corrompere l'unica altra cellula stabile dell'ecosistema (`seo_cell`).\n\n*Azione richiesta:* Riscrivere il TICKET A per il namespace reale `backend/services/crm/` (accettando la violazione architetturale RAG-to-Cell) oppure droppare l'integrazione HGT per il CRM finché quest'ultimo non verrà effettivamente fattorizzato in una `crm_cell`. Cancellare i riferimenti al `hgt_coordinator`.",
"conversation_id": "20cd6007-21e6-42d3-8271-ec8edabf2e5c",
"sources_used": [],
"citations": {},
"references": []
}
}
