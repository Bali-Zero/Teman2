{
"value": {
"answer": "Come Senior Platform Engineer di Nuzantara v5.2.0, procedo all'analisi architettonica basata rigorosamente sui bundle sorgente (snapshot 2026-03-23). Ignorerò i path inesistenti come richiesto e mi atterrò ai contratti canonici di `cell_core` e dell'HGT.\n\nRicorda: il codice è l'unica verità.\n\n### Q3.1. Verifica del contratto a 9 campi di `HGTPublisher`\n**SÌ, confermo che questo è il contratto canonico al 2026-03-23.**\nL'evidenza empirica risiede nei test e nell'implementazione della classe proxy `IntelScraperHGTBridge` che utilizza `cell_core.hgt.publisher.HGTPublisher`. \n* In `apps/bali-intel-scraper/tests/unit/cell/test_hgt_publisher.py` (funzione `test_to_skill_dict_shape`), vengono asseriti esplicitamente 6 campi chiave: `id`, `scope`, `type`, `confidence`, `domain`, e `cell_origin`.\n* Gli altri 3 campi (`procedure`, `precondition`, `success_criterion`) sono definiti nella struttura dati `StructuralPattern` in `apps/bali-intel-scraper/backend/cell/hgt_publisher.py` e fusi nel dizionario finale al momento della chiamata `to_skill_dict`.\nIl contratto a 9 campi è esattamente quello che viene iniettato nello stream Redis `cell:skills`.\n\n### Q3.2. Accettazione del campo annidato `_metadata` nello stream Redis\n**ASSOLUTAMENTE NO. Romperà a runtime.**\nLo stream Redis (`XADD`), interfacciato tramite la libreria `redis.asyncio` (visibile nel protocol `_RedisLike` in `apps/bali-intel-scraper/backend/cell/hgt_publisher.py`), esige tassativamente un dizionario \"piatto\" composto da chiavi stringa e valori stringa o bytes. \nSe `HGTPublisher.publish` o il nuovo `CrmHGTBridge` tenta di passare un dizionario annidato `{\"_metadata\": {\"legacy_payload\": ...}}` all'interno della payload dict, il client `redis-py` solleverà un'eccezione (`DataError` o `TypeError` per invalid input type) oppure forzerà un cast a stringa `\"{'legacy_payload': ...}\"`. In entrambi i casi, questo distruggerà il parsing strutturato a valle. Per passare metadati complessi in uno stream Redis, devi obbligatoriamente fare `json.dumps()` del valore _prima_ della chiamata `XADD`.\n\n### Q3.3. Impatto della nuova `cell_origin=\"crm-cell\"` sui consumer\nL'introduzione di una nuova stringa `\"crm-cell\"` nel campo `cell_origin` **non romperà alcun consumer esistente** a livello di schema.\n* **Perché:** I consumer noti o pianificati (come `sentinel_cell` in `mata-garuda` o il futuro `research-cell`, documentati in `apps/bali-intel-scraper/backend/cell/hgt_publisher.py`) leggono `cell_origin` come una stringa opaca per il tracciamento della provenienza.\n* L'unico vero rischio di fallimento a valle risiede nel campo `domain`. Come dimostrato dal test `test_publish_unknown_domain_normalized_to_generic` in `apps/bali-intel-scraper/tests/unit/cell/test_hgt_publisher.py`, se il CRM pubblica skill con dominio `\"crm\"` e questo non è registrato nella whitelist di `validate_domain` in `packages/cell-core/cell_core/hgt/domains.py`, la skill verrà silenziosamente derubricata al dominio `\"generic\"`. Questo non schianterà i consumer, ma inquinerà semanticamente lo stream.\n\n---\n\n### VERDICT: BLOCK\n\n**Rationale:**\nL'esecuzione del TICKET A.1 deve essere bloccata immediatamente per il difetto strutturale identificato in Q3.2. Tentare di inviare un dizionario annidato (`_metadata`) in un comando Redis `XADD` viola il contratto del protocollo Redis Streams (che accetta solo flat key-value pairs). Questo causerà crash dell'applicazione a runtime o, nel migliore dei casi, corruzione silente del payload in stringhe non-JSON.\nIl payload `_metadata` deve essere convertito in stringa JSON (`json.dumps`) prima di chiamare l'`HGTPublisher`, e questo requisito deve essere formalizzato nello schema prima di procedere.",
"conversation_id": "20cd6007-21e6-42d3-8271-ec8edabf2e5c",
"sources_used": [],
"citations": {},
"references": []
}
}
