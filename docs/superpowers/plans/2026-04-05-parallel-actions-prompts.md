# 10 Parallel Agent Prompts — Strategic Actions 2026-04-05

> Generated by Opus 4.6 max effort analysis. Each prompt is self-contained and can be dispatched to an independent agent in an isolated worktree.

---

## [A] Redis Cache Activation

```
CONTEXT: The Nuzantara backend (apps/backend-rag) has Redis connected (76ms latency, Fly.io)
but cache_service and rate_limiter are both in "fallback_memory" mode. 0 Redis keys.
Search avg latency is 894ms. Cache hit rate is 2.4% (in-memory LRU only).

ROOT CAUSE (verified):
- File: backend/core/redis_manager.py
  - Lines 87-90: If settings.redis_url is empty/None, sets self._available = False and returns
  - Lines 96-109: Sync client creation — catches exceptions, sets _sync_client = None
  - Lines 112-127: Async client creation — same pattern
  - Line 129: self._available = sync_client is not None or async_client is not None
  
- File: backend/core/cache.py
  - Lines 142-162: CacheService.__init__() calls RedisManager.get_async_client()
  - Line 154: Returns None → triggers fallback at line 161
  
- File: backend/middleware/rate_limiter.py
  - Lines 25-46: Same pattern — get_sync_client() returns None → fallback

THE FIX:
1. Check backend/app/core/config.py — verify settings.redis_url reads from REDIS_URL env var
2. Check Fly.io secrets: `fly secrets list -a nuzantara-rag | grep REDIS`
3. If REDIS_URL is set on Fly.io but empty locally, the issue is only on Fly.io deployment
4. Check if redis_manager.initialize() is called during app startup (check app/setup/service_initializer.py)
5. If Redis URL exists but connection fails — check Fly.io internal DNS (redis.fly.io vs upstash URL)

WHAT TO DO:
- Trace the full initialization chain: config.py → service_initializer.py → redis_manager.initialize()
- Ensure REDIS_URL is correctly set (check both Fly.io secrets AND local .env)
- If Redis is Upstash: ensure the URL format is correct (rediss:// for TLS)
- If Redis is Fly.io internal: ensure it's reachable from nuzantara-rag app
- After fix: verify cache_service registers as "active" not "fallback_memory"
- Add a quick cache warm-up for top-10 KBLI queries after startup

DO NOT:
- Change the cache architecture
- Add new dependencies
- Modify the fallback logic (it's correct as defensive code)

VERIFY: After fix, health endpoint should show cache_service: "active" and rate_limiter: "active"
```

---

## [B] CrossEncoder Reranker Wire

```
CONTEXT: The Nuzantara RAG pipeline has a CrossEncoder reranker fully built and tested
but NEVER wired into the production search flow. Currently search uses only vector distance
or Ze-Rank 2 (external API). The local CrossEncoder would improve retrieval quality 20-30%.

ARCHITECTURE (verified):

1. RERANKER MIXIN:
   File: backend/services/rag/reranker_integration.py
   - Class: CrossEncoderRerankerMixin (lines 32-321)
   - Key method: hybrid_search_with_cross_encoder_reranking() (lines 206-321)
   - Helper: add_cross_encoder_reranking(search_service) (lines 324-361) — monkey-patches SearchService
   - Model: cross-encoder/ms-marco-MiniLM-L-6-v2 (local transformer, no API needed)

2. SEARCH SERVICE:
   File: backend/services/search/search_service.py
   - search() at line 438 — dense vector only
   - hybrid_search() at line 877 — BM25 + Dense + RRF fusion
   - search_with_reranking() at line 803 — dense + Ze-Rank 2 (external API)
   - hybrid_search_with_reranking() at line 990 — full pipeline with Ze-Rank 2

3. EXISTING RERANKER:
   File: backend/core/reranker.py — Ze-Rank 2 API-based (EXTERNAL, costs money)
   File: backend/services/rag/reranker.py — CrossEncoderReranker (LOCAL, free)

WHAT TO DO:
Option A (safest): Call add_cross_encoder_reranking(search_service) during service initialization
  - In service_initializer.py, after SearchService is created, call the helper function
  - This monkey-patches the instance with cross-encoder methods
  - Then the orchestrator can choose cross-encoder reranking vs Ze-Rank based on config

Option B (direct): Make the orchestrator prefer CrossEncoder for standard queries
  - In the agentic orchestrator (services/rag/agentic/), when calling search,
    use hybrid_search_with_cross_encoder_reranking() instead of hybrid_search()
  - Keep Ze-Rank as fallback for when local model isn't loaded

STEPS:
1. Read service_initializer.py to find where SearchService is created
2. Add the monkey-patch call after creation
3. Read the orchestrator to find where hybrid_search() is called
4. Add a config flag (e.g., USE_LOCAL_RERANKER=true) to switch between CrossEncoder and Ze-Rank
5. Default to CrossEncoder (free, local, fast on M4)
6. Run existing tests: pytest backend/tests/services/rag/test_reranker_integration.py

DO NOT:
- Remove Ze-Rank integration (keep as fallback)
- Change the CrossEncoder model
- Modify reranker_integration.py itself (it's already correct)

VERIFY: Run a test query and confirm reranked results differ from non-reranked
```

---

## [C] CRM Data Cleanup

```
CONTEXT: The Nuzantara CRM has dirty data that pollutes dashboards and alerts.
100 expiry alerts include passports expired since 2013. Test data in production.

CLIENT MODEL (verified):
File: backend/app/modules/crm/models.py (lines 16-63)
Table: clients
Key columns: id, full_name, email, status (text: 'active'/'inactive'/'prospect'/'lead'),
  assigned_to (text, email), passport_number, tax_id, tags (JSON), custom_fields (JSON),
  created_at, updated_at
NOTE: NO deleted_at/deleted_by columns — cannot soft-delete, must update status to 'inactive'
NOTE: NO passport_expiry in Client model — expiry data is in documents table or compliance_items

DB CONNECTION PATTERN (from existing scripts):
  DB = "postgresql://backend_rag_v2:2zEjit43IF6gNUV@localhost:15432/nuzantara_rag?sslmode=disable"
  (requires fly proxy: fly proxy 15432 -a nuzantara-postgres)
  conn = await asyncpg.connect(DB)

WHAT TO DO — Write a script: apps/backend-rag/scripts/crm_data_cleanup.py

1. ARCHIVE STALE CLIENTS (status → 'inactive'):
   - Clients with passport_expiry (from documents table) before 2024-01-01 AND status = 'inactive'
   - Add tag 'archived_stale_2026-04' to their tags JSON array
   - DO NOT delete — just update status and add tag
   - Log each change

2. REMOVE TEST DATA:
   - Client id=10890 "Mickey Mouse" — set status='inactive', add tag 'test_data'
   - Any client with name containing 'test' or 'Test Client' — flag for review (don't auto-archive)

3. FIX ASSIGNED_TO ANOMALIES:
   - Find all clients with assigned_to = 'company_extract_import' → set to NULL
   - Find all clients with assigned_to containing emails not in team:
     ['ari@', 'vino@', 'krisna@', 'surya@', 'adit@', 'sahira@', 'damar@', 'ruslana@', 'zero@']
   - Report anomalies but don't auto-fix (except company_extract_import)

4. CLEAN EMPTY PRICING COLLECTION:
   - bali_zero_pricing_hybrid has 0 documents in Qdrant — either populate or remove from config

SCRIPT PATTERN: Use --dry-run flag, argparse, asyncio.run(), detailed logging.
Print summary at end: X archived, Y tagged, Z reassigned.

VERIFY: After running, get_expiry_alerts should return fewer historical items.
get_client_stats unassigned count should decrease.
```

---

## [D] Client Reactivation Campaign

```
CONTEXT: 760/1105 clients (69%) have zero interaction in 30 days.
146 clients have no team member assigned. This is revenue left on the table.

CURRENT TEAM LOAD: Ari 112, Vino 105, Krisna 79, Surya 49, Adit 37, Sahira 30, Damar 19, Ruslana 2

CLIENT MODEL: backend/app/modules/crm/models.py — assigned_to is text field (email)
DB: postgresql://backend_rag_v2:2zEjit43IF6gNUV@localhost:15432/nuzantara_rag

EMAIL SYSTEM:
- Service: backend/services/integrations/zoho_email_service.py (ZohoEmailService)
- ALWAYS from: zantara@balizero.com, name: Zantara
- Dual routing: @balizero.com → Zoho SMTP, external → Brevo HTTP API
- Endpoint: POST /api/notifications/send-email + X-API-Key: zantara-secret-2024
- For bulk: use Brevo API directly (has higher rate limits)

WHAT TO DO — Write TWO scripts in apps/backend-rag/scripts/:

SCRIPT 1: assign_unassigned_clients.py
- Query: SELECT id, full_name, email, status FROM clients WHERE (assigned_to IS NULL OR assigned_to = '') AND status IN ('active', 'prospect', 'lead')
- Round-robin assignment targeting equal load:
  Target per member after assignment (approximate):
  Ruslana gets most (currently 2), then Damar (19), Sahira (30), Adit (37), Surya (49)...
  Use inverse-load weighting: member with fewest clients gets next assignment
- UPDATE clients SET assigned_to = $1, updated_at = NOW() WHERE id = $2
- --dry-run flag, summary output

SCRIPT 2: reactivation_email_campaign.py
- Query silent clients: last_interaction_date < NOW() - INTERVAL '30 days' OR last_interaction_date IS NULL
- Join with documents table to find visa/passport expiry
- Segment into 3 groups:
  A) Has document expiring within 90 days → "Your [visa/passport] expires on [date]"
  B) Has open practice → "Update on your [practice_type] case"  
  C) Lead/prospect, no practice → "Still planning your move to Bali?"
- Generate email list (CSV) with: client_id, email, segment, personalization_data
- Optionally send via POST /api/notifications/send-email (with --send flag)
- Default: --dry-run generates CSV only

EMAIL TEMPLATE RULES:
- From: zantara@balizero.com / Zantara
- Professional, warm, not pushy
- Include unsubscribe link
- Subject line personalized per segment

VERIFY: Run with --dry-run first. Review CSV. Then --send for a small batch (10 clients) to test.
```

---

## [E] GA4 Debug + Analytics Router Deploy

```
CONTEXT: Three analytics systems are broken in Nuzantara.

ISSUE 1: GA4 RETURNS 0 DATA
Root cause (verified): NEXT_PUBLIC_GA_MEASUREMENT_ID is EMPTY in .env.local
File: apps/mouth/.env.local line 24 — shows "NEXT_PUBLIC_GA_MEASUREMENT_ID=" (empty)
File: apps/mouth/src/app/layout.tsx lines 266-268 — conditional render, won't render if empty
Tag value: G-S3H2M6VXWT

FIX:
1. Set NEXT_PUBLIC_GA_MEASUREMENT_ID=G-S3H2M6VXWT in apps/mouth/.env.local
2. Check Vercel environment variables for the mouth project — ensure it's set there too
3. Since NEXT_PUBLIC_ vars are build-time, a redeploy is needed: git push (Vercel auto-deploys)

ISSUE 2: QUERY ANALYTICS 404
Router exists: backend/app/routers/query_analytics.py (143 lines, 5 GET + 1 POST endpoint)
Router IS registered: backend/app/setup/router_registration.py line 99 (import) + line 304 (include)
Prefix: /api/analytics/query-insights

The 404 might be caused by:
- Import error in QueryAnalyticsRepository (check if db/repositories/query_analytics_repository.py exists)
- Or the router import fails silently during startup
- Check fly logs: fly logs -a nuzantara-rag --region sin | grep -i "query.analytics\|import.*error\|traceback"

FIX:
1. SSH or check logs for import errors
2. If repository missing, create it (check what the router imports)
3. If import path wrong, fix it
4. Redeploy: cd apps/backend-rag && fly deploy --strategy rolling

ISSUE 3: TEAM PRODUCTIVITY EMPTY
Service: backend/services/analytics/productivity_scorer.py
Method: calculate_productivity_scores() lines 26-109
Queries: team_work_sessions table WHERE status = 'completed'
Returns empty because table has no records or all have wrong status.

FIX:
1. Check if team_work_sessions table exists and has data
2. If empty: the clock_in/clock_out MCP tools should populate it — verify they write to this table
3. If table doesn't exist: check migrations for its creation
4. Consider seeding initial data from team activity logs

VERIFY:
- GA4: Wait 24h after deploy, then check GA4 realtime report
- Query analytics: curl https://nuzantara-rag.fly.dev/api/analytics/query-insights/volume?period=7d
- Team productivity: Use MCP get_team_productivity tool
```

---

## [F] Visa Oracle Complete

```
CONTEXT: Visa Oracle is a consumer-facing visa guidance product at visa.balizero.com.
Backend code is ALREADY STAGED in git (router, migration #080, service package, tests).
Frontend needs to be built. Full plan exists at docs/superpowers/plans/2026-04-04-visa-oracle-implementation.md

STEP 1: COMMIT BACKEND (already staged)
- Verify staged files: git diff --cached --name-only
- Expected: visa_oracle.py (router), migration_080, services/visa_oracle/, tests/
- Commit with descriptive message
- DO NOT deploy yet (frontend first)

STEP 2: BUILD FRONTEND
- Follow the implementation plan in docs/superpowers/plans/2026-04-04-visa-oracle-implementation.md
- Key components:
  a) Quiz flow (5-7 questions about visa needs)
  b) AI chat (3 guided questions using visa_oracle Qdrant collection — 4,778 docs)
  c) Results page with recommended visa type
  d) WhatsApp handoff button to Damar
- Use existing mouth app patterns (Tailwind, shadcn-style components)
- Route: /visa-oracle or separate visa.balizero.com subdomain

STEP 3: INTEGRATION
- Backend: POST /api/visa-oracle/session (create), POST /api/visa-oracle/chat (AI), GET /api/visa-oracle/result
- Frontend calls backend via NEXT_PUBLIC_API_URL

STEP 4: DEPLOY
- Backend: fly deploy --strategy rolling
- Frontend: git push (Vercel auto-deploy)

Read the full plan file before starting. It has 8 tasks with file-level detail.
```

---

## [G] Wire Dormant Services

```
CONTEXT: 3 services in Nuzantara backend are written but never called from the RAG pipeline.

SERVICE 1: CulturalInsightsService
File: backend/services/misc/cultural_insights_service.py (lines 19-185)
Class: CulturalInsightsService
Constructor: __init__(collection_manager, embedder)
Key method: async query_insights(query, when_to_use=None, limit=3) → list[dict]
Returns: [{"content": str, "metadata": dict, "score": float}]
Qdrant collection: "cultural_insights"
ALREADY registered in service_initializer.py (line 73)
ALREADY available as search_service.cultural_insights
NEVER queried in orchestrator

INTEGRATION POINT: In the agentic orchestrator, after retrieval and before response generation,
query cultural insights based on the user's nationality (from conversation context).
Add a step: if user.nationality is known, query_insights(query, when_to_use="response_enrichment")
and append to the context provided to the LLM.

SERVICE 2: GraphService
File: backend/services/misc/graph_service.py (lines 39-236)
Class: GraphService
Constructor: __init__(db_pool: asyncpg.Pool)
Key methods: add_entity(), get_entity(), search_entities(), get_related()
Uses PostgreSQL kg_nodes/kg_edges tables directly
Different from services/knowledge_graph/ (which is LangGraph-based)

INTEGRATION POINT: In the search pipeline, after hybrid search returns top-K results,
use GraphService.get_related() to expand the result set with KG neighbors.
This adds relational context (e.g., "KITAS requires → NPWP" relationships).

SERVICE 3: ContextSuggestionService
File: backend/services/misc/context_suggestion_service.py (48 lines)
Currently returns empty suggestions. This is a STUB.

INTEGRATION: Skip this one — it needs actual logic first. Focus on services 1 and 2.

WHAT TO DO:
1. Read the orchestrator (services/rag/agentic/orchestrator.py or similar)
2. Find where the response is assembled (post-retrieval, pre-LLM-call)
3. Add CulturalInsights query as optional enrichment
4. Add GraphService.get_related() as KG expansion after search
5. Both should be behind feature flags or config settings
6. Run tests to ensure no regression

DO NOT modify the services themselves — only wire them into the pipeline.
```

---

## [H] KBLI Enrichment Pipeline Start

```
CONTEXT: 4,618 KBLI codes in Qdrant (kbli_2025_final_hybrid collection).
1,241 codes have no editorial content — just raw government data.
Full plan: docs/superpowers/plans/2026-04-02-kbli-enrichment-pipeline.md

This is a LOCAL pipeline running on Pro (M4 Pro, 48GB RAM) using Ollama models.

Follow the plan exactly. Key steps:

1. TRIAGE: Query Qdrant for codes where content is minimal (<200 chars or missing key fields)
2. CLASSIFY: Use Ollama qwen3.5:9b to classify each code into gold/silver/bronze tier
   - Gold: high-traffic codes (PMA-eligible, common business types)
   - Silver: medium-traffic codes
   - Bronze: niche/rare codes
3. ENRICH: For each tier, generate editorial content:
   - Gold: Full article (1000+ words) with qwen3.5:27b
   - Silver: Summary (300-500 words) with qwen3.5:9b
   - Bronze: Basic description (100-200 words) with gemma3:12b
4. VALIDATE: Check generated content for accuracy against source data
5. UPSERT: Update Qdrant payloads with enriched content

CRITICAL RULES:
- Flat Qdrant payloads only (never nested)
- Fields: kode_kbli, judul, content, sektor_id, pma_status, skala_usaha, kategori_risiko
- Ollama: think: false for Qwen 3.5 (via native /api/chat, NOT OpenAI-compat)
- Embedding: text-embedding-3-small (1536 dims) — NEVER CHANGE
- Batch size: 10 codes per batch, with progress logging
- --dry-run first, then --batch 50 for first real run

Read the full plan for file structure and implementation details.
```

---

## [I] Satellite Apps Completion (Drive + Mail + Calendar)

```
CONTEXT: 3 satellite apps are 70-85% complete. Each needs 1-2 key features to reach production quality.

APP 1: DRIVE (drive.balizero.com) — Currently 85%
Location: apps/drive/
Has: 33 components, 3-column layout, grid+list views, file ops, keyboard nav
MISSING: File preview modal + share dialog

WHAT TO DO:
a) Create FilePreview component that handles:
   - Images (inline display)
   - PDFs (iframe or react-pdf)
   - Text/code files (syntax highlighted)
   - Other types: "Download to view" fallback
b) Wire it to the existing file click handler
c) Create ShareDialog component with:
   - Public link toggle
   - Permission dropdown (viewer/editor)
   - Uses Google Drive API sharing endpoint

APP 2: MAIL (mail.balizero.com) — Currently 70%
Location: apps/mail/
Has: Email list, compose with drafts, attachments, Zoho integration
MISSING: Thread conversation view + rich email viewer

WHAT TO DO:
a) Create EmailViewer component:
   - Render HTML emails safely (DOMPurify)
   - Show inline images
   - Display attachments as download chips
b) Create ThreadView component:
   - Group messages by thread/conversation
   - Expandable/collapsible messages
   - Reply inline

APP 3: CALENDAR (calendar.balizero.com) — Currently 80%
Location: apps/calendar/
Has: Month/list toggle, Google Calendar integration, event creation
PROBLEM: Everything in ONE 798-line page.tsx file

WHAT TO DO:
a) Refactor page.tsx into components:
   - CalendarGrid.tsx (month view)
   - EventCard.tsx (single event)
   - EventCreateForm.tsx (create/edit form)
   - EventList.tsx (list view)
   - UpcomingEvents.tsx (sidebar)
   - CalendarHeader.tsx (navigation, view toggle)
b) Add recurring event support (RRULE parsing)
c) Add iCal export button

Each app follows Next.js patterns. Use existing UI components from each app's own lib.
Follow the design system: BZ tokens (--bz-base: #0c0c0e, --bz-accent: #d4845a).
```

---

## [J] CRM↔Portal Auto-Sync

```
CONTEXT: CRM has 427 active clients but portal profiles are created manually.
Full plan: docs/superpowers/plans/2026-04-02-crm-portal-auto-sync.md

KEY DELIVERABLES:
1. Auto-create portal profile when CRM client is created
2. Smart OCR dispatch on portal document upload
3. Bidirectional notifications (CRM update → portal notification, portal upload → CRM log)

Follow the implementation plan exactly. It has checkbox-tracked tasks.

KEY INTEGRATION POINTS:
- CRM create_client endpoint → trigger portal profile creation
- Portal document upload → trigger OCR → update CRM documents table
- CRM practice status change → push notification to portal
- Portal message → create CRM interaction log

CRITICAL:
- Use existing auth: nz_access_token httpOnly cookie on .balizero.com
- Portal API is in apps/mouth under /my routes
- OCR uses Ollama qwen2.5vl:7b for vision (the ONLY working vision model)
- All prices from PricingTool only

Read the full plan before starting.
```
