# Flaky-test harvest — Bali-Zero/Teman2 / tests.yml

Generated: 2026-09-09T19:16:56+00:00 · window: last 7 days

**Quarantine thresholds (>= 5 failures on >= 50 runs, 2% suite cap, no expiry policy defined yet) are NOT applied — owner ruling pending.** This is a measurement report only: no test was skipped, no job disabled, no PR touched.

## Pairing method (read this before the numbers)

The queue ref `gh-readonly-queue/main/pr-<N>-<sha>` does NOT carry PR <N>'s own head commit — GitHub's merge queue stacks entries, so `<sha>` is the synthetic head of the entry ahead of <N> in the queue. Pairing on that sha against a `pull_request` run's headSha was measured live and matches almost nothing. This report instead extracts the PR NUMBER from the ref, resolves the PR's real head via `gh pr view`, and matches that against `pull_request`-event runs' own headSha.

**Window coverage finding:** requested 7 days, but `gh`'s workflow-run listing hard-caps at 1000 results (confirmed live: the API's own `total_count` exceeded what page-by-page fetching actually returns) — at this repo's CI volume that only reaches back **5.6 days**, not 7. Everything below is honest about that smaller window, not a silent under-count.

## Summary

| Metric | Count |
|---|---|
| Runs scanned (pull_request + merge_group, completed) | 972 |
| `pull_request` runs | 629 |
| `merge_group` runs | 343 |
| Unique PRs seen in the queue | 292 |
| PRs unreadable via `gh pr view` | 0 |
| PRs with no matching `pull_request` run in window | 2 |
| Job-pair comparisons: pr_vs_queue | 5047 |
| Job-pair comparisons: queue_reentry | 760 |
| Flake candidates (conclusion flipped, no diff) | 66 |
| Real reds (failed in both runs — not a flake) | 30 |
| gh call errors (counted, not guessed) | 0 |

**Finding:** 2/292 (1%) of queued PRs had no `pull_request` run with a matching headSha inside the 7-day window (older run, rerun-without-cache-hit, or the run itself fell outside the window). That is a finding about window coverage, not a script bug.

## Flake candidates by job

| Job | Candidates | Observations | Real reds | Run ids |
|---|---|---|---|---|
| Test Summary | 26 | 392 | 2 | 34381980545, 34383272600, 34381980545, 34385735425, 34385735425, 34386788390, 33969753540, 33988200543, … (+44) |
| npm audit (advisory) | 21 | 85 | 7 | 33865008999, 33869159332, 33852620526, 33853864437, 33852620526, 33858087989, 33852620526, 33866377801, … (+34) |
| E2E Tests (Playwright) | 10 | 288 | 1 | 34381980545, 34383272600, 34381980545, 34385735425, 34385735425, 34386788390, 33969753540, 33988200543, … (+12) |
| Visa Oracle fullstack smoke | 9 | 290 | 1 | 34381980545, 34383272600, 34381980545, 34385735425, 34385735425, 34386788390, 33950671964, 33961735052, … (+10) |

## Top 20 failing test ids on flake-candidate runs

No test ids extracted — no flake-candidate log matched a `FAILED ` line this window (or none could be fetched). This usually means the candidates above are JOB-level failures — an external service error, a container/setup step, or an aggregator job like `Test Summary` — rather than a specific pytest/Playwright assertion; open the job list above and read the run's own log for the actual cause. That is itself a finding about this window's flake granularity, not an extraction bug.
