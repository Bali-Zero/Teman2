# Scratch probe — conflict-subject variant A (throwaway)

Not a real doc. Subject branch for the discriminating experiment ordered
2026-08-30 (team-lead, PENDING-ARMS.md: `autoMergeRequest` survival across
a genuine CONFLICTING transit). A sibling branch adds the same path with
different content and lands first, forcing a real ADD/ADD conflict here.
This PR is armed, read while genuinely CONFLICTING, resolved by a LOCAL
merge with NO re-arm, read again, then closed without merging.
