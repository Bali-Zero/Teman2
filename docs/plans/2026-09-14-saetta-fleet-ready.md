# SAETTA fleet readiness

Mandate: SAETTA-FLEET-READY. BLUE. Coordinator prepares; an independent Claude
session verifies and releases. User authorized BLOCK fixes, three-host operation,
Codex context at 60%, followed by a Kita pilot.

The September 13 M5 session scripts let VO questions follow a blocked specification,
and K2 gamma follow a blocked row-identity result. They also contained host paths
and scratch briefs. Those active scripts remain immutable: changing them under
live agents can create duplicate writers. Future missions consume the durable
`infra/workflows/saetta.js` replacement, prepared by `scripts/saetta.py`.

Acceptance:

- Reject invalid dependencies, cycles and excessive concurrency before dispatch.
- Only complete Dux results followed by an independent exact-head PASS, read-back
  gate receipts, merge and prove-live can release dependent slices.
- BLOCK, missing receipt, changed SHA, failed child, missing merge and failed live
  proof keep dependants closed; unrelated slices continue. Close never upgrades BLOCK.
- Resolve host paths at launch; no copied credentials or home-directory assumptions.
- Pro, M5 and Mini execute a read-only native Workflow smoke and report its journal.
  Mocked scheduler tests are separate evidence, never native readiness claims.
- Verify installed Codex parent and native-child behavior immediately below and at
  60%, and real hook registration/trust. Preserve already-correct installations.

Kita pilot scope: further proven-dead surface removal; existing operational and tax
assignments remain separate. Four designated Tax Leads gain team-leader capabilities
only within tax. No global admin promotion, new hardcoded client page or client data
in evidence. The pilot gets its own briefs/worktrees and negative authorization tests.

Rollback: select the previous versioned workflow for new missions; never replace a
running script. Any required host installation preserves its prior files first.
