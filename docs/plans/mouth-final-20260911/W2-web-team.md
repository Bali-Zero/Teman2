# W2 — Website: Team e founder home

## 1. Mandate

`SHWEB-20260911 / W2-WEB-TEAM` · BLUE default · organ directory pubblica Team · Gear 2 minimo. Mini; nuovo worktree `~/nuzantara/.worktrees/mouth-shweb-w2-team-20260911`, task id `shweb-w2-team-20260911`; registrare branch broker e SHA `origin/main` fetchato. Successo: directory editoriale leggibile e fascia founder coerente, senza alterare il roster interno.

## 2. Owned perimeter

Dentro `apps/mouth`: `src/app/(blog)/team/page.tsx`, `src/app/(marketing)/page.tsx` solo innesto founder, `src/app/v2/_components/SocialProof.tsx`; nuovi `team.module.css` e `team.test.tsx` colocati alla pagina Team, `SocialProof.test.tsx` e `SocialProof.module.css` colocati al componente; `e2e/shweb-team.spec.ts`.

`src/data/team-roster.ts` read-only; niente cancellazioni record o riscritture ruolo. Vietati Footer, layout blog/root, globals, NavShell, packages/core, font/theme/i18n/proxy, package/lock, pipeline articoli, Oracle e Studio. SocialProof owner W2; W5 lo eredita dopo merge. Perimetro verificato a mano.

## 3. Sibling contract

`studio-web-contract-v1`; congelare hash roster e fixture pubblica prima di BUILD. Handoff R19 e research map sono fonti visive, non autorità per nuovi dati. Esclusioni pubbliche già istruite: Faysha/Faisha e Sahira; home con soli founder Zainal e Heru. Altri profili/ruoli e recensioni restano dai dati correnti, niente numeri inventati. Ari collega lo Studio, Surya E-VOA usando destinazioni reali verificate. Varianti SocialProof opt-in dalla home, default `/v2` invariato. Errori immagine/link trattati esplicitamente, nessun profilo inventato come fallback.

## 4. Acceptance

- Directory mantiene tutti gli altri profili; i due esclusi non compaiono nelle superfici pubbliche interessate; lookup interno byte-invariato. Home founder-only, recensioni e fonti preservate. Controllare anche `/v2/company/about` senza editarlo se già corretto: nella base verificata non elenca i due esclusi.
- Negativi: immagine assente, nome/titolo lungo, viewport 360px; layout senza overflow oltre 1px, focus visibile e navigazione tastiera. 360/390/768/1440, immagini decodificate e contrasto verificato.
- Integrazione Home → Team → Ari → Studio e back; link Surya arriva alla vera pagina E-VOA, non una 200 not-found. Reload e navigazione client; `/v2` invariata.
- TSC, lint mirato, test fixture/browser, build. Dopo release ripetere screenshot e identità pagina con revisione servita; nessun invio contatto richiesto.

## 5. Team

Risolvere incarichi da `army-map.md §1bis`: Dux, eventuale implementer, reviewer di famiglia diversa, gate fresco fuori catena e release owner. Registrare modelli/effort/thread effettivi. Non riusare come gate gli Astra autori della spec.

## 6. Appetite and stop-loss

4h; deadline UTC al lancio, rinnovo Zero/staff room. Token non richiesti, subscription. Un implementer, profondità 1, un hop, due rework per blocco; child 50 tool call/45 minuti attivi, N=0. Max due window operative totali. Ledger/mailbox/ack e fallback budget come README. Non aprire restyle globale per risolvere un dettaglio locale.

## 7. Evidence and release

Percorsi dal tool evidence_paths; screenshot base/candidato, elenco pubblico prima/dopo e hash roster, test e revisione servita. **Bites:** visitatore che sceglie la persona e raggiunge lo strumento. W2 parallela W1; precede W4/W5. Gate receipt sul HEAD corrente e release owner autorizzato. Rollback per profilo escluso visibile, profilo legittimo perso, link sbagliato o regressione `/v2`.
