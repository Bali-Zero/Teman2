[L2] [HIGH] W-ORACLE prompt read-only list prevents PR-O3 execution — FINAL.md §5 prompt strictly freezes "engine rules" and "questions", contradicting FINAL.md §3 which requires PR-O3 to open flow.ts/tree.ts for Δ1 — Update the prompt to explicitly exempt flow.ts/tree.ts during PR-O3.
[L2] [HIGH] Rollback via Vercel promote risks reverting the concurrent window — 08-WEBSITE-ADDENDUM-topic5.md §2 instructs rollback via "vercel promote della Production precedente", which will revert W-ORACLE if it deployed immediately prior to W2's rollback — Change to promote the specific deployment ID prior to the window's own PR.
[L2] [MED] W-ORACLE prompt lacks explicit gate on D5 threshold failure — FINAL.md §5 prompt halts if a decision is missing, but fails to instruct halting if the census fails the >= 38/67 threshold required by 02-DECISIONI-ZERO.md D5 — Add "or if D5 targets fail" to the prompt's STOP conditions.
[L1] [MED] Missing inventory script name in W-ORACLE prompt — W0-oracle-reasons.md §2 states the new inventory script must have a "nome fissato nel brief", but the prompt in FINAL.md §5 omits it — Add the exact script name to the W-ORACLE prompt.
[L2] [MED] W2 perimeter forbids enforcing founder exclusion on 'about' page if base changed — 08-WEBSITE-ADDENDUM-topic5.md §2 requires exclusion in the 'about' consumer, but W2-web-team.md §2 strictly forbids editing /v2 pages — Allow W2 to edit /v2/company/about strictly to apply this exclusion if not already present.
[L1] [LOW] PR-O0 instructions omit unconditionally committing the live-state — FINAL.md §3 says PR-O0 commits live-state, but the prompt limits live-state updates to "the same PR that changes state" — Instruct PR-O0 to commit LIVE STATE explicitly even if the state hasn't changed.
[L3] [LOW] W-ORACLE prompt treats D6 as a single boolean despite dual deltas — Prompt says "ONLY if Zero has said yes to D6", but 02-DECISIONI-ZERO.md splits it into "Δ1: SÌ. Δ2: SÌ" — Instruct the agent to check Δ1 for PR-O3 and Δ2 for PR-O4 independently for robustness.
LAUNCHABLE-AS-WRITTEN: conditional — The launch plan is structurally sound but requires fixing the Vercel promote rollback instruction to prevent cross-window deployment collisions and correcting the W-ORACLE prompt to exempt flow.ts from the read-only freeze during PR-O3.
SEAT: Gemini 3.1 Pro (High)
