[L2] [HIGH] W2 cannot complete the public-profile exclusions — FINAL.md §2 requires “fuori dalle pagine pubbliche”, but W2 §2 freezes the roster and omits the book; `apps/mouth/src/components/book/book-data.ts:128` still maps both unflagged profiles into `/book/team` — include the book presentation filter and regression check in W2, preserving internal lookup.
[L2] [HIGH] Independent releases can overwrite the sibling window’s work despite disjoint files — FINAL.md §5 assigns both windows release ownership, while §4 coordinates only shared-file conflicts; both publish the same `apps/mouth` deployment — serialize promotion and rollback, recording deployment IDs and checking candidate ancestry against the currently served SHA.
[L2] [HIGH] Immediate rollback can be undone before the revert merges — FINAL.md §4 requires promoting the previous Production “PRIMA che il revert atterri”; `infra/launchagents/wrappers/mini-vercel-autopromote.sh:35` defaults promotion on, and `scripts/vercel_prod_deploy.py:513` selects the latest relevant commit — require a verified promotion hold through rollback and revert deployment, including the sibling window.
[L1] [MED] FINAL’s frozen backend-test glob misses the actual tests — FINAL.md:73 names `backend/{tests,scripts}/visa_engine/**`, but `backend/tests/visa_engine` is absent; W0:18 correctly names `backend/tests/services/visa_engine/test_interview_walk_census.py` — use separate, verified test and script paths consistently.
[L2] [MED] The website addendum silently opens a forbidden global file — FINAL.md §5 and 08-WEBSITE-ADDENDUM §2 permit “un blocco additivo scoped .r19-*” in `globals.css`, while W2 §2 and W7:29 prohibit it — remove the blanket permission and keep styling within each window’s declared files.
[L2] [MED] NewsHero has contradictory W7 ownership — 08-WEBSITE-ADDENDUM §3 assigns “NavShell/Footer/NewsHero” to W7, but W7:19 lists Footer/MobileNav/HeroBlueprint/PersonaDoors and omits NewsHero; README’s ownership section assigns it to W4 — explicitly assign W7’s identity-only changes and subsequent handoff to W4.
[L2] [MED] Copied website prompts do not carry the authoritative decision-reading guard — LAUNCH’s W2/W7 blocks require README and window specs, whose README still says M is unrecorded; the W7 block itself declares “Scelgo D-A”, whereas 02-DECISIONI-ZERO D4 remains deferred — require reading FINAL and the decisions file before execution and every PR, with explicit precedence over historical prompts.
[L1] [MED] Census provenance overstates production verification — FINAL.md §1 describes the census as verified “su disco e in prod”, but `2026-09-11-review-reason-audit/README.md:44` explicitly identifies the 67-walk result as offline replay; its live evidence covers a separate synthetic case — distinguish offline census coverage from the specifically observed live probes.
[L2] [MED] The binding screenshot gate contradicts authorized Oracle changes — FINAL.md §5 imports v6.2’s gates, but `2026-09-11-oracle-final-window-spec.md:100` permits screenshot changes “only by PR-O4”, although O2 changes visible explanations and O3 adds questions — define permitted visual differences per PR and compare website controls with the same Oracle revision.
[L2] [MED] O3’s target can pass while exercising only affirmative sponsor answers — FINAL.md §3 requires corpus regeneration, but `apps/mouth/scripts/visa-oracle/generate-walk-corpus.ts:135` selects the first option, which `tree.ts:868` defines as “yes” — explicitly require yes/no/unsure cases on both changed branches, with and without disclosure flags, alongside the unchanged 67-walk baseline.

LAUNCHABLE-AS-WRITTEN: no — Resolve the scope contradictions, incomplete public exclusion, decision propagation, and shared release hazards before launching.
SEAT: gpt-6-astra
