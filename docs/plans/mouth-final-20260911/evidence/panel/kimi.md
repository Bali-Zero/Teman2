• I'll verify the checkable factual claims against the repo before writing findings.

• Two more quick checks, then I'll write the findings.

• All verifiable claims checked against the repo on Mini (hostname `mini-pro2`): `persona-doors.spec.ts:67` is the one-red-CTA P2 test (lines 67–79), `engine-response.ts:251` is the HUMAN_REVIEW/missing_facts invariant, the Keychain entry `nuzantara-postgres-readonly` is indeed MISSING, `kbli/layout.tsx:8` imports Footer, `v2/page.tsx:11,95` consumes NewsHero, `rosterBySlug` has exactly three consumers (team, SocialProof, about) and does not filter, the about page does not list the two excluded members, and Ari/Surya exist in the roster. Findings:

  - [L2] [HIGH] W2 ship authority is self-contradictory — LAUNCH.md W2 block: "preparazione fino a candidato reviewable… Consegna PR/candidato al release owner; nessun deploy implicito" vs FINAL §5/08 §2 addendum: "questa finestra Claude shippa da sola (review → merge → arm → deploy → prove-live); «release owner» nel testo Astra sei tu" — the addendum redefines "release owner" but never voids "preparazione fino a candidato reviewable" / "nessun deploy implicito" — fix: one line in the addendum explicitly striking those two phrases.
  - [L2] [HIGH] W-ORACLE prompt freezes what it orders changed — FINAL §5 prompt: "…oracle.css/questions/consents read-only per W0 §2" and two sentences later "then PR-O3 and PR-O4 ONLY if Zero has said yes to D6 (declared scope deltas on questions and outcome presentation)"; D6 is registered yes and Δ1 opens `flow.ts`/`tree.ts`, Δ2 opens `ConsentHandoff.tsx` text — fix: qualify the read-only list with "except the declared Δ1/Δ2 perimeters of spec v6.2 §2".
  - [L2] [MED] globals.css write-permission conflict — addendum (valid "per ogni lotto website W2/W7/W4/W5/W6"): "globals.css: al massimo un blocco additivo scoped .r19-*" vs W2 §2 "Vietati … globals …" and W7 §2 "Vietati root layout/font preload, globals.css R19 o mouth"; no precedence stated, so a window can either write globals.css against its spec or fail a scope check by obeying it — fix: state the addendum amends W7 §2 only (or strike the line for D-B lots).
  - [L2] [MED] D5 target (a) "senza flag ≥ 57/67 dopo Δ1" is likely unreachable by the package's own analysis — 02-DECISIONI-ZERO.md D5 + Fable note: baseline without flags is 55/10/2 and "con i flag [i due NEEDS_INPUT] diventano review ACTIVITY_BOUNDARY", i.e. once Δ1 makes the question reachable those 2 walks plausibly become review, yielding 55/12/0, not 57; the "da MISURARE / STOP" hedge turns a predicted failure into a mid-window halt after Δ1 has landed — fix: pre-compute the post-Δ1 measure in PR-O1's census harness before building O3, or restate the target as "≥55 decisive + 0 dead-end NEEDS_INPUT".
  - [L2] [MED] D5 target (b) fails on its own baseline — 02-DECISIONI: "NEEDS_INPUT senza domanda raggiungibile = 0, prima e dopo", but the same file's note records 2 such NEEDS_INPUT in the without-flags baseline, so "prima" = 2 ≠ 0 and a literal gate fails PR-O1's baseline — fix: define "prima" as the with-flags baseline or as post-Δ1 only.
  - [L1] [MED] Provenance of the load-bearing rulings is recorded inconsistently — 02-DECISIONI-ZERO.md header: "Compilato da Fable 5.1 su delega esplicita di Zero" vs FINAL §1/§4: "compilato da topic 3 su delega esplicita di Zero"; the bundle never identifies topic 3 (nuzantara-c8) as Fable 5.1, and M=Oracle-only / D6=yes-yes exist only as agent-compiled delegations — fix: reconcile the attribution in one line so "REGISTRATA" rests on a named session.
  - [L3] [MED] "Home" is two different routes and W2's acceptance doesn't say which — W2 §2/§3: founder band in `(marketing)/page.tsx` plus "Varianti SocialProof opt-in dalla home, default /v2 invariato", while `/v2` is itself a home surface (`v2/page.tsx:95` NewsHero); FINAL §2's "home con i soli founder" doesn't name the route — fix: write "home = /" (and state whether /v2 must also be founder-only).
  - [L1] [LOW] Line cite drift — FINAL §3 cites `models.py:1426`; the guard is at `apps/backend-rag/backend/services/visa_engine/models.py:1428` ("state=HUMAN_REVIEW_REQUIRED forbids missing_facts"); `engine-response.ts:251` verified exact — fix: cite the rule name, not a bare line number.
  - [L3] [LOW] PR #6124 status disagrees within the bundle — README "Stato della consegna": head `d23454ce…`, red because "manca brief per churn ≥400" vs FINAL §1/§7: head `cdfe4a3d26`, red for missing `harness/fable-gate` verdict — fix: mark the README status block superseded by FINAL §7.
  - [L3] [LOW] Audit-branch instruction reads as opposite in two binding texts — W0 §1: "SHA da main aggiornato, nessuna ripresa della branch audit" vs FINAL §3: "PR-O0 committa il branch audit + live-state" (reconcilable as fresh-worktree copy-and-commit, but not written that way) — fix: one clarifying clause in W0 §1.
  - [L3] [LOW] Window naming drift — FINAL §2 declares "Scheduler unico: le W0–W7" and README's sequence table says "M=solo Oracle: soltanto W0", but the launched mandate is W-ORACLE with PR-O0..O4; the alias "W-ORACLE = W0 estesa" exists only in §3 prose — fix: one alias line in §5.
  - [L3] [LOW] W2 launch mechanics unpinned — W-ORACLE gets "claude --model claude-opus-5 --effort xhigh in tmux su Mini" (FINAL §5) but W2 gets no host/model line, and its prompt pins a Mini-local package path that LAUNCH itself warns is not proof of existence on another host — fix: add the same launch line for W2.

  LAUNCHABLE-AS-WRITTEN: conditional — the two HIGH contradictions (W2 ship authority, W-ORACLE questions/consents freeze) are one-line fixes away; everything else is survivable.
  SEAT: Kimi K3 (kimi-code CLI)

