# IG-2 — HALT note (2026-05-08)

Sub-session IG-2 halted at Phase 1 because both halves of the brief premise
were stale at HEAD `6aa846d6a`:

1. **Empirical dup count is 5, not 13.** `comm -12 <pro_labels> <mini_labels>`
   yields 5 pairs (`cost-advisor-daily-cap`, `cost-advisor-weekly`,
   `fly-restart-loop-detector`, `login-healthcheck`, `ollama-warm-pin`),
   none of which are the 13 `com.matagaruda.*` labels named in the brief.
2. **The 13-label cleanup was already shipped** by PR #495
   (commit `03f66d924`, merged 2026-05-07): genome.yaml entries removed,
   regression test added (`apps/organism/tests/test_genome_no_active_active.py`,
   currently green at HEAD), live `launchctl bootout` performed.

`.claude/rules/cicatrix-scars.md:8` still carries the STRUCTURAL entry
without a RESOLVED marker, which propagated the stale premise into this
brief. Recommended follow-up: small docs PR marking that scar RESOLVED
with link to #495.

Halt artifact + reasoning: `~/.agent/decisions/halts/ig-2.md`.

No production code modified by IG-2. No live `launchctl` state modified.
Worktree (`~/Desktop/nuzantara-ig-2`) safe to delete after the
`[HALTED]` PR is closed.
