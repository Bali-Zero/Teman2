---
title: "B2 — ENGINE · retrieval-primary relevance with a support signal, thresholds, and the abstain record · battle window"
date: 2026-09-11
adversarial_review: codex
---

# B2 — ENGINE · retrieval-primary relevance with a support signal, thresholds, and the abstain record (v7)

| Field | Value |
|---|---|
| Mandate id | **B2** |
| Colour | **BLUE**. No fallback between colours. |
| Dux role | Opus 5 `xhigh` on BLUE, appointed by the staff room. Dux and release owner (including the additive migration: autonomous at L2 once its dry-run is green, `AUTONOMOUS_OPS.md:82-90`; migration rules `:161-164`; released after the fresh gate). |
| Worktree / branch | `agent/nuzantara/backend-rag/b2-retrieval-primary`, created by the command in §1 |
| Wave | 2 — opens only on the five conditions in README §Sequencing (B1 merged, pulled, acked, manifest frozen, support method chosen) |
| Opening | BLUE: Zero opens a fresh `claude --model claude-opus-5` window at effort `xhigh` on Pro and pastes the **Opening prompt** below. |

**Opening prompt (paste verbatim):**

> You are the Dux of mandate **B2**, colour **BLUE**, Opus 5 `xhigh`, on Pro. Read `research/operations/2026-09-11-bot-staff-room/README.md`, `B1-design.md` (its inventory, contract table, manifest and B1-4 note are your inputs) and `B2-engine.md` from disk in `/Users/nuzantara/nuzantara`. Load the `bot` corner and `modus`. Verify the wave-2 conditions yourself (B1 PRs MERGED; Pro pulled — blob check; manifest sha256 in B1's `pack.yml` equals the file on disk; the support method is recorded in README §Sequencing); if any fails, checkpoint `BLOCKED: wave-2 conditions` and stop. State colour, Dux role, mandate id and worktree path, then execute §1–§7. Staff-room window: `<Zero names it at open>`.

## 1. Mandate

B2 · BLUE · **organ: the evidence scorer and the abstain record** — `backend/services/rag/agentic/reasoning_utils.py::calculate_evidence_score`, `_reasoning_evidence.py::compute_evidence_score`, `_abstain_policy.py`, the curated-score minting at `wa_package_builder.py:529`, the support signal chosen from B1.4, the additive `wa_outbox` columns and their carrier. **Gear:** 3 (a client-facing safety gate with panel-ruled thresholds; the CI floor says so; every Gear-3 PR carries its own council evidence — §5). **Host:** Pro.

**Objective:** retrieval provenance becomes the PRIMARY relevance signal, read by `score_kind` with bands derived per kind from B1's contract table; the chosen support signal decides sufficiency on the requested fact; lexical overlap corroborates and never gates alone; the curated synthetic 1.0 raises source quality but never relevance; the "cosine" penalty is re-derived for the formatted scale (an unboosted raw zero arrives as 0.5 and the strict `< 0.5` penalty does not fire) or retired with its reason; the tax 0.10 / visa 0.12 reliefs are re-examined on the frozen benchmark and — only with the authorized, verified B1.5 artifact — on a vector-only retrieval sample (README queue item 7's three cases, §4 B2.2), and either restored toward 0.15 with the numbers or kept with a written reason and a tripwire; and every generated reply leaves its frozen evidence decision in `wa_outbox`. **What success changes:** the four spec pairs (B1 §4 B1.3, `spec_pair`) make the correct gate decision in both context languages; the counterfactual pairs are distinguished; the two strict xfails become passes without weakening any tripwire; B3 can observe the abstain decision in SQL.

**Worktree:** `cd ~/nuzantara && WT=$(python3 scripts/agent_start.py --lane backend-rag --task-id b2-retrieval-primary | awk '/^WORKTREE_READY /{print $2}') && echo "$WT"`; base sha and the absolute root-mandate deadline (`infra/codex-hooks/mandate_budget.py`) recorded at open; same test invocation and load discipline as B1.

**Ground** (re-run on your base sha): everything in B1 §1, plus B1's merged outputs: the inventory and contract table (`evidence/…/b1-score-contract*/pack.yml`), `score_kind` on every source, the mandatory manifest and its sha256, the baseline report per cell, the B1-4 note and the method the staff room chose. `_abstain_policy.py:1-40` (the four named gates and the 2026-06-14 panel ruling against collapsing them; `tax: 0.10`, `visa: 0.12`, `kbli: 0.20`, `default: 0.15`). `wa_package_builder.py:527-548,599-604` (`context_length` = len(sealed chunks), INCLUDING curated, EXCLUDING the pricing block) and `:594` (the scorer call) with `:599-611` (`evidence_inputs`, the frozen contract read by `wa_finalize`; freeze comment `:591-592`). `wa_finalize.py:204-224` (answer-length check) and `:822-909` (cautioned answers with `abstain=true` are deliberately released, subject to the unchanged text-cleaning and egress checks — D8; decision from `:822`, `:890-894` DEFECT on an egress veto, caution-note log `:896-898`, cautioned SEND `:905-909`; `:753-772` has neither an outbox id nor a DB handle). `wa_codex_leg.py:598-607` (parses sealed evidence), `:928-945` (passes it to finalization), `:415-436` (`CodexLegResult` lacks evidence fields), `:964` (completion returns text and reason); `wa_outbox_worker.py:1000-1017` (receives the result), `:1378-1398` (fenced terminal write: outbox id, claim token, expected status), `:1399-1401` (delivery receipts are distinct from submission status); `wa_broker.py:789-808` (consumption clears package and evidence — reading the broker afterwards cannot recover them). Migrations: `apps/backend-rag/backend/db/migrations_v2/NNN_*.sql` with forward SQL above `-- === ROLLBACK ===` (`db/migration_manager.py:315-327`, the `migrations_v2` assignment at `:323`; `.github/workflows/migration-lint.yml:104` selects that directory for Squawk); `.github/workflows/fly-deploy.yml:262-282` runs pre-deploy migrations on the PREVIOUS image and applies new SQL after the new image starts. `AUTONOMOUS_OPS.md:163` preserves applied migration files.

## 2. Owned perimeter

- **Writable:** `reasoning_utils.py` — `calculate_evidence_score` for B2.1, and for B2.2 ONLY the `tax`/`visa` entries of `DOMAIN_ABSTAIN_THRESHOLDS_DEFAULT` (they live at `reasoning_utils.py:690`, not in `_abstain_policy.py`), subject to B2.2's measured acceptance; `_reasoning_evidence.py`; `_abstain_policy.py` keeps its existing gate construction and divergence untouched (it reads the label threshold through `get_abstain_threshold`, `:103`); `wa_package_builder.py:529/:595` (how the curated score is minted and passed; the support signal's input), the support-signal implementation at the consumer B1.4 named, one additive migration `apps/backend-rag/backend/db/migrations_v2/NNN_wa_outbox_evidence.sql` (number chosen against fresh main AND open migration PRs at cut time, never reserved earlier), the **narrow additive exception**: `wa_codex_leg.py::CodexLegResult` + completed-result construction (the frozen evidence label, score and opaque package reference travel in the result; nothing recomputed, nothing inferred from reply text) and B2.1's support-negative branch before the broker offer (§4 B2.1 Terminal route) and the outbox worker's fenced terminal write (same outbox id / claim token / expected-status fence), tests, the benchmark SUPPLEMENT file (append only).
- **Forbidden:** everything in B1 §2's forbidden list except the two named exceptions above and a THIRD — the calls the support-signal method chosen from B1.4 makes by construction (the local Ollama seat, or the Codex seat reached ONLY through the unchanged `CodexExecClient.generate` adapter exactly as B1 §2 exception (1) states) in B2's tests, benchmark runs and the production consumer; this exception adds no embedding call, no paid call and no send; B1's `score_kind` vocabulary (renaming breaks the frozen contract); any change to a mandatory benchmark label, context or band; any threshold change without the §4 numbers; `context_length`'s meaning (len of sealed chunks incl. curated, excl. pricing — pinned by tests for empty, curated-only, vector-only and pricing-only packages through finalization); the FAQ/semantic cache keys; `wa_finalize`'s delivery policy (D8) and any DB write from it; `zantara_core.py`; #5337's veto logic; any WhatsApp send; any embedding, generation, paid reranking, cache write or network fallback during B2.2's sample.
- **Bans:** as B1.

## 3. Sibling contract

- **B1 → B2:** consumes the frozen inventory, table, manifest and chosen method; supplements go to the separate file with their own label review, reported separately from the mandatory set.
- **RC2 → B2.2:** the production sample runs only after RC2's data-operation receipt (deletions + detector verified) is recorded with the collection/version state measured, so B2.2 and B3 describe the same corpus.
- **#5337:** stays suspended; `wa_codex_leg.py` is shared and SERIALIZED — B2.3b lands first, #5337 needs a fresh base afterwards; its veto logic is untouched by B2.
- **Migration order and rollback:** B2.3a (schema) merges and is proven applied (fresh-image migration job green + `\d wa_outbox` + runner history) BEFORE B2.3b (carrier/writer) opens, because a writer shipped with its migration meets absent columns during the previous-image window. Normal rollback = retire the writer (revert B2.3b) and KEEP the nullable columns and the applied migration history; a production schema down is a separately authorized destructive operation after dependent writers are drained, never part of a routine rollback.
- **Ledger and freeze:** as B1 §3.

## 4. Acceptance

- **PR B2.1 — the inversion with a support signal.** With B1's kinds and the chosen support method: per-kind relevance bands (hybrid formatted: rank-derived through the known fusion + transform; dense formatted: cosine-derived through the known transform; reranked: its own band; curated synthetic: source quality only; KG entity / trusted-tool bypass: as their documented contracts) make `semantic_relevance` primarily a function of retrieval provenance, the support signal decides sufficiency on the requested fact, and the lexical ratio is a corroborating term that can raise a weak band by at most one step and never creates relevance from a zero-retrieval package. **Input wiring (PII):** the support signal's production input is the package's REDACTED query and capped REDACTED context (`wa_package_builder.py:576`), never the raw `query` argument of the scorer call (`:594-597`); B2.1 may extend the consumer's signature for that, and if the chosen method is asynchronous the CONSUMER at the generation-gate/package layer becomes async while `calculate_evidence_score` stays synchronous and pure. **Perimeter decision (mandatory, recorded in the PR body):** the trusted-tool 0.85 bypass (`_reasoning_evidence.py:356-371`) and the FAQ/KG fast paths (`orchestrator_core.py:428-441,1084-1103`, which return before any scorer call) are each either (i) brought under the support signal, with counterfactual cells added to the SUPPLEMENT file, or (ii) explicitly excluded with a written reason and a tripwire test per path; silence is not an option, and an uncovered path that can release unsupported substantive advice is named as a residual risk in `pack.yml`. **Falsifiable acceptance on the frozen mandatory manifest, real scorer, no mocks around the formatter, reported per (query language × context language × stratum) and per gate:** (a) the four spec pairs (the manifest cases tagged `spec_pair` — all-in price, NIB/OSS requirements, minimum paid-up capital, registration duration, EN and ID; B1 §4 B1.3) land within one band of each other across context languages and make the correct decision at both gates; (b) sufficient positives: **0** observed false abstentions per cell; (c) negatives (irrelevant, relevant_insufficient, generic_overlap, company_prefix_chunk, the fee-policy offenders as a separate column): **0** observed false acceptances per cell; (d) every counterfactual pair distinguished without case-ID rules or benchmark-specific exceptions; (e) every standing tripwire green, byte-identical, with README D5's ONE exception: the label-selected golden loop at `test_evidence_cross_language.py:207-216` is replaced by the fixed-input cases and corrected expectations B1.3 recorded (`activates_in: B2.1`), reported by the balanced language/stratum cells — nothing else in any tripwire changes; (f) the two strict xfails become passes and only their `xfail` markers are removed; (g) **final delivery, with acceptance criteria:** synthetic candidate texts, case references and independently reviewed expected dispositions are FROZEN before the candidate run; two separately reported, frozen tracks — (g1) direct-finalizer CHARACTERIZATION: the mandatory negatives cross the REAL finalizer (`wa_finalize`, unchanged — D8) with substantive synthetic candidate text, identical inputs keep identical outcomes, and every unsupported release observed here is documented as a vulnerability, never counted as a safety pass; (g2) END-TO-END candidate delivery through the actual upstream consumer (the support-negative branch of the Terminal route below), where an unsupported case produces NO generation and terminates as stub / hand-off; the report gives, per case, baseline and candidate frozen evidence inputs, final disposition, reason and content verdict; identical finalizer inputs must keep identical policy outcomes; the grounded-cautioned POSITIVE control must still release; **on (g2), any unsupported substantive advice emitted by the candidate is an acceptance FAILURE even when labelled "cautioned"** (the predicate at `wa_finalize.py:204` checks context length, evidence and text length, not support; `test_wa_finalize.py:508` (`test_codex_ungrounded_abstain_serves_the_stub_and_tells_a_human`, the refusal) and `:521` (`test_codex_grounded_abstain_sends_the_answer_with_the_caution_note`, the positive control) protect both dispositions and stay green). **Mechanism:** the unchanged finalizer releases — subject to its unchanged text-cleaning and egress checks (`:890-894` returns DEFECT on an egress veto) — any cautioned answer whose package has `context_length > 0`, `evidence_score > 0.0` and text ≥ `_ABSTAIN_MIN_SENDABLE_CHARS` (`wa_finalize.py:212-224`; cautioned SEND `:905-909`, caution-note log `:896-898`), so (g2) can pass under unchanged D8 only if the support signal's CONSUMER sits UPSTREAM of the leg call — at the generation gate / package build — and an unsupported package never reaches the finalizer with substantive text; the frozen expected disposition of an unsupported case is therefore "not generated → stub / hand-off", never "released with caution". **Terminal route (a B2.1 exception on `wa_codex_leg.py`, distinct from B2.3b's telemetry work):** stopping package construction is NOT that disposition — an unbuildable package returns a fall-off reason (`wa_codex_leg.py:580-581`) that the worker raises as `codex_leg_fell_off` (`wa_outbox_worker.py:1026-1027`) into its retry/failure ladder, and ordinary finalization runs only after generation (`wa_codex_leg.py:928-945`). B2.1 therefore adds a support-negative branch BEFORE the broker offer / generator invocation, with the package-result contract it consumes written in the PR body: the branch reaches the unchanged finalizer's safe-abstention handling with the sealed decision and the unchanged score/context, and returns the terminal stub / hand-off through the existing worker fence. Tests prove: no broker offer, no generation, no generation-failure retry classification, and D6's semantics preserved — the branch generates nothing, so `abstained_at` stays NULL while `evidence_score` carries the sealed package's frozen score; B3 tells the two apart by disposition, and making the support-negative branch visible in SQL on its own would be a D6 amendment for Zero, not a Dux decision. If (g) cannot pass under unchanged D8, the PR suspends for staff-room adjudication — `wa_finalize`, `context_length`, thresholds and telemetry are never moved to manufacture suppression; (h) the validation set (drawn by the adversarial seat) reported beside the mandatory set; disagreement between them or with B2.2's live sample SUSPENDS acceptance for adjudication. **Bites:** consumer = `wa_package_builder.build_context_package:594` freezing `evidence_score` and `wa_finalize` reading it; observation = the per-cell report on this head next to B1's baseline, the delivery report, and the six innocence files green.
- **PR B2.2 — the thresholds (criterion 5).** `tax: 0.10` and `visa: 0.12` re-run on the manifest AND — **conditionally, README queue item 7's three cases** — on a retrieval sample: (i) authorization recorded in README item 7 (`Item 7: yes — <date>`) AND the B1.5 artifact verified (sha256 equal on disk) → the live sample is MANDATORY; (ii) authorization absent or declined → the benchmark-only examination is the whole of B2.2's measurement, the live sample is recorded `BLOCKED — authorization absent` in the PR body and `pack.yml`, the live harness is never invoked, and that declared omission suspends neither B2.2 nor later B2 work; (iii) authorization recorded but the artifact missing or invalid → SUSPEND for staff-room resolution. The sample, when it runs, goes through a documented, tested **no-send / no-generation retrieval harness**: approved retained synthetic query vectors only, from the immutable artifact B1.5 produced under README queue item 7 (entry prerequisite: its sha256, exact query → vector mapping, model/dimension provenance, language coverage, approval reference, attempted-call count and handoff location are recorded in the brief; `rag_canary.py`'s `save_baseline` is never run and no replacement vectors are generated; the canary baseline's reference-text vectors may serve only their own exact texts, never as query embeddings), a missing vector ABORTS and never calls a provider, read-only retrieval, query expansion / embedding calls / paid reranking / cache writes / network fallbacks disabled, call graph inspected before execution; retrieved client content is never exported — only synthetic fixtures, approved redacted aggregates and opaque references persist. If the sample would need any paid call, stop and present provider, count and cost for explicit authorization (the withdrawn embedding allowance is not reused). The PR either restores the reliefs toward `default: 0.15` with the measured per-cell deltas, or keeps them with the written reason and a tripwire test that fails if a relief ever masks a false acceptance in the manifest. The panel-ruled divergence (generation gate flat 0.15 vs per-domain label gate) is NOT collapsed. The report records the EFFECTIVE thresholds, including whether a `DOMAIN_ABSTAIN_THRESHOLDS` environment override supersedes the defaults in prod (read via `fly ssh console` `printenv | grep -c DOMAIN_ABSTAIN` — presence only, no value printed if it carries anything else); no environment mutation is implied. **Bites:** consumer = `_abstain_policy.build_abstain_policy` at the label site (`orchestrator_response.py`); observation = the per-domain report before/after and the harness's own no-network proof (its test).
- **PR B2.3a — the schema.** `migrations_v2/NNN_wa_outbox_evidence.sql`: `abstained_at timestamptz NULL`, `evidence_score numeric NULL`, no defaults, no backfill, forward SQL above `-- === ROLLBACK ===`. Required green: migration-number lint, Squawk, `backend.db.migrate apply-all --dry-run`, isolated up/down/up validation. Released by the BLUE Dux after the fresh gate (no Zero item). **Bites:** consumer = the migration runner on the next deploy; observation = the fresh-image migration job green, `scripts/pg.sh -Atc "\d wa_outbox"` showing both columns with exact types/nullability/no default, runner history carrying NNN, and a fixed pre-migration cohort (a recorded set of outbox ids + their `status`) unchanged — never equality of a changing live total.
- **PR B2.3b — the carrier and the writer.** `CodexLegResult` carries the sealed evidence label, score and opaque package reference from the parsed sealed evidence (`wa_codex_leg.py:598-607`) into completion; the outbox worker persists them under the existing fence at the terminal write; `wa_finalize` remains free of DB writes; scripted/unscored routes keep NULL. Tests with an isolated DB and a stubbed sender: generated-supported (label false → `abstained_at` NULL, `evidence_score` set), generated-abstain-label (label true → both set), retry, stale claim, takeover, failed send (nothing persisted as a send), discarded completion, scripted greeting (both NULL). **Bites:** consumer = B3's battery query (`SELECT abstained_at, evidence_score FROM wa_outbox WHERE id = ANY($1)`); observation = the isolated-DB tests on this head plus, after deploy, the container grep of the carrier fields — live telemetry stays UNPROVEN until B3's first authorized supported and abstained cases correlate the persisted fields with their sealed package references, terminal status, delivery acknowledgement and tester verdict. No production send is added to B2 as a probe. A PENDING-ARMS row "`abstained_at`/`evidence_score` rows inspected at B3 open" is added in B2's ledger-only PR, so the record is read by someone before B3 rather than assumed.

## 5. Team

- **BLUE:** Dux and release owner (Opus 5 `xhigh`); implementer Sonnet 5 pinned; supports Haiku 4.5.
- **Council, per Gear-3 PR (B2.1, B2.2, B2.3a, B2.3b each carry their own candidate-bound council evidence — the migration floor is 3 by itself):** the canonical panel of `.claude/skills/modus/SKILL.md` — Gemini (`agy`) constructive, Codex (`gpt-5.6-sol`) red-team, Kimi (`kimi-code/k3`) refuter — dispatched the moment a frozen diff exists; findings disposed in the PR body BEFORE the fresh gate reads the candidate and all three reviews. R9's qualifying seats are Codex and Kimi (`scripts/evidence_pack_lint.py` counts `codex-gpt-5.6-sol`, `kimi-code/k3`, `tp1-qwen3.8-max`; GLM does NOT count); reviews recorded through `scripts/council_journal.py` with `role=review`, `ok=true` and a timestamp; `council_run` resolves inside that PR's evidence pack; journal and complete evidence lint checked BEFORE arming.
- **External builder lane, mandatory and bounded:** GLM (`tp1-glm-5.2`) ONLY builds the retrieval harness (B2.2) or the carrier tests (B2.3b), prepare-only, own worktree, verified by the Dux. Kimi never builds: it is the panel's mandatory refuter and one family cannot both contribute and refute (modus SKILL.md:149). Gemini, Codex and Kimi stay independent reviewers outside the contribution chain.
- **Final on-disk gate:** a FRESH Opus 5 `xhigh` session outside the chain, commissioned by this top-level Dux, sequential after the council, never counted as a council member; it signs and posts `harness/fable-gate` on the real head sha (`max` only for a declared adjudication).

## 6. Appetite and stop-loss

- **Budget:** 7 h, 2 adversarial rounds per PR + the council on each Gear-3 PR, 2.5M tokens; declared in `brief.yml`, spent in `pack.yml`; one absolute root-mandate deadline through `infra/codex-hooks/mandate_budget.py`, inherited by children; one renewal by the staff room, never resetting attempts or reservations; continuation hops ≤1; adapter ceiling declared; ship reserve zero.
- **Limits:** as B1; a metric that needs a weakened tripwire → REWORK-DESIGN, never a green by subtraction.
- **Checkpoints:** keys `B2-checkpoint`/`B2-ack`, same delivery rule; at: wave-2 conditions verified; B2.1 armed; B2.2 armed; B2.3a applied and probed; B2.3b armed; deploy content verified (the wave-3 trigger, together with RC2/RC3 receipts).
- **Stop and escalate if:** any cell of (b) or (c) cannot reach 0 without relabelling (adjudication with the staff room, never a relabel by the Dux); the validation set or the live sample disagrees with the mandatory set (suspend for adjudication); the support signal cannot decide a counterfactual pair; B2.1(g) cannot pass under unchanged D8 (adjudication, never a policy edit); the migration would touch a row; any path still hands the scorer an unlabelled score; the sample would need a paid or generating call, or — with authorization recorded — its vector artifact is missing or invalid (authorization absent is case (ii) of B2.2: recorded as BLOCKED, never a stop).

## 7. Evidence and release

As B1 §7 (`brief_ref` = `evidence/brief.yml`; `Bites:` on every PR; bare `--auto`; gate receipt on the real head), with: merge order B2.1 → B2.2 → B2.3a → (applied + probed) → B2.3b → ledger-only PR; each follow-up from a fresh origin/main; rollback of the metric = revert PR from fresh origin/main; rollback of the record = revert B2.3b only, columns and migration history retained; branch alive until content-on-main is verified blob-per-file.

**B3 (queued):** its acceptance contract is README §B3 (Astra's protocol); the file is written at B2's close, after Zero's queue items 2–4 are answered and the runtime receipts of RC2, RC3 and B2 are recorded.

## Adversarial review

Seat `codex` (`gpt-6-astra`, `model_reasoning_effort=xhigh`, read-only sandbox), four rounds on the whole packet; verdicts, bound sha256 and dispositions in `README.md` §Adversarial review. Findings that landed in THIS file: round 1 — `context_length` corrected and delivery outcomes reported separately (D8), B2.3 split into schema (B2.3a, `migrations_v2`, previous-image deploy order) and carrier/writer (B2.3b, `CodexLegResult` + fenced worker write), `abstained_at` semantics and a Bites that cannot pass with a dead writer, B2.2's vector-only harness, RC2/RC3 runtime receipts as gates, the Gear-3 council with R9's qualifying seats, migration release moved off Zero's queue; round 2 — 17 (delivery acceptance criteria with frozen dispositions and the positive control), 19 (the tax/visa defaults live at `reasoning_utils.py:690`), 20 (GLM-only builder), 21 (`migration_manager.py:315-327`); round 3 — 23 (queue item 7's three cases made explicit in §1, §4 B2.2 and §6), plus the positive control's real line (`test_wa_finalize.py:521`). All applied in v4; round 4 (Astra, ACCEPT at the v4 hashes) raised no finding against this file; surviving objections: none survived, 12 raised. The README §Adversarial review carries every verdict; the re-bind on the shipped bytes is recorded in `evidence/2026-09/agent-nuzantara-docs-bot-staff-room-c41145a4/codex-round-4.md`. Council read of v5 (Gemini 3.1 Pro, Kimi K3 — README §Adversarial review), applied here in v6: G1 exception 3, G2 input wiring and the async consumer, G4 the spec pairs, K3 the (g) mechanism, K4 the perimeter decision, K5 pins, the pre-B3 ledger row; G3 (weaken (g)) rejected. Astra round 5 on v6, applied here in v7: 25 the two (g) tracks, 26 the Terminal route, 28 the `wa_finalize` pins.
