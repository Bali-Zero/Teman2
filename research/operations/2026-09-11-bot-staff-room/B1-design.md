---
title: "B1 — DESIGN · the score contract, the frozen benchmark and the support-signal design · battle window"
date: 2026-09-11
adversarial_review: codex
---

# B1 — DESIGN · the score contract, the frozen benchmark and the support-signal design (v6)

| Field | Value |
|---|---|
| Mandate id | **B1** |
| Colour | **BLUE** (undeclared → BLUE). Chosen before the window opens; no fallback between colours. |
| Dux role | Opus 5 `xhigh` on BLUE, appointed by the staff room (Fable 5.1 + Astra, packet approved at a named hash — README §Adversarial review). Dux and release owner. |
| Worktree / branch | `agent/nuzantara/backend-rag/b1-score-contract`, created by the command in §1 |
| Wave | 1, alone on the scorer's and package's paths; RC2/RC3 run alongside on disjoint files |
| Opening | BLUE: Zero opens a fresh `claude --model claude-opus-5` window at effort `xhigh` on Pro and pastes the **Opening prompt** below. The first message restates colour, Dux role, mandate id and worktree path, then executes. |

**Opening prompt (paste verbatim):**

> You are the Dux of mandate **B1**, colour **BLUE**, Opus 5 `xhigh`, on Pro. Read `research/operations/2026-09-11-bot-staff-room/README.md` and `research/operations/2026-09-11-bot-staff-room/B1-design.md` from disk in `/Users/nuzantara/nuzantara` (if the files are not on this checkout, ask the staff room for them; do not proceed from memory). Load the `bot` corner and `modus`. State colour, Dux role, mandate id and worktree path, then execute §1–§7 of B1-design.md. Staff-room window for checkpoints: `<Zero names it at open>`. You do not touch RC2/RC3 paths, the corner, `zantara_core.py`, `wa_codex_leg.py`, any migration, any prod collection, and you make no embedding, generation or paid call — with exactly two exceptions written in §2 and §4: B1.4's synthetic classification probes through the local Ollama seat or the Codex CLI subscription seat reached ONLY through the unchanged `CodexExecClient.generate` adapter (§2), and B1.5's single embedding batch ONLY if Zero's authorization (README queue item 7) is recorded, capped at 40 provider attempts including retries (§4).

## 1. Mandate

B1 · BLUE · **organ: the evidence-score contract** — the number that travels producer → `result_formatter`/reranker → source projection → `wa_package_builder` → `calculate_evidence_score`; the benchmark that judges the scorer; and the design of the support signal the scorer lacks. **Gear:** 2 expected for B1.1–B1.3 (CI's floor wins; tests + additive fields, no behaviour change); B1.4 is a docs/measurement PR. **Host:** Pro.

**Objective:** by the end of this window (1) every source score that reaches the scorer carries a `score_kind` and, where one exists, its raw pre-transform value, on every path, and the sealed package proves the scorer receives the same provenance whose bytes were sealed; (2) the eleven historical tripwires are inventoried and the pipeline-real variants are ADDED beside the preserved originals, assertions untouched; (3) a frozen mandatory benchmark (≥28 cases, labels never selecting inputs) reports false abstention and false acceptance per (query language × context language × stratum) with positive denominators; (4) the support-signal method is designed and measured offline on counterfactual pairs, so the staff room can choose it before B2 opens. **What success changes:** B2 can invert the metric against a benchmark that measures sufficiency, with a support signal that exists, and nobody can again argue about cosines a field never carried.

**Worktree:** `cd ~/nuzantara && WT=$(python3 scripts/agent_start.py --lane backend-rag --task-id b1-score-contract | awk '/^WORKTREE_READY /{print $2}') && echo "$WT"`. The broker prints one `WORKTREE_READY` line and cannot change your cwd; every later command is `git -C "$WT" …` or `cd "$WT" && …`. **Base sha:** record `git -C "$WT" rev-parse HEAD` at open. Tests: `cd "$WT/apps/backend-rag" && PYTHONPATH=. SKIP_SENTRY_INIT=1 .venv/bin/python -m pytest <files>` (the repo config already sets verbosity; do not add `-q`). Load discipline: `sysctl -n vm.loadavg` before any run wider than one file and before every push; 1-min > 8.0 → wait 120 s, re-check (≤10×). **Budget:** record the absolute root-mandate deadline through `infra/codex-hooks/mandate_budget.py` at open (§6).

**Ground** (Astra 01:36 WITA + round 1; Fable 17:3xZ; re-run all of it on your base sha — line numbers move):
- The transform chain: `apps/backend-rag/backend/core/qdrant_db.py:1309` (RRF requested), `:1335` (`distance = 1 − returned_score`), `:1362` (internal dense fallback); `backend/services/misc/result_formatter.py:83` (consumes distance), `:89` (`1/(1+distance)` then boosts); `backend/services/search/search_service.py:1158` (formatter on hybrid), `:1142` (default `search_type` can mislabel a fallback), `:1100-1103` (cached results returned BEFORE formatting — legacy cache entries carry no provenance), `:1187-1190` (hybrid path writes the cache); `backend/services/rag/agentic/tools.py:279-288` (reconstructs source dicts, keeps `score`, drops everything else); `backend/core/reranker.py:169-180` and `backend/services/rag/reranker.py:345-352` (overwrite `score` with reranker output, keep the previous value as `vector_score`); `backend/services/rag/agentic/wa_package_builder.py:289` (score forwarded without provenance), `:434-453` (the digest hashes full chunks AND `evidence_inputs`), `:527-548` and `:599-604` (`context_length` = sealed chunks INCLUDING synthetic curated content, EXCLUDING the separate pricing block), `:529` (synthetic 1.0 for curated), `:595` (both into the scorer); `backend/services/rag/agentic/_reasoning_evidence.py:356-381` (trusted-tool 0.85 bypass beside the ordinary scorer call); `backend/services/rag/agentic/orchestrator_core.py:428-440,1084-1103` (FAQ and KG fast paths bypass the ordinary scorer — they are bypass contracts, not numeric-score writers); `backend/services/rag/agentic/reasoning_utils.py:214-699` (`calculate_evidence_score`; source bands 0.7/0.5/0.3/0.15; the "cosine" penalty `0 < top < 0.5 and final_score > 0.15 → ×0.7`, strict, `:665`).
- Formatter probe, no boost (Astra, twice): raw 0.000 → 0.5000 · 0.038 → 0.5097 · 0.155 → 0.5420 · 0.373 → 0.6146 · 0.900 → 0.9091. RRF under the installed client's LOCAL mode (`apps/backend-rag/.venv/lib/python3.11/site-packages/qdrant_client/hybrid/fusion.py:4-11`, `ranking_constant = 2`): `1/(2 + zero_based_position)` per contributing list; first-in-one-list 0.5 → formatted 0.6667, first-in-both 1.0 → formatted 1.0. The in-repo helpers (`backend/services/rag/hybrid_search.py:32` `RRF_K = 60`, 1-based; `backend/services/search/search_service.py:897-916`) are NOT the hot path: production fusion is server-side (`core/qdrant_db.py:1309`). **Your FIRST artifact is a MEASURED table** of rank ↔ returned score from the running Qdrant server — a read-only `query_points` on an existing collection using existing point ids as the query (no embedding call, no write, no client text exported: only ranks, list membership and returned scores) — reconciled against the local formula above; if they differ, the measured table wins and every B1.1(c) value is derived from it, never from the local client.
- The eleven historical tripwires: recover them from #5618's history (`gh pr view 5618 --json body,commits`, the withdrawn candidate commits on `agent/nuzantara/ops/bot-wa-evidence*`, its brief `evidence/2026-09/agent-nuzantara-ops-bot-wa-evidence-e34da839/brief.yml:26-31` which records only aggregate 11/9 counts, and any CI/pytest receipt). A score-literal grep (17 matches of `"score": 0.[89]` across `test_evidence_scoring_abstain.py`, `test_reasoning_utils.py`, `test_confidence_scoring.py`) is NOT that inventory.
- Golden set: `test_evidence_cross_language.py:47-56` (contexts), `:183-187` (labels), `:195` (per-language rate test), `:207-212` (the label SELECTS the context — the defect D2 removes), the two strict xfails at `:241` and `:260`.
- Counterfactual pair (Astra, round 1): same query, source score 0.9, context WITH the capital amount vs context saying it is unspecified → scorer 0.8 / 0.8. That pair, and its siblings, are B1.4's measurement set.

## 2. Owned perimeter

- **Writable, additive only** (no existing field renamed or removed, no numeric value changed, no served behaviour changed): `backend/services/misc/result_formatter.py`, `backend/services/search/search_service.py`, `backend/services/rag/agentic/tools.py` (the source projection at `:279-288`), `backend/core/reranker.py` and `backend/services/rag/reranker.py` (the `score` writers), `backend/services/rag/agentic/wa_package_builder.py` (projection at `:289`, curated minting label at `:529`, scorer call at `:595`, and the digest inputs if provenance lives in `evidence_inputs`), `_reasoning_evidence.py` ONLY to label the 0.85 trusted-tool bypass with its own `score_kind` (no value change); every test file under `backend/tests/**` that pins the scorer or the package; the new benchmark files and their reporting harness; B1.4's design note under `research/operations/2026-09-11-bot-staff-room/`.
- **Read-only:** `reasoning_utils.py`, `_abstain_policy.py`, `orchestrator_core.py` (its FAQ/KG bypasses are DOCUMENTED as contracts in B1.1's inventory, not edited), `wa_finalize.py` (its delivery policy is D8's). If the contract work proves a behaviour change unavoidable, stop and checkpoint: it belongs to B2.
- **Forbidden:** RC2 paths (`scripts/reingest_training_data.py`, `apps/backend-rag/scripts/curated_qa_harvest.py`, prod Qdrant), RC3 paths (`backend/prompts/zantara_core.py`, `docs/plans/2026-09-03-relaunch-lanes/ZERO-DECISIONS.md`), `wa_codex_leg.py` (#5337 / B2.3b), the corner, `fly.toml`, `.env*`, any migration, any prod write, any WhatsApp send, any embedding / generation / reranking API call, any paid call — **two narrowly scoped exceptions:** (1) B1.4's synthetic classification probes may run through the local Ollama seat (`ollama run …` on Pro/Mini, no egress) or the Codex CLI subscription seat — the latter ONLY through the unchanged WA adapter `backend/llm/codex_exec_client.py::CodexExecClient.generate`, called from the offline probe harness, so the probe inherits the adapter's complete fixed argument prefix (`codex exec --sandbox read-only --skip-git-repo-check --ephemeral --ignore-user-config --ignore-rules -m <model> -`, `:35-36`), its fresh empty working directory (`tempfile.mkdtemp(prefix="codex-exec-wa-")`, `:1089`), its minimal child environment (`_build_env`, `:975`) and its stdin-only prompt transport; a hand-typed `codex exec …` line is NOT this exception, and if the adapter or any of its controls is unavailable the candidate is recorded UNAVAILABLE — nothing authorizes a looser invocation. Flat subscription, never an API key; benchmark text only; invocation, model/rubric identity, timeout and failure handling written in the B1-4 note. (2) B1.5's embedding batch, only after Zero's explicit authorization is recorded in README queue item 7 (the line `Item 7: yes — <date>`), ceiling **40 total provider attempts INCLUDING retries** (the product's embedding path builds `AsyncOpenAI` without overriding retries, `backend/core/embeddings.py:271`; the installed SDK defaults to `max_retries=2`, so 40 logical calls could become 120 attempts), synthetic queries only, run once under the pre-call record of §4 B1.5.
- **Bans:** no Claude model through a paid per-token Anthropic endpoint; no credential echoed; no client PII anywhere (every benchmark case is synthetic and says so in a field; B1.4's offline measurement uses only benchmark text).

## 3. Sibling contract

Frozen before BUILD; changes go to the staff room through Zero.

- **RC2/RC3 (Fable window, in flight):** disjoint files by construction (§2). B1's follow-up PRs start from a fresh origin/main after any sibling merge; nothing to coordinate on content.
- **#5337 (suspended):** untouched; its historical veto corpus (eleven sentences) and #5615's checked-in fixtures (`backend/tests/unit/services/test_curated_qa_government_fee_gate.py:26-112`, review marker `:156-203`) may be READ as sources of benchmark negatives under the `fee_policy` property, never edited; fee-policy compliance is a separate report column from sufficiency.
- **B2 consumes B1:** the `score_kind` vocabulary, the producer-to-consumer inventory, the contract table (numeric value per path), the mandatory benchmark manifest (contexts, labels, band boundaries, baseline results) and B1.4's chosen method are FROZEN at B1's last merge: sha256 of the manifest file and of the contract table in `pack.yml`. B2 supplements live in a separate file; B2 never relabels.
- **The ledger** (`.claude/skills/modus/PENDING-ARMS.md`, `merge=union`): rows only in one final ledger-only PR from a fresh origin/main, opened when no other open PR touches the file, diff +N/-0; `Bites:` consumer `scripts/pending_arms_report.py`, observation `python3 scripts/pending_arms_report.py --json --ref origin/main` listing the new rows after merge.
- **Freeze:** once armed, the branch is read-only. Real DIRTY → close, successor from fresh origin/main by cherry-pick, push/create/arm as three commands. Phantom DIRTY → judge the diff, let the queue cure it. Never `--disable-auto` → merge → push → re-arm.

## 4. Acceptance

- **PR B1.1 — the contract, pinned end to end.** (a) A producer-to-consumer INVENTORY (a table in the PR body and in `pack.yml`) covering hybrid RRF, dense fallback, both rerankers, curated injection, the search cache, trusted-tool evidence (`_reasoning_evidence` 0.85) and the FAQ/KG bypasses: for each, where `score` is written, what it numerically is, whether it reaches `calculate_evidence_score`, and its `score_kind`. (b) Every writer emits `score_kind` (vocabulary fixed in the PR: e.g. `hybrid_rrf_formatted`, `dense_formatted`, `reranked`, `curated_synthetic`, `kg_entity`, `trusted_tool_bypass`) and the raw pre-transform value where one exists; cached or legacy values without provenance are labelled `unknown` — a kind is NEVER inferred from numeric magnitude. (c) Tests cross producer → formatter/reranker → source projection → package construction → DLP/capping → canonical serialization → scorer input, and assert the exact numeric `score` and `score_kind` that reach the scorer for: rank-1 hybrid (one list / both lists), rank-5 hybrid, dense fallback at cosine 0.0 / 0.5 / 0.9, a reranked source, curated synthetic 1.0, one collection boost, one `unknown` cache hit. Negative: a dense fallback without provenance is labelled, never silently `hybrid_rrf` (a test that fails on origin/main). (d) The PR states where provenance lives (hashed chunks, `evidence_inputs`, or both), whether the sealed digest changes, and pins compatibility with in-flight packages (check `wa_finalize` and the outbox for rows mid-flight before merge; a digest change with rows in flight is a STOP). (e) Numeric behaviour unchanged: the six innocence files stay at their origin/main counts. **Bites:** consumer = `wa_package_builder.build_context_package` → `calculate_evidence_score` (the scorer's input now carries `score_kind`); observation = the crossing test module green on this head, plus a printed real package from the fixture path showing every source with its kind, plus the digest-compatibility statement. May be split by producer file if it exceeds ~400 net lines; the split is declared in the brief.
- **PR B1.2 — the eleven, inventoried and re-based by addition.** The exact eleven node IDs, the nine re-base candidates, the two other regressions and the withdrawn candidate SHA (or an immutable failure receipt) are listed; anything unrecoverable is marked "unresolved", not guessed. Each of the nine ORIGINAL fixtures is PRESERVED as it is; beside each, a pipeline-derived VARIANT is ADDED that specifies provider/version, contributing ranks, list membership, fusion configuration, raw score, fallback path, formatter transform, boosts and rounding (one comment line naming the B1.1 inventory row). Assertions byte-identical (a script diff of the assertion lines is empty and its output is in the PR body). A guard test fails if any tripwire fixture declares a source `score` without a `score_kind` from the frozen vocabulary. **Bites:** consumer = CI `Backend Tests (Python)` shards; observation = before/after counts on the touched files, both green, and the guard test red when an unlabelled value is re-introduced (shown in the PR body).
- **PR B1.3 — the benchmark, frozen.** Two files: the MANDATORY manifest (frozen) and an empty SUPPLEMENT file for B2. Each case stores: `case_id` (opaque), `query`, `query_lang` (EN/ID), `context` (the actual text), `context_lang`, `provenance_fixture` (a B1.1 inventory row + raw value), `requested_fact`, `supporting_span` or `missing_fact_explanation`, `stratum` ∈ {sufficient, relevant_insufficient, irrelevant, generic_overlap, company_prefix_chunk} with the documented precedence, nuisance properties (`generic_overlap`, `company_prefix`, `fee_policy`) as separate booleans, `expected_generation_gate`, `expected_label_gate`, `synthetic: true`. Cases derived from the golden set's four EN/ID topic pairs (all-in price, NIB/OSS requirements, minimum paid-up capital, registration duration — `test_evidence_cross_language.py:179-190`) carry `spec_pair: <topic>`: these are "the four spec pairs" B2 §4 B2.1(a) measures. **Labels never select inputs.** The three current golden rows whose contexts lack the fact keep their ORIGINAL contexts under `relevant_insufficient`; enriched sufficient counterparts are ADDITIONAL cases. **B1.3 also RECORDS, without activating, the independently reviewed correction of those three golden expectations** (`test_evidence_cross_language.py:183,186-187` say `True`; `:207-212` lets the label pick the context; `:216` forces acceptance): the corrected expectations and the fixed-input replacement cases are written in the manifest and marked `activates_in: B2.1`; the checked-in test stays untouched in B1 so B1 remains behaviour-preserving (README D5's one exception). Balance: per EN/ID query × EN/ID context combination ≥3 sufficient and ≥1 per negative stratum → ≥28 cases, every required cell with a positive denominator; nuisance properties also appear on sufficient cases. The harness reports per cell: false abstention count/denominator and false acceptance count/denominator, for the generation gate and the label gate separately, on the mandatory set and on an independently selected validation set (drawn by the adversarial seat, not by the Dux). The two strict xfails keep their names. The harness asserts nothing new about the scorer in B1; its numbers on origin/main are the baseline, recorded in the PR body and `pack.yml`. Independent label review by the adversarial seat, case by case, BEFORE the freeze; the manifest's sha256, the band boundaries and the baseline hash go in `pack.yml`. **Bites:** consumer = the reporting harness shipped in this PR; observation = its report on this head with every required denominator, the fixture hash and the baseline result. B2 is its downstream user.
- **PR B1.4 — the support-signal design note** (`research/operations/2026-09-11-bot-staff-room/B1-4-support-signal.md`, adversarially reviewed): what checks that the retrieved text CONTAINS the requested fact; candidates measured offline on ≥8 counterfactual pairs from the manifest (same entity, language, provenance and rank; fact present vs absent), each candidate with inputs, failure behaviour (what happens when it cannot decide), consumer (where in the package/finalize chain it is read), cost/latency and PII posture. Candidates to measure at minimum: (i) a deterministic span check on `requested_fact` (expected to fail on paraphrase — measure it anyway); (ii) a local model judge on Pro/Mini (`ollama`, no egress, $0) answering "is the requested fact stated in this context?"; (iii) the Codex seat already used by the WA leg (flat subscription), reached ONLY through the unchanged `CodexExecClient.generate` adapter under §2's exception (1) — same fixed argument prefix, fresh empty cwd, minimal env, stdin prompt — as a sufficiency autorater with a fixed rubric; an unavailable adapter or control makes the candidate UNAVAILABLE, never a looser invocation; (iv) "no support signal, scorer as today" as the control (already measured at 0.8 / 0.8 — it is expected to FAIL and is recorded, not excluded). **Selection contract:** measure every candidate, including failures and the control; the SELECTED method must correctly classify BOTH members of every one of the ≥8 independently labelled pairs; an unknown/unavailable result is never counted as successful discrimination and must stay distinguishable from "supported" in the method's output; the method receives only inputs obtainable at runtime — never expected labels, supporting-span annotations or case IDs — and any requested-fact extraction it needs is part of the measured method, not a hand-supplied field. Each LLM-judge candidate ((ii), (iii)) runs at its production-intended decoding settings, pinned in the note (model, temperature, seed where available), THREE repetitions per pair; any disagreement across repetitions on a pair marks the candidate UNSTABLE on that pair, and an UNSTABLE pair counts as not distinguished. For candidate (iii), any production-bound design consumes the REDACTED query and the capped REDACTED context (`wa_package_builder.py:576` redacts package fields; the scorer at `:597` still receives the original `query` — that raw argument and the reversal map are never sent to the seat); outputs and receipts stay synthetic/redacted. Each candidate's invocation, model/rubric identity, timeout and failure handling are written in the note. Acceptance: a table (candidate × pair → decision, latency, cost) and a recommendation; the staff room chooses (README §Sequencing (e)); no qualifying candidate → §6's stop (B2 cannot open; the staff room redesigns). No production call, no send, no client text.
- **PR B1.5 — the approved query/vector artifact for B2.2 (conditional).** Opens ONLY if README queue item 7 is answered yes and recorded. One bounded batch (OpenAI `text-embedding-3-small`, the product's own embedding path, synthetic EN/ID tax/visa queries drawn from the manifest; ceiling **40 total provider attempts including retries** — the batch harness constructs the client with `max_retries=0` or counts every transport attempt against the ceiling and stops at 40) produces an immutable checked-in artifact with: artifact sha256, the exact synthetic-query → vector mapping, model/dimension provenance, language coverage, the approval reference (queue item 7 + date), the number of provider attempts made and the handoff location. **Pre-call record:** BEFORE the first call the harness persists, in the evidence directory, the authorization reference, the sha256 of the selected query list and a batch-start receipt (timestamp, host, base sha); completion appends the attempted-call count and the artifact sha256. A start receipt without a completion, or any partial or ambiguous prior execution, STOPS the Dux for staff-room resolution — a second batch is never started silently. Existing canary vectors (`scripts/.rag_canary/embedding_baseline.json`, untracked, reference texts) may be reused only for their exact associated texts after explicit selection, never relabelled as query embeddings; `rag_canary.py`'s `save_baseline` is not run. If item 7 is declined or still unanswered at B1's close, B1.5 is skipped with a ledger row and B2.2's live sample stays blocked. B1.5 first verifies on Pro's main checkout that `scripts/.rag_canary/embedding_baseline.json` exists (untracked: absent from every worktree) before deciding whether any canary vector is reusable. **Bites:** consumer = B2.2's retrieval harness (it aborts on a missing vector); observation = the artifact's sha256 in `pack.yml` and a test that loads it and verifies dimension and mapping without any network call.
- **Innocence, every PR:** the six files Astra's coordinator ran (`test_evidence_cross_language.py`, `test_evidence_scoring_abstain.py`, `test_wa_package_builder.py`, `test_wa_greeting.py`, `test_curated_qa_government_fee_gate.py`, `test_wa_finalize.py`) at their origin/main counts or better, and the full `backend/tests/services/rag` + `backend/tests/unit/services/rag` suites green in CI.
- **Production observation:** none is claimed for behaviour. Each merge deploys (`apps/backend-rag/**`); the Dux proves content only (`fly ssh console -a nuzantara-rag --machine 1781e5eda03438 -C "grep -c score_kind /app/backend/services/rag/agentic/wa_package_builder.py"` ≥ 1 after B1.1, `rag` machine STARTED) and states the digest change, if any.

## 5. Team

Every seat from the colour table (`docs/architecture/dual-consul/army-map.md` §1bis).

- **BLUE:** Dux and release owner, this window (Opus 5 `xhigh`). Implementer Sonnet 5 pinned in every Agent call (`model: "sonnet"`); supports on Haiku 4.5. Adversarial reviewer: an independent Codex seat outside the chain (`codex exec --sandbox read-only -c model_reasoning_effort=high … < /dev/null` from the worktree), reading the frozen diff; for B1.3 it reviews the labels case by case and draws the validation set; for B1.4 it refutes the recommendation. If Codex is dead: `~/.kimi-code/bin/kimi -p … -m kimi-code/k3` — available only for a candidate Kimi did not contribute to (family exclusion, modus SKILL.md:149); since the external builder below is GLM, Kimi never contributes here; if both seats are dead, say so and suspend — a seat that did not run is not a seat that agreed. Final on-disk gate: a FRESH Opus 5 `xhigh` session outside the chain, commissioned by this top-level Dux; it only signs and posts `harness/fable-gate` on the real head sha.
- **External builder lane, MANDATORY and bounded** (`army-map.md:108`, multi-PR campaign): B1.2's mechanical variant-addition goes through GLM (`tp1-glm-5.2`) ONLY, prepare-only, own worktree, verified by the Dux; Kimi never builds, so both the Codex reviewer and the Kimi fallback stay cross-family and outside the build chain.
- Record each seat's model, effort and thread id in `brief.yml`.

## 6. Appetite and stop-loss

- **Budget:** 5 h, 2 adversarial rounds per PR, 1.5M tokens, declared as `appetite:` in `brief.yml`; `spend:` in `pack.yml`. One absolute root-mandate deadline recorded through `infra/codex-hooks/mandate_budget.py` at open; children and continuations inherit the remaining time; a renewal (one, by the staff room) never resets attempts or reservations; continuation hops (≤1) and the adapter tool ceiling declared at opening; ship reserve zero; a capped child checkpoints and returns.
- **Limits:** three reds for one cause → suspend; fix-of-a-fix depth 1; at most 3 children, depth 1.
- **Checkpoints:** publish `scripts/fleet_mail.sh local broadcast --key B1-checkpoint --ttl 24 "$STATE"`, then `SendMessage` the same text with the envelope id to the staff-room window Zero named; ack key `B1-ack` within 15 min quoting the id; one retry; then `BLOCKED: undelivered` and suspend. Checkpoint at: base sha + deadline recorded; B1.1 armed; B1.2 armed; B1.3 frozen + armed; B1.4 reviewed; all merged + Pro pulled + sha256 frozen (the wave-2 trigger).
- **Stop and escalate if:** the contract cannot be pinned without changing a served value; the digest changes with packages in flight; a benchmark label cannot be decided from the context alone (adjudication, never a coin flip); any fixture turns out to carry real client text; B1.4 finds no candidate that distinguishes the pairs (then B2 cannot open and the staff room re-designs).

## 7. Evidence and release

- **Evidence:** `cd "$WT" && python3 scripts/ci/evidence_paths.py --ref "$(git rev-parse --abbrev-ref HEAD)"` names the directory. `brief.yml` (gear, appetite, team with thread ids, the four timestamped sibling outputs: open PRs' paths, `python3 scripts/agent_start.py --list`, `ListAgents`, `scripts/fleet_mail.sh local --list`; `brief_ref` = `evidence/brief.yml` — `.github/workflows/harness-floor.yml:983` stages pack+brief into a canonical path and lints with that as repo root, so a per-PR `brief_ref` passes locally and fails in CI); `pack.yml` with spend, the inventory, the contract table, the manifest sha256 and the baseline. A bare 40-hex `base_sha` trips `Detect Secrets`: mark it the way briefs on origin/main do (grep `pragma` under `evidence/2026-09/`). **Every PR** carries `Bites:` (§4).
- **Release**, three separate commands per PR: `git -C "$WT" push -u origin HEAD`; `cd "$WT" && gh pr create --title "$TITLE" --body-file "$BODY"`; `gh pr merge "$PR" --auto` at once (bare `--auto`; the queue rejects strategy flags — `docs/runbooks/merge-queue-discipline.md:274-278`). At Gear 2+ the `harness/fable-gate` success must sit on the real head sha before `Harness floor recompute` turns green; the Dux commissions the fresh gate and never posts a verdict on its own diff.
- **Gate receipt:** a PR comment with mission id, colour, HEAD sha, gate thread id, commands with exit codes, verdict; then `python3 scripts/harness_fable_gate.py --verdict PASS --sha "$HEAD" --description "$RECEIPT"` by the gate session (PASS-WITH-CONDITIONS needs `--conditions-ref`).
- **Merge order:** B1.1 → B1.2 → B1.3 → B1.4 (docs) → B1.5 (only if authorized) → ledger-only PR. Each follow-up from a fresh origin/main after the previous merge; after each backend merge watch the release (`fly status -a nuzantara-rag`, `rag` machine STARTED).
- **Rollback trigger:** any served-behaviour change observed (a digest mismatch that breaks `wa_finalize`, or a changed abstain decision on the six innocence files) → revert PR from a fresh origin/main; the branch stays alive until content-on-main is verified blob-per-file.

## Adversarial review

Seat `codex` (`gpt-6-astra`, `model_reasoning_effort=xhigh`, read-only sandbox), four rounds on the whole packet; each round's verdict, the sha256 it bound to and the disposition of every finding are recorded in `README.md` §Adversarial review. Findings that landed in THIS file: round 1 — the D4/B1.4 support signal and its counterfactual pairs, D3 inventory-not-grep and "rank does not determine value", the provenance perimeter (`tools.py:279-288`, both rerankers, the search cache, the trusted-tool 0.85 bypass, the FAQ/KG bypass contracts), the benchmark schema with ≥28 cases and labels that never select inputs, the budget mechanism; round 2 — 15 (selection contract, probe exception, redacted inputs for candidate iii), 16 (the three known-wrong golden expectations recorded in B1.3, activated in B2.1), 18 (the approved vector artifact → conditional B1.5), 20 (GLM is the only external builder; Kimi never builds), 21 (`harness-floor.yml:983`); round 3 — 22 (the Codex probe goes ONLY through the unchanged `CodexExecClient.generate` adapter), 24 (40 total provider attempts including retries; pre-call record). All applied in v4; round 4 (Astra, ACCEPT at the v4 hashes) raised no finding against this file; surviving objections: none survived, 12 raised. The README §Adversarial review carries every verdict; the re-bind on the shipped bytes is recorded in `evidence/2026-09/agent-nuzantara-docs-bot-staff-room-c41145a4/codex-round-4.md`. Council read of v5 (Gemini 3.1 Pro, Kimi K3 — README §Adversarial review), applied here in v6: G4 `spec_pair`, K1 the measured fusion table as the first artifact, K5 pins, K6 determinism contract, B1.5 "declined or unanswered" and the canary-presence check.
