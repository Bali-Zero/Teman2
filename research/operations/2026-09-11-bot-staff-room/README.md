---
title: "B — ZANTARA WA BOT · staff-room spec (Fable 5.1 + Astra) and the windows that finish engine and design"
date: 2026-09-11
adversarial_review: codex
---

# B — ZANTARA WA BOT · staff-room spec and battle windows (v6)

**Board:** successor of item 6 ("/bot — cliente e team") of the 2026-09-10 organism audit. Both imperators measured the same mandate independently on 2026-09-11: Fable 5.1 (Claude session `nuzantara-8c`, Pro) and Astra (Codex session opened 01:28 WITA on the Sol seat, switched to `gpt-6-astra`; its read-only child issued the score-contract BLOCKER at 01:36 WITA; its addendum lives in the worktree `.worktrees/backend-rag-bot-cycle-359-360/research/operations/2026-09-11-bot-cycle-359-360-revalidation.md`, branch `agent/nuzantara/backend-rag/bot-cycle-359-360`, prepare-only, nothing pushed). The measurements agree on every load-bearing fact. **v6** applies Astra's rounds 1–4 and the council read of v5 (Gemini 3.1 Pro, Kimi K3; §Adversarial review). Zero's rulings are in §0; his open items in §Zero's queue. Every window below is BLUE (undeclared colour); no colour fallback. **D1–D7 are proposed technical decisions pending approval of the corrected packet by both imperators; Astra's status: APPROVED at v5 (round 4 ACCEPT, round 4b re-bind — `evidence/2026-09/agent-nuzantara-docs-bot-staff-room-c41145a4/codex-round-4.md`); v6 carries the council read and awaits Astra's round 5 re-read and re-bind. `army-map.md:98-99` asks for both imperator approvals on the same packet hash (the Dux-appointment record): the hash-bound ACCEPTs and Fable's approval of identical bytes are those approvals. Zero's opening instruction remains required.**

## 0. What the staff room found, and decided

**Measured (re-measure before trusting; each line names who measured and where):**
- **Root cause 1 (the cross-language abstain gate) is HALF built, not "never started".** PR #5618 (`2cd3cf84b8b602afbc508c19fba8aafca9766ed5`, merged 2026-09-09) shipped the vocabulary-aware keyword filter in `apps/backend-rag/backend/services/rag/agentic/reasoning_utils.py::calculate_evidence_score` (`_SHORT_IDENTIFIERS`, whole-token matching, the legacy-ratio floor) and `backend/tests/unit/services/rag/agentic/test_evidence_cross_language.py`. Its cured evidence brief (`evidence/2026-09/agent-nuzantara-ops-bot-wa-evidence-e34da839/brief.yml`) records **122 passed / 2 xfailed**; the current file holds 124 parameterized cases including two strict xfails — `test_a_question_naming_no_identifier_is_still_language_blind` (`:241`) and `test_one_generic_word_should_not_be_evidence` (`:260`); "53 passed / 2 xfailed" was an intermediate result. The retrieval-primary inversion was built on that branch and WITHDRAWN: it regressed eleven standing abstain tripwires, nine of them fixtures whose `sources[].score` the PR called "impossible cosines". The board's "🔴 MAI iniziata" is stale. Historical execution is not a fresh run on any window's base.
- **Astra's BLOCKER (2026-09-11 01:36 WITA): the number the scorer receives is not a cosine.** Paths relative to `apps/backend-rag/backend/`: `core/qdrant_db.py:1309` requests RRF fusion and `:1335` stores `distance = 1 − returned_score` (`:1362` internal dense fallback); `services/misc/result_formatter.py:83` consumes the distance and `:89` computes `1/(1+distance)`, then optional collection boosts; `services/search/search_service.py:1158` uses that formatter for hybrid results and its default `search_type` at `:1142` can mislabel a dense fallback that omitted provenance; `services/rag/agentic/wa_package_builder.py:289` forwards the formatted score without provenance, `:529` mints a SYNTHETIC 1.0 for the curated block, `:595` feeds both to the same scorer; `reasoning_utils.py:650` describes the low-score discount as a cosine check. Formatter probe (Astra, synthetic inputs, no boost; reproduced in the round-1 reading): raw 0.000 → **0.5000**, 0.038 → 0.5097, 0.155 → 0.5420, 0.373 → 0.6146, 0.900 → 0.9091. **An unboosted raw zero arrives as 0.5, and the scorer's penalty fires only on `top < 0.5` (strict) and only when `final_score > 0.15` (`reasoning_utils.py:665`), so it does not fire there: fixing provenance is necessary and does not by itself repair that scoring behaviour.** The installed Qdrant client's local-mode RRF (`apps/backend-rag/.venv/lib/python3.11/site-packages/qdrant_client/hybrid/fusion.py:4-11`, `ranking_constant = 2`, `1/(2 + zero_based_position)` per contributing list) gives: first in one list → 0.5 → formatted 0.6667; first in both lists → 1.0 → formatted 1.0. That file is the package's, not the repo's: the two in-repo RRF helpers (`backend/services/rag/hybrid_search.py:32` `RRF_K = 60`, 1-based ranks; `backend/services/search/search_service.py:897-916`) are NOT the hot path, because production fusion is server-side (`core/qdrant_db.py:1309`, `{"fusion": "rrf"}`) and the server's constant is not verified from any file in this repo. The table above is therefore an expectation, not a measurement: B1.1's FIRST artifact is a measured rank ↔ returned-score table from the running server (B1 §1). **#5618's "real cosine 0.038 vs fixture 0.90" argument compared a raw embedding number with a field that never carries one.** Fable's own re-measurement lane (dux-rc1a, Opus 5) died on a session limit at 18:26Z before delivering; B1 reproduces the table itself (B1.1) and reconciles any difference explicitly.
- **The scorer cannot tell "fact present" from "fact missing" (Astra, round 1, executed via AST without app init):** same query, source score 0.9, one context supplying the requested capital amount and one explicitly saying it is unspecified → **0.8 and 0.8**. Neither retrieval rank, source authority nor topical overlap establishes sufficiency; that is the signal D4 demands and B1.4 designs.
- **The golden set measures topical affinity, not answerability, and its labels select its inputs.** `test_evidence_cross_language.py:183-187` labels three rows answerable (minimum capital, registration duration) whose contexts at `:47-56` contain neither fact; at `:207-212` the boolean label chooses the context, so flipping a label swaps the company context for `UNRELATED_CONTEXT` instead of testing relevant-but-insufficient evidence.
- **Two residual defects** (the coordinator of Astra's Codex session, baseline `be45266252f4e1ccbaf19d2b81c73f03af676e05`, `--runxfail`: 2 failed, 122 deselected; six test files 341 passed / 2 xfailed in 8.54 s — Astra's read-only child inspected the pipeline and ran no tests): a question naming no identifier is still language-blind (`"What is the price of new company setup?"` vs Indonesian context = 0.08); a generic-word overlap clears the gate (`"Berapa harga sewa motor di Canggu?"` vs a company chunk = 0.16 with `POOR_RETRIEVAL=[{"score": 0.18}]`).
- **Production, Fable's dated read-only measurements (`scripts/pg.sh`, 2026-09-10 17:3xZ, receipt in the `nuzantara-8c` transcript):** `wa_outbox.generation_route` since 2026-08-27 = `codex` 58 rows, NULL 19 rows, both ending 2026-09-01; last `status='done'` row **2026-09-01 09:27:53Z** (cycle 359's own battery). Absence of outbox rows is not an independently verified absence of all inbound traffic or channel activity. Container `rag` machine `1781e5eda03438`, release v4387 (2026-09-10T16:16Z): `grep -c _SHORT_IDENTIFIERS reasoning_utils.py` = 4 and `wa_greeting` present → #5618 and #5613 (`99207591bf83899283088dc8ac0ebfffc7c73620`) are DEPLOYED by content, unproven by traffic.
- **Two distinct corpora, never conflated:** #5337's historical veto measurement (eleven sentences: nine compliant one-price answers, two genuine splits — a SUSPENDED PR, `harness/fable-gate` failure, touching `wa_codex_leg.py`), and #5615's checked-in ingest-detector fixtures (`580b107a817f9dcd3e2d4dc3680ba5ef642f9aef`; `backend/tests/unit/services/test_curated_qa_government_fee_gate.py:26-112`: nine offenders, four no-figure compliant exemplars, three texts without government-fee mentions; the review-marker cases are `TestTheReviewMarker` at `:156-203`). The "27 compliant rows" of the corner is a historical population, not 27 checked-in fixtures. Fee-policy compliance is reported separately from factual sufficiency: an offender text is not automatically an insufficient-context negative.
- **Root causes 2 and 3 are decided (below); root cause 4 (greeting, #5613) is cured and stays as regression coverage.**

**RULED Zero 2026-09-11 (Fable window; saved in memory; do not re-request):**
1. Build order: root cause 1's missing half first; corpus and citation decisions execute in parallel.
2. DELETE the `curated_qa` government-fee offenders (seven id prefixes: `8b520434`, `eacec21f`, `8ebf681f`, `11b7e26d`, `12d804e9`, `1b53de60`, `e407d532`; resolved live, snapshot with vectors first).
3. DELETE the `training_conversations_hybrid` points that teach the PNBP-vs-fee split (the live strict-split set measured by lane RC2 is 22, not the corner's 8; the 8 was a stale count) and add #5615's detector to `scripts/reingest_training_data.py`.
4. RATIFIED the citation-rule replacement (`docs/plans/2026-09-03-relaunch-lanes/ZERO-DECISIONS.md` §Item 3) with one amendment (" from a client" dropped from case (a), for v5 client-audience purity: `test_zantara_core_v5.py::TestClientAudiencePurity`), applied to `apps/backend-rag/backend/prompts/zantara_core.py` by Zero's own commit (protected path; Gate 1 of `.husky/pre-commit` blocks agent commits by design).

Rulings 2–4 are IN FLIGHT under the Fable window (lanes `dux-rc2`, `dux-rc3`, own worktrees, branches `agent/nuzantara/backend-rag/rc2-split-corpus-purge` and `…/rc3-citation-rule-exit`); they are not windows of this board. Their FILES are disjoint from B1/B2; their RUNTIME EFFECTS are dependencies of B2.2 and B3 (§Sequencing).

**Decided by the staff room (technical; both imperators approve the corrected packet; Zero vetoes before a window opens):**
- **D1 — Contract before metric.** No change to the scorer's weights, bands or thresholds until the number it receives has a NAME and a PROVENANCE end to end, pinned by tests that cross producer → formatter/reranker → source projection → package construction → DLP/capping → canonical serialization → scorer input, for hybrid RRF, dense fallback, both rerankers, curated injection, caches, trusted-tool evidence and the FAQ/KG bypasses. B1.1.
- **D2 — Benchmark before inversion.** A frozen, hand-labelled benchmark whose labels never select inputs: each case stores query, actual context, provenance fixture, expected answerability, the requested fact, the supporting span or the missing-fact explanation, language pair and expected gate outcomes per gate. Five reporting strata with precedence (sufficient · relevant_insufficient · irrelevant · generic_overlap · company_prefix_chunk; the last two are nuisance properties with sufficient counterparts, not proof of insufficiency). Every required cell has a positive denominator: per EN/ID query × EN/ID context combination at least three sufficient cases and one from each negative stratum → **≥28 cases**. Frozen (manifest, contexts, labels, band boundaries, baseline hash) after independent label review; B2 supplements live in a separate file; mandatory-set results are reported separately from independently selected validation cases; "zero errors" means zero observed errors on those sets, never a population claim; disagreement with live measurements SUSPENDS acceptance for adjudication, it never invalidates the benchmark automatically. B1.3.
- **D3 — The historical fixtures are re-based by inventory, not by grep, and difficulty is never traded for smaller numbers.** B1.2 recovers the exact eleven failing node IDs, the nine proposed re-bases, the other two regressions, and the withdrawn candidate SHA or an immutable failure receipt from #5618's history; a score-literal grep is not that inventory. Each pipeline fixture specifies provider/version, contributing ranks, list membership, fusion configuration, raw score, fallback path, formatter transform, boosts and rounding — final rank alone never determines a value. Existing high-score mismatch challenges are PRESERVED; pipeline-derived variants are ADDED. Assertions unchanged. Unrecoverable inventory is recorded as explicitly unresolved. No embedding API call is needed (Zero's earlier ≤40-call allowance is withdrawn as unnecessary and is not reused).
- **D4 — Three signals, three channels, and the third one has to be built.** Source authority (curated synthetic 1.0), retrieval ranking (RRF-derived, formatted) and support for the requested fact are separate contracts. B1 freezes the first two (`score_kind` + raw value, additive) and DESIGNS the third (B1.4): the support-checking method, its inputs, its failure behaviour and its consumer, measured offline on mandatory counterfactual pairs that keep entity, language, provenance and rank while adding or removing the requested fact. B2 may not open until that design is chosen by the staff room under B1 §4's selection contract (every candidate measured, failures and the unchanged-scorer control included; the SELECTED method correctly classifies BOTH members of every independently labelled pair from runtime inputs only — never labels, spans or case IDs; an unknown result is not a success and stays distinguishable from "supported"); B2 must distinguish those pairs without case-ID rules or benchmark-specific exceptions.
- **D5 — Innocence is the floor, with ONE named exception.** Existing tripwire queries, contexts and assertions are frozen. The only planned removals are the two named strict-xfail markers, after their unchanged assertions pass. Pipeline-derived scenarios are additions. `pt`, `rp`, `cv` (removed identifiers) are not restored. **The exception:** the three golden cases at `test_evidence_cross_language.py:183,186-187` carry KNOWN-WRONG expectations (`True` on contexts that lack the requested fact), a label-selected context (`:207-212`) and a forced acceptance (`:216`). B1.3 records their independently reviewed correction with the original query/context inputs preserved; B2.1 replaces that label-selected loop with fixed-input cases and the corrected expectations, activated together with B2.1's scorer change so B1 stays behaviour-preserving. The exception permits nothing else: no genuine safety-tripwire assertion changes, no weakening of high-score mismatch challenges.
- **D6 — Abstain must leave a record before Cycle 360, with one meaning.** `wa_outbox.abstained_at timestamptz NULL` = the successful-send timestamp of a GENERATED reply whose frozen evidence label was true (it does not mean a refusal stub was delivered); `wa_outbox.evidence_score numeric NULL` = the same sealed package's frozen score; scripted/unscored routes keep NULL, nothing is inferred from reply text; final disposition is recorded separately by B3. Schema (B2.3a) and carrier/writer (B2.3b) ship as two PRs because `fly-deploy.yml:262-282` runs pre-deploy migrations on the PREVIOUS image.
- **D7 — Cycle 360 is a window with an acceptance contract (B3, §below), not a footnote.** It opens only after B2, RC2 and RC3 are merged AND their runtime effects are observed, on identities Zero names, with a stop rule, and it is the ONLY prove-live this mandate accepts. **Merged B2 does not mean the bot no longer answers unsupported questions:** B1 is behaviour-preserving by design, B2's zero-error claims are bounded to the frozen sets, and the trusted-tool/FAQ/KG paths are covered only through B2.1's perimeter decision; B3 is the only live check, and every path left unmeasured is listed in B2's `pack.yml` as a residual risk.
- **D8 — The cautioned-answer delivery policy is untouched.** `wa_finalize.py:822-898` (decision from `:822`, release `:890`, caution-note log `:896-898`) deliberately releases substantive, cautioned answers carrying `abstain=true` (a ruled distinction between a confidence label and discarding advice). B2 reports generation-gate, label-gate and final-delivery outcomes separately and preserves that behaviour. **The delivery report has acceptance criteria, not just observations** (B2 §4 B2.1(g)): synthetic candidate texts, case references and independently reviewed expected dispositions are frozen BEFORE the candidate run; identical finalizer inputs keep identical policy outcomes; the grounded-cautioned positive control still releases; unsupported substantive advice is an acceptance FAILURE even when labelled "cautioned" (the current predicate at `wa_finalize.py:204` checks context length, evidence and text length — not requested-fact support). If that cannot pass under unchanged D8, B2 suspends for staff-room adjudication; nobody alters `wa_finalize`, redefines `context_length` or moves thresholds/telemetry to manufacture suppression. Any reversal of the delivery policy is a separate decision for Zero.

## The windows

| ID | Window · organ | Wave | Host | Colour | Dux | One line | Operator items |
|---|---|---|---|---|---|---|---|
| [B1](B1-design.md) | DESIGN · the score contract, the frozen benchmark, the support-signal design | 1 | Pro | BLUE | Opus 5 `xhigh` | The abstain gate reads a ranking transform as a cosine, cannot see whether the fact is present, and its benchmark lets the label choose the context | 0 |
| [B2](B2-engine.md) | ENGINE · retrieval-primary relevance + support, thresholds, abstain record | 2 | Pro | BLUE | Opus 5 `xhigh` | Language still decides the abstain on identifier-less questions; generic overlap still clears the gate; an abstain leaves no row | 0 |
| B3 (queued) | CYCLE 360 · the controlled battery on the real channel | 3 | Pro + phone | BLUE | Opus 5 `xhigh` | Nothing merged since 2026-09-01 has been observed on WhatsApp | 4 (identities/thread, send window, latency+stop rule, cadence if changed) |

## Sequencing

- **Wave 1: B1 alone** on the scorer's and package's paths; RC2/RC3 run alongside on disjoint files. B1 opens when Zero says so, after both imperators approved this packet at a named hash.
- **Wave 2: B2** opens only when (a) every B1 PR is MERGED, (b) Pro has pulled them (blob check, not ancestry), (c) the staff room acked B1's final checkpoint, (d) the mandatory benchmark manifest is frozen (sha256 in B1's `pack.yml`), (e) the staff room has CHOSEN the support-signal method from B1.4's measured options (recorded in this README before B2 opens).
- **B2.2's production sample** runs only after RC2's data-operation receipt (deletions + detector) is recorded and the collection/version state it measures is written down, so B2.2 and B3 describe the same corpus — AND, for its LIVE sample only, with the approved query/vector artifact (B1.5, which exists only if Zero authorizes queue item 7). Three cases, the same rule as B2 §1, §4 B2.2 and §6 state it: authorization recorded AND artifact verified → the live sample is mandatory; authorization absent or declined → B2.2 runs the benchmark-only threshold examination, records the live sample as `BLOCKED — authorization absent`, never invokes the live harness, and that declared omission suspends neither B2.2 nor later B2 work; authorization recorded but the artifact missing or invalid → B2.2 suspends for staff-room resolution.
- **Wave 3: B3** opens only when: RC2's deletions and detector are verified live; RC3's prompt content is verified in the deployed container; B2's consumer content is verified in the deployed container; the schema migration is applied (runner history + `\d wa_outbox`); and Zero's four B3 items are answered. Merge status alone satisfies none of these.
- **Concurrency:** one bot window at a time on `apps/backend-rag/backend/services/rag/agentic/**` and on `wa_codex_leg.py`; RC2/RC3 and #5337 are the only siblings.

## Sibling contract — summary

| Shared surface | Owner | Touched by | Rule |
|---|---|---|---|
| `backend/services/rag/agentic/reasoning_utils.py`, `_reasoning_evidence.py`, `_abstain_policy.py` | B2 | B1 (tests only) | B1 changes no line of scorer behaviour; B2 changes nothing else's |
| `backend/services/misc/result_formatter.py`, `backend/services/search/search_service.py`, `backend/services/rag/agentic/tools.py:279-288` (source projection), `backend/core/reranker.py:169-180`, `backend/services/rag/reranker.py:345-352`, `backend/services/rag/agentic/wa_package_builder.py` | B1 (additive provenance) | B2 (reads `score_kind` at `:529/:595`) | B1 adds fields, removes none, changes no numeric value; B2 may change how `:529` mints the curated score |
| Existing tripwire tests + the frozen benchmark manifest B1 creates | B1 | B2 (separate supplement file only) | Labels, contexts, bands frozen by sha256 in B1's pack |
| `backend/services/integrations/wa_codex_leg.py::CodexLegResult` + completed-result construction; `wa_outbox_worker.py` terminal write (`:1378-1398`) | B2 (B2.3b, **narrow additive exception** to the #5337 fence) | #5337 (suspended) | Serialized: #5337 needs a fresh base after B2.3b; its veto logic untouched |
| `apps/backend-rag/backend/db/migrations_v2/` | B2 (B2.3a) | — | One additive SQL file, number chosen against fresh main + open migration PRs |
| `scripts/reingest_training_data.py`, `apps/backend-rag/scripts/curated_qa_harvest.py`, prod Qdrant collections | RC2 (Fable window) | B1 (one read-only rank ↔ score probe by point id, B1 §1) | Forbidden to B1/B2/B3 as write targets or sources of exported client text; RC2's receipt gates B2.2 and B3 |
| `backend/prompts/zantara_core.py`, `ZERO-DECISIONS.md` | RC3 (Fable window) | — | Forbidden to B1/B2/B3; its deployed content gates B3 |
| `.agents/skills/bot/SKILL.md` (the corner) | the Fable window | — | One corner PR at the close, from the windows' packs; close checklist: corrected score contract, lane states, telemetry semantics, B3 result or pending, superseded historical claims |
| `.claude/skills/modus/PENDING-ARMS.md` (`merge=union`) | shared | all | Rows only in each window's final ledger-only PR from a fresh origin/main, diff +N/-0; `Bites:` consumer `scripts/pending_arms_report.py`, observation `--json --ref origin/main` listing the rows after merge |

**Freeze rule, checkpoint delivery (publish → wake-up → ack with envelope id within 15 min → one retry → `BLOCKED: undelivered`) and ledger discipline:** as `research/operations/2026-09-10-fable-max-sessions/README.md`; keys `B1-checkpoint`/`B1-ack`, `B2-…`, `B3-…`. **Budget:** one absolute root-mandate deadline per window through `infra/codex-hooks/mandate_budget.py`; children and continuations inherit remaining time; a renewal never resets attempts or reservations; continuation hops and adapter tool ceiling declared at opening; ship reserve zero; a capped child checkpoints and returns.

## B3 — CYCLE 360 acceptance contract (queued; file written at B2's close)

B3 preserves distinct authorized client and team identities and uses synthetic content only. Its mandatory matrix covers: supported client answers versus missing-support abstention/hand-off, with one all-inclusive PricingTool price and no fee split; operational bank/document questions without invented legal citations, paired with substantive legal questions that retain grounded citations; bare greetings without retries, greeting-plus-question through normal answering, explicit human requests not swallowed by greeting handling; team status/task/hand-off assistance without sales copy, unsupported CRM claims or cross-client disclosure; DLP and finalization protections across both audiences. Every case records opaque case/package references, audience, language, runtime code reference, scripted/generated/unclassified route, frozen evidence decision (`abstained_at`/`evidence_score`), final disposition, latency, terminal status, delivery acknowledgement and tester content verdict; no phones, raw chat or reversible identity maps enter the artifact. Delivery is necessary but insufficient: package/API success is not WhatsApp delivery, and delivery alone does not establish correct content; a safe abstention must reach a terminal action rather than disappear into retry/park; a partial battery remains partial, and passing the fixed battery is not a zero-error production claim. Stop on unsupported substantive advice, fee-split leakage, wrong audience, PII exposure or broken delivery. The existing cadence limit binds unless Zero changes it; no repeated automatic probes.

## Zero's queue (this board)

1. **APPROVAL OF THE PACKET.** Both imperators approve at a named hash (Astra round 4); then you say when B1 opens.
2. **B3 IDENTITIES AND THREAD.** Which synthetic client identity and which team identity may send, on which thread; the send window.
3. **B3 LATENCY BUDGET AND STOP CONDITIONS** (Astra's protocol above lists the stop triggers; you set the latency budget).
4. **CADENCE.** The ≤10 messages/hour limit binds unless you change it (cycle 359 sent 22 in 56 min; Meta rating stayed GREEN).
5. **PR #5337 PRIORITY.** Suspended since 2026-09-01; after B2.3b it needs a fresh base. Reopen, close, or leave suspended?
6. **COMMERCIAL FUTURE OF THE BOT LINE** (your 09-10 queue item 5): retire, quiet fallback, or advertise again.
7. **ONE BOUNDED EMBEDDING BATCH FOR B2.2 (paid third-party API — needs your explicit yes).** B2.2 may not call an embedding provider, and no retained query vectors exist: the only surface, `scripts/.rag_canary/embedding_baseline.json` (untracked; 20 vectors, 1536-dim, `text-embedding-3-small`, 2026-03-25), holds REFERENCE-TEXT embeddings, not queries, and `scripts/rag_canary.py:318` regenerates it through an embedding call. Requested: provider OpenAI `text-embedding-3-small`, ceiling **40 total provider attempts including SDK retries** (the product's client leaves `max_retries` at the SDK default of 2, `backend/core/embeddings.py:271`; the batch harness disables retries or counts every attempt), synthetic EN/ID tax/visa queries only, run ONCE by B1 (B1.5) under a pre-call record (authorization reference, query-list sha256, start receipt) and checked in with sha256 as the immutable artifact; estimated cost below one cent. Your answer is recorded HERE, on the next line, as `Item 7: yes — <date>` or `Item 7: no — <date>`, and nowhere else; until that line exists this item is a request, not an authorization, and B1.5 does not open. Declined or unanswered → B2.2's live sample is recorded BLOCKED and the thresholds are re-examined on the benchmark alone, without suspending B2.
   Item 7: _unanswered_

Not on your queue, by ruling: the additive migration (autonomous at L2 once its dry-run is green — `AUTONOMOUS_OPS.md:82-90` keeps only migrations WITHOUT a green dry-run with the operator; migration rules `:161-164` — released by the BLUE Dux after the fresh gate), score kinds, fixture coverage, migration numbering, and the four decisions already ruled today.

## Adversarial review

**Round 1 — REWORK** (Astra `gpt-6-astra` `xhigh`, read-only, 2026-09-11 ~02:4x WITA, bound to v1 sha256 `757d1824…`/`e03872fe…`/`1a9754d7…`): 14 findings, 10 BLOCKER, 4 MEDIUM, all applied in v2 — D4 support signal (B1.4 + counterfactual pairs); D3 inventory-not-grep and "rank does not determine value"; B1 perimeter widened to `tools.py`, both rerankers, the search cache, the trusted-tool 0.85 bypass and the FAQ/KG bypass contracts, with the sealed-digest compatibility proof; benchmark schema, ≥28 cases, frozen manifest, separate supplement and validation sets; `context_length` corrected and delivery outcomes reported separately (D8); B2.3 split into schema (B2.3a, `migrations_v2`, previous-image deploy order) and carrier/writer (B2.3b, `CodexLegResult` + fenced worker write); `abstained_at` semantics and a Bites that cannot pass with a dead writer; B2.2's retrieval harness (vectors only, no embedding/cache/paid calls); RC2/RC3 runtime receipts as gates; council per Gear-3 PR with the canonical Gemini/Codex/Kimi panel and R9's qualifying seats (GLM builds, never counts); citation and attribution corrections; budget mechanism; migration release moved off Zero's queue. **Round 2 — REWORK** (Astra, same seat, bound to v2 sha256 `7b66c8a2…`/`b2c977a0…`/`a13f0b7d…`, against `origin/main = 4c48ca065b3e`): round-1 findings 2, 3, 6, 7, 8, 10, 13, 14 RESOLVED; 1, 4, 5, 9, 11, 12 PARTIAL, each closed by a new finding — 15 B1.4's selection contract and the probe exception (BLOCKER), 16 the three known-wrong golden expectations under the assertion freeze (BLOCKER; Astra's own round-1 blanket freeze was too broad), 17 delivery acceptance criteria for B2.1(g) (BLOCKER), 18 the missing approved vector artifact for B2.2 (BLOCKER → queue item 7 + B1.5), 19 threshold defaults live in `reasoning_utils.py:690`, not `_abstain_policy.py` (BLOCKER), 20 Kimi cannot both build and refute (BLOCKER → GLM is the only external builder), 21 citation drift (`migration_manager.py:315-327`, the 10/4 tally, `harness-floor.yml:983`) (MEDIUM). All applied in v3. Astra recorded that an explicit ACCEPT bound to the packet's hashes is its imperator approval on that packet hash — the approval `army-map.md:98-99` requires from both imperators for the Dux-appointment record — together with Fable's approval of identical bytes and Zero's opening instruction. **Round 3 — REWORK** (Astra, same seat, bound to v3 sha256 `919be95c…`/`98afe207…`/`b53eff61…`, against `origin/main = 70546e5d96`): 16, 17, 19, 20, 21 RESOLVED; 15 and 18 PARTIAL, closed by three new findings — 22 the Codex probe exception named a hand-typed `codex exec` line without the WA adapter's isolation controls (BLOCKER → the probe goes ONLY through the unchanged `CodexExecClient.generate`: full fixed argv prefix, fresh empty cwd, minimal env, stdin prompt); 23 the declined-authorization branch of queue item 7 was contradicted by B2's "manifest AND sample" acceptance and its unconditional missing-artifact stop (BLOCKER → three explicit cases in §Sequencing and B2 §1/§4/§6); 24 "≤40 calls" ignored the SDK's default retries and had no pre-call record (MEDIUM → 40 total provider attempts including retries, `max_retries=0` or attempt counting, start receipt before the first call); plus the positive delivery control lives at `test_wa_finalize.py:521` (`:508` is the refusal). All applied in v4, together with the `## Adversarial review` sections B1 and B2 lacked (`scripts/check_adversarial_review.py`, the R1 gate, failed on them). **Round 4 — ACCEPT** (Astra, same seat, bound to v4 sha256 `871361d5…`/`35021f7c…`/`721d86c2…`, final inspected `origin/main = 5b082226014e`; verbatim record `evidence/2026-09/agent-nuzantara-docs-bot-staff-room-c41145a4/codex-round-4.md`): 22, 23, 24 RESOLVED; the six delta checks found no blocking defect — `CodexExecClient.generate(prompt, *, model=None, timeout_s=None)` is callable offline with benchmark text and no broker/outbox/DLP dependency (in-memory probe, mocked boundaries, no subprocess); `Item 7: _unanswered_` matches neither dated answer, so item 7 stays unauthorized; the three cases agree; the SDK's retry loop calls the HTTPX client on every attempt, so a persistent pre-send counter works and `max_retries=0` is sufficient but not mandatory; the review sections attribute correctly; every new citation holds. Two LOW notes, applied in v5: "written identically" → "the same rule"; the B1/B2 review sections now name round 4's verdict. **This reading is Astra's imperator approval on the packet hash (the approval `army-map.md:98-99` requires from both imperators for the Dux-appointment record).** **Round 4b — re-bind:** Astra confirms the ACCEPT on the v5 bytes (this line included); the confirmation and the final sha256 are appended to `codex-round-4.md` and quoted in the PR body, never here, so the approved bytes are the shipped bytes. Fable 5.1 approves the same bytes. Shipped as PR #6143 (merge `7f8b70b7fc`). **Council read of v5** (Zero, 2026-09-11 ~03:35 WITA: "have the big LLMs read the spec"; packet inline, seats independent of file access): Gemini 3.1 Pro (`agy --model gemini-3.1-pro --effort high`, 277 s) — REWORK, 4 findings + 2 additions: G1 B2 §2 forbade the generation calls an LLM support signal makes (APPLIED, B2 §2 exception 3); G2 the redacted-query wiring for the signal was unspecified (APPLIED, B2.1 input wiring, async consumer); G3 proposed weakening B2.1(g) to "unsupported advice delivered WITHOUT a caution" (REJECTED — it reverses Astra's finding 17 and the safety property; the cure is K3); G4 "the four spec pairs" undefined (APPLIED, `spec_pair` in B1.3 and B2.1(a)); additions: async consumer (folded into G2), B1.5 "declined or unanswered" (APPLIED). Kimi K3 (`kimi -m kimi-code/k3`, 718 s, verified on disk from the frozen worktree at base `074b4fde01`) — REWORK, 6 findings + 5 additions; core claims (a)–(d) SURVIVE its falsifiers, (e) over-read: K1 `hybrid/fusion.py` "does not exist" — it is the installed `qdrant_client` package file, now cited by full path; Kimi's substantive point stands (in-repo RRF helpers are k=60 1-based and not the hot path; fusion is server-side) → B1.1's first artifact is a MEASURED rank ↔ returned-score table (APPLIED); K2 `army-map.md:98-99` is the Dux-appointment record, not a rule that a hash-bound ACCEPT is approval (APPLIED as wording — the ACCEPTs ARE the approvals it requires); K3 B2.1(g) passes under unchanged D8 only if the support signal's consumer sits upstream of the leg call (APPLIED, B2.1(g) mechanism); K4 trusted-tool/FAQ/KG paths outside the signal (APPLIED, perimeter decision per path); K5 citation batch — `migration_manager.py:311` REJECTED (Kimi's base predates #6123; `:323` verified on origin/main), the other six APPLIED (`AUTONOMOUS_OPS.md` wording, `evidence_inputs :599-611`, curated test path + `:156-203`, contexts `:47-56`, `wa_finalize :822-898`, penalty guard `final_score > 0.15` at `:665`); K6 determinism contract for LLM-judge candidates (APPLIED); additions: "merged B2 ≠ the bot never answers unsupported questions" (APPLIED, D7), pre-B3 ledger row (APPLIED, B2.3b), B1.5 canary-presence check (APPLIED), re-measure Astra's probes at open (already B1 §1). Qwen 3.8 Max (`scripts/tp1_call.py --model qwen3.8-max --effort max`): HTTP 403 "Access to model denied" on both attempts — the seat did not run and is not counted; the `qwen-cloud-code` CLI seat stays UNARMED pending Zero. Records: `evidence/2026-09/agent-nuzantara-docs-bot-staff-room-c41145a4/council-*.md`. **Round 5 — pending** on v6 (Astra re-read of the council delta + re-bind).
