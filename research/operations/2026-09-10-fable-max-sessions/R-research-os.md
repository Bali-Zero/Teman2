---
title: "R — RESEARCH OS · staff-room spec and two battle windows (R1 DESIGN, R2 ENGINE)"
date: 2026-09-11
adversarial_review: codex
---

# R — RESEARCH OS · staff-room spec (Fable 5.1 + Astra) and two battle windows

**Board:** successor item of the 2026-09-10 fable-max board (mandate 7, "Research OS"). Both imperators measured the same mandate independently on 2026-09-11 (Fable, Claude session `nuzantara-04`; Astra, Codex session 01:29 WITA) and agree on every load-bearing fact. Astra then red-teamed v1 of this file (round 1, 19 findings, 12 BLOCKER); v2 applies them. The result of the tranche is **P06 slice completion**, with deferred packet obligations named — not completion of the Research OS engine. Zero's items are in §0.

## 0. What the staff room found, and decided

**Measured (dated command receipts, re-run before trusting):**
- Cohort B was NOT "never started". P05/P06 preparation bundles merged 2026-08-26 (#4996, #4997); RULING B1 applied (#5029); a P06 build lane (slice 1) started the same day and stopped on purpose: the canonical valid-time is not text-orderable (fraction optional; `.` (0x2E) sorts before `Z` (0x5A), so the microsecond-zero instant sorts last although it is chronologically first) and the correct `text::timestamptz` index is refused by PostgreSQL (STABLE, not IMMUTABLE). Ledger row `.claude/skills/modus/PENDING-ARMS.md:21`, owner "the lane that owns Work Packet 04's surface — NOT the P06 build lane". In the searched scope (that ledger, `gh pr list --state all`, `git branch -r`, `git worktree list`, `shared/escalations_pro.jsonl`, 2026-08-27→09-10) no lane, PR, branch or worktree ever took that row. Guard tests on main: `apps/backend-rag/backend/tests/db/test_research_os_valid_time_is_not_text_orderable.py` (8) and `apps/backend-rag/backend/tests/unit/research_os/test_naga_evidence_mapping_preconditions.py` — 22 passed on Pro 2026-09-11 01:55 WITA (`.venv/bin/python -m pytest <both files>`); Astra's seat: 19 passed, 3 PostgreSQL not executed.
- Pro was not saturated: `gh pr list --state merged --search 'head:agent/nuzantara/ merged:2026-08-27..2026-09-10'` → 252 PRs, zero on Research OS. The retro `research/operations/2026-08-26-retro-fleet-sessions-25-26.md` Q4 asked "abandoned, deferred, or superseded?"; no ruling in `docs/rules/RULINGS.md` or the memory store answers it.
- `research/operations/execution/research-os-v1.0.0/README.md:7` still says `prepared_not_dispatched`. False. Migration head is 308, not the 287 SESSION-BOARD §5 records.
- Production, read-only through `scripts/pg.sh` 2026-09-11 01:50 WITA: `research_os_objects` exists, 0 rows, triggers `research_os_objects_immutable` and `research_os_objects_no_wipe` present; `naga_claims` 3,119 rows. This supersedes D4's "applied in no environment".
- **A persistence consumer already exists:** `apps/backend-rag/backend/services/autonomous_lab/consul_executor.py:175` carries an executable `INSERT INTO research_os_objects` with replay verification, hashes payloads before validation (`:170`) and emits instants with optional fractions (`:228`). It is not active in production (0 rows) and the Fly image does not ship `packages/research-os-core` (`apps/backend-rag/Dockerfile:36` copies `cell-core`), but it binds the contract. Its tests: `apps/backend-rag/backend/tests/unit/services/autonomous_lab/**`, `apps/backend-rag/backend/tests/integration/test_dual_consul_postgres.py`.
- P03 shipped code (`scripts/wr3_flowkit_client.py:45-47`: zero-spend authority, credit ledger). The P06 bundle names 17 of 32 required Evidence paths (the guard pins `absent == 15` and asserts `named < absent`, `:277-289`).
- `python -m research_os.cli fixtures --check` → `checked: 218, failures: []` (79 accepted, 139 expected rejections); with the 25 schemas that is the 243 JSON files embedding `object_hash`; 0 fixtures carry a sub-second instant. The published `UtcDateTime` schema (`primitives.py:362`) admits `.1Z`, `.11Z`, `.0000001Z` and a lowercase `t` (Astra, live probe). Astra's in-memory trial of a six-digit serializer: valid fixtures 79/79 → 3/79, `compatible: false, major` from the repository's own compatibility checker.
- Codex Sol is not the designated builder any more (`docs/architecture/dual-consul/army-map.md`: a general, Dux only on ORANGE; default implementer Terra/Sonnet). Sol's 2026-08-28 verdict on Research OS: "AHEAD in production, BEHIND in promotion".

**Decided by the staff room (technical; Zero may veto before a window opens):**
- **D1 — Scope.** The 23 packets stay a roadmap. One tranche: P04-surface design decisions → P06 NAGA claim ledger, slice 2. No other packet opens. Deferred obligations named at the end of §0.
- **D2 — Valid-time ordering is repaired in storage, the wire form and every hash stay byte-identical.** The migration ships `research_os_instant_key(text) RETURNS text IMMUTABLE`, a pure regexp/substring normalisation with a COMPLETE key grammar over everything the published schema admits: separator uppercased to `T`; `+00:00` → `Z`; the fraction normalised to exactly six digits — absent → `.000000`, shorter → right-padded with zeros, longer → truncated (declared precision policy: the key is exact to the microsecond and sub-microsecond differences are not orderable by it); output `YYYY-MM-DDTHH:MM:SS.ffffffZ`, 27 bytes, compared and indexed under `COLLATE "C"`. So the KEY is fixed-width and its order is chronological for every admitted spelling. Expression indexes on `research_os_objects` over `research_os_instant_key(payload->'time'->>'valid_from')` and `…'valid_to'`; every valid-time predicate compares keys, never raw text. R2's persistence API additionally REJECTS before INSERT any instant with more than six fractional digits or a lowercase separator, so nothing the truncation would fold is ever stored by this slice. Tests cover equality and ordering ACROSS representations (`.1Z` = `.100000Z`; `.1Z` < `.11Z`; `Z` = `+00:00`; `t` = `T`), not only sanctioned-serializer examples. No cast is declared IMMUTABLE. The contract, `_UTC_OFFSET_PATTERN`, the 218 fixtures and Consul's hashes do not change. The guard test file stays green (its own §"scoped honestly", `:70-76`: a (b)-shaped cure is confirmed from the migration, never from that module). Ledger row 21 closes with the migration as proof-of-armed.
- **D3 — Two axes, two mechanisms, one reader.** (i) *Correction*: a successor claim plus its `ObjectSuccessorEdge`, committed in ONE transaction (CONTRACTS.md:142, :267), successor `recorded_at` strictly later (CONTRACTS.md:140; `graph.py:121`); the predecessor's effective system interval is `[recorded_at, successor.recorded_at)` (CONTRACTS.md:88), derived, never written (RULING B1). (ii) *Scheduled succession* (a rate change known in advance) is NOT supersession and NOT two members of one family: CONTRACTS.md:141 requires a unique terminal member per family and `graph.py:135` quarantines two edge-less members (`non_unique_current_member`, Astra live probe) without looking at valid intervals. Each scheduled interval is therefore its OWN canonical family, and the families that answer one domain question are grouped by an explicit **`subject_key`** (jurisdiction + instrument + provision, carried in the Claim's namespaced extension, defined by R1). Fixture `bitemporal/03` is rewritten to (ii) as two families sharing a `subject_key`; `supersession/01` to (i) with one exact predecessor hash. (iii) The reader is `read(subject_key, valid_at=T, known_at=S)`: for every family under the key, the graph selects the current member as known at S (versions with `recorded_at <= S`, successor edge absent or `successor.recorded_at > S`); across those members the valid filter `valid_from <= T < valid_to` (`null` end = open) applies; a finite `valid_to` is never extended by succession; a `null` `valid_from` on a consequential claim is not admissible (CONTRACTS.md:266); the result is **at most one admissible answer, otherwise explicit abstention (no answer) or quarantine (fork, missing node, hash mismatch, cycle — CONTRACTS.md:141)**. A quarantined family is reported, never filtered away to bypass the graph. Never "exactly one per family".
- **D4 — Admission is a decision, not a mapping.** R1 delivers executable admission rules for the legacy → canonical path: statement identity (no fabricated triples; `02-p04-adapter-mapping.md:59`), source version and body hash (never a URL hash as content hash, `:83-84`), exact span, IntelEvent identity (`:87`), rights, retention, classification, stable family identity that is persisted in the admission manifest and never written back into legacy NAGA (supersedes `:58`). Required canonical fields (`research_os/models/evidence.py:130`) are truthfully populated or the record is EXCLUDED with the reason named. Documented mapping coverage, available source information and admissible records are three different counts and are reported separately. A dry-run that admits zero legacy records is a valid, honestly reported outcome.
- **D5 — Storage topology.** Canonical objects and edges live in `research_os_objects` (migration 279 §"Purpose": one polymorphic store, domain packets add projections, never parallel cores). R2's migration, symbolic name `research_os_naga_claims`, creates the D2 function and indexes and ONE projection table, `research_os_naga_admission` (legacy claim id, family id, canonical claim/evidence ids and hashes, source snapshot hash, decision, run id, recorded_at) — the admission manifest that makes replay idempotent. The hash is verified by the persistence API before INSERT; SQL enforces only structural and append-only constraints (migration 279:54 refuses a database re-implementation of RFC 8785; never `sha256(payload::text)`). **Classification-lowering successors are out of this slice:** CONTRACTS.md:142 requires a risk- or sensitivity-lowering successor to commit with its receipts in the same transaction, and those receipt kinds are not in Z2b; R2's persistence API therefore REJECTS such a successor before INSERT (zero-write test), and receipt-bearing transitions are deferred with the other services.
- **D6 — Migration mechanics.** Integer `max+1` measured from `apps/backend-rag/backend/db/migrations_v2/` in the integrating session (sequence not dense). PostgreSQL 15 compatible, additive, no data movement. Merging a PR that touches non-test, non-doc paths under `apps/backend-rag/**` triggers the deploy workflow (`.github/workflows/fly-deploy.yml:11` excludes tests and docs) — R2's PR does; successful rollout and migration completion are observed separately, and an image rollback never reverts the database (`fly-deploy.yml:559`). That deploy is a production effect above the packet's default ceiling (`execution/research-os-v1.0.0/README.md:145`: `deployment: none`, `database: disposable_test_only`; `:161` requires an exact, unexpired, effect-specific authority) and is authorized by Z2a, not by this decision.
- **D7 — Two windows, one per organ, disjoint paths, one release order.** R1 DESIGN owns the contract surface's tests, the P06 bundle and the control room; R2 ENGINE owns `services/research_os/naga_*`, the migration and its tests. R2 may BUILD in parallel; it pushes, opens and arms only after R1 has merged and been integrated (no xfail bridge, no post-arm rebase: Builder Contract rule 1).
- **D8 — Team.** BLUE unless Zero declares otherwise: Dux Opus 5 `xhigh`; implementer Sonnet 5 (Haiku 4.5 support) and nothing else; adversarial reviewer an independent Codex seat (Sol); R2 is Gear 3 (production migration and production writes) and therefore declares a council — Codex Sol + Kimi K3, both review-only, neither ever implements (FLEET_TOPOLOGY.json:339 excludes a contributing family; modus SKILL.md:70: the council is declared for the production effect, not automatic); final gate a fresh Opus 5 `xhigh` session outside the chain that publishes `harness/fable-gate` on the candidate HEAD (`scripts/harness_fable_gate.py --verdict … --sha …`, `--conditions-ref` on PASS-WITH-CONDITIONS) and posts the receipt comment. Codex Sol never builds in this tranche.
- **D9 — Control room.** R1 updates `README.md:7` and adds `SESSION-BOARD.md` §13 with the state above; migration head corrected. Frozen `specs/**` untouched; a contradiction becomes `evidence/p04/freeze-change-proposal-002.md`.
- **D10 — Runtime placement of the backfill.** R2's admission and backfill run from the Pro checkout against production PostgreSQL through the Fly proxy, never inside the Fly image (no Dockerfile change in this tranche). `scripts/pg.sh` is read-only by construction; the write identity is Zero's (Z2).

**Zero's items (Legge 5):**
- **Z1 — Ratify D1** (one tranche, P06 slice 2; roadmap kept) or name a different scope.
- **Z2a — Production deployment and additive DDL.** Authorize, or not, R2's merge: it deploys the backend and applies `research_os_naga_claims` (one IMMUTABLE function, two expression indexes, one projection table; no data movement) to production. Scope and expiry are recorded in the ledger row and in R2's `brief.yml` `authority:` block before R2 opens its PR; without it R2 builds, checkpoints and stops before opening — even when Z2b is "dry-run only".
- **Z2b — Production writes (backfill DML).** Authorize, or not, a bounded production effect above the packet's default ceiling (`database: disposable_test_only`), recorded the same way with its own expiry: (a) the **eligible cohort by name** — recommendation: a seed of at most 20 public regulatory claims (P06 packet §"bounded public, non-PII domain", `06-naga-claim-ledger.md:76`) prepared by R1 as fully sourced canonical fixtures, PLUS whatever the legacy admission dry-run admits (expected: 0 today — legacy rows carry URL hashes, no spans, no IntelEvent identity); (b) **maximum record count**; (c) **target kinds and tables** — `Claim`, `Evidence`, `ObjectSuccessorEdge`, `IntelEvent` in `research_os_objects`, rows in `research_os_naga_admission`, nothing else; (d) **execution identity** — a write-capable PostgreSQL role for the backfill, its credential held by you; legacy readers stay authoritative (`06-naga-claim-ledger.md:117`). Alternative: dry-run only; the tranche ends at the admission report and 0 rows.
- **Z3 — Colour per mission** (R1, R2): BLUE if undeclared.
- **Z4 — Veto window on D2–D10** closes when R1 opens. A veto or a technical decision never substitutes for Z2a/Z2b: those are authority grants with scope and expiry, not defaults.

**Success bar of the tranche (P06 slice completion):**
- R1: D3 reader rule and D4 admission rules as executable fixtures and tests; the P06 fixture bundle validates against the exported schemas with exact, consistent references; mapping guard rewritten (not a number flip); control room truthful; freeze-change proposal filed if any decision needs one.
- R2: migration live (D2 function + indexes + projection); persistence API with atomic successor+edge, hash check before INSERT, crash-rollback, concurrent-fork rejection and zero-write replay tests, all executed on `postgres:15` in CI (no skips, no xfail); the reader answers the temporal case list below; **Bites** = the named backfill command AND the reader observed against the admitted cohort in production (ids and hashes returned, references intact, B1 preserved, replay writes 0), reported as before/after deltas per kind: `eligible / inserted / already_present / excluded / rejected`.
- Temporal cases (both windows): both interval boundaries; zero and non-zero microseconds; unknown bounds; correction recorded before and after discovery; scheduled change known in advance; an existing finite expiry; a no-answer result; a quarantined fork.
- Forbidden in both: fabricated statements; URL-as-content hashes; placeholder references presented as provenance; silent approval or classification defaults; writes into legacy NAGA tables; declaring a STABLE cast IMMUTABLE; production DELETE/DROP/TRUNCATE or trigger removal.

**Deferred, named:** statement atomizer (`naga_statement_atomizer.py`), invalidation/transition/review-queue services and the `invalidation/01` fixture's effects, consumer wiring (`app/routers/naga.py`, MCP tool), D7 cross-implementation hashing (P05), Consul activation on Fly (packaging of `research-os-core`), P01–P03, P05, P07–P23.

---

# R1 — DESIGN · battle window

| Field | Value |
|---|---|
| Mandate id | **R1** |
| Colour | **BLUE** (default). Chosen before the window opens; no fallback between colours. |
| Dux role | Opus 5 `xhigh` on BLUE (Sol `gpt-5.6-sol` `xhigh` on ORANGE), appointed by the staff room (Fable 5.1 + Astra, both readings 2026-09-11). |
| Worktree / branch | `agent/nuzantara/backend-rag/r1-research-os-design`, created by the command in §1 |
| Wave | 1. R2 builds beside it on disjoint paths and releases after R1. |
| Opening | BLUE: Zero opens a fresh `claude --model claude-opus-5` window at effort `xhigh` on Pro and pastes this file. ORANGE: a Codex window on the Sol seat as army-map §1bis names it. |

## 1. Mandate

R1 · BLUE · **organ: the Research OS contract surface (Work Packet 04) as tests, fixtures and design records** — the core's test suite, the P06 preparation bundle, the control room. **Gear:** 2 expected (CI's floor wins). **Host:** Pro.

**Objective:** turn D3 and D4 into executable rows and canonical fixtures, make the bundle's mapping and fixtures truthful, and make the control room tell the truth (D9). **What success changes:** R2 builds against decided semantics and validated fixtures instead of prose; ledger row 21's owner exists.

**Worktree:** `cd ~/nuzantara && WT=$(python3 scripts/agent_start.py --lane backend-rag --task-id r1-research-os-design | awk '/^WORKTREE_READY /{print $2}') && echo "$WT"`. The broker prints one `WORKTREE_READY` line; it cannot change the caller's cwd, so every command runs as `cd "$WT" && …`. Record `git -C "$WT" rev-parse HEAD` as `base:` in the claim commit and the PR body and compare it with `git rev-parse origin/main` (the broker falls back to the LOCAL base on fetch failure).

**Ground** (staff room 2026-09-11; re-run all of it):
- `packages/research-os-core/research_os/primitives.py:84` (`_UTC_OFFSET_PATTERN`), `:362` (`UtcDateTime`); `hashing.py:46-70`; `graph.py:115-125`; `models/evidence.py:130`; `models/successor_edge.py:28`. The core has no `tests/` of its own: its tests live under `apps/backend-rag/backend/tests/unit/research_os/`.
- `apps/backend-rag/backend/tests/unit/research_os/test_naga_evidence_mapping_preconditions.py:277-294` and `apps/backend-rag/backend/tests/db/test_research_os_valid_time_is_not_text_orderable.py:70-76`.
- `research/operations/execution/research-os-v1.0.0/evidence/p06/ros-v1-p06-naga-prep-b01/`: `02-p04-adapter-mapping.md` (§2; `:58`, `:59`, `:83-84`, `:87`, `:104`), `05-test-matrix.md`, `07-open-questions-and-corrections.md` (§B2 residual, §B3, §B4, `:173`), `fixtures/bitemporal/{01,02,03}`, `fixtures/supersession/01` (`:49` and `:60` bind the same predecessor to two hashes), `fixtures/invalidation/01` (deferred, `:24`).
- `research/operations/specs/evidence-to-action-freeze-2026-08-15/CONTRACTS.md:88`, `:140-142`, `:265-267` (frozen; read, never edit).
- `research/operations/execution/research-os-v1.0.0/README.md:7`, `SESSION-BOARD.md` §5 (head "287"), `.claude/skills/modus/PENDING-ARMS.md:21`.
- Consul consumer: `apps/backend-rag/backend/services/autonomous_lab/consul_executor.py:170-178`, `:228`.

## 2. Owned perimeter

- **Writable:** `apps/backend-rag/backend/tests/unit/research_os/**` (new admission and temporal rows; the mapping guard rewritten); `research/operations/execution/research-os-v1.0.0/evidence/p06/ros-v1-p06-naga-prep-b01/**` (mapping §2 completed to 32 paths with the three counts of D4; fixtures made canonical — validated against the exported schemas, exact consistent references, invented numbers only; `bitemporal/03` and `supersession/01` rewritten per D3; the note at `02-p04-adapter-mapping.md:104` superseded by the atomic rule; §B2–B4 dispositions); a new seed-cohort fixture set `fixtures/seed_public_regulatory/**` for Z2 (fully sourced: body hash, exact span, valid time, rights); `research/operations/execution/research-os-v1.0.0/README.md`, `SESSION-BOARD.md` (§13 and the §5 head line only), `evidence/p04/freeze-change-proposal-002.md` if needed; the evidence directory that `scripts/ci/evidence_paths.py` names for this branch.
- **Forbidden:** `packages/research-os-core/**` (the contract surface stays byte-identical in this tranche; a needed change is a freeze-change proposal, not an edit); `apps/backend-rag/backend/services/**`, `db/migrations_v2/**` (R2); `apps/backend-rag/backend/services/naga/**`; `specs/**`; any production write.
- **Shared:** the ledger (§3). `.lane-check.json`'s `scope_globs` only decides whether a check applies; VERIFY and the gate compare changed paths against this list by hand.
- **Bans:** no Claude model through a paid per-token Anthropic endpoint. No client PII or OSINT in cleartext. Off-limits files per `docs/rules/RULINGS.md`.

## 3. Sibling contract

Frozen before BUILD. Changes go to the staff room through Zero.

- **R2 consumes R1's fixtures and rows.** R2 does not push, open or arm before R1 has merged and R2 has integrated `origin/main` containing it.
- **The ledger — the one surface all windows share.** `.claude/skills/modus/PENDING-ARMS.md` carries `merge=union`, which GitHub's mergeability ignores. **One final tranche ledger PR**, opened by R2's Dux after both code PRs merged, cut from a fresh `origin/main`, append-only (`+N/-0`), row 21 preserved and its closure appended, R1's and R2's rows added. Open it only when `gh pr list --state open --limit 200 --json number,files --jq '[.[]|select(any(.files[];.path==".claude/skills/modus/PENDING-ARMS.md"))|.number]'` prints `[]`. R1 files no ledger PR of its own.
- **Freeze.** Once armed, the branch is read-only (Builder Contract rule 1). A real DIRTY on an armed PR: close it naming the successor, cut a fresh branch from `origin/main`, cherry-pick the same content, open and arm the successor.

## 4. Acceptance

- **Negative (innocence):** `python -m research_os.cli fixtures --check` still reports `checked: 218, failures: []` — 79 accepted, 139 rejected with their expected reasons — and fixture bytes and hashes are unchanged (`git diff --stat origin/main -- packages/research-os-core` is empty); Consul's unit and integration tests still pass.
- **Temporal rows:** every case in §0's temporal list is a test over canonical fixtures, executed (no skip, no xfail); `bitemporal/03` answers as two families under one `subject_key`, each with a unique current member; `supersession/01` as an atomic correction with one predecessor hash; the D3 reader specification is one function signature with a truth table, and a fork returns quarantine, a gap returns abstention; the `subject_key` extension is defined once, in a namespaced extension, and validates against the exported Claim schema without a contract change.
- **Admission rows:** the D4 predicate as tests: a legacy-shaped record with a URL hash is EXCLUDED with reason `content_hash_is_url`; one without a span → `exact_span_missing`; one with a fabricated triple → `statement_not_from_source`; a seed fixture with all inputs → admitted, and the three counts differ from each other on the mixed set.
- **Mapping:** `test_naga_evidence_mapping_preconditions.py` rewritten: `named < absent` and the `15` gone, replaced by an executed mapping over a canonical fixture that validates against `evidence.schema.json`; documented paths 32/32.
- **Control room:** `README.md:7` reads the real program state and names this file; `SESSION-BOARD.md` §13 carries the 2026-09-11 measurement; §5's head line reads "measure `max+1`; 308 on 2026-09-11".
- **Bites:** the PR body names the consumer — R2's persistence tests import these fixtures — observed by running R2's fixture loader (or, before R2 exists, the schema validation of every fixture in CI) before the work is reported done.

## 5. Team

Every seat comes from the colour table (`docs/architecture/dual-consul/army-map.md` §1bis).

- **BLUE:** Dux and release owner, this window (Opus 5 `xhigh`). Implementer Sonnet 5, pinned in every Agent call (`model: "sonnet"`); supports on Haiku 4.5 (`model: "haiku"`); an unpinned child inherits the Dux's model. Adversarial reviewer: an independent Codex seat (`codex exec -m gpt-5.6-sol --sandbox read-only`, stdin via `-`), never the builder. Final gate: a fresh Opus 5 `xhigh` session outside the chain; it publishes `harness/fable-gate` on HEAD and posts the receipt; it signs, never arms.
- **ORANGE**, only if declared before opening: every seat from §1bis. A dead seat suspends the mission.
- Record each seat's effective model, effort and thread id in `brief.yml` at start.

## 6. Appetite and stop-loss

- **Budget:** `appetite.wall_clock_hours: 4` (the key `scripts/evidence_pack_lint.py:1844` accounts for), 2 adversarial rounds, 1.2M tokens; descendants: at most 4 attempts in total, concurrency 2, depth 1, 1 continuation hop; adapter tool ceiling 120 tool calls per child, ship reserve N = 0: a capped child checkpoints and returns, shipping is the Dux's and consumes the mandate budget. Declared in `brief.yml`; `spend:` in `pack.yml`.
- **Deadline:** opening + 4 h, ONE absolute instant recorded in `brief.yml`. The runtime mandate ledger (`infra/codex-hooks/mandate_budget.py`) initialises its clock once at first open with `MANDATE_SECONDS` = 3,600 s and `renew(seconds)` sets `deadline = now + seconds` (`:101`): the Dux binds the ledger before the first dispatch by reading its `deadline` at open and, if it is not the recorded instant, requesting `renew(opening + 14,400 − now)` — never `renew(14400)`, which would overshoot — counters untouched. Renewal authority beyond that binding: the staff room, once, by the same formula.
- **Limits:** three reds for one cause → suspend; fix-of-a-fix depth 1.
- **Checkpoints — published, then delivered** (`army-map.md:81-90`): `$STATE` is one line, no PII, prefixed `R1 g<generation> <sender>→<recipient>`; publish with `scripts/fleet_mail.sh local broadcast --key R1-checkpoint --ttl 24 "$STATE"`; the envelope id is the file name plus the sha256 of the file, binding mission, generation, sender and recipient. Wake-up: on BLUE, `SendMessage` the same text quoting the id to the staff-room Claude window Zero names at opening; on ORANGE the Dux Sol has no `SendMessage` — the envelope itself is the wake-up (the staff-room Claude window reads the mailbox at its next tool call) and the Dux tells Zero in its own window. Ack `R1-ack` within 15 minutes quoting the same id and generation; an ack for an older generation is stale and ignored; one retry with the same envelope; then `BLOCKED: undelivered` and suspend. A fresh window after a jump increments the generation and re-publishes the last checkpoint before doing anything else. Sent and received are both recorded in `brief.yml`.
- **Stop and escalate if:** a decision needs a change under `specs/**` or `packages/research-os-core/**` (write the freeze-change proposal, stop); a seed fixture needs a figure that cannot be sourced from a public document; Consul's tests go red.

## 7. Evidence and release

- **Evidence:** `cd "$WT" && python3 scripts/ci/evidence_paths.py --ref "$(git rev-parse --abbrev-ref HEAD)"` names the directory. `brief.yml`: gear, appetite, team with thread ids, `base:` sha, sent/received checkpoint records.
- **Release**, three separate commands: `git -C "$WT" push -u origin HEAD`; `cd "$WT" && gh pr create --title "$TITLE" --body-file "$BODY"`; `gh pr merge "$PR" --auto` at once (bare `--auto`; the queue rejects strategy flags). One code PR.
- **Gate:** `harness/fable-gate` success on the candidate HEAD (modus SKILL.md, gate gear threshold) plus the receipt comment: mission id, colour, HEAD sha, gate thread id, commands with exit codes, verdict; re-checked against the current HEAD before posting.
- **Deploy:** none (tests, fixtures, docs). Prove-live = CI green on `postgres:15` for the new rows on main, and the puller on Pro at the merged sha.
- **Rollback trigger:** Consul's or the core's tests red on main because of this PR → revert PR, then a successor.

---

# R2 — ENGINE · battle window

| Field | Value |
|---|---|
| Mandate id | **R2** |
| Colour | **BLUE** (default). Chosen before the window opens; no fallback between colours. |
| Dux role | Opus 5 `xhigh` on BLUE (Sol `gpt-5.6-sol` `xhigh` on ORANGE), appointed by the staff room (Fable 5.1 + Astra, 2026-09-11). |
| Worktree / branch | `agent/nuzantara/backend-rag/r2-research-os-naga-engine`, created by the command in §1 |
| Wave | 1 for BUILD (parallel to R1 on disjoint paths); **release only after R1 merged and integrated**. |
| Opening | BLUE: Zero opens a fresh `claude --model claude-opus-5` window at effort `xhigh` on Pro and pastes this file. ORANGE: a Codex window on the Sol seat as army-map §1bis names it. |

## 1. Mandate

R2 · BLUE · **organ: the NAGA claim ledger on the Research OS substrate (Work Packet 06, slice 2)** — persistence API, admission and backfill under `apps/backend-rag/backend/services/research_os/`, plus their migration. **Gear:** 3 (production migration and, under Z2, production writes). **Host:** Pro.

**Objective:** canonical `Claim`, `Evidence`, `IntelEvent` and `ObjectSuccessorEdge` objects are written to `research_os_objects` atomically and read bitemporally per D3; legacy `naga_claims` rows pass the D4 admission or are excluded with a reason; the admitted cohort (Z2) is backfilled idempotently and read back. **What success changes:** the first real Research OS rows exist in production with provenance that survives the admission rules, and "what did we hold true on date T, as known at time S" has one executable answer.

**Worktree:** `cd ~/nuzantara && WT=$(python3 scripts/agent_start.py --lane backend-rag --task-id r2-research-os-naga-engine | awk '/^WORKTREE_READY /{print $2}') && echo "$WT"`. Same `base:` discipline as R1.

**Ground** (staff room 2026-09-11; re-run all of it):
- `apps/backend-rag/backend/db/migrations_v2/279_research_os_contract_core.sql` (`:36` one canonical store; `:54` hashing stays in the application; `:209` `recorded_at TIMESTAMPTZ NOT NULL DEFAULT now()`; `:247` the `(object_kind, recorded_at DESC)` index) and `280_research_os_objects_truncate_guard.sql`. Head measured at build time (`308` on 2026-09-11; not dense).
- `research/operations/execution/research-os-v1.0.0/evidence/p06/ros-v1-p06-naga-prep-b01/06-future-file-list.md`, `03-migration-design-notes.md` (`:47` recommends `research_os_objects`; its parallel-table fallback is REJECTED by D5), `04-golden-set-and-adversarial-plan.md`, `05-test-matrix.md`, R1's fixtures once merged.
- Legacy: `apps/backend-rag/backend/services/naga/persist.py` (the INSERT), the four UPDATE sites in `services/naga/quality/{dedup,claim_scorer,expiry}.py` (`expiry.py:58` moves a claim out of `active`). Read, never edited.
- Existing adapters and the persistence consumer: `apps/backend-rag/backend/services/research_os/{action_item_adapter,action_intent_adapter,operational_receipt_adapter,_core_path}.py`; `services/autonomous_lab/consul_executor.py:170-178` (`seal()` + `_persist()`), its tests under `tests/unit/services/autonomous_lab/**` and `tests/integration/test_dual_consul_postgres.py`.
- Production, read-only: `scripts/pg.sh` (role `nuzantara_readonly`); `naga_claims` 3,119 rows; `research_os_objects` 0 rows. `apps/backend-rag/Dockerfile:36` does not ship `packages/research-os-core`: this window's commands run from the Pro checkout (D10).
- `.github/workflows/fly-deploy.yml` (`:559` image rollback does not revert the database).

## 2. Owned perimeter

- **Writable:** `apps/backend-rag/backend/services/research_os/naga_admission.py` (D4 rules, three counts), `naga_persistence.py` (atomic successor+edge, hash check before INSERT, replay), `naga_bitemporal_reader.py` (D3 `read(family, valid_at, known_at)`), `naga_backfill.py` (`--dry-run` default; manifest-bound replay; per-kind deltas); `apps/backend-rag/backend/db/migrations_v2/<max+1>_research_os_naga_claims.sql` (D2 function + indexes + `research_os_naga_admission`); `apps/backend-rag/backend/tests/services/research_os/naga/**`; `apps/backend-rag/backend/tests/migrations/test_migration_<n>_research_os_naga_claims.py`; `research/operations/execution/research-os-v1.0.0/evidence/p06/ros-v1-p06-naga-engine-b02/**` (this slice's evidence: counts and hashes only, never claim text); the evidence directory that `scripts/ci/evidence_paths.py` names for this branch.
- **Forbidden:** `packages/research-os-core/**`; `apps/backend-rag/backend/services/naga/**` (legacy runtime; no identity write-back); `app/routers/**`, `nuzantara_mcp/**` (consumer wiring is a later slice); `Dockerfile`, `fly.toml`; feature flags defaulting ON; any write to a table other than `research_os_objects` and `research_os_naga_admission`; `specs/**`; the P06 bundle (R1's).
- **Shared:** the ledger (§3); the migration train (`migration-ledger-research-os`: integer bound at integration, order preserved, adjacency not).
- **Bans:** as R1. The parity and admission reports carry counts and hashes only.

## 3. Sibling contract

- **R2 consumes R1.** Build in parallel on fixtures of your own under `tests/services/research_os/naga/fixtures/` if R1 has not merged; when R1 lands, integrate `origin/main` BEFORE the first push, switch to R1's canonical fixtures, run the full acceptance and the adversarial round on the integrated tree, then release the frozen candidate. No `xfail`, no post-arm rebase, no queue-position promise.
- **The ledger:** R2's Dux opens the ONE final tranche ledger PR after both code PRs merged (R1 §3).
- **Freeze:** as R1.

## 4. Acceptance

- **Migration:** applies and rolls back cleanly against a throwaway `postgres:15` in CI; `research_os_instant_key` is IMMUTABLE by construction (regexp only) and `pg_proc.provolatile = 'i'`; the two expression indexes exist; `research_os_naga_admission` has its own append-only guard; the 279/280 triggers untouched.
- **Ordering (closes ledger row 21):** on live PostgreSQL in CI, for two canonical instants inside one second written by the sanctioned serializer, `research_os_instant_key($1) < research_os_instant_key($2)` agrees with `$1::timestamptz < $2::timestamptz`, and a half-open `valid_from <= T` predicate over the key returns the row the raw-text predicate dropped.
- **Persistence:** successor + edge commit in one transaction and a crash between them leaves neither; a second successor for the same predecessor is rejected (fork → quarantine, never a winner); a payload whose `object_hash` does not match is rejected before INSERT; a classification-lowering successor is rejected before INSERT (zero rows written, asserted); an instant with more than six fractional digits or a lowercase separator is rejected before INSERT; replay of the same manifest writes 0 rows; the predecessor row is byte-identical before and after, `object_hash` unchanged (B1). Consul's tests still pass.
- **Reader:** `read(subject_key, valid_at, known_at)`; every case in §0's temporal list executed on `postgres:15` in CI (no skip, no xfail), over R1's fixtures; abstention and quarantine are explicit return values; the ordering tests of D2 run across all admitted spellings.
- **Admission:** the D4 rules over the legacy shape produce the three counts; every exclusion carries its reason; nothing is defaulted silently.
- **Production observation (the Bites, under Z2b):** (1) `naga_backfill.py --dry-run --cohort <name>` on Pro, read-only, prints per kind `eligible / inserted(0) / already_present / excluded / rejected` and the manifest hash; (2) with Zero's write identity, `naga_backfill.py --apply --cohort <name> --manifest <hash>` refuses if the source snapshot drifted since the dry-run, writes exactly `eligible − already_present` rows, and prints the ids and hashes; (3) `naga_bitemporal_reader` on Pro answers T/S questions on the admitted cohort with references intact; (4) a second `--apply` writes 0 rows; (5) `scripts/pg.sh -c "select object_kind, count(*) from research_os_objects group by 1"` shows the before/after delta equal to (2). Fixture success never stands in for this observation. Under "dry-run only": steps (1) and (3)-on-CI, and the PR body says 0 rows were written.

## 5. Team

- **BLUE:** Dux and release owner, this window (Opus 5 `xhigh`). Implementer Sonnet 5 (`model: "sonnet"`), Haiku 4.5 support; no other implementing family. Adversarial reviewer: an independent Codex seat (`gpt-5.6-sol`, read-only). **Council (Gear 3, declared for the production effect):** Codex Sol + Kimi K3 (`kimi-code/k3`), both review-only, journal recorded. Final gate: a fresh Opus 5 `xhigh` session outside the chain (`max` only if declared as Gear-3 adjudication); it publishes `harness/fable-gate` on HEAD and posts the receipt; it signs, never arms.
- **ORANGE**, only if declared before opening: every seat from §1bis.
- Record each seat's effective model, effort and thread id in `brief.yml`.

## 6. Appetite and stop-loss

- **Budget:** `appetite.wall_clock_hours: 6`, 2 adversarial rounds, 2M tokens; descendants: at most 6 attempts in total, concurrency 2, depth 1, 1 continuation hop; adapter tool ceiling 150 tool calls per child, ship reserve N = 0. Declared in `brief.yml`; `spend:` in `pack.yml`.
- **Deadline:** opening + 6 h, bound as in R1 §6 (`renew(opening + 21,600 − now)` if the ledger's deadline is not the recorded instant). Renewal authority: the staff room, once, by the same formula.
- **Limits:** three reds for one cause → suspend; fix-of-a-fix depth 1.
- **Checkpoints:** as R1 §6 (generation-bound envelopes, BLUE/ORANGE wake-up routes), keys `R2-checkpoint` / `R2-ack`.
- **Stop and escalate if:** admission needs a canonical field the contract lacks (freeze-change proposal, stop); legacy data needs a statement transformation that is not a pure mapping (the atomizer — deferred); the backfill would write anywhere else; D7 cross-implementation hashing turns out to be required; the write identity is not granted (finish at dry-run).

## 7. Evidence and release

- **Evidence:** as R1; the admission and backfill reports under this slice's evidence directory, counts and hashes only.
- **Release:** three separate commands as R1; arm at PR-open; open only after (a) R1 merged and integrated and (b) Z2a is recorded with scope and expiry in the ledger and in `brief.yml`. Without Z2a the window ends at a built, reviewed, unpushed candidate and a checkpoint that says so.
- **Gate:** as R1 §7 plus the council journal (two non-Anthropic review seats) in `pack.yml`.
- **Merge order:** (1) R1 code PR; (2) R2 code PR; (3) the tranche ledger-only PR.
- **Deploy (Z2a):** merge triggers the deploy workflow; observe rollout and migration completion separately (`fly-deploy.yml`), then the `fly-split-brain-verify` procedure, then §4's production observation (dry-run first; `--apply` only under Z2b with Zero's identity).
- **Rollback:** stop canonical writers (no further `--apply`), preserve and inventory the appended rows (P06 keeps audit history), revert the code PR when appropriate, assess schema state independently. Production DELETE/DROP/TRUNCATE and trigger removal are not rollback steps and are not authorized here. A change to any legacy table is a separate recovery incident.

## Adversarial review

**Seat:** Astra, Codex `gpt-6-astra` `xhigh`, read-only, outside the author chain.

**Round 1 (v1) — REWORK, 19 findings, 13 BLOCKER, 6 MEDIUM.** Applied in v2: D2 became storage-local (F1); the Consul persistence consumer named and its tests bound (F2); D3 rewritten as two mechanisms and a two-axis reader with abstention/quarantine (F3, F4); storage topology fixed on `research_os_objects` + one projection (F5); atomic successor+edge (F6); hash checked by the API, not SQL (F7); admission separated from mapping, zero-admission a valid outcome (F8); Z2 bounded by cohort, count, kinds, identity (F9); manifest-bound idempotent backfill and a reader-based Bites with per-kind deltas (F10); invalidation deferred, fixtures must validate (F11); test paths, evidence roots, proposal path, Fly placement (F12); no xfail, release after integration (F13); `harness/fable-gate` status (F14); council review-only, single implementing family (F15); rollback rewritten (F16); one tranche ledger PR, envelope-bound checkpoints (F17); appetite fields and the 3,600 s default (F18); guard rewritten, measurements dated, absolutes scoped (F19). Astra's additions carried: admission decisions in R1, the temporal case list, the forbidden list, "P06 slice completion".

**Round 2 (v2) — REWORK, 8 findings, 4 BLOCKER, 3 MEDIUM, 1 LOW.** Applied in v3: (1) D2 given a complete key grammar (separator, terminator, fraction to exactly six digits with a declared truncation policy, `COLLATE "C"`), admission-time rejection of >6 digits and lowercase `t`, cross-representation tests — the published schema admits `.1Z`, `.11Z`, `.0000001Z` and `t`; (2) scheduled intervals are separate families under a `subject_key` (CONTRACTS.md:141, `graph.py:135` quarantines edge-less siblings), reader signature updated, fixture 03 rewritten as two families; (3) classification-lowering successors rejected before INSERT in this slice (CONTRACTS.md:142), receipt-bearing transitions deferred; (4) Z2 split into Z2a (deployment + additive DDL) and Z2b (backfill DML), each with scope and expiry, R2 does not open without Z2a; (5) `appetite.wall_clock_hours`, `renew(opening + ceiling − now)`, numeric tool ceilings; (6) generation-bound envelopes, stale-generation rejection, ORANGE wake-up route; (7) fixture statement corrected to 218 cases (79 accepted, 139 expected rejections); (8) `.` sorts BEFORE `Z`, deploy trigger qualified by `fly-deploy.yml:11`, UPDATE sites attributed to `quality/{dedup,claim_scorer,expiry}.py`, round-1 count corrected.

**Round 3:** not run — the staff room's appetite for this file was two rounds. Each window's own adversarial reviewer re-reads this file at opening as its first round, and any finding against the spec goes back to the staff room through Zero.
