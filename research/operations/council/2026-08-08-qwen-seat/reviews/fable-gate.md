---
adversarial_review: exempt-council-artifact
---

# GATE VERDICT: SHIP-AFTER-FIXES

The document is honest, unusually so after the council pass: of the 13 evidence claims I re-verified on disk myself, 12 were exact and the thirteenth is a one-clause error inside a finding that is otherwise *stronger* than written. The seat configuration is safe **as a configuration** because it is self-suspending — it arms nothing, and §2.6/§4 correctly park every activation behind operator gates. The seat itself remains **UNARMED** until Zero's ratifications and the credential rotation; nothing in this verdict changes that.

## Exact fixes required before this document is committed

1. **W2, one clause is false on disk.** The parenthetical "`.Codex/skills/modus/…` — no `.Codex` directory exists" is wrong: `/Users/balizero/nuzantara/.Codex/` **exists** (an untracked Codex-harness dir containing `agents/`, `hooks/`, `config.toml`, `hooks.json`). What does not exist is `.Codex/skills/`, `.Codex/rules/`, `.Codex/commands/` — so all 6 `.Codex`-prefixed references in the `.agents` copy still fail to resolve (3× the `skills/modus` ledger paths at SKILL.md lines 38/73/179, 2× `rules`/`commands` at 216/217, 1× a `claude-glm`→`Codex-glm` wrapper rename at 122 that corrupts prose, not just paths). Rewrite the clause to the measured truth. The finding survives and is *sharper*: the machine-rename also rewrote factual content served to non-Claude harnesses, exactly the seats the canonical store exists for. This is the W113 shape — a correction pass (v3) that itself shipped an unverified negative — in a document that says "all figures re-measured".
2. **R1 gate compliance (required check, no path filter — the PR will be red as-is).** `scripts/check_adversarial_review.py` gates every new `research/**/*.md`: (a) the candidate needs frontmatter `adversarial_review: codex` (the gate accepts exactly ONE known seat token — full three-seat parentage stays in §3) plus a body heading matching `^##+ Adversarial review` — retitle `# 3. Council record` to `## Adversarial review — council record`; (b) the 7 council artifacts (3 reviews + 4 prompts) need frontmatter `adversarial_review: exempt-council-artifact` (honest per W109: none declares `sources`/`client_case`/`discovered_by`); same for `fable-gate.md` once filled.
3. **Declare Gemini's parentage (W100).** The costruttivo prompt says verbatim "you do not need repository access" — that chair reviewed the document as appended text. §3.1 and every "converged (all three seats)" claim must account the convergence as 2 disk-grounded + 1 text-only.
4. **§3.5 carries this verdict verbatim or by file pointer** — never a generator paraphrase (see governance findings).
5. *Minor, fold into fix 1:* §3.4's disposition table skips M-F5 (GLM's precision-softness finding) with no row; it is de-facto accepted via R-F8 — add the row or a note.

# SPOT-CHECKS (re-verified on disk this session)

| Claim | Command | Result |
|---|---|---|
| W2: untracked `.agents/skills/modus/` on M5 main checkout, differing, phantom paths | `git -C /Users/balizero/nuzantara status --short -- .agents/skills/modus`; `cmp`; `grep -c '\.Codex'` both copies; `ls .Codex/skills/modus` | **CONFIRMED** — `??` untracked; SKILL.md 56,743 B ≠ `.claude` 56,785 B; 6 `.Codex` refs vs 0; `.Codex/skills/modus/` → "No such file". **Except**: `.Codex/` itself exists (fix 1). M-F2 REJECTED disposition **correct** — GLM searched the worktree, where untracked files don't propagate |
| W3: three sacred docs disagree, and move between refs | `sed -n '1,10p' docs/AI_ONBOARDING.md` (worktree = origin/main) vs same on `/Users/balizero/nuzantara`; `grep -nE "327\|746\|1449" AGENTS.md` | **CONFIRMED** — 330/689/1307 [origin/main] vs 332/673/1277 [local] vs AGENTS.md 327/746/1449. The M-F1 adjudication (both parties quoted different refs correctly) holds |
| W9: discovery blind corner + mypy hollowing | `find … -name 'test_*.py' \| wc -l`; `grep -n testpaths pytest.ini pyproject.toml`; `sed -n '54,60p;103,110p' pyproject.toml` | **CONFIRMED** — 1307 in `backend/tests`, 276 in `apps/backend-rag/tests` (outside `testpaths`), 1610 total; `[tool.mypy]` at L56 with `backend.* → ignore_errors=true` at L106-107. GLM's "tests/=34" was a repo-root scope error (root `tests/`=34); its "mypy unlocated" was false — **M-F6 REJECTED disposition correct** |
| S3 figures | `wc -l .husky/pre-push`; `find .github/workflows …`; `grep -l merge_group *.yml \| wc -l` | **CONFIRMED** — 657 / 89 / **22** (candidate right; Codex's 21 was off by one; my own first count returned 0 from a zsh glob abort — probe re-run, the W97 lesson live) |
| P0 mitigation | `stat -f '%Sp' ~/.qwen/settings.json`; `ls -ld ~/.qwen/projects`; key-paths-only walk | **CONFIRMED** — 0600 / 0700; `BAILIAN_TOKEN_PLAN_API_KEY` set → rotation `operator[credential]` still OPEN |
| R-F5 name collision | `ls -l ~/scripts/qwen-code-review.sh`; `grep -n -i qwen scripts/arsenal_probe.py` | **CONFIRMED** — legacy wrapper exists (Apr 13); the probe knows only the *local* `qwen3.5` seat, not the cloud seat — the rename to `qwen-cloud-code` is necessary |
| S1 ledger sizes; catalog; apps counts; Q1 textual basis | `stat -f '%z' …PENDING-ARMS.md` both refs; catalog via `python3 -c`; `find apps -maxdepth 1 -type d \| wc -l` both refs; `grep -n "merge" AGENTS.md` | **CONFIRMED** — 793,771 B [local] / 972 KB [origin/main]; `2026-04-16`, 46 entries; 34 [local] / 32 [origin/main]; AGENTS.md:11 "Never merge your own work, never push to `main`, never arm" |

# Q1–Q4 FINAL RULINGS

**Q1 — RULED: no doctrine conflict; AGENTS.md binds this seat.** The two documents govern different actor-classes: CLAUDE.md's SHIP-LIFECYCLE replaces an absent codeowner with *the Claude session*; AGENTS.md:11 explicitly binds external agents ("never merge your own work, never push to main, never arm"). This matches the standing Antigravity precedent (external arm builds; Claude session independently verifies, ships, proves live). The seat prepares — branch, diff, tests, PR — and a Claude lane grades, merges, arms. The one-line cross-reference in both docs is approved as follow-up work for a Claude lane. Zero's ratification is a formality on this one; the reading is not in doubt.

**Q2 — RULED: the seat never self-arms, ever.** A Claude lane authors the probe registration and wrapper. Cumulative preconditions before any arming PR: credential rotated to Keychain (`operator[credential]`); allowlisted wrapper (no `review submit`/`publish-assets`/`channel`/`serve`/`--comment`, approval `plan`, `--chat-recording=false` wrapper-enforced); caps + stop-loss per Zero's confirmed contract; collision audit vs the legacy wrapper; machine-scoped probe semantics (the Mini daily probe must not overwrite M5 seat truth with `NOT_INSTALLED`); structured probe per R-F6 (no shell pipeline, true rc, exact match, provider metadata, fallback off).

**Q3 — RULED: no cron, no cascade entry, interactive/additive-only.** Qwen Code CLI is a 7th *cloud* stack with its own billing domain (~6.95M tokens in one session on the token-plan endpoint). Under the cost-constraint hard rule, the subscription's economics (plan, reset, overage, concurrency) require Zero's explicit confirmation — that authorization is `operator[business]` by Legge 5 and cannot be ruled by a gate. The default posture I *can* rule is final: until that confirmation, the seat exists only as this unarmed candidate.

**Q4 — RULED: absolute PII bar, no exceptions, Kimi-class.** Same regime as the other Chinese-cloud seat: no direct access to CRM/WhatsApp/OSINT/Drive or unredacted tool output; prompt instructions are not controls; any future input passes a deterministic local redaction/aggregation gateway; the Ollama lane keeps all PII transforms. **Q5 is ratified as part of Q4, not as a nicety**: a runtime that transcribes every tool output to disk is itself a PII retention surface — recording-off must be wrapper-enforced (0600/0700 mitigations verified on disk today; they are necessary, not sufficient).

# Answers to the gate's three assessment questions

**1. Is the analysis honest?** Yes — v3 is evidence-dense, ref-labels its claims, and its strengths section is verified, not flattery (S1/S3/S6 all held under my own commands). One residual defect (the `.Codex` clause, fix 1). One thing it **misses** as a named structural weakness: the operator-queue itself as the binding capacity constraint. The document cites the 972 KB PENDING-ARMS ledger as strength-evidence (S1) and lists aging `operator[*]` items (W8, §4), but never names the pattern: in a one-human organism, `operator[credential]`/`operator[business]` throughput is the scarcest resource, the suspension ledger only grows, and every council (this one included) *appends* to that queue. Worth a W10 in some future revision; not blocking.

**2. Is the seat safe to adopt?** Adopt-after-fixes, as configured: additive-only in intent, self-suspending in mechanism, with the correct inversion of skill-canonicality, the correct rename, and the correct refusal to self-arm. Its safety currently rests on declared discipline plus the council's preconditions — which is acceptable *only because* nothing is armed.

**3. Are the dispositions correct?** Yes — all 18 checked, including both REJECTED rows, which I confirmed independently (M-F2: GLM's refutation was a worktree blind spot; M-F6: the mypy config exists exactly where claimed). No mis-disposition found; one bookkeeping gap (missing M-F5 row, fix 5).

# Governance findings on how this council was run

- **The generator convened its own final gate and pre-created its output file.** `prompt-fable-gate.md` was authored by the candidate's author, and a headless gate attempt at 23:33 produced zero-byte `fable-gate.md`/`fable-gate.err` (no `.done` marker). The invariant held in practice — this session is sequential, independent, and read disk directly — but the failure mode deserves naming: an orchestrator that owns dispositions (as this one does, legitimately) must never also be the scribe of its own gate's verdict. Hence fix 4: §3.5 quotes this verdict verbatim or points to the file; the gate record is written by the gate lane, never summarized by the generator.
- **Chair asymmetry undeclared** (fix 3): Gemini reviewed text-only by prompt design; Codex and GLM had disk access. The prompts themselves were well-constructed — genuinely differentiated chairs, no leading verdicts, cross-family (GPT/Gemini/GLM vs a Qwen generator, with Fable gating) — this is the anti-W100 composition done right, minus the parentage declaration.
- **Positive findings:** worktree discipline held (all writes confined to `agent/air-m5/ops/qwen-seat-review`; only the two expected untracked paths); the generator's main-checkout access was read-only; W65 was practiced for real — the refuter hallucinated twice and was caught on disk, and my own re-verification caught my own broken probe (the zsh glob abort) before it could report a false zero.

**Recorded:** decision saved to MOS (`mem`, importance 8) + memory file `decision_qwen_cloud_code_seat_gate_ship_after_fixes_2026_08_08.md`. The MEMORY.md index line was deliberately skipped — the index sits at 24,976 of its ruled 24,985-byte cap, and adding a line would push tail entries off; a session doing index curation should fold it in. Applying the five fixes and committing the document is a Claude lane's work, not this gate's — per the mandate I modified nothing in the repo.
