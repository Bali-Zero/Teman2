---
date: 2026-05-26
domain: operations
client_case: none
status: handoff
sources:
  - PR #882 merged to main (800ebbc568)
  - research/operations/specs/L5.2-server-side-enforcement-2026-05-26.md
  - research/operations/audits/2026-05-26-L5.2-phase0-empirical-audit.md
  - research/operations/audits/2026-05-26-L5.2-phase1-ship-report.md
---

# L5.2 Phase 2 / 3 / 4 — Copy-Paste Prompts per future sessioni

Phase 0 ✅ + Phase 1 ✅ SHIPPED 2026-05-26 (PR #882 merged `800ebbc568`).
Branch protection main ora enforce CODEOWNERS + dismiss-stale + last-push-approval + strict.

Sotto: 3 prompt self-contained per quando vuoi continuare il rollout.

---

## PROMPT PHASE 3 (priority alta, low-risk, ~30 min)

> **CONTEXT**: L5.2 Phase 1 shipped 2026-05-26 (PR #882, branch protection enforce CODEOWNERS). Phase 0 audit ha rilevato che `.github/CODEOWNERS` ha 2 entry con path che **non esistono** sul filesystem:
> - `/apps/backend-rag/backend/services/auth/` (no directory)
> - `/apps/backend-rag/backend/services/billing/` (no directory)
>
> Sono forward-declarations o stale entries.
>
> **TASK**: Phase 3 cleanup CODEOWNERS. Decide per ognuna delle 2 entry:
> 1. **Option A**: rimuovere entry (se forward-decl mai materializzata)
> 2. **Option B**: lasciare entry + aggiungere comment esplicito `# forward-decl, materialize when services/auth or services/billing dirs created`
>
> Verifica:
> - `git log --all --source --remotes -- 'apps/backend-rag/backend/services/auth*' 2>&1 | head -10` per capire se dir è mai esistita
> - `git log --all --source --remotes -- 'apps/backend-rag/backend/services/billing*' 2>&1 | head -10` idem
> - Cross-check con `apps/backend-rag/backend/app/auth/` (esiste) per capire se path corretto è `app/auth` invece di `services/auth`
>
> **Procedure**:
> 1. Crea worktree dedicato: `python scripts/agent_start.py --lane workflow-discipline --task-id L5.2-phase3`
> 2. Edit `.github/CODEOWNERS` con scelta A o B per ciascuna entry
> 3. Commit con messaggio `docs(codeowners): L5.2 Phase 3 cleanup stale entries (services/auth + services/billing)`
> 4. Push + open PR `L5.2 Phase 3: CODEOWNERS cleanup`
> 5. NOTA: il branch protection ora enforce CODEOWNERS sul file CODEOWNERS stesso (matching `/.github/CODEOWNERS @Balizero1987`), quindi avrai bisogno di self-approve flow come PR #882 (relax `require_last_push_approval` temporaneamente).
>
> **DONE quando**: PR merged + CODEOWNERS file non ha più path mismatch vs filesystem.
>
> **Reference**: `research/operations/audits/2026-05-26-L5.2-phase0-empirical-audit.md` § "Hot-zone paths reality check".

---

## PROMPT PHASE 2 (priority media, ~12-16h work, è il pezzo più impegnativo)

> **CONTEXT**: L5.2 Phase 1 shipped 2026-05-26 (branch protection CODEOWNERS enforce attivo). Phase 2 implementa il vero server-side enforcement: una GitHub Action che **replica lato server i lease-check + lint-launchagents + lint-migration** che attualmente girano solo client-side via husky pre-commit. Questo chiude il bypass `HUSKY=0` documentato in cicatrix W34.
>
> **TASK**: Phase 2 ship `.github/workflows/hot-zone-pr-gate.yml` come required check.
>
> **Spec di riferimento**: `research/operations/specs/L5.2-server-side-enforcement-2026-05-26.md` § "Componente 2".
>
> **Architettura richiesta** (5 critical contracts dal panel iter-1):
> - **C1**: workflow gira anche su PR che modifica `.github/workflows/` stessi (anti workflow self-modification trap)
> - **C2**: required check con `expected source app` (app_id=15368, anti CI spoofing)
> - **C3**: NO `paths:` filter — workflow gira SEMPRE, decide internamente cosa controllare (anti skip-success trap)
> - **C4**: `dismiss-stale-reviews` + `require-latest-push-approval` (✅ già attivi via Phase 1)
> - **C5**: per merge queue, check ricalcola su latest base SHA (anti lease stale race)
>
> **Plus Componente 2.5 (L2.A2 amendment)**: CODEOWNERS self-modification protection — Telegram alert + block se non-human modifica `.github/CODEOWNERS`.
>
> **Logica workflow**:
> 1. Trigger: `pull_request` + `merge_group`
> 2. Checkout diff base SHA (NOT branch HEAD — anti `pull_request_target` trap)
> 3. Per ogni file in diff, classifica hot-zone vs cold
> 4. Hot-zone paths (audit-verified 2026-05-26):
>    ```
>    apps/backend-rag/backend/db/migrations_v2/
>    apps/backend-rag/backend/app/auth/
>    apps/backend-rag/backend/app/core/config.py
>    apps/backend-rag/backend/services/invoicing/
>    apps/backend-rag/backend/services/pricing/
>    scripts/nuzantara-sentinel.py
>    scripts/dlq_autopilot.py
>    scripts/lint_launchagents.sh
>    scripts/lint_migration_numbers.py
>    infra/launchagents/
>    docs/infra/launchagents/
>    .github/workflows/
>    .github/CODEOWNERS
>    fly.toml
>    apps/backend-rag/fly.toml
>    ```
> 5. Se hot-zone files presenti:
>    - Connect Redis lease registry via `secrets.REDIS_URL` (already configured 2026-04-11)
>    - Verifica nessun lease attivo
>    - Re-esegue `scripts/lint_launchagents.sh` se LaunchAgent files toccati
>    - Re-esegue `scripts/lint_migration_numbers.py` (W41 lint) se migrations toccate
> 6. Fail-CLOSED se Redis non raggiungibile (hot-zone presenti)
> 7. Componente 2.5: separate step `if contains(changed_files, '.github/CODEOWNERS')` → Telegram alert via `secrets.TELEGRAM_BOT_TOKEN` + `secrets.TELEGRAM_OWNER_CHAT_ID` (already configured) + block se actor != Balizero1987
> 8. Output: required check `hot-zone-enforcement` con conclusion success/failure/neutral. Mai `skipped`.
>
> **Test plan**:
> - PR dummy che tocca `migrations_v2/*.sql` → check fires, lint replay, pass/fail come previsto
> - PR dummy che tocca `.github/CODEOWNERS` da actor non-Balizero1987 → block + Telegram alert
> - PR dummy con Redis down (chaos test) → fail-CLOSED hot-zone, graceful cold
> - Workflow self-modification PR → check ancora gira (C1)
>
> **Rollout in 2 step**:
> - Phase 2a (monitor-mode): `continue-on-error: true`, output solo logs, 1 settimana data
> - Phase 2b (enforce-mode): flip + aggiungi check name al `required_status_checks.contexts` array via `gh api PUT`
>
> **Kill switch**: env `L5_2_SERVER_ENFORCEMENT_ENABLED=false` nel workflow → return neutral.
>
> **Procedure**:
> 1. Worktree: `python scripts/agent_start.py --lane workflow-discipline --task-id L5.2-phase2`
> 2. Implementa workflow file
> 3. Phase 2a monitor-mode 1 settimana
> 4. Phase 2b flip a enforce + add a required checks
> 5. PR + merge con self-approve flow
>
> **DONE quando**: PR dummy hot-zone touch blocca correttamente in enforce-mode, Redis fail-closed verified, Telegram alert su CODEOWNERS self-mod verified.

---

## PROMPT PHASE 4 (priority bassa, ~6-8h, polish)

> **CONTEXT**: L5.2 Phase 1 ✅ + Phase 2 ✅ shipped. Phase 4 aggiunge l'observability layer: **audit append-only log su GitHub issue + Telegram alert real-time per soli hot-zone events**.
>
> **TASK**: Phase 4 ship audit + Telegram pipeline.
>
> **Spec**: `research/operations/specs/L5.2-server-side-enforcement-2026-05-26.md` § "Componente 4".
>
> **Mechanism A — Server-side audit GitHub issues**:
> - Cron GitHub Action `on: push` su main: detecta commit con bypass signal (no `Hook-Verified` trailer OR replay server-side fail)
> - Append a issue dedicata `[AUDIT] Workflow discipline bypass log` (evergreen, write-only)
> - Event log structured: `{timestamp, actor, sha, files_changed, bypass_signal, source}`
>
> **Mechanism B — Telegram real-time SOLO hot-zone**:
> - Trigger: hot-zone files modified + bypass signal detected + direct push main + workflow self-modification
> - Use `sentinel_lib/alerter.py` retry pattern (W55 cicatrix) per resilienza
> - **NO Telegram per**: HUSKY=0/--no-verify su feature branch (alert fatigue, panel Q4)
>
> **Mechanism C — Digest settimanale**:
> - Cron lunedì 08:00 WITA: summary 7-day bypass events
> - Format: bypass count by file zone + actor + intent (commit msg analysis)
> - Pipe via Telegram digest channel
>
> **Canary obbligatorio (Codex panel iter-1 missing failure mode #14)**:
> - Daily canary: Telegram bot health-check + GitHub issue write permission verify
> - Se fail 2 ticks consecutivi → fallback alert via email Antonello
>
> **Test plan**:
> - Dummy push con HUSKY=0 → audit issue append + NO Telegram (feature branch)
> - Dummy hot-zone push main → audit + Telegram fire
> - Disable Telegram bot token → canary fire after 2 days → email fallback
>
> **Procedure**:
> 1. Worktree: `python scripts/agent_start.py --lane workflow-discipline --task-id L5.2-phase4`
> 2. Implement audit workflow (Mechanism A)
> 3. Implement Telegram alert workflow (Mechanism B)
> 4. Implement digest cron (Mechanism C)
> 5. Implement canary (Mechanism D)
> 6. PR + merge
>
> **DONE quando**: dummy bypass detection + audit issue append + Telegram alert hot-zone only + 30-day observation senza alert fatigue.
>
> **Sister cicatrix scar**: W55 alerter retry pattern (per resilienza Telegram).

---

## Summary stato L5.2 post-2026-05-26

| Phase | Status | Effort speso | Effort residual |
|---|---|---|---|
| Phase 0 (empirical audit) | ✅ SHIPPED | ~1h | - |
| Phase 1 (branch protection) | ✅ SHIPPED | 10 min | - |
| Phase 2 (hot-zone-pr-gate.yml) | ⏳ READY | - | 12-16h |
| Phase 3 (CODEOWNERS cleanup) | ⏳ READY | - | 30 min |
| Phase 4 (audit + Telegram) | ⏳ READY | - | 6-8h |
| Phase 5 (30d observation) | ⏳ PENDING | - | passive |
| **Totale stimato** | | **~1h10min** | **~18-25h** + 30d observation |

L5.1 iter-2 status `pending-iter-3` — 7 difetti residui R1-R7 (vedi `specs/L5.1-iter2-agent-worktree-enforcement-2026-05-26.md`). Affrontare DOPO Phase 4 completata.

## Notes for next session

- Branch protection ha `require_last_push_approval=true` → merge PR proprie richiede temporary relax (vedi PR #882 workflow per pattern)
- CODEOWNERS rules attive: tutte PR che toccano hot-zone paths richiedono review esplicita Balizero1987
- `auto-merge-whitelist.yml` già attivo: PR Dependabot/bot su path non-protetti auto-merge
- Redis Pro running, lease registry vuoto
- Tutti secrets necessari (`REDIS_URL`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_OWNER_CHAT_ID`) già configurati GitHub Actions
