You are a cross-family second-opinion reviewer (adversarial refuter role) for a security-finding review in the Bali Zero "nuzantara" monorepo. A prior analyst reported 3 findings about a scenario where a CLIENT-role user (logged into my.balizero.com / the client portal) opens https://kita.balizero.com/dashboard (the STAFF/operator workspace) in the same browser, because both domains share the same `nz_access_token` cookie.

Your job: try to REFUTE each finding — find a reason it is wrong (mis-read code, cites a path/line that doesn't say what's claimed, a symptom mistaken for a cause, or a protection that exists elsewhere in the code that the analyst missed). Do not just agree. Be specific and cite file:line if you disagree.

=== FINDING 1 ===
Title: kita.balizero.com's edge middleware performs zero identity check — hasPortalSession() is scoped only to my.balizero.com
Claim: apps/mouth/src/proxy.ts's isAppDomain branch (~line 441-552) is a pure host/pathname router (legacy redirects, SEO headers, subdomain rewrites) and never reads/validates the nz_access_token cookie. hasPortalSession() (defined ~line 157-159) is called exactly once in the whole file, inside the isPortalDomain branch (~line 262) — never for isAppDomain (kita.balizero.com). Live curl to https://kita.balizero.com/dashboard returns HTTP 200 with byte-identical body/etag whether or not any cookie is sent (Vercel prerendered page, x-nextjs-prerender:1, x-vercel-cache:HIT).

=== FINDING 2 ===
Title: Backend authorization is architecturally split from authentication: only 3 of the router files gate on role, everything else uses ownership-scoping or nothing
Claim: HybridAuthMiddleware.dispatch (apps/backend-rag/backend/middleware/hybrid_auth.py, dispatch method starting ~line 180) fail-closes on identity (401 if no valid token) but never inspects the `role` claim. get_current_user() (apps/backend-rag/backend/app/deps/auth.py) passes role through verbatim from the JWT/session. require_team_member (apps/backend-rag/backend/app/deps/auth.py ~line 155-168) is the only dependency that 403s role=='client', and `grep -rl require_team_member apps/backend-rag/backend/app/routers/` returns only 3 files: e33_cases.py, debug.py, crm_intelligence.py. Most CRM/workspace endpoints (e.g. crm_clients.py list_clients ~line 757+, dashboard.py get_clients_geo ~line 810+) instead use get_crm_user_filter (apps/backend-rag/backend/app/deps/crm_access.py ~line 76-94), which returns None (full access) only for admins (is_crm_admin) and otherwise filters `assigned_to = caller's own email` — this only coincidentally excludes clients because assigned_to is populated exclusively with staff emails today, not because it was designed as a role gate. dashboard.py get_stats (~line 975-1029) takes `request: Request` only — NO Depends(get_current_user) at all in its signature — though the global middleware still requires a globally-valid (any-role) token for that route (curl confirmed 401 without any credentials).

=== FINDING 3 ===
Title: The client-side role redirect (kita workspace layout) is UX-only and happens to be airtight in this specific scenario, but only because of an unrelated loading-gate design
Claim: In apps/mouth/src/app/(workspace)/layout.tsx, the render guard is `if (isLoading || !gateChecked)` (~line 339) which shows ONLY a loading spinner. `gateChecked` is set true only inside `refetchGate()` (~line 159), and the client-role check (`if (profile?.role === "client") { window.location.replace(...); return; }`, ~line 172-182) runs BEFORE `await refetchGate()` (~line 199) and returns early — so gateChecked never becomes true for a client session, and the full workspace JSX (sidebar/header/children, ~line 390-483) never mounts. The claim is that this full protection is a side effect of a guard built for a different purpose (an unrelated INTAKE-gate anti-flash design per the comment at ~line 337-338), not itself a deliberate security control — and that it would not stop a client who issues the underlying HTTP requests directly (bypassing the React component), which is why findings 1 and 2 (edge + backend) are the real boundary questions.

=== YOUR TASK ===
For EACH of the 3 findings, answer:
1. Do you find a flaw in its reasoning or is a cited file:line likely NOT to say what's claimed? (You may not have direct repo access — reason from Next.js/FastAPI conventions and flag anything structurally implausible, e.g. "middleware.ts pure host routers without a cookie check are common and this is not unusual, so this alone would not be a real exposure unless paired with a data-bearing SSR fetch" etc.)
2. Is there a common web-security pattern this analysis might have missed that would ALREADY mitigate the issue (e.g. Next.js RSC data fetching being separately authenticated, a WAF/edge rule outside the repo, CSRF-scoped cookies, SameSite cookie scoping across subdomains that would prevent the cookie from even being sent)?
3. Verdict per finding: SURVIVES (evidence supports it) or REFUTED (give the specific reason).

Be terse. Structure your answer as:
FINDING 1: <verdict> — <reason>
FINDING 2: <verdict> — <reason>
FINDING 3: <verdict> — <reason>
OVERALL: <1-2 sentence summary>
