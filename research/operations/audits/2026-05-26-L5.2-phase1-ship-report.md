---
date: 2026-05-26
domain: operations
client_case: none
phase: L5.2-Phase-1
status: SHIPPED
sources:
  - gh api /repos/Balizero1987/Teman2/branches/main/protection (pre + post)
  - /tmp/branch-protection-payload.json (PUT body)
  - PR #882 statusCheckRollup post-patch
---

# L5.2 Phase 1 — Ship Report (2026-05-26 14:55 WITA)

## Status

✅ **SHIPPED in 10 min** (vs original 4-6h estimate).

Phase 0 audit aveva rivelato che CODEOWNERS già esisteva su main; mancava solo branch protection update per leggerlo. Phase 1 = singolo `gh api PUT` call.

## What changed

| Setting                           | Before   | After                      |
| --------------------------------- | -------- | -------------------------- |
| `enforce_admins`                  | True     | True (unchanged)           |
| `required_pull_request_reviews`   | **NULL** | object configured          |
| `required_approving_review_count` | n/a      | **1**                      |
| `dismiss_stale_reviews`           | n/a      | **True**                   |
| `require_code_owner_reviews`      | n/a      | **True** ← KEY             |
| `require_last_push_approval`      | n/a      | **True**                   |
| `required_status_checks.strict`   | False    | **True** ← anti base-stale |
| Required checks count             | 9        | 9 (unchanged)              |
| `allow_force_pushes`              | False    | False (unchanged)          |
| `allow_deletions`                 | False    | False (unchanged)          |

## Failure modes addressed (vs panel iter-1/iter-2 findings)

| Failure mode                                       | Source                      | Status                                                                                                                       |
| -------------------------------------------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| CODEOWNERS-orphan (file exists but no enforcement) | Phase 0 audit               | ✅ CLOSED                                                                                                                    |
| Approval stale race                                | Codex iter-1 Q3             | ✅ CLOSED (require_last_push_approval=true)                                                                                  |
| Branch base-stale merge                            | Codex iter-1 Q8 lease-stale | ✅ CLOSED (strict=true)                                                                                                      |
| Dismiss stale on new push                          | Codex iter-1 Q3             | ✅ CLOSED (dismiss_stale_reviews=true)                                                                                       |
| Admin bypass review                                | Pre-existing                | Partially CLOSED (enforce_admins=true already, but admin can still self-approve own PR — accepted limitation for single-dev) |

## Empirical evidence post-ship

### PR #882 (this branch) state change

| Field                  | Before patch                    | After patch                                    |
| ---------------------- | ------------------------------- | ---------------------------------------------- |
| `mergeStateStatus`     | BLOCKED (CI fail)               | BEHIND (branch up-to-date enforced, CI now ok) |
| `mergeable`            | MERGEABLE                       | MERGEABLE                                      |
| `reviewDecision`       | (none)                          | **REVIEW_REQUIRED** ← CODEOWNERS active        |
| Backend Tests (Python) | FAILURE (fastapi MAL-2026-4750) | SUCCESS (main fixed in parallel)               |

CODEOWNERS enforcement live: PR #882 ora richiede approval esplicita su path protetti (questa PR tocca research/\* + AGENTS.md → AGENTS.md potrebbe match CODEOWNERS rules, verificare).

### Pre-patch backup

Snapshot completo pre-PATCH preservato in:

- `research/operations/audits/2026-05-26-branch-protection-pre-L5.2-phase1.json`
- Size: 1606 bytes
- Restore: `gh api -X PUT /repos/.../branches/main/protection --input <file>` (filtered for writable fields only)

## PUT payload used

```json
{
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "E2E Tests (Playwright)",
      "MCP Server Tests",
      "Frontend Tests (Next.js) (mouth)",
      "Detect Secrets",
      "Backend Tests (Python)",
      "Bandit Python Security",
      "CodeQL Analysis (python)",
      "CodeQL Analysis (javascript)",
      "root-guard"
    ]
  },
  "enforce_admins": true,
  "required_pull_request_reviews": {
    "required_approving_review_count": 1,
    "dismiss_stale_reviews": true,
    "require_code_owner_reviews": true,
    "require_last_push_approval": true
  },
  "restrictions": null,
  "allow_force_pushes": false,
  "allow_deletions": false,
  "required_linear_history": false,
  "required_conversation_resolution": false
}
```

## Rollback procedure

Se Phase 1 causa friction inattesa (es. Antonello bloccato su PR proprie con CODEOWNERS che richiedono se stesso):

```bash
# Option A: disable CODEOWNERS enforcement, keep review count
gh api -X PUT /repos/Balizero1987/Teman2/branches/main/protection \
  --input <(jq '.required_pull_request_reviews.require_code_owner_reviews=false' \
    research/operations/audits/2026-05-26-branch-protection-pre-L5.2-phase1.json)

# Option B: full rollback to pre-Phase-1 state
# (need to extract writable fields from backup JSON, since GET response has URLs)
```

Override emergency:

```bash
# Direct admin push (enforce_admins still requires CI green)
git push origin main  # admin bypass review check only, NOT status checks
```

## Next steps

| Phase   | Status     | Action                                                                                             |
| ------- | ---------- | -------------------------------------------------------------------------------------------------- |
| Phase 0 | ✅ DONE    | Audit report committed (`2b41635db`)                                                               |
| Phase 1 | ✅ DONE    | Branch protection patched (this report)                                                            |
| Phase 2 | ⏳ READY   | Implement `hot-zone-pr-gate.yml` (12-16h) — può iniziare quando vuoi                               |
| Phase 3 | ⏳ PENDING | CODEOWNERS cleanup stale entries (`services/auth` + `services/billing` not on filesystem) — 30 min |
| Phase 4 | ⏳ PENDING | Audit issue + Telegram alert wiring (6-8h)                                                         |
| Phase 5 | ⏳ PENDING | 30-day observation period                                                                          |

## Lessons

- Phase 0 audit ha **risparmiato 3.5h-5.5h** rilevando che 5/7 assunzioni L5.2 erano già implementate.
- Single API call può sostituire ore di lavoro quando lo stato target è già parzialmente in place.
- `gh api PUT /branches/main/protection` è REPLACE (non PATCH): ogni call deve includere tutti i campi desiderati per evitare regressioni.
- `strict=true` (branch up-to-date required) ha effetto immediato: PR #882 è ora BEHIND. Quando vorrai merge, dovrai prima fare merge/rebase di main nel branch.

## Related artifacts

- Phase 0 audit: `research/operations/audits/2026-05-26-L5.2-phase0-empirical-audit.md`
- Pre-patch backup: `research/operations/audits/2026-05-26-branch-protection-pre-L5.2-phase1.json`
- PR #882: https://github.com/Balizero1987/Teman2/pull/882
- L5.2 spec: `research/operations/specs/L5.2-server-side-enforcement-2026-05-26.md`
