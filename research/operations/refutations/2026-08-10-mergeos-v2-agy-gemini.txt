F1 [P0] Migration-lint on merge_group skips Squawk or silently passes missing diffs due to empty PR payload variables and || true swallow.
    QUOTE: "Regola A with the diff SPECIFIED (Qwen F6): on merge_group, migration-lint and the tg-gateway anti-regrowth lint read the immutable event pair github.event.merge_group.base_sha and github.event.merge_group.head_sha, then diff exactly base_sha..head_sha... a missing or unresolvable event SHA fails closed."
    WHY WRONG: In .github/workflows/migration-lint.yml:59-62, diff extraction uses base="${{ github.event.pull_request.base.sha }}" and head="${{ github.event.pull_request.head.sha }}" followed by files=$(git diff ... || true). On merge_group events, github.event.pull_request is null, leaving $base and $head empty. git diff outputs nothing and || true swallows exit status, producing an empty file list. Step 73 (if: steps.changed.outputs.files != '') evaluates to false, completely skipping Squawk linting on merge_group runs while returning exit 0 (green). Furthermore, even if github.event.merge_group.base_sha were used, actions/checkout@v7 (line 40) uses filter: blob:none without fetching base_sha. An unresolvable base_sha causes git diff to fail, which || true swallows, resulting in an empty file list and a silent green pass rather than failing closed.
    EVIDENCE: .github/workflows/migration-lint.yml:40-84 (specifically lines 59-62: base="${{ github.event.pull_request.base.sha }}" and files=$(git diff ... || true)).
    SCENARIO: A PR adds a migration containing a forbidden SQL statement (e.g. DROP TABLE users;). The PR enters merge_group. migration-lint.yml runs on merge_group. Because github.event.pull_request is empty, $base is empty; git diff fails; || true suppresses the error; outputs.files is empty. The Squawk lint step is skipped. migration-lint reports SUCCESS (green), and the destructive migration lands on main.

F2 [P0] Heal PR frozen-branch protocol creates a perpetual stale-tree hazard and queue starvation.
    QUOTE: "Freeze before arm; an armed head is immutable. After R1 is green, the organ records the candidate head SHA and transitions it to ready only once. From that point through every queue state, it MUST NOT push, rebase, regenerate, or otherwise move the branch."
    WHY WRONG: A heal PR's regenerated documentation is calculated against a specific main commit M0. Under §2.2, condition (a) blocks arming if docs-affecting PRs are in-flight in the queue. When an in-flight PR #102 merges into main (producing M1), the queue clears and heal PR #101 arms. However, because §2.2 strictly forbids updating, rebasing, or regenerating the heal PR branch after R1, heal PR #101 lands documentation reflecting state M0 onto M1. The resulting merged commit M2 (M1 + H1) is immediately out of sync with M1's added routes/services. docs_sync.py --check on M2 fails, leaving main dirty again. The organ spins every 15 minutes, generating new heal PRs that either land stale content or starve indefinitely waiting for an empty queue during peak 50-merge/day traffic.
    EVIDENCE: scripts/docs_sync.py:60-150 (dynamic count extractors) and research/operations/2026-08-10-merge-os-v2-submission-system.md:108-135 (§2.2 heal PR specification).
    SCENARIO: PR #100 adds a new backend router, causing main docs to drift. docs-sync-heal creates draft PR #101 at main SHA M0. Meanwhile PR #102 (adding another router) is in the queue and merges into main (M1). The queue clears; PR #101 transitions to ready and arms without regenerating (per §2.2 freeze rule). PR #101 merges into M1 producing M2. docs_sync.py --check fails on M2 because PR #101's docs only counted PR #100's router and missed PR #102's router. main is left dirty, triggering another heal PR in an infinite loop.

F3 [P1] Wave 1 baseline organ acceptance criterion for author-class ejection rates is unmeasurable due to lack of native queue bisection.
    QUOTE: "Wave 1 — the baseline organ (7 days, NO behavior change). Nightly probe (Pro) records: billed minutes/PR (Actions billing API, not wall-clock), queue transit p50/p95, ejection rate BY CLASS (INFRA/CODE/FLAKE) and BY AUTHOR CLASS (human / agent / bot — Codex F16's split)..."
    WHY WRONG: In GitHub Native Merge Queue (merge_group), workflow runs execute against synthetic branch refs gh-readonly-queue/main/pr-... combining up to max_entries_to_build: 5 PRs (scripts/ci/setup_merge_queue_ruleset.sh:100). As acknowledged in §3 and §7.4 of the design doc itself ("native GitHub does not bisect — naming the culprit PR remains a manual step"), GitHub's Actions and GraphQL APIs do not attribute a failed merge_group run to a specific PR within a multi-PR batch. An automated Nightly probe running via script/API cannot programmatically determine which author class (human vs agent vs bot) caused an ejection in a grouped batch.
    EVIDENCE: scripts/ci/setup_merge_queue_ruleset.sh:100 (max_entries_to_build: 5), scripts/ci/queue_rearm.sh:82-89 (queue GraphQL query structure showing mergeQueue nodes contain pullRequest lists), and research/operations/2026-08-10-merge-os-v2-submission-system.md:156-157,302-304 (§3 and §7.4 manual attribution declaration).
    SCENARIO: A queue batch containing PR #201 (authored by human) and PR #202 (authored by agent bot) fails in merge_group due to a code collision between the two PRs. GitHub ejects the entire batch. The Wave 1 baseline probe inspects the failed run: GitHub API attributes the run to github-actions[bot] on the queue ref. The probe cannot attribute the failure to either the human or the agent without manual bisection, making automated recording of "ejection rate BY AUTHOR CLASS" impossible.

F4 [P1] Removing top-level paths: causes innocent PRs to be ejected on merge_group when main docs are drift-stale.
    QUOTE: "On merge_group, heavy jobs run UNCONDITIONALLY. The event check is first, structural, and not shared with any classifier logic: if: github.event_name == 'merge_group' || <classifier says needed>."
    WHY WRONG: In .github/workflows/docs-sync.yml:4-44, the workflow currently relies on top-level paths:. Removing paths: as mandated by Wave 0 and running Docs Sync Check unconditionally on merge_group (if: github.event_name == 'merge_group') means python scripts/docs_sync.py --check will execute on merge_group for EVERY PR, including pure frontend/CSS PRs that skipped docs-sync during R1. If main has existing documentation drift (or pending async heal), python scripts/docs_sync.py --check will fail on the merge_group run. The innocent CSS PR will be ejected from the queue despite introducing no docs changes.
    EVIDENCE: .github/workflows/docs-sync.yml:4-44,79 and scripts/docs_sync.py:15-18.
    SCENARIO: Developer submits PR #300 editing a single CSS file. On PR (R1), docs-sync change detection sees no docs files changed and skips (green). PR #300 enters merge_group. On merge_group (R2), docs-sync runs unconditionally per §2.1. Unrelated docs drift exists on main. docs_sync.py --check fails on the merge_group synthetic commit. PR #300 is ejected from the queue as a failure, even though PR #300 had zero docs changes.

VERDICT: BLOCK (F1, F2)
UNVERIFIABLE:
- §1 Radiography table: "25.7 min median job / 21.7 min pytest (16 real runs measured via jobs API, 2026-08-09/10)" — historical GitHub Actions API execution logs from 2026-08-09/10 are live runner telemetry not stored in the repository.
- §1 Radiography table: "~12.85 runner-h/day PR-side redundant load" and "≈ merge volume, ~49/day main-push duplicates" — live GitHub event counts and billing API records are external runtime metrics not committed in git.
- Frontmatter claim: prior refutation by "Codex gpt-5.6-sol effort=high (20 findings)" and "Qwen 3.8 Max (12 findings)" — external model review transcripts are not present in the workspace.
