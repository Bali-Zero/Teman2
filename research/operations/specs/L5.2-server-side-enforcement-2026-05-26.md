---
spec_id: L5.2
title: Server-Side Enforcement — required hot-zone check + bot privilege downgrade + audit-append-only
date: 2026-05-26
domain: operations
status: ready-for-implementation
priority_change: ELEVATED — panel iter-2 (2026-05-26) ha invertito priorità vs L5.1 (L5.2 deve shippare PRIMA)
prerequisites:
  - PR #852 (L1 worktree broker) merged to main
  - PR #853 (L2 redis lease registry) merged to main
parent_research:
  - research/operations/2026-05-26-gap23-panel-synthesis.md
  - research/operations/specs/L5.1-L5.2-panel-iter2-synthesis-2026-05-26.md (iter-2 verdict)
  - research/operations/2026-05-25-sota-workflow-gap-analysis-and-l5-spec.md
priority: P0
panel_iter2_verdict: APPROVE_WITH_MINOR_AMENDMENTS (Gemini + DeepSeek 2/2 convergent, Codex abstain)
panel_amendments_applied:
  - L2.A1 (Gemini): rollout priority — Phase 1+2 enforce PRIMA o contemporaneo a L5.1
  - L2.A2 (Gemini): CODEOWNERS self-modification protection nella GitHub Action
empirical_motivation:
  - Branch protection `enforce_admins=true` ma `required_pull_request_reviews=NONE` (verificato 2026-05-26)
  - Husky pre-commit/pre-push/post-commit zero detection di `HUSKY=0` bypass (verificato 2026-05-26)
  - Cicatrix W34 documenta `HUSKY=0 git push origin HEAD:main` usato per workaround SIGPIPE
  - Sessione 026f2cd1 (2026-05-26 03:39): PR auto-mergiata via whitelist senza review durante hook setup
  - Same trust domain: Claude bot + Antonello + Codex sulla stessa workstation, stessi token (no real independence)
---

# L5.2 — Server-Side Enforcement (post-Panel)

## Status post panel iter-2 (2026-05-26)

**Panel verdict**: APPROVE_WITH_MINOR_AMENDMENTS (Gemini + DeepSeek 2/2 convergent).

**Priorità invertita** vs sessione 026f2cd1: L5.2 deve shippare PRIMA di L5.1. Razionale (Gemini): "Costruire i recinti (L5.2) ha la priorità rispetto all'addestrare i cani (L5.1)". L5.1 iter-2 ha 7 difetti residui (R1-R7) → richiede iter-3, NON è ready-for-implementation.

**2 amendments applicati**:

- **L2.A1**: Phase 1+2 enforce-mode PRIMA o contemporaneamente a L5.1 (sezione 7 rivista).
- **L2.A2**: CODEOWNERS self-modification protection esplicita nella GitHub Action (sezione 2.4 nuova).

## TL;DR

L5.1 chiude la disciplina worktree client-side. **L5.2 chiude il gap server-side**:

1. **Bot privilege downgrade** — Claude bot non deve avere merge rights su main.
2. **Required hot-zone GitHub Action** — replica lease-check + lint-launchagents server-side al merge.
3. **CODEOWNERS scope-ristretto** — review obbligatoria SOLO su file hot-zone, SOLO per PR bot.
4. **Audit append-only** — bypass events (HUSKY=0, --no-verify, direct push) loggati su GitHub issue + Telegram per soli hot-zone events.

**Filosofia**: client hooks = ergonomia + early warning. CI server-side = enforcement non-bypassable.

**Kill switch globale**: `L5_2_SERVER_ENFORCEMENT_ENABLED=false` (env workflow).

## Motivazione empirica

| Evidenza                                                                                      | Source                                                                   | Implicazione                                                    |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------- |
| `enforce_admins=true` + `required_pull_request_reviews=NONE`                                  | `gh api repos/.../branches/main/protection` 2026-05-26 14:08 WITA        | Admin (incluso bot) può mergiare PR critical senza review umana |
| Husky scripts (pre-commit 8385B + pre-push 1659B + post-commit 487B) zero `HUSKY=0` detection | `grep -rn HUSKY .husky/` 2026-05-26 14:08 WITA                           | Bypass silenzioso lease-check + lint-launchagents + test        |
| W34 cicatrix scar                                                                             | `HUSKY=0 git push origin HEAD:main` workaround SIGPIPE                   | Bypass empiricamente usato in produzione                        |
| Sessione 026f2cd1 commit `b8df2f996` cancella `.github/CODEOWNERS` (87 entries)               | git log worktree feat/ai-agent-workflow-spec-2026-05-25                  | "Require code owner reviews" è inutile senza CODEOWNERS         |
| Same trust domain (Codex)                                                                     | Antonello + Claude bot + Codex stessa workstation, stessi token keychain | Bot reviewer LLM non è indipendenza forte                       |

## Architettura

### Tre layer enforcement

```
┌────────────────────────────────────────────────────────────────┐
│  Layer 1 — Client hooks (husky)                                │
│  Pre-commit lease-check + pre-push tests = ERGONOMIA            │
│  Bypass possibile via HUSKY=0 — accettato come trade-off        │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  Layer 2 — Server-side required check (L5.2 NEW)                │
│  GitHub Action replica lease-check + lint-launchagents al merge │
│  Non bypassable (required + fail-closed + expected source app)  │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  Layer 3 — Branch protection + CODEOWNERS scope-ristretto       │
│  Required review SOLO per PR bot + hot-zone                     │
│  dismiss_stale + require_latest_push                            │
└────────────────────────────────────────────────────────────────┘
```

### Componenti

#### Componente 1 — Bot privilege downgrade (P0.1)

**Stato attuale**: ipotetico Claude bot ha admin → può `merge_pull_request` + bypass branch protection.

**Action**:

- Audit `gh api orgs/Balizero1987/teams` + `gh api repos/Balizero1987/Teman2/collaborators` per identificare tutti gli admin.
- Se Claude bot esiste come account separato → downgrade da `admin` a `write` o `triage`.
- Se Claude opera con token di Antonello → creare account bot dedicato `nuzantara-bot[bot]` con permessi minimi (read + write su PR, NO merge, NO admin).
- Documentare in `~/.nuzantara-secrets.env` quale token usare per quale ruolo.

**Failure mode evitato**: bot+admin merge same actor (Codex Q1).

#### Componente 2 — Required hot-zone GitHub Action (P0.2)

**File**: `.github/workflows/hot-zone-enforcement.yml`

**Trigger**: `pull_request` events + `merge_group` events (per merge queue).

**Logica**:

1. Checkout diff base SHA (NON branch HEAD — anti `pull_request_target` trap).
2. Per ogni file in diff, classifica: `hot-zone` (migrations / sentinel / dlq / LaunchAgent wrappers / .github/workflows / auth / billing / pricing) vs `cold`.
3. Se `hot-zone` files presenti:
   - Connect Redis lease registry (production credentials da `secrets.REDIS_URL_RO`).
   - Verifica nessun lease attivo su file da altro agent task-id.
   - Re-esegue `scripts/lint_launchagents.sh` se LaunchAgent files toccati.
   - Re-esegue `scripts/lint_migration_numbers.py` (W41 lint) se migrations toccate.
   - Verifica `required_approving_review_count >= 1` su PR.
4. **Fail-closed** se Redis non raggiungibile (hot-zone files presenti). **Graceful warn** se Redis non raggiungibile (solo cold files).
5. Output: required check status `hot-zone-enforcement` con `conclusion: success | failure | neutral`. Mai `skipped`.

**Critical contracts**:

- **C1**: workflow DEVE girare anche su PR che modificano `.github/workflows/` stessi (anti workflow-self-modification trap, Codex Q6 missing failure mode).
- **C2**: required check `expected source app: github-actions` (anti CI spoofing, Codex Q4 missing failure mode).
- **C3**: NO `paths:` filter — workflow gira SEMPRE, decide internamente cosa controllare (anti skip-success trap, Codex Q5 missing failure mode).
- **C4**: `dismiss-stale-reviews` + `require-latest-push-approval` configurati su branch protection (anti approval stale race, Codex Q3 missing failure mode).
- **C5**: per merge queue, check ricalcola su latest base SHA (anti lease stale race, Codex Q8 missing failure mode).

**Empirical motivation**: cicatrix W34 documenta bypass HUSKY=0 in produzione. Senza C1-C5, ogni patch a workflow stesso o ogni PR con stale approval può bypassare.

#### Componente 2.5 — CODEOWNERS self-modification protection (L2.A2, panel iter-2 amendment)

**File**: aggiungere step al workflow `hot-zone-enforcement.yml` (Componente 2).

**Logica**:

```yaml
- name: Detect CODEOWNERS self-modification
  if: contains(steps.changed-files.outputs.all_changed_files, '.github/CODEOWNERS')
  run: |
    echo "🚨 CRITICAL: PR modifies .github/CODEOWNERS"
    # Telegram alert INDIPENDENTE dal lease check (cf L2.A2)
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
      -d "chat_id=${{ secrets.TELEGRAM_OWNER_CHAT_ID }}" \
      -d "text=🚨 PR #${{ github.event.pull_request.number }} modifies CODEOWNERS by ${{ github.actor }}: ${{ github.event.pull_request.html_url }}"
    # Forza failure se actor non è Antonello human
    if [ "${{ github.actor }}" != "Balizero1987" ]; then
      echo "❌ CODEOWNERS modification by non-human actor — BLOCK"
      exit 1
    fi
```

**Razionale (Gemini panel iter-2 Q9)**: bot ridotto a `write` può ancora auto-modificare `.github/CODEOWNERS` rimuovendo Antonello dai ruleset all'interno del proprio commit. Senza questo gate esplicito, il bot può self-elevate il proprio scope di review eliminando i path protetti. Alert Telegram fires anche se actor è human (audit cognitivo + paper trail).

**Failure mode chiuso**: bot self-elevates by removing CODEOWNERS protection on hot-zone paths.

#### Componente 3 — CODEOWNERS scope-ristretto (P1.1)

**File**: `.github/CODEOWNERS` (ripristino con scope ridotto vs versione cancellata in `b8df2f996`).

**Contenuto** (esempio):

```
# CODEOWNERS — hot-zone only (post-L5.2 ripristino)
# Scope ristretto: review obbligatoria SOLO su questi path per PR di bot/Dependabot.

# Migrations
apps/backend-rag/backend/db/migrations_v2/*.sql  @Balizero1987
scripts/lint_migration_numbers.py                 @Balizero1987

# Auth / billing / pricing
apps/backend-rag/backend/services/auth/   @Balizero1987
apps/backend-rag/backend/services/billing/ @Balizero1987
apps/backend-rag/backend/services/pricing/ @Balizero1987

# LaunchAgent wrappers
infra/launchagents/*.sh                   @Balizero1987
scripts/lint_launchagents.sh              @Balizero1987

# CI/CD workflows + protection
.github/workflows/*.yml                   @Balizero1987
.github/CODEOWNERS                        @Balizero1987

# Sentinel + DLQ
scripts/nuzantara-sentinel.py             @Balizero1987
scripts/dlq_autopilot.py                  @Balizero1987
```

**Branch protection update**:

- `required_approving_review_count: 1`
- `require_code_owner_reviews: true`
- `dismiss_stale_reviews: true`
- `require_last_push_approval: true`
- **Bypass actors**: solo Antonello (human) per break-glass + per scope NON-hot-zone (path filtering via CODEOWNERS).

**Effetto**: PR umane su file cold = no review needed (single-dev throughput intatto). PR bot su hot-zone = review obbligatoria. PR umane su hot-zone = review obbligatoria solo se CODEOWNERS match (Antonello rivede se stesso = no-op, ma forza una pausa cognitiva).

#### Componente 4 — Audit append-only + Telegram (P1.2)

**Mechanism A — Server-side audit GitHub issues**:

- GitHub Action `on: push` su main rileva commit signature anomala (no `Hook-Verified` trailer OR trailer forgeable → re-run server-side per validare).
- Append issue body con event log structured: `{timestamp, actor, sha, files_changed, bypass_signal, source}`.
- Issue dedicata `[AUDIT] Workflow discipline bypass log` evergreen, write-only.

**Mechanism B — Telegram real-time SOLO hot-zone**:

- Trigger: hot-zone files modified + bypass signal detected (server-side replay fail) + direct push main + workflow self-modification.
- Payload: 1-line alert via `sentinel_lib/alerter.py` retry pattern (W55 cicatrix).
- **NO Telegram per**: HUSKY=0/--no-verify su feature branch (alert fatigue, Gemini Q4).

**Mechanism C — Digest settimanale (P2.2)**:

- Cron lunedì 08:00 WITA: summary 7-day bypass events.
- Format: bypass count by file zone + actor + intent (commit msg analysis).
- Pipe via Telegram digest channel.

**Canary obbligatorio (Codex missing failure mode #14 — alert blind spot)**:

- Daily canary: Telegram bot health-check + GitHub issue write permission verify.
- Se fail 2 ticks consecutivi → fallback alert via email Antonello.

## Failure modes addressed (vs L5.1 + brief originale)

| Failure mode                 | Brief originale                | L5.2                                                      |
| ---------------------------- | ------------------------------ | --------------------------------------------------------- |
| HUSKY=0 client bypass        | Detect lato husky (IMPOSSIBLE) | Server-side replay required check ✅                      |
| --no-verify bypass           | Non mappato                    | Server-side replay ✅                                     |
| Direct push main             | Non mappato                    | Branch protection + Telegram alert ✅                     |
| Bot+admin same actor merge   | Non mappato                    | P0.1 privilege downgrade ✅                               |
| Workflow self-modification   | Non mappato                    | C1 workflow gira anche su PR che lo modifica ✅           |
| CI status spoofing           | Non mappato                    | C2 expected source app ✅                                 |
| Path filter skip-success     | Non mappato                    | C3 no paths filter ✅                                     |
| Approval stale race          | Non mappato                    | C4 dismiss-stale + require-latest-push ✅                 |
| Lease stale/race merge time  | Non mappato                    | C5 ricalcolo su latest base SHA ✅                        |
| Same trust domain LLM review | Opzione A originale            | P0.1 bot non admin + non bot-approves-bot ✅              |
| Redis fail-open              | Non mappato                    | Fail-CLOSED su hot-zone ✅                                |
| `git commit --amend` bypass  | Non mappato                    | Server-side replay (vede diff finale, non amend) ✅       |
| Rename file critical         | Non mappato                    | Hot-zone classifier su path + diff content ✅             |
| HUSKY_SKIP_INSTALL / CI=true | Non mappato                    | Server-side replay ✅                                     |
| Attestation forgeability     | Hook-Verified trailer          | Re-run server-side, no trust attestation ✅               |
| Alert blind spot             | Non mappato                    | Canary daily + email fallback ✅                          |
| Break-glass abuse            | "Admin can bypass" implicit    | TTL + label/reason + actor allowlist + immediate alert ✅ |

## Open questions per panel iter-2

**Q1**: P0.1 downgrade Claude bot — esiste già un account bot separato OR Claude opera con token Antonello? (Empirical audit needed).
**Q2**: Required check `expected source app: github-actions` — GitHub Actions vs GitHub App custom — quale boundary contro CI spoofing è realmente strong?
**Q3**: Fail-CLOSED su Redis — quanto tempo è tollerabile downtime Redis prima che PR si accumulino? Backup plan se Redis down 4h?
**Q4**: CODEOWNERS auto-referenziale Antonello → Antonello — è cargo cult o "pausa cognitiva" reale?
**Q5**: Merge queue (GitHub native) vs custom required check — quale gestisce meglio lease race al merge time?
**Q6**: Trappole specifiche / failure mode che ho dimenticato di mappare oltre i 15 sopra?

## Rollout plan (revised post panel iter-2 — L2.A1 priority elevation)

**IMPORTANT (L2.A1 panel amendment)**: L5.2 Phase 1+2 deve raggiungere `enforce-mode` PRIMA o contemporaneamente al rollout di L5.1 enforce. L5.2 funziona da safety net assoluta durante la validazione empirica di L5.1 (sub-agent inheritance pendente). L5.1 iter-2 status `pending-iter-3` non blocca questo rollout.

1. **Phase 0** (pre-rollout, 2-3h): empirical audit `gh api` per stato attuale admin/permissions/protection + Redis credentials in GitHub secrets + verify Telegram bot/owner chat IDs accessible da Actions.
2. **Phase 1** (week 1, 4-6h): ship Componente 1 (bot downgrade). Verify zero merge da bot via `gh api` audit periodico. **Gate**: 0 merge bot-originate in 48h prima procedere Phase 2.
3. **Phase 2** (week 2-3, 12-16h): ship Componente 2 (required hot-zone Action) + Componente 2.5 (CODEOWNERS self-mod protection) in `monitor-mode` (`continue-on-error: true`, output solo logs). Raccogliere 1 settimana data empirica su false positive / true positive. **Gate**: <5% false-positive rate, 0 missed hot-zone bypass.
4. **Phase 3** (week 4, 8-12h): flip Componente 2 + 2.5 a `enforce-mode`. Ship Componente 3 (CODEOWNERS ristretto) + branch protection update (`required_approving_review_count=1` con `require_code_owner_reviews` + `dismiss_stale_reviews` + `require_last_push_approval`). **Gate**: empirical observation 48h post-flip senza regressione.
5. **Phase 4** (week 5-6, 6-8h): ship Componente 4 (audit append-only GitHub issue + Telegram hot-zone). Canary live + digest cron settimanale.
6. **Phase 5** (week 7+, observation): 30 giorni monitoring. Rollback automatico se >5% PR umane bloccate per false positive. Trigger spec L5.1 iter-3 design ONLY dopo L5.2 Phase 3 enforce-mode stable (panel-dictated priority order).

**Kill switch globale**: `L5_2_SERVER_ENFORCEMENT_ENABLED=false` env nel workflow → workflow ritorna `neutral` invece di failure. Permette emergency disarm senza modificare branch protection.

**Interaction effect L5.1**: L5.1 iter-2 può girare in monitor-mode (Phase 1 probe, no block) in PARALLELO con L5.2 Phase 1-3. Probe collects data per iter-3 design (sub-agent inheritance, tool coverage). L5.1 enforce-mode DOPO L5.2 Phase 3 stable.

## Backward compat

- Escape `L5_2_SERVER_ENFORCEMENT_ENABLED=false`.
- Break-glass label `break-glass:emergency` su PR + commit message intent marker (`emergency:`, `hotfix:`, `cicatrix-fix:`).
- Break-glass richiede: label + issue link + TTL 24h + actor allowlist (solo Antonello human) + immediate Telegram alert + audit issue append.

## Estimated effort

- Phase 0: 2-3h empirical audit
- Phase 1: 4-6h (audit + downgrade + verification)
- Phase 2-3: 12-16h (GitHub Action + tests + monitor + enforce flip)
- Phase 4: 6-8h (audit issue + Telegram pipeline + canary)
- Phase 5: ongoing observation
- **Total**: ~30-40h spread over 6-8 settimane

## Sister specs

- **L5.1**: client-side enforcement (worktree isolation hook). Status: `draft`, 4 panel defects pending fix.
- **L5.2** (this): server-side enforcement (CI required check + bot downgrade + audit).
- **L5.3** (future): merge queue + monorepo branch protection at scale (if team grows beyond single-dev).
