# Main Dirty Checkout + Runtime LaunchAgent Triage - 2026-06-14

Date: 2026-06-14 23:07-23:25 WITA
Dispatch key: `nuzantara-main-dirty-tree`
Source report: `/Users/nuzantara/codex-spark-loop/reports/scout-20260614_230555.md`
Runner branch: `codex-overnight/spark-alarm-20260614_230747-spark-dispatch-20260614_230555-scout-nuzantara-main-dirty-tree-20260614_230751`
Machine: Pro, `nuzantara@Nuzantara`
Peer sync: Mini unreachable, so peer git sync was not verified.

## Decision

Do not mutate `/Users/nuzantara/Desktop/nuzantara` or recreate
`/Users/nuzantara/Desktop/nuzantara-deploy` from this unattended run.

The actionable signal is real, but the safe remediation is an operator-gated
handoff:

1. preserve and split the broad dirty main checkout;
2. restore the missing runtime checkout only when a human/runtime owner accepts
   that LaunchAgents may immediately become active again;
3. then re-run bounded launchd checks.

## Live Evidence

Spark itself is not the failing surface:

- `com.nuzantara.codex-spark-loop`: `state = running`, `pid = 1212`,
  `last exit code = (never exited)`.
- `com.nuzantara.codex-spark-alarm`: timer job, `runs = 32`,
  `last exit code = 0`, `run interval = 120 seconds`.
- `com.nuzantara.codex-spark-harvester`: timer job, `runs = 21`,
  `last exit code = 0`, `run interval = 180 seconds`.
- Codex state files under `/Users/nuzantara/.agent/decisions/state` were fresh
  for Spark/overnight at 23:06-23:08 WITA.

The main checkout is dirty and stale:

```text
/Users/nuzantara/Desktop/nuzantara
HEAD: e2b355f45
branch: main...origin/main [ahead 2, behind 175]
```

Observed dirty groups:

| Group | Examples | Safe handling |
| --- | --- | --- |
| Staged-looking executable changes | `scripts/dlq_autopilot.py`, `scripts/nuzantara-sentinel.py` | Treat as intentional code until owner proves otherwise. Review and commit separately from generated artifacts. |
| Modified NLM/evaluator pipeline code | `apps/evaluator/nlm_deep_research/*.py`, `apps/mata-garuda/mata_garuda/workers/nlm_feeder.py` | Validate with the NLM owner; do not fold into docs/content cleanup. |
| Generated inventories/reports | `docs/DOCS_INVENTORY.md`, `docs/AUTOMATIONS_REFERENCE.md`, `research/nb-health/*.md`, `research/regulatory/*-delta.json` | Keep only if generated by a known current job; otherwise regenerate from a clean checkout. |
| Articles/content | `apps/mouth/src/content/articles/**` | Review editorial provenance; commit as content batch if intended. |
| Bulk outputs/corpus | `outputs/`, `research/coherence-corpus/`, `apps/evaluator/nlm_deep_research/output/multimodal/*.m4a` | Do not commit blindly. Move to artifact storage or add a manifest only after size/privacy review. |
| Shared JSONL/data | `shared/escalations_pro.jsonl`, `apps/bali-intel-scraper/data/published_articles.json` | Inspect for raw/private content before staging. |

Recurring non-Codex LaunchAgent failures are also real:

| Label | Last exit | Current finding |
| --- | ---: | --- |
| `com.nuzantara.cost-breaker` | 127 | plist calls `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/cost_breaker_run.sh`; log says file not found. |
| `com.nuzantara.cost-breaker-deadman` | 127 | plist calls `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/cost_breaker_deadman.sh`; runtime root is missing. |
| `com.nuzantara.intake-review-reader` | 127 | plist calls `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/intake_review_reader_run.sh`; log repeats file not found. |
| `com.nuzantara.mcp-integrity` | 127 | plist calls `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/verify_mcp_integrity.sh`; log repeats file not found after older successful/yellow/red checks. |
| `com.balizero.wr2.supervisor` | 66 | wrapper reports missing `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/wr2_supervisor.py`. |
| `com.balizero.wr2.supervisor-watchdog` | 66 | wrapper reports missing `/Users/nuzantara/Desktop/nuzantara-deploy/scripts/wr2_supervisor_watchdog.py`. |
| `com.nuzantara.intake-worker` | 78 | plist working directory is under missing `nuzantara-deploy`; log also shows OCR/runtime config degradation. |
| `com.balizero.wa-meta-inbox` | 78 | plist working directory is under missing `nuzantara-deploy`; log shows upstream fetch failures. |

The runtime root itself was absent during verification:

```text
git -C /Users/nuzantara/Desktop/nuzantara-deploy status
fatal: cannot change to '/Users/nuzantara/Desktop/nuzantara-deploy': No such file or directory
```

The referenced scripts do exist in the current repo and in `origin/main`, so this
is not a missing-source problem:

```text
origin/main:scripts/cost_breaker_run.sh
origin/main:scripts/cost_breaker_deadman.sh
origin/main:scripts/intake_review_reader_run.sh
origin/main:scripts/verify_mcp_integrity.sh
origin/main:scripts/wr2_supervisor.py
origin/main:scripts/wr2_supervisor_watchdog.py
```

## Root Cause

Primary cluster: runtime checkout path drift/removal.

Several LaunchAgents are still armed and scheduled, but their ProgramArguments
or working directories point into `/Users/nuzantara/Desktop/nuzantara-deploy`.
That directory does not currently exist on Pro, so launchd repeatedly exits on
missing scripts or configuration before the service logic can run.

The dirty main checkout is a separate risk multiplier: it is behind `origin/main`
by 175 commits and contains staged/untracked work. It must not be used as the
source to recreate runtime state until the dirty work is split or preserved.

## Operator-Gated Recovery Plan

### Phase 0 - Preserve main checkout state

Run from Pro, read-only first:

```bash
cd /Users/nuzantara/Desktop/nuzantara
git status --short --branch
git diff --stat
git diff --cached --stat
git ls-files --others --exclude-standard | sed -n '1,200p'
```

If the dirty work is owned and should survive, commit it on a dedicated branch or
create a normal non-destructive stash/patch bundle before any pull/rebase. Do not
run `git reset --hard` or clean untracked files from an agent session.

### Phase 1 - Restore runtime checkout only after approval

This step can reactivate cost, MCP, intake, WR2, and WA services. It should be
run by the runtime owner, not an unattended triage agent.

Expected invariant after restoration:

```bash
test -d /Users/nuzantara/Desktop/nuzantara-deploy
test -x /Users/nuzantara/Desktop/nuzantara-deploy/scripts/cost_breaker_run.sh
test -x /Users/nuzantara/Desktop/nuzantara-deploy/scripts/intake_review_reader_run.sh
test -x /Users/nuzantara/Desktop/nuzantara-deploy/scripts/verify_mcp_integrity.sh
test -f /Users/nuzantara/Desktop/nuzantara-deploy/scripts/wr2_supervisor.py
test -f /Users/nuzantara/Desktop/nuzantara-deploy/scripts/wr2_supervisor_watchdog.py
```

Use a clean `origin/main`-aligned runtime checkout. Do not derive it from the
dirty main checkout.

### Phase 2 - Bounded verification

After runtime restoration:

```bash
launchctl print gui/$(id -u)/com.nuzantara.cost-breaker | rg 'state =|runs =|last exit'
launchctl print gui/$(id -u)/com.nuzantara.intake-review-reader | rg 'state =|runs =|last exit'
launchctl print gui/$(id -u)/com.nuzantara.mcp-integrity | rg 'state =|runs =|last exit'
launchctl print gui/$(id -u)/com.balizero.wr2.supervisor | rg 'state =|runs =|last exit'
launchctl print gui/$(id -u)/com.balizero.wr2.supervisor-watchdog | rg 'state =|runs =|last exit'
```

Then sample logs with redaction-aware tooling. Do not paste full inherited
LaunchAgent environments into reports.

## Non-Goals

- Do not classify Spark as broken from idle timer jobs with `last exit code = 0`.
- Do not mutate `/Users/nuzantara/Desktop/nuzantara` from an isolated
  overnight worktree.
- Do not create or sync `/Users/nuzantara/Desktop/nuzantara-deploy`
  unattended, because doing so can restart real services.
- Do not deploy.
- Do not edit `backend/prompts/zantara_core.py`, `fly.toml`, `.env*`, or secrets.

## Next Step

Assign one runtime-owner pass to:

1. preserve/split the dirty main checkout;
2. recreate or resync `/Users/nuzantara/Desktop/nuzantara-deploy` from clean
   `origin/main`;
3. kickstart only the labels whose scripts and env files have been verified;
4. record post-kickstart `runs` and `last exit` evidence in a follow-up report.
