---
spec_id: T2.7
title: CLAUDE.md project 29.6KB → 8KB router refactor (MOVED TO WAVE 4)
tier: 4
priority: P1
effort_estimate: 120-180 min (revised: was 60-90min, DS panel realistic)
status: DRAFT — MOVED to Wave 4 (after T3.x) per DS panel medium concern
basis: 2026-05-19-bestconfig P2 #10 + 2026-05-21-karpathy "Cosa Nuzantara fa MALE" + Codex synthesis + DS panel 2026-05-21
empirical_today:
  - /Users/nuzantara/Desktop/nuzantara/CLAUDE.md = 29.6 KB
  - apps/<x>/CLAUDE.md existing for some apps (verify)
  - Target SOTA: 60-120 lines (Codex), max 200 (Boris Cherny threshold)
revision_history:
  - 2026-05-21 20:33 created with 60-90min estimate, Tier 2
  - 2026-05-21 22:15 DS panel:
    - Effort revised 120-180min (realistic manual migration + cross-ref + multi-app testing)
    - Moved to Wave 4 — refactor must include T3.3 lane aggregators, T3.4 slash commands, T3.5 SessionStart, T0.2 nuzantara-mcp fix outcome
---

# T2.7 — CLAUDE.md project refactor

## Problem

CLAUDE.md project corrente = **29.6KB** = ~750 righe. SOTA May 2026 target = 60-120 righe (~8KB). Claude Code carica auto a SessionStart, consumando ~15% del context window upfront. Karpathy compliance ~80% (declining oltre 200 righe per Boris Cherny threshold).

Sezioni in CLAUDE.md attuali (estimate):

- §0 Machine ID (16 righe)
- §1 Project Overview (~60 righe — apps list, tech stack)
- §2 Claude Code Behavior Rules (~30 righe)
- §3 MOS Memory (~30 righe)
- §4 Golden Rules (~15 righe)
- §5 Critical Paths (~50 righe — backend structure)
- §6 Domain-Specific Knowledge (~40 righe)
- §7 MCP Servers (~30 righe)
- §8 Deployment Architecture (~50 righe)
- §9 Language Protocol (~10 righe)
- §10 Resources & Routes (~80 righe — team table, channels)
- §11 Pre-Deploy Checklist (~30 righe)
- §12 AI Dispatch System (~30 righe)
- §13 Anthropic API Quick Ref (~25 righe)
- §14 CRITICAL OPERATIONAL RULES (~150 righe — venv, drive, OCR, cache, LLM structured output, observability, KG, Cron, etc.)
- §15 Escalation Tasks (~15 righe)
- §16 Research Capture (~50 righe)

Total ~750+ righe.

## Context

Sezione che **DEVE** restare load-bearing in root (no delega):

- Machine ID (sempre check first)
- Autonomous Ops L2 reference
- Anti-hallucination + 4-LLM panel
- Email rule (hardcoded zantara@balizero.com)
- Golden Rules (11 punti)
- Language Protocol (Italian colloquial)
- Owner identity (Zero codename)

Sezioni che **POSSONO** delegare ad apps/<x>/CLAUDE.md o docs/:

- §5 Backend structure → `apps/backend-rag/CLAUDE.md`
- §6 Domain knowledge KBLI/Pricing → `apps/backend-rag/CLAUDE.md` o `docs/PRICING_REFERENCE.md` (existing)
- §11 Pre-Deploy Checklist → `apps/backend-rag/docs/DEPLOY.md`
- §12 AI Dispatch → `docs/AI_DISPATCH_REFERENCE.md` (already exists per attribute)
- §13 Anthropic API → `docs/ANTHROPIC_API_REFERENCE.md` (already exists per attribute)
- §14 CRITICAL OPERATIONAL RULES sub-sections → distribute among apps/

## Design

Refactor strategy: **Router pattern**. Root CLAUDE.md = ~120 righe minimal con:

1. Identità + machine + autonomous ops
2. Behavior + language + hard rules
3. Pointers ai docs/apps subtree (`@import` o esplicito "See ...")
4. Cross-link a memory file load-bearing
5. Cross-link cicatrix-scars

Apps/<x>/CLAUDE.md auto-loaded SOLO quando cwd in subtree (Anthropic Skills lazy load).

## Acceptance criteria

- [ ] `~/Desktop/nuzantara/CLAUDE.md` ≤ 9KB (~220 righe max)
- [ ] All content existing preserved (delegated, not deleted)
- [ ] `apps/backend-rag/CLAUDE.md` (existing) potenziato con §5 + §6 content
- [ ] `docs/AI_DISPATCH_REFERENCE.md` verified contains §12 content
- [ ] `docs/PRICING_REFERENCE.md`, `docs/VISA_TYPES_REFERENCE.md` (existing) cross-linked
- [ ] Empirical test: next session SessionStart context −15% size measured
- [ ] All cross-references resolved (no broken pointers)

## Implementation steps

### Step 1 — Backup current state

```bash
cp ~/Desktop/nuzantara/CLAUDE.md ~/backups/CLAUDE-md-pre-refactor-20260521.md
wc -c ~/backups/CLAUDE-md-pre-refactor-20260521.md
# expected: ~29600 bytes
```

### Step 2 — Audit existing app-level CLAUDE.md

```bash
find ~/Desktop/nuzantara/apps -name "CLAUDE.md" -maxdepth 2 -exec wc -c {} \;
find ~/Desktop/nuzantara/docs -name "*REFERENCE*.md" -exec wc -c {} \;
```

Expected output: list of existing app CLAUDE.md + reference docs.

### Step 3 — Compose new ROOT CLAUDE.md

Skeleton target ~120 righe:

```markdown
# CLAUDE.md — Nuzantara Project Context

> Read `SYMBIOSIS.md` first. Read `VADEMECUM.md` before building.
> Atlas: [`INDEX.md`](INDEX.md).

## 0. Machine Identification

[16 lines — keep verbatim, table + check command]

## 1. Project Overview (1-line summary)

**Nuzantara** v5.2.0 — production AI business intelligence platform for Bali Zero
(immigration/company/tax/property Indonesia, 5000+ clients).
URL: https://kita.balizero.com

For backend architecture details: see `apps/backend-rag/CLAUDE.md`.
For app inventory: see [`INDEX.md`](INDEX.md).

## 2. Claude Code Behavior Rules

[KEEP: Autonomous Ops L2 reference, browser automation rules, Federation Orchestrator triggers, Preflight SDD, Escalations]

[~30 lines — keep verbatim, load-bearing]

## 3. MOS Memory + Auto-memory

[~15 lines — mem CLI quick ref + save discipline]

## 4. Golden Rules (ENFORCE STRICTLY)

[~15 lines — 11 numbered rules, 1 line each, NO inline detail]

## 5. Critical Paths Summary

Apps/backend-rag/ structure: see `apps/backend-rag/CLAUDE.md`.
Prompt SSOT: `backend/prompts/zantara_core.py`.
Migrations: `apps/backend-rag/backend/db/migrations_v2/`.

## 6. Domain Knowledge — Quick Ref

KBLI/Pricing/Visa: see `docs/PRICING_REFERENCE.md`, `docs/VISA_TYPES_REFERENCE.md`, `apps/backend-rag/CLAUDE.md`.

## 7. MCP & Skills Quick Map

8 MCP servers active (see `.mcp.json`).
Custom skills: `~/.claude/skills/` (karpathy-discipline, bali-zero-brand, wr2-_, wr3-_).
Slash commands: see `~/.claude/commands/`.

## 8. Deployment Architecture (1-line)

Fly.io 2 apps (nuzantara-rag, nuzantara-postgres) + Vercel frontend. Backup → Tigris daily.
Full details: `docs/DEPLOY.md`.

## 9. Language Protocol

[~10 lines — Italian colloquial, codename Zero, team list pointer]

## 10. Team Reference

[1-line: see `MEMORY.md` "Subhi/team mapping" + memory `reference_*` files]

## 11. Pre-Deploy

`./scripts/pre-deploy-check.sh` (see `docs/PRE_DEPLOY_CHECKLIST.md`).

## 12-13. AI Dispatch + Anthropic API

See `docs/AI_DISPATCH_REFERENCE.md` + `docs/ANTHROPIC_API_REFERENCE.md`.

## 14. Critical Operational Rules

See `apps/backend-rag/CLAUDE.md` §14 (Virtualenv, Drive polling, OAuth, OCR, cache, KG, cron).

## 15. Escalations

Check `~/.agent/decisions/claude_tasks/` at SessionStart. Handle by priority.

## 16. Research Capture Convention

≥400 parole + ≥3 sources + checklist + client-case → `~/Desktop/nuzantara/research/<domain>/YYYY-MM-DD-slug.md`.
Index: `MEMORY_RESEARCH_CAPTURES.md`.

---

**Last Updated:** 2026-05-21 (refactored T2.7)
**Maintained by:** Bali Zero AI Team
```

### Step 4 — Move content to apps/backend-rag/CLAUDE.md

Identifica all sections §5, §6, §14 detail → append a `apps/backend-rag/CLAUDE.md` (existing).

```bash
# Verify existing app-level CLAUDE.md has placeholder for new content
cat apps/backend-rag/CLAUDE.md | head -10
```

Strategy: append sections like:

```markdown
## §5 Backend Structure (moved from project root 2026-05-21 T2.7)

[paste verbatim from root]

## §6 Domain Knowledge

[paste verbatim from root]

## §14 Operational Rules (moved from project root 2026-05-21 T2.7)

[paste verbatim from root]
```

### Step 5 — Verify docs/AI_DISPATCH_REFERENCE.md current

```bash
test -f ~/Desktop/nuzantara/docs/AI_DISPATCH_REFERENCE.md && \
  wc -l ~/Desktop/nuzantara/docs/AI_DISPATCH_REFERENCE.md
# expected: existing, with §12 content covered
```

Se gap, augment.

### Step 6 — Test SessionStart load empirical

```bash
# Quit + restart Claude Code session
# Look at system reminder at SessionStart — should show new CLAUDE.md content
# Estimate context consumed: target ~15% reduction in initial context
```

### Step 7 — Git commit atomic

```bash
cd ~/Desktop/nuzantara
git add CLAUDE.md apps/backend-rag/CLAUDE.md docs/*.md
git diff --cached --stat
git commit -m "refactor(claude-md): project root 29.6KB→8KB router; delegate detail to apps/+docs/

T2.7 of specs plan 2026-05-21. Reduces SessionStart context ~15%.
Empirical SOTA target Karpathy 60-120 lines, compliance ~80% past 200.

Moved sections:
- §5 Backend Structure → apps/backend-rag/CLAUDE.md
- §6 Domain Knowledge → apps/backend-rag/CLAUDE.md + docs/PRICING + docs/VISA
- §14 Critical Operational Rules → apps/backend-rag/CLAUDE.md

Preserved verbatim in root (load-bearing):
- Machine ID, Autonomous Ops, Golden Rules, Language Protocol

Spec: research/operations/specs/T2.7-claude-md-project-refactor.md

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

## Verification

### Test 1 — Size target

```bash
wc -c ~/Desktop/nuzantara/CLAUDE.md
# Expected: ≤9000 bytes
```

### Test 2 — All cross-references resolve

```bash
grep -oE '`[^`]+\.md`' ~/Desktop/nuzantara/CLAUDE.md | tr -d '\`' | while read f; do
  [ -f "$f" ] || [ -f "$(dirname ~/Desktop/nuzantara/CLAUDE.md)/$f" ] || echo "BROKEN: $f"
done
# Expected: no "BROKEN" lines
```

### Test 3 — Critical content NOT lost

```bash
for keyword in "zantara@balizero.com" "Autonomous Ops" "Anti-hallucination" "4-LLM panel" "Italian" "Zero"; do
  count=$(grep -c "$keyword" ~/Desktop/nuzantara/CLAUDE.md ~/Desktop/nuzantara/apps/backend-rag/CLAUDE.md 2>/dev/null | awk -F: '{s+=$2}END{print s}')
  echo "$keyword: $count occurrences"
done
# Expected: ≥1 for each
```

### Test 4 — SessionStart context reduction empirical

Next session, screenshot first reminder block. Measure character count delta vs previous.

## Rollback

```bash
cp ~/backups/CLAUDE-md-pre-refactor-20260521.md ~/Desktop/nuzantara/CLAUDE.md
git -C ~/Desktop/nuzantara restore apps/backend-rag/CLAUDE.md
# (verify no other files touched outside CLAUDE.md hierarchy)
```

## Open questions

1. **`@import` Anthropic feature**: CLAUDE.md supporta `@external-file.md` import syntax? Empirical TODAY `hasClaudeMdExternalIncludesApproved: false`. Use explicit "See `<path>`" pointer (safer, no opt-in needed).
2. **Apps without `CLAUDE.md`**: per ogni `apps/<x>/` senza CLAUDE.md, sezione root non può delegare. Step intermedio: prima crea stub `apps/<x>/CLAUDE.md` (`# <App Name>\n\nSee project root CLAUDE.md.`) ? Default = solo per backend-rag dove serve, NON proliferare stub.
3. **`docs/` directory existing**: verifica empirical che `docs/AI_DISPATCH_REFERENCE.md`, `docs/ANTHROPIC_API_REFERENCE.md`, `docs/PRE_DEPLOY_CHECKLIST.md` (?) esistano. Se NO, prima crearli, poi delegate.
4. **Team list in §10**: completo o esterno?: 12 team member, 30 righe. Delegate a memory `reference_team_balizero_2026.md` (1 sola row in CLAUDE.md "see ...").
5. **Cron schedule sezione**: §14 "Cron Air" obsoleta (Air decommissioned 2026-05-05). Delete o move to ARCHIVE? Default = delete in refactor.

## Estimated breakdown (revised DS panel)

| Step                                                                                          | Tempo                                    |
| --------------------------------------------------------------------------------------------- | ---------------------------------------- |
| Backup + audit (24 apps + docs/)                                                              | 15 min                                   |
| Compose new root with refs to T3.3 lane aggregators / T3.4 slash commands / T3.5 SessionStart | 40 min                                   |
| Move sections to apps + docs (multi-app testing)                                              | 60 min                                   |
| Verify cross-refs (24+ app directories)                                                       | 20 min                                   |
| Git commit                                                                                    | 5 min                                    |
| Empirical SessionStart test (context reduction measurement)                                   | 10 min                                   |
| Iteration if context too big or info lost                                                     | 20 min                                   |
| **Total**                                                                                     | **120-180 min** (revised from 60-90 min) |
