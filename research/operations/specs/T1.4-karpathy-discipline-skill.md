---
spec_id: T1.4
title: karpathy-discipline skill — 4 principi verbatim
tier: 1
priority: P1
effort_estimate: 10 min
status: DRAFT
basis: 2026-05-21-claude-code-best-practices-karpathy-nuzantara.md + forrestchang/andrej-karpathy-skills (109K⭐)
---

# T1.4 — Skill `karpathy-discipline`

## Problem

Karpathy 4 principi (Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution) sono SOTA May 2026 anti-pattern resolver per LLM coding (109K stars su GitHub, 65 righe, ~80% compliance reported). Nuzantara CLAUDE.md root NON li menziona. Antonello li vuole embedded.

## Context

Karpathy file canonical: `forrestchang/andrej-karpathy-skills/blob/main/CLAUDE.md`, fonte miraflow.ai + agentpedia + techtimes (220K combined stars).

Decisione orchestratore: skill _invocabile_ tramite `/karpathy-discipline` o auto-trigger su task implementazione, NON inline in CLAUDE.md root (Karpathy stesso dice "60 righe target, no bloat").

Pattern Anthropic Skills 3-layer:

- Layer 1: metadata (name + description) sempre in system prompt
- Layer 2: SKILL.md body — caricato quando triggered
- Layer 3: references — on-demand

## Design

Skill directory + SKILL.md con 4 principi VERBATIM dal source canonical. Description specifica: triggera su task "feature impl / refactor / bug fix". Allowed-tools restrictive.

## Acceptance criteria

- [ ] Directory `~/.claude/skills/karpathy-discipline/` creata
- [ ] File `~/.claude/skills/karpathy-discipline/SKILL.md` con 4 principi verbatim
- [ ] Frontmatter YAML valido (name, description)
- [ ] Skill enumerata in next SessionStart available-skills list
- [ ] Test invoke `/karpathy-discipline` (o auto-trigger) carica body

## Implementation steps

### Step 1 — Crea skill directory

```bash
mkdir -p ~/.claude/skills/karpathy-discipline
```

### Step 2 — Scrivi SKILL.md verbatim

```bash
cat > ~/.claude/skills/karpathy-discipline/SKILL.md << 'EOF'
---
name: karpathy-discipline
description: Use BEFORE any feature implementation, refactor, bug fix, or non-trivial code change. Applies 4 Karpathy principles to reduce common LLM coding mistakes (silent assumptions, hypertrophy, collateral changes, vague success criteria).
allowed-tools: Read, Edit, Write, Bash(git diff:*), Bash(git status:*), Bash(grep:*), Bash(rg:*)
---

# Karpathy Discipline

Source: `forrestchang/andrej-karpathy-skills` (109K+ ⭐). Verbatim from canonical CLAUDE.md.

**Tradeoff dichiarato**: *"These guidelines bias toward caution over speed. For trivial tasks, use judgment."*

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it — don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

**The test**: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```

1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]

```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**These guidelines are working if**: fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.

---

## Nuzantara-specific applications

(Per CLAUDE.md project context)

- **Think Before Coding** → si combina con regola 2026-05-13 "4-LLM panel review BEFORE user approval" per spec ad alto stakes
- **Simplicity First** → applicato in fix MEMORY.md 60.6KB→13.8KB (2026-05-19)
- **Surgical Changes** → riflette cicatrix-scars rule "atomic commits, no `--no-verify`, no `--amend` on pushed"
- **Goal-Driven Execution** → richiede TaskCreate/TaskUpdate + acceptance criteria in spec
EOF
```

### Step 3 — Verify skill enumerated

```bash
ls -la ~/.claude/skills/karpathy-discipline/
# Expected: SKILL.md exists

cat ~/.claude/skills/karpathy-discipline/SKILL.md | head -5
# Expected: frontmatter YAML valid

# In next session, list skills:
# system reminder should include "- karpathy-discipline: Use BEFORE any feature implementation..."
```

### Step 4 — Optional: link in CLAUDE.md root (without inlining)

Edit `~/Desktop/nuzantara/CLAUDE.md` (project) section **§2 Claude Code Behavior Rules**, aggiungere:

```markdown
- Before any feature impl / refactor / bug fix → invoke skill `karpathy-discipline` (4 principi)
```

Una riga. NO inline dei 4 principi (Karpathy stesso dice "60 line target").

## Verification

### Test 1 — file structure

```bash
test -f ~/.claude/skills/karpathy-discipline/SKILL.md && echo OK
head -3 ~/.claude/skills/karpathy-discipline/SKILL.md
# Expected: frontmatter present
```

### Test 2 — frontmatter parse

```bash
python3 -c "
import re, pathlib
p = pathlib.Path.home() / '.claude/skills/karpathy-discipline/SKILL.md'
t = p.read_text()
m = re.match(r'^---\n(.*?)\n---', t, re.DOTALL)
assert m, 'no frontmatter'
print(m.group(1))
"
# Expected: prints YAML frontmatter
```

### Test 3 — empirical next session

Antonello apre nuova sessione, dà task "refactor `apps/wa-mirror/bridge/session.ts` per renderlo idempotente". Claude dovrebbe:

1. Riconoscere task = refactor non triviale
2. Auto-invoke `karpathy-discipline` (o annunciare "applying Karpathy discipline")
3. Iniziare con principio #1 (state assumptions explicitly) — chiedere se ci sono test esistenti, branch policy, ecc.

## Rollback

```bash
rm -rf ~/.claude/skills/karpathy-discipline/
# revert CLAUDE.md root edit
```

## Open questions

1. **Auto-trigger condition**: skill descrizione menziona "Use BEFORE any feature implementation, refactor, bug fix". Verifica se Claude effettivamente auto-invoca o richiede `/karpathy-discipline` esplicito.
2. **Italian translation**?: dossier Karpathy ha `karpathy-discipline_it.md` come variante? Default = English only (Anthropic skill format docs).
3. **Conflict con `superpowers:test-driven-development`**?: Karpathy #4 (Goal-Driven) dice "Write a test that reproduces it" — TDD pattern. Convergenti, no conflict.

## Estimated breakdown

| Step                         | Tempo      |
| ---------------------------- | ---------- |
| mkdir + SKILL.md write       | 4 min      |
| Verify Test 1-2              | 2 min      |
| CLAUDE.md root edit (1 line) | 2 min      |
| **Total**                    | **10 min** |
