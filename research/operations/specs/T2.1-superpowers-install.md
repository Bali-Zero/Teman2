---
spec_id: T2.1
title: Install Superpowers (obra/superpowers) — Jesse Vincent 7-phase workflow
tier: 2
priority: P1
effort_estimate: 15 min
status: DRAFT
basis: 2026-05-21-arming-arsenal Part 3 Skills + ClaudeFa.st 9 best skills ranking (Superpowers 94K⭐)
---

# T2.1 — Install obra/superpowers plugin

## Problem

Top-ranked Claude Code skill May 2026 (94K⭐) implementa Jesse Vincent 7-phase workflow:

> _"Brainstorm, Spec, Plan, TDD, Subagent Development, Review, Finalize"_ — with automatic code deletion for tests-first enforcement.

Nuzantara già usa `superpowers:*` skill names nelle system reminders (vedi available-skills list a SessionStart: brainstorming, executing-plans, dispatching-parallel-agents, ecc.) → ma verifica: sono installate localmente o sono solo referenced names da test? Spec verifica + completa install se gap.

## Context

Empirical (this session SessionStart): system reminder lista `superpowers:using-superpowers`, `superpowers:test-driven-development`, `superpowers:writing-skills`, `superpowers:executing-plans`, `superpowers:dispatching-parallel-agents`, `superpowers:requesting-code-review`, `superpowers:brainstorming`, `superpowers:receiving-code-review`, `superpowers:systematic-debugging`, `superpowers:finishing-a-development-branch`, `superpowers:subagent-driven-development`, `superpowers:verification-before-completion`, `superpowers:using-git-worktrees`.

→ **Superpowers GIÀ installato e attivo** (13 skill members). Spec degenera a "verifica + ottimizza", non "install ex novo".

## Design

3 step:

1. Verifica installazione locale (path, version, last update)
2. Test invocation di una skill chiave
3. Documenta uso in CLAUDE.md o memory entry

## Acceptance criteria

- [ ] `~/.claude/superpowers/` directory exists (o equivalente installation path)
- [ ] Test `Skill` invocation di `superpowers:brainstorming` → carica body
- [ ] Memory entry `fact_superpowers_installed.md` documenta versione + 13 skill members
- [ ] CLAUDE.md root §2 cita "use `superpowers:*` skills for non-trivial impl"

## Implementation steps

### Step 1 — Verify install path

```bash
ls -la ~/.claude/superpowers/ 2>/dev/null
ls -la ~/.claude/plugins/ 2>/dev/null | grep -i super
find ~/.claude -name "*.md" -path "*superpowers*" 2>/dev/null | head -10
find ~/.claude -name "SKILL.md" 2>/dev/null | xargs grep -l "superpowers" 2>/dev/null | head -5
```

### Step 2 — Identifica path canonical

Possibili location (verifica which one):

- `~/.claude/skills/superpowers/` — Anthropic native skill dir
- `~/.claude/plugins/superpowers/` — plugin format
- `~/.claude/superpowers/` — Jesse Vincent original location

### Step 3 — Test invocation

In una sessione Claude Code interactive:

```
/help  # verifica `/superpowers:brainstorming` o `Skill` tool available
```

In Claude conversation:

```
Antonello: "invoke superpowers:brainstorming for adding feature X to wa-mirror"
Claude: should auto-call `Skill` tool with skill="superpowers:brainstorming"
```

### Step 4 — Documenta in memory

```bash
cat > ~/.claude/projects/-Users-nuzantara-Desktop-nuzantara/memory/fact_superpowers_installed_2026_05_21.md << 'EOF'
---
name: superpowers-installed
description: obra/superpowers (Jesse Vincent 7-phase workflow) attivo. 13 skill members invocabili via Skill tool.
metadata:
  type: fact
---

# Superpowers (obra) — 13 skill members attive 2026-05-21

Path: (verifica empirica T2.1)

Members ranked by use frequency expected:
1. `superpowers:using-superpowers` — entry point, always first
2. `superpowers:brainstorming` — Phase 1 of 7-phase
3. `superpowers:writing-plans` — Phase 2
4. `superpowers:test-driven-development` — Phase 3
5. `superpowers:subagent-driven-development` — Phase 4
6. `superpowers:requesting-code-review` — Phase 5
7. `superpowers:receiving-code-review` — Phase 5 reverse
8. `superpowers:verification-before-completion` — Phase 6
9. `superpowers:finishing-a-development-branch` — Phase 7
10. `superpowers:systematic-debugging` — debug flow
11. `superpowers:executing-plans` — multi-session plan exec
12. `superpowers:dispatching-parallel-agents` — fan-out pattern
13. `superpowers:using-git-worktrees` — worktree isolation

**How to apply**: per ogni feature impl / refactor / bugfix non-triviale, invoke `superpowers:using-superpowers` FIRST → routes to appropriate phase.

**See also**: [[karpathy-discipline]] (T1.4) — convergente, Karpathy = behavioral, Superpowers = procedural.
EOF
```

### Step 5 — Update CLAUDE.md root (1 line)

In `~/Desktop/nuzantara/CLAUDE.md` §2:

```markdown
- Non-trivial impl/refactor/bugfix → invoke `superpowers:using-superpowers` FIRST (7-phase workflow)
```

## Verification

### Test 1 — skill list in SessionStart

```bash
# Open new session, check system-reminder available-skills section
# Expected: 13 `superpowers:*` members listed
```

### Test 2 — invocation works

In Claude session, manually invoke:

```
Use `superpowers:brainstorming` to explore a new feature for wa-mirror
```

Expected: Claude calls Skill tool with skill="superpowers:brainstorming", loads body, follows phase-1 protocol.

### Test 3 — empirical efficacy

Track in next 7 days: count `Skill` invocations in transcripts. Target: ≥1 invocation per dense session (vs 0 in 3/4 sessions today 2026-05-21).

## Rollback

Se Superpowers crea conflict (es. forces TDD where Antonello wants skip):

```bash
mv ~/.claude/skills/superpowers/ ~/.claude/skills/.superpowers-disabled-20260521/
```

## Open questions

1. **Path real**: Step 1 verifica empirica. Sospetto `~/.claude/plugins/superpowers/` ma da confermare.
2. **Update strategy**: Superpowers ha repo GitHub, push updates regolarmente. Strategy:
   - Manual: `git -C ~/.claude/superpowers pull` weekly
   - Auto: cron `git pull` su path
   - Locked: stay on current version, pin
     Default proposto = manual weekly.
3. **Tradeoff con `karpathy-discipline` (T1.4)**: Superpowers è procedural (7 phase), Karpathy è behavioral (4 principi). Non si scontrano. Ordine invocation: karpathy-discipline FIRST (mindset) → superpowers:using-superpowers (action).
4. **Conflict con cicatrix-scars `commit_atomic`**: Superpowers TDD può richiedere commit di test BEFORE impl. Cicatrix dice "atomic commit, un fix per commit". Verifica: test+impl = "un fix" atomico? Sì.

## Estimated breakdown

| Step                  | Tempo      |
| --------------------- | ---------- |
| Verify install + path | 5 min      |
| Test invocation       | 3 min      |
| Memory fact write     | 2 min      |
| CLAUDE.md edit        | 2 min      |
| Verify Test 1-2       | 3 min      |
| **Total**             | **15 min** |
