• Adversarial review complete. I opened every load-bearing citation on this tree (directly or via three verification passes whose results I spot-checked against the raw files). Findings, most severe first.

  ---

  **F1 [CRITICAL]** The i18n mockup promises a deletion right the session model cannot deliver → `._panel_input/sponsor-i18n/delegate-i18n.body.html:232-243` renders "the traveller can delete this check at any time" / "wisatawan dapat menghapus pemeriksaan ini kapan saja" on the delegate surface. Verified on this tree: `garuda_voa_check_results` binds the result to `session_secret_hash CHAR(64)` (`286_garuda_voa_check_results.sql:99`) with no traveller identifier column at all (full column list 93-142 — no email, no client_id), and `deleteEligibilityResult` reads the `garuda_result_session` cookie (`garuda_voa_public.py:578`) with the match enforced via `store.delete` (`:590-595`). In delegate mode the sponsor's browser holds the only credential — the traveller cannot delete anything, at any time. The delegate dossier's own §5.2 says exactly this, and its mockup states the opposite honestly: "This browser controls this result — viewing or deleting it later needs this same device and session" (`research/design/mockups/delegate-flow/authorization-checkpoint.body.html:136-137`). The two mockups under joint review contradict each other on a data right — the exact consent-lane burn, re-introduced in translated copy. → Proposal: rewrite Screen 4 to name the session-holder ("whoever ran this check can delete it from this browser and session") or gate the traveller-named line to self-mode; add a §4 acceptance that deletion copy never names an actor the session model can't reach.

  **F2 [MAJOR]** "Sponsor" vocabulary contradiction between the dossiers → Delegate dossier §3.6/§5.5 forbids "sponsor" for the compiler (the codebase's `family_sponsor_*` vocabulary owns the word — verified `fact-mapper.ts:628-645`). The sponsor-i18n dossier is titled "Sponsor i18n", addresses the compiler as "the sponsor" (§3.2: "Indonesian copy addresses the sponsor directly as 'Anda'"), and its mockup Screen 1 carries the gate why-text "If someone is helping — a sponsor, a family member, an assistant…" (verified verbatim at `journey.html:199`) plus its ID twin "sponsor, anggota keluarga, atau asisten" (`delegate-i18n.body.html:136-137,148-149`). The delegate dossier ports that same gate (§3.1) without flagging that R5b's own copy violates its §3.6 rule. Neither dossier acknowledges the collision. → Proposal: one dossier owns the vocabulary decision; scrub "sponsor" from gate copy in both mockups or explicitly amend §3.6 with an R5b-inheritance exception.

  **F3 [MAJOR]** The i18n deliverable does not localize the surface the delegate dossier actually designed → Delegate §3.1 defines a three-option gate whose third option ("I'm arranging this for someone else, or for more than one person") is its central T1 contribution, and §3.2 adds the scope-limit line "This does not authorize payment on the traveller's behalf…". The sponsor-i18n mockup translates only R5b's two-option gate and the un-amended `delegate_confirm` (`delegate-i18n.body.html:126-195`) — no ID string exists anywhere for the third option or the scope-limit line. Its stated purpose ("the key-set item 4's build lane should consume", §5.2) fails on exactly the parts that are new. → Proposal: add EN/ID keys and mockup screens for the three-option gate and the scope-limit line, or re-scope the dossier explicitly to R5b's prototype strings.

  **F4 [MAJOR]** §6 evidence-class contradiction → Delegate dossier §6 is headed "(adopted under the standing pre-confirmation, async veto open)" while its own front matter declares "this draft has NOT been through the round-grade panel yet" (`2026-08-28-delegate-flow-design.md:15,25-26`). The sponsor-i18n dossier's §6 preamble states that writing "adopted" before a panel runs "would misstate this file's own evidence class under R7 §3.1" (`._panel_input/sponsor-i18n/2026-08-28-sponsor-i18n-design.md:280-289`). Opposite positions on the same doctrine clause, same review batch. → Proposal: re-label the delegate dossier's §6 as PROPOSED until its panel runs, matching the sibling's reading of R7 §3.1/§3.2.

  **F5 [MAJOR]** A business/funnel decision sits in the delegate dossier's §6 as a design declaration → §6.1 routes the `NOT_SELF_PAY ∪ GROUP_CASE` union to WhatsApp before any question. Today's funnel collects the full form first (verified step order `case_type→purpose→trip→dates` in `page.tsx:115,155,184,270`; decline only at `[hash]/page.tsx:157-160`), so this changes lead-capture economics; the dossier's own §5.4 admits nobody has measured the conversion cost. R7's mandate for item 4 (`r7-doctrine-loop-closure.md:184-187`) lists gate, checkpoint, pronoun layer, escapes — it does not mandate re-sequencing the decline routing, and R7:170 states ownership assignment is Zero's. → Proposal: recast the early exit as an owner decision (the consent lane's own §5.9 "business decision" class is the precedent), shipping R5b's binary gate with deferred decline as the default unless Zero rules otherwise.

  **F6 [MAJOR]** False universal propping up §5.7 → Delegate §1 and §5.7 claim "'excluded from pilot' appears at every exclusion site (:207-224)" / "every decline site in eligibility.py says 'excluded from pilot'". Verified: the string appears 5× (`eligibility.py:208,210,212,221,224`); the work/business decline (`:214-217`) lacks it, and `NOT_SELF_PAY` itself (`:201-202`, reason "not self-pay") lacks it and sits outside the cited range. §5.7's "pilot-shaped, not law-shaped" handoff to a future gate rebuild overstates its evidence for the very decline the dossier is built around. → Proposal: restrict the claim to the five tagged sites; ground NOT_SELF_PAY's pilot status only in the SOP docstring (`eligibility.py:3-8`) and say so.

  **F7 [MAJOR]** Persona-grounding inversion on R1:70 → Sponsor-i18n §1 anchors the whole item on R1:70 P3 ("The persona whose trust trigger THIS ITEM exists to satisfy is sourced, not assumed"). Verified `r1-psicologia…:70`: P3 is "The spouse (family & marriage route)" — the applicant — and "Bahasa Indonesia for the sponsor" there names the family-route sponsor subject. The delegate dossier §1 explicitly scopes that sponsor as "the person being asked about… a different axis entirely." Both readings of the same line cannot hold. (R6:102-105 — verified — does support an Indonesian-sponsor-operator reading, so valid grounding exists.) → Proposal: sponsor-i18n should anchor on `R6:102-105`/`R6:290-291` and drop or re-scope the R1:70 citation.

  **F8 [MINOR]** The "zero delegate hits" grep claim is false in both dossiers → Delegate §1 ("returns nothing outside journey.html itself") and sponsor-i18n §1 ("returns zero hits in either"). Actual: `apps/mouth/src/app/(visa-oracle)/visa-oracle/_lib/flow.ts:1208` contains "Delegate to `getCategoryQuestionIds`…" (unrelated verb, but a hit). Additionally journey.html is not inside either grepped tree, so "outside journey.html itself" is incoherent as stated. Substance (no delegate UX) stands. → Proposal: qualify the claim — "no delegate-mode UX anywhere; one incidental comment hit."

  **F9 [MINOR]** Citation-precision pattern in the sponsor-i18n dossier → (i) `pron()` cited `journey.html:336-343`; actual span 336-342 (343 is blank — the delegate dossier's 336-342 is the correct one). (ii) `delegateBanner` cited `journey.html:156-157` (also in the mockup at line 200); actual 156-158. (iii) R6:304 cited for "M5 supported+payment. Wharton passes…"; actual quote at R6:119-120 — :304 is the adversarial-review section. (iv) "checkout dossier §1: R1 rated it 5-quits"; the 5-quits line is in the checkout dossier's §2 T1 (`2026-08-27-checkout-garuda-design.md:62`). (v) §3's mockup path `mockups/sponsor-i18n/delegate-i18n.html` exists nowhere on this tree — UNVERIFIED here; the file under review is `._panel_input/sponsor-i18n/delegate-i18n.body.html`. → Proposal: correct all five.

  **F10 [MINOR]** Citation-precision pattern in the delegate dossier → (i) `eligibility.py` docstring cited `:1-27`; actual 1-30. (ii) Decline surface cited `[hash]/page.tsx:159`; the branch starts at :157 (:159 is `primaryDeclineCode`). (iii) `declineEducation.ts` cited as `apps/backend-rag/backend/components/garuda/declineEducation.ts`; actual path is `apps/mouth/src/components/garuda/declineEducation.ts` — the "(apps/mouth)" parenthetical does not rescue a nonexistent path. (iv) `i18n.ts:191-202,340-357` cited for three `family_sponsor_*` facts including `family_sponsor_confirmed`; its strings live at 393-398, outside the cited ranges. (v) `garuda_voa_public.py:585-587` "requires it to match"; the conditional spans 585-589 and the equality check is delegated to `store.delete` (590-595). (vi) The `ConsentHandoff.tsx:44` quote drops the string's trailing period (spans 44-45). Content verified in every case; the pins are sloppy in a dossier whose §1 header says "every claim below re-verified this round." → Proposal: correct the pins.

  **F11 [MINOR]** Wrong consumer attributed to the global i18n provider → Sponsor-i18n §1 lists `LanguageSettings.tsx` among `apps/mouth/src/i18n/` users. Refuted: `apps/mouth/src/components/portal/settings/LanguageSettings.tsx` imports `writeLanguageCookie` from `@/hooks/useLanguage` and persists via portal preferences — it never touches `src/i18n`. → Proposal: drop it from the provider's consumer list (the second-home `[locale]` routing part of the claim is verified).

  **F12 [MINOR]** "Verbatim" escape-copy claims don't match the mockups → Delegate §3.8 promises R5b's phrasing "ported verbatim in kind" and the mockup comment claims "R5b's own phrasing (journey.html:396), ported", but the rendered copy is "Check with them first, or hand this to a person →" (`authorization-checkpoint.body.html:158-159`) — R5b's actual string is "check with the traveller, or hand this to a person" (verified `journey.html:396`; the full sentence is assembled across 395-398, which also makes the sponsor mockup's attribution of the whole string to ":396" imprecise). → Proposal: port verbatim or declare the edit; the pronoun-layer discipline the dossier itself mandates (§3.5) forbids silent rewording.

  **F13 [MINOR]** Custody-density contract inconsistent between the two mockups → Sponsor-i18n §3.8 puts the delegate banner at the consent lane's full/HIGH tier *because it discloses who-sees* (`delegate-i18n.body.html:197-219`); the delegate mockup renders the same who-sees disclosure ("Who sees what: {CUSTODY_WHO_SEES}") inside the compact hint (`authorization-checkpoint.body.html:60-66,133-138`). Consent §6.1 (verified, `2026-08-27-consent-placement-design.md:125,251-253`) ties density to sensitivity — one fact, two densities, no stated rule. → Proposal: one dossier states which density a who-sees disclosure earns.

  **F14 [MINOR]** Indonesian-language defects → (i) §3.7 "KITAS/KITAS" is a typo for "KITAS/KITAP" — in the paragraph whose claim is terminological precision. (ii) The "Kasus mereka, fakta mereka" rationale misdiagnoses the defect: "mereka" is not "colder", it is number disagreement — a singular traveller takes the enclitic -nya; "mereka" is plural. The dossier's second reason ("under-specifies a single named relation") is the correct one. (iii) The recommended ID banner adds "bukan pendapat Anda sendiri", an obligation absent from the EN source ("answer as the traveller would"), while §3.4 claims it "keeps every regulatory-honesty commitment of the English" — it adds one, in ID only, against the EN/ID parity the consent lane already handed to legal (its finding #11). (iv) Screen 4 ID "Siapa yang melihat:" drops "apa" vs EN "Who sees what:" — in the copy type §3.3 itself classed as faithful/literal, legal-adjacent (`delegate-i18n.body.html:239-242`). (v) "Sebenarnya, biar wisatawan yang mengisi sendiri" (`delegate-i18n.body.html:189-190`) — "biar" is colloquial against the dictionary's own warm-formal law (verified `i18n.ts:10-11`); "sebaiknya" or "biarkan" fits the register. → Proposal: fix per item.

  **F15 [MINOR]** "Wisatawan" overreaches as the universal neutral noun → Correct for the GARUDA VOA surface (tourism-only SOP, verified `eligibility.py:3-8`), but the mockup also localizes Oracle-tree content — `family_relation`'s branchNote and the sponsor sub-flow badge (`delegate-i18n.body.html:331-336`) — where the traveller may be a family-route applicant, not a tourist; "Menjawab atas nama wisatawan" mislabels a spouse-route applicant. The dossier's scope clause covers this only implicitly. → Proposal: scope the "wisatawan" default to the VOA surface explicitly, or pick a relation-neutral noun for Oracle content.

  ---

  Verified-clean (no finding): R7:184-187/188-190 quotes, R4:108/122/124/130/140, R3:112, R1:70 quote, R6:102-105/290-291, R5b §5 quote + §8 item 2, consent §3.7b tiers / §3.8 audit / findings #4-#5, checkout.html `{LANG_TOGGLE}` header, ConsentHandoff.tsx guard/render lines, declineEducation.ts copy blocks, migrations 284/286/091 substance, `CheckoutFlow.tsx` 192 lines + bare labels + zero i18n, journey.html strings (who_answers, delegate_confirm, banner, branchNote, sub-progress badge) — all verbatim-accurate.

  Tally: 15 findings / 1 critical / 6 major / 8 minor.

To resume this session: kimi -r session_2a0ba764-4853-49df-9fd0-b2e30610232a
