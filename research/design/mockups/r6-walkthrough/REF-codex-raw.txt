1. **HIGH — False “zero welds” premise.** Claim: “audit found ZERO welds… Until that weld exists.” Current code enqueues `portal_invite`, registers `PortalInviteHandler`, and calls `ensure_portal_profile` (`repository.py:438–445`; `outbox_handlers.py:370–379, 467–474, 528–537`). The real documented blocker is that no scheduler/router invokes `drain_once` (`outbox_handlers.py:246–255`). The cited CRM memory contains no GARUDA, `portal_invite`, or `ensure_portal_profile` evidence. The report misdiagnoses an unarmed consumer as an absent weld.

2. **HIGH — The four-seat method is falsely described.** Claim: “4 cross-family seats judged FULL pages… then the red palette on both landings.” Qwen received stripped copy only, scored only T/S, choice, and dark-pattern risk, and never saw Task B (`prompt-qwen.txt:1–4`). `perception.json:3–4` itself admits “copy-only minimal payload, TASK A only,” contradicting the report’s method headline.

3. **HIGH — Qwen clarity scores were invented.** Claim: “copy clarity scored 5/5 by all four seats on both variants” / “All four seats scored status CLARITY 5/5.” Qwen supplied no C score at all. The defensible result is 3/3 responding full-rubric seats, not 4/4.

4. **HIGH — The “unanimous verifiability” result is fabricated.** Claim: “all four seats… condition the named variant on the VERIFIABILITY… [it] scores below the control for every seat.” Only Kimi explicitly states the stock/fictional counterfactual and ranks named below control for skeptical users. Qwen merely says unverifiability “can feel risky” while still scoring named 4.5 versus control 4; Codex and Agy never impose this condition. No four-seat below-control counterfactual exists in the raw outputs.

5. **HIGH — The WhatsApp unanimity is false.** Claim: “every seat flagged [WhatsApp-only] on every page that carries it.” Agy explicitly reports “Dark Pattern Audit — None” and says the named specialist breaks the scam-broker pattern (`PERC-agy-raw.txt:13–21`). Codex and Kimi flag WhatsApp; Qwen mentions an unclear handoff as a possible risk. That is mixed evidence, not unanimity across every page.

6. **HIGH — The panel was label-blind, not hypothesis-blind.** Claim: “Blind perception panel.” The prompts explicitly prime the target association—red-heavy + WhatsApp are “ALSO scam patterns”—and force “INSTITUTIONAL (state-adjacent, Merah Putih) or SCAM-ADJACENT” (`prompt-*.txt:5,10`). They also supply the later celebrated mechanism: red for structure and wine for errors (`:93–97`). Kimi’s “best sentence” substantially echoes that briefing. Task-B convergence is demand-contaminated and cannot be presented as independent discovery.

7. **MED — Order effects were not ruled out.** Claim: “the dissent is not order-explainable.” Agy choosing differently from Kimi under the same order only rules out order as a deterministic sole cause. With two seats per order, heterogeneous models, and Qwen using a different instrument, the study cannot estimate or exclude an order effect.

8. **HIGH — The a11y `PARTIAL` masks its own declared failure.** Claim: “Final: 7 PASS · 0 FAIL · 1 PARTIAL.” JSON does report that summary, but probe 5 declares CSS-zoom overflow a failure condition and records `scrollWidth=1108 > clientWidth=360`. In `a11y_suite.py:644–649`, the earlier `if hollow` branch returns `PARTIAL` before evaluating the triggered overflow condition. Under the suite’s own `would_fail_if`, the result is FAIL, not PARTIAL.

9. **MED — The walkthrough invents a prototype control.** Claim: “`Not sure` on marriage registration routes to a person.” `family_marriage_registered` offers Yes/No/Doesn’t apply and has no `notSure:true` (`journey.html:215`). The renderer creates the human handoff only for questions with that flag (`:394–398`). The report appears to conflate this prototype with the R1 baseline.

10. **HIGH — Archive and source declarations are presently false.** Claims: raw files are “archived with this round’s mockup dir,” the deliverable includes `research/design/mockups/r6-walkthrough/`, and “Everything load-bearing is executable or archived.” That directory does not exist in the R6 worktree; the panel/a11y artifacts remain in temporary scratchpads. The frontmatter also omits the primary a11y JSON/suite, while its relative R4/R5/R5b source reports are unresolved in the report’s own worktree.

11. **MED — R1 is assigned the wrong PR.** Claim: “R1… (PR #5064).” Git history identifies R1 as PR **#5060** (`66da2a21f`); PR **#5064** is the R2 competitor-census report (`30f954b24`).
