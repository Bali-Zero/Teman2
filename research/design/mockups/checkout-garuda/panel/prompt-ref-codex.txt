You are an adversarial refuter with FILESYSTEM ACCESS. Repo root: /Users/nuzantara/nuzantara (a monorepo; read from the worktree below for the file under review, from the main checkout or origin/main for live surfaces).

FILE UNDER REVIEW: /Users/nuzantara/nuzantara/.worktrees/docs-checkout-design/research/design/2026-08-27-checkout-garuda-design.md
MOCKUP UNDER REVIEW: /Users/nuzantara/nuzantara/.worktrees/docs-checkout-design/research/design/mockups/checkout-garuda/checkout.body.html

Your job: REFUTE. Verify every ground claim against the actual tree and every mockup claim against the actual mockup file. Priorities:
1. §1 ground claims vs live files: apps/mouth/src/app/visa/voa/checkout/[resultId]/CheckoutFlow.tsx (the no-price comment, the guard copy, read-only fields), apps/backend-rag/backend/services/payments/xendit.py (Invoices API, sandbox enforcement, fee-absorbed docstring, single return route), services/garuda_orders/models.py (Order shape), the claim that NO BZ-XXXX case-code generator exists anywhere (search hard), the claim that commit 63bfa19ec / main_api.py wired the outbox drain dark while outbox_handlers.py's docstring still denies it.
2. §2-T1: does R4's pricing law (research/design/2026-08-27-r4-identity-merah-putih-token-spec.md, the Pricing surfaces paragraph) actually support putting the price on the checkout page, or does its "two places" wording contradict the report's "three Q6c places"? Quote exactly.
3. §3 vs the mockup file: is the type scale actually rem-native (any px font sizes left?), are there actually two price renderings on the page + a claim about the verdict elsewhere, is the guard copy verbatim-identical to CheckoutFlow.tsx's, is the talk-first card actually BELOW the trust block in DOM order, does anything violate R4 tokens (colors, radius, min-height 44px = 2.75rem)?
4. §5 findings: are they real against the tree?
5. Any fabricated quote, wrong file:line, or overclaim anywhere.

Output: numbered findings, each with SEVERITY (HIGH/MED/LOW), the failing fragment quoted, and WHY with file:line evidence. No praise. If a claim survives your attack, do not mention it.
