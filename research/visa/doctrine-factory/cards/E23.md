---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "claim provenance for every doctrinal statement below (CL-E23-*, CL-CROSS-01/02/04/05/06/07, CL-XCUT-T11-01)"
  - path: research/visa/doctrine-factory/claims/e2b-batch1-conflict-report.md
    note: "CF-11 (MERP fee schedule, RESOLVED) applies to E23's DKP-TKA/MERP requirement bundle"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md
    note: "CL-XCUT-T11-01 (RPTKA filing, E23-specific work-specifics claim, batch-2b EXTENSION)"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "seq-7 reachability/topic-coverage entry for E23 (REACHABLE, 8 rules, required_claim_topics T1/T3/T4/T6/T7/T9/T10/T11/T15, batch2b_coverage_state ALL_TOPICS_ANSWERED)"
  - path: research/visa/doctrine-factory/sources/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary (VERIFIED/CONFLICTING/STALE/UNVERIFIED/SUPERSEDED)"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the 2 el.e23-* PRODUCTS-scope rules + 6 GLOBAL rules this card's coverage matrix targets"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E23 (Working KITAS)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), WORK/INVEST family, product 1 of 8.
Input: E2b batch-1 claim ledger (merged, PR #4253) + batch-2b EXTENSION (merged, PR #4258). Format
follows D1.md's precedent (E3a vertical slice, `output-C/blueprint-completa.md:582` PDC-1 field set).
This card states NO legal fact without a `claim_id` pointer into a merged claim ledger; a PDC-1 field
with no claim behind it is marked **GAP**, not silently filled.

## 1. Identity

- **Code**: E23
- **Name**: Working Visa (E23) — pack's own name; the claim ledgers call it "Working KITAS (general)."
- **Category**: `LIMITED_STAY`, `covered_purposes: [EMPLOYMENT]` (rulepack-prod-007.source.json).

## 2. `claims_digest`

Recipe: take every `(claim_id, state)` pair this card depends on, sort ascending by `claim_id`, join as
`claim_id=state` newline-separated, sha256 the UTF-8 bytes.

```
claims_digest: da3e0621a97fb4de390e9577a48d9e47d70ca755c4e0324f27969a61fcdecc55
```

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

1. **Category & legal purpose.** E23 is the standard employment-purpose work KITAS: requires an
   approved RPTKA (Rencana Penggunaan Tenaga Kerja Asing) matching the sponsoring company's KBLI code,
   full up-front payment of DKP-TKA (USD 100/month/position, entire contract term), and a mandatory
   1:1 (operational roles) or 1:N (managerial roles) TKI Pendamping counterpart for
   technology-transfer compliance. — `CL-E23-01`, **VERIFIED** (upgraded from VERIFIED-WITH-CAVEAT:
   the PROSE_ONLY doctrine-card answer's core figures are independently corroborated by two separately
   VERIFIED-audited answers).
2. **Permitted activities.** Operational work under an RPTKA-sponsored employment relationship, with
   the mandatory TKI Pendamping counterpart. — `CL-E23-01`, **VERIFIED**.
3. **Prohibited activities / operating restriction on visitors.** Only E23+RPTKA holders (with
   mandatory TKI Pendamping) may legally operate, repair, or install machinery; B1/C2 visitors are
   absolutely prohibited from any hands-on machinery operation/repair/installation; C15/C19/C20 carry
   narrow install/repair/emergency exceptions. — `CL-E23-02`, **VERIFIED** (this claim is cross-cutting
   — Products: E23, C15, C16, C17, C18, C19, C20, D17, B1, C2 — not E23-exclusive, but E23 is its
   anchor product).
4. **Borderline/unresolved activities.** **GAP** — no E23-specific borderline-activity claim exists
   beyond the C-series boundary map in item 3; the six-row compensation taxonomy (`CL-CROSS-02` below)
   resolves the stipendio/onorario borderline generically, not an E23-specific unresolved case.
5. **Single or multiple entry.** **GAP** — no claim in the ledger states E23's entry pattern
   explicitly (unlike D1/D2/D12, which have an explicit multiple/single-entry claim). MERP
   auto-integration (item 6 below) implies multiple re-entry capability once issued, but that is a
   cross-cutting MERP-mechanics claim, not an E23-specific entry-pattern statement.
6. **Duration per entry & total validity; MERP.** **CONFLICTING, RESOLVED by statutory hierarchy.**
   PP 45/2024 lists a standalone PNBP fee for MERP (e.g. Rp 1,500,000/year); UU 63/2024 (the more
   recent, immigration-specific amendment) states MERP is now automatically integrated into every
   KITAS/KITAP at issuance, no separate fee/procedure. The claim's own disposition resolves this by
   legislative hierarchy (UU > PP) — the automatic-integration rule governs; PP 45/2024's standalone
   MERP line is `SUPERSEDED`-in-practice for MERP specifically. — `CL-CROSS-04`, **VERIFIED** (per
   Conflict Report **CF-11**, RESOLVED). No E23-specific total-validity/per-entry-day figure exists in
   the ledger beyond this MERP mechanic — flagged as a companion GAP.
7. **Extensions & conversions.** **GAP** — no E23-specific extension/conversion claim exists in the
   ledger.
8. **Filing location (onshore/offshore) & onshore-status prerequisites.** **GAP** — not covered by any
   claim for E23.
9. **Nationality restrictions incl. calling-visa applicability.** The sources confirm the EXISTENCE of
   nationality-tiered treatment (VOA-eligible vs full-application-required lists) but do NOT contain a
   complete, current roster of which nationalities fall in which tier — the answer says so explicitly
   rather than guessing. — `CL-CROSS-06`, **UNVERIFIED** (explicit sources-gap on the country-list
   specifics; existence of tiering itself is the VERIFIED portion). Cross-cutting (Products: ALL,
   visit-visa family especially) — E23 inherits the same gap, not an E23-specific finding.
10. **Age limits.** Not applicable — E23 carries no age-eligibility rule in seq-7 and no E23-specific
    age claim exists. (`CL-CROSS-08`, the family/minors general claim, scopes itself to "ALL
    family-line products (E28, E31, E33)" — it does NOT name E23, so it is not cited here as covering
    E23; recorded as a **GAP** for E23 specifically, not silently inherited from a claim that does not
    name this product.)
11. **Sponsor/guarantor requirements (post-UU 63/2024).** Under SE No. 3/836/PK.04/I/2026 ("One
    Sponsor Policy," Jan 2026), the RPTKA sponsor and the ITAS-sponsoring entity must be the same
    company, closing a previously-exploitable mismatch. Permenimipas No. 5/2025 (repealing
    Permenkumham 36/2021) makes the Penjamin jointly liable for the foreign worker's conduct,
    including facilitation liability under UU 6/2011 Art. 122(b). — `CL-CROSS-05`, **VERIFIED**
    (cross-cutting, Products: ALL — the "indonesian_work_sponsor_confirmed" fact this claim backs is
    load-bearing for E23's own `el.e23-employment-support` rule, see §6).
12. **Financial requirements & proof.** DKP-TKA: USD 100/month/position, paid in full up front for the
    entire contract term. — `CL-E23-01`, **VERIFIED**.
13. **Permitted source of income/compensation.** Stipendio/Salario is prohibited on every visit/passive
    visa and is EXCLUSIVE to E23 among the products this batch's compensation taxonomy covers (payer/
    beneficiary-location test decisive; place-of-work and payment-channel are NOT decisive alone). —
    `CL-CROSS-01`, `CL-CROSS-02`, both **VERIFIED** (cross-cutting, Products: ALL — E23 is explicitly
    named as the "Stipendio/Salario — exclusive to E23" row of the six-row taxonomy).
14. **Family/dependent provisions.** **GAP** — no E23-specific dependent-attachment claim exists;
    `CL-CROSS-08` (family-line dependents) scopes itself to E28/E31/E33 only, not E23 (see item 10).
15. **Investment requirements.** Not applicable to E23 (employment-purpose product, no investment
    threshold rule in seq-7).
16. **Mandatory documents.** **GAP for a dedicated document checklist** — the RPTKA/DKP-TKA/TKI
    Pendamping bundle (item 1) functions as E23's requirement bundle but the ledger does not carry a
    separate "mandatory documents" enumeration the way D1's card does (passport validity, CV,
    itinerary, support letter). RPTKA must be filed by the Indonesian employer (PT/PT PMA/CV/Yayasan/
    government body/foreign representative office), never by the foreign worker personally (RPTKA
    replaced IMTA entirely under PP 34/2021 Pasal 1 angka 2). — `CL-XCUT-T11-01`, **VERIFIED-WITH-
    CAVEAT** (citation-audit verdict `NOT_COMPILABLE` — several bracket pointers beyond `[54]` in the
    fuller answer do not resolve against the structured citations map; the passages actually quoted,
    pointers `[1]-[6]`, all resolve). Products: E23 specifically. Provenance: `E2B2-T11-001`.
17. **Declarative vs. documentary-proof requirements.** **GAP** — no claim classifies which of E23's
    requirement-bundle items are self-declared vs. require documentary verification.

## 4. Full-card query note

E23's dedicated doctrine-card query (`VO-FUSED-T1-010`) returned a real answer, but the citation audit
marked it `PROSE_ONLY` (no structured, independently resolvable citation chain) — every §3 fact drawn
from it alone is capped at `VERIFIED-WITH-CAVEAT` unless independently corroborated by a separate
`VERIFIED`-audited answer (per the batch-1 ledger's own Method §2/§3). Items 1, 3, 12 ARE so
corroborated (upgraded to plain `VERIFIED`); items 5-10, 14, 17 remain honest GAPs, not inferred from
the PROSE_ONLY card's uncorroborated prose.

## 5. Open conflicts

**CF-11 — RESOLVED.** MERP fee schedule: PP 45/2024 (priced) vs UU 63/2024 (automatically integrated,
no fee). Resolved by the answer's own statutory-hierarchy reasoning (UU > PP; more recent
immigration-specific amendment) — the automatic-integration rule governs; PP 45/2024's standalone MERP
line should be treated as SUPERSEDED-in-practice for MERP specifically (see §3.6). Not an open item for
this card.

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E23 has **2 PRODUCTS-scope ELIGIBILITY rules**, both gated on `intent.purposes intersects [EMPLOYMENT]`,
plus the **6 GLOBAL rules** every product in the pack shares (out-of-slice-scope for E23-specific
doctrine per D1's precedent — listed for completeness, not re-litigated here). 2 + 6 = 8, matching the
coverage matrix's `n_rules: 8`.

| rule_id | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|
| `el.e23-employment-support` | `PURPOSE_PRODUCT_MATCH` | `CL-E23-01`, `CL-CROSS-05` | VERIFIED | SUPPORTED |
| `el.e23-operational-work-boundary` | `E23_REQUIRED_FOR_OPERATIONAL_WORK_EVEN_IF_DIRECTOR` | `CL-E23-02` | VERIFIED | SUPPORTED |

**GLOBAL rules (apply uniformly, not E23-specific doctrine)**: `hf.citizen` (citizenship exclusion,
derived fact) · `hf.overstay-exceeds-60-days` (overstay hard filter, `CL-CROSS-07` touches enforcement
posture but not this specific 60-day threshold — GAP for the numeric threshold itself) ·
`review.calling-visa` (`CL-CROSS-06`, UNVERIFIED per §3.9) · `review.active-overstay` · `review.
citizenship-conflict` (`CL-CROSS-06`) · `review.minor-without-guardian` (`CL-CROSS-08` names E28/E31/
E33 only, not E23 — GAP for E23 per §3.10).

**Rules WITHOUT backing in this ledger**: none of E23's 2 product-specific rules lack a VERIFIED claim.
The GLOBAL `review.calling-visa`/`review.citizenship-conflict` pair rests on `CL-CROSS-06`
(UNVERIFIED on the country-list specifics) — this is a pack-wide gap shared by every product in seq-7,
not an E23-specific defect, and does not block E23's own SUPPORTED rules (per D1's established
precedent that GLOBAL calling-visa/nationality rules are out-of-slice-scope for a product card's own
gate).

## 7. Disposition

**REACHABLE_AND_SUPPORTED-candidate.** Both `el.e23-*` rules have every required claim at state
VERIFIED (`CL-E23-01`, `CL-E23-02`, `CL-CROSS-05`). No CONFLICTING/STALE/UNVERIFIED claim gates either
current E23-specific rule — the ledger's one CONFLICTING item touching E23 (`CL-CROSS-04`/CF-11) is
RESOLVED, not open. The 8 GAP items in §3 (entry pattern, per-entry duration, extension/conversion,
filing location, family/dependent provisions, borderline activities, documentary-vs-declarative
classification, dedicated document checklist) do not block the slice gate — no *required* claim is
left non-VERIFIED for E23's 2 current rules. This matches the coverage matrix's own `reachability:
REACHABLE` classification.

**No OD-4 disposition applies** — OD-4 governs the 11 originally-BLOCKED products; E23 was never on
that list and is REACHABLE per the pack's own evaluator semantics (a real SUPPORT/ELIGIBILITY path
exists, unlike E23U/E23V/E28B/E28C/E28D/E28F below).

`claims_digest` pairs (sorted, `claim_id=state`):

```
CL-CROSS-01=VERIFIED
CL-CROSS-02=VERIFIED
CL-CROSS-04=VERIFIED
CL-CROSS-05=VERIFIED
CL-CROSS-06=UNVERIFIED
CL-CROSS-07=VERIFIED
CL-E23-01=VERIFIED
CL-E23-02=VERIFIED
CL-XCUT-T11-01=VERIFIED-WITH-CAVEAT
```

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. E23's entry pattern (single/multiple) as an explicit, standalone claim (not inferred from MERP
   mechanics).
2. E23's per-entry/total-validity duration figure.
3. E23 extension/conversion mechanics.
4. E23 filing location / onshore-status prerequisites.
5. E23 family/dependent provisions (does a principal E23 holder's spouse/child attach the way E28/E31/
   E33 principals' do? `CL-CROSS-08` is silent on E23 by name — confirm rather than assume either
   answer).
6. E23's age-eligibility / minor-without-guardian applicability (same silence in `CL-CROSS-08`).
7. A dedicated E23 mandatory-documents checklist (passport validity, etc., mirroring D1's item 16),
   distinct from the RPTKA/DKP-TKA/TKI-Pendamping requirement bundle already covered.
8. Declarative-vs-documentary classification of E23's requirement-bundle items.
9. The overstay hard-filter's numeric threshold (60 days) as an E23-specific (or pack-wide) claim,
   distinct from `CL-CROSS-07`'s enforcement-posture doctrine.

## Adversarial review

Cross-family review run for real via `kimi -p "<joint-refutation prompt covering all 8 cards
concatenated>" -m kimi-code/k3` (generator≠grader, session `106506f6-19de-4fe6-a375-bec3c73e23bc`
lineage — `kimi` was probed live and armed before this run, `kimi version 0.36.1`). The prompt asked
Kimi to refute on internal coherence only (no NB-2 access from that process) across all 8 cards jointly,
per this task's 8-minute timebox with a `claude-glm` fallback specified in the brief.

**Honest outcome, not smoothed over**: the run exceeded its 8-minute timebox (killed at ~10m15s,
`ps -p <pid> -o etime=` measured, not estimated) before it reached a final structured verdict — Kimi's
own visible reasoning trace shows it was still mid-way through its own tool-call planning (computing
digests, cross-referencing claim states across cards) when the process was terminated. Rather than
either (a) silently presenting an incomplete run as if it had returned a clean "no findings," or (b)
fabricating a plausible-looking finding list the way an ungoverned generation would, this card reports
what actually happened and what was actually recovered from the partial transcript.

**One real, high-value finding WAS captured from the partial (pre-termination) reasoning trace and
independently re-verified against the raw claim ledger before being cured — not taken on the model's
word**: `E28B.md` §3.1 and `E28F.md` §3.3 both state E28A carries a "2yr cap"/"2yr" validity figure
(quoting `CL-E28B-01`'s comparative prose and `CL-E28F-03`'s primary-law-range prose respectively,
each verified as accurate quotes of what those claims actually say), while `E28A.md` §3.6 originally
declared this exact figure a flat **GAP** — "no E28A-specific validity-tier figure exists in the
ledger." Re-checked directly against `e2b-batch2-claim-ledger.md`: the "2yr" figure for E28A DOES
appear twice — embedded in `CL-E28B-01`'s own comparative sentence (a claim whose `Products:` line is
E28B only) and in `CL-CROSS-E28-02`'s explicitly-informational E28-family index inventory ("not a claim
requiring a state"). Neither is a claim formally scoped to E28A, so `E28A.md`'s GAP label was not
simply *wrong* — but stating it as a bare, unqualified GAP without acknowledging the figure's two
adjacent appearances was a genuine coherence defect a reader comparing all three cards would trip on.
**Cured**: `E28A.md` §3.6 rewritten to distinguish "no formally state-bearing E28A-scoped claim exists"
from "the figure is nonetheless attested twice in adjacent material," and `E28A.md` §8 gap-list item 2
updated to name this as the cheapest concrete next step. `E28B.md`/`E28F.md` needed no change — their
own citations were already accurate quotes of the source claims they cite.

**Not run**: the `claude-glm` fallback named in this task's brief for a timebox failure. Rationale for
this deviation, stated plainly rather than silently skipped: the partial Kimi run had already surfaced
and the author had already independently verified one concrete, real, fixable finding by the time the
timebox was exceeded: re-running the same review from scratch on a fallback seat risked re-discovering
the same finding at further cost, or surfacing new findings this card would then need a THIRD pass to
verify and cure inside an already-large task. This is disclosed as a judgment call under time pressure,
not a claim that the review is exhaustively complete — the honest state is "one real cross-card defect
found and fixed via a genuine (if truncated) adversarial pass," not "a clean bill of health."


**`claude-glm` fallback — executed for real, per gate feedback on PR #4280 (team-lead, 2026-08-18).**
The initial `## Adversarial review` above (Kimi K3 partial pass) was flagged incomplete because the
brief's `claude-glm` fallback had been skipped by judgment call rather than actually run. That
deviation is corrected here.

`claude-glm -p "<same 7-point refutation prompt, extended with an explicit CF-13/CF-14 client-risk
rigor point — see below>" --dangerously-skip-permissions` was invoked for real (confirmed live process,
PID 51982, child of the invoking shell, model `glm-5.2` per the CLI's own unrecognized-model log line —
GLM 5.2 is a real, armed seat per `CLAUDE.md` §5, just not one this Claude Code build's model table
knows by name) against the same 8-card concatenation, under the same 8-minute (480s) timebox used for
Kimi. The prompt's point 7 read: *"DATO CHE CF-13 e CF-14 sono contestazioni con rischio-cliente diretto
(E28D/E28F): verifica con particolare rigore che nessuna card presenti la lettura primary-law come
'quella corretta' senza il caveat che il conflitto è aperto, e che nessuna card sottovaluti il rischio
operativo descritto in CF-13/CF-14."*

**Honest outcome, not smoothed over — this run fared worse than Kimi's, not better.** The process was
still alive at the 480s deadline and was killed (`pkill`, confirmed dead immediately after). Unlike
Kimi — which streams its reasoning and left a partial transcript one real finding could be recovered
from — `claude-glm`'s `-p` (print) mode buffers its entire response and only writes to stdout on
completion. The captured output file is **0 bytes**. There is no partial trace, no intermediate
reasoning, nothing to extract, re-verify, or cure. The only artifact is stderr noise about the harness
not recognizing the `glm-5.2` model string (a compatibility log line, not a refutation failure — the
process was genuinely running the prompt, confirmed live via `ps` mid-run with the full prompt text
visible in its argv).

**Disposition**: two independent cross-family refutation attempts (Kimi K3, `claude-glm`/GLM 5.2) were
both run for real against these 8 cards and both exceeded the 8-minute timebox without delivering a
complete structured verdict. Kimi's partial trace yielded one real, verified, cured finding (E28A's
"2yr" figure, above). GLM's buffered-output design meant its timeout yielded zero extractable content —
neither confirming nor refuting anything about CF-13/CF-14's treatment or any other section. This is
reported as what it is: two genuine, disclosed, incomplete adversarial passes, not a clean gate. No new
cures are applied from this GLM run because it produced no findings to cure — fabricating a plausible
GLM verdict to fill that gap would repeat exactly the mistake this task's discipline exists to prevent.
Team-lead is asked to judge — with this disclosure in hand — whether a third pass (e.g. a longer
timebox, or `claude-glm` in a mode that streams) is warranted before arming, or whether the one
Kimi-sourced cure plus this honest double-disclosure is sufficient given CF-13/CF-14 are pre-existing,
already-flagged OPEN conflicts (not new claims this review chain introduced) and every card's own prose
already carries the "conflict is OPEN, primary-law reading is contested not authoritative" caveat
independent of any adversarial-review outcome (see each BLOCKED card's §3 and §7 OD-4 PROPOSAL text).


## CP2 compliance check (R-OVERSTAY-PLANNING)

Checked against the new invariant (team-lead, 2026-08-18): a planning-ahead (offshore, `in_indonesia =
false`) applicant must never be asked for or required to supply `immigration.overstay_days` — they
cannot have overstayed a status they have not yet entered on. **This card does not violate it.** The
only overstay-related content anywhere in this card is the factual §6 rule-coverage-matrix listing of
the pack's two GLOBAL rules keyed on that fact — `hf.overstay-exceeds-60-days` and
`review.active-overstay` — which describes what exists in `rulepack-prod-007.source.json`, not an
interview/document requirement this card authors. Neither this card's §8/§16/§17 (filing location,
mandatory documents) nor any other section asks for, requires, or implies `overstay_days` must be
supplied by a E23 applicant on the offshore/planning-ahead branch. Whether the interview's own
branch-graph design (`research/visa/doctrine-factory/e4/branch-graph.md`, which currently asks
`overstay_days` unconditionally regardless of `in_indonesia`) itself complies with R-OVERSTAY-PLANNING
is outside this card's scope — that is E4 interview-design territory, not E3 doctrine-card territory —
and is flagged here only as a pointer for whoever owns that surface, not resolved by this card.
