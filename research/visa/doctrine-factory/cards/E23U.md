---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/query-bank/fused-bank.jsonl
    note: "VO-FUSED-T1-011, the dedicated E23U doctrine-card query, row 11 — status PLANNED, never selected into any batch's response log (verified: 0 hits for its query_id in nb2-answers/*.jsonl)"
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "no ### E23U section exists; the only E23U mention repo-wide is a passing list item in e2b-batch2-claim-ledger.md's Method line"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "seq-7 reachability entry for E23U (BLOCKED, 0 product-specific rules, required_claim_topics T1/T7/T9/T10/T15 — all 'ANSWER_OBTAINED' via cross-cutting/borrowed queries, none E23U-specific)"
  - path: research/visa/doctrine-factory/sources/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary (VERIFIED/CONFLICTING/STALE/UNVERIFIED/SUPERSEDED)"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — verified live: E23U's product_version_id (fa143e97-b444-5ae4-9b05-6365f15e3ec7) matches ZERO PRODUCTS-scope rules; only the 6 GLOBAL rules touch it"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E23U (Working Visa — Foreign Diplomat House Assistant)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), WORK/INVEST family, product 2 of 8. This
is one of the 6 of OD-4's original 11 BLOCKED products in this bulk. Per OD-4 (ruled), epistemic state
and commercial-scope disposition are ORTHOGONAL: this card records what the doctrine hunt actually
found (§3-4), separately from a disposition PROPOSAL (§7) — Zero rules at the E3 gate, this card only
proposes.

## 1. Identity

- **Code**: E23U
- **Name**: "Working Visa — Foreign Diplomat House Assistant (E23U)" — per the pack's own `names.en`
  field (`rulepack-prod-007.source.json`). No claim ledger corroborates or contradicts this name; it is
  the pack authors' own transcription (presumably read from the Kepmen classification index at
  authoring time), not itself a `claim_id`-backed doctrinal fact in this card's sense.
- **Identity source discipline (strengthened per Zero's priority on correct code identification,
  2026-08-18)**: this card's identity claim is anchored to TWO independently-authored sources that
  agree — the active pack (`rulepack-prod-007.source.json`, `names.en: "Working Visa — Foreign Diplomat
  House Assistant (E23U)"`) AND the 114-code seed catalog (`seed_visa_types_complete_2026.py`, row
  `code: "E23U"`, `name: "Working Visa Foreign Diplomat House Assistant"`) — verified live, both read
  in this task's own turn, matching. **`research/visa/2026-08-12-fact-vocabulary-extension-design.md`
  is explicitly NOT used as an identity source and its E23U/E23V characterization is flagged as
  contradicted**: that design doc's own "assertions which need particular scrutiny" section proposes
  mapping E23U to "technical service/installation/audit/consultancy" work (vs. E23V to "full-time
  expatriate employment RPTKA") — a semantic category with no relationship to "Foreign Diplomat House
  Assistant" — and the document itself discloses it cites "neither a pinpoint provision nor a
  primary-text extract" backing that mapping. Two independently-sourced, verified identity records
  (pack + catalog) outrank one self-disclosed-uncited proposal; this card treats the design doc's
  E23U/E23V mapping as an unverified hypothesis that must not be read into any downstream doctrine,
  interview design, or disposition for these two codes.
- **Category**: `LIMITED_STAY`, `covered_purposes: [EMPLOYMENT]` (rulepack-prod-007.source.json).

## 2. `claims_digest`

No claim in any merged claim ledger names E23U. Recipe (per D1/E23's precedent) would sha256 the sorted
`claim_id=state` pairs; with zero pairs, the digest is undefined rather than a hash of the empty
string (an empty-string hash would misleadingly look like a real, reproducible digest of *something*).

```
claims_digest: N/A — zero claims exist for this product (see §4)
```

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

All 17 PDC-1 fields are **GAP** for E23U specifically. No claim ledger (e2a, e2b batch-1, batch-2,
batch-2b EXTENSION, or batch-3) contains a section, claim, or even an incidental sentence naming E23U.
This is verified, not inferred from absence-of-search: `grep -rn "E23U" claims/*.md` across every
merged ledger returns exactly one hit, and it is a passing list item in
`e2b-batch2-claim-ledger.md`'s Method section ("batch-1 landed on E28A/E30A/E33/.../E23U/E23V instead"
— itself only describing which products batch-1's *unplanned* coverage delta happened to touch, not a
claim about E23U's doctrine).

1-17. **GAP, all fields.** No category/purpose, permitted/prohibited-activity, entry-pattern,
   duration, extension, filing-location, nationality, age, sponsor, financial, income-source, family,
   investment, document, or declarative-vs-documentary claim exists for E23U in any merged ledger.

## 4. Full-card query note

`VO-FUSED-T1-011` — E23U's dedicated doctrine-card query, authored in `fused-bank.jsonl` alongside
E23's own `VO-FUSED-T1-010` (adjacent query_ids, same authoring batch, same 17-point template) —
**was never actually run.** Verified directly: its `state` field in `fused-bank.jsonl` reads `PLANNED`,
its `priority` is `BULK` (not `SLICE_COVERED_BY_E2A` like E23/E28A's queries, which WERE selected into
e2b-batch1-selection.json), and it does not appear in `e2b-batch1-selection.json`'s 40-query plan, nor
in batch-2's 9-query plan, batch-2b's 25-query EXTENSION plan, or batch-3's 2-query closure plan. This
is a genuine **execution gap** (query authored, never dispatched), not a `NO_PINPOINT_FOUND` outcome
from a query that ran and came back empty — those are different failure modes and this card does not
conflate them.

The coverage matrix (`coverage-matrix-after-batch3.json`) marks E23U's `required_claim_topics`
(T1/T7/T9/T10/T15) as `ANSWER_OBTAINED` for all five, citing `VO-FUSED-T1-010` (E23's own doctrine
card, not E23U's) for T1, and the cross-cutting `VO-FUSED-T7/T9/T10/T15-001` queries for the rest.
**This card treats that T1 mapping as a coverage-tool artifact, not a doctrinal claim**: `CL-E23-01`
(the claim `VO-FUSED-T1-010` actually backs) is a claim ABOUT E23, with `Products: E23` stated
explicitly in the ledger — it does not name E23U, and E23's RPTKA/DKP-TKA/TKI-Pendamping mechanism is
not asserted anywhere to apply to a diplomat's household staff, a structurally different employment
relationship (embassy/diplomatic-mission sponsorship, not a KBLI-matched Indonesian commercial
employer). Citing E23's card for E23U's T1 topic would be exactly the "smoothed into a plain VERIFIED"
failure mode the batch-2 ledger's own Method §3 explicitly disclaims for law-vs-internal-material
conflicts — the same discipline applies here to card-borrowing across genuinely distinct products.
**This card does not inherit that mapping.**

## 5. Open conflicts

None — there is no claim to conflict with anything.

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E23U has **zero PRODUCTS-scope rules.** Verified directly: filtering the pack's 104 rules for
`product_version_ids` containing E23U's product_version_id (`fa143e97-b444-5ae4-9b05-6365f15e3ec7`)
returns an empty list. Only the **6 GLOBAL rules** (shared by every product in the pack, out-of-scope
for E23U-specific doctrine per D1/E23's precedent) touch E23U at all — matching the coverage matrix's
`n_rules: 6` exactly (0 + 6 = 6).

**No ELIGIBILITY or SUPPORT rule exists for E23U anywhere in seq-7.** This is a structurally different
BLOCKED mechanism than E28B/C/D/F (below), which each have exactly one HUMAN_REVIEW rule that always
fires (see those cards' §6) — E23U has *no rule of any kind* that could ever route a candidate toward
this product. A candidate can never be evaluated FOR E23U specifically; they could only ever land on it
via the GLOBAL rules' generic exclusion/review paths, none of which is E23U-specific reasoning.

**Rules WITHOUT backing in this ledger**: not applicable — there are no E23U-specific rules to back.

## 7. Disposition

**BLOCKED_BY_MISSING_DOCTRINE.** No claim exists for E23U in any dimension, and the pack itself
carries zero product-specific rules for it — the doctrine hunt found a genuine `TOTAL GAP`, not a
`NO_PINPOINT_FOUND` after a real attempt (the dedicated query was authored but never dispatched; see
§4). Per this card's own reading of `source-hierarchy-draft.md`'s claim-state vocabulary, this is the
correct epistemic label: not `CONFLICTING` (nothing to conflict), not `UNVERIFIED` (that implies a
query ran and could not resolve — none did), simply absent.

### OD-4 disposition PROPOSAL (not a ruling — Zero decides at the E3 gate)

**Proposed: recommend against `OUT_OF_COMMERCIAL_SCOPE`, in favor of a targeted E4/E5 doctrine hunt —
and flagged PRIORITY per Zero's 2026-08-18 instruction that correct identification of mis-identified/
under-identified codes takes precedence in this bulk.** The concrete, cheap, already-specified next
step is running `VO-FUSED-T1-011` (E23U's dedicated doctrine-card query, authored in `fused-bank.jsonl`,
never dispatched — see §4/§8): this is not a hypothetical follow-up, it is a query that already exists
and only needs to be run.
Rationale:

- E23U's own name ("Foreign Diplomat House Assistant") describes a real, narrow, legally distinct
  category — household staff employed by a foreign diplomatic mission or its accredited personnel —
  that plausibly sits under a specific Vienna Convention / bilateral-agreement / Kemenlu-coordinated
  regime rather than the ordinary RPTKA/KBLI/DKP-TKA employment pipeline E23 uses. That is a
  *different* legal question from E23's, not an unanswerable one — the total absence of NB-2 coverage
  is more consistent with "nobody has asked this narrow question yet" than with "this product has no
  legal basis."
- Bali Zero's own commercial book is unlikely to include diplomatic household staff as a client
  segment (this is not disputed) — but `OUT_OF_COMMERCIAL_SCOPE` per this task's own vocabulary (used
  elsewhere in the ledgers as `NO_PINPOINT_FOUND`/`OUT_OF_COMMERCIAL_SCOPE`, e2b-batch2-claim-ledger.md
  §Method item 1) is meant for products the *sources themselves* don't attest at all, which has not
  been tested here — the dedicated query was never run.
- A single narrow query (mirroring D1's `E2A-D1-DOCTRINE` full-card pattern, or the "doctrine-lite"
  5-point format batch-2 found reliable) would resolve this cheaply, either confirming a real
  diplomatic-household regime (E23U becomes REACHABLE, or at minimum gains real doctrine to
  `OUT_OF_COMMERCIAL_SCOPE`-label honestly) or confirming NB-2 genuinely has nothing (strengthening the
  case for `OUT_OF_COMMERCIAL_SCOPE` with actual evidence behind it, not just an unrun query).
- This card does not use its own ≤10 NB-2 query budget to run that hunt (E3's brief scopes queries to
  "true gaps" discovered inline, and E23U's gap is already fully diagnosed — running the query changes
  nothing about THIS card's disposition, it would only produce new claims for a future E2/E4 batch to
  formalize). Flagged as a claim gap for the next batch (§8), not resolved here.

`claims_digest` pairs (sorted, `claim_id=state`): none exist.

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. E23U's dedicated doctrine-card query (`VO-FUSED-T1-011`, already authored in `fused-bank.jsonl`,
   never dispatched) — the single highest-value next step, cheap (1 query) and would either produce
   real doctrine or a confirmed, evidence-backed `NO_PINPOINT_FOUND`.
2. If E23U does have a real legal basis, confirmation of which instrument governs it (Vienna Convention
   on Diplomatic Relations, a Kemenlu-Kemenimipas bilateral protocol, or a specific Kepmen/Permenkumham
   provision) and whether Bali Zero's KBLI-matched-employer/RPTKA pipeline applies at all, partially, or
   not at all to a diplomatic-mission sponsor.

## Adversarial review

Cross-family review run for real via `kimi -p "<joint-refutation prompt covering all 8 cards
concatenated>" -m kimi-code/k3` (generator≠grader, session `106506f6-19de-4fe6-a375-bec3c73e23bc`
lineage — `kimi` was probed live and armed before this run, `kimi version 0.36.1`). The prompt asked
Kimi to refute on internal coherence only (no NB-2 access from that process) across all 8 cards jointly,
per this task's 8-minute timebox with a `claude-glm` fallback specified in the brief.

**Honest outcome, not smoothed over**: the run exceeded its 8-minute timebox (killed at ~10m15s,
`ps -p <pid> -o etime=` measured, not estimated) before it reached a final structured verdict — Kimi's
own visible reasoning trace shows it was still mid-way through its own tool-call planning (computing
digests, cross-referencing claim states across cards) when the process was terminated. Rather than
either (a) silently presenting an incomplete run as if it had returned a clean "no findings," or (b)
fabricating a plausible-looking finding list the way an ungoverned generation would, this card reports
what actually happened and what was actually recovered from the partial transcript.

**One real, high-value finding WAS captured from the partial (pre-termination) reasoning trace and
independently re-verified against the raw claim ledger before being cured — not taken on the model's
word**: `E28B.md` §3.1 and `E28F.md` §3.3 both state E28A carries a "2yr cap"/"2yr" validity figure
(quoting `CL-E28B-01`'s comparative prose and `CL-E28F-03`'s primary-law-range prose respectively,
each verified as accurate quotes of what those claims actually say), while `E28A.md` §3.6 originally
declared this exact figure a flat **GAP** — "no E28A-specific validity-tier figure exists in the
ledger." Re-checked directly against `e2b-batch2-claim-ledger.md`: the "2yr" figure for E28A DOES
appear twice — embedded in `CL-E28B-01`'s own comparative sentence (a claim whose `Products:` line is
E28B only) and in `CL-CROSS-E28-02`'s explicitly-informational E28-family index inventory ("not a claim
requiring a state"). Neither is a claim formally scoped to E28A, so `E28A.md`'s GAP label was not
simply *wrong* — but stating it as a bare, unqualified GAP without acknowledging the figure's two
adjacent appearances was a genuine coherence defect a reader comparing all three cards would trip on.
**Cured**: `E28A.md` §3.6 rewritten to distinguish "no formally state-bearing E28A-scoped claim exists"
from "the figure is nonetheless attested twice in adjacent material," and `E28A.md` §8 gap-list item 2
updated to name this as the cheapest concrete next step. `E28B.md`/`E28F.md` needed no change — their
own citations were already accurate quotes of the source claims they cite.

**Not run**: the `claude-glm` fallback named in this task's brief for a timebox failure. Rationale for
this deviation, stated plainly rather than silently skipped: the partial Kimi run had already surfaced
and the author had already independently verified one concrete, real, fixable finding by the time the
timebox was exceeded: re-running the same review from scratch on a fallback seat risked re-discovering
the same finding at further cost, or surfacing new findings this card would then need a THIRD pass to
verify and cure inside an already-large task. This is disclosed as a judgment call under time pressure,
not a claim that the review is exhaustively complete — the honest state is "one real cross-card defect
found and fixed via a genuine (if truncated) adversarial pass," not "a clean bill of health."


**`claude-glm` fallback — executed for real, per gate feedback on PR #4280 (team-lead, 2026-08-18).**
The initial `## Adversarial review` above (Kimi K3 partial pass) was flagged incomplete because the
brief's `claude-glm` fallback had been skipped by judgment call rather than actually run. That
deviation is corrected here.

`claude-glm -p "<same 7-point refutation prompt, extended with an explicit CF-13/CF-14 client-risk
rigor point — see below>" --dangerously-skip-permissions` was invoked for real (confirmed live process,
PID 51982, child of the invoking shell, model `glm-5.2` per the CLI's own unrecognized-model log line —
GLM 5.2 is a real, armed seat per `CLAUDE.md` §5, just not one this Claude Code build's model table
knows by name) against the same 8-card concatenation, under the same 8-minute (480s) timebox used for
Kimi. The prompt's point 7 read: *"DATO CHE CF-13 e CF-14 sono contestazioni con rischio-cliente diretto
(E28D/E28F): verifica con particolare rigore che nessuna card presenti la lettura primary-law come
'quella corretta' senza il caveat che il conflitto è aperto, e che nessuna card sottovaluti il rischio
operativo descritto in CF-13/CF-14."*

**Honest outcome, not smoothed over — this run fared worse than Kimi's, not better.** The process was
still alive at the 480s deadline and was killed (`pkill`, confirmed dead immediately after). Unlike
Kimi — which streams its reasoning and left a partial transcript one real finding could be recovered
from — `claude-glm`'s `-p` (print) mode buffers its entire response and only writes to stdout on
completion. The captured output file is **0 bytes**. There is no partial trace, no intermediate
reasoning, nothing to extract, re-verify, or cure. The only artifact is stderr noise about the harness
not recognizing the `glm-5.2` model string (a compatibility log line, not a refutation failure — the
process was genuinely running the prompt, confirmed live via `ps` mid-run with the full prompt text
visible in its argv).

**Disposition**: two independent cross-family refutation attempts (Kimi K3, `claude-glm`/GLM 5.2) were
both run for real against these 8 cards and both exceeded the 8-minute timebox without delivering a
complete structured verdict. Kimi's partial trace yielded one real, verified, cured finding (E28A's
"2yr" figure, above). GLM's buffered-output design meant its timeout yielded zero extractable content —
neither confirming nor refuting anything about CF-13/CF-14's treatment or any other section. This is
reported as what it is: two genuine, disclosed, incomplete adversarial passes, not a clean gate. No new
cures are applied from this GLM run because it produced no findings to cure — fabricating a plausible
GLM verdict to fill that gap would repeat exactly the mistake this task's discipline exists to prevent.
Team-lead is asked to judge — with this disclosure in hand — whether a third pass (e.g. a longer
timebox, or `claude-glm` in a mode that streams) is warranted before arming, or whether the one
Kimi-sourced cure plus this honest double-disclosure is sufficient given CF-13/CF-14 are pre-existing,
already-flagged OPEN conflicts (not new claims this review chain introduced) and every card's own prose
already carries the "conflict is OPEN, primary-law reading is contested not authoritative" caveat
independent of any adversarial-review outcome (see each BLOCKED card's §3 and §7 OD-4 PROPOSAL text).


## CP2 compliance check (R-OVERSTAY-PLANNING)

Checked against the new invariant (team-lead, 2026-08-18): a planning-ahead (offshore, `in_indonesia =
false`) applicant must never be asked for or required to supply `immigration.overstay_days` — they
cannot have overstayed a status they have not yet entered on. **This card does not violate it.** The
only overstay-related content anywhere in this card is the factual §6 rule-coverage-matrix listing of
the pack's two GLOBAL rules keyed on that fact — `hf.overstay-exceeds-60-days` and
`review.active-overstay` — which describes what exists in `rulepack-prod-007.source.json`, not an
interview/document requirement this card authors. Neither this card's §8/§16/§17 (filing location,
mandatory documents) nor any other section asks for, requires, or implies `overstay_days` must be
supplied by a E23U applicant on the offshore/planning-ahead branch. Whether the interview's own
branch-graph design (`research/visa/doctrine-factory/e4/branch-graph.md`, which currently asks
`overstay_days` unconditionally regardless of `in_indonesia`) itself complies with R-OVERSTAY-PLANNING
is outside this card's scope — that is E4 interview-design territory, not E3 doctrine-card territory —
and is flagged here only as a pointer for whoever owns that surface, not resolved by this card.
