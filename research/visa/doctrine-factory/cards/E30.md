---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "CL-E30-01 (batch-1) — validity tiers 1/2/4 years; CL-E30-02 UNVERIFIED sources-gap on Izin Belajar authority"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md (fetched @ branch agent/air-m5/ops/e2b-batch2b)
    note: "CL-E30-01 (batch-2b EXTENSION) — E30 family index structure, VERIFIED-WITH-CAVEAT, distinct content reusing the same claim_id as batch-1's CL-E30-01"
  - path: research/visa/doctrine-factory/claims/e2b-batch3-claim-ledger.md (fetched @ branch agent/air-m5/ops/e2b-batch3)
    note: "closure verdict — E30 MET"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the 3 el.e30-* rules scoped to E30 alone, re-verified live this session"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E30 (Student KITAS — Generic/Parent Index)

Task: E3 bulk (LONG-STAY family). E30 is the generic parent index of the Student KITAS family
(subdivided into E30A/E30B/E30E/E30F) — a different domain from the E31/E33 family lines this batch
otherwise covers, included per the task brief's "E30" code prefix.

**GOTCHA flagged, not smoothed:** two DIFFERENT ledger entries both use the claim_id `CL-E30-01` for
different content (batch-1's validity-tier claim vs batch-2b's family-index-structure claim). This
card cites both explicitly by batch/date rather than silently picking one, since the `claims_digest`
mechanism assumes claim_ids are unique — this collision is itself a claim-ledger hygiene gap, flagged
in §7.

## 1. Identity

- **Code**: E30
- **Name**: Student KITAS — generic parent index (*"Mengikuti pendidikan"*).
- **Category**: Study-purpose, the umbrella index above E30A (basic/secondary)/E30B (higher-ed)/
  E30E (KEK-zone institution)/E30F (student exchange).

## 2. `claims_digest`

```
CL-E30-01=VERIFIED-WITH-CAVEAT
```
```
claims_digest: 9ff54e151033a99d749da9aa34cef6fc2abfe6211fe13e01cf9e09055caeb06a
```

This digest uses the batch-2b EXTENSION's `CL-E30-01` (family-index-structure claim) as the operative
pair for this card's coverage, since that is the claim this card's §3.1 relies on; batch-1's
same-named claim (validity tiers) is cited separately in §3.2 without contributing a second pair to
the digest (the recipe is per-claim_id, and a duplicate id cannot honestly contribute two states to
one digest — see §7 gap).

## 3. Doctrinal facts (claim-referenced)

1. **Family index structure.** `CL-E30-01` (batch-2b EXTENSION), **VERIFIED-WITH-CAVEAT** —
   `Kepmen M.IP-08.GR.01.01/2025` subdivides the Student KITAS family into E30A (primary/secondary
   education), E30B (higher education — diploma/sarjana/master/doktor), E30E (KEK-zone institution),
   E30F (student exchange); E30 itself is the generic parent index. Caveat: the same EXTENSION's own
   adversarial pass flagged an UNRESOLVED cross-document discrepancy about what E30A specifically
   covers (a different section describes it in passing as "academic research") — not smoothed over
   here, see `E30A.md` §3 for the full treatment.
2. **Validity tiers.** `CL-E30-01` (batch-1, cited by content not claim_id — see the collision note
   above), **VERIFIED** in its own right — 1/2/4-year validity tiers map to E30/E30A/E30B, with E30B
   running longer for multi-year higher-education programs. Source pinpoint: `Kepmen
   M.IP-08.GR.01.01/2025 — Klasifikasi Visa (110-tipe, include C5A)`, Passage Index 10/454.
3. **Izin Belajar authority gap.** `CL-E30-02`, **UNVERIFIED (explicit sources-gap, self-flagged)** —
   which body (Kemenimipas vs Kemendikbud vs the school itself) currently issues the student
   learning-permit endorsement post-Kemenimipas/Kemenkumham split is NOT resolved by any source this
   task consulted. Carried forward honestly, not inferred.
4. **Eligibility mechanics (live pack).** `study.admission_confirmed = true` AND
   `study.sponsor_confirmed = true` — both required facts, consistent with §3.1's category identity.

## 4. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E30 has 3 PRODUCTS-scoped rules, all ELIGIBILITY-stage.

| rule_id | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|
| `el.e30-student-support` | `PURPOSE_PRODUCT_MATCH` | `CL-E30-01` (batch-2b) | VERIFIED-WITH-CAVEAT | SUPPORTED |
| `el.e30-living-cost-2000.e30` | `LIVING_COST_USD2000` | `CL-E30-01` (framing only — no dedicated claim on the USD 2000 figure) | VERIFIED-WITH-CAVEAT (framing) | SUPPORTED |
| `el.e30-passport-validity.e30` | `PASSPORT_VALIDITY_6_MONTHS_REQUIRED` | `CL-E30-01` (framing only) | VERIFIED-WITH-CAVEAT (framing) | SUPPORTED |

**Note**: this product-code (`el.e30-student-support`) is SHARED across E30/E30A/E30B (same
`product_version_ids` list includes all three) — a single rule, three products; the
`el.e30-living-cost-2000.*`/`el.e30-passport-validity.*` reason_codes are per-product suffixed but
functionally identical predicates for each.

## 5. Open conflicts

None new. `CL-E30-02`'s Izin Belajar authority gap (§3.3) is an UNVERIFIED sources-gap, not a
CONFLICTING claim — not re-opened here.

## 6. Disposition

**REACHABLE_AND_SUPPORTED — coverage gate MET** per `coverage-matrix-after-batch3.json`
(`ALL_TOPICS_ANSWERED`). Claim-backed at VERIFIED-WITH-CAVEAT for category identity; the living-cost
and passport-validity rules ride on the same category claim without a dedicated sub-claim of their
own, which is thin but consistent with how E31G/E31H's single-claim coverage was already accepted
elsewhere in this batch.

## 7. Claim gaps discovered (input for next E2 batch)

1. **Claim-ledger hygiene**: `CL-E30-01` is used for two different claims across batch-1 and
   batch-2b — a real collision, not a duplicate restatement. Recommend renaming one on the next E2
   pass (e.g. `CL-E30-VALIDITY-01` for the batch-1 tier claim) so the `claims_digest` mechanism does
   not silently conflate them in a future card.
2. Izin Belajar issuing-authority question (§3.3) — genuinely unresolved, not this task's to answer
   (no NB-2 query budget spent on a question the existing ledger already flags as a sources-gap).
3. The living-cost USD 2000 and passport-validity-6-months figures have no dedicated primary-law
   pinpoint of their own beyond the family-level category claim.

## Adversarial review

Real cross-family review run this session via `kimi -m kimi-code/k3`, batched with E30A/E30B/E30E/
E30F (generator≠grader, live-verified against the pack — 104 rules, 98 PRODUCTS-scoped — and both
ledger sources, not trusting this card's prose).

Findings and disposition:

1. **[not contested]** This card's digest, the `CL-E30-01` collision handling (frontmatter, intro
   "GOTCHA flagged", §2's digest-provenance explanation, §3.1/§3.2's dual citation, §7.1's rename
   recommendation), and the §4 rule matrix (3 rules, exact ids/reason_codes) were all independently
   re-verified live and confirmed accurate — the strongest-verified card of the batch.
2. **[P3, noted, not cured]** §7.3 already flags that the living-cost/passport-validity reason_codes
   assert figures (USD 2000, 6 months) the rules' `when` predicates don't actually test — Kimi
   confirmed this independently and flagged it as worth stating explicitly rather than only implying
   via "framing only." Left as-is since §7.3 already captures the substance; no card text changed.

Net: 0 factual errors found in this card specifically (the two real errors from this batched pass
landed in `E30A.md` and `E30B.md` — see those cards' own Adversarial review sections). Disposition
unchanged.
