---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "cross-cutting claim provenance (CL-CROSS-01/02/05/06/07/08) for T3/T7/T9/T10/T15"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md
    note: "CL-C1C6-01 (T1, thin) and CL-XCUT-T5-01/T5-02 (T5 entry pattern + C-series extension mechanics) provenance"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "C1's required_claim_topics (T1,T10,T15,T3,T5,T7,T9), ALL_TOPICS_ANSWERED"
  - path: research/visa/doctrine-factory/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the el.c1.tourism-family rule this card's coverage matrix targets, plus the product's own structured facts"
adversarial_review: kimi-k3
---

# Product Doctrine Card — C1 (Tourist Visit Visa)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), VISIT/ENTRY family. Input: the merged
e2b-batch1/batch2/batch2b claim ledgers. Format follows `cards/D1.md`'s PDC-1 field set. No legal fact
without a `claim_id` pointer; a PDC-1 field with no claim behind it is marked **GAP**, not silently
filled.

## 1. Identity

- **Code**: C1 (legacy code: B211A)
- **Name**: Tourist Visit Visa (*Visa Kunjungan Wisata*)
- **Category**: Kunjungan (visit) index, single-entry, tourism/family-purpose (Kepmen
  M.IP-08.GR.01.01/2025 classification table).

## 2. `claims_digest`

Recipe: sort `(claim_id, state)` pairs ascending by `claim_id`, join `claim_id=state`
newline-separated, sha256 the UTF-8 bytes.

```
claims_digest: d0c158e1fc8de09e115b6dedef0f0d310c4b76defe29a75d1cd9f15b6a5f9016
```

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

1. **Category & legal purpose.** C1's regulatory-hierarchy classification (`UU 6/2011` as amended by
   `UU 63/2024`/`PP 45/2024`; `Permenkumham 22/2023`/`11/2024`; `Kepmen M.IP-08.GR.01.01/2025`;
   `Permenimipas 5/2025`) was resolved cleanly, but the same answer's product-specific
   category/purpose/activity content was TRUNCATED before delivery ("cut off mid-table"). — `CL-C1C6-01`,
   **VERIFIED-WITH-CAVEAT (thin)** — recorded honestly as coverage-matrix-closing but thin, not a
   complete doctrine card, per the ledger's own disposition. The pack's own structured `covered_purposes`
   for C1 — `TOURISM`, `FAMILY` — is the authoritative decision-relevant fact set (see §6).
2. **Permitted activities.** Same purpose set as (1): tourism, family visits. — `CL-C1C6-01`,
   **VERIFIED-WITH-CAVEAT (thin)**.
3. **Prohibited activities.** Local paid employment, business meetings, journalism — pack's own
   structured `prohibited_activities` field; no dedicated doctrinal-prose claim states this list for C1
   specifically (the thin `CL-C1C6-01` answer was truncated before reaching this detail). **GAP**.
4. **Borderline/unresolved activities.** **GAP** — no C1-specific borderline-activity claim.
5. **Single or multiple entry.** Single; any exit permanently voids the stay permit. —
   `CL-XCUT-T5-01`, **VERIFIED** (C1 named explicitly, alongside A1/B1/B2-4/C2/C7/C8/C12/C18/C22/D1/
   D2/D12).
6. **Duration per entry & total validity.** 60-day initial stay, up to 2 extensions of 60 days each
   (max 180 days total); filing window opens 14 days before expiry; a timely-filed-and-paid extension
   application suspends overstay accrual even if approval lands after the prior permit's expiry; new
   period starts the day after the prior permit's expiry. — `CL-XCUT-T5-02`, **VERIFIED** (C1 named
   explicitly, this claim's query scope was specifically C1/C2/C6). Matches the pack's own structured
   `stay_policy` (60/60 fixed days) and `extension_policy` (allowed, max 2 extensions, 60 days each) for
   C1's `product_version_id` — the doctrinal claim and the pack-structural fact agree exactly, no
   discrepancy found.
7. **Extensions & conversions.** Extension mechanics per (6). Onshore-conversion eligibility: C1
   *appears* named (alongside C18, D2, D12, B211A) in the operational conversion manual's list of visit
   visas eligible for onshore conversion to ITAS, per `CF-16`'s own text — but **this reading is weaker
   than it first looks, per the real Kimi K3 adversarial pass (see Adversarial review below)**: the
   manual's list item is the LEGACY code `B211A`, and `B211A` is the shared legacy code of THREE current
   products in this slice — C1, C2, and C6 (verified directly against
   `rulepack-prod-007.source.json`'s own `legacy_codes` fields: C1→`["B211A"]`,
   C2→`["B211B","B211A"]`, C6→`["B211A"]`). A bare `B211A` mention in a pre-reform manual cannot, by
   itself, disambiguate which of the three current products it refers to — it may mean C1, C2, C6, or
   all three under the old undifferentiated single-entry visit-visa code. This card therefore does
   **NOT** treat C1's presence in that list as confirmed — it is a **GAP**, not a downgraded-but-real
   fact, and the ambiguity is symmetric with C2's own `CF-16` framing (see `C2.md` §5).
8. **Filing location (onshore/offshore) & onshore-status prerequisites.** Extensions per (6) are filed
   onshore (implied by the 14-day-before-expiry filing window and overstay-suspension mechanics, both
   of which presuppose the applicant is already in Indonesia) — `CL-XCUT-T5-02`, **VERIFIED**, though
   the claim's own text does not use the word "onshore" explicitly; read as directly implied, not
   inferred beyond the claim's content.
9. **Nationality restrictions incl. calling-visa applicability.** **GAP** — C1 carries no
   nationality-gating rule in seq-7 (unlike A1/B1's BVK/VoA lists); `CL-CROSS-06`'s T9 claim
   (**UNVERIFIED**-with-caveat, Products: ALL "visit-visa family especially") is topically present but
   has no C1-specific applicability since C1 has no nationality-list rule to back. Calling-visa (GLOBAL)
   out of scope, same as every card in this slice.
10. **Age limits.** Not applicable at the C1-specific rule level.
11. **Sponsor/guarantor requirements.** C1's own `sponsor_types` is `["NONE", "INDIVIDUAL",
    "EMPLOYER"]` — broader than A1/B1's `NONE`-only, and notably C1's own `el.c1.tourism-family` rule
    does NOT itself gate on `family.sponsor_confirmed` (unlike C2/C6's rules, which do — see C2.md/
    C6.md §6). — `CL-CROSS-05`, **VERIFIED** (Products: ALL) covers the general sponsor framework; the
    specific fact that C1's own eligibility rule does not require sponsor confirmation is a
    pack-structural observation, not itself claim-verified.
12. **Financial requirements & proof.** **GAP** — no C1-specific financial-proof claim or pack rule.
13. **Permitted source of income/compensation.** Same cross-cutting test as the rest of this slice. —
    `CL-CROSS-01`, **VERIFIED** (Products: ALL) + `CL-CROSS-02`, **VERIFIED** (Products: ALL).
14. **Family/dependent provisions.** C1 covers family visits as one of its own two purpose values
    (`TOURISM`, `FAMILY`) — the most directly family-relevant of this slice's 6 products at the
    pack-structural level. `CL-CROSS-08` (**VERIFIED**, Products: "ALL family-line products (E28, E31,
    E33)") does not literally name C1 — same documentation-completeness gap flagged across this slice.
15. **Investment requirements.** Not applicable to C1.
16. **Mandatory documents.** **GAP** — no C1-specific document-bundle claim; `CL-C1C6-01`'s truncation
    (§3.1) means this content was never delivered.
17. **Declarative vs. documentary-proof requirements.** **GAP** — no claim classifies this for C1.

## 4. Full-card query note

C1's T1 topic is answered by `E2B2-T1-C1C6`, a paired C1/C6 query whose response was cut off mid-table
before delivering either product's category/purpose/activity content in detail — the ledger's own
disposition is explicit: "coverage-matrix-closing but THIN, not a complete doctrine card." The
regulatory-hierarchy portion resolved cleanly (citation-audit `VERIFIED`); the product-specific
substance did not. This card treats every §3 item sourced only to `CL-C1C6-01` as thin-by-construction,
not as a full doctrine answer — consistent with how D1.md treats its own timeout-affected fields.

## 5. Open conflicts

No CF entry is formally opened against C1 in either conflict report. `CF-16` (C2's onshore-conversion
dispute) mentions the legacy code `B211A`, which C1 also carries — **this card does not treat that as a
confirmed positive fact about C1** (see §3.7's corrected reading, post-Kimi-review) precisely because
`B211A` is ambiguous across C1/C2/C6. Not escalated to a new numbered CF here (that is C2's card's own
conflict, not a new one about C1), but the honest reading is "unresolved, not undisputed."

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

C1 has 1 rule: 1 ELIGIBILITY (purpose/duration match). No HARD_FILTER rule specific to C1 in seq-7
(unlike A1/B1's nationality gates).

| rule_id | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|
| `el.c1.tourism-family` | `C1_VISIT_ELIGIBLE` | `CL-C1C6-01`, `CL-XCUT-T5-01`, `CL-XCUT-T5-02` | VERIFIED-WITH-CAVEAT (thin) / VERIFIED / VERIFIED | SUPPORTED |

T3/T7/T9/T10/T15 topic-level backing (required per coverage matrix, no C1-specific rule to attach to):
`CL-CROSS-01`/`CL-CROSS-02` (T3, VERIFIED), `CL-CROSS-05` (T7, VERIFIED), `CL-CROSS-06` (T9,
UNVERIFIED-caveated, weak applicability since C1 has no nationality rule), `CL-CROSS-08` (T10,
VERIFIED, scoping gap per §3.14), `CL-CROSS-07` (T15, VERIFIED).

**Rules WITHOUT backing in this ledger**: none — C1's single rule has a claim (albeit thin) behind
every one of its decision-relevant facts (`intent.purposes`, `intent.stay_days`).

## 7. Disposition

**REACHABLE_AND_SUPPORTED-candidate.** C1's 1 current rule has every required claim at state VERIFIED
or VERIFIED-WITH-CAVEAT (thin, but not CONFLICTING/STALE). Matches the batch-3 closure verdict
(`coverage-matrix-after-batch3.json` `batch3_closure` table, row "C1 — MET — all required topics
VERIFIED-audited, no manual-check flag"). The GAP items in §3 (prohibited-activity prose, extension/
onshore-conversion prose, document bundle) do not block the slice gate — they are E4/E5
fact-vocabulary candidates, per the same reasoning applied throughout this slice.

`claims_digest` pairs (sorted, `claim_id=state`):

```
CL-C1C6-01=VERIFIED-WITH-CAVEAT
CL-CROSS-01=VERIFIED
CL-CROSS-02=VERIFIED
CL-CROSS-05=VERIFIED
CL-CROSS-06=UNVERIFIED
CL-CROSS-07=VERIFIED
CL-CROSS-08=VERIFIED
CL-XCUT-T5-01=VERIFIED
CL-XCUT-T5-02=VERIFIED
```

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. A dedicated, non-truncated C1/C6-split doctrine-card query (splitting the original combined
   `E2B2-T1-C1C6` ask, mirroring how C2's own T1 was eventually closed with two narrower queries in
   batch-3) — highest-value candidate, since C1's own §3 items 3/16/17 are all downstream of this one
   truncation.
2. C1's onshore-conversion-to-ITAS eligibility in a dedicated claim — currently NOT even a reliable
   incidental mention, since the only textual anchor (`B211A` in `CF-16`'s conversion manual) is
   ambiguous across C1/C2/C6's shared legacy code (see §3.7/§5). Resolving this requires either a
   primary-source query keyed on the CURRENT product codes, or archival research into what `B211A`
   specifically denoted before the 2025 reclassification.
3. `CL-CROSS-08`'s Products scope widened for T10, same recommendation as every other card in this
   slice.

## Adversarial review

**Real run** (not simulated): `kimi -p "<full 6-card combined text + refutation instructions>" -m
kimi-code/k3`, one invocation across all 6 cards in this slice. Ran past the 8-minute timebox producing
a substantial partial transcript, killed per this task's own documented precedent — see A1.md's
Adversarial Review section for the full method note. Raw transcript:
`/tmp/e3refs/kimi-review-output.txt` (session-local). Findings below are the ones concretely affecting
C1, re-verified independently against the source ledgers and `rulepack-prod-007.source.json`:

1. **[P1, CONFIRMED, cured — the strongest finding in this entire review round]** §3.7's original draft
   treated C1's presence in `CF-16`'s conversion-eligible list (via the legacy code `B211A`) as an
   "incidental positive mention," essentially treating C1 as the settled, undisputed contrast to C2's
   disputed conversion status. Kimi caught the actual defect directly: `B211A` is C1's, C2's, AND C6's
   shared legacy code (verified directly against `rulepack-prod-007.source.json`), so a bare mention of
   `B211A` in a pre-reform operational manual cannot disambiguate which current product it names — it
   could equally support (or contradict) C2's `CF-16` claim that the SAME manual "omits C2 entirely."
   §3.7 and §5 both rewritten to state this honestly as a genuine ambiguity, not a confirmed fact for
   C1 nor a settled contrast for C2's conflict.
2. **[P2, CONFIRMED, cured]** §6's table originally listed only `CL-C1C6-01` for the single rule,
   omitting `CL-XCUT-T5-01`/`T5-02` even though both are required-topic backing (T5) that the rule's
   own gating facts (`intent.stay_days`) directly depend on for consistency-checking against the pack's
   structured duration figures. Added both.
3. **[P2, NOT-AN-ISSUE]** Reviewer asked whether C1's broader `sponsor_types` list (vs A1/B1's
   `NONE`-only) should be read as C1 requiring a sponsor — checked directly against
   `el.c1.tourism-family`'s own `when` clause: it does NOT gate on `family.sponsor_confirmed` (unlike
   C2/C6's rules), so `sponsor_types` being non-empty is a catalog-metadata field, not evidence the
   rule requires one. No change — already correctly stated in §3.11.
4. **[P2, NOT-AN-ISSUE]** `VERIFIED-WITH-CAVEAT` flagged as outside the base 5-state vocabulary — same
   disposition as A1.md's finding #6 (established, ledger-defined qualifier, not a defect).

Net: 2/4 raised findings were real and are cured above (finding #1 is the highest-value catch across
this entire 6-card review round); 2/4 were reviewed and confirmed not applicable.
