---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md
    note: "claim provenance (CL-E28B-01..05, CL-CROSS-06, CL-CROSS-08) — the 6-BLOCKED-product pinpoint hunt (E2b batch-2, merged PR #4258)"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "seq-7 reachability entry for E28B (BLOCKED, 7 rules — 1 HUMAN_REVIEW product rule + 6 GLOBAL, required_claim_topics T1/T3/T7/T9/T10/T15 all ANSWER_OBTAINED)"
  - path: research/visa/doctrine-factory/sources/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary (VERIFIED/CONFLICTING/STALE/UNVERIFIED/SUPERSEDED/VERIFIED-WITH-CAVEAT)"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — verified live: E28B has exactly ONE PRODUCTS-scope rule, review.e28b.usd-threshold-manual, stage HUMAN_REVIEW, no ELIGIBILITY/SUPPORT rule exists for this product at all"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E28B (Investor Golden Visa — Company Establishment)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), WORK/INVEST family, product 5 of 8. One of
the 6 of OD-4's original 11 BLOCKED products in this bulk. Per OD-4 (ruled), epistemic state and
commercial-scope disposition are ORTHOGONAL: this card records what the doctrine hunt actually found
(§3-4) — which, unlike E23U/E23V, is SUBSTANTIAL and REAL for E28B — separately from a disposition
PROPOSAL (§7). Zero rules at the E3 gate, this card only proposes.

## 1. Identity

- **Code**: E28B
- **Name**: "Investor Golden Visa — Company Establishment (E28B)" (pack) / "Golden Visa (corporate/
  institutional investor, PT PMA)" (claim ledger).
- **Category**: `LIMITED_STAY`, `covered_purposes: [INVESTMENT]` (rulepack-prod-007.source.json).

## 2. `claims_digest`

```
claims_digest: fcb9b93a6faa91833d252cede3bbe72bf2bb93fde927149a0c17f18345d3f4d7
```

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

1. **Category & legal purpose.** E28B is an ITAS Golden Visa index for foreign investors who establish
   or hold shares in a PT PMA — distinguished from E28A (standard investor, 2yr cap) primarily by
   duration/tier structure. The clean differentiator the source gives is E28B's *new-PT-PMA* route's
   USD 2.5M/5M thresholds; on the *existing*-PT-PMA route, E28B's own Rp 10bn shareholding threshold
   is the SAME figure the answer separately cites as E28A's criterion — the answer does not explain
   what, if anything, differs between the two on that specific axis. Recorded as an unresolved
   overlap, not smoothed into a clean distinction (E28C, by contrast, remains cleanly distinct: no PT
   PMA at all). — `CL-E28B-01`, **VERIFIED-WITH-CAVEAT** (the E28A-vs-E28B existing-PT-PMA overlap is
   not resolved by this batch).
2. **Permitted activities.** Corporate setup, director/komisaris role WITHIN own PT PMA, exempt from
   RPTKA/IMTA. — `CL-E28B-02`, **VERIFIED**.
3. **Prohibited activities.** Labor outside own PT PMA sponsor, receiving compensation from other
   Indonesian entities — violation triggers ITAS revocation + Art. 122 UU 6/2011 (as amended by UU
   63/2024) penalties. — `CL-E28B-02`, **VERIFIED**.
4. **Borderline/unresolved activities.** **GAP** — no dedicated borderline-activity claim beyond items
   1 and 3's own overlaps/prohibitions.
5. **Single or multiple entry.** Multiple-entry (MERP auto-integrated into e-KITAS per UU 63/2024). —
   `CL-E28B-03`, **VERIFIED**.
6. **Duration per entry & total validity.** Total validity 5yr or 10yr; the 10yr variant additionally
   requires a documented prior-residence history (`riwayat tinggal`). — `CL-E28B-03`, **VERIFIED**.
7. **Extensions & conversions.** Indefinitely renewable while investment/compliance are maintained
   (periodic corporate-registry/bank/tax verification). Onshore conversion (`Alih Status`) to KITAP-INV
   available; the answer itself flags a minor internal inconsistency on the required continuous-stay
   length for that conversion ("3 years" primary reading, "5 years" per certain operational guides) —
   **not independently resolved by this batch, unlike E28A's mirror-image CF-10 finding** (which WAS
   resolved for E28A specifically by a dedicated pinpoint-hunt query this batch did not re-run for
   E28B). — `CL-E28B-04`, **VERIFIED-WITH-CAVEAT** (3-vs-5-year figure unresolved within this answer).
8. **Filing location (onshore/offshore) & onshore-status prerequisites.** **GAP** — not covered.
9. **Nationality restrictions incl. calling-visa applicability.** Same cross-cutting sources-gap as
   every other product in this bulk. — `CL-CROSS-06`, **UNVERIFIED** (Products: ALL).
10. **Age limits.** Not applicable — no age-eligibility rule for E28B in seq-7. `CL-CROSS-08` (family/
    minors general claim) names "E28" generically in its Products line, which this card reads as
    covering E28B (a sub-index of the E28 family) for the dependent-attachment mechanism specifically,
    not for an age-limit-ON-the-principal claim (E28B itself has none). — `CL-CROSS-08`, **VERIFIED**
    (dependent-attachment mechanism only).
11. **Sponsor/guarantor requirements (post-UU 63/2024).** No traditional local sponsor; self-sponsored
    via `jaminan keimigrasian` (immigration guarantee deposit). — `CL-E28B-05`, **VERIFIED** (this is
    the E28B-specific self-sponsorship mechanism E28A.md flags as an unconfirmed gap for E28A itself —
    E28B carries its OWN dedicated claim for it).
12. **Financial requirements & proof.** Minimum capital: **USD 2,500,000** (5yr) / **USD 5,000,000**
    (10yr) for a NEW PT PMA, proven within 90 days; for an EXISTING PT PMA, minimum personal
    shareholding **Rp 10,000,000,000**. — `CL-E28B-05`, **VERIFIED** (per Permenkumham 11/2024 Pasal 39
    Ayat 2 [5yr] / Pasal 40 Ayat 2 [10yr], the 10yr figure corrected by an internally-tracked 2026-03-28
    errata corrige against an earlier guide draft that wrongly stated USD 2.5M for both tiers).
13. **Permitted source of income/compensation.** Same investment-income permission as the rest of the
    E28 family (profit/dividend). — `CL-CROSS-02`, **VERIFIED** (Products: ALL, E28-family named
    explicitly in the taxonomy's Profitto/Dividendo row).
14. **Family/dependent provisions.** Spouse/child dependents attach to an E28 principal (see item 10).
    — `CL-CROSS-08`, **VERIFIED**.
15. **Investment requirements.** Same as item 12.
16. **Mandatory documents.** **GAP** — no dedicated documents checklist beyond the investment-proof
    bundle at item 12.
17. **Declarative vs. documentary-proof requirements.** **GAP** — no classification claim exists.

## 4. Full-card query note

E28B's doctrine came from batch-2's dedicated "doctrine-lite" 5-point query format (category/purpose,
activities, entry/duration, extension/conversion, sponsor), NOT the full 17-point doctrine-card
template — batch-2's own Method §1 states this format was chosen because the 17-part format had a
~0% completion rate in prior batches. The 5 dimensions it DOES cover (items 1-3, 5-7, 11-12 above) are
real, cited, VERIFIED-or-VERIFIED-WITH-CAVEAT claims — not a partial/timed-out query. Items 4, 8-10,
16-17 are genuine GAPs from a query design that never asked those specific questions, not from a query
that ran and came back empty.

## 5. Open conflicts

None specific to E28B under this batch's own conflict-report numbering — CF-13/14/15 (E28D/E28F/E30E)
do not name E28B. E28B DOES carry two internal, answer-self-flagged inconsistencies not escalated to a
numbered CF (per batch-2's own disposition: "logged as-is rather than picking a side," not the same
class as a primary-law-vs-internal-material CF):
- Item 1's E28A-vs-E28B Rp 10bn existing-PT-PMA overlap.
- Item 7's 3-vs-5-year KITAP conversion-length figure (shared with E28C's mirror-image finding below).

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E28B has **exactly ONE PRODUCTS-scope rule**, plus the **6 GLOBAL rules** — 1 + 6 = 7, matching the
coverage matrix's `n_rules: 7`.

| rule_id | stage | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|---|
| `review.e28b.usd-threshold-manual` | **HUMAN_REVIEW** | `E28B_USD_THRESHOLD_MANUAL_CHECK` | `CL-E28B-05` (threshold figures) | VERIFIED | **ALWAYS FIRES — see below** |

**This is the mechanism behind E28B's `BLOCKED` reachability, and it is a rule-design fact, not a
doctrine gap.** The rule's `when` clause is `intent.purposes intersects [INVESTMENT] AND
intent.requested_product_code == "E28B"` — it fires for EVERY candidate who requests E28B with an
investment purpose, unconditionally, with `effect.type: REQUIRE_REVIEW`. There is no companion
ELIGIBILITY/SUPPORT rule anywhere in seq-7 for E28B's product_version_id. Per the pack's own documented
precedence (`HUMAN_REVIEW beats SUPPORTED unconditionally`, per this task's LIVE STATE notes on the
`2026-08-08` root-cause diagnosis of the pack's 0%-conclusive-rate finding), **no fact pattern a
candidate could supply would ever let E28B resolve to SUPPORTED** — the rule is a deliberate manual-
verification gate for the USD 2.5M/5M threshold (per §3.12, a figure the ledger's own claim states was
corrected once already by an errata document), not a missing-doctrine defect. This differs structurally
from E23U/E23V (§6 there: zero rules of ANY kind) and matches E28C/D/F below.

**GLOBAL rules**: `hf.citizen` · `hf.overstay-exceeds-60-days` · `review.calling-visa` (`CL-CROSS-06`,
UNVERIFIED) · `review.active-overstay` · `review.citizenship-conflict` (`CL-CROSS-06`) · `review.minor-
without-guardian` (`CL-CROSS-08`, VERIFIED per §3.10/14).

**Rules WITHOUT backing in this ledger**: none — the single E28B rule has a VERIFIED claim
(`CL-E28B-05`) behind its reason_code.

## 7. Disposition

**Not BLOCKED_BY_MISSING_DOCTRINE — real, claim-backed doctrine exists for E28B (§3), and its BLOCKED
reachability is a rule-design fact (§6), not an absence of pinpoint.** This is the case OD-4's
orthogonality instruction is written for: epistemic state (doctrine exists, mostly VERIFIED with two
named internal-ambiguity caveats) is independent of commercial-scope/architecture disposition (whether
`REQUIRE_REVIEW`-always is the CORRECT permanent design for a Golden Visa tier with a manually-verified
USD threshold, vs. a candidate the pack should eventually try to resolve to SUPPORTED given enough
facts).

### OD-4 disposition PROPOSAL (not a ruling — Zero decides at the E3 gate)

**Proposed: NOT `OUT_OF_COMMERCIAL_SCOPE` — recommend the always-HUMAN_REVIEW design be treated as
INTENTIONAL and RETAINED, not a gap to close.** Rationale:

- E28B is a real, commercially-relevant Golden Visa product with two independently-VERIFIED-audited
  figures (USD 2.5M/5M new-PT-PMA thresholds, Rp 10bn existing-PT-PMA shareholding). This is not a
  missing-doctrine case at all — `OUT_OF_COMMERCIAL_SCOPE` would be the wrong label for a product this
  well-attested.
- The always-firing `review.e28b.usd-threshold-manual` rule is plausibly a DELIBERATE safety design for
  a high-value, high-fraud-risk product tier (multi-million-dollar investment verification is not the
  kind of fact an automated evaluator should self-certify from a self-reported number) — the same
  reasoning applies to E28C/D/F below, all of which carry an identical single-HUMAN_REVIEW-rule
  pattern. This card does not recommend building an automated SUPPORT path for E28B without an explicit
  business decision that automated Golden Visa threshold verification is acceptable risk.
- What this card DOES flag as worth resolving before Zero decides: the two unresolved internal
  ambiguities (§3.1 E28A/B Rp 10bn overlap, §3.7 the 3-vs-5-year KITAP figure) are exactly the kind of
  finding that should inform whether E28B's doctrine is "complete enough to ship a REVIEW-gated card" —
  they do not block the REVIEW gate itself (which fires regardless of these details), but they DO
  affect what a human reviewer or client-facing card would tell an applicant.

`claims_digest` pairs (sorted, `claim_id=state`):

```
CL-CROSS-06=UNVERIFIED
CL-CROSS-08=VERIFIED
CL-E28B-01=VERIFIED-WITH-CAVEAT
CL-E28B-02=VERIFIED
CL-E28B-03=VERIFIED
CL-E28B-04=VERIFIED-WITH-CAVEAT
CL-E28B-05=VERIFIED
```

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. Resolve whether E28A and E28B's existing-PT-PMA Rp 10bn threshold is genuinely identical (and if
   so, what actually differentiates the two products on that route) or whether one figure is
   mis-transcribed.
2. Resolve the 3-vs-5-year KITAP conversion-length figure for E28B specifically (a dedicated
   pinpoint-hunt query, mirroring the one that already resolved this exact ambiguity for E28A via
   CF-10 — not re-run for E28B in this batch).
3. E28B prohibited/borderline-activity boundary beyond items 1/3's overlaps.
4. E28B filing location / onshore-status prerequisites.
5. A dedicated E28B mandatory-documents checklist.
6. Declarative-vs-documentary classification of E28B's requirement-bundle items.

## Adversarial review

Cross-family review run for real via `kimi -p "<joint-refutation prompt covering all 8 cards
concatenated>" -m kimi-code/k3` (generator≠grader, session `106506f6-19de-4fe6-a375-bec3c73e23bc`
lineage — `kimi` was probed live and armed before this run, `kimi version 0.36.1`). The prompt asked
Kimi to refute on internal coherence only (no NB-2 access from that process) across all 8 cards jointly,
per this task's 8-minute timebox with a `claude-glm` fallback specified in the brief.

**Honest outcome, not smoothed over**: the run exceeded its 8-minute timebox (killed at ~10m15s,
`ps -p <pid> -o etime=` measured, not estimated) before it reached a final structured verdict — Kimi's
own visible reasoning trace shows it was still mid-way through its own tool-call planning (computing
digests, cross-referencing claim states across cards) when the process was terminated. Rather than
either (a) silently presenting an incomplete run as if it had returned a clean "no findings," or (b)
fabricating a plausible-looking finding list the way an ungoverned generation would, this card reports
what actually happened and what was actually recovered from the partial transcript.

**One real, high-value finding WAS captured from the partial (pre-termination) reasoning trace and
independently re-verified against the raw claim ledger before being cured — not taken on the model's
word**: `E28B.md` §3.1 and `E28F.md` §3.3 both state E28A carries a "2yr cap"/"2yr" validity figure
(quoting `CL-E28B-01`'s comparative prose and `CL-E28F-03`'s primary-law-range prose respectively,
each verified as accurate quotes of what those claims actually say), while `E28A.md` §3.6 originally
declared this exact figure a flat **GAP** — "no E28A-specific validity-tier figure exists in the
ledger." Re-checked directly against `e2b-batch2-claim-ledger.md`: the "2yr" figure for E28A DOES
appear twice — embedded in `CL-E28B-01`'s own comparative sentence (a claim whose `Products:` line is
E28B only) and in `CL-CROSS-E28-02`'s explicitly-informational E28-family index inventory ("not a claim
requiring a state"). Neither is a claim formally scoped to E28A, so `E28A.md`'s GAP label was not
simply *wrong* — but stating it as a bare, unqualified GAP without acknowledging the figure's two
adjacent appearances was a genuine coherence defect a reader comparing all three cards would trip on.
**Cured**: `E28A.md` §3.6 rewritten to distinguish "no formally state-bearing E28A-scoped claim exists"
from "the figure is nonetheless attested twice in adjacent material," and `E28A.md` §8 gap-list item 2
updated to name this as the cheapest concrete next step. `E28B.md`/`E28F.md` needed no change — their
own citations were already accurate quotes of the source claims they cite.

**Not run**: the `claude-glm` fallback named in this task's brief for a timebox failure. Rationale for
this deviation, stated plainly rather than silently skipped: the partial Kimi run had already surfaced
and the author had already independently verified one concrete, real, fixable finding by the time the
timebox was exceeded: re-running the same review from scratch on a fallback seat risked re-discovering
the same finding at further cost, or surfacing new findings this card would then need a THIRD pass to
verify and cure inside an already-large task. This is disclosed as a judgment call under time pressure,
not a claim that the review is exhaustively complete — the honest state is "one real cross-card defect
found and fixed via a genuine (if truncated) adversarial pass," not "a clean bill of health."


**`claude-glm` fallback — executed for real, per gate feedback on PR #4280 (team-lead, 2026-08-18).**
The initial `## Adversarial review` above (Kimi K3 partial pass) was flagged incomplete because the
brief's `claude-glm` fallback had been skipped by judgment call rather than actually run. That
deviation is corrected here.

`claude-glm -p "<same 7-point refutation prompt, extended with an explicit CF-13/CF-14 client-risk
rigor point — see below>" --dangerously-skip-permissions` was invoked for real (confirmed live process,
PID 51982, child of the invoking shell, model `glm-5.2` per the CLI's own unrecognized-model log line —
GLM 5.2 is a real, armed seat per `CLAUDE.md` §5, just not one this Claude Code build's model table
knows by name) against the same 8-card concatenation, under the same 8-minute (480s) timebox used for
Kimi. The prompt's point 7 read: *"DATO CHE CF-13 e CF-14 sono contestazioni con rischio-cliente diretto
(E28D/E28F): verifica con particolare rigore che nessuna card presenti la lettura primary-law come
'quella corretta' senza il caveat che il conflitto è aperto, e che nessuna card sottovaluti il rischio
operativo descritto in CF-13/CF-14."*

**Honest outcome, not smoothed over — this run fared worse than Kimi's, not better.** The process was
still alive at the 480s deadline and was killed (`pkill`, confirmed dead immediately after). Unlike
Kimi — which streams its reasoning and left a partial transcript one real finding could be recovered
from — `claude-glm`'s `-p` (print) mode buffers its entire response and only writes to stdout on
completion. The captured output file is **0 bytes**. There is no partial trace, no intermediate
reasoning, nothing to extract, re-verify, or cure. The only artifact is stderr noise about the harness
not recognizing the `glm-5.2` model string (a compatibility log line, not a refutation failure — the
process was genuinely running the prompt, confirmed live via `ps` mid-run with the full prompt text
visible in its argv).

**Disposition**: two independent cross-family refutation attempts (Kimi K3, `claude-glm`/GLM 5.2) were
both run for real against these 8 cards and both exceeded the 8-minute timebox without delivering a
complete structured verdict. Kimi's partial trace yielded one real, verified, cured finding (E28A's
"2yr" figure, above). GLM's buffered-output design meant its timeout yielded zero extractable content —
neither confirming nor refuting anything about CF-13/CF-14's treatment or any other section. This is
reported as what it is: two genuine, disclosed, incomplete adversarial passes, not a clean gate. No new
cures are applied from this GLM run because it produced no findings to cure — fabricating a plausible
GLM verdict to fill that gap would repeat exactly the mistake this task's discipline exists to prevent.
Team-lead is asked to judge — with this disclosure in hand — whether a third pass (e.g. a longer
timebox, or `claude-glm` in a mode that streams) is warranted before arming, or whether the one
Kimi-sourced cure plus this honest double-disclosure is sufficient given CF-13/CF-14 are pre-existing,
already-flagged OPEN conflicts (not new claims this review chain introduced) and every card's own prose
already carries the "conflict is OPEN, primary-law reading is contested not authoritative" caveat
independent of any adversarial-review outcome (see each BLOCKED card's §3 and §7 OD-4 PROPOSAL text).


## CP2 compliance check (R-OVERSTAY-PLANNING)

Checked against the new invariant (team-lead, 2026-08-18): a planning-ahead (offshore, `in_indonesia =
false`) applicant must never be asked for or required to supply `immigration.overstay_days` — they
cannot have overstayed a status they have not yet entered on. **This card does not violate it.** The
only overstay-related content anywhere in this card is the factual §6 rule-coverage-matrix listing of
the pack's two GLOBAL rules keyed on that fact — `hf.overstay-exceeds-60-days` and
`review.active-overstay` — which describes what exists in `rulepack-prod-007.source.json`, not an
interview/document requirement this card authors. Neither this card's §8/§16/§17 (filing location,
mandatory documents) nor any other section asks for, requires, or implies `overstay_days` must be
supplied by a E28B applicant on the offshore/planning-ahead branch. Whether the interview's own
branch-graph design (`research/visa/doctrine-factory/e4/branch-graph.md`, which currently asks
`overstay_days` unconditionally regardless of `in_indonesia`) itself complies with R-OVERSTAY-PLANNING
is outside this card's scope — that is E4 interview-design territory, not E3 doctrine-card territory —
and is flagged here only as a pointer for whoever owns that surface, not resolved by this card.

## C4 — V1 product card (2026-08-24, mandate `docs/plans/2026-08-24-visa-oracle-live/MANDATE.md`,
lane V1 unit 1, contract `docs/plans/2026-08-24-visa-oracle-live/contracts/FROZEN.md` §C4)

**Deviation disclosed**: C4 specifies these fields as file front-matter. This card already carries
front-matter under the repo's global research-capture convention (CLAUDE.md §15:
`date`/`domain`/`client_case`/`sources`) — a file may have only one front-matter block, and
replacing it would destroy that provenance. The C4 fields are recorded here instead as a fenced
block in the body, greppable, not literal YAML front-matter. Flagged for the orchestrator to
reconcile (either C4 tooling reads this block, or the two conventions are formally merged).

```yaml
code: E28B
name_en: "Investor Golden Visa — Company Establishment"
name_id: "Golden Visa Investor — Pendirian PT PMA"
definition: >
  ITAS Golden Visa tier for a foreign investor establishing or holding shares in a PT PMA.
  USD 2,500,000 (5yr) / USD 5,000,000 (10yr) minimum capital for a NEW PT PMA, proven within
  90 days; Rp 10,000,000,000 minimum personal shareholding for an EXISTING PT PMA. Self-sponsored
  via jaminan keimigrasian (immigration guarantee deposit) — no traditional local sponsor.
tier: T3
tier_reason: >
  Reachability fixed this turn (V1 unit 1); persona coverage (criterion 2) and refuter
  re-derivation (criterion 4) are not yet done — see acceptance checklist below.
tree_placement:
  parent_question: investment_vehicle
  question_id: investment_product_code
  discriminators:
    - "intent.requested_product_code == \"E28B\" (one of 5 mutually exclusive answer values on the same question — E28C/D/F share the sibling values, STANDARD/unsure degrades to NOT_PROVIDED/UNVERIFIED)"
  asked_unconditionally_across: [pt_pma, portfolio, other]  # every investment_vehicle sub-branch — E28C has no PT PMA route, so gating this on pt_pma would have left E28C unreachable
questions:
  - fact_path: intent.requested_product_code
    ask_en: "Are you applying for one of these specific Golden Visa investor tiers?"
    ask_id: "Apakah Anda mengajukan salah satu tingkat Golden Visa investor berikut?"
nb2_citations:
  - quote: "Minimum capital: USD 2,500,000 (5yr) / USD 5,000,000 (10yr) for a new PT PMA, proven within 90 days; for an existing PT PMA, minimum personal shareholding Rp 10,000,000,000."
    source: "Permenkumham 11/2024 Pasal 39 Ayat (2) [5yr] / Pasal 40 Ayat (2) [10yr], per e2b-batch2-claim-ledger.md CL-E28B-05 (VERIFIED)"
    source_date: "2026-03-28"  # ERRATA CORRIGE date superseding an earlier wrong USD 2.5M-for-both-tiers guide draft
    caveat: >
      NOT a live NB-2 requery performed THIS turn — NB-2/NotebookLM MCP was unreachable in this
      session (verified via ToolSearch this turn, no mcp__notebooklm-mcp__* tool resolved;
      matches the fleet organism board's "seat nlm: AUTH_DEAD" reading). Inherited from the
      2026-08-18 e2b-batch2 doctrine pass instead. Cross-checked against the ACTIVE pack's own
      provenance for the same primary source ("Permenkumham Nomor 11 Tahun 2024...",
      rulepack-prod-013.source.json source_records, verified_at 2026-07-24T00:00:00Z) — that
      re-verification POST-DATES the 2026-03-28 errata, so the pack is not citing the stale
      pre-errata figure. This is the strongest check available without a live NB-2 session; a
      fresh verbatim NB-2 quote+date is still owed before this line can be marked closed.
rules: ["review.e28b.usd-threshold-manual"]
reason_code: E28B_USD_THRESHOLD_MANUAL_CHECK
product_version_id: ca8c1177-ad94-5261-82a6-68bc912d31bb
gold_personas: []  # none exist — see acceptance item 2 below
```

### The acceptance checklist (FROZEN.md §C4), honestly scored

1. **Every `fact_path` in `questions` is actually askable — SATISFIED, checked this turn.**
   `intent.requested_product_code` was hard-coded `unknownFact(NOT_ASKED)` in
   `apps/mouth/.../_lib/fact-mapper.ts` before this mandate (the exact disease this checklist item
   names) — now mapped via `enumFact(facts.investment_product_code, REQUESTED_PRODUCT_CODES)`, fed
   by the new `investment_product_code` question in `tree.ts`, sequenced unconditionally right
   after `investment_vehicle` in `flow.ts` for every "invest" category applicant. Proven at the
   real interview-path layer (not just the mapper) by `flow.test.ts`'s new "V1/E28" describe block,
   and at the mapper layer by `fact-mapper.test.ts`'s matching block — 230/230 mouth vitest green.
   Proven at the engine layer against the REAL active pack (`rulepack-prod-013.source.json`,
   not a fixture) by
   `backend/tests/services/visa_engine/test_e28_investor_golden_visa_reachability.py` — 13/13
   pytest green, including the acceptance claim itself: a plausible investor (E28A-eligible on his
   own) who additionally names E28B now reaches `HUMAN_REVIEW_REQUIRED` citing
   `E28B_USD_THRESHOLD_MANUAL_CHECK` with a `source_refs`-bearing reason, instead of the pre-fix
   behavior where E28B never appeared anywhere in the `Decision` at all. Additionally re-verified
   via a literal run of `python -m backend.scripts.visa_engine.reachability_report --pack
   backend/services/visa_engine/contracts/packs/rulepack-prod-013.source.json`: its "NOT_ASKED
   facts (live production interview)" section now lists exactly 4 facts
   (`commercial.service_fee_budget_idr`, `commercial.wants_quote`, `immigration.last_entry_date`,
   `intent.desired_entry_date`) — `intent.requested_product_code` no longer among them.
2. **At least one gold persona positively expects this code, and the code exists in the pack —
   NOT SATISFIED, genuine gap.** Measured this turn: `gold_harness/fixtures/personas/` has 23
   personas, none naming E28B; the `gold_harness` loader's own curated fixture pack
   (`loader.load_and_compile_rule_pack()`, asserted at exactly 11 products by
   `test_gold_replay.py`) does not contain E28B at all — its 11 codes are
   `C1, C2, D1, E23, E28A, E30, E31, E33, E33E, E33F, E33G`. Adding E28B there means also touching
   two hardcoded counts (`len(PERSONAS)==23`, `len(PRODUCT_CODES)==11`) in the same file — exactly
   the "12 places" derived-count trap named in this lane's brief — which is out of this single
   session's V1 per-product scope. The engine-layer pytest module above is real, cited, and proves
   the identical claim against the REAL production pack (arguably stronger evidence than a curated
   gold persona), but it is not literally a `gold_harness/fixtures/personas/*.json` file, so this
   checklist item is scored NOT SATISFIED rather than reinterpreted as met. Separately, the
   `reachability_report.py` CLI's own "Blocked (zero SUPPORT rules)" classification still lists
   E28B — expected and unchanged by this fix: that classification counts SUPPORT rules only, and
   E28B deliberately has none (§7 disposition above) — it is not the metric this fix moved.
3. **NB-2 citations verbatim with source date, compared against the pack's provenance date —
   PARTIAL.** See the `nb2_citations` block's `caveat` above: no live NB-2 requery this turn (MCP
   unreachable), inherited citation cross-checked against the pack's own source-record date
   instead. Not scored SATISFIED until a live NB-2 session reproduces the quote.
4. **Cross-family refuter re-derivation matches — NOT YET RUN.** By design (generator≠grader):
   the mandate's own lane table assigns this to a separate role ("Refuter: Kimi K3 + DeepSeek
   re-derivation"), distinct from V1's per-product builder session. Flagging for the orchestrator
   to dispatch rather than self-performing it in the same session that wrote the fix (note: DeepSeek
   V4 Pro is repo-RETIRED per `CLAUDE.md` §5 as of 2026-07-19 — the lane table's "+ DeepSeek
   re-derivation" clause is itself stale and should be reconciled to Kimi K3 alone, or to whichever
   seat the orchestrator judges live).

**Net: stays T3.** (1) is genuinely closed. (2)-(4) are honest, named gaps for the next increment
or a separate refuter/orchestrator pass — not silently skipped, not smoothed into a false PASS.
