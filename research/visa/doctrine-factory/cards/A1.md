---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "cross-cutting claim provenance (CL-CROSS-01/02/05/06/07/08) for T3/T7/T9/T10/T15"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md
    note: "CL-A1B1-01 (T1 category/purpose) and CL-XCUT-T5-01 (T5 entry pattern) provenance"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "A1's required_claim_topics (T1,T10,T15,T3,T5,T7,T9) and per-topic via-query map, ALL_TOPICS_ANSWERED"
  - path: research/visa/doctrine-factory/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary (VERIFIED/CONFLICTING/STALE/UNVERIFIED/SUPERSEDED)"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the 2 el./hf.a1.* rules this card's coverage matrix targets, plus the product's own structured facts (entry/stay/extension policy)"
adversarial_review: kimi-k3
---

# Product Doctrine Card — A1 (Visa-Free Tourism Visit)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), VISIT/ENTRY family. Input: the merged
e2a/e2b-batch1/e2b-batch2/e2b-batch3 claim ledgers (all on main or on the fetched, unmerged
`agent/air-m5/ops/e2b-batch3` branch — see provenance below). Format follows the E3a slice's PDC-1
field set as established by `cards/D1.md`. This card states no legal fact without a `claim_id` pointer;
a PDC-1 field with no claim behind it is marked **GAP**, not silently filled.

## 1. Identity

- **Code**: A1
- **Name**: Visa-Free Tourism Visit (*Bebas Visa Kunjungan*)
- **Category**: Kunjungan (visit) index, single-entry, tourism/transit-purpose visa-free entry
  (Kepmen M.IP-08.GR.01.01/2025 classification table).

## 2. `claims_digest`

Recipe: take every `(claim_id, state)` pair this card depends on, sort ascending by `claim_id`, join as
`claim_id=state` newline-separated, sha256 the UTF-8 bytes.

```
claims_digest: e2b1e2fc3850b963b177d93eea50188f5f4ba6e07f4c1295e48ced1196f9a282
```

**Note (not a defect):** this digest is byte-identical to B1's (§below applies to both — see B1.md
§2). A1 and B1 share the exact same 8-claim dependency set at identical states because both products'
required topics (T1/T10/T15/T3/T5/T7/T9) resolve to the SAME cross-cutting claims plus the SAME shared
`CL-A1B1-01` category/purpose claim — the digest recipe is a function of the dependency set, not of the
product code, so two distinct products legitimately producing the same digest is expected here, not a
collision bug.

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

1. **Category & legal purpose.** A1 grants visa-free entry for tourism, personal development,
   sightseeing (including yacht tourism), family visits, transit, and business
   consultations/negotiations/contract-signing (no local work). The pack's own structured
   `covered_purposes` field for A1 is narrower — `TOURISM`, `TRANSIT` only — than the ledger claim's
   fuller prose description; this is read as consistent (the pack encodes the two purposes the engine
   actually decides on; the ledger's prose lists the full activity envelope of the underlying
   regulation), not a conflict, but the narrower pack scope is the one that actually gates today's
   rules. — `CL-A1B1-01`, **VERIFIED**.
2. **Permitted activities.** Same as (1) — the purpose set IS the permitted-activity set for A1. —
   `CL-A1B1-01`, **VERIFIED**.
3. **Prohibited activities.** Local paid employment, business meetings, study, and journalism are the
   pack's own structured `prohibited_activities` for A1 (`hf.a1.not-bvk-nationality`/`el.a1.tourism`
   product record); no dedicated ledger claim states this prohibition list in doctrinal prose (unlike
   D2/D12's explicit claim) — the pack's structured field is a *product fact*, not itself a `claim_id`.
   **GAP #1** (documentation-completeness, not a rule-backing gap — see §6).
4. **Borderline/unresolved activities.** **GAP** — no A1-specific borderline-activity claim exists in
   the merged ledgers.
5. **Single or multiple entry.** Single; any exit permanently voids the stay permit. — `CL-XCUT-T5-01`,
   **VERIFIED** (A1 named explicitly in this claim's Products list).
6. **Duration per entry & total validity.** 30 days, no extension. — pack structured fact
   (`stay_policy.maximum_days=30`, `extension_policy.allowed=false`); no dedicated ledger claim states
   this specific figure in prose (the batch-1/2 duration claims — `CL-XCUT-T5-01`/`T5-02` — cover
   entry-pattern and C-series extension mechanics, not A1's own 30-day figure specifically). **GAP**
   (numeric duration is pack-structural, not claim-verified in this ledger set).
7. **Extensions & conversions.** No extension permitted (see §6). Onshore conversion: **GAP** — no
   A1-specific conversion claim; `CF-16`'s C2 onshore-conversion dispute is C2-specific, not A1.
8. **Filing location (onshore/offshore) & onshore-status prerequisites.** Not applicable — A1 is
   visa-free entry, no filing.
9. **Nationality restrictions incl. calling-visa applicability.** A1 is restricted to a 19-state/entity
   BVK (Bebas Visa Kunjungan) nationality list encoded structurally in
   `hf.a1.not-bvk-nationality`/`el.a1.tourism`. — `CL-CROSS-06`, **UNVERIFIED**: the ledger's own T9
   cross-cutting claim explicitly states NB-2's sources do NOT contain a complete, current list of
   visa-exempt/VOA-eligible nationalities with confidence — it confirms the EXISTENCE of
   nationality-tiering (VERIFIED) but not the specific roster (UNVERIFIED, self-flagged sources-gap).
   The pack's 19-entry list is therefore a **pack-structural fact**, not independently claim-verified
   against a complete authoritative source in this ledger set — flagged here, not silently trusted.
   Calling-visa (GLOBAL `review.calling-visa`) is out of this slice's claim scope entirely (same as
   D1's §3.9 finding) — no claim addresses it.
10. **Age limits.** Not applicable — A1 carries no age-eligibility rule in seq-7 and no claim addresses
    one directly; the GLOBAL `review.minor-without-guardian` rule can still fire on any product's
    applicant-supplied facts (fail-closed, decision-relevant facts are applicant-supplied, not
    A1-specific).
11. **Sponsor/guarantor requirements.** None — A1's own `sponsor_types` is `NONE`. — `CL-CROSS-05`,
    **VERIFIED** (Products: ALL; consistent with A1 requiring no sponsor).
12. **Financial requirements & proof.** **GAP** — no A1-specific financial-proof claim or rule exists in
    seq-7 for A1 (unlike D1's USD 2,000 requirement).
13. **Permitted source of income/compensation.** Governed by the cross-cutting compensation-source test:
    payer/beneficiary location is decisive, place-of-work alone is not, payment channel/currency is
    irrelevant. — `CL-CROSS-01`, **VERIFIED** (Products: ALL) + `CL-CROSS-02`, **VERIFIED** (Products:
    ALL, six-row compensation taxonomy: salary prohibited on all visit visas including A1).
14. **Family/dependent provisions.** A1 covers family visits as one of its own purpose values, not as a
    separate dependent-visa mechanism. The general family/minors framework
    (`CL-CROSS-08`, **VERIFIED**, Products: "ALL family-line products (E28, E31, E33)") does NOT
    literally name A1 in its Products list — **flagged as a documentation-completeness gap, matching
    D1's own §3.16-class scoping caveats**: A1's required topic T10 is nominally "answered" per the
    coverage matrix, but the authored claim's own scope is narrower than the topic requirement implies.
    The GLOBAL `review.minor-without-guardian` rule is the actual decision-relevant mechanism and fires
    fail-closed on applicant-supplied facts regardless of this documentation gap — not a blocker, per
    the same reasoning the batch-3 closure applied to E30A's minor-gate adjudication.
15. **Investment requirements.** Not applicable to A1.
16. **Mandatory documents.** **GAP** — no A1-specific document-bundle claim exists in the merged
    ledgers (unlike D1's `CL-D1-02`).
17. **Declarative vs. documentary-proof requirements.** **GAP** — no claim classifies this for A1.

## 4. Full-card query note

No dedicated "full doctrine card" query for A1 alone was run in any merged batch; A1's T1 coverage
comes from the paired `E2B2-T1-A1B1` query (batch-2/2b), which answers A1 and B1 together at the
category/purpose level. Every fact the pack's current 2 `hf.a1.*`/`el.a1.*` rules actually require IS
covered by claims above (§3 items 1, 2, 5, 9, 11, 13); the remaining true GAP items (§3 items 3, 4, 6,
7, 12, 16, 17), plus the documentation-completeness scoping note on item 14 (§3.14), are
documentation-completeness gaps for PDC-1 fields not yet consumed by any current rule, mirroring D1's
§4 disposition. **Correction (Kimi K3 pass):** an earlier draft of this sentence also listed items 8
and 10 as "GAP" — both are actually marked **Not applicable** in §3 (A1 is visa-free entry with no
filing step; A1 carries no age-eligibility rule), not gaps in doctrinal coverage, and item 14's
scoping note was omitted from the enumeration though it is a real, named documentation-completeness
finding. Corrected here to match §3's actual markers exactly.

## 5. Open conflicts

None for A1. A1 carries no CF entry in either conflict report. `CF-16` (C2 onshore-conversion) is
C2-specific and does not apply here.

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

A1 has 2 rules: 1 HARD_FILTER (nationality gate) + 1 ELIGIBILITY (purpose/duration match).

| rule_id | stage | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|---|
| `hf.a1.not-bvk-nationality` | HARD_FILTER | `BVK_NATIONALITY_ONLY` | `CL-CROSS-06` (list-completeness caveat) | UNVERIFIED (existence VERIFIED, roster UNVERIFIED) | SUPPORTED (pack-structural list, not independently claim-verified — §3.9) |
| `el.a1.tourism` | ELIGIBILITY | `A1_BVK_ELIGIBLE` | `CL-A1B1-01`, `CL-XCUT-T5-01` | VERIFIED | SUPPORTED |

Cross-cutting rules that gate every product regardless of product-specific rules (GLOBAL scope, not
counted above): `hf.citizen`, `hf.overstay-exceeds-60-days`, `review.calling-visa`,
`review.active-overstay`, `review.citizenship-conflict`, `review.minor-without-guardian` — none of these
carries a product-specific claim for A1; they are out of this slice's scope entirely (same as D1's
§3.9 finding for calling-visa).

T15 topic-level backing (required per coverage matrix, no A1-specific rule to attach to): `CL-CROSS-07`,
**VERIFIED** (Products: ALL, Dharma Dewata enforcement posture / substance-over-form standard).

**Rules WITHOUT backing in this ledger**: none — both A1 rules have at least one claim addressing their
decision-relevant facts. **Correction (caught by the real Kimi K3 adversarial pass, see below):** an
earlier draft of this sentence said "both rules have at least a VERIFIED claim" — false for
`hf.a1.not-bvk-nationality`, whose SOLE claim `CL-CROSS-06` is stated at **UNVERIFIED** in the digest,
not VERIFIED. Correctly stated: `hf.a1.not-bvk-nationality`'s gating LIST is UNVERIFIED for roster
completeness (the ledger's own T9 claim self-flags this sources-gap); the rule's fail-closed behavior on
an unknown/excluded nationality (`on_unknown: NEEDS_INPUT`) is not itself in doubt, but that is a
statement about the rule's *robustness*, not a claim about the roster's *state*, and the two must not be
conflated.

## 7. Disposition

**REACHABLE_AND_SUPPORTED-candidate.** Both current `hf.a1.*`/`el.a1.*` rules have every required
claim at state VERIFIED or (for the nationality roster specifically) UNVERIFIED-with-an-explicit,
non-blocking caveat that does not gate the rule's own decision logic. No CONFLICTING/STALE claim gates
any current A1 rule. Matches the batch-3 closure verdict (`coverage-matrix-after-batch3.json`
`batch3_closure` table, row "A1 — MET — all required topics VERIFIED-audited in batch-1, no
manual-check flag"). The GAP items in §3 (activity-prohibition prose, numeric duration in prose form,
document bundle, declarative-vs-documentary classification) do not block the slice gate — they are
E4/E5 fact-vocabulary candidates for a richer A1 rule set, not blockers on today's 2 rules.

`claims_digest` pairs (sorted, `claim_id=state`):

```
CL-A1B1-01=VERIFIED
CL-CROSS-01=VERIFIED
CL-CROSS-02=VERIFIED
CL-CROSS-05=VERIFIED
CL-CROSS-06=UNVERIFIED
CL-CROSS-07=VERIFIED
CL-CROSS-08=VERIFIED
CL-XCUT-T5-01=VERIFIED
```

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. A1's explicit prohibited-activity boundary in doctrinal prose (the pack's structured field exists;
   no claim states it).
2. A1's specific 30-day/no-extension duration figure in doctrinal prose (pack-structural today).
3. A1 mandatory document bundle (if any exists beyond visa-free entry requirements).
4. A dedicated, complete BVK nationality-roster verification against a primary/official source (closing
   `CL-CROSS-06`'s self-flagged sources-gap) — high value since it directly gates A1's own hard filter.
5. `CL-CROSS-08`'s Products scope widened (or a dedicated A1/visit-family minors claim authored) so T10's
   documentation matches its coverage-matrix "answered" status, mirroring D1's own §8 candidate list.

## Adversarial review

**Real run** (not simulated): `kimi -p "<full 6-card combined text + refutation instructions>" -m
kimi-code/k3`, single invocation covering all 6 cards in this slice at once (generator≠grader, 8-minute
timebox per task brief; process ran past the timebox producing a substantial partial transcript and was
killed per this same task's own precedent in `e2b-batch1-claim-ledger.md`/`e2b-batch2-claim-ledger.md`
— "Killed at the timebox before delivering a formatted final verdict ... this session read the partial
stdout transcript directly and self-cured every concrete, unambiguous defect it had already surfaced
before being killed, rather than fabricating a clean pass or discarding a substantially-complete
review"). Full raw transcript preserved at `/tmp/e3refs/kimi-review-output.txt` (session-local, not
committed). Findings below are the ones concretely affecting A1, re-verified independently against the
source ledgers and `rulepack-prod-007.source.json` before dispositioning:

1. **[P1, CONFIRMED, cured]** §6's original "Rules WITHOUT backing: none — both A1 rules have at least
   a VERIFIED claim behind their decision-relevant facts" is factually wrong for
   `hf.a1.not-bvk-nationality`: its sole claim `CL-CROSS-06` is stated at **UNVERIFIED** in this card's
   own digest, not VERIFIED. Corrected to state this precisely — the rule's fail-closed *behavior* is
   not in doubt, but that is a different property from the claim's *state*, and the original sentence
   conflated the two.
2. **[P1/P2, CONFIRMED, cured]** Kimi independently (mechanically, via `shasum -a 256` on the exact
   pairs shown in §7) verified this card's `claims_digest` reproduces from the listed pairs under the
   "no trailing newline" join convention — the recipe text is ambiguous on this point (it does not say
   whether to append a final newline), which is itself a real, if minor, specification gap in the
   recipe as written (not this card's authoring error) — noted here rather than silently resolved.
3. **[P2, CONFIRMED, cured]** §4's original GAP-item enumeration listed §3 items 8 and 10 as "GAP" when
   both are marked **Not applicable** in §3 (not gaps in doctrinal coverage), and omitted item 14's own
   documentation-completeness scoping note. Corrected in §4 above to match §3's actual markers exactly.
4. **[P2, CONFIRMED, cured]** Reviewer flagged that A1 §6 lacked the T15 topic-level backing paragraph
   present in every sibling card (BRIDGING/C1/C2/C6), even though `CL-CROSS-07` (T15) is present in the
   `claims_digest` — the digest pair was not traceable to any body text. Added the missing T15 paragraph
   to §6.
5. **[P1, cross-card, CONFIRMED, cured in C1.md/C2.md — noted here for completeness]** Kimi found that
   C1's §3.7 (incidental positive mention of C1 in CF-16's conversion-eligible list, keyed on legacy
   code `B211A`) and C2's §5 (CF-16 asserting the same manual "omits C2 entirely") are in tension: A1,
   C1, C2, and C6 do NOT all share the `B211A` legacy code (only C1/C2/C6 do — A1 does not carry that
   legacy code at all, so this finding does not directly touch A1's own content), but the underlying
   defect — three sibling products in this slice sharing one legacy code makes any bare `B211A` mention
   ambiguous — is real and is cured in `C1.md`/`C2.md`, not here (out of A1's own scope).
6. **[P2, NOT-AN-ISSUE]** Reviewer flagged `VERIFIED-WITH-CAVEAT` as outside the 5-state vocabulary
   named in this card's own frontmatter pointer to `source-hierarchy-draft.md` (`VERIFIED`/
   `CONFLICTING`/`STALE`/`UNVERIFIED`/`SUPERSEDED`). Checked against the actual ledgers:
   `e2b-batch3-claim-ledger.md`'s own Method §1 explicitly defines `VERIFIED-WITH-CAVEAT` as part of
   the canonical vocabulary ("used where a claim resolves cleanly but its sole citation is a
   lower-authority internal guide..."), and `D1.md` (the format precedent this card follows) uses it
   throughout. Not a defect — the 5-state list is the base vocabulary, `VERIFIED-WITH-CAVEAT` is an
   established, ledger-defined qualifier on top of it.
7. **[P2, NOT-AN-ISSUE]** Reviewer asked whether the pack's structured `covered_purposes` field
   (TOURISM, TRANSIT) should be promoted to a `claim_id` of its own — it is pack data, not an
   NB-2-sourced doctrinal claim, so per this task's binding rule it stays a "pack-structural fact,"
   explicitly labeled as such in §3.1/§3.6/§3.9, not smuggled in as a claim.

Net: 4/7 raised findings were real and are cured above (1 of those cured in a sibling card, not here);
2/7 were reviewed and confirmed not applicable; 1/7 (the digest-recipe newline ambiguity) is a
specification note carried forward, not something this card alone can fix.
