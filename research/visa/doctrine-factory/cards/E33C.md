---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch1.json
    note: "reachability BLOCKED for E33C confirmed against pack seq-7 (re-derived live this session)"
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "CL-E33-01 — same absence noted for E33B: E33C is not in CL-E33-01's own enumerated sub-index list"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — E33C's single PRODUCTS-scoped rule, re-verified live this session"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E33C (Second Home Golden Visa — World-Figure Government Invitation) — BLOCKED

Task: E3 bulk, LONG-STAY family, BLOCKED member.

## 1. Identity — priority target (Zero, identity-resolution priority)

Per Zero's explicit priority instruction: product identity for this card is sourced ONLY from the
live pack + the 114-code catalog — **never from the 2026-08-12 fact-vocabulary design doc, which
this task's CP2 pack correction (merged #4254) confirms misidentified E33A/B/C's names.**

- **Code**: E33C
- **Name (pack, authoritative)**: Second Home Golden Visa — World-Figure Government Invitation
  (E33C), per `rulepack-prod-007.source.json` `names.en` — re-independently-verified live this
  session (`names.id`: "Visa Rumah Kedua Tokoh Dunia Undangan Pemerintah (E33C)").
- **Name (114-code catalog, cross-checked)**: "Second Home Visa World Figure Government Invitation",
  per `seed_visa_types_complete_2026.py:1452-1462` — independently re-verified live this session,
  exact match (no discrepancy).
- **Category (pack)**: `LIMITED_STAY`, Second-Home Golden Visa family, `covered_purposes:
  [INVESTMENT, BUSINESS_MEETINGS, TOURISM, FAMILY]`.

## 2. Reachability — confirmed BLOCKED

`coverage-matrix-after-batch3.json` (fetched from branch `agent/air-m5/ops/e2b-batch3` this session, NOT committed in this worktree — cited by content, not a resolvable in-repo path) gives `matrix.E33C.reachability = "BLOCKED"`, consistent with the in-repo `coverage-matrix-after-batch1.json`, which independently confirms BLOCKED for E33C. E33C carries exactly one
PRODUCTS-scoped rule, `review.e33c.central-government-invitation` (stage `HUMAN_REVIEW`, same
`reason_code` — `GOVT_INVITATION_REQUIRED` — as E33A's rule), and zero ELIGIBILITY-stage SUPPORT
rule. Structurally identical shape to E33A/E33B.

## 3. Epistemic state

- **What's VERIFIED**: nothing E33C-specific; same absence from `CL-E33-01`'s enumeration as E33B.
- **A structural observation worth recording, not a claim**: E33C shares the exact `reason_code`
  (`GOVT_INVITATION_REQUIRED`) with E33A, differing only in `intent.purposes` gate
  (`INVESTMENT` for E33C vs `EMPLOYMENT` for E33A) — the pack authors evidently treat these as the
  same review MECHANISM applied to two different applicant categories ("world-figure" vs
  "special-expertise" government invitees). This is a mechanical observation from the pack's own
  `when`/`effect` JSON, not a doctrine claim — no NB-2 source in this ledger chain confirms or denies
  that "world figure" and "special expertise" invitations follow the same review process in practice.
- **What's GAP**: what qualifies an applicant as a "world figure" for this purpose, which government
  body issues the invitation, whether a financial basis (the `INVESTMENT` purpose tag suggests one) is
  also required, term length, and extension/conversion path — all unresearched.

## 4. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E33C has 1 PRODUCTS-scoped rule.

| rule_id | stage | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|---|
| `review.e33c.central-government-invitation` | HUMAN_REVIEW | `GOVT_INVITATION_REQUIRED` | none | GAP (no claim at all) | fires to HUMAN_REVIEW, never to SUPPORTED |

## 5. Disposition (PROPOSAL — Zero rules)

**PROPOSED: keep BLOCKED, log as `AWAITING_DOCTRINE_AND_RULE_AUTHORING`**, same tier as E33B (both
have zero dedicated claims, unlike E33A which at least has the family-enumeration mention and the
naming-collision finding). **Priority step per Zero's identity-resolution directive**: identity is
ALREADY resolved and cross-checked for E33C (§1: pack + 114-code catalog exact match, no design-doc
contamination) — not a remaining action item. Recommend batching E33B and E33C into the SAME future
NB-2 query round as E33A (all three Golden-Visa-tier government-invitation products), since they are
structurally identical in the pack and likely share a common primary-law source (`Kepmen
M.IP-08.GR.01.01/2025`'s Second Home Golden Visa provisions) that a single well-targeted query could
resolve for all three at once — more efficient than three isolated single-product queries.

## 6. Claim gaps discovered (input for next E2 batch)

1. "World figure" qualification criteria and the issuing/inviting government body.
2. Whether the `INVESTMENT` purpose tag implies an independent financial threshold beyond the
   invitation itself.
3. Whether E33A's and E33C's shared `GOVT_INVITATION_REQUIRED` review mechanism is doctrinally
   identical (one review process, two eligible-applicant categories) or should diverge — the pack's
   current shape assumes the former without a citable source.
4. Duration/extension/KITAP-conversion path — unaddressed.

## Adversarial review

Covered by the same single kimi pass documented in `E33A.md`'s Adversarial review section. The
review's finding #1 (citation precision on naming-collision claims) does not apply — this card makes
no naming-collision claim, only the mechanical "shared reason_code" observation in §3, which is
explicitly labeled a structural observation rather than a sourced doctrine claim, so it does not need
NB-2 provenance. Finding #2 (separate rule-gap from content-gap) is applied in §5. Finding #3
(SUPPORTED reachability language) is applied in §4.

## C4 — V1 product card (2026-08-25, mandate `docs/plans/2026-08-24-visa-oracle-live/MANDATE.md`,
lane V1 unit 1 follow-up, team-lead ruling on E33A/B/C reachability, contract
`docs/plans/2026-08-24-visa-oracle-live/contracts/FROZEN.md` §C4)

**Deviation disclosed**: same as `E28B.md`'s C4 section — front-matter is preserved, C4 fields
recorded as a fenced block in the body.

**Correction to §5's disposition, made explicit here rather than silently superseded**: same
correction as `E33A.md`/`E33B.md`'s C4 sections — §5 (2026-08-18) conflated reachability with
automatability. E33C needed no rule-authoring to become reachable, only an interview question, and
unlike E33A/B it rides the "invest" branch, not "work" (E33C's purpose is INVESTMENT). §5's
rule-authoring gap and batching recommendation remain real and open.

```yaml
code: E33C
name_en: "Second Home Golden Visa — World-Figure Government Invitation"
name_id: "Visa Rumah Kedua Tokoh Dunia Undangan Pemerintah"
definition: >
  LIMITED_STAY Second Home Golden Visa tier for a foreign applicant holding a central-government
  invitation on world-figure grounds. Purposes covered: INVESTMENT, BUSINESS_MEETINGS, TOURISM,
  FAMILY. Sponsor gated to GOVERNMENT or NONE only — the product's own HARD_FILTER
  (`hf.e33c.sponsor-not-government-or-none`) excludes every other sponsor.type value, identical in
  shape to E33B's (differs from E33A's GOVERNMENT-only gate). No ELIGIBILITY/SUPPORT rule exists;
  the engine can only route to HUMAN_REVIEW, never auto-approve.
tier: T3
tier_reason: >
  Reachability fixed this turn (V1 unit 1 follow-up); persona coverage (criterion 2), NB-2
  citation (criterion 3), and refuter re-derivation of THIS turn's fields (criterion 4) are not
  yet done — see acceptance checklist below. §6's claim gaps remain fully open.
tree_placement:
  parent_question: sponsor_category
  question_id: investment_product_code_govt
  discriminators:
    - "intent.requested_product_code == \"E33C\" (one of 2 mutually exclusive answer values on this question — STANDARD is the other; unlike E33A/B this question lives on the \"invest\" branch, after investment_product_code, not the \"work\" branch)"
    - "sponsor.type in [\"GOVERNMENT\", \"NONE\"] (same gate shape as E33B's, unified in one question here because there is no second, narrower-gated product sharing this branch the way E33A narrows E33B's shared question)"
  asked_unconditionally_across: []  # gated on sponsor_category being GOVERNMENT or NONE (flow.ts's "invest" branch, spliced after investment_product_code)
questions:
  - fact_path: intent.requested_product_code
    ask_en: "Do you already know if your case is a central-government-invitation investor visa (E33C)?"
    ask_id: "Apakah Anda sudah tahu apakah kasus Anda adalah visa investor undangan-pemerintah-pusat (E33C)?"
nb2_citations: []  # NONE — see acceptance item 3 below and the PENDING-ARMS.md ledger entry (opened 2026-08-25) this gap is tracked under
rules: ["review.e33c.central-government-invitation", "hf.e33c.sponsor-not-government-or-none"]
reason_code: GOVT_INVITATION_REQUIRED  # shared verbatim with E33A's review rule — see this card's own §3 structural observation
product_version_id: 9cc14fee-49dc-5549-8bc8-90bdaf52d1f3
gold_personas: []  # none exist — see acceptance item 2 below
```

### The acceptance checklist (FROZEN.md §C4), honestly scored

1. **Every `fact_path` in `questions` is actually askable — SATISFIED, checked this turn.**
   `intent.requested_product_code` reaches KNOWN for E33C through `investment_product_code_govt`
   (`tree.ts`), inserted into the interview by `flow.ts`'s "invest" branch ONLY when
   `sponsor_category` is GOVERNMENT or NONE — matching `hf.e33c.sponsor-not-government-or-none`'s
   own gate exactly. Proven at the interview-path layer by `flow.test.ts`'s new "V1/E33" gating
   tests, at the mapper layer by `fact-mapper.test.ts`'s matching block (including the
   team-lead-mandated innocence test), and at the engine layer against the REAL active pack by
   `backend/tests/services/visa_engine/test_e33_reachability.py` — 21/21 pytest green, including:
   naming E33C with a valid sponsor reaches `HUMAN_REVIEW_REQUIRED` citing `GOVT_INVITATION_REQUIRED`
   with a `source_refs`-bearing reason (this reason_code is shared verbatim with E33A's own rule —
   the proof is still per-product-correct because `evaluate_product` is scoped to one product at a
   time, not distinguished by reason_code); naming E33C with an invalid sponsor.type (e.g. EMPLOYER)
   resolves to `EXCLUDED`, never demoting a sibling `SUPPORTED_CANDIDATES` outcome (E28A, in this
   INVESTMENT-purpose case).
2. **At least one gold persona positively expects this code, and the code exists in the pack —
   NOT SATISFIED, genuine gap.** Same finding as E33A.md/E33B.md's item 2 — E33C is not in the
   `gold_harness` fixture pack's 11 codes; out of this session's V1 per-product scope. The
   `reachability_report.py` CLI's "Blocked (zero SUPPORT rules)" classification still lists E33C —
   expected, unchanged by a reachability-only fix.
3. **NB-2 citations verbatim with source date — NOT SATISFIED, unchanged from §3/§6 above.** No
   claim names what "world figure" qualification requires, which government body issues the
   invitation, or whether an independent financial threshold applies. Written PROMINENTLY per
   team-lead's explicit instruction: **the human reviewer who now receives a
   `HUMAN_REVIEW_REQUIRED` E33C case has nothing written down to check the invitation/world-figure
   claim against.** Tracked in `.claude/skills/modus/PENDING-ARMS.md`, opened 2026-08-25, owner
   `me`, same row as E33A/E33B (all three share the doctrine gap).
4. **Cross-family refuter re-derivation — NOT YET RUN for this turn's fields.** Same as
   E33A.md/E33B.md's item 4 — the 2026-08-18 Kimi K3 pass predates this turn's design and did not
   review it.

**Net: stays T3.** (1) is genuinely closed this turn. (2)-(4) are honest, named gaps, shared
identically across all three E33 products this follow-up touched.
