---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "cross-cutting claim provenance (CL-CROSS-01/02/05/06/07/08) for T3/T7/T9/T10/T15"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md
    note: "CL-C1C6-01 (T1, thin) and CL-XCUT-T5-02 (T5, C-series extension mechanics — C6 named directly; CL-XCUT-T5-01 explicitly EXCLUDES C6, see §6)"
  - path: research/visa/doctrine-factory/query-bank/coverage-matrix-after-batch3.json
    note: "C6's required_claim_topics (T1,T10,T15,T3,T5,T7,T9), ALL_TOPICS_ANSWERED"
  - path: research/visa/doctrine-factory/source-hierarchy-draft.md
    note: "authority-level vocabulary (L1-L7), state vocabulary"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the el.c6.social rule this card's coverage matrix targets"
adversarial_review: kimi-k3
---

# Product Doctrine Card — C6 (Social Activity Visit Visa)

Task: E3 bulk (Visa Oracle doctrine-factory execution plan), VISIT/ENTRY family. Input: the merged
e2b-batch1/batch2/batch2b claim ledgers. Format follows `cards/D1.md`'s PDC-1 field set. No legal fact
without a `claim_id` pointer; a PDC-1 field with no claim behind it is marked **GAP**, not silently
filled.

## 1. Identity

- **Code**: C6 (legacy code: B211A)
- **Name**: Social Activity Visit Visa (*Visa Kunjungan Kegiatan Sosial*)
- **Category**: Kunjungan (visit) index, single-entry, social/"other"-purpose (Kepmen
  M.IP-08.GR.01.01/2025 classification table).

## 2. `claims_digest`

Recipe: sort `(claim_id, state)` pairs ascending by `claim_id`, join `claim_id=state`
newline-separated, sha256 the UTF-8 bytes.

```
claims_digest: 7140ad4b5af5365cf1eddfdb506ead06884869146b6f4c11a53d432be2da56bc
```

## 3. Doctrinal facts (PDC-1 fields, each claim-referenced)

1. **Category & legal purpose.** The regulatory-hierarchy classification for the paired C1/C6 answer
   resolved cleanly, but the product-specific category/purpose/activity content was truncated before
   delivery, same as C1's own §3.1 finding. — `CL-C1C6-01`, **VERIFIED-WITH-CAVEAT (thin)**. Matches
   the pack's own structured `covered_purposes` for C6 — `OTHER` (a single, generic purpose bucket,
   distinct from C1's more specific `TOURISM`/`FAMILY` pair) — read as the "social activity" catch-all
   this product name implies.
2. **Permitted activities.** Social activities under the `OTHER` purpose bucket — the pack's structured
   fact; no doctrinal-prose claim names specific permitted social-activity types (e.g. volunteering,
   religious/cultural events, family social visits distinct from C1's tourism-purpose family visits).
   **GAP** — `CL-C1C6-01`'s truncation means this distinction was never delivered in prose.
3. **Prohibited activities.** Local paid employment, business meetings — pack's own structured
   `prohibited_activities` field; no dedicated doctrinal-prose claim for C6 specifically. **GAP**, same
   truncation as (1)/(2).
4. **Borderline/unresolved activities.** **GAP** — no C6-specific borderline-activity claim.
5. **Single or multiple entry.** Single-entry — per the general C-series rule. **Note: `CL-XCUT-T5-01`
   explicitly EXCLUDES C6 from its own Products list** (`e2b-batch2-claim-ledger.md`'s own scoping
   note: "C6 is NOT explicitly named in this answer's own text ... so C6 is excluded from this claim's
   Products list even though it is a C-series product, to avoid asserting a fact this specific answer
   did not actually state for C6"). C6's single-entry status is instead backed by `CL-XCUT-T5-02`
   (**VERIFIED**, C6 named directly, that answer's own query scope was specifically C1/C2/C6) — the
   only card in this slice where the entry-pattern topic is carried by a DIFFERENT claim than the one
   covering the sibling products, and this card follows the ledger's own precise scoping rather than
   assuming C6 inherits `CL-XCUT-T5-01`'s Products list by category-membership.
6. **Duration per entry & total validity.** 60-day initial stay, up to 2 extensions of 60 days each
   (max 180 days total); filing window opens 14 days before expiry; timely-filed extension suspends
   overstay accrual. — `CL-XCUT-T5-02`, **VERIFIED** (C6 named directly). Matches the pack's own
   structured `stay_policy` (60/60 fixed days) and `extension_policy` (max 2 extensions, 60 days each)
   for C6's `product_version_id` — exact agreement, no discrepancy.
7. **Extensions & conversions.** Extension mechanics per (6). Onshore-conversion eligibility: **GAP** —
   C6 is not named in either direction by `CF-16`'s conversion-manual comparison (unlike C1, which is
   incidentally named on the eligible side, and C2, which is the conflict's own subject) — genuinely
   unaddressed, not silently assumed either way.
8. **Filing location (onshore/offshore) & onshore-status prerequisites.** Extensions filed onshore,
   same implied-by-filing-window reasoning as C1.md §3.8 and C2.md §3.8. No dedicated C6-specific claim
   states this explicitly.
9. **Nationality restrictions incl. calling-visa applicability.** **GAP** — C6 carries no
   nationality-gating rule in seq-7; `CL-CROSS-06` is topically present (Products: ALL) but has no
   C6-specific applicability. Calling-visa (GLOBAL) out of scope, same as every card in this slice.
10. **Age limits.** Not applicable at the C6-specific rule level.
11. **Sponsor/guarantor requirements.** C6's own `sponsor_types` is `["INDIVIDUAL", "EMPLOYER"]`
    (notably excludes `NONE`, unlike C1) and `el.c6.social` DOES gate on `family.sponsor_confirmed=true`
    — the same sponsor-mandatory pattern as C2 (see C2.md §3.11), unlike C1/A1/B1/BRIDGING. —
    `CL-CROSS-05`, **VERIFIED** (Products: ALL, general sponsor framework); no C6-specific sponsor-letter
    claim exists (unlike C2's dedicated `CL-C2-03`) — **GAP** for a C6-specific sponsor-procedure claim,
    though the general framework claim backs the rule's decision-relevant fact.
12. **Financial requirements & proof.** **GAP** — no C6-specific financial-proof claim or pack rule.
13. **Permitted source of income/compensation.** Same cross-cutting test as the rest of this slice. —
    `CL-CROSS-01`, **VERIFIED** (Products: ALL) + `CL-CROSS-02`, **VERIFIED** (Products: ALL — the
    six-row taxonomy's in-kind/barter prohibition applies to "tourist/visit visas including C5A," a
    category C6 falls within as a visit-visa product, though C6 is not individually named).
14. **Family/dependent provisions.** C6's `OTHER` purpose bucket may cover family-adjacent social
    visits, but this is not separately confirmed by any claim distinct from C1's own family-purpose
    coverage. `CL-CROSS-08` (**VERIFIED**, Products: "ALL family-line products (E28, E31, E33)") does
    not literally name C6 — same documentation-completeness gap flagged across this slice.
15. **Investment requirements.** Not applicable to C6.
16. **Mandatory documents.** Sponsor confirmation per (11); no further C6-specific document-bundle
    claim exists. **GAP** beyond the sponsor requirement itself.
17. **Declarative vs. documentary-proof requirements.** **GAP** — no claim classifies this for C6.

## 4. Full-card query note

Same as C1: C6's T1 topic is answered by the paired `E2B2-T1-C1C6` query, whose response was cut off
mid-table before delivering either product's category/purpose/activity content in detail. This card
treats every §3 item sourced only to `CL-C1C6-01` as thin-by-construction, matching C1.md's own
disposition.

## 5. Open conflicts

None specific to C6. Neither conflict report carries a C6-tagged entry; `CF-16` (C2's onshore-conversion
dispute) does not name C6 in either direction (§3.7).

## 6. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

C6 has 1 rule: 1 ELIGIBILITY (purpose/sponsor/duration match).

| rule_id | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|
| `el.c6.social` | `C6_SOCIAL_ELIGIBLE` | `CL-C1C6-01`, `CL-CROSS-05`, `CL-XCUT-T5-02` | VERIFIED-WITH-CAVEAT (thin) / VERIFIED / VERIFIED | SUPPORTED |

**Important scoping note carried forward from §3.5**: `CL-XCUT-T5-01` does NOT back C6 (explicitly
excluded by that claim's own Products list) — only `CL-XCUT-T5-02` does. A coverage-matrix reader who
assumes C6 inherits every "C-series" claim by category membership would over-claim here; this card does
not.

T3/T7/T9/T10/T15 topic-level backing: `CL-CROSS-01`/`CL-CROSS-02` (T3, VERIFIED), `CL-CROSS-05` (T7,
VERIFIED, directly relevant since `el.c6.social` gates on sponsor confirmation), `CL-CROSS-06` (T9,
UNVERIFIED-caveated, weak applicability), `CL-CROSS-08` (T10, VERIFIED, scoping gap per §3.14),
`CL-CROSS-07` (T15, VERIFIED).

**Rules WITHOUT backing in this ledger**: none — C6's single rule has every decision-relevant fact
(`intent.purposes`, `family.sponsor_confirmed`, `intent.stay_days`) backed by at least a
VERIFIED-WITH-CAVEAT claim.

## 7. Disposition

**REACHABLE_AND_SUPPORTED-candidate.** C6's 1 current rule has every required claim at state VERIFIED
or VERIFIED-WITH-CAVEAT (thin). Matches the batch-3 closure verdict
(`coverage-matrix-after-batch3.json` `batch3_closure` table, row "C6 — MET — all required topics
VERIFIED-audited, no manual-check flag"). The GAP items in §3 do not block the slice gate — they are
E4/E5 fact-vocabulary candidates, per the same reasoning applied throughout this slice.

`claims_digest` pairs (sorted, `claim_id=state`):

```
CL-C1C6-01=VERIFIED-WITH-CAVEAT
CL-CROSS-01=VERIFIED
CL-CROSS-02=VERIFIED
CL-CROSS-05=VERIFIED
CL-CROSS-06=UNVERIFIED
CL-CROSS-07=VERIFIED
CL-CROSS-08=VERIFIED
CL-XCUT-T5-02=VERIFIED
```

## 8. Claim gaps discovered (input for next E2 batch — not queried in this task per brief)

1. A dedicated, non-truncated C1/C6-split doctrine-card query, same recommendation as C1.md §8 item 1
   — this single query would close most of both cards' GAP items at once.
2. C6's specific "social activity" activity-boundary (what qualifies vs C1's tourism/family purposes)
   — currently indistinguishable in prose beyond the pack's generic `OTHER` purpose bucket.
3. C6's onshore-conversion-to-ITAS status (currently unaddressed in either direction, unlike C1/C2).
4. A C6-specific sponsor-procedure claim (mirroring C2's `CL-C2-03`), since C6's own rule shares C2's
   sponsor-mandatory pattern but has no dedicated procedural claim of its own.
5. `CL-CROSS-08`'s Products scope widened for T10, same recommendation as every other card in this
   slice.

## Adversarial review

**Real run** (not simulated): `kimi -p "<full 6-card combined text + refutation instructions>" -m
kimi-code/k3`, one invocation across all 6 cards in this slice (generator≠grader). Ran past the
8-minute timebox producing a substantial partial transcript, killed per this task's own documented
precedent — see A1.md's Adversarial Review section for the full method note. Raw transcript:
`/tmp/e3refs/kimi-review-output.txt` (session-local). Findings below are the ones concretely affecting
C6, re-verified independently against `e2b-batch2-claim-ledger.md` and
`rulepack-prod-007.source.json`:

1. **[P1, CONFIRMED, cured]** First draft's §2 digest computation initially included `CL-XCUT-T5-01` in
   C6's dependency set by mistake (copy-paste from C1's set without checking the exclusion note) —
   re-derived from scratch reading only `CL-XCUT-T5-02` for the T5 topic, matching the ledger's own
   explicit scoping. Digest recomputed and verified against the corrected 8-claim set.
2. **[P1, CONFIRMED, cured]** §6's table originally cited `CL-XCUT-T5-01` alongside `CL-XCUT-T5-02` for
   the rule's duration-consistency backing — removed `CL-XCUT-T5-01` and added the explicit "Important
   scoping note" paragraph so a reader does not repeat the same mistake by pattern-matching against
   C1/C2's own tables in this slice.
3. **[P2, NOT-AN-ISSUE]** Reviewer asked whether C6's exclusion from `sponsor_types: NONE` (unlike C1)
   should be escalated as a conflict — no, it is a straightforward pack-structural difference between
   two distinct products' own rules, not a disagreement between two sources about the SAME fact; no CF
   entry warranted.
4. **[P3, NOT-AN-ISSUE]** Reviewer cross-checked C6 §3.13's "in-kind/barter prohibition applies to
   'tourist/visit visas including C5A,' a category C6 falls within" against A1's and B1's differently
   worded paraphrases of the same CL-CROSS-02 claim — flagged only as a minor cross-card wording
   inconsistency (three cards characterize the same claim's category-membership slightly differently),
   not a defect in C6's own text. No change.
5. **[P3, NOT-AN-ISSUE]** The B211A shared-legacy-code ambiguity that drove the P1 fixes in A1/C1/C2's
   Adversarial Review sections was checked against C6 as well — C6 does share `B211A` as its legacy
   code (see `rulepack-prod-007.source.json`), but C6's own card makes no claim resting on that manual
   listing (unlike C1 §3.7/C2 §5's `CF-16` discussion), so no textual change is needed here; noted for
   completeness only.

Net: 2/5 raised findings were real (both traced to the same root cause — an incautious copy from C1's
already-written card) and are cured above; 3/5 were reviewed and confirmed not applicable.
