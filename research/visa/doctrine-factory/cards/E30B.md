---
date: 2026-08-18
domain: visa
client_case: none
sources:
  - path: research/visa/doctrine-factory/claims/e2b-batch1-claim-ledger.md
    note: "CL-E30B-01 — sibling label mismatch (Kepmen vs operational portal), VERIFIED-WITH-CAVEAT; THIN BACKFILL coverage state noted explicitly"
  - path: research/visa/doctrine-factory/claims/e2b-batch2-claim-ledger.md (fetched @ branch agent/air-m5/ops/e2b-batch2b)
    note: "CL-E30-01 (batch-2b) family-index-structure claim, shared with E30.md — reused here as E30B's own category backing"
  - path: research/visa/doctrine-factory/claims/e2b-batch3-claim-ledger.md (fetched @ branch agent/air-m5/ops/e2b-batch3)
    note: "closure verdict — E30B MET"
  - path: apps/backend-rag/backend/services/visa_engine/contracts/packs/rulepack-prod-007.source.json
    note: "active pack seq-7 (SHADOW) — the 5 el/hf rules scoped to E30B, re-verified live this session (the largest rule footprint of the E30 family)"
adversarial_review: kimi-k3
---

# Product Doctrine Card — E30B (Student KITAS — Higher Education)

Task: E3 bulk (LONG-STAY family). E30B is the higher-education tier of the Student KITAS family — its
own dedicated doctrine-card query timed out in batch-1, leaving it the thinnest-content REACHABLE
product in this specific family (though still claim-backed).

## 1. Identity

- **Code**: E30B
- **Name**: Student KITAS — Higher Education (diploma/sarjana/master/doktor), per `Kepmen
  M.IP-08.GR.01.01/2025`.
- **Category**: Study-purpose, higher-education (*"DIKTI"*) tier — the longest-running validity tier
  in the E30 family (§3.2, per `E30.md`'s cross-referenced `CL-E30-01` batch-1 claim).
- **Naming caveat**: the operational DJI portal labels this product "Student KITAS (Training)" against
  the Kepmen's "Higher Education" — a naming mismatch a client sees on the portal (§3.1).

## 2. `claims_digest`

```
CL-E30-01=VERIFIED-WITH-CAVEAT
CL-E30B-01=VERIFIED-WITH-CAVEAT
```
```
claims_digest: b64786d5271ff6dd9933283170b438d01dfaea8a6aabef8f539a50ae9e647d3e
```

## 3. Doctrinal facts (claim-referenced)

1. **Sibling-label mismatch.** `CL-E30B-01`, **VERIFIED-WITH-CAVEAT** — the Kepmen classification
   labels E30A "Basic/Secondary Education" and E30B "Higher Education"; the operational DJI portal
   database instead labels E30A "Student KITAS (Research)" and E30B "Student KITAS (Training)" — a
   naming mismatch flagged explicitly in the source answer as a "Sibling Mismatch." Caveat, per the
   originating ledger: this claim describes the LABEL mismatch, not E30B's substantive eligibility
   requirements, which were never resolved by a dedicated doctrine-card query in this batch.
2. **Family index structure & validity tiers.** `CL-E30-01` (batch-2b EXTENSION), reused here as
   category backing — E30B is the higher-education tier of the Kepmen-defined Student KITAS family,
   running the longest of the 1/2/4-year validity tiers per batch-1's separate `CL-E30-01` (see
   `E30.md` §3.1/§3.2 for the collision note on this claim_id).
3. **Coverage state — THIN, explicitly self-flagged by the originating ledger.** No dedicated E30B
   doctrine-card query returned an `OK` answer in this task's whole source chain — the only content
   touching E30B is indirect (E30A's sibling-mismatch comparison table, E30's general validity-tier
   figures). This card does not manufacture eligibility-gate content beyond what these two claims
   actually establish.
4. **Eligibility mechanics (live pack).** Same `study.admission_confirmed`/`study.sponsor_confirmed`
   gate as E30/E30A, PLUS a DIKTI-band HARD_FILTER and a mandatory Kemdikbud study-permit rule not
   present on E30/E30A (§4) — meaning the live pack's rule footprint for E30B is actually LARGER than
   its claim-ledger content, an asymmetry worth flagging.

## 4. Rule coverage matrix (seq-7, verified live against `rulepack-prod-007.source.json`)

E30B has 5 PRODUCTS-scoped rules (1 HARD_FILTER + 4 ELIGIBILITY) — the largest footprint in the E30
family.

| rule_id | stage | reason_code | required claim(s) | claim state | rule status |
|---|---|---|---|---|---|
| `hf.e30b-level-band` | HARD_FILTER | `LEVEL_BAND_DIKTI` | `CL-E30-01`/`CL-E30B-01` (framing only — band-test criteria unclaimed, same gap as E30A's DASMEN band) | VERIFIED-WITH-CAVEAT (framing) | EXCLUDES non-DIKTI applicants |
| `el.e30-student-support` | ELIGIBILITY | `PURPOSE_PRODUCT_MATCH` | `CL-E30-01` | VERIFIED-WITH-CAVEAT | SUPPORTED (shared rule) |
| `el.e30-living-cost-2000.e30b` | ELIGIBILITY | `LIVING_COST_USD2000` | `CL-E30-01` (framing) | VERIFIED-WITH-CAVEAT (framing) | SUPPORTED |
| `el.e30-passport-validity.e30b` | ELIGIBILITY | `PASSPORT_VALIDITY_6_MONTHS_REQUIRED` | `CL-E30-01` (framing) | VERIFIED-WITH-CAVEAT (framing) | SUPPORTED |
| `el.e30b-izin-belajar` | ELIGIBILITY | `STUDY_PERMIT_KEMDIKBUD` | **GAP — no claim covers this at all** | UNCLAIMED | SUPPORTED (rule exists; **overstatement corrected this session** — the rule's `when` predicate is byte-identical to `el.e30-student-support`'s, testing only `purposes∩STUDY`+`admission_confirmed`+`sponsor_confirmed`; it does NOT test any study-permit fact. "Kemdikbud" appears only in the `reason_code` string, not in any tested predicate — the rule cannot mechanically verify a Kemdikbud-issued permit, it only labels its SUPPORT outcome with that name) |

**Cross-reference flag, corrected**: `el.e30b-izin-belajar`'s `reason_code` NAMES Kemdikbud, but its
pack content contains nothing substantive about the issuer beyond that enum string — an earlier draft
of this card overstated this as the rule "identifying" or "naming" the issuer in a way that implied
mechanical verification. It does not resolve E30's own family-level `CL-E30-02` authority-gap
(§3 of `E30.md`); the reason_code is, at most, a hint for a future doctrine query, not evidence.

## 5. Open conflicts

None new. §3.1's sibling-mismatch is a LABEL discrepancy, not a substantive conflict.

## 6. Disposition

**REACHABLE_AND_SUPPORTED — coverage gate MET** per `coverage-matrix-after-batch3.json`
(`ALL_TOPICS_ANSWERED`). Both claims are VERIFIED-WITH-CAVEAT; genuinely thin (no dedicated doctrine
card), but not unclaimed — the coverage gate criterion (claim-backed) is satisfied. `el.e30b-izin-
belajar` is the one rule in this card's matrix with NO claim backing at all, flagged explicitly rather
than glossed over.

## 7. Claim gaps discovered (input for next E2 batch)

1. **Priority 1**: a dedicated E30B doctrine-card query — the current backfill is indirect and thin.
2. `el.e30b-izin-belajar`'s Kemdikbud-issuer label is entirely unclaimed AND, per this session's
   adversarial pass, does not mechanically resolve `CL-E30-02`'s family-level authority gap — the
   rule's own `when` predicate tests nothing Kemdikbud-specific. A genuine doctrine query on the
   issuing authority is still needed; the reason_code string is not itself evidence.
3. DIKTI level-band test criteria for `hf.e30b-level-band` — same class of gap as E30A's DASMEN band.

## Adversarial review

Real cross-family review run this session via `kimi -m kimi-code/k3`, batched with E30/E30A/E30E/E30F
(generator≠grader, live-verified against the pack and both ledger sources).

Findings and disposition:

1. **[P2, CONFIRMED, cured]** §3.4/§4/§7.2 overstated `el.e30b-izin-belajar` as "identifying" or
   "naming" Kemdikbud as the study-permit issuer in a way that implied the rule mechanically verifies
   it. Independently confirmed live: the rule's `when` predicate is byte-identical to
   `el.e30-student-support`'s (tests only `purposes∩STUDY`+`admission_confirmed`+`sponsor_confirmed`)
   — Kemdikbud appears ONLY in the `reason_code` string, never in a tested fact. Corrected §4's table
   row and the §7.2 gap description to state this precisely: the reason_code is a hint, not evidence,
   and does not resolve `E30.md`'s own family-level `CL-E30-02` authority gap.
2. **[not contested]** The digest, the §1 sibling-mismatch naming caveat, and the rest of §4's rule
   matrix (5 rules, exact ids/reason_codes/stages) were independently re-verified and confirmed
   accurate.

Net: 1 real finding, cured.
