---
adversarial_review: kimi
---

Ignoring 51 permissions.allow entries from .claude/settings.json: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog, or set projects["/Users/nuzantara"].hasTrustDialogAccepted: true in /Users/nuzantara/.claude-zero-team/.claude.json.
Ho verificato tutte le affermazioni load-bearing contro il repo a HEAD `f3bf426de3`. Scrivo il verdetto finale. (Nota di lingua: questo è un technical review doc che si affianca ai 4 file del panel, tutti in inglese, con header di formato prescritti in inglese — quindi lo redigo in inglese per coerenza col corpus e con la regola "technical doc: English".)

---

## FABLE 5 FINAL VERDICT — **FIX-FIRST**

The synthesis is the correct plan and its spine is verified — I re-ran every load-bearing check on disk myself and the panel did not self-attest its way past me. M1's real mechanism (catch-all proxy exists, break is the auth-floor registry omission), the 3-of-35 fact starvation, the non-existence of a public evaluate endpoint, the 18-field `Decision` vs `MockVisaCard` payload gap, the collector constants, and the NEEDS_INPUT-strips-the-disclaimer bug all reproduce exactly as written. So this is not NO-GO, and it is more than a blocker list — it correctly re-sequences the critical path around the evaluate endpoint. But it is not yet SHIP, because under its own recommended D1(c) the flagship gate G-a quietly degenerates into a re-run of G-b, and three concrete substrate gaps (no synthetic/real marker, undefined `request_category` emission on the new endpoint, and a 3-of-10-category interview) will surface during execution, not before. Adopt the plan, apply the seven deltas below, then it ships.

## ATTACKS ON THE SYNTHESIS — confirmed / partial / refuted + evidence

- **C1 (M1 mechanism) — CONFIRMED.** `apps/mouth/src/app/api/[...path]/route.ts` exports `POST` at line 416 and proxies all `/api/*` to Fly; `public_endpoints.py` contains only `/api/v1/visa-oracle/*` (460–475) and `/api/knowledge/visa` (292) — **no bare `/api/visa/*` entry exists** (grep exit 1). Git: the auth floor landed `57d50a705` **2026-04-25 (#108)**; proxy landed `7c1d23a68` **2025-12-19**. Root cause verified.
- **C5 (SHADOW-on-v1 thin facts) — CONFIRMED.** `shadow.py build_shadow_facts` maps exactly 3 fields (`person.nationalities`, `intent.purposes`, `intent.stay_days` from `duration_months*30`); docstring + code: "the remaining 32 stay `UNKNOWN(NOT_ASKED)`." Budget not passed.
- **P0-3 (evaluate endpoint does not exist) — CONFIRMED.** No router references `ApplicantFacts` or `evaluator.evaluate`; the **only** path into the engine over HTTP is `maybe_spawn_shadow_match`, called solely from `visa_check.py:267` (+ tests). Codex's "other route REFUTED" is correct.
- **C6 (category vocabularies) — CONFIRMED, with a precision the panel muddled.** `Purpose` enum = 8 (7 + `other`); the collector's `REQUIRED_INTERVIEW_CATEGORIES = Purpose − OTHER = 7`, so the gate's "7 categories" **does** map to a real in-code vocabulary — the design-seat's "matches no vocabulary" is overstated. The real defect is different and worse: v2 `CATEGORY_KEYS` = **10** (incl. `business`, `diaspora`, which have **no `Purpose` value**), and `BEHAVIORAL_CATEGORIES = {work, remote, tourism}` — **only 3 of 10 lanes collect facts**; the other 7 route to `HUMAN_REVIEW`.
- **C7 (source-swap understated) — CONFIRMED.** `Decision` has exactly the 18 required fields listed; live `flow.ts:20` imports `evaluate` from `mock-engine`; `EvaluatedCandidate extends MockVisaCard` with `allInclusivePriceIDR`/`timelineDays`/`eligibility` — none on the engine candidate. Adapter layer is real work.
- **P1-2 (NEEDS_INPUT disclaimer) — CONFIRMED and under-rated.** The `result.state !== "NEEDS_INPUT"` guard opens at `OutcomeSheet.tsx:455` and closes at 582; the 4-line disclaimer (575–580) is **inside** it. There is one and only one `NEEDS_INPUT` reference in the file → the state has no body. In engine mode this is a terminal legal-advice screen rendered with **no disclaimer** — Law-2-adjacent, not cosmetic.
- **G-a collector constants — CONFIRMED.** `MIN_DISTINCT_REQUESTS=1000`, `MIN_CONSECUTIVE_DAYS=7`, `MIN_DISTINCT_VISA_CODES=30`; `enforce_ready` hardcoded `False`, G-b `UNMEASURED`.
- **C2/C4 (regulatory dates) — PANEL-ATTESTED, not re-verified here.** The web seat read primary PDFs (dictum KELIMA → 2025-06-01). I did not re-open the sources; per the org regulatory-honesty rule these stay "TO VERIFY at authoring" — which the plan's G-c source-hygiene gate (VERIFIED status + canonical URL + validity periods) already forces on P0-4. Acceptable.
- **DB-attested facts (28 rows, 0 pack rows, last row 2026-04-21, Fly secrets) — RECEIPT OWED.** No DB/Fly access from this session; codex and web couldn't reverify either. The *mechanism* is git-confirmed; the *counts* remain architect-reported.

## CONFLICT ADJUDICATION

**(a) Deadlock — Gemini is WRONG; Codex + design + orchestrator are RIGHT; the DAG encodes the winner (with one missing line).**
Gemini commits a category error: it conflates the v2 **evaluate endpoint** (a headless backend API — audit-only, renders nothing) with the v2 **UI launch** (the ENFORCE-gated rendered product). SHADOW consuming the evaluate endpoint is exactly what STEP-6c already does off `visa_check.py` today — a server-side fire-and-forget audit that returns nothing to a user. Nothing about ENFORCE-gating the UI blocks a headless audit endpoint from existing and being armed. **No deadlock.** The synthesis DAG encodes this correctly (step 1: P0-3 ∥ 4a; step 2: arm SHADOW on the new endpoint; step 3: window). **But** the DAG never names *who calls the endpoint to produce real-fact rows during the window* — and that is the legitimate anxiety underneath Gemini's wrong framing. The clean answer (v2 interview wired via 4a, shadow-POSTing facts while rendering curated output) needs 4a live in production, on a `noindex` prototype with near-zero organic traffic. So Gemini's instinct ("where does the traffic come from") is real; its conclusion ("don't repoint, deadlock") is false. Add the missing traffic-source line (Delta 4) and the winner stands.

**(b) D1 / D2 / D3 — my recommendations below.**

## RECOMMENDATIONS FOR ZERO ON D1 / D2 / D3

**D1 — adopt (c) hybrid, but rename it honestly and make "labeled synthetic" actually implementable.** As written, (c) claims breadth from "gold-persona fleet + labeled synthetic." Two facts on disk break the label: there is **no `is_synthetic` column** in the SHADOW substrate (migration 255 has only `request_fingerprint`, `request_category`, `candidate_summary`, `grounding_summary`), and the live interview collects facts for **only 3 of 10 categories**. So in practice the breadth half of G-a can be satisfied *only* by a synthetic corpus you author — real traffic can never reach 7 categories or most of 30 codes. My recommendation: adopt (c) but split G-a into a **real-volume** sub-gate (report whatever organic arrives; owner-set threshold given the ~7/day reality) and a **synthetic-breadth** sub-gate (30 codes / 7 categories via gold personas, explicitly labeled). Do not let "1,000 distinct real requests" stay ambiguous — and require the labeling column first, or the number is unauditable.

**D2 — adopt full 110-code pack for ENFORCE, but pair it with the interview build-out or it is a half-gate.** A "zero wrong answers" flagship cannot flip with 72% of the catalog as no-path/HUMAN_REVIEW; Gemini is right. But 110 signed codes behind an interview that collects facts for only 3 categories still abstains on 7 lanes. The true ENFORCE condition is **110 codes signed AND behavioral interview trees for every launched category** (Track B FASE 2 is on the critical path to the flip, not a fast-follow). Adopt D2 with that coupling made explicit.

**D3 — adopt the 9 E-gates, but let the session tier them.** The verified NEEDS_INPUT-disclaimer bug is precisely the defect the current four gates wave through — a legal disclaimer missing from a terminal legal-advice state (E-e / Law-2). E-g (handoff PII) is compliance, not polish. So adopt. My one caution: 9 hard ENFORCE blockers is a large surface that can make the flip unreachable; let the session mark each E-gate blocking vs. fast-follow (E-e, E-g, E-a blocking; E-b latency, E-f Lighthouse-95 can be fast-follow) rather than treating all nine as equal.

## THE BLIND SPOT — what everyone missed

**Under D1(c), G-a and G-b collapse into the same test — and the plan's "1,000 real end-user requests" language manufactures confidence the mechanism cannot deliver.** Trace it: the interview collects facts for only 3 of 10 categories (`BEHAVIORAL_CATEGORIES`), and the substrate has no way to separate synthetic from real (no marker column — even though the team already built exactly this pattern in migration 187's `probe-sandbox` isolation flag, and did not reuse it here). So the breadth+grounding dimension of G-a can be satisfied *only* by feeding the evaluate endpoint a gold-persona/synthetic corpus authored by the same side that authored the RulePack. That is definitionally what G-b (gold-persona replay) already measures. The two "independent" gates test the same fixtures. In three weeks this looks obvious: someone runs the window, hits 1,000 distinct fingerprints via synthetic injection, every gate goes green, ENFORCE flips — and the first real user in the `business` or `family` lane (7 uncovered categories) lands on HUMAN_REVIEW, or on a NEEDS_INPUT card with no disclaimer, because real usage was never actually inside the gate. The panel caught an M1 that *looked* dead and was; the equivalent here is a **gate that looks like it measures reality and structurally measures the test corpus.** Secondary, concrete: **`request_category` emission on the new endpoint is undefined** — P0-3's envelope spec omits it, the 10-tile→8-enum mapping is unwritten, and `business`/`diaspora` have no enum home, so they fall to `other`, which the collector *excludes* from breadth. Even the synthetic breadth count silently miscounts until P0-3 emits and maps it.

## DELTAS TO THE PLAN — numbered, minimal

1. **Add `is_synthetic` (or `traffic_source`) to `visa_decisions` (new migration 256)**, following the migration-187 `probe-sandbox` precedent; G-a must report real vs synthetic volume separately. Without it, D1(c)'s "labeled synthetic" is not implementable and the 1,000-count is unauditable.
2. **Split G-a into `G-a-vol` (real traffic, reported, owner-set threshold) and `G-a-breadth` (30 codes / 7 categories, corpus-driven).** State in the criterion text that breadth is synthetic-corpus coverage, not real-usage coverage.
3. **P0-3 spec: emit `request_category` and define the 10-tile→8-enum mapping**; make an explicit `business`/`diaspora` ruling (map→`other`=excluded from breadth, or extend the enum + collector). Currently undefined → silent breadth miscount.
4. **Add one line to the DAG naming the evaluate endpoint's traffic source during the window:** live v2 interview (4a, shadow-POST while rendering curated) = real-fact source; gold-persona/synthetic driver = breadth source. This closes Gemini's legitimate (if mis-framed) concern.
5. **Couple D2 with Track B FASE 2 behavioral trees:** ENFORCE = 110 codes signed **AND** interview fact-collection for every launched category. Pack-completeness alone is a half-gate given `BEHAVIORAL_CATEGORIES = 3`.
6. **Promote P1-2 (NEEDS_INPUT disclaimer, `OutcomeSheet.tsx:455`) out of "spec errata" into the P0-1/telemetry batch.** It is a Law-2-adjacent legal-disclaimer omission on a terminal state; ship the guard fix now, don't wait for the full wiring PR.
7. **Mark all DB/Fly facts "architect-reported, receipt-owed"** (28 rows, 0 pack rows, last row 2026-04-21, secret state) and have the Pro/DB seat re-run the queries before Zero sets the D1 threshold — I could not re-verify them from this session, and neither could codex or web.
fable exit=0

## Adversarial review

Orchestrator adopted all 7 deltas into the synthesis addendum (none rejected); migration-187 probe-sandbox precedent verified on disk; C6 vocabulary refinement recorded. None survived, 0 raised.
