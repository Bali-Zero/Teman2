---
adversarial_review: gemini
---

Ignoring 51 permissions.allow entries from .claude/settings.json: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog, or set projects["/Users/nuzantara"].hasTrustDialogAccepted: true in /Users/nuzantara/.claude-zero-team/.claude.json.
I have everything I need. All claims verified against disk. Let me write the gate report.

## PR #3032 — `agent/air-m5/backend-rag/visa-feed-hotfix`

**VERDICT: SHIP**

Additive-only (158 insertions, 0 deletions, 3 files). Every load-bearing claim checks out against disk. This is the correct, minimal fix for the funnel dead since 2026-04-25.

**Findings (numbered, severity):**

1. **[INFO — verified correct] Exact/template match semantics are right.** `PublicEndpoint.matches()` (public_endpoints.py:52-62) does `path == prefix` for exact, and single-segment `{param}` matching for template (segment-count equality + non-empty segment). The 5 registrations map **1:1** to real mounted routes: `router = APIRouter(prefix="/api/visa")` with `POST /check/start`, `POST /clock`, `POST /match`, `GET /clock/{hash}`, `GET /match/{hash}` (visa_check.py:55,174-326; included at router_registration.py:209). No registered path lacks a route; no route is over-registered.

2. **[INFO — verified correct] No over-broad public opening.** There is **no** `/api/visa/` or `/api/` prefix entry in the registry — grep confirms only the 5 exact/template rows. The innocence set genuinely fails against `matches()`: `/api/visaevil/match`, `/api/visa`, `/api/visa/`, `/api/visa/clockadmin` (exact mismatch), `/api/visa/match/abc/def` (5 segments ≠ 4-part template), `/api/visa/unknown` — all correctly non-public. `_VISA_ORACLE` is spread into `PUBLIC_ENDPOINTS` (line 540).

3. **[INFO — verified correct] Security-gate allowlist is genuinely enforced, not cosmetic.** `test_every_public_mutating_route_is_accounted_for` (test_mutating_routes_are_gated.py:561) walks the **live FastAPI app**, and any public *mutating* route absent from `INTENTIONALLY_PUBLIC_MUTATIONS`/`KNOWN_UNGATED` fails CI. The 3 POSTs are added; the 2 GET result-pages are correctly omitted (not mutations). `test_no_stale_justified_entries` also forces each entry to match a live public route → method+path exactness is machine-checked.

4. **[INFO — verified correct] CSRF-dispatch claim holds.** `validate_csrf()` is reached **only** inside the cookie-JWT branch of `authenticate_request` (hybrid_auth.py:432). `dispatch()` Step 1 (line 196) short-circuits registered-public paths with `call_next` + `X-Auth-Type: public` and returns *before* Step 2 auth. An anonymous POST with no cookie/CSRF header passes — the test exercises the real `dispatch`.

5. **[INFO — verified correct] Rate-limit class claim holds.** `_get_rate_limit` returns the first prefix match; for all 5 paths no earlier prefix matches and they fall through to `/api/` → `(120, 60)`. Asserted exactly by the test.

**Cannot verify from disk:** live prod behavior (that anonymous calls now return 200 instead of 401); Redis-backed rate-limit enforcement under real load; the registry integration test's "every registered path resolves to a mounted route" only runs when the full app imports in CI. Also: the *no-PII-persisted* claim rests on the pre-existing `visa_check.py` handler INSERT bodies, which this diff does **not** touch and which I did not audit here — the PR only flips the routes public.

---

## PR #3033 — `agent/air-m5/mouth/visa-feed-telemetry`

**VERDICT: SHIP**

PII-free telemetry + a correct legal-disclaimer guard fix. Strong guilt/innocence test coverage. One real content issue exists but is pre-existing and explicitly deferred by the author.

**Findings (numbered, severity):**

1. **[INFO — verified correct] No PII in the new event, at the wire level.** The `app_form_submit_failed` type carries only `{type, app, endpoint, status}` (funnel-app.ts:49-57). `emitFunnelAppEvent` POSTs `payload: event` wholesale (line 145), so the wire body is exactly those 4 fields — **no `payload_keys`**, unlike `app_form_submitted`. Call sites pass **literal** endpoint strings (`"/api/visa/clock"`, `"/api/visa/match"`) and an int/null status — never the URL-with-hash, never form values. Triple-tested: funnel-app.test.ts:86 (`Object.keys(payload)` === the 4 keys, status 503 and null), funnel-app-events.test.ts:29-57 (`wire.not.toContain("nationality")`, null on network failure), match/page.test.tsx:62-118.

2. **[INFO — verified correct] Backend `FUNNEL_APP_EVENTS` mirror is necessary, not decorative.** analytics.py:189 rejects any event not in `ALLOWED_EVENTS = FUNNEL_PAGE_EVENTS | FUNNEL_APP_EVENTS` (returns `ok:False`, silently dropped). Without the backend line the failure event would never persist. The pre-existing `test_analytics_funnel_parity.py` regex-parses `APP_EVENTS` from the TS and enforces the mirror **bidirectionally** — CI fails if either side drifts. Both sides are updated; `scrub_pii` is applied to the payload before storage as defense-in-depth (line 192).

3. **[INFO — verified correct] Disclaimer guard fix + EN/ID parity.** On main the whole footer was wrapped in `state !== "NEEDS_INPUT"` (OutcomeSheet.tsx:455) — so NEEDS_INPUT rendered with no WhatsApp handoff, no receipt, no print/copy, **no disclaimer**. The fix narrows the gate to the next-steps `<section>` only; disclaimer/receipt/actions/handoff are now unconditional. All 4 disclaimer keys exist in both EN (i18n.ts:231-237) and ID (508-514); `it.each(ALL_STATES)` asserts the verbatim 4-line disclaimer on all 5 states in **both** languages.

4. **[MEDIUM — pre-existing, author-documented, NOT a regression] Next-steps step 2 dangles on 3 states.** `types.ts` guarantees `visas` is `[]` for every state except `SUPPORTED_CANDIDATES`, and the requirements/"documents" list renders only under `SUPPORTED_CANDIDATES` (OutcomeSheet.tsx:116). Yet next-steps is gated only on `!== NEEDS_INPUT`, so step 2 — EN "Gather the documents listed above" / ID "Siapkan dokumen yang tercantum di atas" — renders on **HUMAN_REVIEW_REQUIRED, NO_SUPPORTED_PATH, TEMPORARILY_UNAVAILABLE** with no list above. The inline comment's rationale ("the candidate checklist exists only when candidates exist") logically implies the gate should be `=== "SUPPORTED_CANDIDATES"`, not `!== "NEEDS_INPUT"`. **However:** this behavior is byte-identical to main (the PR does not change next-steps gating), and the author explicitly flags it in OutcomeSheet.test.tsx:142 ("Visibility on the other three states is pre-existing main behavior, deliberately left untouched by W0b — flagged for content review"). Not a blocker; recommend a follow-up to gate next-steps on `SUPPORTED_CANDIDATES`.

5. **[INFO — verified correct] Silent-death edge case handled.** `status = res.status` is set outside the catch, so a 2xx whose JSON parse throws reports the HTTP status, not `null` (network-failure sentinel). Directly tested at match/page.test.tsx:101.

**Cannot verify from disk:** GA4 `gtag` actual transmission; live analytics DB writes and that `scrub_pii` runs in the prod request path; real-browser rendering of the disclaimer on the 5 states. I audited match/page thoroughly; clock/page.tsx code diff is identical-pattern (verified), but I read clock/page.test.tsx only via diff-stat, not assertion-by-assertion.

---

## GATE SUMMARY

- **PR #3032 — SHIP.** Additive-only public-endpoint registration; exact/template matches correct with no over-broad opening, security-gate allowlist machine-enforced against live routes, CSRF-bypass + `(120,60)` rate-limit claims verified. Only unaudited surface (pre-existing handler INSERTs, not in this diff) noted.
- **PR #3033 — SHIP.** New failure event is PII-free `{type,app,endpoint,status}` at the wire (triple-tested), backend mirror is necessary and CI-parity-guarded, disclaimer now renders on all 5 states with full EN/ID coverage; the one real defect (next-steps step 2 dangling on 3 states) is pre-existing, unchanged, and already flagged by the author for content follow-up.
fable-gate exit=0

## Adversarial review

Gemini R1 pass (2026-07-24): P2 'logging 2xx on JSON-parse failure conflates success with client errors' — disposition: deliberate (status = wire truth; null = network failure); follow-up filed for Track C: add a client_error kind field to the event. Non-blocking. 1 raised, 0 blocking.
