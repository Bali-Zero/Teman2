| Q# | ANSWER | EVIDENCE | RECOMMENDATION | CONFIDENCE |
|----|--------|----------|----------------|------------|
| 1  | **Omni’s persistence claims are a classic FALSE FRIEND until proven for A007 Zantara.** The “character, background, movement persistence across edits” demo covers selfie/claymation snippets, not a multi‑shot narrative with a stylised character. It does **not** magically supersede the current pipeline’s guard layers (which already do not use ArcFace gate, but rely on prompt engineering with Veo 3.1 Fast). | I/O facts: demo only shows simple persistence; no stress‑test with a consistent Indonesian‑language avatars that spans 6+ shots. WR3 state: uses 6×8s ffmpeg concat, no ArcFace; consistency is achieved by careful manual prompting and visual inspection. | Run a side‑by‑side consistency test: generate 3 consecutive Omni Flash clips of Zantara and check drift. Retain the concat‑based pipeline until Omni proves equal or better shot‑to‑shot fidelity. | **Low** |
| 2  | **10s max clip is a hard generation limit, not just a UI restriction.** WR3’s 60s episodes are still buildable by concatenating 6×10s clips – fewer seams than the current 8s chunks. The limit does **no** harm; it may even slightly improve rhythm. | I/O facts state “10s clip max from Gemini app” – no indication of a longer hidden limit. WR3 current: 6×8s concat for 60s; switching to 6×10s keeps the same count, reduces stitching points. | Adjust shot‑planning templates to target exactly 10s per clip. Keep the existing ffmpeg concat script. No need to pause production. | **Medium** (limit could be lifted later via API, but treating it as hard is safe now). |
| 3  | **Flow Agent (free) can likely replace the wr3‑design‑architect orchestrator** for brainstorming, shot breakdown, and batch prompt drafting. It directly clashes with any paid Claude agent contracts that handle the same creative orchestration. | I/O facts: Flow Agent is “FREE for ALL Flow users globally — brainstorm/edit/plan/batch”. WR3’s architect likely transforms episode scripts into per‑shot language and camera instructions; a free in‑Flow agent that can ingest a text summary and output Omni‑ready prompts would undercut external services. | Pilot Flow Agent on one full episode script. If output quality matches the current Claude agent, terminate the paid contract and migrate completely. Monitor for token‑saving benefits. | **High** |
| 4  | **FLOW TOOLS custom workflows allow rebuilding wr3‑shot‑director and pre‑render‑gatekeeper inside the Flow UI, drastically reducing reliance on the undocumented FlowKit extension.** The effort is low (natural‑language workflow definition), but the gatekeeper’s 25w prompt cap logic must be replicated or replaced by Flow’s own safety gates. | Flow Tools: “custom workflows via natural language — all deploy, AI subscribers create”. The current Python‑based director sequences shots and checks token usage; a Flow Tool can call Omni Flash per shot and query credit balance if the API is exposed. However, WR3’s “25w prompt cap (FlowKit safety filter)” is a FlowKit‑specific limit that may not exist natively in Flow. | Prototype a Flow Tool that does: 1) ingest shot list, 2) for each shot call Omni Flash, 3) check against a credit‑balance step (if available). Freeze FlowKit usage once the prototype works. | **Medium** |
| 5  | **Lyria 3 Pro AI music videos are complementary, not competition, for WR3’s episodic content.** WR3 is dialogue‑driven with local TTS; Lyria can supply original background scores and optionally short musical visualisers as episode transitions, boosting production value without threatening the core narrative pipeline. | I/O facts: “Flow Music + Lyria 3 Pro … AI music videos via Omni”. WR3 state: uses Chatterbox local TTS Zantara Bahasa Indonesia; no music video plans. The music‑video feature is designed for song‑visualisation, not 60s story episodes. FALSE FRIEND: “AI music videos” might tempt a pivot, but it’s a different product format. | Integrate Lyria 3 Pro to generate original BGM and maybe 10‑15s transition animations. Do not reposition WR3 as a music‑video series. | **High** |
| 6  | **AI Plus is likely priced ~$9.99/month (50% of Pro). It could be sensible for Bali Zero if it includes Omni Flash access and Flow Tool creation, but the unknown generation quota is the decisive factor.** Downgrading from Pro saves money, yet may throttle output. | Google’s typical AI tiering (Google One AI Premium at $9.99) suggests this. I/O confirms AI Plus tier exists and that Flow Tool creation is for “AI subscribers” (AI Plus included). Bali Zero’s Pro cost $19.99; credits wallet 28360. No quota per tier disclosed. | Wait for official pricing & quota announcement. Test credit consumption per Omni Flash clip on Pro now. If AI Plus offers, say, 200 clips/month and the same workflow‑creation rights, switch after confirming. | **Medium** (pricing is speculative). |
| 7  | **FlowKit Chrome extension is unofficial and undocumented. It will almost certainly break when Omni replaces Veo in the Flow UI.** The extension scrapes/hooks into the current Veo interface; the underlying backend and UI will change radically with Omni. | WR3 state: “Veo 3.1 Fast Tier 1 via FlowKit Chrome ext.” No official support mentioned. I/O: Omni becomes the world model in the Gemini app and Flow UI – meaning the entire generation stack is replaced, not just upgraded. FALSE FRIEND: the assumption that “FlowKit” is a stable tool. | Immediately start migrating all generation to native Flow Tools or the official Flow web UI. Freeze any new episode work that depends on FlowKit. Create a fallback procedure that uses the official Flow UI for Omni Flash generation. | **High** |
| 8  | **PROCEED with Veo 3.1 Fast for current production while parallel‑testing Omni Flash. Do not pause.** A full halt would delay content with no certainty that Omni Standard will be ready or better for WR3’s specific needs at launch. | WR3 state: 6/6 PoC clips already rendered; pipeline is functional. Omni Flash is live but limited to 10s clips; Omni Standard timeline unknown. The 60s concat method works. False friend: “Omni replaces Veo” as an immediate, drop‑in replacement. | Keep rendering episodes 7‑12 with Veo 3.1 Fast. Use 20% of the Pro wallet credits for Omni Flash experiments (character consistency, audio quality). Set a decision gate after episode 12 to switch if Omni proves superior. | **High** |

**FALSE FRIENDS IDENTIFIED (with Bali Zero context)**

- **“Character + background + movement persistence across edits”** – Framed as a solved problem, but demonstrated only on short, simple clips. Bali Zero’s 6‑shot, 60‑s narrative with a distinct avatar like Zantara is a far more brutal consistency test. The promise can easily become a “false friend” that leads to discarding manual stitching too early.  
- **“10s clip max from Gemini app”** – May be mistaken as a UI‑only restriction, encouraging developers to wait for an unofficial API. In WR3’s current set‑up (6×8s ffmpeg concat), the 10s limit is actually irrelevant and harmless; its danger is the false hope of longer hidden generation.  
- **“Flow Agent FREE for all”** – Sounds like a full director‑agent; in reality, its “brainstorm/edit/plan/batch” capabilities may not handle the script‑to‑shot breakdown at the depth of **wr3‑design‑architect**, which also respects the **25w prompt cap (FlowKit safety filter)**.  
- **“AI music videos via Omni”** – Could distract Bali Zero from its core 60s narrative episodes (built with **Chatterbox local TTS Zantara Bahasa Indonesia**) into a different product category that doesn’t serve the same audience.  
- **“AI Plus tier”** – A tantalising lower price point vs. the current **Pro plan $19.99/mo** , but undisclosed quota limits might cripple production, making it a false economy.  
- **“FlowKit Chrome ext”** – The entire current WR3 pipeline rests on this unofficial extension (**Veo 3.1 Fast Tier 1 via FlowKit Chrome ext**). Its familiarity is a false friend that obscures the imminent breakage risk when Flow’s UI shifts from Veo to Omni.

[tokens: 7414]
