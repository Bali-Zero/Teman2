import type { Locale } from "./book-data";

interface TimelineComponentProps {
  locale?: Locale;
}

// Timeline removed — content pending verification
export function TimelineComponent({
  locale: _locale = "en",
}: TimelineComponentProps) {
  return null;
}
