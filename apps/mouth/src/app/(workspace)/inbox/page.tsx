import { InboxTimeline } from "@/components/workspace/InboxTimeline";

export const metadata = {
  title: "Inbox — Kita",
  description: "Omnichannel unified feed for team operators.",
};

export default function InboxPage() {
  return <InboxTimeline />;
}
