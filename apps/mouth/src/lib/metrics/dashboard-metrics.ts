/**
 * Dashboard Metrics (Stub)
 *
 * This is a stub implementation to prevent build errors.
 * Metrics tracking is handled by other systems.
 */

export const dashboardMetrics = {
  endPerformanceMark: (_name: string, _userId: string): number => {
    // No-op: Performance tracking handled elsewhere
    return 0;
  },
  trackPageView: (_userId: string): void => {
    // No-op: Page view tracking handled elsewhere
  },
};
