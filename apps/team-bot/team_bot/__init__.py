"""team-bot — Bot B, the agentic team WhatsApp operator (I DUE BOT MANDATE F4-F7).

Ships lane B3's tool-registry/typed-loop/confirmation units, lane B8's
per-member memory, and lane B9's executor seam (``team_bot.executor`` —
one tool, ``get_required_documents``, wired end to end against a fake
transport; the other nine are ``NOT_IMPLEMENTED`` by design). See README.md.
"""
