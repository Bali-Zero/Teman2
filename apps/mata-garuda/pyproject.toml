[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mata-garuda"
version = "0.1.0"
description = "Mata Garuda — Intelligence Super Hub (OSINT blindato, CLI-only, Lamarckian meta-agent)"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Balizero1987" }]
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
]
foundations = [
    "httpx>=0.27,<1.0",
    "tenacity>=8.2,<10.0",
    "transformers>=4.40,<5.0",
    "torch>=2.1,<3.0",
    "sentencepiece>=0.2,<1.0",
    "scikit-learn>=1.4,<2.0",
    "numpy>=1.26,<3.0",
]
foundations-observability = [
    "traceloop-sdk>=0.20,<1.0",
]

[project.scripts]
mata-garuda = "mata_garuda.cli:main"

[tool.setuptools.packages.find]
include = ["mata_garuda*"]
exclude = ["tests*", "feedback*", "logs*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
