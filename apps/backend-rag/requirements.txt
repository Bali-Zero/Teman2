# ZANTARA RAG Backend Dependencies v5.8 - SECURITY + PERFORMANCE UPDATE
# Last updated: 2026-03-14
# Major upgrades: certifi 2026.2.25, langchain-core 1.2.19, fastmcp 2.1.0, litellm 1.83.7, sse-starlette 3.3.2
# Previous: HTTPX 0.28+ MIGRATION (2025-12-29)

# Core Framework
fastapi>=0.135.2  # Upgraded for starlette 0.50+ compatibility
uvicorn[standard]>=0.40.0  # Upgraded for async improvements
sentry-sdk[fastapi]>=2.0.0  # Error monitoring
pydantic>=2.12.5  # Compatible with MCP 1.25+, google-genai 1.56+
pydantic-settings>=2.5.2  # Compatible with pydantic 2.12+
python-multipart>=0.0.22  # Security fix: arbitrary file write (CVE-2024-47874)

# Vector Database
qdrant-client>=1.12.0  # Upgraded to support urllib3>=2.0.0

# Google Cloud Integration (Core for v5.3 Hybrid) - Use compatible versions
google-genai>=1.56.0  # Unified SDK with MCP support
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-cloud-storage>=2.10.0
google-cloud-aiplatform>=1.40.0  # Now compatible with OTEL (protobuf 5.x)

# Tigris (S3-compatible) — used by scripts/wr2_image_generator.py +
# scripts/wr2_draft_generator.py to upload hero images to the warroom
# bucket. No backend module imports it; pinned here so production venv
# rebuilds (Pro `nuzantara-deploy` worktree) stay self-sufficient.
boto3>=1.43.0

# AI/ML - UPGRADED FOR HTTPX 0.28+ COMPATIBILITY
openai>=2.36.0  # Upgraded for httpx 0.28+ (proxies parameter removed)
anthropic>=0.75.0  # Upgraded for httpx 0.28+ compatibility
# PyTorch - Explicitly specify compatible version to avoid RuntimeError with sentence-transformers
# PyTorch 2.2.0+ required for transformers compatibility (register_pytree_node)
torch>=2.6.0  # Security: RCE via torch.load fixed in 2.6.0 (CVE-2025-32434)
sentence-transformers>=5.3.0  # Updated 2026-03-14: tested compatible, CrossEncoder API stable, sparse embeddings support
huggingface-hub>=0.34.0,<1.0  # Required by sentence-transformers CrossEncoder model loading
tiktoken>=0.5.0  # For accurate token estimation (optional, has fallback)

# PII Detection (UU PDP compliance)
presidio-analyzer>=2.2.362
presidio-anonymizer>=2.2.362

# Document Processing
openpyxl>=3.1.5  # CVE-2023-43515 fixed in 3.1.5
email-validator>=2.2.0
pypdf>=6.8.0  # Security fix: CVE-2026-31826 memory exhaustion
Pillow>=11.0.0  # Updated 2026-03-24: CVE fixes for image parsers (was 10.0.0)
EbookLib>=0.20
beautifulsoup4>=4.14.3  # Security and stability updates
reportlab>=4.2.0  # PDF generation (LKPM ready-pack, invoices); also in -prod.txt

# OCR and Advanced Document Processing (Gemini Vision based)
# Uses PDFVisionService with Gemini-2.0-flash for OCR (same as passport box)
python-magic>=0.4.27  # MIME type detection from file content
# Note: PyMuPDF (fitz) is already included above for PDF processing

# LangChain Ecosystem
langgraph>=1.0.0
langgraph-checkpoint-postgres>=2.0.0  # PostgreSQL checkpointing for stateful workflows
langchain-core>=1.2.19  # Updated 2026-03-14: patch fixes for streaming + callbacks
langchain-anthropic>=1.0.0  # Required for KG LangGraph orchestrator
langchain-openai>=1.0.0  # Required for KG LangGraph orchestrator fallback
langsmith>=0.4.0  # Observability: @traceable on RAG nodes + KG nodes
# langchain-google-genai removed to resolve conflicts

# HTTP/Network - UPGRADED TO RESOLVE CONFLICTS
httpx>=0.28.1,<1.0.0  # Upgraded for google-genai compatibility (requires >=0.28.1)
requests>=2.32.4  # Security: .netrc credential leak fix
aiohttp>=3.13.3  # Security: zip bomb, directory traversal, DoS fixes
urllib3>=2.0.0  # Required by selenium, types-requests
sse-starlette>=3.3.2  # Updated 2026-03-14: SSE disconnect handling improvements

# Web Scraping (Peraturan Spider & Putusan Spider)
playwright>=1.57.0  # Security and browser updates
fake-useragent>=2.2.0  # Updated user agent database
selenium>=4.15.0
webdriver-manager>=4.0.0

# Authentication
PyJWT>=2.12.0  # Security fix: CVE-2026-32597 crit header validation
python-jose[cryptography]>=3.4.0  # CVE-2024-33663, CVE-2024-33664 fixed in 3.4.0
bcrypt>=5.0.0  # Security and performance improvements
passlib[bcrypt]>=1.7.4

# Database
asyncpg>=0.31.0  # Performance and security improvements
psycopg2-binary>=2.9.9
redis>=7.1.0  # Security updates and new features
sqlmodel>=0.0.31  # Bug fixes and compatibility updates
sqlalchemy>=2.0.49

# Compression
brotli-asgi>=1.4.0  # Brotli compression middleware (replaces GZip, ~20% better compression)

# Utilities - UPGRADED VERSIONS
python-dotenv>=1.0.0
# loguru removed - using standard logging instead
pytz>=2024.2  # Required by dateparser
packaging>=23.2,<26  # Compatible with langchain-core if needed

# Development & Quality Tools
pre-commit>=4.5.1  # Security and performance updates
ruff>=0.15.12
pytest>=9.0.3  # Upgraded for better async support
pytest-asyncio>=1.3.0  # Major upgrade for improved async testing
pytest-cov>=7.0.0  # Upgraded for coverage improvements
testcontainers>=4.0.0  # For integration tests with Docker containers

# Observability
prometheus-fastapi-instrumentator>=6.1.0
# OpenTelemetry - CONFLICT RESOLVED (Dec 2025)
# Old versions required protobuf<5.0, new versions require protobuf>=5.0
# Now compatible with google-cloud packages
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.41.1
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0  # For Grafana Cloud (HTTP + auth)
googleapis-common-protos>=1.63.0  # Fix pkg_resources deprecation warning

# Langfuse POC (observability) - kill-switch via LANGFUSE_ENABLED env var
# Added 2026-04-21 in feat/observability-langfuse-poc (see apps/backend-rag/README.md)
langfuse>=2.60.0,<4.0.0  # LLM observability, OTEL-based v2+
openinference-instrumentation-anthropic>=0.1.14  # Auto-trace Anthropic calls
openinference-instrumentation-google-genai>=0.1.16  # Auto-trace google-genai SDK (Gemini)
openinference-instrumentation-openai>=0.1.45  # Auto-trace OpenAI-compat (DeepSeek, Ollama)

# Data Processing
datasets>=2.19.0
pandas>=2.0.0
numpy>=2.0.0,<3.0.0  # Updated for compatibility with scipy/sklearn
psutil>=5.9.0  # Required by metrics module/tests

# PDF Processing - UPGRADED
pdfplumber>=0.11.0
pdfminer.six>=20251107  # Match pdfplumber requirement

# Protocol Buffers - Pin to 5.x (compatible with OTEL + Google packages)
protobuf>=5.27.0,<6.0.0

# XML/HTML Processing - UPGRADED
lxml>=5.3.0  # Required by trafilatura, htmldate

# Agentic RAG Dependencies
pymupdf>=1.23.0  # For PDF rendering in Vision Service
scikit-learn>=1.3.0  # For cosine similarity in Golden Router

# Notification Services
sendgrid>=6.11.0  # For email notifications via SendGrid API
twilio>=9.4.0  # For WhatsApp and SMS notifications via Twilio API; >=9.4.0 fixes CVE-2024-45158/45159/45160
tenacity>=8.2.3  # For retry logic with exponential backoff

# Rate Limiting & Caching (Article Composer Best Practices 2026)
slowapi>=0.1.9  # Rate limiting for FastAPI
redis>=7.1.0  # Already present, used for caching

# MCP (Model Context Protocol)
mcp>=1.25.0  # Upgraded for pydantic 2.12+ compatibility
# fastmcp removed 2026-05-03: not imported by backend-rag runtime. The MCP
# server lives in apps/nuzantara-mcp/ with its own venv (fastmcp 3.2.4).
# Removal closes 8 Dependabot alerts on fastmcp 2.1.0 (1 critical + 4 high
# + 3 medium): SSRF/Path Traversal (GHSA-vv7q-7jx5-f767), OAuth Proxy
# Confused Deputy (GHSA-rww4-4w9c-7733, GHSA-c2jp-c369-7pvx), token reuse
# (GHSA-5h2m-4q8j-pqpj), CVE-2025-66416 (GHSA-rcfx-77hg-w2wv), command
# injection variants, reflected XSS callback page.
python-json-logger>=2.0.0

# Security & HTTP
certifi>=2026.2.25  # Updated 2026-03-14: SSL certificates updated Feb 2026

# LLM Routing
litellm>=1.83.7  # Updated 2026-04-25: Gemini 2.5 + new provider fixes + security fix

# Build trigger 1771636448

# Security: setuptools fixes for CVE-2024-6345, PYSEC-2025-49, PYSEC-2022-43012
setuptools>=78.1.1

# Structured logging
structlog>=24.1.0

# Monorepo workspace package: cell-core (DNA-recording — Genome, Experience,
# Skill, Metabolic, HGT). Editable install; path is resolved relative to the
# working directory where `pip install -r` is invoked, which for this
# requirements file is always `apps/backend-rag/` (CI, Fly build, local venv).
-e ../../packages/cell-core
