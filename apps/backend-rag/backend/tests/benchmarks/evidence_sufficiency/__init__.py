"""
The evidence-sufficiency benchmark (B1.3): a frozen manifest of labelled
cases plus a reporting harness (`harness.py`) that runs the real scorer and
abstain policy against them — never the reverse.
"""
